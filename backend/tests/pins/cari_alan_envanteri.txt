GET /api/auth/verify-email	read	email	H,H,H,H,H
# CS1 CEK/SENET PORTFOYU (goc 20260914_0085). `hesap_no` KESIDECININ banka
# hesabidir ve `alan_maskeleme.MASKELENEN_ALANLAR`a (`maskele_iban`: son dort
# hane) eklendi; yanit `maskele_cari`den gecer. Vektor URETILDI: `payments`
# tasiyan uc rolun ucu de (`yonetici`, `muhasebe`, `satis`) `MASKESIZ_ROLLER`de
# -> F; `depo`/`rapor` `payments` TASIMAZ -> D. Yani maske BAGLI ama bugun
# HICBIR rolde TETIKLENMIYOR (M hucresi dogamiyor).
# `phone` TARAYICININ GENIS TARAFTAKI YANLIS POZITIFIDIR ve bu olculdu: BFS
# `_cek_gorunum -> maskele_cari -> _alan_maskesi` zincirinde `_alan_maskesi`
# docstring'indeki "customer_phone" ornegini goruyor. Yanit modeli
# (`CekSenet`) `phone` TASIMAZ; `customers` rotalari ayni zincirden ayni
# kelimeyi zaten gorur. Tarayicinin sozlesmesi "yanlis pozitif incelenir,
# yanlis negatif sizar" -- incelendi, satira dokunulmadi.
GET /api/cek-senetler	payments	hesap_no,phone	F,F,F,D,D
GET /api/cek-senetler/{evrak_id}	payments	hesap_no,phone	F,F,F,D,D
# H49 DISA AKTARIM MANIFESTI (goc YOK). Tarayici bu rotayi H49'un `email`
# metin sabiti (`user_emails` haritasi: `{"id", "email"}`) yuzunden GORMEYE
# basladi. Harita PLATFORM KULLANICISININ e-postasidir ve YALNIZ firmanin
# disa aktarilan uyelerini kapsar. MUAFLAR'a KONMADI ve bu bilincli: zip
# 5.1a'dan beri kiracinin TAMAMINI, `customers`/`suppliers` satirlarinin
# email/phone/address/tax_number alanlari DAHIL, maskesiz tasiyor -- yani
# "cari degil" demek yanlis olurdu. Maske YOK ama sizinti da YOK: izin
# `__admin_only__`, bes rolun BESI de rotayi goremez (D,D,D,D,D; yalniz
# `admin` jokeri). Maruziyet H49'dan ONCE de vardi; H49 onu gorunur yapti.
GET /api/company/export	__admin_only__	email	D,D,D,D,D
GET /api/customers	read	address,email,phone,tax_number	F,F,F,M,M
GET /api/customers/{customer_id}	read	address,email,phone,tax_number	F,F,F,M,M
GET /api/customers/{customer_id}/statement	sales	address,email,phone,tax_number	F,F,F,D,D
GET /api/customers/{customer_id}/statement.pdf	sales	address,email,phone,tax_number	F,F,F,D,D
# E4a e-IRSALIYE SURETI (goc 20260913_0083, PR #116). MASKELENMEZ ve bu
# gerekce OLCULDU, varsayilmadi: uc `edespatch.build_despatch_xml` ile bir
# UBL-TR e-Irsaliye BELGESI uretiyor ve `_taraf` alicinin VKN/TCKN'sini
# `cac:PartyIdentification/cbc:ID[@schemeID]`, adresini
# `cac:PostalAddress/cbc:StreetName` olarak YAPISAL eleman yaziyor
# (`app/einvoice/edespatch.py:378-394`) -- yani ikisi de mali belgenin
# ZORUNLU parcasidir, gosterim suslemesi degil. Maskelemek gecersiz bir
# belge uretirdi; dahasi CALISMAZDI: maskeli `tax_number` `"*******890"`
# olur (`alan_maskeleme.py:245`) ve `_kimlik_semasi` TAM 10/11 HANE ister,
# yildizli deger VKN de TCKN de sayilmaz ve uc hata verir.
# YENI BIR ACIKLIK SINIFI DEGIL: satir, ZATEN pinli IKI e-Fatura kardesinin
# BIRLESIMIDIR -- `einvoice/download` (sales, address, F,F,F,D,D) ve
# `invoices/{id}/pdf` (sales, tax_number, F,F,F,D,D). Izin de gorunurluk
# vektoru de onlarla BIREBIR AYNI: `depo` ve `rapor` `sales` TASIMADIGI icin
# rotayi HIC goremez (D), kalan uc rol zaten `MASKESIZ_ROLLER`dedir.
GET /api/despatch-notes/{despatch_id}/edespatch/download	sales	address,tax_number	F,F,F,D,D
GET /api/finance/accounts	finance	iban	H,H,H,H,H
GET /api/finance/summary	finance	iban	H,H,H,H,H
GET /api/invoices/{invoice_id}/einvoice/download	sales	address	F,F,F,D,D
GET /api/invoices/{invoice_id}/pdf	sales	tax_number	F,F,F,D,D
# H75 KALICI KATLANMIS ARAMA SUTUNLARI (goc 20260925_0092). Tarayici `email_katli`yi
# `global_search`in SABIT arama metninden okuyor: sutun YALNIZ `WHERE`dedir (musteri
# aramasinin maskesiz dali), SELECT listesinde YOKTUR ve yanita cikmaz; alt yazi
# hala maskeli `phone`/`email`den kurulur. `email_katli` `MASKELENEN_ALANLAR`a
# `email` ile ayni kuralla eklendi, yani vektor DEGISMEDI (F,F,F,M,M). Maskeli
# dal onu HIC eslemez (SEC-3b); davranis `test_h75_katli_sutun_esitligi.py`de.
GET /api/search	read	email,email_katli,phone	F,F,F,M,M
GET /api/suppliers	purchases	address,email,phone,tax_number	F,F,D,M,D
GET /api/suppliers/{supplier_id}	purchases	address,email,phone,tax_number	F,F,D,M,D
GET /api/suppliers/{supplier_id}/statement	purchases	address,email,phone,tax_number	F,F,D,M,D
GET /api/suppliers/{supplier_id}/statement.pdf	purchases	address,email,phone,tax_number	F,F,D,M,D
GET /api/whatsapp/links	users	phone	H,H,H,H,H
# F10-1a TARAF BAGLANTI DEFTERI (goc 20260918_0090). Tarayici `phone`i
# GORUYOR cunku sorgu `whatsapp_party_links.c.phone` sutununu okuyor; YANIT
# o degeri TASIMAZ, yalniz `_maskeli_telefon` ciktisini (`phone_masked`,
# son dort hane) dondurur — bu yuzden bes hucre de S (SUZULUYOR). Izin
# `read` gorunur ama uc KORUMALIDIR: handler `_require_permission` ile
# CUSTOMER tarafinda `sales`, SUPPLIER tarafinda `purchases` ister
# (kesif §7 K4). `GET /api/whatsapp/links` ikizi MUAFLAR'da oldugu icin
# H,H,H,H,H; bu uc MUAF DEGILDIR ve suzulme GERCEKTEN olculdu.
GET /api/whatsapp/party-links	read	phone	S,S,S,S,S
GET /api/work-orders/{work_order_id}/invoice	read	address,tax_number	S,S,S,S,S
