# DEVİR NOTU — 9 Eylül 2026 akşam (yeni hesap için)

Yeni Claude oturumuna yapıştır: **"F:\nazgul\docs\DEVIR-2026-09-09.md oku, şef olarak devam et."** Hafıza yeni hesapta yok; bu dosya tek kaynak. Yol haritası kopyası: `docs/yol-haritasi.html` (tarayıcıda aç).

## Rol ve kural
- Şef kod yazmaz. Ajanlara İngilizce prompt verir — **dosya olarak** `docs/prompts/*.txt` (chat mesajları berkay'a bozuk geliyor); berkay kopyalayıp ajana yapıştırır. Her iddiayı GitHub'dan ölçer (sandbox'ta `git clone --filter=blob:none` + `git ls-remote`; PR sayfaları Chrome ile), lens raporlarını yargılar, "**Merge et: PR #N**" der. **Sadece berkay merge eder.** Tasarım kararları şefte; plan sırası, şema anlamı, PR kapatma, kural gevşetme, altyapı berkay'ın OK'iyle.
- berkay: kısa/samimi Türkçe ("olur", "verr", "tamam"). Cevaplar kısa.
- Kapı (asla gevşemez): 17 zorunlu CI bağlamı (`deploy/ci-required-contexts.json`) + aynı head'de **contract lens GO + runtime lens GO**. Test/doc-only PR → tek lens. Kimse kendi işine lens olmaz; iki lens iki ayrı ajan (Antigravity'nin ikinci oturumu da Antigravity sayılır). Rebase sonrası `git diff base head -- backend/app backend/alembic | git hash-object --stdin` aynıysa GO taşınır. Hayatta kalan mutasyon → aynı dalda düzeltme turu.
- Sıra: `gh pr create` → numara → `python scripts/durum.py --sonraki N` → tek satır `docs/durum/pr-NNNN.md` → `--kapi origin/develop --pr N`. (Ajanlar hep #93 diye tahmin ediyor; numarayı PR açıldıktan sonra al.)
- Pinler envanterdir, ÖLÇÜLÜR (aritmetik değil): rota 391/301, GET 186, TENANT_TABLES 120, core-query 160/171, dinamik SQL 269, alembic head pinleri (eski id'yi grep; 7 dosya), `pins/pg_twins.txt`, `pins/alt_surec_sql.txt`.
- Runner: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4 <files>`; PG ikizleri pytest ile, oturum başına ÖZEL db (`lens<PR>_test`), `postgresql+psycopg://lens:lens@localhost:5433/<db>`, `REQUIRE_PG=1`.
- Makineler: **ana PC** (F:\ sürücü): taşınabilir PostgreSQL 16.4 5433'te var, Docker yok → runtime lens'ler burada. **Diğer PC**: PG yok, Docker/WSL bozuk → contract/SQLite işleri; develop orada 11 test kırmızı (core-query/tenant-scoping, ortam sorunu).
- İzibiz: kimlik `backend/.env.izibiz.local` (git-ignored; ana PC `F:\nazgul\backend\`, diğer PC `...\nazgül\backend\.env.izibiz.local`); ajan `IZIBIZ_ENV_FILE` ile okur, asla yazdırmaz. `IZIBIZ_EFATURA_SUBMIT_VERIFIED=False` kalır. Sandbox test imzalayıcısı hiçbir belgeyi bitirmiyor (herkesinki "İşleniyor").
- Yasaklar: `F:\nazgul`'da çalışmak (ajanlar kendi klon/worktree), `2>/dev/null`, transcript okumak (Antigravity promptlarının ilk satırı), kimlik yazdırmak, force-push (yalnız `--force-with-lease` kendi dalında).

## Ajanlar
- **Claude Code** (yazar + çapraz runtime lens). Haftalık limit 14 Eylül'de sıfırlanır. Bulut/web oturumu İzibiz'e çıkamıyor (403) — sandbox işleri yerel.
- **Antigravity** (`agy`): contract lens + test-only yazar. Kendi klonu: `F:\nazgul-agy-lens`.
- **Codex/Cursor**: ~20 Eylül'de dönüyor. **Grok** yok. Gemini/Spark: yalnız araştırma.

## Durum (9 Eylül akşam)
- develop **be20e07**, **79 merge**, alembic **20260912_0082**, son kayıt pr-0099. **Açık PR yok.**
- Bugün merge: #94 SEC-5 env örnekleri · #95 E2b ölçüm (docs) · #96 SEC-4 açılış göç kapısı · #97 SEC-2 PDF kaçış · #98 E2c e-Arşiv PDF+status alias+NOT_FOUND · #99 SEC-1 eşleştirme kodu hedef telefon (göç 0082).
- Bitti: Faz 1, 1B A–H, 5.1a/b, 5.4a/b/c, WhatsApp WA1–WA5 + website köprüsü, e-belge E1/E2/E2b/E2c, hijyen H1–H11, SEC-1/2/4/5.
- İzibiz gerçeği (E2b/E2c ile ölçüldü): belgeler bizim uuid5 ETTN ile anahtarlanıyor, status `100`=kuyrukta (PENDING alias eklendi), PDF `GetEArchiveInvoiceList CONTENT_TYPE=PDF` ile bayraksız geliyor, iptal 10008 = belge imzalanmadığı için (sandbox imzalamıyor) → NOT_FOUND/tekrar dene. İzibiz'e ikinci mail gereksiz.

## Sıradaki işler (öncelik sırasıyla)
| İş | Kim | Not |
|---|---|---|
| **SEC-3** read izni daraltma (91 GET; depo/rapor bakiye/TCKN/alış fiyatı görüyor) | Claude Code | En büyük SEC; sözleşme sayaçları oynar |
| **E4 e-İrsaliye** (EIrsaliyeWS, göç) | Claude Code, ana PC | 1 Temmuz 2026 zorunlu — öne alındı |
| **E3 e-Müstahsil** (CreditNoteWS/e-MM, göç) | Claude Code, ana PC | aynı |
| SEC-6 login IP limiti + webhook IP limiti | Claude Code | |
| SEC-7 defusedxml (provider.py SOAP, TCMB) + zip gerçek bayt sayacı | Claude Code | |
| SEC-8 cryptography 50 + CORS/CSP/Caddy/compose 127.0.0.1/digest | Antigravity (config) | |
| SEC-9 5 idempotency tablosuna user_id (göç 0083) | Claude Code | |
| SEC-10 SecretStr ×3, verify-email POST, quoteattr, restore sütun kesişimi | Antigravity | |
| SEC-11 `machine.serial_number` max_length yok → PDF LayoutError 500 kalıcı | Antigravity | küçük |
| SEC-4b PG twin AUTO_MIGRATE=false sıfır advisory lock + `maintenance_status` import-time yazma penceresi kararı | Claude Code | küçük |
| H12 `test_self_service_registration.py` zaman-duvarı flake (6 ms) | Antigravity | |
| WA zamanlama oracle (%12–20, yanlış numara vs yok kod) PG'de ölç | lens notu | |
| Yarış testi docstring bayat (`test_wa2_eslestirme_postgresql.py`) | Antigravity | doc |
| `docs/prompts/` .gitignore'a | herhangi | |

Sonra büyük sıra: 5 platform yönetim paneli (2–3 PR) · 6 canlı test (outbox bayrağı kararı) · 7 arayüz (~8 PR) + website v2 (~5) + isim temizliği · 8 Faz 9 (8) · 9 Faz 10 (10) · **10 Faz 11 pazar boşlukları (10 iş, `docs/FAZ-11-adaylar-2026-09-09.md`)** · 11 mobil. **berkay kararı: hepsi mobilden önce.** Kalan ≈56 PR (mobil hariç); 79/≈135.

## SEC listesi (referans)
SEC-1 ✓#99 · SEC-2 ✓#97 · SEC-3 açık · SEC-4 ✓#96 · SEC-5 ✓#94 · SEC-6..11 açık. Temiz bulunanlar: dinamik SQL (230 site), WA metni→SQL, yedekler, export, family revoke/logout-all/çerezler, log hijyeni, genel idempotency, Docker non-root/cap_drop/read_only, HSTS/CSP.

## Küçük kuyruk (eski)
H8d (1B-H ikizi tekrarlanabilir), WA6 (FATURA_TASLAK göçü + gerçek çözücü, altyazılı medya, indirme sınırı, worker canlı), NOT_FOUND ✓, provider-önce sırası davranışsal test, 5.1c geri yükleme, PUSH gerçek teslimat, website `.gitattributes`, `--force-with-lease` izin kuralı.

## Prompt dosyaları (docs/prompts/)
Şablon olarak kullan: `LENS-97-98-99-contract.txt` / `-runtime.txt` (lens), `SEC-1-devam.txt` (push/PR sırası), `E2b-earsiv-status-lookup.md` (ölçüm işi), `ARASTIRMA-eksik-ozellikler-gemini.txt` (araştırma).
