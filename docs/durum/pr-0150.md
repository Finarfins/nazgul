#150 — H49b GERİ YÜKLEMEDE ÜYELİK E-POSTAYLA EŞLEME (backend, göç YOK, uç imzası değişmedi): 5.1c geri yükleme H49 manifestinin `user_emails` haritasını okur — kimliği hedefte olmayan üye, e-postayı taşıyan TAM BİR aktif `app_users` satırına eşlenir (değer `auth.normalize_email` = girişin `strip().lower()`i, `authenticate` de onu kullanır; sütun `lower(email)`); rapor `memberships.mapped_by_email: [{from_id, to_id}]` yalnız kimlik, e-posta yanıta/denetim izlerine/`app_users`a yazılmaz; 0 aktif eşleşme, >1 eşleşme ya da aynı e-postayı iki manifest kimliğinin istemesi → atlanır (`skipped_user_ids`); H49 öncesi arşiv (anahtar yok) → yalnız kimlik; bozuk `user_emails` → 422 `RESTORE_MANIFEST_INVALID`; kapsam `_aranacak_epostalar` (üyelik satırı olup hedefte kimliği olmayanlar ∩ harita), sorgu `_epostayla_esle` (id + lower(email), parçalı IN, boş kümede koşmaz); Core envanteri 233 → 234 (select 158 → 159), parmak izi 494f7b8b → f76df3a2, tarayıcıdan; kiracı kapısı 191 değişmedi; testler SQLite 31 (7 yeni) + PG ikizi 8 (1 yeni) taze PG 16'da 0 atlamayla; mutasyon 9/9 KIRMIZI — <merge tarihi>
