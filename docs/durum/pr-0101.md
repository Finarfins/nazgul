#101 — SEC-11: makine TANIMLAYICILARI artık sütun uzunluğuyla SINIRLI ve sınırsız bir seri numarası bir faturanın PDF'ini KALICI olarak kapatamıyor (GÖÇ YOK — `alembic/` diff'i BOŞ, ROTA YOK, ŞEMA DEĞİŞMEDİ, alembic başı `20260912_0082` KIMILDAMADI); AÇIK ÖLÇÜLDÜ İDDİA EDİLMEDİ: `app/machines.py` `serial_number`ı `String(120)` tanımlıyordu ama `app/schemas.py`de `MachineCreate.serial_number` (ve ondan TÜREYEN `MachineUpdate`) `max_length` TAŞIMIYORDU ve SQLite `String(N)` uzunluğunu ZORLAMAZ — yani sınır İKİ KATMANIN HİÇBİRİNDE durmuyordu, "sütunda yazıyor" olması onu bir sınır YAPMIYORDU; bu depoda `be20e07` üzerinde koşuldu: `POST /api/machines` 5 000 karakterlik `serial_number` ile **201** döndü ve okunan değer 5 000 karakterdi (kırpılmadı, reddedilmedi), sonra o makineyi anan faturada `GET /api/invoices/{id}/pdf` `reportlab.platypus.doctemplate.LayoutError` atarak **500** verdi — `app/invoice_pdf.py`de seri no 60 mm'lik SABİT GENİŞLİKLİ bir hücrede `Paragraph` gövdesine girer ve hücre sayfadan uzun olunca reportlab onu yerleştiremez; KUSURUN AĞIRLIĞI BURADA: veri KAYITLI olduğu için hata GEÇİCİ DEĞİL, o faturanın PDF'i KALICI olarak kapanır ve her istek yeniden 500 döner, yani tek bir kötü kayıt bir belgeyi süresiz erişilemez kılar; PostgreSQL YÜZÜ ARTIK KOŞULLU DEĞİL, ÖLÇÜLDÜ — ölçen ÇALIŞMA ZAMANI MERCEĞİdir (Claude Code runtime, PostgreSQL 16.4) ve ölçüm şudur: `be20e07`de `POST`/`PUT` 121 ya da 5 000 karakterlik `serial_number` ile **500** `psycopg.errors.StringDataRightTruncation ... varying(120)` döner, `675806a`da AYNI gövde **422** döner ve hata `loc=['body','serial_number']` taşır; `manufacturer` 161 karakterle AYNI kalıp `varying(160)` üzerinde tekrarlanır; 120 karakterlik seri numarasıyla PDF **200**; yani iki lehçe İKİ FARKLI 500 veriyordu (SQLite'ta PDF'te `LayoutError`, PostgreSQL'de PDF'e HİÇ VARMADAN INSERT'te kesme hatası) ama İKİSİ DE 422 OLMASI GEREKEN YERDEYDİ; BU YÜZ BU PR'DA ÇİVİLİ DEĞİL ve bu eksiklik SAKLANMIYOR KAYDEDİLİYOR: kapının uçtan uca katmanı bağlantıyı `sqlite:///` olarak SABİT yazar (`tests/test_sec11_makine_tanimlayici_uzunlugu.py` :453), yani PostgreSQL yüzü YALNIZ mercekte ölçüldü, testte PİNLENMEDİ — takip işi SEC-11b dizge tanımlayıcı halini ZATEN `pins/pg_twins.txt`te bulunan `test_machine_workorder_bounds_postgresql.py`ye ekleyecek — aşırı uzun gövde İSTEMCİ hatasıdır ve sunucu çöküşü olarak raporlanması hem yanlış hem de çağıranın düzeltebileceği bilgiyi gizler; ÖNCE ÖLÇÜLDÜ SONRA YAZILDI: `machines` tablosunun HER `String(N)` sütunu ve `MachineCreate` karşılığı tek tek çıkarıldı — `brand` 160 (sınır ZATEN VARDI), `manufacturer` 160 (YOKTU), `model` 160 (VARDI), `variant` 160 (YOKTU), `serial_number` 120 (YOKTU), `chassis_number` 120 (YOKTU), `registration_number` 120 (YOKTU), `engine_number` 120 (YOKTU), `status` 40, `created_at` 40, `updated_at` 40; yani açık YALNIZ `serial_number`da değildi, ALTI isteğe bağlı tanımlayıcının ALTISI DA sınırsızdı ve bulgu tek alan olarak gelmiş olsa da düzeltme kümenin TAMAMINA yapıldı — biri bırakılsaydı aynı 500 başka bir alandan geri gelirdi; ÇARE sınırsız kalan altı alana sütunla BİREBİR AYNI `Field(default=None, max_length=N)`dir ve kalıp İCAT EDİLMEDİ, deponun ZATEN kullandığı kalıptır (`base_unit` `max_length=40`, `lot_code` `max_length=80`): `Field(max_length=...)` + "after" kipli `_clean_optional` doğrulayıcısı; `clean_optional_identifiers` DEĞİŞMEDİ ve bu bir atlama değil ölçülmüş bir karardır — kırpma ve "boş -> None" davranışı aynen duruyor, `Field` kısıtı "after" doğrulayıcıdan ÖNCE koştuğu için sınır HAM uzunluğa bakar ve temizleyici sonra kırpar, bu sıra `base_unit`/`lot_code` ile AYNIDIR yani şema bu dosyada TEK KALIPTA kalır; `MachineUpdate` mirasla kapsanır ve MİRASIN KENDİSİ ÇİVİLENDİ (`issubclass` + alan alan `max_length` eşitliği) çünkü PUT yolu sınırsız kalsaydı açık aynı gövdeyle o kapıdan GERİ GELİRDİ ve POST tarafındaki yeşil testler bunu HİÇ GÖRMEZDİ; İKİ MUAFİYET VAR ve İKİSİ DE SUSARAK DEĞİL ADIYLA ÖLÇÜLÜYOR: `status` serbest metin DEĞİLDİR, `MACHINE_STATUSES` SAYIMIYLA kapalıdır (en uzun üye `maintenance` 11 karakter, sütun 40) ve kapı bunu `max(len(...)) <= 40` olarak ÖLÇER — sayıma 40'tan uzun bir üye eklenirse muafiyet O AN geçersizdir ve test KIRMIZI olur; `created_at`/`updated_at` sunucunun yazdığı alanlardır ve `MachineCreate`te YOKTURlar, kapı bunu da `not in model_fields` ile ölçer, yani biri gövdeye eklenirse muafiyet düşer; `notes` `Text`tir (uzunluksuz) ve KASTEN sınırsızdır — `Text` SQLAlchemy'de `String` TÜREVİDİR ama `length`i `None`dur, süzgeç uzunluklu sütunlara baktığı için `notes` KENDİLİĞİNDEN dışarıda kalır ve bu davranış testte yazılıdır; SINIRIN YETERLİ OLDUĞU DA ÖLÇÜLDÜ, VARSAYILMADI: "sınırladık" demek sınırın DOĞRU YERDE olduğunu söylemez, o yüzden fatura hücresinin kırılma eşiği ikili aramayla ölçüldü — kesintisiz karakterle **1412**, boşluklu metinde **2001**; 120'lik sınır eşiğin ~11 KATI İÇERİDE kalır, yani sınır hem kapatıyor hem de kabul ettiği HER değer için PDF'in yerleştiğini garanti ediyor; bu ölçüm testin gerekçesine yazıldı çünkü sınırı ileride "biraz gevşetmek" isteyen biri eşiğin NEREDE olduğunu bilmeden karar veremesin; KAPI `tests/test_sec11_makine_tanimlayici_uzunlugu.py` (19 test, ÜÇ KATMAN): (1) STATİK — `machines`teki her `String(N)` sütununun `MachineCreate` karşılığında `max_length == N` olduğu, EŞİTLİK aranarak ("var mı" değil, çünkü sütundan BÜYÜK bir sınır aynı 500'ü PostgreSQL'de geri açardı) ve ölçüm tablosunun kendisi `OLCULEN_SUTUNLAR` olarak çivili — `app/machines.py`de bir uzunluğu değiştirmek ya da YENİ bir `String(N)` sütunu eklemek kapıyı KIRMIZI yapar, yani GELECEKTEKİ bir sütun da sessizce sınırsız kalamaz ve bu, bulgunun tek seferlik yamadan çok bir SÖZLEŞMEYE dönüşmesidir; tarayıcı KENDİNİ DOĞRULUYOR (sentetik sınırsız alan, sütundan büyük sınır ve HİÇ OLMAYAN alan yakalanmak, doğru sınır geçirilmek zorunda) çünkü bu olmadan statik kapı her zaman yeşil dönen BOŞ BİR DÖNGÜYE indirgenebilirdi; (2) ŞEMA — her sınırlı alan tek tek parametrik: N karakter KABUL, N+1 karakter `ValidationError` ve hata ALAN DÜZEYİNDE (`loc` o alanı gösteriyor, tür `string_too_long`) çünkü "bir yerde 422 aldık" istemciye HANGİ alanı kısaltacağını söylemez; ayrıca kırpma ve "boş -> None" davranışının DURDUĞU ayrı bir testle çivili — sınır eklemek temizleyiciyi etkisizleştirmemeli; (3) UÇ — gerçek şemada alt süreçte (`app.config.Settings` modül düzeyinde TEK KOPYA olduğu için taze `DATABASE_URL` ancak ayrı süreçte görülür, `test_pdf_paragraf_kacisi.py` ile AYNI gerekçe): her alan için N -> 201 ve N+1 -> 422 (hata `loc`u o alanı taşıyor), ÖLÇÜLEN 5 000'lik gövde artık 422, ve MAX SINIRDAKİ (120) seri numarasıyla makine yaratılıp o makinenin KENDİ anlık görüntüsüyle fatura kurularak `GET /api/invoices/{id}/pdf` -> **200 `application/pdf`** — bu son adım sınırın "doğru yerde" olduğunu ölçen adımdır ve anlık görüntü elle YAZILMAZ, makinenin döndürdüğü değerden kurulur ki sınır tuttuysa PDF'in de tuttuğu ölçülsün; uç smoke'u `scope="module"` bir fixture'la BİR KEZ koşup üç test aynı çıktıyı okur (aynı ağır kurulumu üç kez ödememek için); MUTASYON KOŞULDU İDDİA EDİLMEDİ: `serial_number`dan `max_length=120` düşürüldü ve ALTI test KIRMIZI oldu — `test_KAPI_her_String_sutunu_MachineCreate_te_SINIRLI`, `test_MachineUpdate_SINIRLARI_MIRAS_ALIYOR`, `test_SEMA_sinirda_KABUL_bir_fazlasinda_RED[serial_number-120]`, `test_ISTEGE_BAGLI_TANIMLAYICILAR_hepsi_SINIRLI`, `test_UC_HER_ALAN_sinirinda_201_bir_fazlasinda_422` ve adı önceden verilen `test_UC_121_karakterlik_seri_no_422` (uç 201 döner, yani mutant tam olarak ÖLÇÜLEN açığı geri getirir); mutasyon sırasında COMMIT YAPILMADI ve kaynak `cp` ile geri yazıldı; SAYAÇLAR ÖLÇÜLDÜ VARSAYILMADI ve HİÇBİRİ KIMILDAMADI: rota sözleşmesi 391/301, GET izin envanteri 186, `TENANT_TABLES` 120, alembic başı `20260912_0082` (göç eklenmedi, yedi yerdeki pin dokunulmadı), çekirdek sorgu envanteri ve dinamik-SQL parmak izleri — değişen tek şey bir Pydantic alan kısıtı olduğu için SQL yüzeyi HİÇ değişmedi; `pins/alt_surec_sql.txt`e TEK SATIR eklendi (`tests/test_sec11_makine_tanimlayici_uzunlugu.py	1`) ve bu bir gerileme DEĞİL kapının KENDİ TASARIMIDIR — yeni bir alt-süreç SQL dosyası ADIYLA kırmızı olur (`test_alt_surec_yeni_dosya_adiyla_kirmizi` bunu zaten ölçüyor), dosya sıralı tutuldu; `types.gen.ts` DEĞİŞMEDİ ve bu ÖLÇÜLDÜ: `maxLength` OpenAPI şemasına GİRİYOR (`MachineCreate.serial_number` -> `{"type":"string","maxLength":120}`) ama `openapi-typescript` doğrulama anahtarlarını TS tipine yansıtmaz — üretici hem baz commit'te hem bu dalda koşuldu ve çıktı BAYT BAYT AYNI çıktı, ayrıca depodaki `types.gen.ts` ZATEN sınırlı olan `lot_code`/`base_unit`/`brand` için de SIFIR `maxLength` içeriyor, yani yeniden üretim GEREKMEDİ ve yapılmadı (`backend/openapi.json` `.gitignore`dadır); MEVCUT KAYITLARA DOKUNULMADI ve bu bir eksiklik değil KAPSAM KARARIDIR: düzeltme ÖN KAPIYA konuldu, göç YOK, yani `be20e07` öncesinde yazılmış 120'den uzun bir seri numarası varsa o satır DURUYOR ve onu anan faturanın PDF'i HÂLÂ 500 döner — bunu görünür kılmak için not düşülüyor, temizlik ayrı bir iştir ve bir veri göçü kararı gerektirir (kırpmak mı, işaretlemek mi, elle düzeltmeye bırakmak mı — üçü farklı sorumluluk); KOŞU `python backend/run_isolated_tests.py --workers 4` ile dört dosya: yeni kapı + `test_route_security_contracts` + `test_route_get_permission_inventory` + `test_ci_gates` -> 4/4 dosya, 59 test YEŞİL; ayrıca komşu korumalar koşuldu ve BAZ ÖLÇÜMÜYLE KARŞILAŞTIRILDI: `test_core_query_inventory` 7 kırmızı, `test_core_tenant_scoping_guard` 4 kırmızı, `test_tenant_scoping_guard` 1 kırmızı — BU ÖLÇÜM YAZARIN MAKİNESİNE AİTTİR ve SİLİNMİYOR, NİTELENİYOR: iki mercek AYNI üç dosyayı İKİ BAĞIMSIZ TEMİZ `be20e07` klonunda koştu (biri SQLite, biri PostgreSQL erişimli) ve 49/69/55 GEÇTİ, SIFIR kırmızı; yani kırmızılık DALA DEĞİL yazarın makinesine aittir (başka PC, devir notunda kayıtlı BİLİNEN ORTAM SORUNU) ve bu PR o üç dosyaya DOKUNMUYOR — "develop kırmızı" diye okunmamalıdır; `test_tenant_scoping_guard` bir ara 2 kırmızı göründü ve sebep pin dosyasının güncellenmemiş olmasıydı, eklendikten sonra baz ile TAM PARİTEYE döndü; kayıt adı `python scripts/durum.py --sonraki 101`
