#95 — E2b ÖLÇÜM KOŞUSU (GERÇEK SANDBOX, YEREL MAKİNE, 2026-09-09): §8'İN ANA TEŞHİSİ ÇÜRÜTÜLDÜ ve BU PR'DA `backend/app` ALTINDA HİÇBİR DAVRANIŞ DEĞİŞMEDİ — değişen dört dosyanın üçü belge/test yorumu, dördüncüsü (`backend/app/einvoice/endpoints.py`) YALNIZCA BİR YORUM SATIRINDAKİ TARİHTİR (kod, akış, SQL, imza KIMILDAMADI); ÖLÇÜLEN DÖRT HÜKÜM: (1) H2 — `provider.py:1285`'in kurduğu ÇIPLAK `<UUID>` gövdesi ŞEMAYA UYGUNDUR ve düzeltilecek bir şey YOKTUR, canlı WSDL'den okundu (`EIArchiveWS/EFaturaArchive?wsdl` → `?xsd=5`): `GetEArchiveInvoiceStatusRequest = REQUEST_HEADER + UUID (xsd:string, 1..500)` ve BAŞKA ALAN YOK; alanın tipinin `xsd:string` olması AYRICA ÖLÇÜLDÜ, yani oraya fatura numarası yazmak da şemaya uygundur ve sağlayıcı bunu bir DOĞRULAMA hatasıyla değil bir İŞ SONUCUYLA reddeder; (2) H3 — ANAHTARLAMA ÇÜRÜTÜLDÜ ve İLİŞKİ TERSİNE DÖNDÜ: §8'in "geri bulunamıyor" dediği ÜÇ belgenin ÜÇÜ DE BİZİM uuid5 ETTN'imizle KAYIT DÖNDÜRDÜ (`405acba6-…f2141939`→`SNG2026518354588`, `5266133a-…54169dee`→`SNG2026471382557`, `70dfc021-…081fbcc45`→`SNG2026589807117`, hepsi `STATUS=100 KUYRUĞA EKLENDİ` ve `WEB_KEY` DOLU, 190 hane), AYNI belgeler fatura NUMARASIYLA sorulunca `STATUS=200 FATURA ID BULUNAMADI` verdi ve İzibiz'in KENDİ listelediği `EAR2026000001023` de `200` verdi — yani belge BİZİM ETTN'imizle ANAHTARLANIYOR ve sağlayıcı kendi `INVOICE_ID`'sini o sorgunun YANITINDA veriyor, §8'in yazdığının TAM TERSİ; §7.3'ün "ZAMANLAMA DEĞİL" sonucu da YANLIŞTI çünkü YALNIZ ~100 saniyelik bir pencerede ölçülmüştü ve belgeler BUGÜN HÂLÂ kuyrukta, o yüzden iki `xfail(strict=False)` sandbox testi artık XPASS olabilir ve gözden geçirilmeleri §9.6'da ADIYLA kayda geçti; (3) `PDF_YOK`'UN GERÇEK SEBEBİ BİZİM KENDİ KODUMUZDA YAZILI ve bu ÖLÇÜLDÜ VARSAYILMADI: saklanan `WEB_KEY`'den çıkarılan `webValidationKey` (190 haneli portal URL'inden 80 haneli anahtar) ile `GetEArchiveInvoice` ÇAĞRILDI ve BELGE DÖNDÜ — `INVOICE` alanı 2333 bayt ve sihirli sayısı `PK`, yani bir ZIP; ZIP'in TEK üyesi `SNG2026518354588.xml` (6465 bayt, `<Invoice…` ile başlıyor), yani dönen şey UBL BELGESİDİR PDF DEĞİL ve `_decode_pdf()` onu `%PDF-` taşımadığı için DOĞRU biçimde reddediyor (`b""` → adaptör `PDF_YOK` der); iki gerçek ayrı ayrı ölçüldü: (a) `GetEArchiveInvoice` bir PDF UCU DEĞİLDİR, adının çağrıştırdığı şeyle döndürdüğü şey AYNI DEĞİL, (b) PDF'i sağlayıcı ANCAK GÖNDERİCİ İSTERSE üretir çünkü şemada PDF gönderim isteğinin bir alanıdır (`ArchiveInvoiceExtendedContent/INVOICE_PROPERTIES/PDF_PROPERTIES`: `EARSIV_PDF_FLAG`, `PDF_SIGNATURE_FLAG`, `PDF_NAME`, `EARCHIVE_PDF_XSLT_FILENAME`, `PDF_CONTENT`, `EARCHIVE_PDF_VISUALSIGN_FILE`) ve BİZ `provider.py:1576` ile `<PDF_PROPERTIES><EARSIV_PDF_FLAG>N</EARSIV_PDF_FLAG></PDF_PROPERTIES>` gönderiyoruz (`sandbox/izibiz_smoke.py:465` de AYNI) — yani PDF ÜRETİLMESİN diye AÇIKÇA söyleyip sonra PDF istiyoruz; BAYRAK BU TURDA AÇILMADI ve bu bilinçlidir: `EARSIV_PDF_FLAG=Y` yapmak yeni bir GÖNDERİM davranışıdır, geri alınamaz belge üretir ve `PDF_CONTENT`/XSLT tarafının ne beklediği HENÜZ ÖLÇÜLMEDİ — ölçülmemiş bir bayrağı "zararsızdır" diye açmak §8.3'ün reddettiği şeyin ta kendisi, AYRI BİR DİLİMİN İŞİ; (4) İPTAL `10008` TEK AÇIK KALANDIR ama ARTIK "YANLIŞ ANAHTAR" DİYE OKUNAMAZ: `CancelEArchiveInvoice`, `provider.py:1372-1374`'ün kurduğu gövdeyle BİREBİR tekrarlandı ve `ERROR_CODE=10008 "Belirtilen kritere uygun kayıt bulunamamıştır. Belge ETTN : 405acba6-…"` verdi, oysa AYNI ETTN ile durum sorgusu KAYIT DÖNDÜRÜYOR ve belge İNİYOR, yani anahtar YANLIŞ OLAMAZ; şema `?xsd=5`'ten BİREBİR kaydedildi (`CancelEArsivInvoiceContent` = `UPLOAD_FLAG?`, `FATURA_UUID` ZORUNLU, `FATURA_ID?`, `EARSIV_CANCEL_EMAIL?`, `DELETE_FLAG?`, `IPTAL_TARIHI?`, `TOPLAM_TUTAR?`, `INVOICE_CONTENT?`, `IPTAL_NOTU?`) ve opsiyonel `FATURA_ID` ÖLÇÜLDÜ ÇÜNKÜ ŞEMA ONU TAŞIYOR: AYNI belgeye iki çağrı, TEK FARK `FATURA_ID=SNG2026518354588` ve İKİSİ DE `10008`, hata METNİ de AYNI — yani sorun ANAHTARDA DEĞİL BELGENİN DURUMUNDA (`STATUS=100 KUYRUĞA EKLENDİ`) ve `provider.py:1363-1366`'nın `FATURA_ID` GÖNDERMEME kararı bu ölçümle DOĞRULANDI; şema dışı bir `IPTAL_NEDENI` denendiğinde sağlayıcı `10013 cvc-complex-type.2.4a` ile `TOPLAM_TUTAR INVOICE_CONTENT IPTAL_NOTU` üçlüsünü ADIYLA saydı, hangisinin ön koşul olduğu ÖLÇÜLMEDİ ve VARSAYILMIYOR; §8.2'DEKİ MEKTUP ARTIK YANLIŞTIR ve GÖNDERİLMEMELİDİR — `entegrasyon@izibiz.com.tr`'ye yazılmış metnin 1. ve 2. maddeleri (durum boş dönüyor, belge içeriği gelmiyor) BU KOŞUDA YANLIŞLANDI, geriye sorulacak TEK gerçek soru kuyrukta bekleyen bir e-Arşiv belgesinin neden iptal edilemediği ve iptalin ÖN KOŞULUDUR; §8 BAŞLIĞINDAKİ GELECEK TARİH DÜZELTİLDİ (`2026-09-11`→`2026-09-09`, dört ölçüm yorumunda: `docs/izibiz-sandbox-bulgular.md` §8 başlığı, `backend/app/einvoice/endpoints.py:362`, `backend/sandbox/test_e2_izibiz_iptal_sandbox.py:88,149`, `backend/test_e2_ebelge_yasam_dongusu.py:152`) ama §7.2'DEKİ İKİ `2026-09-11` BİLEREK KORUNDU çünkü onlar bir ÖLÇÜM TARİHİ DEĞİL, sağlayıcının `ERROR_CODE=10003 SCHEMATRON` ile REDDETTİĞİ `cbc:IssueDate` DEĞERİNİN BİREBİR ALINTISIDIR ve düzeltilseydi kayıt sağlayıcının söylemediği bir şeyi söylemiş olurdu; ÖN ÖLÇÜMLER (STOP KAPILARI) GEÇİLDİ: `EIArchiveWS/EFaturaArchive?wsdl` HTTP `200`, `origin/develop` `589c984` (`git ls-remote` ile doğrulandı), kimlik dosyası `IZIBIZ_ENV_FILE` ile YERİNDE gösterildi (KOPYALANMADI, içeriği YAZDIRILMADI) ve `.gitignore:14 **/.env.izibiz.*` ile git-ignore olduğu `git check-ignore -v` ile ÖLÇÜLDÜ, `backend/requirements-dev.txt` kuruldu; PG İKİZİ GEREKMEDİ ve Postgres KURULMADI — bu turda göç yok, şema yok, SQL yok; SIR SIZMADI: kayda giren tek `WEB_KEY` bilgisi UZUNLUĞUDUR (190/80 hane), ETTN ve fatura numaraları ZATEN §8'de yayımlıydı; kayıt adı `python scripts/durum.py --sonraki 95`
