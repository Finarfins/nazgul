#159 — H73/H74/H60/H70 hijyen, GÖÇ YOK / ROTA YOK: (H73) fatura uçlarının `*_at` alanları iki lehçede UTC ISO-8601 — ölçüldü: SQLite boşluklu metin, PG oturum dilimli `+03:00`, `_einvoice_view` `str()`; ortak `app/zaman.py` çıkarıldı ve `despatch_notes._utc` oraya taşındı, frontend `yerelGun` ile YEREL günü gösteriyor, types.gen.ts DEĞİŞMEDİ; (H74) `_belge_numarasi` `func.upper` ve `harvest_scheduling` iki yanlı `UPPER` için eski küçük harfli satır yazan testler, üç mutant elle koşulup öldürüldü (mevcut H53/harvest testleri üçüne karşı yeşil kalıyordu), `search.py` UPPER'ı dokunulmadı; (H60) `GET .../response` satır başına `quantity_mismatch` + `warnings` (YANIT_MIKTAR_UYUSMAZLIGI, iki yönlü), sync yeni yanıtta bir kez log, 4xx ve durum değişikliği YOK, sevk miktarı mevcut `_sevk_satiri_haritasi` okumasından; (H70) test bu cp1254 makinede hiçbir kipte kırmızı üretilemedi, bulunan kusur `yerel_hesap` StreamHandler(stdout) cp1252'de Türkçe satırı `Logging error` ile düşürüyordu — `KodlamayaDayanikliAkis` kaçışla yazıyor. Pinler tarayıcıdan: kiracı kapsamı invoices.py dinamik text() 2'de sabit, iz 8ecf3bf6->5ed11745; Core envanteri 250->250, iz fa4c5e26->214d1cf5. Kanıt: izole suite 485/485 dosya, 4211 sonuç, 0 başarısız; yedi PG 16 ikizi taze veritabanlarında 0 atlama. — <merge tarihi>
