#31 — PARTİ + SKT + FEFO PR 1: parti deposu (göç `20260903_0067`) ile SAF seçici (`app/parti.py`), HİÇBİR ŞEYE BAĞLANMADAN — `backend/app` altında (`app/parti.py` HARİÇ) NE `product_lots` LİTERALİ NE DE `app.parti` İTHALİ vardır ve İKİSİ DE ÖLÇÜLDÜ, iddia edilmedi — iddia bu turda DARALTILDI çünkü ilk hâli ÖLÇÜLEBİLİR biçimde YANLIŞTI: `test_PARTI_MIKTARI_bu_PR_da_HICBIR_YERDEN_guncellenmiyor` yalnız BELGE DİZGİSİ OLMAYAN metin sabitlerinde `product_lots` arıyor (AST üzerinde, çünkü ham `grep` seçicinin KENDİ düzyazısındaki anmayı çağırı sanıyordu — düzyazıda anmak ÇAĞIRMAK DEĞİLDİR) ve bu kapı bir SQL sabitini yakalar ama SEÇİCİYİ PYTHON SEVİYESİNDE ÇAĞIRAN dosyayı YAKALAMAZ: `from app.parti import Parti, fefo_sec` yazıp onu çağıran, tablo adını hiç anmayan yeni bir dosya o kapıdan SESSİZCE GEÇİYORDU ve BÜTÜN TAKIM YEŞİL KALIYORDU (106 passed — ölçüldü, akıl yürütülmedi), bu yüzden İKİNCİ KAPI eklendi (`test_APP_ALTINDA_app_parti_ITHALI_ve_fefo_sec_REFERANSI_YOKTUR`, `Import`/`ImportFrom` düğümlerinde `app.parti`/`.parti`/`parti` ve `Name`/`Attribute` düğümlerinde `fefo_sec` arar) ve AYNI PROBE ARTIK KIRMIZI: iki ihlali de ADIYLA sayıyor (`app/parti_tuketici.py:5 from app.parti import`, `app/parti_tuketici.py:11 fefo_sec`), yalnız YORUMDA/BELGE DİZGİSİNDE anan dosya ise YEŞİL kalıyor — "çağıranı yoktur" cümlesi TEK BAŞINA bir kapı DEĞİLDİ, iki ayrı yoldan ihlal edilebiliyordu ve kapı yalnız birini görüyordu; `app/parti.py` BU TURDA DEĞİŞMEDİ, #27'nin duruşunun aynısı: bir seçicinin SIRALAMASI, onu unutacak bir çağıran ortaya çıkmadan ÖNCE çivilenmelidir; ÖLÇÜLEN KUSUR: stok TEK BİR SAYIDIR (`products.stock NUMERIC(18,4)`) ve o sayının hangi PARTİDEN geldiğini SÖYLEMEZ, `stock_movements` de parti taşımaz — bunun iki sonucu var ve ikisi de BUGÜN GÖRÜNMEZDİR çünkü kimse parti sormuyor: (a) süresi geçmiş mal SESSİZCE çıkar, zira ayırt edecek veri kayıtta yoktur ve ayırt edilemeyen bir şey reddedilemez de, (b) bir parti geri çağrıldığında "o partiden kimlere ne gitti" sorusunun cevabı HİÇBİR YERDE yoktur, ve soran ilk gün GEÇMİŞ VERİ OLMAYACAKTIR — depo bu yüzden sorudan ÖNCE açılıyor; ŞEMA: `product_lots(company_id, product_id, lot_code TEXT NOT NULL, expiry_date DATE NULL, quantity NUMERIC(18,4) NOT NULL DEFAULT 0, created_at)`, `UNIQUE(company_id, product_id, lot_code)` (bir firmanın bir ürünü için bir parti kodu TEK satırdır; iki satır miktarı ikiye böler ve hangisinin gerçek olduğu SORULAMAZDI) ve `(company_id, product_id) -> products(company_id, id)` BİLEŞİK yabancı anahtar yani 0062'nin kuralı, ayrıca `stock_movements.lot_id` NULL kabul eder ve `(company_id, lot_id) -> product_lots(company_id, id)` ile YİNE BİLEŞİK bağlanır (bunun için `product_lots` üzerinde `UNIQUE(company_id, id)` açıldı, çünkü hedefi tekil olmayan bir bileşik anahtar KURULAMAZ) — `lot_id` NULL iken kısıt `MATCH SIMPLE` gereği DENETLENMEZ ve bu tam olarak İSTENEN davranıştır, zira partisi olmayan hareket geçerli bir harekettir ve mevcut satırların HEPSİ öyledir; SAHİP KARARI 2 ŞEMANIN MERKEZİNDEDİR: SKT İSTEĞE BAĞLIDIR ve `expiry_date` NULL kabul eder, çünkü bu depodaki ürünlerin ÇOĞUNUN son kullanma tarihi YOKTUR (yedek parça, makine, alet) ve bir cıvatanın SKT'sinin olmaması EKSİK veri DEĞİL o cıvatanın DOĞRU verisidir — `NOT NULL` yapmak ya uzak bir tarih UYDURTURDU (`9999-12-31`, yani 0066'nın `base_unit = unit` kopyalamasını reddetme gerekçesinin aynısı: ölçülmemiş bir olguyu ölçülmüş gibi kaydetmek) ya da SKT'siz ürünlere parti açtırmazdı ve bu, geri çağırma ihtiyacını SKT'ye BAĞLARDI oysa ikisi AYRI olgudur, bir cıvata partisi de geri çağrılabilir; NULL burada "bu partinin SKT'si YOKTUR" der, "BİLİNMİYOR" DEMEZ ve bu ikinci anlam bu PR'da AÇILMADI çünkü ayırmak ikinci bir sütun ister ve o sütunu bugün kimse dolduramaz — doldurulacak geçmiş veri yoktur; BU KARARIN BEDELİ SIRALAMAYA DÜŞER: FEFO "en erken SKT önce" demektir ve SKT'si olmayan bir parti bu sıraya doğal olarak GİRMEZ, seçici onu EN SONA koyar çünkü tarihi olan parti BOZULABİLİR ve olmayan bozulmaz, bozulabilen önce çıkmalıdır — tersi (NULL'u başa koymak) bozulabilir malı rafta BEKLETİRDİ ve tam da bu göçün düzeltmek için var olduğu kusuru ÜRETİRDİ; NULL-SON KURALI `ORDER BY`A BIRAKILMADI VE GEREKÇESİ ÖLÇÜLDÜ, akıl yürütülmedi: gerçek PostgreSQL 16.14 artan sırada NULL'ları SONA koyar, SQLite ise BAŞA koyar (`SELECT d FROM (...) ORDER BY d ASC` iki motorda ayrı ayrı koşuldu), yani sırayı veritabanına bırakmak İKİ DİYALEKTTE İKİ FARKLI CEVAP verirdi ve bu PR'ın SQLite testleri ile PostgreSQL ikizi o farkı GÖRMEZDİ çünkü her biri KENDİ diyalektinin sırasında yeşil kalırdı — kural bu yüzden tek bir yerde, `_sira_anahtari` içinde duruyor ve dört anahtarlıdır (SKT artan, SKT'siz sona, `created_at` yani aynı SKT'de FIFO, `id` yani toplu içe aktarmanın aynı damgayı bastığı yerde BELİRLENİMCİLİK — kararsız bir seçici geri çağırma kaydını SORGULANAMAZ yapardı); SEÇİCİ SAFTIR VE SAAT OKUMAZ, `bugun` ZORUNLU bir argümandır ve `date.today()` dosyada GEÇMEZ (statik bir test bunu çiviliyor), gerekçe ölçülebilir: takvim okuyan bir SKT testi yazıldığı gün yeşil altı ay sonra kırmızı olurdu ve kırmızılığı KUSURU DEĞİL TAKVİMİ gösterirdi, ayrıca iş günü bu depoda İstanbul'a göre tanımlıdır ve hangi günün "bugün" olduğunu seçmek ÇAĞIRANIN işidir; SÜRESİ GEÇMİŞ PARTİ VARSAYILAN OLARAK SEÇİLMEZ AMA GİZLENMEZ DE — dağıtımın dışında bırakılır ve `Secim.suresi_gecmis` içinde RAPOR EDİLİR, ayrıca `ParticiYetersiz` istisnası onu ÜZERİNDE TAŞIR, çünkü yalnız reddetmek "elde mal YOK" derdi oysa gerçek "elde mal VAR ama süresi geçmiş"tir ve bu iki cümle operatörü TAMAMEN farklı iki işe yollar (satın alma / imha), reddi sessiz bir yokluğa çevirmek bilgiyi red anında YOK EDERDİ; `izin_ver_suresi_gecmis` VARSAYILAN OLARAK `False`tur ve olmaması karardır, varsayılan olsaydı kimse bayrağı yazmadığı için süresi geçmiş mal SESSİZCE çıkardı yani bu göçün ölçtüğü kusurun TA KENDİSİ olurdu; SINIR DARDIR: `expiry_date < bugun`, `<=` DEĞİL, çünkü "son kullanma tarihi" SON KULLANILABİLİR GÜNDÜR ve o günü dışarıda bırakmak kullanılabilir malı bir gün erken imhaya yollardı; `CHECK (quantity >= 0 AND quantity <> 'NaN'::numeric)` diyalekte bağlıdır ve 0066'nın `_katsayi_kisit_metni`sinin birebir eşidir — `>= 0` ÇÜNKÜ `> 0` DEĞİL: TÜKENMİŞ parti SİLİNMEZ, o satır geri çağırmanın KANITIDIR ve silinirse "bu partiden mal girdi ve bitti" cümlesi "bu parti HİÇ OLMADI"ya dönüşür; NaN yarısının gerekçesi 0066'da PAHALIYA öğrenildi ve burada TEKRARLANMADI, UYGULANDI: gerçek PostgreSQL 16.14'te `SELECT 'NaN'::numeric >= 0` -> `t` ölçüldü, yani YALIN kısıt NaN'ı KABUL EDERDİ ve `<>` kullanılıyor çünkü aynı motorda `SELECT 'NaN'::numeric <> 'NaN'::numeric` -> `f` ölçüldü (PostgreSQL'de `NaN = NaN` TRUE'dur); SONLULUK KAPISI KARŞILAŞTIRMADAN ÖNCE gelir ve bu #27'nin dersinin uygulanmasıdır — Python'da `Decimal("NaN") < 0` KARŞILAŞTIRMANIN KENDİSİ `decimal.InvalidOperation` atar (testte ayrıca ölçülüyor), yani denetim `< 0`dan sonra konsaydı NaN ona HİÇ ULAŞAMAZ ve aile DIŞINDA bir istisna sızardı; İKİ AYRI SEBEP AÇILDI ve ayrım ÇARENİN farklı olmasından gelir: `needed` bozuksa `ISTENEN_GECERSIZ` (GİRDİ kusuru, operatör yeniden girer), bir partinin `quantity`si bozuksa `PARTI_MIKTARI_GECERSIZ` (DEFTER kusuru, yeniden girmek onu düzeltmez, birinin DEFTERİ düzeltmesi gerekir); BARKOD AYRIŞTIRILMADI ve bu bilinçlidir: GS1'de GTIN/EAN-13 ÜRÜNÜ tanımlar ÖRNEĞİNİ DEĞİL, parti `AI(10)` ve SKT `AI(17)` YALNIZ GS1-128/DataMatrix taşıyıcılarında bulunur ve EAN-13'te BULUNMAZ, yani mevcut `products.barcode` alanından parti ÇIKARILAMAZ — `lot_code` ve `expiry_date` GİRİLDİĞİ GİBİ saklanıyor ve ayrıştırma yazmak ölçülmemiş bir taşıyıcı varsayımı olurdu; KANIT MUTASYONLADIR ve BEŞ mutasyonun her biri ADI KONMUŞ bir testi kırmızıya çevirdi (NULL-son kuralı bozuldu -> `test_SKT_SIZ_parti_EN_SONA_dusar_NULL_ONE_alinamaz`; süresi geçmiş reddi silindi -> `test_SURESI_GECMIS_parti_VARSAYILAN_olarak_SECILMEZ` ve dört kardeşi; sonluluk kapısı silindi -> `test_SONLU_OLMAYAN_istenen_REDDEDILIR_ISTENEN_GECERSIZ` ve on kardeşi, ama `-Infinity` YEŞİL KALDI ve bu #27'nin ölçtüğü olgunun aynısıdır: `-Inf < 0` TRUE'dur, yani o değer kapı olmadan da doğru kovadaydı; `CHECK` yalın `>= 0`a döndürüldü -> `test_CHECK_kisiti_NaN_MIKTARI_GERCEKTEN_REDDEDER`; `app/parti.py`ye sahte bir SQL sabiti eklendi -> `test_PARTI_MIKTARI_bu_PR_da_HICBIR_YERDEN_guncellenmiyor`), göç `upgrade -> downgrade -> upgrade` İKİ DİYALEKTTE de koşuldu (SQLite ve gerçek PostgreSQL 16.14, ikisi de temiz), 44 birim + 18 ikiz + 28 kiracı nöbetçisi testi yeşil ve `backend/tests` süpürmesi tabandaki YEDİ bilinen kırmızının aynısını verdi (yeni kırmızı YOK); ÇİVİLENMİŞ DELİK: `products.stock` ile `SUM(product_lots.quantity)` AYRIŞABİLİR ve bu PR bunu DÜZELTMİYOR çünkü tüketim yolu YOKTUR (sahip kararı 3: parti miktarları bu PR'da hiçbir yerde güncellenmiyor), tutarlılık kısıtı bugün eklenirse MEVCUT HER ÜRÜNÜ bozardı zira bugün hiçbir ürünün partisi yoktur yani `SUM(lots) = 0 <> stock` HER SATIR için doğrudur ve kısıt ancak partiler stoğun TAMAMINI kapsadığında anlamlıdır ki o gün BAĞLAMA PR'ının içindedir — yokluk `test_stok_ile_parti_toplami_AYRISABILIR_ikinci_katman_YOK` ile 0066'nın `test_products_stock_NaN_KABUL_EDER_ikinci_katman_YOK` testiyle AYNI gerekçeyle çivilendi: kısıt bir gün eklenirse O TEST KIRMIZI OLUR ve bu DOĞRUDUR, o gün orası kısıtı DOĞRULAYAN teste döner, zira çivilenmemiş bir delik sessizce unutulur ama çivilenmiş delik bir KARARDIR; ÇİVİLER: alembic başı üç yerde `20260903_0067`ye alındı, kiracı nöbetçisine `product_lots` eklendi (eklenmeden ÖLÇÜLDÜ ki nöbetçi kırmızı oluyor, yani kayıt SÜS DEĞİL), PG popülasyonu 100 -> 101 oldu ve sayı ÜÇ yerde birden güncellendi (`ci.yml` sabiti, `test_pg_test_population_exact_101`in ADI ve İDDİASI, ve `test_ci_workflow_has_frozen_pg_population_constant`ın ci.yml sabitini arayan iddiası — üçüncüsü göreve verilen listede YOKTU ve ÖLÇÜLEREK bulundu, ilk koşuda kırmızı oldu), taban develop'un 98 glob dosyası `ls` ile ÖLÇÜLDÜ önceki ölçümün üzerine aritmetik yapılarak değil; TABAN TAŞINDI: `caf4114`ten `40921fb`ye, ORADAN DA `3158135`e (her ikisi `--no-ff`, İKİSİ DE ÇAKIŞMASIZ) ve BİLİNEN ÖRTÜŞME ÖLÇÜLDÜ — `.github/workflows/ci.yml`de #29 `container` işine doğrulama adımı eklemişti (~:968), bu PR ise aynı dosyanın PG işine `BEKLENEN_PG_DOSYA_SAYISI=101` çivisini koymuştu (~:482); birleşmeden SONRA dosya İKİSİNİ BİRDEN taşıyor ve ikisi de doğrulandı; 0079'u #29 (`0079-pr-0029.md`) ÖNCE develop'a indirdiği için bu kaydın sırası 0079'dan 0080'e alındı — EN YÜKSEK kayıt artık 0081 (`0081-pr-0032.md`), BU KAYIT 0082'DİR ve numara `scripts/durum.py --sonraki 31` ile ÜRETİLDİ, elle seçilmedi — sıra bu PR'da ÜÇ KEZ kaydı: 0077'yi #30 ve 0078'i #25 aldığında 0079'a, #29 0079'u aldığında 0080'e, ve #34 0080'i #32 de 0081'i aldığında 0082'ye; her seferinde ÜRETİLDİ, önceki sayının üzerine aritmetik YAPILMADI. Son turun tabanı `3158135`tir ve ÖRTÜŞME ÖLÇÜLDÜ, GÖREVDE VERİLEN "örtüşme yok" beyanına GÜVENİLMEDİ: #32 gerçekten değmiyor (yalnız `backend/tests/test_security_audit_visibility.py` + kendi kaydı), AMA aynı pencerede gelen #34 `.github/workflows/ci.yml`ye DOKUNUYOR ve bu PR da aynı dosyaya `BEKLENEN_PG_DOSYA_SAYISI=101` çivisini koyuyor — yani ÖRTÜŞME VARDI. Birleşme yine de ÇAKIŞMASIZDI ve İKİ TARAF DA AYAKTA: çivi `ci.yml:482-483`te duruyor (`test_ci_workflow_has_frozen_pg_population_constant` onu ayrıca arıyor) ve #34'ün eklediği `--degrade-on-unavailable` bloğu da yerinde. #34'ün düzelttiği arıza bu PR'ın ÖLÇTÜĞÜ arızadır: npm kayıt defteri ucu 2026-09-03 ~22:00'den beri zaman aşımına uğruyordu ve `frontend` işi YALNIZ bu yüzden kırmızıydı — kontrol olarak develop `caf4114`ün KENDİ koşusu (33807936220) DEĞİŞMEDEN yeniden koşuldu ve AYNI `AUDIT_TIMEOUT` ile düştü, yani arıza bu PR'a ait DEĞİLDİ; LENS'İN İKİNCİ GÖZLEMİ, ADIYLA VE KOD DEĞİŞTİRİLMEDEN: `fefo_sec`in sonundaki `assert kalan == 0` `python -O` altında TAMAMEN DÜŞER, yani bölüştürme değişmezinin TEK bekçisi optimize edilmiş çalıştırmada YOKTUR — bugün ZARARSIZDIR çünkü o satıra sıfır olmayan bir kalanla ULAŞILAMAZ (yeterlilik `mevcut < istenen` ile YUKARIDA denetlenir ve pay her adımda `min(parti.quantity, kalan)`dır), yani `assert` bir KAPI değil bir İÇ TUTARLILIK BEKÇİSİDİR; kod DEĞİŞTİRİLMEDİ çünkü bu turun sözleşmesi `app/parti.py`nin AST'sinin AYNEN KALMASIDIR ve değiştirmek ölçülmemiş bir davranış farkı üretirdi — bir çağıran geldiği gün bu satır yeniden değerlendirilmelidir; ÖLÇÜLMEDİ, ADIYLA: EŞZAMANLILIK ölçülmedi — iki çıkışın aynı partiyi yarıştırması, kilitleme ya da `SELECT ... FOR UPDATE` bu PR'da YOKTUR ve olmaması yazan yol olmamasındandır; BAŞARIM ölçülmedi — seçici bütün partileri bellekte sıralıyor, sayfalama yoktur ve parti sayısı büyüdüğünde maliyetin ne olacağı BİLİNMİYOR; GERÇEK ÜRETİM VERİSİYLE GÖÇ SÜRESİ ölçülmedi — `product_lots` boş doğuyor ama `stock_movements`a sütun eklemenin üretim boyutundaki maliyeti ölçülmedi; üçü de BAĞLAMA PR'ının kapsamıdır ve burada çağıran olmadığı için bugün ısırmazlar.
