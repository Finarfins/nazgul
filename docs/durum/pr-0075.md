#75 — 1B-F HASAT PARTİ AÇAR, GÖÇ YOK: outbox tüketicisi (`app/field_stok_tuketici.py`) hasadı stok hareketine çevirirken `lot_id`yi NULL bırakıyordu ve ÖLÇÜLEN KUSUR İKİ CÜMLEYDİ — (1) "bu ürünün hangi hasadı sahada" sorusu defterde SORULAMIYORDU, çünkü aynı ürün alışla PARTİLİ giriyor (1B-A) ve defter "bir kısmı partili bir kısmı partisiz" diyordu, (2) satış FEFO'su (`_parti_tuket` yalnız `product_lots`tan seçer) hasat malını HİÇ göremiyordu, yani stok 1000 derken defter 0 diyor ve ikisi hiçbir kırmızı vermeden ayrışıyordu — `Tuketim.defter_bosaldi` bayrağının ADIYLA reddettiği durumun ta kendisi; TEK BİR SÜTUN EKLENMEDİ çünkü gereken her şey vardı: `product_lots` 0067, deposu ve tekilliği `(company_id,product_id,lot_code,warehouse_id)` 0073, `stock_movements.lot_id` 0067, `crop_seasons.product_id` 0062; TABAN `77be889` (`origin/develop`, #72/5.4b birleşmiş hâli) ve `git rev-parse origin/develop` OTURUM BAŞI ANLIK GÖRÜNTÜSÜNE GÜVENİLMEDEN YENİDEN KOŞTURULDU; ÜÇ ALANIN ÜÇÜ DE ÖLÇÜLDÜ VE HİÇBİRİ UYDURULMADI — göç `20260807_0044`ün sütun listesi OKUNDU (id, company_id, season_id, harvested_on, quantity, unit, harvested_area_decare, quality_grade, moisture_percent, notes, status; 0046/0047/0048 yalnız güvenlik ve gelir alanları ekledi) ve `field_harvests`te NE parti/seri NE hedef depo NE son kullanma sütunu VAR: (a) KOD `HASAT-<harvest_id>` biçiminde HASADIN KİMLİĞİNDEN TÜRETİLİR (`_parti_kodu`, önek `PARTI_KODU_ONEKI` sabitinde) çünkü operatöre SORULAMAZ — tüketici bir HTTP isteği değil arka plan döngüsüdür ve soracağı kimse yoktur; (b) DEPO `inventory.default_warehouse`tan, yani HAREKETİN deposundan gelir ve başka bir kaynağı YOKTUR — ayrı bir depo seçmek malı bir depoya sokup partiyi başkasında açardı ve `_parti_ac`ın tekilliği depoyu İÇERDİĞİ için ikisi SESSİZCE ayrışırdı; (c) SKT `SKT_SORULMADI` sentineliyle geçilir, `None` DEĞİL — `None` "SKT'si YOKTUR" BEYANIDIR ve `_parti_ac`ın çatışma denetimini bu yolda ÇALIŞTIRIRDI, sentinel ise hiçbir şey söylemez (ayrım 1B-C'de ölçüldü ve burada BİREBİR geçerli); üçü de çağrı yerinde AST ile çivili (`test_hasat_dali_UC_ALANI_da_ACIK_yaziyor`); SIRA — parti ÖNCE açılır çünkü hareket satırı `lot_id` TAŞIYOR ve satır yazılırken kimlik ELDE olmalı, 1B-B'nin "önce stok sonra parti" kuralı BURADA GEÇERSİZDİR ve gerekçesi ölçüldü: o kural stok korumasının REDDEDEBİLECEĞİ bir ÇIKIŞ içindi, bu ise bir GİRİŞTİR (`yon=+1`, `field_harvests.quantity>0` kısıtlı) ve `adjust_warehouse_stock` burada `allow_negative=True` ile çağrıldığı için reddetmesi MÜMKÜN DEĞİL; SKT'SİZ PARTİ FEFO'DA SONA GİDER ve bu bir yan etki değil bu dilimin DAVRANIŞIDIR — `app/parti.py::_sira_anahtari`in ilk elemanı NULL-SON kuralıdır (tarihi olan partilerin HEPSİ, tarihi ne kadar uzak olursa olsun, tarihi olmayanların ÖNÜNDE gelir) ve uçtan uca ölçüldü: 2098 SKT'li `ALIS-1` ÖNCE tükeniyor, `HASAT-<id>` SONRA, ayrıca sıra İKİ DİYALEKTTE AYRI AYRI soruldu çünkü `_skt` PostgreSQL'de `datetime.date`, SQLite'ta `str` görüyor; KAPSAM SINIRI SESSİZ BIRAKILMADI, ADIYLA ÇİVİLENDİ (`test_PARTI_YOLU_YALNIZ_HASAT_kaynaginda_KAPSAM_SINIRI`) — faaliyet girdisi ve KANTAR FİŞİ FARKI bugünkü davranışlarını AYNEN korur (`lot_id` NULL) ve fiş farkı bir hasadın KENDİ satırını düzelttiği için partiye de yazılabilirmiş gibi görünüyor ama YAZILMADI, çünkü fark EKSİ olabilir (kantar hasadın bildirdiğinden az tartarsa) ve eksi bir farkı partiye uygulamak bir DÜŞME'dir: yetersiz partide 409 üretir, yani bir kantar düzeltmesi olayı `DEAD` kovasına ATABİLİRDİ — o yol ayrı ölçülmeden AÇILMAZ; ÇAĞIRANLAR 5 -> 6 ve bu bir GEVŞEME DEĞİL: tüketici `product_lots`a DOKUNMUYOR, `parti_defteri._parti_ac`ı ÇAĞIRIYOR, `tests/test_1b_a_alis_lot.py::CAGIRANLAR` kapalı kümesi TAM EŞİTLİKLE ölçülüyor ve bu İLK YÖNLENDİRİCİ OLMAYAN çağıran — küme "yönlendiriciler" diye değil "defteri çağıranlar" diye tanımlıydı ve outbox tüketicisi de stok yazıyor, yazdığı satır `lot_id` taşımasaydı kapının savunduğu ayrışmanın kendisi olurdu; MODÜL BAŞLIĞINDAKİ BAYAT MADDE DÜZELTİLDİ — "hasat bugün uygulanamaz çünkü ürüne giden yol yok" (ölçüm c9d3eb1) cümlesi 0062 `crop_seasons.product_id`yi getirdiğinden beri YANLIŞTI, kova (`SKIPPED_NO_PRODUCT`) KALDIRILMADI KAÇINILABİLİR oldu; İDEMPOTENSİ ÜÇ KATMAN VE HİÇBİRİ PARTİ KODU DEĞİL, İLK GEREKÇE ÖLÇÜLDÜ VE DÜZELTİLDİ — "belirlenimci kod tekrar teslimde ikinci partiyi engeller" cümlesi DOĞRU ama SINANAMAZ, çünkü tekrar teslim parti katmanına VARMADAN ÖNCE düşüyor: (1) olay yalnız `PENDING` iken alınır ve talep+hareket+sonlandırma TEK işlemdir, (2) `SENT` olay yeniden kuyruklanamaz, uç 409 `EVENT_ALREADY_SENT` verir (#55), (3) olay ELLE `PENDING`e çekilse bile göç `20260821_0060`ın KISMİ benzersiz indeksi (olay+ürün başına EN FAZLA BİR hareket) hareketi reddeder, `_kurtar` `db.rollback()` yapar ve PARTİ ARTIŞI DA GERİ ALINIR — bu yüzden kodun SINANABİLİR iddiası "BİR HASAT = BİR PARTİ"dir ve İKİ AYRI HASATLA ölçülüyor (kod hasada bağlı olmazsa iki hasat tek satırda birleşir, toplam yine DOĞRUDUR ve kaybolan tek şey "hangi hasat sahada"dır); TESTLER — `backend/tests/test_1b_f_hasat_lot.py` 12 test (4 statik: defteri ÇAĞIRIYOR/doğrudan YAZMIYOR, `_parti_kodu` İKİ YÖNDE belirlenimci, çağrı yerinde üç alan, kapsam sınırı; 4 davranış: parti açılıyor + İKİ HASAT İKİ PARTİ + satış FEFO'su SKT'siz partiyi SONRA tüketiyor, `SENT` yeniden kuyruklanamaz VE uç atlansa bile parti büyümüyor, kiracı ayrımı, BAYRAK KAPALIYKEN hiçbir parti açılmıyor — zamanlayıcı thread'inin YOKLUĞU da ölçülüyor; 4 mutasyon ADIYLA kırmızı: `IKI HASAT TEK PARTIYE DUSTU`, `SKT UYDURULDU`, `HAREKET PARTISIZ YAZILDI`, `PARTI YANLIS DEPODA ACILDI` — hepsi modülü YAMALAYARAK kuruluyor, kaynağı yeniden yazarak DEĞİL) ve `backend/test_1b_f_hasat_lot_postgresql.py` 5 test (yalnız geliştirme diyalektinde GÖRÜNMEYEN yarılar: `expiry_date` gerçek `date` sütununda NULL + `2.5 TON -> 2500.0000 KG` `NUMERIC(18,4)` ölçeğinde TAM, FEFO NULL-son gerçek diyalektte, `fk_product_lots_warehouse_same_company` GERÇEKTEN ısırıyor — SQLite yabancı anahtarları varsayılan olarak UYGULAMAZ, tekrar teslim kısmi indeks+zehirlenmiş işlem+rollback, ve İKİ AYRI SÜREÇ tek olayı yarışırken TEK parti satırı TEK hareket — hareket sayısı TEK BAŞINA yetmez çünkü talep koruması gevşeseydi kaybeden partiyi ikinci kez büyütür, hareketi kısıta çarpardı ve PARTİ ARTIŞI KALIRDI, bu yüzden parti satırının MİKTARI AYRICA sorulur); ONARILAN KOMŞU İKİZ — `test_field_stok_tuketici_postgresql.py::_HASAT_KURULUM` artık `DELETE FROM product_lots WHERE product_id >= 960000` de yapıyor ve bu ÖLÇÜLDÜ VARSAYILMADI: hasat artık parti açtığı için o dosyanın İKİNCİ hasat testinin kurulumu ürünü SİLEMİYOR ve `ForeignKeyViolation` ile ÖLÜYORDU, kusur YALNIZ PG kulvarında görünüyor ve sıra ZORUNLU (`stock_movements` yukarıda zaten siliniyor — parti satırına bakan bir hareket kalsaydı bu silme de düşerdi); ÇİVİLER — `CAGIRANLAR` 5 -> 6 gerekçesiyle, `backend/tests/pins/pg_twins.txt` +1 SIRALI satır (113 -> 114, `test_1b_f_hasat_lot_postgresql.py`; `ci.yml` sayıyı `wc -l` ile TÜRETİYOR, çivilenmiş tamsayı YOK), `backend/tests/pins/alt_surec_sql.txt` +1 SIRALI satır (`tests/test_1b_f_hasat_lot.py	4`, 123 -> 124 dosya / 208 metin ama TOPLAM TÜRETİLİR ÇİVİLENMEZ) ve ADET `_alt_surecte_sql()` ile CPython 3.12'de ÖLÇÜLDÜ, PG ikizi alt süreci YALNIZ yarış probu için açıyor ama gömülü metni SQL FİİLİ taşımadığı için SAYILMIYOR; TÜKETİCİ DİNAMİK-`text()` PARMAK İZİ LİSTESİNDE DEĞİL — ölçüldü, `tests/test_tenant_scoping_guard.py` ve `tests/test_core_tenant_scoping_guard.py` DOKUNULMADAN yeşil (102 test); ÇEKİRDEK SORGU ENVANTERİ, `TENANT_TABLES`, rota/izin envanterleri ve ALEMBİK BAŞI KIMILDAMADI çünkü bu dilim NE göç NE yeni uç getiriyor; SÖZLEŞME DRIFT'İ YOK: tek bir FastAPI rotası ya da Pydantic modeli değişmedi, `frontend/` altında SIFIR dosya; `workflow.py` / `warehouses.py` / `routers/auth.py` / push koduna DOKUNULMADI; KANIT — kanonik `python run_isolated_tests.py --workers 4` (`backend/` içinden) 415 aktif dosyanın 415'ini GEÇTİ, 2378 test sonucu, 1133.6s, çıkış 0; PG 16.14 üzerinde DOSYA BAŞINA TAZE ŞEMA (`DROP SCHEMA public CASCADE`) ile yeni ikiz 5/5 ve AYNI kirli şemada İKİNCİ koşuda yine 5/5 (yani ikiz kendini de tekrar edebiliyor), tüketici ikizi 23/23, 1B-A 9, 1B-B 10, 1B-C 9, 1B-D 1, 1B-E 4, `test_parti_skt_postgresql.py` 17, `test_kantar_fisi_postgresql.py` 16, `test_field_stok_0061_postgresql.py` 2, `test_farm_management_api_postgresql.py` 1, `test_uretici_kayit_defteri_postgresql.py` 1 — hepsi yeşil; 17 bağlam SABİT; kayıt adı `python scripts/durum.py --sonraki 75` ile TÜRETİLDİ (YENİ BİÇİM, `pr-0075.md`).
