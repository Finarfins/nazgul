#71 — DEPO TRANSFERİ PARTİ FARKINDA (FAZ 1B-D): `POST /api/warehouses/transfers` kalemi isteğe bağlı `lot_code` alıyor ve ADIYLA verilen parti KAYNAKTA `_parti_dus` ile düşülüp HEDEFTE `_parti_ac` ile KAYNAĞIN SKT'Sİ KOPYALANARAK açılıyor, her iki hareket (`transfer_out`/`transfer_in`) kendi `lot_id`sini yazıyor, `lot_code` VERİLMEZSE davranış BİREBİR eskisi (iki hareket, `lot_id` NULL); GÖÇ YOK ve bu VARSAYILMADI ÖLÇÜLDÜ — dalın `fcdaf8f..HEAD` dosya listesinde `alembic` altında TEK dosya yok, çünkü `stock_movements.lot_id` göç `20260903_0067`ten, `product_lots.warehouse_id` ile `(company_id, product_id, lot_code, warehouse_id)` tekilliği göç `20260908_0073`ten beri veritabanında ZATEN VAR; TABAN `eb3a0c2` (`origin/develop`) ve `git rev-parse origin/develop` YENİDEN koşturuldu çünkü oturum başı anlık görüntüsü bayattır; DAL `fcdaf8f`ten `eb3a0c2`ye YENİDEN OTURTULDU ve ARADAKİ BEŞ BİRLEŞME (#66 #68 #65 #67 #69) ÇATIŞMASIZ geçti — bu VARSAYILMADI, DOSYA LİSTESİ ÖLÇÜLDÜ: değişen on iki yolun tamamı TEST/BELGE tarafındadır (`run_isolated_tests.py`, beş test dosyası, `docs/NUMARALAMA.md` ve beş durum kaydı) ve `app/` altında TEK satır yok, bu yüzden ÜRETİM ÖLÇÜMLERİ TAŞINMADI ama yine de YENİDEN yapıldı; KAYIT NUMARASI `0108 -> 0113`tür çünkü aynı beş birleşme `0108`–`0112`yi ALDI ve numaralandırma TABANLA BİTİŞİK olmak zorundadır — devralınan uç `be09157` özgün tabandan TEK commit ileriydi, `8ca803a` onun üstüne bindi; DEFTERİ ÇAĞIRANLAR KAPALI KÜMESİ `4 -> 5` ve BEŞİ DE ADIYLA ÖLÇÜLDÜ (`app/` altında `parti_defteri`yi ithal eden dosyalar, AST ile sayıldı: `app/routers/products.py`, `app/routers/transactions.py`, `app/routers/warehouse_counts.py`, `app/routers/warehouses.py`, `app/routers/workflow.py`) — bu dilimin kümeye KATTIĞI TEK dosya `warehouses.py`dir ve o `product_lots`a DOĞRUDAN YAZMAZ, yalnız defteri çağırır; YAZICININ TEKLİĞİ KIMILDAMADI (`test_IKINCI_YAZICI_parti_defterini_ATLAYAMAZ` hâlâ `['app/parti_defteri.py']` ölçüyor) ve ÜÇÜNCÜ EKSENİN (çağıranlar) ayrı durması TAM DA BU TURDA KARŞILIĞINI VERDİ: `warehouses.py` tablo ADINI HİÇ anmıyor, yalnız fonksiyon çağırıyor, yani ilk iki eksen kalsaydı yeni bir yönlendiricinin deftere bağlanması HİÇBİR kapıyı kırmazdı; DEFTERE KATILAN TEK ŞEY `PartiSatiri`nin ÜÇÜNCÜ ALANIDIR (`expiry_date`) ve üçünün TEK okumada gelmesi KARARDIR — transfer SKT'yi kimlikten AYRI okusaydı iki okuma arasında satır değişebilir ve hedefte KOPYALANAN tarih kaynağın o anki tarihi OLMAYABİLİRDİ; karşılaştırma METİN üzerindedir çünkü `expiry_date` PostgreSQL'de `date`, SQLite'ta `str` döner (0073 dilimi bunu zaten ölçmüştü, burada YENİDEN kullanıldı, YENİDEN TÜRETİLMEDİ); HEDEFTE SKT ÇATIŞMASI 422 `LOT_SKT_CELISKI`dir SESSİZ KABUL DEĞİL — 0073 depo başına AYRI satıra izin verdiği için hedefte AYNI kodun BAŞKA tarihli bir partisi TEMSİL EDİLEBİLİRDİR ve iki tarihten hangisinin doğru olduğunu depo BİLEMEZ; PARTİ 409'U DEPO 409'UNU GİZLEMESİN DİYE ERKEN, YAZMAYAN BİR RED VAR (`kaynak_parti.quantity < item.quantity`) ama YARIŞTA YETEN SATIRIN TEK OTORİTESİ O DEĞİLDİR, depo kilidinden SONRA bir kez koşan korumalı `_parti_dus`tur (koruma yazmanın KENDİ `WHERE`ünde) — bu ayrım SQLite'ın tek-yazar modelinde ÜRETİLEMEZ ve ikizin var olma sebebi budur; PG İKİZİ DEVRALINIRKEN ÖLÇÜLMEMİŞTİ, ÖLÇÜLDÜ VE BİR KUSUR ÇIKARDI: `_BEHAVIOUR` girişi `admin123`e ÇİVİLİYDİ ve çıkışta şifreyi `TransferLot!123` BIRAKIYORDU, yani AYNI veritabanına ikinci koşu `AssertionError: (401, 'Kullanıcı adı veya şifre hatalı')` ile düşüyordu — iddia değil, ÖLÇÜLDÜ; CI'da PostgreSQL ikizleri AYNI veritabanını paylaştığı için tuzak İKİ YÖNLÜYDÜ (ikiz hem SONRAKİ dosyaları zehirliyor hem ÖNCEKİ bir dosya şifreyi çekmişse KENDİSİ düşüyor) ve SIRAYA BAĞLI, SESSİZ bir kırmızıydı; ÇARE UYDURULMADI 1B-B İKİZİNDEN DEVRALINDI (`_acilisa_cek`) ve İKİ UÇTAN çağrılıyor çünkü tek yönlü bir çare (yalnız teardown) dosyayı iyi bir KOMŞU yapar ama KENDİSİNİ korumaz; ikinci bir kusur çareyle BİRLİKTE doğdu ve o da ölçüldü — `to_regclass` PostgreSQL'e ÖZGÜDÜR ve kanonik koşucu modülü SQLite altında da TOPLADIĞI için fixture ATLAMA yerine KURULUM HATASI veriyordu, kapı `_postgres_url()` ATLAMASINI ÖNE alıp diyalekt denetimi ekleyerek kapandı; İKİZ ÖLÇÜMÜ (yerel PG 16, taze veritabanı, `alembic upgrade head`, `REQUIRE_PG=1`): x20 YARIŞ GERÇEKTEN KOŞTU ve 20/20 turda TAM BİR `201` ve BİR `409` verdi, 409'un kodu her turda `LOT_MIKTARI_EKSIYE_DUSER`; ikiz arka arkaya İKİ kez yeşil (önce ikinci koşu 401), ÜÇÜNCÜ bir şifreyle zehirlenmiş veritabanında yeşil (giriş ucu), çıkışta `admin123` + `must_change_password=true` geri yazılmış (çıkış ucu), PG env'i olmayan ağaçta TEMİZ `skip`; BU DİLİMİN İKİ DOSYASI İKİ AYRI ŞERİTTE KOŞAR ve bu ÖLÇÜLDÜ — `run_isolated_tests.py` her alt sürece `DATABASE_URL`i KENDİ çalışma alanındaki SQLite dosyasına KOŞULSUZ yazar (`_subprocess_environment`), yani kanonik koşucu bir PG ikizini PostgreSQL'e HİÇBİR ZAMAN bağlayamaz ve `REQUIRE_PG=1` ile birlikte çağrılırsa ikiz de SQLite davranış dosyası da KURULUM HATASI verir; CI zaten böyle yapmıyor: `backend-postgresql` işi `REQUIRE_PG=1` + dışa aktarılmış `DATABASE_URL` ile dosya başına ŞEMA SIFIRLAYIP DOĞRUDAN `python -m pytest` koşuyor ve YALNIZ `test_*postgresql*.py` ile iki özel dosyayı alıyor, davranış dosyası ise `backend-quality` şeridinde `REQUIRE_PG` KAPALI ve SQLite üzerinde koşuyor; ÖLÇÜM DE ŞERİTLERİNE GÖRE YAPILDI (ikiz: taze şema + `pytest` -> 1 geçti; davranış: `run_isolated_tests.py` -> 7 geçti); İĞNELER `fcdaf8f` ÜZERİNDE SIFIRDAN ÖLÇÜLDÜ ARİTMETİKLE DEVRALINMADI — PG TEST POPÜLASYONU `110 -> 111` ve sayım YENİ TABANDA YENİDEN yapıldı, ÖNCEKİ TURDAN TAŞINMADI: `git ls-tree -r origin/develop` -> 108 `postgresql`-adlı, artı 2 özel = 110 (beş birleşmenin İKİZ EKLEMEDİĞİ böylece ÖLÇÜLDÜ, varsayılmadı), dalda `ls backend/test_*postgresql*.py | wc -l` -> 109, artı 2 özel = 111, ÜÇ PİN BİRLİKTE hareket etti (`ci.yml` sabiti `BEKLENEN_PG_DOSYA_SAYISI=111`, `test_pg_test_population_exact_111` ADI, `assert len(all_files) == 111`) ve ÜÇÜNCÜ ÇİVİ — `ci.yml` metnini ARAYAN kapı — UNUTULMADI, bu üçlünün en kolay kaçırılanı odur; ALT SÜREÇ SQL YÜZEYİ `119/200 -> 121/202` ve ölçüm `_alt_surecte_sql()` DOĞRUDAN koşturularak yapıldı (ölçülen 121/202, pinlenen 121/202 — birebir), artışın tamamı TEST tarafındadır (SQLite davranış dosyası + PG ikizi birer gömülü SQL metni ekliyor), `app/` üretim kodu alt sürece SQL VERMİYOR; `TOTAL_CORE_QUERIES` **145**te KIMILDAMADI ve `warehouses.py` DİNAMİK SQL YÜZEYİ **0**da kaldı (AST ile ölçüldü: sabit-olmayan ilk argümanlı `text()` çağrısı YOK), dosyanın AST parmak izi `9796b386…fd04` ve bu turda ÜRETİM KODU HİÇ DEĞİŞMEDİ — devralma yalnız İKİZİ düzeltti; KONTRAT ZİNCİRİ yeniden üretildi ve fark YOK: `python backend/export_openapi.py` + `openapi-typescript` sonrası `frontend/src/api/types.gen.ts` commit'lenmiş hâliyle BAYT-BİREBİR aynı (665320 bayt), yani `StockTransferItem.lot_code` eklemesi zaten senkrondu; `lot_code` SINIRI DÖRT GİRİŞTE DE 80'dir (göç sabiti + `schemas.py`de 3 alan + sayım) ve dördüncü giriş bu dilimle geldi; KANONİK KOŞU YENİ TABANDA YENİDEN ÖLÇÜLDÜ (eski turun sayısı TAŞINMADI, çünkü develop'ın beş birleşmesi tam da `run_isolated_tests.py`e ve dört test dosyasına dokunuyordu, yani devralınan sayı BAYATTI) — `python run_isolated_tests.py --workers 4`: TÜM 409 aktif test dosyası geçti, 2313 test sonucu (2311 -> 2313), 755.7s; TRANSFERİN GERİ ALMA / TELAFİ YOLU ÖLÇÜLMEDİ ve sebebi BÖYLE BİR YOLUN OLMAMASIDIR, ihmal DEĞİL: `warehouses.py` transferler için `DELETE` ucu AÇMIYOR (rota listesi ölçüldü: `GET ''`, `POST ''`, `GET /stock`, `GET /replenishment`, `POST /critical-stock`, `POST /transfers`, `GET /transfers`, `GET /{warehouse_id}`) ve `app/` altında `stock_transfers` üzerinde silme yapan BAŞKA bir yazıcı da yok — bir gün transfer iptali gelirse partinin telafisi O DİLİMDE ölçülmelidir, bugün sessizce doğru olduğu VARSAYILMIYOR, YOK; TOPLU STOK ve EXCEL İÇE AKTARMA partiyi hâlâ GÖRMÜYOR (1B-C'nin kapsam kararı, bu turda değişmedi) ve `lot_id` o yollarda NULL kalıyor.
