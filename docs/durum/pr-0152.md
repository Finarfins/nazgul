#152 — H46 `PUT /api/payments/{id}` ÇEK/SENET KİLİDİ (backend, göç YOK, yeni rota YOK): ölçülen (base `bf8e73f`) — bağlı çek ödemesinde HER PUT (yalnız not dahil) 409 `CEK_BAGLI_ODEME`, nakit→çek PUT 200 ve evraksız YETİM çek ödemesi, evraksız çek→nakit 200; şef kararı uygulandı — çek/senet yöntemine GİRİŞ/ÇIKIŞ (çek↔senet dahil) 409 `CEK_YONTEM_DEGISTIRILEMEZ` (yol sil + yeniden gir), çek/senet ödemesinde YALNIZ `note` yazılır (tahsisli ödemede de; finans/evrak/tahsis dokunulmaz), `entity_type/entity_id/amount/payment_date/account_id/reference_*` ya da gönderilen `cek_senet` bağlı evraktan farklıysa 422 `CEK_ALANI_DEGISTIRILEMEZ`; `cek_senet` PUT'ta POST'un kapısından (`_cek_bilgisi`, `cek_zorunlu=False`) geçer; `CEK_BAGLI_ODEME` DELETE'te kalır; idempotens (5.4b) değişmedi; `odemenin_evraki` `odemenin_evrak_satiri`ye indi (tek sorgu); Core envanteri 233→233 `494f7b8b→a16e155f`, `finance.py` dinamik `text()` 7→7 `56867455→3cca3886` (tarayıcı çıktısından); CS2 SQLite 33/33 ileri+ters, dört mutasyon kırmızı, PG ikizi CS2 9/9 + CS1 16/16 taze db 0 skip (base `finance.py` ile kırmızı); açık: `Payments.tsx:70` yorumu eski 409'u anlatıyor.
