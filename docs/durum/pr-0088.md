#88 — 1B-H: LOT-SUZ YAZICILAR KAPANDI (GÖÇ YOK, ROTA YOK); ÖLÇÜLEN EKSİK: `docs/PARTI_MUTABAKAT.md` §4a BEŞ yazıcıyı ADIYLA sayıp KAPSAM DIŞINDA bırakmıştı (`imports.py:352` Excel güncelleme dalı, `imports.py:360` Excel ekleme dalı, `products.create` açılış stoku, `products.update_product` stok alanı, `products.bulk_stock`) ve beşi de `warehouse_stocks`a yazıp `product_lots`a HİÇ dokunmuyordu — yani parti TAKİPLİ bir üründe herhangi biri `SAPMA` ÜRETEBİLİRDİ ve mutabakat onu ancak ERTESİ GÜN gösterirdi; satır numaraları bu PR'dan ÖNCE DOĞRULANDI, varsayılmadı; KURAL TEK CÜMLE: lot-suz yazmak TASARIMDIR — ama YALNIZ defter o (ürün, depo) çifti için KAPALIYKEN, satır varsa yazıcı ya partiyi SORAR ya 409 `LOT_TAKIPLI_URUN_LOTSUZ_YAZILAMAZ` ile REDDEDER; DÖRT YAZICI İKİ FARKLI CEVAP ve ayrım ÜSLUP DEĞİL O YOLUN NE OLDUĞUDUR: Excel içe aktarma (İKİ dal) ve ürün açılışı SORAR çünkü ikisi de bir GİRİŞTİR — mal depoya YENİ giriyor ve hangi partiden girdiği SORULABİLİR bir olgudur, isteğe bağlı bir alan (`Parti Kodu` sütunu / `ProductCreate.lot_code`) bugün çalışan partisiz akışı KIRMADAN cevabı mümkün kılar; ürün kartı stok düzenlemesi (`PUT`) ve toplu stok yazımı REDDEDER çünkü ikisi de bir DÜZELTMEDİR, bir mal hareketi değil — operatör bir sayıyı ELLE değiştiriyor ve parti takipli bir üründe o sayının hangi partiye ait olduğunun cevabı YOKTUR, UYDURULAMAZ; DOĞRU YOL ZATEN VAR (`POST /api/products/{id}/stock`, 1B-C, parti kodunu SORAR ve defteri yazar) yani red bir yeteneği KALDIRMIYOR, cevaplanabilir olana YÖNLENDİRİYOR; DÖRDÜNE DE AYNI CEVABI VERMEK İKİ YÖNDE DE ÖLÇÜLDÜ VE REDDEDİLDİ: dördüne de "sor" demek toplu stok yazımına ÜRÜN BAŞINA parti kodu taşıtmak demekti (o uç TEK bir değeri onlarca ürüne basar; ürün başına kod alan bir gövde ucun NE OLDUĞUNU değiştirirdi), dördüne de "reddet" demek ise Excel ile açılış yapan bir firmanın parti takibine HİÇ GEÇEMEMESİ demekti — ürünü partili açacak yol kalmazdı; KAPI `app/parti_defteri.py`DE ve orada olması ZORUNLU: yüklem `product_lots`u OKUR, çağıranın içine yazmak 1B-A'nın tablo adını ANAN dosyalar kümesini `imports.py` ile GENİŞLETİRDİ; defterden çağırmak ise ÜÇÜNCÜ ekseni (`CAGIRANLAR`, "defteri KULLANANLAR" diye tanımlıydı) bir adımla büyütür 6 -> 7 ve `imports.py` bu eksenin CANLI ÖRNEĞİDİR — `product_lots` literalini HİÇ taşımıyor, yani ilk iki eksen onu GÖRMEZ ve yalnız onlar olsaydı bu dilim defteri SESSİZCE genişletirdi; ayrıca red METNİ ve KODU TEK KOPYADIR, dört yazıcı aynı cümleyi kurmaz; YÜKLEM `kova_sec`İNKİ İLE AYNI — SATIR VAR MI, TOPLAM > 0 MI DEĞİL: tükenmiş bir parti (`quantity=0`, satır DURUYOR, 0067'nin kararı) o ürünün parti takipli olduğunun KANITIDIR; "toplam > 0" deseydik defteri sonuna kadar tüketilmiş bir ürüne lot-suz yazmak SERBEST kalırdı ve o yazma çifti DOĞRUDAN `SAPMA`ya iterdi, yani kapı tam da savunması gereken durumda AÇILIRDI; yüklem DEPO BAŞINADIR (0073 tekilliği) çünkü aynı ürün bir şubede takipli ötekinde takipsiz olabilir ve ürün düzeyine çıkarmak hiç parti görmemiş bir depoya açılış stoku yazmayı da reddederdi; İŞARET KURALI DEFTERE TAŞINDI, KOPYALANMADI: `_ayarlama_partisi`nin gövdesi 1B-C'de `products.py`deydi ve TEK çağıranlıydı, 1B-H ikinci çağıranı getirdi — `_parti_ayarla` artık defterde ve İŞARET karar veriyor (`diff < 0` DÜŞER, değilse AÇAR), `mode` ya da dal DEĞİL; iki kopya ayrıştığı gün aynı parti kodu iki uçtan İKİ FARKLI satır üretirdi ve geri çağırma kaydı hangisinin doğru olduğunu SÖYLEYEMEZDİ; `_ayarlama_partisi` yine de DURUYOR çünkü çözdüğü şey SÖZLEŞMEDİR (`lot_code` yokluğu ile `expiry_date` alanının GÖNDERİLMEMİŞ olması AYRI sorulardır ve ikisi de `model_fields_set`ten okunur) — defter bir Pydantic modelini okusaydı gövde biçimi değiştiği gün defter KIRILIRDI; RED TÜM İSTEĞİ DÜŞÜRÜR, O ÜRÜNÜ ATLAMAZ ve bu `bulk_price`ın #157'de kapattığı kusurun AYNISIDIR: atlamak 200 dönerdi ve `updated` sayısı istenen ürün sayısından küçük olurdu, yani KISMEN UYGULANMIŞ bir seçim TAMAMLANMIŞ olandan AYIRT EDİLEMEZDİ; Excel'de `errors` listesine kayıt düşürüp devam etmek de aynı şeydi (depo YARI aktarılmış bir katalogla kalırdı), çare bu yüzden SATIR NUMARASI taşır — "bir ürünün defteri açık" demek yüzlerce satırlık bir dosyayı ELLE taramak demekti; AÇILIŞ REDDETMEZ ve ETMEMELİ: ürün o istekte DOĞUYOR, defterinde satır BULUNAMAZ ve kapıyı oraya koymak hiçbir zaman doğru olamayacak bir yüklemi sorgulamak olurdu — kapı gibi görünen, hiçbir şey savunmayan bir SQL; SIFIR MİKTARLI AÇILIŞ DA PARTİ AÇMAZ ve `adjust` yolunun sıfır farkından AYRILIYOR (ayrım ölçüldü: orada operatör bir kod SAYDI, burada stok alanını BOŞ BIRAKTI) çünkü hiç mal girmemiş bir partiyi defterde VAR göstermek o ürüne sonraki HER lot-suz yazmayı 409 yapardı — kapı, hiç kullanılmamış bir parti yüzünden kapanırdı; BOS_CIFT ELEMESİ, 1B-G'NİN §2'DE ADIYLA YAZDIĞI BORÇ, ÖLÇÜLDÜ: ürün açılışı AKTİF HER DEPO için sıfır miktarlı bir `warehouse_stocks` satırı doğurur ve o çiftlerin hepsi `LOTSUZ_TASARIM`a düşüp kovayı ürün × depo ÇARPIMIYLA büyütüyordu; 1B-G test veritabanında 24 satırın 10'u, 1B-H davranış veritabanında 16'nın 9'u elendi (%42 ve %56) ve `LOTSUZ_TASARIM` kovası 16 -> 6 ile 11 -> 2 (%62 ve %82) küçüldü — §2'nin "gürültü" tahmini bir TAHMİN DEĞİLDİ, kovanın ÇOĞUNLUĞUYDU; kural `stok == 0` VE parti satırı YOK (iki defter de BOŞ, karşılaştırılacak bir sayı olmadığı için "uyuşuyor mu" sorusu bile SORULAMAZ; `LOTSUZ_TASARIM` ise bir ŞEY söyler: "burada mal VAR ve partisiz girdi"); DÖRDÜNCÜ BİR KOVA AÇILMADI ve `KOVALAR` ÜÇ kaldı çünkü boş çift bir SINIFLANDIRMA değil bir YOKLUKTUR ve sınıflandırmaya bir "hiçbiri" kovası eklemek kovaların ÜÇÜNÜN DE MAL hakkında konuştuğu olguyu bozardı, ayrıca `sayimlar` dört anahtarlı olsa operatör dört sayıyı toplayıp "hangisi benim ürünlerim" diye sormak zorunda kalırdı; ELEME SESSİZ DEĞİL: düşen sayı gövdede `bos_ciftler` olarak DURUR ve `total`a DAHİL DEĞİLDİR — sessizce atmak `total`ın deponun çift sayısından neden küçük olduğunu SORULAMAZ yapardı; SIRA YİNE ZORUNLU (`kova_sec` -> SONRA `bos_cift_mi`) ve `bos_cift_mi` `stok == 0` TAM EŞİTLİĞİNE bakar `not stok`a değil: negatif stoklu partisiz bir çift `SAPMA`dır (`0 > stok`) ve eleme öne alınsaydı o çift "boş" sayılıp RAPORDAN SİLİNİRDİ — 1B-G'nin `SAPMA`-önce kuralının kapattığı kaçağın AYNISI, bu kez öteki uçtan: eksi bakiye bir ETİKETLE değil RAPORDAN DÜŞÜRÜLEREK gömülürdü; 1B-G'NİN KENDİ TESTİ DEĞİŞTİ ve değişmesi bu dilimin en açık kanıtıdır: `(urun_f, depo_b)` çiftini `LOTSUZ_TASARIM` bekleyen satır, o beklentiyi ancak BEŞ SATIRLIK bir paragrafla savunabiliyordu — gürültünün KENDİ TESTİNDE BİLE açıklanmak zorunda kalması elemenin gerekçesidir; satır artık "çift raporda YOK" ve "`bos_ciftler` > 0" diye soruyor; #79 MERCEK BULGUSU KAPANDI: `ProductLotsPanel` başlığı "tükenmiş satır HEM SOLUK HEM 'Tükendi'" diyordu ama YALNIZ ETİKET sorulmuştu — `opacity` kaldırılsa test YEŞİL kalırdı, yani belgenin YARISI savunmasızdı; soldurma artık ADIYLA ölçülüyor ve dolu partinin TAM OPAK olduğu da soruluyor (yoksa "her satır soluk" gibi bozuk bir uygulama da geçerdi); mutasyon KOŞULDU: `opacity:tukendi?0.55:1` -> `opacity:1` ile TEK test kırmızı ADIYLA, ve eski "Tükendi" testi YEŞİL kaldı — boşluğun gerçekten orada olduğunun kanıtı; KAPILAR, KIMILDAMAYANLAR: Core sorgu envanteri 175 (yeni sorgular `text()` LİTERALİDİR, Core ifadesi değil), GET envanteri 185 (YENİ UÇ YOK), dinamik `text()` TOPLAMI 269 ve `products.py`nin dinamik çağrı sayısı 6 (altı SQL argümanının HEPSİ eskisi; bu dilimin `text()` çağrıları LİTERALDİR ve dinamik yüzeye HİÇ girmiyor), kiracı nöbetçisi, `TENANT_TABLES` (yeni tablo yok); BÜYÜYENLER: `CAGIRANLAR` 6 -> 7 (`app/routers/imports.py`), `tests/pins/pg_twins.txt` 120 -> 121, `tests/pins/alt_surec_sql.txt` 129 -> 130 (yeni PG ikizi, 1 gömülü SQL), `DYNAMIC_SQL_FILE_ALLOWLIST['backend/app/routers/products.py']` PARMAK İZİ (SQL DEĞİŞMEDİ: parmak izi DOSYANIN TAMAMININ AST hash'idir, yani `products.py`de nereye dokunulursa kımıldar ve KASITLI olarak YENİDEN İNCELEMEYE zorlar — pr-0085'in dört uç dosyasında ölçülen desenin AYNISI) ve `lot_code` sınır pini 3 -> 4 (`ProductCreate`; kapının ölçtüğü "kaç giriş var" DEĞİL "her girişin sınırı AYNI mı" — sayıyı sabit bırakmak yeni girişi kapının DIŞINDA bırakırdı, büyütmek onu ADIYLA içeri alıyor); ŞEMA ALANLARI YÜZÜNDEN `frontend/src/api/types.gen.ts` YENİDEN ÜRETİLDİ (`backend/openapi.json` `.gitignore`dadır, izlenmiyor); TESTLER `tests/test_1b_h_lotsuz_yazici.py` (9: yedi STATİK kapı + davranış) ve `test_1b_h_lotsuz_yazici_postgresql.py` (GERÇEK PostgreSQL 16'da koştu; SQLite ikizinin betiği AST ile OKUNUYOR kopyalanmıyor — iki metin ayrıştığı gün "PG ikizi SQLite'ın ölçtüğünü mü ölçüyor" sorusu SORULAMAZDI); PG İKİZİNİN DÖRT GEREKÇESİ ve dördü de yalnız orada görünür: (i) reddin TÜM İSTEĞİ geri aldığı SATIR SAYARAK — toplu yazım listede ÖNCE gelen partisiz ürünü ZATEN yazmıştı ve `db.rollback()` onu da geri almalı (ters sırada test hiçbir şey ölçmezdi), (ii) TÜKENMİŞ PARTİLİ ürüne lot-suz yazmanın DAVRANIŞLA da reddedildiği — SQLite ikizi bu senaryoyu yalnız STATİK ölçüyor ve `quantity>0` diye daraltılmış bir yüklem BURADA kırmızı olur, (iii) `ProductCreate.lot_code` ile açılan partinin `NUMERIC(18,4)` ONDALIK olduğu ve sahte bir `SAPMA` ÜRETMEDİĞİ (float olsaydı parti toplamı ile stok sessizce ayrışır ve mutabakatın KENDİSİ yalan üretirdi; sütun tipleri ayrıca `information_schema`dan OKUNUYOR), (iv) Excel'in EKSİ dalında `_parti_dus`un `WHERE quantity>=` korumasının 0067'nin `CHECK`i ile ÇAKIŞMADIĞI — koruma çalışmasaydı `IntegrityError` çıkardı, yani SQLite'ta SESSİZ olan kusur PG'de GÜRÜLTÜLÜ, ve elde olmayanı düşmek 409 `LOT_MIKTARI_EKSIYE_DUSER` dönüyor 500 değil; MUTASYON BATARYASI BEŞ MUTANT HEPSİ ADIYLA KIRMIZI ve HANGİSİNİ HANGİSİNİN YAKALADIĞI ÖLÇÜLDÜ VARSAYILMADI: (1) `_lotsuz_yazmayi_reddet`in gövdesi erken `return`e çevrildi -> YALNIZ `test_1b_h_DAVRANIS` kırmızı ve `test_dort_yazicinin_KARARI_ADIYLA` YEŞİL KALDI (çağrı duruyor, gövde boşaldı) — ikisinin AYRI durması bu yüzden zorunlu: statik kapı ÇAĞRIYI, davranış kapısı REDDİN KENDİSİNİ ölçer; (2) `_parti_kalemi` çağrısı `imports.py`nin GÜNCELLEME dalından silindi -> İKİSİ BİRDEN kırmızı (davranış: aynı kodla ikinci dosya stoğu 40'a çeker ama parti 15'te kalır; statik: çağrı sayısı 2 değil 1); (3) `bos_cift_mi` `kova_sec`in ÖNÜNE alındı -> YALNIZ `test_bos_cift_ELEMESI_kova_secimden_SONRA` (statik) kırmızı ve DAVRANIŞIN BUNU TUTMADIĞI DÜRÜSTÇE YAZILDI: bugünkü yüklem `stok == 0` TAM EŞİTLİĞİ olduğu için eksi bakiyeli çift sıra ters olsa da ELENMEZ — kapı, henüz kırmızı olmayan bir kusurun ÖN KOŞULUNU çiviliyor (yüklem bir gün `not stok` ya da `stok <= 0` diye gevşerse sıra TEK savunma olurdu) ve davranışla ölçülemeyeceği için STATİK; (4) `bos_cift_mi`den `stok == 0` koşulu DÜŞÜRÜLDÜ -> KOMŞU DOSYA (`test_1b_g_mutabakat.py::test_A_dan_F_ye_MUTLU_YOLLAR_SAPMA_URETMIYOR`) kırmızı: MALI OLAN ama partisiz HER çift de "boş" sayılıp düşerdi ve eleme o gün gürültüyü değil RAPORUN VAR OLMA SEBEBİNİ silerdi; kapıyı 1B-H'ye KOPYALAMAK aynı olguyu iki yerden sormak olurdu; (5) `_parti_takipli_mi`nin yüklemi `quantity>0` ile daraltıldı -> `test_lotsuz_red_SATIR_SAYISINA_bakiyor_TOPLAMA_degil` (statik) kırmızı ve davranışın bunu TEK BAŞINA tutmadığı da ölçüldü (SQLite davranışında tükenmiş partili ürüne lot-suz yazma senaryosu YOK, PG ikizinde VAR); KANONİK İZOLE SÜİT (`run_isolated_tests.py`) KOŞTU ve BULDUĞU TEK KIRMIZI `tests/test_1b_c_ayarlama_lot.py`nin `lot_code` sınır pinidir — aynı turda 3 -> 4 büyütülüp yeniden ölçüldü; İLK KOŞU AYRICA BİR ARAÇ KUSURU GÖSTERDİ ve kaydı burada: `run_isolated_tests.py` düşen dosyanın çıktısını basarken Windows konsolunun `cp1254` kodlamasında `UnicodeEncodeError` ile ÇÖKTÜ ve süit 295/431'de YARIDA KALDI — yani kırmızı bir dosya koşunun KENDİSİNİ de düşürüyor ve geri kalan 136 dosya HİÇ ÖLÇÜLMÜYOR; `PYTHONIOENCODING=utf-8` ile yeniden koşuldu, kusur bu PR'da DÜZELTİLMEDİ (kapsam) ama ADIYLA yazıldı; CI (koşu 34270820631, `b4b4ccd`) ÜÇ PİN BULDU ve DÖRDÜNCÜSÜNÜ ÖĞRETTİ: `backend-quality` iki kulvarında `test_ci_gates.py::test_pg_twins_inventory_matches_disk` ile `test_tenant_scoping_guard.py`nin İKİ kapısı (dinamik-SQL parmak izi + alt süreç SQL envanteri) kırmızıydı — üçü de yukarıda ADIYLA kapatıldı; AMA ASIL DERS `backend-postgresql`DE: DÖRT KULVARIN DÖRDÜ DE düştü ve sebebi bir TEST DEĞİL, ci.yml'in SAYIM KAPISIYDI — `BEKLENEN_PG_DOSYA_SAYISI=$(wc -l < tests/pins/pg_twins.txt)` TÜRETİLİYOR ve glob 121 dosya bulurken liste 120'de kalmıştı, yani yeni ikiz listeye YAZILMADAN shard SEÇİMİNDEN ÖNCE düşülüyor ve HİÇBİR PG testi KOŞMUYOR; bu bir kusur DEĞİL kasıtlı bir bedeldir (envanter bir SAYI değil LİSTE olduğu için iki PR aynı sayıda ÇAKIŞMIYOR, karşılığında listeyi yazmayı UNUTMAK tüm kulvarı düşürüyor) ama YEREL KOŞU BUNU GÖSTERMEZ: `run_isolated_tests.py` PG ikizini SQLite'ta ATLAR ve `pins/pg_twins.txt` ile ci.yml'i HİÇ okumaz — yani PG nüfusunun ÜÇÜNCÜ pin yeri (ci.yml'in türetilmiş sayacı) yalnız CI'da görünür; TABAN `origin/develop` 554d87a; kayıt adı `python scripts/durum.py --sonraki 88`
