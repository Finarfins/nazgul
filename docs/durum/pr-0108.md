#108 — SEC-6: IP BAŞINA saatlik tavan giriş yoluna ve WhatsApp webhook'una KONDU; tek bir IP'nin N kullanıcı adına yayılan kimlik-doldurması ve imzasız webhook yükü KAPANDI (GÖÇ YOK — `backend/alembic` diff'i BOŞ ve bu ölçüldü: yama karması `e69de29bb2d1d6434b8b29ae775ad8c2e48c5391`, yani BOŞ BLOB; alembic başı `20260912_0082` KIMILDAMADI, ROTA YOK, ŞEMA DEĞİŞMEDİ, TABLO EKLENMEDİ); AÇIK ÖLÇÜLDÜ İDDİA EDİLMEDİ ve ölçüm develop `8cfa7a0` üzerindedir: `app/routers/auth.py` ZATEN bir `_consume_ip_limit(db, action, ip_address, maximum)` taşıyordu (1 saatlik pencere, 429) ve onu register/forgot/reset yollarında BEŞ ayrı yerde kullanıyordu — yani mekanizma vardı, GİRİŞ ondan MAHRUMDU; girişte duran tek fren `login_lock_status(db, username, ip_address)`tir ve o kilit **(kullanıcı adı, IP) İKİLİSİNE** bağlıdır, bu yüzden tek bir IP N FARKLI kullanıcı adına, her biri için kilit eşiğinin (`MAX_LOGIN_FAILURES = 5`) BİR ALTINA kadar deneme yapıp HİÇBİR kilidi tetiklemeden ilerleyebiliyordu — denetimin adını koyduğu kimlik-doldurma (credential stuffing) açığı tam olarak budur ve yeni kapının (a) testi o senaryonun KENDİSİNİ koşar: 201 FARKLI kullanıcı adı, TEK IP; `POST /api/whatsapp/webhook` (WA1, #80) ise Meta'nın HMAC imzasını doğruluyordu ama imzası DÜŞEN isteklere hiçbir tavan uygulamıyordu ve bu uç OTURUMSUZDUR — imzasız gövde üretmek bedavadır, yani sınırsız bırakılan `verify_signature` saldırganın elinde ÜCRETSİZ BİR CPU POMPASIDIR; ÇARE İKİ SATIRLIK BİR YAMA DEĞİL BİR SIRA KARARIDIR ve bu dilimin en önemli cümlesi odur: giriş yolunda SAYIM kimlik doğrulamasından ÖNCE, KAYIT ise SONRA koşar — fren işi yaptıktan sonra değil ÖNCE çekilir ama BAŞARILI bir giriş bütçe HARCAMAZ; bunu mümkün kılmak için `_consume_ip_limit`e `kaydet: bool = True` parametresi eklendi (`kaydet=False` YALNIZ SAYAR ve pencereyi süpürür, satır YAZMAZ) ve `insert` gövdesi `_kaydet_ip_denemesi(db, action, ip_address)` adlı TEK yazma noktasına çıkarıldı; VARSAYILAN `True` OLARAK BIRAKILDI ve bu bir tembellik değil ölçülmüş bir karardır — register/forgot/reset'in beş çağrısı BAYT BAYT aynı kaldı ve davranışları KIMILDAMADI; İKİSİ TEK ÇAĞRIDA BİRLEŞSEYDİ ortak NAT arkasındaki bir dükkânda gün boyu DOĞRU parolayla giren 10-15 kişi kendi tavanlarını kendileri yerdi, yani tavan saldırganı değil müşteriyi kilitlerdi; KAYIT ÜÇ YOLDAN İKİSİNE KONDU ve dışarıda bırakılan yol ADIYLA gerekçelendirildi: kullanıcı başına kilit 429'u ve yanlış parola 401'i bütçeden DÜŞER, e-postası doğrulanmamış kullanıcının 403'ü DÜŞMEZ çünkü o cevap bir saldırı denemesi değil DOĞRU kimlik bilgisiyle gelen bir kullanıcıya verilen yönergedir; TAVAN 200/saat ve SAYI KEYFİ DEĞİL: sayaç YALNIZ BAŞARISIZ girişleri yediği için bu, tek bir çıkış IP'sinden saatte 200 YANLIŞ PAROLA demektir — ortak NAT arkasındaki bir dükkânda 10-15 kişi bir saat boyunca parolasını karıştırsa bile tavana değmez, kimlik-doldurma ise ilk dakikada aşar; KULLANICI BAŞINA KİLİT YERİNDE DURUYOR ve tavan onun YERİNE GEÇMEZ (5 deneme / 15 dakika kımıldamadı, `test_v2_9_login_rate_limit.py` YEŞİL), ikisi FARKLI şeyler ölçer: kilit tek hesaba yoğunlaşan saldırıyı, tavan N hesaba YAYILAN saldırıyı kapatır ve kilidin göremediği saldırı tam olarak ikincisidir; WEBHOOK TAVANININ YERİ SÖZLEŞMENİN KENDİSİDİR: sayaç `verify_signature` BAŞARISIZLIĞININ TAM İÇİNE kondu, yani imzası TUTAN bir Meta çağrısı o satıra HİÇ ULAŞMAZ ve meşru teslimat HİÇBİR KOŞULDA kısılamaz — tavan doğrulamanın ÖNÜNE konsaydı Meta'nın yoğun bir dakikası 429 alır ve Meta tarafında bir YENİDEN TESLİMAT FIRTINASI başlardı (dosyanın kendi yorumu `phone_number_id` süzgecinde aynı gerekçeyle 4xx dönmeyi zaten reddediyor); tavan 60/saat ve `action="wa_webhook_bad_sig"`tir, giriş bütçesinden AYRIDIR (`test_WEBHOOK_ve_GIRIS_BUTCELERI_AYRI` bunu ölçer — tek `action` kullanılsaydı bir webhook selleyicisi meşru kullanıcıların giriş bütçesini tüketirdi); IP ÇÖZÜMLEMESİ İCAT EDİLMEDİ, ÖLÇÜLÜP YENİDEN KULLANILDI: `app/client_ip.py::install_trusted_proxy_client_resolution` `Request.client` özelliğini SÜREÇ GENELİNDE yamalar ve `app/config.py:469`da içe aktarma sırasında koşar, yani `request.client.host` ZATEN güvenilen-vekil farkındadır ve `X-Forwarded-For` yalnız TANINAN bir vekilden geldiğinde dikkate alınır — webhook başlığa ELLE BAKMAZ ve bir kapı bunu çiviliyor (`test_WEBHOOK_IP_ADRESINI_X_FORWARDED_FOR_DAN_OKUMUYOR`); O KAPI METİN GREP'İ DEĞİL AST TARAMASIDIR ve bu ayrım deneyerek öğrenildi: ilk hali düz metin araması yapıyordu ve dosyanın KENDİ YORUMUNU (başlığı NEDEN okumadığımızı anlatan cümleyi) yakalayıp KIRMIZI yanıyordu; şimdi yalnız `ast.Constant` dize sabitlerine bakıyor ve tarayıcı KENDİNİ DOĞRULUYOR (`"/webhook" in sabitler`) çünkü bu olmadan boş bir küme her zaman yeşil dönerdi; SORGU ENVANTERİ KIMILDAMADI ve BU İDDİA ÖLÇÜLDÜ: `tests/test_core_query_inventory.py` `select`/`update`/`delete`i (dosya, fonksiyon, işlem, AST parmak izi) dörtlüsüyle pinler ve `_consume_ip_limit`in `delete` + `select` zincirleri O FONKSİYONUN İÇİNDE BIRAKILDI — dışarı çıkan tek şey `insert`tir ve `insert` bu kapının kapsamı DIŞINDADIR (dosyanın kendi başlığı bunu yazıyor); doğrulama iki bağımsız ağaçta koşuldu: baz `8cfa7a0` için ayrı bir worktree açıldı ve HESAPLANAN envanter parmak izi hem bazda hem bu dalda `5d6f151836bb5ac1a65b5ca1b336ed6ef60f6bb35e39b1178512b12f84322e13` çıktı, yani DRIFT SIFIRDIR; fonksiyonu paylaşılan yeni bir modüle taşımak ya da ikiye BÖLMEK bu pinleri kırardı ve `kaydet` bayrağı tam olarak o yüzden bir parametredir, bir refaktör değil; ÇAPRAZ İTHAL DEPONUN KENDİ KALIBIDIR: `app/routers/whatsapp.py` `from .auth import _consume_ip_limit` der ve bu ilk değildir (`app/routers/companies.py` zaten `from .auth import validate_tax_number` diyor), döngüsel ithal YOKTUR ve ölçüldü (`import app.routers.whatsapp` temiz); AYARLAR ENV İLE AYARLANABİLİR ve ÜÇ YERE BİRDEN yazıldı çünkü tamlık kapısı (`tests/test_env_example_completeness.py`, #94) bunu ZORUNLU kılar: `LOGIN_IP_LIMIT_PER_HOUR=200` ve `WHATSAPP_WEBHOOK_BAD_SIGNATURE_LIMIT_PER_HOUR=60` hem `.env.production.example`a hem `backend/.env.example`a hem de `docker-compose.prod.yml`in app servisi `environment:` haritasına (`${KEY:-varsayilan}` biçimiyle) girdi — biri atlansaydı ayar BELGELİ AMA ETKİSİZ sınıfına düşer ve o kapı zaten bunun için vardır; ayarların yeri `trusted_proxy_cidrs`in HEMEN ALTIDIR ve bu bilinçlidir: ikisi de ona BAĞIMLIDIR, liste boşken ters vekilin arkasındaki her istek vekilin adresine düşer ve tavan TÜM kullanıcılar için ORTAKLAŞIR (uyarı `client_ip.py` içinde zaten yazılı); KAPI `backend/tests/test_sec6_ip_limitleri.py` (10 test, ALT SÜREÇ YOK — dosya kendi geçici SQLite'ını modül başında kurar ve `TestClient(app, client=(ip, 40000))` ile HER TESTE KENDİ IP'sini verir, ortak IP kullanılsaydı testler birbirinin bütçesini yer ve sıraya bağlı açıklanamayan kırmızılar üretirdi): (a) 200 başarısız giriş 401, 201'inci 429 VE kilidin HİÇ ateşlemediği AYRI ölçülüyor (`login_attempts`ta 200 satır, hepsinin `fail_count == 1`, hepsinin `locked_until is None`) çünkü 429'u kilidin getirdiği bir dünyada test YİNE yeşil olur ve YANLIŞ şeyi kanıtlardı; (b) doğru parolayla 5 giriş sayacı 0'da bırakır, ARDINDAN aynı IP'den bir YANLIŞ parola sayacı 1 yapar — ikinci adım testin "sayaç hiç çalışmıyor" diye VACUOUS yeşil dönmediğini ölçer; (c) 60 bozuk imza 403, 61'inci 429, ve TAVAN DOLUYKEN doğru imzalı istek YİNE 200 `{"status": "ok"}` — bu son adım testin can alıcı yarısıdır; (d) pencere: 200 satır `attempted_at`i 2 SAAT GERİYE yazılıp (saati ileri sarmak yerine satırlar geriye yazılır, sonuç aynıdır ve sürecin saatine dokunulmaz) tavanın DOLU GÖRÜNMESİNE rağmen isteğin 401 aldığı VE eski satırların gerçekten SİLİNDİĞİ ölçülür — yalnız sayılmamış olmaları yetmezdi, süpürme olmadan tablo süresiz büyürdü; ayrıca 429 gövdesinin diğer uçlarla HARFİ HARFİNE aynı olduğu, iki bütçenin ayrı olduğu, `TENANT_TABLES`ın 120'de kaldığı ve `auth_rate_limits`in içinde OLMADIĞI (tablo `company_id` TAŞIMAZ — taşısaydı bir IP'nin bütçesi kiracı başına sıfırlanır ve tavan HİÇBİR ŞEY frenlemezdi) çivilendi; DÖRT MUTASYON KOŞULDU İDDİA EDİLMEDİ ve her birinden sonra kaynak `cp` ile geri kondu, geri koyma `git hash-object` ile DOĞRULANDI (`app/routers/auth.py` -> `8fc2dbd097ce3cb82049d057699523b3f9836102` iki kez aynı): (1) `login`deki `_consume_ip_limit` çağrısı SİLİNDİ -> `test_TEK_IP_201_FARKLI_KULLANICI_ADI_201inci_429`, `test_TAVAN_ASILDIGINDA_govde_DIGER_UCLARLA_AYNI` ve `test_BIR_SAATTEN_ESKI_SATIRLAR_SUPURULUYOR` KIRMIZI (mutant tam olarak ölçülen açığın kendisidir: 201 denemenin 201'i de 401 döner); (2) webhook tavanı `verify_signature`ın ÖNÜNE alındı, yani imzası TUTAN istekleri de sayar -> `test_IMZASI_TUTAN_ISTEK_TAVANDAN_SONRA_DA_200` ve `test_IMZASI_TUTAN_ISTEK_SAYACA_HIC_DOKUNMUYOR` KIRMIZI; (3) pencere süpürmesi (`delete ... attempted_at < cutoff`) düşürüldü -> `test_BIR_SAATTEN_ESKI_SATIRLAR_SUPURULUYOR` KIRMIZI; (4) `kaydet=False` -> `True` -> DÖRT test KIRMIZI (yukarıdaki üçü + `test_BASARILI_GIRIS_BUTCE_HARCAMIYOR`) ve sebep tek değildir: başarılı giriş bütçe yemeye başlar VE her başarısız deneme İKİ KEZ sayılır (biri kontrolde biri kayıtta), yani tavan 100'de dolar; SAYAÇLAR ÖLÇÜLDÜ VARSAYILMADI ve HİÇBİRİ KIMILDAMADI: rota sözleşmesi **391/301** (yeni rota yok — yeni 429 cevabı OpenAPI'ye GİRMEZ çünkü `responses=` bildirimi eklenmedi ve KASTEN eklenmedi: mevcut üç çağıranın 429'u da bildirilmiş değildir, biri bildirilip diğerleri bildirilmeseydi sözleşme kendi içinde tutarsız olurdu), GET izin envanteri **186**, `TENANT_TABLES` **120**, alembic başı `20260912_0082` (yedi yerdeki pin dokunulmadı), `pins/` DEĞİŞMEDİ (`pins/alt_surec_sql.txt`e satır EKLENMEDİ çünkü yeni kapı alt süreç KULLANMIYOR — WA1 kalıbıyla aynı, ortam modül başında kurulur ve koşucu her dosyayı kendi sürecinde koşturur), `frontend/src/api/types.gen.ts` DEĞİŞMEDİ (şema değişmedi, yeniden üretim GEREKMEDİ ve yapılmadı); İZOLE KOŞUM `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4` ile ALTI dosya: yeni kapı (10) + `test_self_service_registration` (4) + `tests/test_wa1_ingress` (22) + `tests/test_route_security_contracts` + `tests/test_ci_gates` + `tests/test_env_example_completeness` -> **6/6 dosya, 76 test YEŞİL**; giriş yoluna dokunan komşu korumalar AYRICA koşuldu: `test_v2_9_login_rate_limit` (1) + `tests/test_54a_mobil_auth` (9) + `test_f0_email_registration` (4) + `test_registration_hardening` (25) -> **4/4 dosya, 39 test YEŞİL**; ÜÇ BİLİNEN YEREL KIRMIZI SİLİNMİYOR NİTELENİYOR ve bu kez BAZ ÖLÇÜMÜ AYNI MAKİNEDE ALINDI: `test_core_query_inventory` 7 kırmızı, `test_core_tenant_scoping_guard` 4 kırmızı, `test_tenant_scoping_guard` 1 kırmızı — bu üç sayı bu dalda ve baz `8cfa7a0` için açılan AYRI worktree'de BİREBİR AYNI çıktı (7/4/1 = 7/4/1), yani kırmızılık DALA DEĞİL yazarın makinesine aittir (#101'de kayıtlı BİLİNEN ORTAM SORUNU, temiz klonlarda yeşil) ve bu PR o üç dosyaya DOKUNMUYOR; ÇAKIŞMA NOTU: #106 (SEC-10) hâlâ AÇIK ve `backend/app/routers/auth.py`, `backend/app/config.py`, `backend/test_self_service_registration.py` dosyalarına DOKUNUYOR — üçü de bu dilimin dosyalarıdır, ama metinsel örtüşme YOKTUR ve bu ölçüldü: #106'nın auth.py parçaları 243/323/360/715 satırlarındadır, bu dilimin parçaları ise `_consume_ip_limit` (383) ve `login` (517) çevresindedir; config.py'de #106 83/180/390/405/447'ye, bu dilim 63'ün hemen altına yazar; `test_self_service_registration` bu dilimde HİÇ DEĞİŞTİRİLMEDİ, yalnız koşuldu; SIRA NOTU: bu iş "SEC-7 itildikten SONRA başla" talimatıyla verildi ama SEC-7 depoda HİÇBİR YERDE YOKTUR (dal yok, PR yok, commit yok, `docs/durum` girdisi yok; `gh pr list` #87…#106 arasında SEC-7 içermiyor), bu yüzden kapı DOĞRULANAMADI ve dal talimatın dediği gibi `origin/develop` üzerine kesildi — SEC-7 gerçekten beklemedeyse bu dal rebase edilmelidir; dal `fix/sec6-ip-limits`; kayıt adı `python scripts/durum.py --sonraki 108`
