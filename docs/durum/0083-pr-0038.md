#38 — FİRMA PROFİLİ (Faz 5.2): kayıt anında seçilen iş kolları SAKLANIYOR, HİÇBİR DAVRANIŞ BUNA BAĞLANMIYOR — göç `20260904_0068` `companies`e `profiller TEXT NOT NULL DEFAULT ''` ekliyor ve bu turda o sütuna bakan tek bir yetki, menü ya da uç YOKTUR; iddia düzyazıda BIRAKILMADI, `test_HICBIR_MODUL_profil_ADIYLA_ACILIP_KAPANMIYOR` dört profil adının (`ciftci`, `pazarci`, `tuccar`, `veteriner`) `app/firma_profilleri.py` DIŞINDA hiçbir metin sabitinde geçmediğini AST üzerinde ölçüyor (belge dizgileri eleniyor: düzyazıda anmak çağırmak değildir) ve bir gün biri `if "veteriner" in profiller:` yazarsa o test KIRMIZI olur — bu DOĞRUDUR, o gün kapı anahtarı DOĞRULAYAN teste dönüşmelidir; ÖLÇÜLEN KUSUR: `companies` bugün ON İKİ sütun taşıyor (`app/tenancy.py`de sayıldı: id, name, tax_number, is_active, negative_stock_policy, credit_limit_policy, beş `farm_*` politikası, created_at) ve HİÇBİRİ firmanın NE İŞ YAPTIĞINI söylemez, kendi kendine kayıt olan kiracı (`POST /api/auth/register`, tek işlemde firma + Merkez şubesi + admin + üyelik + doğrulama e-postası) iş kolunu bırakacağı yeri BULAMAZ; kusur bugün görünmezdir çünkü bütün kiracılar aynı ekranı görüyor, modül anahtarları geldiği gün görünür olur ve o gün GEÇMİŞ VERİ OLMAYACAKTIR — depo bu yüzden sorudan ÖNCE açılıyor; KÜME, ENUM DEĞİL ve gerekçe sahibin KENDİ işletmesidir: aynı işletme hem bayilik (tüccar) hem servis hem tarla (çiftçi) hem sürü (veteriner) taşıyor, tek değerli bir `profil` sütunu bu kiracıyı ÜÇ kere yanlış sınıflandırır ve hangi üçünü kaybettiği SORULAMAZDI — karışmak burada kenar durum değil ilk kullanıcının NORMAL hâlidir; DEPO BİÇİMİ ÖLÇÜLEREK SEÇİLDİ, tercihle değil: 0016 (`einvoice_seam`) kuralı bu depoya zaten yazmış ("``einvoice_payload`` is TEXT holding a JSON string (NOT JSONB): the codebase keeps SQLite<->PostgreSQL16 parity and has been bitten by dialect JSON typing before") ve `sa.JSON()` bu depoda YALNIZ tedarikçi fiyat köprüsünün (0037/0038) yutma tablolarındadır — orada saklanan şey SATICIDAN gelen şekli bilinmeyen ham yüktür, burada saklanan şey DÖRT elemanlı KAPALI bir kümedir, yani dialect JSON tiplemesinin bilinen riskini almak için sebep YOK; virgülle birleştirme de yeni değildir, `app/routers/seasonal_plan.py`deki `_parse_months` aynı şekli (virgülle böl, kırp, KÜMEYE al, `sorted()`, tanınmayanda 422) aylar için zaten uyguluyor ve 0068 onu firma profiline taşıyor; SIRALAMA SAKLANIRKEN UYGULANIR çünkü `pazarci,ciftci` ile `ciftci,pazarci` AYNI kümedir ve iki ayrı dizgi olarak saklanırlarsa dizgi düzeyindeki her eşitlik karşılaştırması sessizce YANLIŞ cevap verir; normalleştirme TEK yerdedir (`app/firma_profilleri.py`, `profilleri_birlestir`) ve İSTEMCİDE YAPILMAZ — tarayıcıya güvenilerek saklanan bir sıra ilk `curl`da bozulurdu, bu yüzden kayıt formu tıklama sırasını gönderiyor ve sunucu normalleştiriyor (vitest bunu adıyla çiviliyor); BOŞ DİZGİ "SEÇİLMEDİ" DEMEKTİR, "HİÇBİRİ" DEMEZ: varsayılan `''` ve BACKFILL YOKTUR, mevcut kiracıların hepsi `''` ile kalır ve bu DOĞRU veridir — onlara bu soru hiç sorulmadı, cevaplarını uydurmak ölçülmemiş bir olguyu ölçülmüş gibi kaydetmek olurdu (0067'nin `expiry_date` ve 0066'nın `base_unit = unit` gerekçelerinin AYNISI); `NOT NULL` + `''`, NULL yerine SEÇİLDİ çünkü iki ayrı "yok" durumu okuyan her yerde iki dallı kontrol isterdi ve o dallardan biri er geç unutulurdu; İKİ KATMAN: uçların `Literal` şeması 422 verir (`RegisterPayload`, `CompanyCreate`, `CompanyPolicyUpdate` — üçünde de İSTEĞE BAĞLI, zorunlu yapmak bugün istek atan her istemciyi kırardı) ve `profilleri_birlestir` BUNA GÜVENMEZ, tanınmayan değeri kendisi de reddeder çünkü onu Pydantic'ten geçmemiş bir çağıranın (betik, göç, gelecek bir uç) çağırması ENGELLENMİŞ DEĞİLDİR ve tek katmanlı doğrulama o çağıran ortaya çıktığı gün SESSİZCE delinirdi; OKUMA `GET /api/company-settings`e eklendi, `GET /api/companies/{id}`e DEĞİL çünkü O UÇ YOKTUR — ölçüldü, firma rotaları `GET`/`POST /api/companies` ve `GET`/`PUT /api/company-settings`tir; keyfi bir firma kimliği alan yeni bir okuma yüzeyi açmak bu kiracı-korumalı depoda YENİ bir sınır demekti, oysa `company-settings` zaten `company_id(request)` ile o kiracıya bağlı ve politika sütunlarını zaten döndürüyor; uç dışarıya LİSTE veriyor (`profilleri_coz`), depo virgüllü TEK dizgi tutuyor — iki uçta iki farklı şekil istemciyi depo biçimini ayrıştırmaya zorlardı; KAYIT UCU 201 DEĞİL 200 DÖNER ve gövdesi BİLİNÇLİ olarak OPAKTIR (var olan hesapla BİREBİR aynı cevap + `REGISTER_RESPONSE_FLOOR_SECONDS` zaman tabanı, hesap sayımını engellemek için), bu yüzden "profillersiz kayıt `''` saklar" iddiası GÖVDEDE ÖLÇÜLEMEZ ve `companies.profiller` SATIRINDA ölçülüyor — gövdede ölçmek, yeni firmanın durumunu kayıt cevabına sızdırmak yani o güvenlik özelliğini BİLEREK bozmak olurdu; ÇİVİLER: alembic başı ÜÇ yerde `20260903_0067` -> `20260904_0068` (`tests/test_goc_zinciri.py`:424, 449, 511), `app/tenancy.py` parmak izi `b50d5fd5` -> `e81ca586` (`tests/test_tenant_scoping_guard.py`) çünkü `companies` Table'ına TEK sütun bildirimi eklendi — eklenen şey bir Core SÜTUN BİLDİRİMİDİR, `text()` çağrısı DEĞİL: dosyadaki dinamik `text()` sayısı 4te SABİT ve dördünün de argümanları develop ile BİREBİR aynı (ÖLÇÜLDÜ, CPython 3.12.3), parmak izi kımıldadı çünkü dosya AST'sinin TAMAMINDAN türüyor; `companies` KİRACI KÖKÜDÜR (`company_id` TAŞIMAZ), bu yüzden yeni sütun kiracı yüklemi GEREKTİRMEZ ve `TENANT_TABLES`a GİRMEZ — nöbetçinin envanter kapısı bu turda da yeşil; alt süreç SQL sayacı 102/165 -> 103/166 (+1 dosya, +1 metin: `tests/test_firma_profili.py`in tek gömülü senaryosu; AYRI SÜREÇ şart çünkü `import app.main` göç zincirini süreç başına BİR KEZ kurar ve senaryo SIFIRDAN bir kiracı doğurup satırı okur) ve `app/` altında yine SIFIR; PG glob 99'da SABİT (`ls backend/test_*postgresql*.py | wc -l`) ve `BEKLENEN_PG_DOSYA_SAYISI=101` KIMILDAMADI — bu tur PG İKİZİ EKLEMEDİ, çünkü kayıt yolunun zaten bir ikizi YOKTU ve sütun davranışının diyalektten ayrışan bir yanı yok (`TEXT` + `''` iki motorda da aynı); ÖLÇÜLEN KOŞULAR: göç `upgrade -> downgrade -> upgrade` İKİ DİYALEKTTE de temiz (SQLite ve GERÇEK PostgreSQL 16.13; sütun her turda göründü, düştü, yeniden göründü ve baş `20260904_0068`de kaldı), `tests/test_firma_profili.py` 7/7, hedefli arka uç süpürmesi 217/217, `test_tenant_scoping_guard.py` 28/28 (CPython 3.12 ile — 3.11'de parmak izi kapısı ZATEN kırmızıdır ve bu develop'un KENDİ dosyasında da böyledir, ölçüldü: hesap makinesi önce develop'un `b50d5fd5` çivisini BİREBİR yeniden üreterek doğrulandı), `tests/test_ci_gates.py` 16/16, `npx tsc -b --force` çıkış 0, vitest 89 dosya 611 -> 614 test (+3, hepsi `Register.test.tsx`), ESLint 0 hata (iki `import/order` UYARISI develop'un kendi dosyalarında da AYNEN var — ölçüldü, bu turun getirdiği değil); TİPLER ELLE YAZILMADI: `frontend/src/api/types.gen.ts` CI'nin kendi zincirinden geçirildi (`export_openapi.py` -> `npm run types:gen` -> `capture_frontend_fixtures.py`) ve üretilen birleşim `("ciftci" | "pazarci" | "tuccar" | "veteriner")[]` oldu, fixture'lar DEĞİŞMEDİ; MUTASYON — BEŞ mutasyon, BEŞ adı konmuş kırmızı: `sorted()` kaldırıldı -> `test_profiller_TEKILLESTIRILIR_ve_SIRALI_saklanir` (+ uçtan uca duman), tekilleştirme kaldırıldı (`set` -> `list`) -> aynı test, ikinci katman doğrulaması kaldırıldı -> `test_TANINMAYAN_profil_IKINCI_KATMANDA_da_REDDEDILIR`, `RegisterPayload`ta `Literal` -> `str` (yani KÜME DOĞRULAMASI YOK) -> `test_SEMALAR_profilleri_ISTEGE_BAGLI_alir_ve_KUMEYI_dayatir` (+ uçtan uca duman), `app/` içine profil adı kondu -> `test_HICBIR_MODUL_profil_ADIYLA_ACILIP_KAPANMIYOR`; ÖLÇÜLMEDİ, ADIYLA: MODÜL ANAHTARLARININ KENDİSİ — hangi profilin neyi açıp kapattığı bu PR'da YOKTUR ve tasarımı da yapılmadı, yalnız karar saklanıyor; GEÇİŞ — mevcut kiracıların `''`den gerçek profillere nasıl taşınacağı ölçülmedi, çünkü onlara soruyu soracak ekran (ayarlar formu) bu turda EKLENMEDİ, yalnız uç açıldı; PROFİL KÜMESİNİN BÜYÜMESİ — beşinci bir iş kolu geldiğinde `PROFILLER` demeti ile `FirmaProfili` `Literal`ının ELLE eşleşmesi ayrışma riskidir ve bugün `test_PROFILLER_ile_Literal_AYRISMAZ` ile çivilendi, ama `Literal` çalışma zamanında türetilemediği için ikisi ELLE aynı kalmak zorunda; ÜRETİM BOYUTUNDA GÖÇ SÜRESİ — `ALTER TABLE companies ADD COLUMN` varsayılanlı bir sütun ekliyor ve `companies` küçük bir tablodur, ama süresi gerçek üretim verisiyle ölçülmedi.
