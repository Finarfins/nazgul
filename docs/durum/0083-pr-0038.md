#38 — SIRA-BAĞIMLI BEŞ TEST DOSYASI + DEĞİŞEN SQLite TESTLERİ İÇİN TERS-SIRA CI KAPISI; `backend/app` DOKUNULMADI ve bu ÖLÇÜLDÜ (değişen yolların `^backend/app/` sayımı = 0), değişen yedi dosyanın altısı test biri `ci.yml`; TABAN `24fcf522` yani #31'in birleşmesinden SONRAKİ develop ve sha ÖLÇÜLDÜ, oturum başındaki anlık görüntüye GÜVENİLMEDİ; HER DOSYA İÇİN DÖRT KOŞU YAPILDI — tek başına / tanım / TERS / karışık(20260903) — ve protokol TEK YÖNLÜ DEĞİLDİR: önce kırmızı ÜRETİLDİ, sonra düzeltildi, sonra DÜZELTME GERİ ALINIP AYNI KIRMIZININ DÖNDÜĞÜ ölçüldü, çünkü yalnız yeşile bakmak düzeltmenin kırmızıyı GERÇEKTEN kapatıp kapatmadığını SÖYLEMEZ; `pytest-randomly` KURULU DEĞİLDİR ve bu ÖLÇÜLDÜ, bu yüzden karışık sıra `random.Random(20260903).shuffle(dugum_kimlikleri)` ile ÜRETİLDİ ve yöntem burada YAZILIDIR — A5'in kendi karıştırma yöntemi ELDE OLMADIĞI için bu sütun A5'inkiyle BİREBİR AYNI DEĞİLDİR ve öyleymiş gibi sunulmuyor. MEKANİZMALAR, DÖRDÜ DE ÖLÇÜLDÜ: `test_platform_backups.py` — `with sqlite3.connect(path) as db:` COMMIT EDER AMA KAPATMAZ (Python'un belgelenmiş davranışı), dört noktada (27/98/140/157) bağlantı GC'ye bırakılıyordu, toplama zamanı test sırasına göre kaydığında `offline.sqlite`a CANLI handle kalıyor ve `app.backup_cli restore`un `offline.sqlite.restore.tmp -> offline.sqlite` rename'i Windows'ta `[WinError 5]` ile düşüyordu; çare `closing(sqlite3.connect(path)) as db, db` yani DIŞTA kapatma İÇTE commit, işlem semantiği AYNEN korunur ve kapatma GC'ye değil sözleşmeye bağlanır. `tests/test_field_outbox_writer.py` — `_uygulamaya_ait_metadata()` `sys.modules`u tarıyordu ama o sözlüğü dolduran `importlib.import_module("app.main")` çağrıları BAŞKA testlerin gövdesindeydi, ve GÖREVDE YAZILANDAN DAHA KÖTÜ BİR OLGU ÖLÇÜLDÜ: test yalnız ters sırada değil TEK BAŞINA DA KIRMIZIDIR (1 failed in 0.41s), yani "sıra-bağımlı" nitelemesi EKSİKTİ, test baştan beri başka testlerin yan etkisine ASALAKTI ve tek başına HİÇBİR ŞEY ÖLÇMÜYORDU; import yardımcının İÇİNE alındı. `test_harman_v2c_allocation_postgresql.py` ve `test_field_status_write_postgresql.py` — GÖREVDE VERİLEN İKİ ATIF DA ÖLÇÜMLE YANLIŞ ÇIKTI: `FIELD_STOCK_OUTBOX_ENABLED` o dosyada HİÇ GEÇMEZ (grep = 0) ve harman ikizi 49 satırlık İNCE SARMALAYICIDIR, bayrak alt sürece AÇIKÇA verildiği için sıra hassasiyeti bayraktan GELEMEZDİ; her ikisinde GERÇEK mekanizma AYNIDIR: paylaşılan PostgreSQL'de bu ailelerin smoke'ları AYNI bootstrap kullanıcısını kullanıyor ve her biri şifreyi KENDİ sabitine döndürüyor (ChargeFlagOff123!, FieldEk!123, FieldParts!123, FieldStatus!123, FieldIscilik!123), giriş ise `admin123` SABİTİNİ yazıyordu yani smoke İLK KOŞMAYA bağımlıydı ve ters sırada `KeyError: 'access_token'` / `{"detail":"Kullanıcı adı veya şifre hatalı"}` veriyordu — DİKKAT ÇEKİCİ OLAN, çarenin dosyanın BAŞKA BİR YERİNDE ZATEN VAR VE BELGELENMİŞ olmasıdır (aday-şifre döngüsü, yorumu: "an earlier smoke may already have rotated the bootstrap password"), yani kusur bilinmeyen bir olgu değil BİLİNEN BİR ÇARENİN UYGULANMADIĞI yerlerdi. `test_supplier_price_import_postgresql.py` ÜÇ ayrı mekanizma taşıyordu: (a) import smoke girişi FALLBACK'SİZ `admin123`, (b) bridge `login()` aday listesi EKSİK (`admin123Changed!` ve `SupplierBridge123!` yoktu, ilkini aynı ailedeki tier1 smoke'u üretiyor), (c) bridge içe-aktarma listesinin TÜM İÇERİĞİNİ tek elemanlı sanıyor ve revizyon zincirini 1/2/3/4 MUTLAK değerlerine çiviliyordu; (c)'de İDDİA GEVŞETİLMEDİ ve bu bir karardır — liste iddiası artık yalnız KENDİ `import_id` satırını arıyor, zincir İLK GÖZLENEN revizyona çapalanıyor (ilk_rev, +1, +2, +3), böylece "her uygulama sayacı BİR artırır" sözleşmesi AYNEN ölçülmeye devam ediyor; mutlak sayıyı `>= 1`e indirmek sözleşmeyi ölçmeyi BIRAKIRDI ve sıra sorununu bir ÖLÇÜM KAYBIYLA takas etmek olurdu. KANIT TABLOSU ÖLÇÜLMÜŞTÜR: platform_backups ters KIRMIZI -> yeşil -> geri alındı ters KIRMIZI; outbox_writer ters VE TEK BAŞINA kırmızı -> yeşil -> geri alındı aynı kırmızı; harman ters kırmızı -> yeşil -> geri alındı aynı kırmızı; field_status ters kırmızı -> yeşil -> geri alındı aynı kırmızı; supplier_price TERS 2 / KARIŞIK 1 kırmızı -> yeşil -> geri alındı TERS 2 / KARIŞIK 1, yani geri-alma görevde verilen tabloyu BİREBİR üretti. PG koşuları taze `postgres:16` konteynerinde (5433) yapıldı ve HER KOŞUDAN ÖNCE şema `DROP SCHEMA public CASCADE` + `alembic upgrade head` ile sıfırlandı, aksi hâlde paylaşık satır kusurları önceki koşudan SIZAR ve ölçüm KENDİ ARTIĞINI ölçerdi. Değiştirilen yardımcıların SQLite kardeşleri ayrıca koşuldu (24 passed) çünkü bu dosyaların ÇOĞU iki motoru birden besler ve yalnız PG ikizine bakmak SQLite tarafını GÖRMEZDİ. PART 2 — TEK adım `backend-quality-canonical` işine kondu ve SEÇİM GEREKÇESİ ÖLÇÜLDÜ: görevde "SQLite env'i olan işi seç" deniyordu ama İKİ ADAYIN DA AÇIK `DATABASE_URL`İ YOKTUR (ikisi de yalnız `PYTEST_DISABLE_PLUGIN_AUTOLOAD` koyar; SQLite oradaki ÖRTÜK varsayılandır), yani seçim başka gerekçelerle yapıldı — canonical TEK runner'dır (`backend-quality` 4'lü shard matrisidir ve adım orada AYNI girdiyle DÖRT KEZ boşuna koşardı), PG servisi yoktur, bağımlılıklar zaten kuruludur ve iş ZATEN ZORUNLUDUR, dolayısıyla `deploy/ci-required-contexts.json` DEĞİŞMEDEN koruma kazanılıyor. `actions/checkout@v4` `fetch-depth: 1` ile geldiği için `origin/$BASE_REF...HEAD` üç noktalı farkı ÇALIŞMAZDI ve bu görevde YAZILI DEĞİLDİ; adım tabanı KENDİ İÇİNDE çekiyor (`--depth=200`, merge-base bulunamazsa `--unshallow`) çünkü işin tamamına `fetch-depth: 0` açmak HER koşuyu yavaşlatırdı. Adım yalnız `pull_request`te koşar, boş fark hâlinde `değişen test dosyası yok` yazıp geçer (ayrıca koşuldu, çıkış 0), ve BÜTÜN değişen dosyaları koşup kapıyı SONDA kapatır — ilk düşen dosyada `set -e` ile durmak AYNI MALİYETE daha az teşhis verirdi ve bu ilk sürümde ÖLÇÜLEREK görüldü (mutasyon dosyası düşünce son iki dosya HİÇ koşmamıştı). MUTASYON KANITI ÇİFT YÖNLÜDÜR: kasten sıra-bağımlı iki testlik dosya eklendiğinde adım çıkış 1 ile düştü ve dosyayı ADIYLA bildirdi, kaldırılınca çıkış 0. Adımın YAN KAZANCI ölçüldü: bu PR'ın değişen ALTI dosyasının hepsi kapının kendi koşusunda ters sırada YEŞİL, yani PART 1 kapıdan BAĞIMSIZ ikinci kez doğrulandı. SINIR ADIM YORUMUNDA DA YAZILIDIR: bu işte PostgreSQL servisi YOKTUR, bu yüzden `*postgresql*.py` ikizleri adımın DIŞINDADIR ve onların ters-sıra koruması bu adımda DEĞİL `backend-postgresql` işinde aranmalıdır; bu PR o korumayı EKLEMİYOR. ÇİVİLER: PG globu 99'DA KALDI ve `git ls-files | grep -c` ile ÖLÇÜLDÜ, önceki ölçümün üzerine aritmetik YAPILMADI — not: aynı glob #29 sonrası develop'ta 98'di ve #31 bir ikiz ekleyerek 99'a çıkardı, yani görevde verilen "99" rakamı ancak #31 BİRLEŞTİKTEN SONRA doğrudur; `tests/test_ci_gates.py` 16 passed; `deploy/ci-required-contexts.json` DEĞİŞMEDİ; yeni `test_*postgresql*.py` YOK; sıra `scripts/durum.py --sonraki 38` ile ÜRETİLDİ (elle seçilmedi) ve en yüksek kayıt 0082 olduğu için BU KAYIT 0083'TÜR. ÖLÇÜLMEDİ, ADIYLA: LINUX ÖLÇÜMÜ YEREL OLARAK YAPILMADI — `test_platform_backups.py`in `[WinError 5]`i Windows'a özgü bir handle sızıntısı olarak GÖRÜNÜYOR ve Linux'ta açık handle rename'i engellemediği için orada yeşil kalması BEKLENİR, ama bu bir BEKLENTİDİR ve elimde Linux koşusu YOKTUR; cevabı bu PR'ın CI koşusu verecektir ve sızıntı yine de düzeltildi çünkü GC'ye bağlı kapatma PLATFORMDAN BAĞIMSIZ olarak yanlıştır. A5'İN DOSYA BAŞINA SÜRELERİ ELDE YOKTUR ve maliyet tahmini A5'ten DEĞİL yerel ölçümden türetildi (harman 10.12s, platform_backups 3.34s, v3_bridge 3.08s, v3_import 3.54s, outbox_writer 21.79s, field_status_contract 12.38s; altı dosya ~54 sn + dosya başına `--collect-only` ek yükü, toplam ~65-70 sn; tipik PR bir-iki test dosyası değiştirdiği için beklenen maliyet 10-25 sn), GitHub runner süreleri yerel makineyle AYNI OLMAYACAKTIR. ADIMIN KENDİSİ GERÇEK BİR CI KOŞUSUNDA HENÜZ KOŞMADI — mutasyon kanıtı adımın shell'inin YEREL İKİZİNDE yapıldı (`if`/`working-directory`/`env` alanları YAML'dan okunarak doğrulandı, gövde `bash -n`den geçti), ama `FETCH_HEAD` yolu ancak gerçek `pull_request` olayında sınanır. BEŞ DOSYA DIŞI TARANMADI — aynı paylaşık-şifre kalıbının başka ailelerde de bulunup bulunmadığı ÖLÇÜLMEDİ ve yeni kapı bunu ancak o dosyalar DEĞİŞTİĞİNDE görecektir.
