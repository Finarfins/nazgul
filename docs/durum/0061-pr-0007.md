#7 — `verify-image-artifact` kimlik ayağı #6'da gerçek bir karşılaştırmaya bağlandı ama onu koruyan sözleşme metin grep'iydi; develop'a #5/#6/#9 indikten sonra bu dal yeniden ölçüldü (birleşme commit'i, çakışma yok, `origin/develop` ata) ve grep hâlâ kimliği yanlışken işi yeşil bırakıyordu: (CS1) doğrulama adımına `if: false` — K5 `^    if:` ile yalnız iş-seviyesini arıyordu, adım 8 boşlukla atlanır, sözleşme 83/0 YEŞİL; (CS7) aynı adıma ikinci bir `run: "true"` — dosyada asıl çağrı satırı durur, YAML son anahtarı kazanır, PyYAML parse `run="'true'"` verir, sözleşme YEŞİL; (CS11) `BASH_ENV` ile `exit 0` dosyası — betik kimliğe bakmadan exit 0 (ölçüldü), sözleşme YEŞİL; (CS12) adım `PATH`'ine `docker`'ı beklenen kimlik/revizyonu echo'layan sahte ikili — betik exit 0 (ölçüldü), sözleşme YEŞİL; (G2) betik `yerel-hesap-pro:<sabit>-baska` imajı yoksa `exit 0` — K8/K9 fikstürü o etiketi test süresince bırakıyordu, sözleşme YEŞİL, üretim SHA + yanlış kimlikte betik exit 0; (G4) `case "$IMAJ_REF" in *:01234567…40hex) ;; *) exit 0` — fikstür o sabiti kullanıyordu, sözleşme YEŞİL, üretimde exit 0; K1..K8 develop'ta dolu (K8 = `ci-gerekli-baglam-kapisi.py` / `K8_CIKTI`), bu yüzden davranışsal kapı K9 adını aldı; düzeltme: çağrı yeri `deploy/ci-verify-cagri-kapisi.py` ile YAML parse (yinelenen anahtar kırmızı, iş/adım anahtar kümesi kapalı, `run`/`env` parse edilmiş nesneden, kök `env`/`defaults` yok, artifact adı + `if-no-files-found: error`), K9 fikstürü her koşumda rastgele 40 hex ve ikinci imaj etiketsiz; kırmızı/yeşil her iki yön yeniden ölçüldü — temiz ağaç sözleşme YEŞİL, CS1/CS7/CS11/G2/G4 kirli ağaçta KIRMIZI (CS paketi Docker'sız statik parse ile kanıtlandı; G paketi yerel ortamda Docker daemon olmadığı için NOT MEASURED işaretlendi; CI container işinde davranışsal olarak koşan şey K9/K10 kapılarıdır, G-suite mutasyonları CI'da yeniden çalıştırılmadı). C4 bulgusu ve çalışma-anı çivisi: `deploy/deploy-sozlesme-testi.sh` içindeki 83 denetim kimliği `deploy/deploy-sozlesme-manifest.json` dosyasına donduruldu; yalnız statik metin ayrıştırmasının `if false` (M1) veya erken `exit 0` (M2) ile devre dışı bırakılan adımları kaçırdığı ölçüldü; bu nedenle `yesil` fonksiyonu koşan her denetimi çalışma anında `SUNGUR_DENETIM_DOSYASI` dosyasına ekleyecek şekilde güncellendi ve `container` işine `if: always()` ile koşan bağımsız `deploy/ci-deploy-sozlesme-kapisi.py --emitted` adımı eklendi; CI üzerinde açılan geçici PR'larla uçtan uca ölçüldü: M1 (PR #10, koşu 33253203306) ve M2 (PR #11, koşu 33253366613) koşullarında test betiği exit 0 dönse dahi doğrulama adımı 4 eksik K9 denetimini yakalayarak `container` işini KIRMIZI yaptı (temiz ağaçta 83/83 YEŞİL); KABUL EDİLEN ARTIK (ACCEPTED HOLE): K10 yalnız adı geçen denetimin KOŞTUĞUNU doğrular; denetim içindeki assertion mantığının anlamlı olduğunu DOĞRULAYAMAZ; `yesil` çağrısını koruyup koşulu `if true` yapan veya guard'ı kaldıran TEK bir dosyadaki TEK bir düzenleme (F1 bulgusu, ölçüldü) hem statik hem de çalışma-anı katmanını yeşil bırakır; bu durum K10'a ya da bu PR'a özgü olmayıp `backend/tests/test_ci_gates.py` dahil tüm test suiteleri için geçerlidir; CI tarafındaki hiçbir kod bu açığı kapatamaz, çünkü her denetimi denetleyen yeni bir kapı da aynı şekilde etkisizleştirilebilir; nihai çözüm `deploy/**` yolları üzerinde zorunlu insan incelemesi kuralıdır.
