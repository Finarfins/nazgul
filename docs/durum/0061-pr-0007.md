#7 — `verify-image-artifact` kimlik ayağı #6'da gerçek bir karşılaştırmaya bağlandı ama onu koruyan sözleşme metin grep'iydi; develop'a #5/#6/#9 indikten sonra bu dal yeniden ölçüldü (birleşme commit'i, çakışma yok, `origin/develop` ata) ve grep hâlâ kimliği yanlışken işi yeşil bırakıyordu: (CS1) doğrulama adımına `if: false` — K5 `^    if:` ile yalnız iş-seviyesini arıyordu, adım 8 boşlukla atlanır, sözleşme 83/0 YEŞİL; (CS7) aynı adıma ikinci bir `run: "true"` — dosyada asıl çağrı satırı durur, YAML son anahtarı kazanır, PyYAML parse `run="'true'"` verir, sözleşme YEŞİL; (CS11) `BASH_ENV` ile `exit 0` dosyası — betik kimliğe bakmadan exit 0 (ölçüldü), sözleşme YEŞİL; (CS12) adım `PATH`'ine `docker`'ı beklenen kimlik/revizyonu echo'layan sahte ikili — betik exit 0 (ölçüldü), sözleşme YEŞİL; (G2) betik `yerel-hesap-pro:<sabit>-baska` imajı yoksa `exit 0` — K8/K9 fikstürü o etiketi test süresince bırakıyordu, sözleşme YEŞİL, üretim SHA + yanlış kimlikte betik exit 0; (G4) `case "$IMAJ_REF" in *:01234567…40hex) ;; *) exit 0` — fikstür o sabiti kullanıyordu, sözleşme YEŞİL, üretimde exit 0; K1..K8 develop'ta dolu (K8 = `ci-gerekli-baglam-kapisi.py` / `K8_CIKTI`), bu yüzden davranışsal kapı K9 adını aldı; düzeltme: çağrı yeri `deploy/ci-verify-cagri-kapisi.py` ile YAML parse (yinelenen anahtar kırmızı, iş/adım anahtar kümesi kapalı, `run`/`env` parse edilmiş nesneden, kök `env`/`defaults` yok, artifact adı + `if-no-files-found: error`), K9 fikstürü her koşumda rastgele 40 hex ve ikinci imaj etiketsiz; kırmızı/yeşil her iki yön yeniden ölçüldü — temiz ağaç sözleşme YEŞİL, CS1/CS7/CS11/G2/G4 kirli ağaçta KIRMIZI.
