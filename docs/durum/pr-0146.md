#146 — H41 İZOLE KOŞUCU META-TEST SÜRESİ + ZAMAN AŞIMI HATA METNİ (yalnız test altyapısı, uygulama/göç/rota DEĞİŞMEDİ): develop `bf8e73f`de ÖLÇÜLDÜ — `tests/test_isolated_test_runner.py` tek başına koşucu altında 116.6 s (25 test, düz pytest 130 s), `--workers 4` altında 177-210 s (DEVIR-15), koşucunun 180 s dosya sınırında; genel `--timeout` YÜKSELTİLMEDİ; seçenek (a) BÖLME: testler fixture paylaşmıyor (her `run_test_files` çağrısı ~7 s'lik kendi şablonunu kuruyor), katı hızlı/yavaş bölme yavaş yarıyı ~130 s'de bırakırdı, bu yüzden temaya göre ölçülen süreyle dengelendi — `test_isolated_test_runner.py` toplama/kanonik manifest/shard 16 test, yeni `test_isolated_test_runner_yurutme.py` yürütme/hata atfı/kodlama/zaman aşımı 9+2 test; ZAMAN AŞIMI METNİ: `isolated_test_reporter` isteğe bağlı `ISOLATED_TEST_PROGRESS` (toplandı/başladı/bitti satırları) ve `ISOLATED_TEST_DEADLINE`+`ISOLATED_TEST_TRACEBACK` (kill'den min(5 s, sınır/4) önce `faulthandler` yığın dökümü) yazar, koşucu bunlardan çalışan test+süresi, toplanan/tamamlanan, en yavaş 5, yığın ve çıktı kuyruğunu kurar; satır içinde, son özette ve `--report-json` `results[].failure_text`te basılır; PG hattı değişkenleri vermediği için davranışı AYNI; mutasyonlar 4/4 KIRMIZI (`failure_text` düşürülür, faulthandler kurulmaz, `logstart` silinir, özet yalnız `reason`); `alt_surec_sql.txt` tarayıcı çıktısıyla satır yeniden adlandırıldı (`DATABASE_TEMPLATE_TEST` taşındı), 137 dosya/224 SABİT; ileri/ters sıra 16/16 ve 11/11 yeşil; `--workers 4` tam süit: taban `bf8e73f` (yüklü makine, 3475 s) eski dosya TIMEOUT 180.3 s 0 test (H41 yeniden üretildi; aynı koşuda ilgisiz 3 TIMEOUT + 1 FAIL makine yükü), dal iki koşu 474/474 dosya 3618 sonuç yeşil (1566 s ve 950 s), bölünen dosyalar 64.1/66.0 s ve 61.5/64.4 s — <merge tarihi>
