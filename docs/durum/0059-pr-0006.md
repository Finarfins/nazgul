#6 — `verify-image-artifact` işinin KİMLİK ayağı bir kapı DEĞİLDİ: adım `Verify loaded image identity and OCI revision` adını taşıyor, `image_id`'yi `docker image inspect --format '{{.Id}}'` ile hesaplıyor ve onu HİÇBİR ŞEYLE karşılaştırmıyordu — tek kontrol `[ -n "$image_id" ]`, yani "boş mu?"; `set -euo pipefail` altında `docker image inspect` imaj yoksa zaten patladığı için o satır pratikte hiçbir zaman kırmızı olamazdı, hesaplanan değer yalnızca `printf` ile yazdırılıyordu; İDDİA DÜZELTİLDİ — iş BÜTÜNÜYLE "green by construction" DEĞİLDİR ve bu ayrım önemlidir: `revision` ayağı GERÇEK bir karşılaştırmadır (`[ "$revision" = "${{ github.sha }}" ]`), indirme ve `docker load` adımları da kırılabilir, kör olan yalnız kimlik ayağıydı; ASIL BULGU ise eksik bir satır değil eksik bir BEKLENTİDİR — karşılaştırılacak değer depoda HİÇBİR YERDE yoktu, `container` işi test ettiği imajın kimliğini dışarı hiç bildirmiyordu, bu yüzden düzeltme önce beklentiyi ÜRETMEK zorundaydı: `container.outputs.tested_image_id` (`docker save`in girdisiyle AYNI nesneden okunur) eklendi ve `verify-image-artifact` yüklediği imajın kimliğini buna eşitliyor, eşit değilse İKİ DEĞERİ DE adlandırıp exit 1 veriyor; beklenti BİLEREK artifact'in İÇİNDE taşınmıyor — tarball'ın yanında taşınsaydı takas edilmiş bir tarball kendi kimliğini de beraberinde getirir ve kapı yine yeşil kalırdı, yani kapı kendi kopyasına bakardı (bu depo o sınıftan bir kapıyı zaten görmüştü: kendi mock'una iddia eden beş yeşil frontend testi); ÖLÇÜLDÜ (gerçek Docker 29.6.1, simülasyon değil): (1) `docker save` → `docker load` `.Id`'yi AYNEN korur, dolayısıyla karşılaştırma iyi tanımlıdır; (2) AYNI etikete ve AYNI `org.opencontainers.image.revision` etiketine sahip AMA BAŞKA bir imaj yüklendiğinde ESKİ adım exit 0 veriyordu — imajın gerçekten başka olduğu içerikle kanıtlandı (`/marker` = `SWAPPED-NEVER-TESTED`, doğrusu `TESTED-BY-CONTAINER-JOB`) — YENİ adım aynı girdide exit 1 verdi ve iki ID'yi de adlandırdı; (3) `container` kimliği bildirmediğinde (boş çıktı) yeni adım fail-closed exit 1 veriyor; OCI revision etiketi bu boşluğu KAPATMAZ çünkü etiket imajın İÇERİĞİNE bağlı değildir; HER İKİ YÖN GERÇEK CI'DA DA KOŞTURULDU (yalnız yerel benzetim değil): YEŞİL — bu PR'ın kendi koşusunda `verify-image-artifact` geçti ve log `expected_image_id="sha256:6ba81f78…"` ile `artifact_image_id=sha256:6ba81f78…` eşitliğini gösteriyor, yani `needs.container.outputs.tested_image_id` ifadesi runner'da GERÇEKTEN çözülüyor (boş değil); KIRMIZI — ATILACAK bir dalda `container`, kimliği BİLDİRDİKTEN SONRA kaydedilecek imajı bilerek değiştirdi (`FROM <imaj>` + `LABEL kirmizi-kanit=1`; `FROM` revision etiketini MİRAS ALIR, bu yüzden revision ayağı yeşil kaldı ve kırmızıyı üreten TEK şey kimlik eşitsizliği oldu) ve `verify-image-artifact` exit 1 verdi: `##[error]artifact imaji container'in TEST ETTIGI imaj degil: yuklenen='sha256:dbc1b5d3…' beklenen='sha256:6bd108e4…'` (koşu 33201568659; dal ölçümden sonra silindi, koşu kaydı duruyor); sözleşme: `deploy/deploy-sozlesme-testi.sh` K5 genişletildi (job çıktısının bildirimi, `expected_image_id` ataması ve `[ "$image_id" = "$expected_image_id" ]` karşılaştırması sabit-dizge olarak aranıyor) ki kimlik karşılaştırması sessizce silinemesin — genişletilmiş K5 develop `37f7af7` üzerinde KIRMIZI, bu dal üzerinde YEŞİL ölçüldü, K2/K3/K4/K6 ile `ci-yayin-needs-kapisi.py` (K1) ve `ci-postgresql-shard-kapisi.py` (K7) etkilenmedi ve yeşil kaldı; DONDURULMUŞ KAYIT HAREKET ETMEDİ: `test_durum_kaydi.BEKLENEN_SINIR_IZI` `ci.yml` içinde YALNIZ `durum-kaydi` deklare-edilmiş-sınır bloğunu özetler ve bu turdaki değişiklikler `container`/`verify-image-artifact` bloklarındadır, ölçüldü ve iz değişmedi; taban develop `37f7af7` izole koşum (CPython 3.12.10, sıralı, Windows 11) 361 dosya / 360 dosya geçti olarak DOĞRULANDI (devralınmadı) ve tek başarısız dosya `test_platform_backups.py` DEVELOP'TA DA başarısızdır — nedeni depo değil platformdur (`WinError 5` ile `offline.sqlite.restore.tmp` → `offline.sqlite` yer değiştirmesi Windows dosya kilidine takılıyor), bu dalda da AYNI tek dosya başarısızdır, yani bu turun eklediği başarısızlık YOKTUR
