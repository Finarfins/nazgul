#80 — WA1 GİRİŞİ: Meta webhook'u ve yazdığı PLATFORM kuyruğu (göç `20260910_0078`, down_revision `20260909_0077`); ÖLÇÜLEN EKSİK: WA3 (#77/#78) niyet çözücüyü getirdi ve başlığında "veritabanı yok, tablo yok, rota yok" diyordu — ölçüldü, `app/routers/` altında `whatsapp` YOKTU ve `alembic/versions/` altında `wamid` sütunlu tek bir tablo bile YOKTU; iki yeni tablo (`whatsapp_inbound`, `whatsapp_pairing_attempts`) İKİSİ DE PLATFORM TABLOSU — `company_id` YOK ve bu bilinçli: webhook'a gelen mesaj henüz hiçbir firmaya ait değildir, gövdedeki `from` bir kimlik iddiası değil bir dizedir; `TENANT_TABLES` envanteri göç edilmiş şemadan `company_id` taşıyan tabloları TARAYARAK türetildiği için muafiyet elle yazılmadı (sütunun YOKLUĞU muafiyetin kendisi) ve envanter 116'da SABİT kaldı — iki kapı aynı olguyu iki yerden tutuyor (statik kaynak + GERÇEK PG kataloğu); `wamid` UNIQUE idempotency'nin KENDİSİ (hakem uygulama değil veritabanı; "önce SELECT" yarışa açık), `status` CHECK'li kapalı küme RECEIVED/PROCESSING/ANSWERED/IGNORED/DEAD — kaynaktaki `FAILED` adı DEAD'e ÇEVRİLDİ çünkü bu depoda `FAILED` outbox'ta YENİDEN DENENEBİLİR anlamında kullanılıyor ve iki kuyruğun aynı sözcüğü ters anlamda kullanması süpürücüyü yazan kişinin yapabileceği en sessiz hatadır; UÇLAR +2 TEK yol (GET/POST `/api/whatsapp/webhook`), TAM YOL `PUBLIC_API`de (önek DEĞİL — bu router'a yarın eklenecek bir yönetim ucu sessizce oturumsuz açılmasın) ve CSRF için AYRI liste YOK, ÖLÇÜLDÜ: CSRF denetimi `security_and_audit`in `PUBLIC_API`yi eleyen dalının İÇİNDE (`/api/auth/login` ile birebir aynı biçim); GET sabit zamanlı `hub.verify_token` + `text/plain` challenge, POST ise JSON AYRIŞTIRILMADAN ÖNCE HAM GÖVDE üzerinde `X-Hub-Signature-256` (sıra AST ile çivili), uca özel 256 KiB sınırı → 413 (küresel 2 MiB bu uç için çok geniş, küreseli daraltmak içe aktarma uçlarını kırardı), yabancı `phone_number_id` → 200 + sessiz eleme (4xx Meta'da teslimat fırtınası üretir), imzasız ve imzası bozuk istek AYNI kodu alır (403; ayrım sızdırılmaz), üç ayardan biri bile boşken iki uç da 404 (403 kurulumun WhatsApp taşıdığını SÖYLERDİ) — yani bu PR kendi başına HİÇBİR ŞEYİ AÇMAZ; İŞÇİ, CEVAP, EŞLEŞTİRME ve GİDEN MESAJ BU PR'DA YOK, satırlar `RECEIVED` kalır; KAPILAR: alembic başı 5 dosyada 0077→0078, rota sözleşmeleri 382/293→384/294 ve GET envanteri 182→183 (parmak izleri EN SON türetildi, dosyaların notundaki sıraya göre), yeni `ROUTE_REASONS` grubu (public Meta webhook, ham gövde üzerinde HMAC, oturum ve kiracı bağlamı yok), `tests/pins/pg_twins.txt` +1 satır ve `alt_surec_sql.txt` KIMILDAMADI (göç turu betiği SQL fiili taşımıyor), `test_undeniable_endpoint_population`a DÖRDÜNCÜ ÇAPA SINIFI — ÖLÇÜLDÜ: `_gated_operations()` `PUBLIC_API` üyelerini sessizce atıyordu, yani `PUBLIC_API`ye eklenen YAZAN bir uç o dosyanın HİÇBİR kuralına girmiyordu (kapının koşmaması doğru, GÖRÜNMEZLİK kusurdu); kural ikinci evrene yazıldı (`PUBLIC_SESSION_WRITE_EXEMPTIONS` + `PUBLIC_WEBHOOK_EXEMPTIONS`), kapsam ÜÇ evrende birden ölçülüyor (dördüncü bir kaçış yolu KIRMIZI olur) ve webhook'un imza kapısı GERÇEK isteklerle kanıtlanıyor — YÖN E geçerli imza + OTURUMSUZ istemci → 200, YÖN F bozuk imza → 403, YÖN G imzasız → 403 aynı kod; `types.gen.ts` yeniden üretildi, fixture'lar kımıldamadı; TESTLER `tests/test_wa1_ingress.py` (20) + `test_wa1_ingress_postgresql.py` (8, GERÇEK PostgreSQL 16'da koştu: 20 eşzamanlı aynı `wamid` → tam BİR satır ve HİÇ istisna, `status` CHECK'i gerçekten ısırıyor, `company_id` göç edilmiş KATALOGDA yok, `TIMESTAMPTZ`, göç turu up→down→up gerçek diyalektte kapanıyor); TABAN `origin/develop` `4d30597`; kayıt adı `python scripts/durum.py --sonraki 80`
