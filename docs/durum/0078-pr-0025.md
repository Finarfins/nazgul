#25 — BİTKİ ADI KATLAMASI TÜRKÇE OLDU ve ÖNİZLEME İLE SUNUCU İLK KEZ AYNI KURALI UYGULUYOR (`_bitki_katla` / `bitkiKatla`, göç YOK, yeni uç YOK, SQL metnine HİÇ dokunulmadı); TABAN BU TURDA ÜÇ KEZ DEĞİŞTİ ve TABANI DEĞİŞEN ÖLÇÜM ÖLÇÜM DEĞİLDİR: dal önceki turda `7498ab3` üzerine oturuyordu, ardından #26 (xmldom kilidi), #21 (donmuş saat), #24 (tarla testleri) ve #27 (`units.py` + göç `20260902_0066` + PostgreSQL çivisi 100) develop'a indi ve taban `f623190` oldu; İLK BİRLEŞME İTİLDİKTEN SONRA #28 develop'a İNDİ ve taban BİR KEZ DAHA, `23b11ef`e taşındı — ikinci birleşme de `--no-ff` yapıldı ve o da ÇAKIŞMASIZDI; #28'in dokunduğu dosyalar ÖLÇÜLDÜ (`git diff --name-only f623190 origin/develop`): `backend/donmus_saat.py`, `backend/tests/test_donmus_saat_kapisi.py` ve `docs/durum/0076-pr-0028.md` — yani #28 ne `farm.py`ye ne koruma testine ne göçlere ne de PostgreSQL nüfusuna DOKUNDU, dolayısıyla aşağıdaki çivilerin HİÇBİRİ bu ikinci hareketten etkilenmedi ve yeniden koşularak DOĞRULANDI; birleşme `--no-ff` ile yapıldı ve ÇAKIŞMA SIFIRDI — önceki turda `test_tenant_scoping_guard.py` çakışmıştı, bu turda hiçbir dosya çakışmadı ve sebebi ÖLÇÜLDÜ: `git diff --stat 7498ab3 origin/develop -- backend/app/routers/farm.py` BOŞ, yani develop bu dilimin dokunduğu üretim dosyasına HİÇ dokunmadı; KAYIT İKİ KEZ NUMARALANDI ve YARIŞ KAYBEDİLDİ: önce 0072'den 0076'ya alındı (eski ad ÇAKIŞIYORDU çünkü `0072-pr-0026.md` develop'a #26 ile inmişti), ama 0076'yı PR #28 de istiyordu ve ÖNCE O BİRLEŞTİ — `0076-pr-0028.md` develop'a indi, `durum-kaydi` kapısı ilk koşuda (run 33794149103) tam bu yüzden 7 saniyede KIRMIZI verdi; kapı HAKLIYDI, kayıt ikinci birleşmeden sonra `scripts/durum.py --sonraki 25`in yazdırdığı `0077-pr-0025.md`ye alındı; ARDINDAN TABAN ÜÇÜNCÜ KEZ HAREKET ETTİ ve YARIŞ BİR KEZ DAHA KAYBEDİLDİ: PR #30 develop'a indi, taban `23b11ef`ten `fa3d493`e taşındı ve 0077'yi #30 da istiyordu — `0077-pr-0030.md` ÖNCE develop'a indiği için kayıt `scripts/durum.py --sonraki 25`in yazdırdığı `0078-pr-0025.md`ye alındı: develop'taki EN YÜKSEK kayıt artık 0077 (`0077-pr-0030.md`), BU KAYIT 0078'DİR; #30'un dokunduğu dosyalar ÖLÇÜLDÜ (`git diff --name-only 23b11ef origin/develop`): `backend/tests/test_field_stok_zamanlayici.py`, `backend/tests/test_self_service_exemption.py`, `backend/tests/test_supplier_price_bridge_units.py` ve `docs/durum/0077-pr-0030.md` — bu dilimin 6 dosyasıyla KESİŞİM BOŞ, üçüncü birleşme de `--no-ff` yapıldı ve ÇAKIŞMASIZDI, dolayısıyla aşağıdaki ölçümlerin ve çivilerin HİÇBİRİ bu üçüncü hareketten etkilenmedi; `farm.py` parmak izi `bd42a626`da SABİT, dinamik `text()` 15te ve küresel toplam 254te SABİT; ÖLÇÜLEN KUSUR: göç 0063 (PR #18) kataloğu açarken bitki eşleşmesini bilerek SQL `LOWER()`ında değil PYTHON'da bırakmıştı ve gerekçesi doğruydu, ama seçilen Python katlaması `casefold()`TU ve `casefold()` TÜRKÇE BİLMEZ: `"İNCİR".casefold()` = `i` + U+0307 (birleşen üstteki nokta) verir ve bu `incir`e EŞLEŞMEZ, dolayısıyla katalog satırı Türkçe klavyeyle BÜYÜK yazılmış girdiyle BULUNAMIYORDU; ön yüz ise aynı işi `toLocaleLowerCase('tr')` ile yapıyordu, yani ÖNİZLEMENİN GÖSTERDİĞİ SATIR İLE SUNUCUNUN BULDUĞU SATIR tam İ karakterinde AYRILABİLİYORDU ve iki diyalektin aynı kataloğu farklı çözmesi, ikizi koşulan bir testin YAKALAYAMAYACAĞI bir sapmadır; KATLAMA ÖLÇÜMLERİ ÖNCEKİ TURDA, `7498ab3` BİRLEŞMİŞ AĞACI ÜZERİNDE ALINDI ve BU TURDA YENİDEN ALINMADI çünkü alınması GEREKMİYORDU: `farm.py` de `farmApi.ts` de taban hareketinden ETKİLENMEDİ (yukarıdaki boş diff) — `7498ab3` üzerinde ÖLÇÜLDÜ ki Türkçe klavyede BÜYÜK yazılmış 18 gerçek bitki adından `casefold()` ile yalnız 7'si kendi küçük hâlini buluyordu (`MISIR`/`mısır`, `BİBER`/`biber`, `PATLICAN`/`patlıcan`, `İNCİR`/`incir` hepsi ıskalıyordu), yeni katlama ile 18'inin 18'i buluyor ve `İstanbul`/`istanbul` çifti artık EŞLEŞİYOR, develop'ta EŞLEŞMİYOR; yine `7498ab3` üzerinde (CPython 3.12.10 / Node v24.18.0, küçük harfe çeviren 1616 kod noktası tek tek iki tarafa verilerek) `lower()` temelli bu katlamanın JavaScript'ten 0 KOD NOKTASINDA, `casefold()` temelli olanın 297 kod noktasında ayrıldığı ve U+00DF'nin o 297'nin İÇİNDE olduğu ÖLÇÜLDÜ, yani teste seçilen karakter divergans kümesinin GERÇEK bir üyesidir, temsilî değildir; TESTİN KENDİSİ MUTASYONLA BU TURDA YENİDEN ÖLÇÜLDÜ (`f623190` birleşmiş ağacı üzerinde; #28 bu dosyalara dokunmadığı için `23b11ef` sonrasında yeniden ölçülmesi gerekmedi): `_bitki_katla`nın sonu `lower()`dan `casefold()`a çevrildiğinde `tests/test_farm_bku_katalogu.py:98`deki `assert not _bitki_esit("Weißkohl", "weisskohl")` DÜŞÜYOR (`AssertionError: assert not True`, `test_katalog_bitki_eslesmesi_dialektten_bagimsiz`) ve o satır dosyadaki TEK başarısızlıktır (`1 failed, 1 passed`) — üstündeki bütün İ/ı iddiaları mutasyon altında da GEÇİYOR çünkü onları açık `.replace()` zinciri taşıyor, yani `lower()` ile `casefold()` arasındaki farkın TEK nöbetçisi o satırdır; mutasyon geri alındı ve geri alındığı ÖLÇÜLDÜ: `farm.py` AST parmak izi `88cc25ec…`e döndü ve `git status --porcelain` yalnız kayıt yeniden adlandırmasını gösterdi; İLK YAZILAN TEST KÖRDÜ ve DEĞİŞTİRİLDİ: iddia önce `WEISSKOHL` (BÜYÜK) biçimindeydi ve ÖLÇÜLDÜ ki o biçim iki katlama altında da `False` verir — içindeki `I` Türkçe katlamada `ı`ya iner ve ß farkını MASKELER — yani test kuralı ayırt etmeden GEÇERDİ; ayırt eden çift (`Weißkohl`/`weisskohl`, BÜYÜK HARF İÇERMEYEN) onun yerine kondu ve o çift `casefold()` altında `True`, `lower()` altında `False` verir; SIRA ÖNEMLİ: `I` + U+0307 dizisi ÖNCE `i`ye indirilir, çıplak `I` `ı`ya inmeden — ters sırada hiçbir yerde bulunmayan `ı` + U+0307 üretilirdi ve bu da teste yazıldı; İKİ KATLAMA VAR ve BİLEREK BİRLEŞTİRİLMEDİLER, KARAR BUDUR: #27 ile develop'a `backend/app/units.py` ve içinde `turkce_katla()` indi; o fonksiyon YUKARI katlıyor (`metin.strip().replace("i", "İ").upper()`), bu dilimin `_bitki_katla`sı AŞAĞI katlıyor (`.lower()`) ve İKİSİ AYNI DENKLİĞİ ÜRETMİYOR — ÖLÇÜLDÜ (CPython 3.12.10, `f623190` birleşmiş ağacı): (a) `"Weißkohl"` (U+0057 U+0065 U+0069 U+00DF U+006B U+006F U+0068 U+006C) ile `"weisskohl"` (U+0077 U+0065 U+0069 U+0073 U+0073 U+006B U+006F U+0068 U+006C) YUKARI katlamada EŞİT (`True`), AŞAĞI katlamada DEĞİL (`False`), çünkü `.upper()` ß'yi `SS`e AÇAR; (b) `"I"` + U+0307 + `"stanbul"` (U+0049 U+0307 U+0073 U+0074 U+0061 U+006E U+0062 U+0075 U+006C) ile `"İstanbul"` (U+0130 U+0073 U+0074 U+0061 U+006E U+0062 U+0075 U+006C) AŞAĞI katlamada EŞİT (`True`), YUKARI katlamada DEĞİL (`False`), çünkü ayrıştırılmış nokta YALNIZ aşağı katlamada yeniden birleşiyor; ÖRTÜŞTÜKLERİ YER DE VAR ve TUZAK BUDUR: `"İncir"`/`"incir"` İKİSİNDE de `True`, yani tek bir örnek çifte bakıp "aynılar" sonucuna varmak MÜMKÜNDÜR ve YANLIŞTIR; İKİ KATLAMA AYRI ALANLARA HİZMET EDİYOR — `_bitki_katla` SERBEST METİN bitki adı EŞİTLİĞİ içindir ve sözleşmesi ön yüzün `toLocaleLowerCase('tr')`ıyla İKİZ kalmaktır, `turkce_katla()` ise KAPALI bir kümedeki BİRİM KODU aramasıdır ve sözleşmesi kanonik BÜYÜK biçimdir — bu yüzden bu dilim `units.turkce_katla`yı NE ÇAĞIRIYOR NE de ona karışıyor; ileride birleştirmek yukarıdaki iki çiftin YÖNÜNÜ DEĞİŞTİRİR, yani DAVRANIŞ DEĞİŞİKLİĞİDİR ve KENDİ PR'ını, kendi kayıp analizini gerektirir; aynı gerekçe `farm.py`de `_bitki_katla`nın üstüne `#` yorumu olarak da yazıldı — docstring DEĞİL, çünkü `ast.dump` yorum taşımaz ve açıklamanın parmak izini KIMILDATMAMASI istendi (ÖLÇÜLDÜ: yorum eklendikten SONRA da parmak izi `88cc25ec…`); BİLEREK KAYBEDİLEN, AÇIKÇA: Türkçe'de `I`nın küçüğü `ı`dır, dolayısıyla LATİN yazımlı `I` taşıyan bir bitki adı TERS yönde bozulur — `Iceberg` (marul) ve `Italyan Çimi` develop'ta `iceberg` / `italyan çimi` satırına EŞLEŞİYOR, bu PR'dan sonra EŞLEŞMEYECEK ve bu ÖLÇÜLDÜ (develop `casefold()`: her ikisi `True`; bu dal: her ikisi `False`); OKUYUCUNUN BİLMESİ GEREKEN TİCARET BUDUR ve iki başarısızlık yönü de AYNI TARAFA, İZİN VERİCİ tarafa düşer: eşleşmeyen bir katalog satırı bitkiye ÖZEL PHI yerine bitkiden BAĞIMSIZ PHI'ye ya da HİÇBİR ŞEYE düşer, yani hasat DAHA ERKEN serbest kalır — kilit sıkılaşmaz, GEVŞER, ve kaybedilen yön de kazanılan yön de bu özelliği paylaşır; kayıp kabul edildi çünkü (a) ürün Türkçe ve baskın girdi Türkçe klavyedir (ölçülen 7/18 -> 18/18 kazanç, iki adlık kayba karşı), (b) ÇEŞİT adları (`Isabella` gibi) `crop` sütununda DEĞİL ayrı `variety` sütununda durur ve bu karşılaştırmaya HİÇ uğramaz; İKİZLİK TEK YERDE: ön yüzdeki kural artık `catalogueMatch` içine gömülü değil, dışa açılan `bitkiKatla` fonksiyonundadır ve testi doğrudan onu çağırır, çünkü çağrı yerine gömülü bir kuralı ikizinden ayrı düşürmek sessizdir; ÖN YÜZ KIRMIZISI BU TURDA KAPANDI ve SEBEBİ ÖLÇÜLDÜ: develop'a #23 ile inen `PlantProtectionProduct` tipi `origin` ve `origin_reference` alanlarını ZORUNLU tutuyor, bu dalın fikstürü ise o değişiklikten ÖNCEYDİ; `vitest` bunu GÖREMEZ çünkü TİP DENETİMİ YAPMAZ, gerçek kapı `tsc -b`dir (`frontend/tsconfig.json` yalnızca `references` taşır, `files: []`) ve fikstür düzeltmesi `adb8e68`de zaten inmişti; DÜZELTMENİN YÜK TAŞIDIĞI MUTASYONLA ÖLÇÜLDÜ: `origin: 'MANUAL' as const, origin_reference: null,` satırı `farmApi.test.ts`ten çıkarıldığında `tsc -b --force` KIRMIZI oluyor (`TS2345` ve `TS2739`, "missing the following properties from type 'PlantProtectionProduct': origin, origin_reference", dört hata, çıkış kodu 2), geri konduğunda YEŞİL (çıkış 0); `--force` BİLEREK kullanıldı çünkü `tsc -b` ARTIMLIDIR ve duran `tsconfig.app.tsbuildinfo` YEŞİL SANRISI verebilirdi; ayrıca `tsconfig.app.json`ın `include`u `["src"]`tir, yani fikstür dosyası kapının İÇİNDEDİR; ÇİVİLER BİRLEŞMİŞ AĞAÇ ÜZERİNDE YENİDEN TÜRETİLDİ, ÖNCEKİ TURUN SAYILARI TAŞINMADI ve HER BİRİNİN GEREKÇESİ AYRI YAZILDI çünkü bir "geçsin diye" düzenlemeyi yakalayacak olan onlardır: `farm.py` dinamik `text()` sayısı 15'te SABİT — bu dilim SQL metnine HİÇ dokunmadı, değişen tek şey saf Python; küresel dinamik `text()` toplamı 254'te SABİT — #27 `units.py`yi getirdi ama izin listesine dinamik çağrı EKLEMEDİ; `farm.py` AST parmak izi `88cc25ec…`de SABİT (develop'unki `8635fc6d…`) — aradaki tek fark `_bitki_esit`in gövdesi ile yeni `_bitki_katla`dır; `EXPECTED_UNDENIABLE` 99'da SABİT — yeni uç YOK, yetki nüfusu kımıldamadı; `GET_INVENTORY_COUNT` 166'da SABİT — yeni GET rotası YOK; alembic başı `20260902_0065`ten `20260902_0066`ya KIMILDADI — DEVELOP'UN göçüdür (#27 birim dönüşümü), bu dilimin göçü YOKTUR ve çivi yalnız taban taşındığı için taşındı; PostgreSQL test nüfusu 99'dan 100'e KIMILDADI — DEVELOP'UN ikizidir (`test_birim_donusumu_postgresql.py`) ve ÖLÇÜLDÜ: 98 `test_*postgresql*.py` glob + 2 adlandırılmış özel dosya = 100, bu dilim PG ikizi EKLEMEDİ; `TENANT_TABLES` artık `product_unit_factors` İÇERİYOR — DEVELOP'UN tablosudur (#27), bu dilim tablo eklemedi; HESAP MAKİNESİ ÖNCE DOĞRULANDI, SONRA KULLANILDI: parmak izi `ast.dump` çıktısına bağlıdır ve kayıtlar CPython 3.12.3'e çivilidir, bu tur 3.12.10 ile koşuldu, bu yüzden ÖLÇÜM ALINMADAN ÖNCE develop'un KENDİ `farm.py`si aynı yorumlayıcıdan geçirildi ve develop'un kayıtlı çivisi `8635fc6d68d781b0…` BİREBİR yeniden üretildi — yani bu turda alınan `88cc25ec…` güvenilirdir; SQL `LOWER()` BU İDDİALARI TAŞIYAMAZ ve bu da teste yazıldı (`"İNCİR".lower() != "incir"`, `"MISIR".lower() != "mısır"`): SQLite'ın `LOWER`ı ASCII dışına DOKUNMAZ, yani karşılaştırmanın Python'da durması bir tercih değil ZORUNLULUKTUR ve bu dilim 0063'ün gerekçesini ÇÜRÜTMÜYOR, TERSİNE ONU TESTE YAZIYOR; KIRPMA ASİMETRİSİ BAKILDI ve KUSUR DEĞİL: `bitkiKatla` kırpmayı KENDİ İÇİNDE yapar (`.trim()`), `_bitki_katla` yapmaz ama Python'daki İKİ çağrı yerinin de girdisi çağrı ÖNCESİNDE `.strip()`lenmiştir (`farm.py:2382`, `farm.py:2399` — satır numaraları bu turda eklenen yorum bloğu yüzünden 25 satır kaydı ve YENİDEN OKUNDU), yani davranış aynıdır; ÖLÇÜLEN KOŞULAR (hepsi `23b11ef` birleşmiş ağacı üzerinde ve YEREL, #28 indikten SONRA yeniden koşuldu): `tests/test_tenant_scoping_guard.py` + `tests/test_ci_gates.py` 44/44 geçti, açıklama yorumu eklendikten sonra bu ikisi `tests/test_farm_bku_katalogu.py` ile birlikte 46/46 geçti, `tests/test_authorization_population_reconciliation.py` + `tests/test_route_get_permission_inventory.py` + `tests/test_farm_bku_katalogu.py` 17/17 geçti, `tests/test_farm_*.py` 33/33 geçti, ön yüzde `tsc -b --force` çıkış 0 verdi ve `vitest run` 89 dosyada 611/611 geçti; ÖLÇÜLMEDİ: AKSAN KATLAMASI BU DALDA BİLEREK ELE ALINMADI ve alınmamalıydı — depo bugün hem `Buğday` hem `Bugday`, ayrıca `Patlican` tutuyor, yani operatörler ASCII'ye indirgenmiş Türkçe yazıyor ve HİÇBİR harf-durumu kuralı bunu düzeltmez (`Bugday` ile `Buğday` bu PR'dan sonra da eşleşmez); bu ölçüldü ve DOKUNULMADAN bırakıldı çünkü aksan katlaması ayrı bir karardır ve kendi kayıp analizini gerektirir; ÖLÇÜLMEDİ: `units.turkce_katla` ile `_bitki_katla`nın BİRLEŞTİRİLMESİNİN BİRİM KODU tarafına ne yapacağı — yukarıda iki çiftin yön değiştirdiği ÖLÇÜLDÜ ama birleştirilmiş bir katlamanın birim aramasındaki etkisi ölçülmedi, çünkü bu dilim o kararı BİLEREK almadı; ÖLÇÜLMEDİ: bu dalın kendi PostgreSQL ikizi YOKTUR ve gerekmedi — karşılaştırma SQL'e hiç inmiyor, saf Python'da duruyor, dolayısıyla diyalekt farkı bu iddiaya DOKUNAMAZ; BAĞIMSIZ LENS ÖLÇTÜ (bu tur, ben DEĞİL): `0 kod noktasında ayrılıyor` iddiası YALNIZ CPython 3.12'nin (UCD 15.0) harf-durumu DEĞİŞTİRDİĞİ kod noktaları için geçerlidir — TÜM BMP taranınca 8 kod noktası AYRILIYOR (U+1C89, U+A7CB, U+A7CC, U+A7CE, U+A7D2, U+A7D4, U+A7DA, U+A7DC: Unicode 16 eklemeleri, Node'un ICU'su küçültüyor, CPython küçültmüyor) ve tarama kümesi Python'un DEĞİŞTİRDİKLERİYLE tanımlandığı için bu sapmaları YAPISAL OLARAK göremez; sapma İZİN VERİCİ yöne düşüyor (sunucu, ön yüzün bulduğu satırı ıskalar ve bitkiden BAĞIMSIZ PHI'ye ya da hiçbir şeye düşer), yani kilit yine GEVŞİYOR, sıkışmıyor; ÖLÇÜLMEDİ: tam CI matrisi (izolasyon koşucusuyla tam backend taraması, PostgreSQL shard'ları, `lint`, sözleşme yeniden üretimi) bu turda YEREL olarak koşulmadı ve CI'a bırakıldı.
