#5 — Arındırmanın ETRAFINDAKİ kapının üç boşluğu: PR #4'ün eklediği arındırma DOĞRUDUR ve bu turda ona DOKUNULMADI (mercek gerçek PG üzerinde altı gerçek başarısızlık şekli üretti ve hiçbirinde sızıntı ölçemedi); kapatılan şey onu KORUYAN kapının görmediği üç yer — üçü de bugün bir şey sızdırmıyor, üçü de gelecekteki bir düzenlemenin sızıntıyı HER TEST YEŞİLKEN yeniden açmasına izin veriyordu; (b) ASIL KUSUR, arındırma metni bir İŞARETTEN keser (`_HAM_ISTISNA_ONEKI`, uçta) ama o işareti yazan biçim dizgisi BAŞKA modüldedir (`app/field_stok_tuketici.py`) ve arada HİÇBİR BAĞ YOKTU — ÖLÇÜLDÜ: tüketicinin literalindeki tek kelime değiştirildiğinde (`beklenmeyen`→`beklenmedik`) 49 `test_field_stok_*` testi ve yüzeyin 4 testi YEŞİL kaldı (23 passed) ve sızıntı uçtan uca YENİDEN AÇILDI, kanarya bunu göremez çünkü öneği KENDİ kopyasından yazar; iki şekil arasından TÜKETİCİYİ GERÇEKTEN KOŞTURMAK seçildi (statik "biçim dizgisi öneği içeriyor mu" iddiası bir seviye yukarısını kaçırırdı: biçim dizgisi doğru kalıp birleştirme yeri değişse sütuna GİREN metin önekle başlamaz ve statik kapı yine yeşil kalırdı) ve kullanılan hata UYDURMA DEĞİL göç 0060'ın kısmi benzersiz indeksidir (`uq_stock_movements_field_event`) — çakışan hareket önceden yazılır, tüketicinin `INSERT`i veritabanınca reddedilir, gerçek PG 16.13'te sütuna kısıt ADI + GERÇEK satır anahtar DEĞERLERİ + ham SQL giriyor ve kapı VAKUM KARŞITIDIR (metnin önekten sonra gerçekten ham ayrıntı taşıdığı aynı koşumda ölçülür); (a) kanarya yalnız bir `DEAD` satır dikiyordu ve bu dağılımın YANLIŞ ucuydu — gerçek şekillerin çoğu `DEAD`e HİÇ VARMAZ, `_kurtar` tavan dolmadan `_denemeyi_kaydet` çağırır ve olay `PENDING` kalır (koşum bunu bağımsız doğruladı: gerçek hata `RETRY_SCHEDULED`/`PENDING`/`attempts=1` üretti), ÖLÇÜLDÜ: arındırma `if status == "DEAD"` ile daraltıldığında kanarya ve API testi YEŞİL kalıyordu (4 passed) ve `PENDING` satır ham metnin TAMAMINI sunuyordu, fikstüre artık AYRI kanarya parçaları taşıyan bir `PENDING` satır da dikiliyor (parçaların ayrı olması bilinçli: kırmızının etiketi hangi taşıyıcının sızdırdığını DOĞRU söylesin diye); (c) uç "ham istisna `last_error`e TEK yazım yerinden girer" diyordu ve bu YANLIŞTI, İKİ yer var — ikincisi `default_warehouse` çağrısını saran `except RuntimeError` kolu ve `str(hata)`yı ÖNEKSİZ yazar, yani arındırmanın YANINDAN geçer; bugün zararsız (o çağrının TEK `raise`i sabit `RuntimeError("Aktif depo bulunamadı")` ve SQLAlchemy/psycopg hataları `RuntimeError` DEĞİLDİR) ama bu, güvenlik savının TAMAMINI başka bir modüldeki SINANMAYAN bir olguya dayandırıyordu: iddia düzeltildi ve o yol artık gerçekten koşturulup sunulan metnin TAM OLARAK o sabit cümle olduğu ve içinde SQL/istisna/kısıt izi bulunmadığı ölçülüyor; BİRLEŞİK KONTROL — üç ekleme yerindeyken `DEAD` daraltması artık İKİ kapıyı birden kırmızıya çeviriyor (kanarya: sentetik `PENDING` taşıyıcı sızdı; önek bağlantısı: GERÇEK bir veritabanı hatasından doğan ham metin HTTP yanıtında göründü), önceden dördü de yeşildi; dondurulan kayıtlar: `BEKLENEN_ALT_SUREC_SQL_DOSYA/METIN` 100/161→102/163 (iki YENİ kapı dosyası alt süreçte koşuyor; mevcut kanaryaya eklenen `PENDING` satır sayacı HAREKET ETTİRMEDİ çünkü gömülü SQL yine TEK senaryo sabitinin içinde; `app/` altında yine SIFIR), `routers/entegrasyon_olaylari.py` AST parmak izi ise BEKLENTİNİN AKSİNE DEĞİŞMEDİ ve bu ölçüldü — bu turdaki tek `app/` değişikliği YORUM satırlarıdır ve `ast.dump` yorumları TAŞIMAZ, kapı da bunu doğruladı (`test_every_dynamic_text_call_is_exactly_reviewed` geçti, yalnız alt süreç sayacı kırmızı oldu), dinamik `text()` çağrısı 3'te ve toplam 251'de sabit; CPython 3.12.3 ve PostgreSQL 16.13 ile ölçüldü, taban (645cc6d, izole koşum) 359 dosya / 1762 sonuç / hepsi geçti olarak DOĞRULANDI (devralınmadı)
