#8 — E2E ÇÖKME KAPISI ÇALIŞIYORDU AMA UYGULAMANIN ÇOĞU EKRANINA HİÇ UĞRAMIYORDU: `Insights` ekranına (`/analizler`) gerçek bir render çökmesi enjekte edildiğinde üç zorunlu kapı da yeşil kaldı — `npm run build` (tsc + vite) çökmeyi paketin İÇİNE koyarak geçti, `playwright test` 111 testle exit 0, `vitest run` exit 0 — buna karşılık AYNI çökme KAPSANAN bir ekrana (`/`, Pano) konduğunda e2e KIRMIZI oldu, yani düzenek (helpers.ts "Konsol temiz olmalı" fixture'ı) sağlamdı ve eksik olan tek şey ZİYARETTİ; bu tur yeni bir mekanizma KURMAZ, o fixture'ı ziyaret edilmeyen rotalara taşır: `frontend/e2e/rota-render-kapisi.spec.ts` tohum GEREKTİRMEYEN 30 rotayı (28 oturumlu + `/sifremi-unuttum` ve `/sifre-sifirla`) veri-güdümlü bir listeyle gezer ve her rotada ÜÇ ölçüm yapar: (1) sayfaya özgü işaret görünür — yalnız konsola bakmak YETMEZ, boş ekran da konsol-temizdir; (2) `networkidle` — havada olan ağ isteklerinin sonuçlanmasını bekler, böylece yoldaki bir hatanın assert öncesinde düşmesini sağlar, test gövdesi içindeki hataları helpers.ts yakalar ancak test bittikten sonra çıkan gecikmeli hataları (testi aşan zamanlayıcı) yakalamaz (yarışı daraltır, kapatmaz); (3) pathname AYNI kalır — rota bir izin duvarına çarpıp `/`'a düşmüş olmasın, bu üçüncüsü olmadan kapsam sayısı şişer ve kapı Pano'yu ölçerdi; BULGU — `/yedekler` E2E'DE ZİYARET EDİLEMİYOR ve bu kapsamdan daha değerli: rota `platform` iznine bağlı, o izin `*` ile GELMEZ (`AuthContext.can` onu doğrudan `is_platform_operator` bayrağına, backend de `SUNGUR_PLATFORM_OPERATORS` ortam değişkenine bağlar — `platform_access.is_platform_operator`), `e2e/serve.py` bu değişkeni KURMAZ, ÖLÇÜLDÜ (atılacak kopyada): `/yedekler` → pathname `/`, "Yedekler" başlığı YOK, Pano çizildi; değişkeni e2e sunucusunda açmak kapsamı tek satırda büyütürdü ama BÜTÜN spec'lerin sertifikaladığı güvenlik duruşunu değiştirirdi, bu yüzden rota dışarıda ve gerekçesi dosyadaki `KAPSANMAYAN` kaydında YAZILI, kaydın ve gerekçesinin sessizce silinmesini/boşaltılmasını donmuş sözleşme testi engelliyor; ÖLÇÜMLER (gerçek Chromium + gerçek backend, e2e/serve.py; simülasyon yok): POZİTİF KONTROL ÖNCE — `/analizler`'e çalışma zamanında çöken ama tipçe geçerli bir ifade enjekte edildi (atılacak git worktree kopyasında), `npm run build` YİNE YEŞİL (exit 0, çökme paketin içinde) ama yeni spec KIRMIZI oldu ve rotayı ADLANDIRDI ("TypeError: s.generated_at.toFixed is not a function"), diğer 30 rota yeşil kaldı yani kırmızı hedefliydi; temiz ağaçta yeni spec 31/31 YEŞİL; KARARLILIK: üst üste 3 koşum, 3/3 kez 31 geçti ve üçünde de 1.4 dk — kararsız rota YOK; GÜRÜLTÜLÜ ROTA YOK: otuz rotanın hiçbiri meşru `console.error` üretmedi, dolayısıyla hiçbir yerde assert gevşetilmedi (gevşetme gerekseydi rota ve gerekçesi burada adlandırılacaktı); TAM SÜİT 142 geçti exit 0 (taban 111 test / 19 dosya → 142 test / 20 dosya, `playwright test --list` ile ayrı ayrı sayıldı), `npm test -- --run` 551 geçti / 82 dosya, `npm run build` exit 0; DONMUŞ SÖZLEŞMELER KIPIRDAMADI: `frontend/e2e` lint uyarısı 8 → 8 (ci.yml'deki `BEKLENEN=8` değişmedi, dolayısıyla `ci.yml`e DOKUNULMADI), `typecheck:e2e` temiz, `src/` değişmediği için nav sayıları ve `ROUTE_PERMISSIONS` türevleri yerinde ve `navigation.consistency` testleri yeşil; KAPSAM 32/70 → 62/70 ve SAYIM YÖNTEMİ kayda geçer: statik `goto` taraması YETERSİZDİR (menu-groups.spec.ts ve machine-360.spec.ts gezinmeyi TIKLAYARAK yapar), bu yüzden ölçüm ÇALIŞMA ZAMANINDA yapıldı — atılacak kopyada `helpers.ts` fixture'ına `framenavigated` dinleyicisi eklenip tam süit koşuldu ve ziyaret edilen her pathname 70 adlandırılmış rotaya (63 `ROUTE_PERMISSIONS` + 6 `ANONYMOUS_PATHS` + `/sifre-degistir`) eşlendi; ölçümün KÖR NOKTASI da kayda geçer: dinleyici `page` fixture'ına takılıdır, `context.newPage()` ile açılan ikincil sekmeler görünmez ve bilinen tek vaka `/saha`'dır (`field-write-epoch-race.spec.ts`), kaynakta doğrulanıp sayıya elle eklendi; ziyaret EDİLMEYEN 8 rota: yedi `:id` rotası (`/tedarikciler/:id`, `/urunler/:id`, `/saha/:id`, `/hayvancilik/hayvanlar/:id`, `/depo-transferleri/:id`, `/stok-sayimlari/:id`, `/depolar/:id`) BİLEREK kapsam dışı çünkü tohumlanmış kayıt ister ve ayrı bir turdur, artı `/yedekler`; KAPSAM ORANINI DONDURAN BİR EŞİK/RATCHET BU TURDA YOK ve bu bilinçlidir: naif bir donmuş sayı, sabiti olduğu yerde bırakıp testleri silerek yenilebilir — çalışma zamanı toplama ister, ayrı tasarım; KABUL EDİLEN ARTIK (KAPININ KENDİSİNİ KORUMADIĞI AÇIK NOKTALAR): dört tek-düzenleme zayıflatması ölçüldü ve hepsi yeşil kaldı — (1) `OTURUMLU_ROTALAR` listesinden rota eksiltme (30 geçti), (2) döngüye `test.skip` koyma (28 atlandı, 3 geçti), (3) `.toBe(rota.path)` yerine `.not.toBe('/')` yazarak yönlendirme assert'ünü gevşetme (geçti), (4) iki `networkidle` beklemesini silme (geçti); ilk ikisi bilinçle sonraya bırakılan ve ayrı bir değişiklik olarak planlanan çalışma zamanı kapsam eşiği (ratchet) ile kapatılacaktır, son ikisi ise assert zayıflatmasıdır ve bu depoda daha önce yaşandığı üzere testin kendi assert'lerini dondurma tuzağına düşülmeyerek bilinçle açık bırakılmıştır (statik metin anlambilimi sabitleyemez); ÖLÇÜLMEYEN: backend pytest süiti bu turda yerelde koşulmadı (değişiklik yalnız `frontend/e2e/` altında bir spec dosyasıdır ve backend kodunun hiçbir satırına dokunmaz), `/yedekler`'in platform operatörü AÇIKKEN nasıl çizildiği, kapsanan 30 rotanın boş veritabanı DIŞINDAKİ durumları (dolu liste, hata, yükleme, izin kısıtlı roller) ve 390px dokunma hedefi ekseni (o kapı `touch-targets.spec.ts`in kendi manifestindedir)
