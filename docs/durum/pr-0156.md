#156 — F9-5 MUHASEBE FİŞİ / KDV DIŞA AKTARIM + MALİ MÜŞAVİR ROLÜ KEŞFİ (BELGE-YALNIZ; backend/frontend/alembic DOKUNULMADI, göç YOK, yeni rota YOK): tek dosya `docs/f9-5-muhasebe-disa-aktarim-kesif-2026-09-24.md` (891 satır), taban `8a6e3e1`. ÖLÇÜLEN BULGULAR — `orders/purchases/returns/payments` PUT ile değişir, SERT silinir ve `created_at/updated_at` TAŞIMAZ (tam göçlenmiş şemada PRAGMA) → durumsuz belirlenimci aktarım + dönem SHA uyarısı önerildi (K5); `muhasebe` rolü ZATEN VAR ve yazar, izin modeli okuma/yazma AYIRMAZ → yeni `musavir` rolü + metoda bakan salt-okunur kapı (K7, UNIVERSAL_PERMISSIONS korunsun diye farm/herd.view dahil); aylık KDV özeti tek Core `union_all` sorgusuyla `seed_demo_data.py` SQLite üzerinde KOŞTURULDU (2026-07 satış 192.939,61/38.587,94, alış 480.880,00/96.176,00) ve dokuz boşluk adıyla yazıldı (tevkifat yok, `vat_rate` kapalı küme değil, servis faturası işçilik %0 + iskonto KDV düşürmüyor, tohumda 60 satışın 11inde başlık≠satır KDV, PGde `timestamptz` substr); hesap planı eşlemesi SIFIR isabet → `muhasebe_hesap_eslemeleri` (TENANT_TABLES 127→128); Luca zorunlu alanları + 50 fiş sınırı halka açık kaynakla ölçüldü, Mikro/Logo DOĞRULANMADI; 9-5a..d pin deltaları ve K1–K9 belgede. 17 bağlam SABİT. DÜZELTME 1 (doc lens): taban `63e57f6`e rebase, pinler yeniden ölçüldü (aynı); §0.2 örneği `/api/payments` (`auth.py:1075`) ile değişti (`/api/orders` GET'i `read`e düşer); `withholding` yalnız beş avans/müstahsil dosyasında; parmak izi 11 kez; `accounting.view` İKİ rol satırı; ödeme silmesi/satış PUT izi düzeltildi; müstahsilde düzenleme ucu YOK; Alembic 17 isabet/15 çivi; `can()` 16 çağrı, dördü okuma gizler; rol pin dosyası BEŞ (+herd); eklendi: `MASKESIZ_ROLLER`, `navigation.consistency.test.ts:451`, `financial_instruments` logsuz sert silme (K1 v2), iptalin `tax_liabilities` silmesi (risk 4 + 9-5d notu), tutar sütunları, 0037 iki tablo, K4 politika, `EXPECTED_GET_PERMISSIONS`+iki parmak izi, #155 sonrası Core 252/ifade 209/göç 0092, K8 fix dilimi; şef kararları K10 durum kümesi, K11 e-Defter dört alan, K12 dönem ≤1 ay, K13 müşavir maskesiz — hepsi KABUL; KDV SQL taze tohumda yeniden koştu, sayılar AYNI; belge 886 satır.
