#156 — F9-5 MUHASEBE FİŞİ / KDV DIŞA AKTARIM + MALİ MÜŞAVİR ROLÜ KEŞFİ (BELGE-YALNIZ; backend/frontend/alembic DOKUNULMADI, göç YOK, yeni rota YOK): tek dosya `docs/f9-5-muhasebe-disa-aktarim-kesif-2026-09-24.md` (891 satır), taban `8a6e3e1`. ÖLÇÜLEN BULGULAR — `orders/purchases/returns/payments` PUT ile değişir, SERT silinir ve `created_at/updated_at` TAŞIMAZ (tam göçlenmiş şemada PRAGMA) → durumsuz belirlenimci aktarım + dönem SHA uyarısı önerildi (K5); `muhasebe` rolü ZATEN VAR ve yazar, izin modeli okuma/yazma AYIRMAZ → yeni `musavir` rolü + metoda bakan salt-okunur kapı (K7, UNIVERSAL_PERMISSIONS korunsun diye farm/herd.view dahil); aylık KDV özeti tek Core `union_all` sorgusuyla `seed_demo_data.py` SQLite üzerinde KOŞTURULDU (2026-07 satış 192.939,61/38.587,94, alış 480.880,00/96.176,00) ve dokuz boşluk adıyla yazıldı (tevkifat yok, `vat_rate` kapalı küme değil, servis faturası işçilik %0 + iskonto KDV düşürmüyor, tohumda 60 satışın 11inde başlık≠satır KDV, PGde `timestamptz` substr); hesap planı eşlemesi SIFIR isabet → `muhasebe_hesap_eslemeleri` (TENANT_TABLES 127→128); Luca zorunlu alanları + 50 fiş sınırı halka açık kaynakla ölçüldü, Mikro/Logo DOĞRULANMADI; 9-5a..d pin deltaları ve K1–K9 belgede. 17 bağlam SABİT.
