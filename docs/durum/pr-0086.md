#86 — WA5: giden mesajın KİMLİĞİ `external_id`ye, gelen MEDYANIN faturası ÖZETLENİYOR (kayıt YOK), ve .md satır sonu ÇİVİSİ (GÖÇ YOK, ROTA YOK); TABAN `origin/develop` `554d87a` (#84 birleşmesi); ÜÇ İŞ, ÜÇÜ DE GÖÇSÜZ VE ROTASIZ; (1) `saglayici.metin_gonder` ARTIK MESAJ KİMLİĞİ DÖNDÜRÜYOR ve WHATSAPP outbox adaptörü onu `external_id`ye yazıyor: WA4 bu alanı BİLEREK boş bırakmıştı ve gerekçesini yazmıştı (`WA4_KANALLAR.md` §4) — kimliği okumak için WA3'ün taşıyıcı sözleşmesini genişletmek gerekiyordu ve "o sözleşmenin tek çağıranı bugün işçidir", yani WA4 uğruna WA3'ü genişletmek olurdu; WA5'te O SÖZLEŞMENİN İKİNCİ BİR ÇAĞIRANI DOĞDU (`fatura.py` aynı sağlayıcıdan `medya_indir` istiyor) ve genişlemenin BEDELİ ÖLÇÜLDÜ: dönüş tipi `None` -> `str | None`, başarı ölçütü DEĞİŞMEDİ (istisna atılmaması), `SENT`in anlamı DEĞİŞMEDİ ("Meta POST'u 2xx ile KABUL ETTİ", teslimat kanıtı DEĞİL), dönüşü YOK SAYAN çağıran (WA3 işçisi) ETKİLENMEDİ — bu son cümle bir iddia değil bir kapı: `test_DONUSU_YOK_SAYAN_CAGIRAN_ETKILENMIYOR` işçinin kaynağını AST ile okuyup `metin_gonder` çağrısının bir ATAMAYA BAĞLANMADIĞINI çiviliyor; KİMLİK GELMEZSE `None` KALIR VE SATIR YİNE `SENT`TİR ve bu "kanıt yoksa hata" kuralının BİLEREK REDDİDİR: `supports_idempotency = False` olduğu için başarısız sayılan bir gönderim outbox tarafından YENİDEN DENENİR, yani Meta gövde biçimini değiştirdiği gün kullanıcı aynı mesajı İKİ KEZ alırdı — ayrıştırmanın her başarısızlığı (`{}`, boş liste, sözlük olmayan eleman, bozuk JSON, geçersiz UTF-8, sözlük olmayan kök) `None`dur, istisna DEĞİL ve DOKUZ parametreyle ölçülüyor; `WA4_KANALLAR.md`nin `SENT` satırı DÜZELTİLDİ çünkü tablo "Meta bir `wamid` DÖNDÜRDÜ" diyordu ama kodun kendi docstring'i "2xx ile kabul etti" diyordu — iki cümle aynı depoda hem doğru hem yanlıştı; (2) `app/whatsapp/fatura.py` (YENİ; kaynak `nazgul_website/backend/app/whatsapp/fatura.py`, 318 satır): gelen görsel/PDF Meta'nın İKİ ADIMLI medya ucundan İNDİRİLİYOR, çözülüyor ve kullanıcıya ÖZET dönülüyor — HİÇBİR KAYIT AÇILMIYOR ve gerekçe KAPSAM DEĞİL GÖÇ ZORUNLULUĞUDUR: kaynağın son yarısı (`ONAY` -> taslak alış belgesi) `whatsapp_pending_actions.action_type`a `'FATURA_TASLAK'` yazar, o sütun `schema.py`de `ISLEM_TURLERI = frozenset({TAHSILAT})` ve CHECK `action_type IN ('TAHSILAT')` (`ck_wpa_action_type`, göç `20260910_0080`) ile KAPALI bir kümedir, yani yeni tür BİR GÖÇ demektir ve bu dilim göçsüzdür; SONUÇ KULLANICIYA AÇIKÇA SÖYLENİYOR: özet metni kaynağın "ONAY yazın" davetini TAŞIMIYOR, yerine "NOT: Bu bir ÖZETTİR — kayıt OLUŞTURULMADI. Faturayı web panelinden girin." duruyor — davet dursaydı kullanıcı `ONAY` yazar, o mesajı işleyecek bekleyen satır olmadığı için mesaj düşer ve kullanıcı FATURAYI GİRİLMİŞ SANIRDI (`test_OZET_KAYIT_ACILMADIGINI_SOYLUYOR`, "ONAY" ve "İPTAL" kelimelerinin metinde OLMADIĞINI da çiviliyor); "YAZMIYORUZ" BİR YORUMLA DEĞİL YAZACAK NESNENİN YOKLUĞUYLA GARANTİ: modül bir `Session` ALMAZ — kaynağın `oku_ve_taslakla(db, kimlik, telefon, satir, saglayici)` imzasına karşılık buradaki imza `medya_ozeti(saglayici, satir, cozucu=None)`; İKİ AYRI KAPI var ve BİRİ YEŞİLKEN ÖTEKİ KIRMIZI OLABİLİYOR (ölçüldü): `test_FATURA_MODULU_VERITABANINA_DOKUNMUYOR` imzayı + modülün AST'sini ölçüyor (yasak içe aktarma `sqlalchemy`/`bekleyen`/`app.db`, yasak çağrı `execute`/`commit`/`add`/`flush`/`insert`/`update`/`delete`/`taslak_olustur`) ve `test_DAVRANIS_MEDYA_MESAJI_HICBIR_ISLEM_YAZMIYOR` GERÇEK ŞEMADA akışı koşturup `whatsapp_pending_actions` satırını SAYIYOR — ikinci mutant (`service.py`nin özeti aldıktan SONRA kendi satırını yazması) kurulduğunda YAPISAL KAPI YEŞİL KALDI, yani ikisi gerçekten tamamlayıcı; MODEL ÇAĞRISI AYRILDI ve DURUM ÖLÇÜLDÜ: görev "kaynağın BELİRLENİMCİ ayrıştırıcısı" diyordu, ölçüm şudur — KAYNAKTA BELİRLENİMCİ BİR AYRIŞTIRICI YOKTUR, kaynağın okuma adımı `app.assistant.llm.belge_oku` yani bir MODEL çağrısıdır; belirlenimci olan, model çıktısı ALINDIKTAN SONRA çalışan karar katmanıdır (`_sayi`, `_kalem_kararlari`, `_ozet_metni`) ve İŞTE O taşındı, kaynağın kendi altın vakalarıyla birlikte; okuma bir ARAYÜZE çekildi (`BelgeCozucu`) ve bu turun tek gerçekleştirimi `NoOpCozucu`dur (DAİMA `None`), `cozucu_al()` bugün daima onu verir; İNDİRME ÇÖZÜCÜ NoOp'KEN DE KOŞUYOR ve bu bilinçli: indirme, WA3'ün "çağıranı yok" diye TAŞIMADIĞI gerçek ağ yoludur, çağıran BU modüldür ve sıra tersine çevrilseydi ("çözücü kapalıysa indirme") o yol yeniden ÖLÇÜLMEZ olurdu — sahte `urlopen` ile ölçülen güvenlik özellikleri: İKİNCİ istekte de `Authorization` (düşerse Meta 401 verir), ikinci adres `https://` OLMAK ZORUNDA (o adresi Meta'nın GÖVDESİ söyler ve gövde bizim denetimimizde DEĞİLDİR; `http://`, `file://`, metin olmayan ve eksik alan REDDEDİLİYOR ve İKİNCİ İSTEK HİÇ KURULMUYOR), boyut sınırı OKURKEN uygulanıyor (`Content-Length` yalan olabilir; `MEDYA_MAKS_BAYT` 8 MiB ve `sınır+1` okunur çünkü TAM sınırda duran okuma dosyanın bitip bitmediğini AYIRT EDEMEZ); AKIŞ KİMLİK ÇÖZÜLDÜKTEN SONRA BAĞLANDI: bağsız numaraya ya da firma seçmemiş kullanıcıya fatura özeti dönmek, ERP bağlamı olmayan birine ERP cevabı vermek olurdu ve üstteki iki dal onları KENDİ cevaplarıyla karşılıyor; ayrıca `not metin` yüklemi artık `media_id` ile BİRLİKTE okunuyor çünkü ALTYAZISIZ bir fotoğrafın `text` alanı BOŞ STRING'tir (`cloud_api._medya_mesaji`; sütun NOT NULL) ve yüklem düzeltilmeseydi o satır "boş mesaj" sayılıp IGNORED ile kapanır, FATURA YOLU HİÇ KOŞMAZDI; DEĞİŞEN DAVRANIŞ ADIYLA KAYITLI: WA3 medya satırını `last_error='medya'` ile IGNORED kapatıyordu ("medya bu turda işlenmiyor; indirilen baytı okuyacak çağıran YOK"), WA5'te `ANSWERED` — eski sözleşmeyi çivileyen test bu turda GÜNCELLENDİ ve ADI DEĞİŞTİ: `test_wa3_worker.py::test_MEDYA_ve_BOS_METIN_SESSIZCE_KAPANIYOR` -> `test_BOS_METIN_SESSIZCE_KAPANIYOR_MEDYA_ARTIK_CEVAPLANIYOR`, boş metnin yarısı DURUYOR (hâlâ IGNORED), medyanın yarısı DEĞİŞTİ (ANSWERED, `last_error` `None`, cevap gönderildi); YETKİ YÜKLEMİ VE GÜNLÜK KOTA SAYACI TAŞINMADI ve ikisi de BORÇ olarak `docs/whatsapp/WA5_FATURA.md`ye yazıldı: kaynak `has_permission(role, "purchases")` arıyordu ve gerekçesi taslak ALIŞ BELGESİ açmasıydı — açılan belge olmayınca yüklemin koruduğu şey de yok, BELGE AÇAN TUR ONU GERİ GETİRMELİDİR; kota sayacının gerekçesi kaynakta "her fotoğraf bir model çağrısıdır" diye YAZILI ve bu turda model çağrısı YOK, ayrıca o sayaç bir `SELECT`tir yani çekirdek sorgu envanterine bir satır eklerdi; (3) `.gitattributes`: `*.md text eol=lf` + `docs/whatsapp/*.md text eol=lf` — BU BİR DÖNÜŞÜM DEĞİL BİR ÇİVİDİR ve ölçüldü: depodaki 259 `.md` dosyasının TAMAMI zaten indekste LF (`git ls-files --eol '*.md'` TEK bir `i/lf` kümesi veriyor), yani İNDEKSTE SIFIR DEĞİŞİKLİK; çivi mevcut durumu adlandırır ve `core.autocrlf` yanlış kurulmuş bir istemcinin CRLF bir blob'u indekse sokmasını engeller; `docs/whatsapp/*.md` AYRICA yazılı çünkü o dosyalar yalnız OKUNMUYOR, AYRIŞTIRILIYOR: `test_wa3_kopru.py` `KOPRU_SOZLESMESI.md` içindeki json bloğunu bir düzenli ifadeyle çıkarıp ÜÇ ALTIN VEKTÖRÜ oradan okuyor; DÜRÜST SINIR YAZILDI — bu ayrışma bugün CRLF'e RAĞMEN de çalışırdı ama çalışmasının NEDENİ dosya değil Python'un `read_text` evrensel satır sonu çevrimidir, çivi o ÖRTÜK KATMANA olan bağımlılığı kaldırır; KANIT `git ls-files --eol docs/whatsapp/KOPRU_SOZLESMESI.md` -> `i/lf w/crlf attr/text eol=lf`; İKİ KAPI: dosyanın İÇERİĞİ (`test_MD_SATIR_SONU_CIVISI_ve_FIXTURE_KAPSAMI`) ve git'in niteliği GERÇEKTEN UYGULADIĞI (`git check-attr text eol`, `test_MD_CIVISI_GITTE_GERCEKTEN_UYGULANIYOR`); KAYNAK TESTLERİNDEN NE TAŞINDI (sayıldı): `test_whatsapp_fatura.py` 10 test -> TAŞINDI 6 (`_kalem_kararlari`ın üç altın vakası, biri ALTI parametreli; `_ozet_metni`nin iki vakası; NoOp'un medya indirmemesi), TERSİNE ÇEVRİLDİ 1 (`test_islem_turu_kapali_kumede_kayitli` kaynakta türün kümede OLDUĞUNU çiviliyordu, burada OLMADIĞINI: `test_FATURA_TASLAK_TURU_bu_turda_TANIMLI_DEGIL` — aynı kapı, ters yön, çünkü sözleşme ters; modülün bir `ISLEM_TURU` sabiti TANIMLAMADIĞI da çivili çünkü tanımlamak, onu kümeye eklemek isteyen bir sonraki eli DAVET EDERDİ), ZATEN KAPSANIYOR 3 (medya ayrıştırma vakaları; kaynakta `parse_media_messages` ayrı fonksiyondu, burada `cloud_api.gelen_mesajlari_coz` TEK giriştir ve `test_wa1_ingress.py` `media_id`/`media_mime` iddialarıyla onu ölçüyor); `test_whatsapp_fatura_akisi.py` 28 test -> TAMAMI DÜŞTÜ, her biri ya model çağrısına ya bekleyen-işlem yazımına ya `belge_olustur`a bağlı ve üçü de bu turda YOK — düşenler "unutuldu" DEĞİL KAPSAM DIŞI, göç geldiğinde geri gelecek dosya odur; KAPILAR VE PİN ENVANTERLERİ — HİÇBİRİ KIMILDAMADI ve bu TASARIM GEREĞİ: `fatura.py` SIFIR SQL içerir (`app/whatsapp/*` çekirdek sorgu envanterinin KAPSAMINDADIR, ölçüldü), dolayısıyla `TOTAL_CORE_QUERIES` 175 SABİT, `BEKLENEN_CORE_IFADE_SAYISI` 150 SABİT, `VARIABLE_ARG_ALLOWLIST` 4 SABİT, dinamik SQL toplamı (`DYNAMIC_SQL_FILE_ALLOWLIST`) 269 SABİT, `TENANT_TABLES` 120 SABİT (yeni tablo yok), `tests/pins/` dizininde SIFIR DIFF (`pg_twins.txt` 120, `alt_surec_sql.txt` 129); ALEMBIC DIFF SIFIR SATIR — göç EKLENMEDİ, baş `20260910_0080`de KALDI ve ALTI baş pininin HİÇBİRİNE dokunulmadı (sayılmadı, GREPLENDİ); ROTA ENVANTERLERİ KIMILDAMADI (yeni uç yok; `types.gen.ts` ve `frontend/` HİÇ DOKUNULMADI); PG İKİZİ EKLENMEDİ ve gerekçesi ÖLÇÜLDÜ: bu dilimin YENİ kodu HİÇ SQL çalıştırmıyor (`fatura.py` sıfır sorgu, `saglayici.py` yalnız `urllib`), yani bir ikiz lehçe farkı ölçemezdi — eklenseydi SQLite'ta ölçülenin aynısını ölçerdi; TESTLER `backend/tests/test_wa5_fatura.py` (38 test: mesaj kimliği 2 + dokuz parametreli kimliksiz-2xx + geriye uyumluluk AST + medya indirme 4 + belirlenimci karar katmanı 11 + akış 3 + yapısal yazma kapısı 2 + gitattributes 2 + gerçek şemalı davranış 1); MUTASYON BATARYASI — ÜÇ MUTANT, HEPSİ ADIYLA ÖLDÜ: (1) `metin_gonder`in `return mesaj_kimligi(govde)` satırını `return None` yapmak -> `test_MESAJ_KIMLIGI_2xx_GOVDESINDEN_external_ide` KIRMIZI (adaptör `SENT` yazmayı sürdürür ama denetim satırı kimliksiz kalır); (2a) `fatura`ya `db` geçirip yazmak -> `test_FATURA_MODULU_VERITABANINA_DOKUNMUYOR` KIRMIZI; (2b) `service.py`nin özeti aldıktan SONRA `whatsapp_pending_actions`a satır yazması -> `test_DAVRANIS_MEDYA_MESAJI_HICBIR_ISLEM_YAZMIYOR` KIRMIZI VE YAPISAL KAPI YEŞİL KALDI (iki kapının tamamlayıcı olduğunun KANITI); (3) `.gitattributes`tan `*.md text eol=lf` satırını silmek -> `test_MD_SATIR_SONU_CIVISI_ve_FIXTURE_KAPSAMI` KIRMIZI, İKİ satırı birden silmek -> `test_MD_CIVISI_GITTE_GERCEKTEN_UYGULANIYOR` DA KIRMIZI (tek satır silindiğinde öteki hâlâ kapsıyor, yani ayrı satırın gerekçesi de ölçüldü); BİR KUSUR BU TURDA BULUNDU VE DÜZELTİLDİ: `provider.py`nin yeni docstring'ine yazdığım "HALA" kelimesinin sapkali yazimindaki U+00C2 harfi `test_backend_encoding.py::MOJIBAKE_MARKERS` listesinde bir MOJİBAKE İŞARETİDİR ve dosya tertemiz UTF-8 olmasına rağmen kapıyı KIRMIZI yaptı — o liste U+00C3 / U+00C5 / U+00C4 / U+00C2 gibi latin-1 bozulması imzalarını arıyor ve düzgün Türkçe bir harfi kapsaması bilinçli bir seçimdir; kelime `HALA` olarak yazıldı ve kapı yeşillendi; ÖLÇÜM NOTU (kanonik sürücü): `pytest tests/test_wa1..wa4 + goc_zinciri` DÜZ KOŞUMDA ÜÇ WA4 davranış testi DÜŞTÜ ve bu bir kusur DEĞİL dosyalar arası sızıntıdır — `test_wa4_bekleyen.py` TEK BAŞINA 25/25 geçti ve KANONİK `run_isolated_tests.py` ON DOSYAYI (wa1-wa5 + envanterler + göç zinciri) 84.1 saniyede 384 test ile TAMAMEN YEŞİL koşturdu; kayıt adı `python scripts/durum.py --sonraki 86`
