#56 — ALIŞ KALEMİ PARTİ AÇAR (FAZ 1B-A): 0067'nin ÇAĞIRANSIZ parti defterine İLK ÇAĞIRAN bağlandı ve defter DEPOYA indi; göç `20260908_0073`, down_revision `20260907_0072`, TABAN `0d601ad` (`origin/develop`) ÜSTÜNE E1b (#53, `20260907_0072`) YIĞILI — dal `origin/claude/e1b-plantback-rei-migration-bjdd4f` (`c2a2f38`) üzerine kuruldu çünkü 0072 develop'ta HENÜZ YOKTU ve down_revision'ı olmayan bir göçle hiçbir kapı koşturulamazdı; #53 birleştikten SONRA develop'a REBASE EDİLDİ ve iğnelerin tamamı REBASE SONRASI yeniden ölçüldü; açılıştan önce `gh pr list` ve `git ls-remote --heads origin` KOŞTURULDU, tek göç açan PR #53'tü ve o indi, yani `20260908_0073` TEK BAŞ; ÖLÇÜLEN KUSUR — 0067 parti defterini ve FEFO seçicisini kurdu ve İKİSİNİ DE HİÇBİR ŞEYE BAĞLAMADI, kendi başlığı bunu adıyla söylüyordu ("ÇAĞIRANI YOKTUR") ve İKİ KAPI onu çiviliyordu, yani depo bugün "elde 50 var" diyor ama o 50'nin hangi partiden geldiğini ve son kullanma tarihini SÖYLEYEMİYOR; bu dilim yalnız ALIŞI bağlıyor (parti AÇMA), satışın parti TÜKETMESİ dilim B'dir ve `app/parti.py` seçicisi BU PR'DA DA çağrılmıyor; GÖÇÜN ÜÇ PARÇASI: (a) `product_lots.warehouse_id INTEGER NOT NULL` + BİLEŞİK yabancı anahtar `(company_id, warehouse_id) -> warehouses(company_id, id)` — hedefin tekili (`uq_warehouses_company_id`) BU GÖÇTE KURULDU ve gerekliliği ÖLÇÜLDÜ, VARSAYILMADI: `UNIQUE(company_id, id)` `warehouses` üzerinde NE `app/inventory.py`nin `Table` bildiriminde NE de bir göçte VARDI (`uq_warehouses` literali depoda hiç geçmiyordu), yani satır olmasaydı göç `upgrade` sırasında PostgreSQL'de DÜŞERDİ; (b) TEKİL DEĞİŞTİ — 0067'nin `uq_product_lots_company_product_code`u (company, product, code) DÜŞÜRÜLDÜ, yerine `(company_id, product_id, lot_code, warehouse_id)` KURULDU, çünkü aynı parti kodlu mal İKİ AYRI DEPODA durabilir (üretici bir partiyi iki şubeye böler) ve eski tekil o gerçeği TEK SATIRA sıkıştırıp "hangi depoda ne kadar var" sorusunu SORULAMAZ yapardı; ikisi BİRLİKTE yapılmak zorunda — yalnız yenisini eklemek eski kısıtın meşru ikinci depoyu reddetmesini sürdürürdü, yalnız eskisini düşürmek hiç kısıt bırakmazdı; (c) `purchase_items.lot_code VARCHAR(80) NULL` + `expiry_date DATE NULL`, GERİYE DOLDURMA YOK çünkü eski alışların hangi partiden geldiği bilinmiyor ve bilinemez, boş dizgiyle doldurmak "parti kodu boş bir parti var" derdi ki BAŞKA bir cümledir; `lot_code` kalemde `VARCHAR(80)` ama defterde `Text` ve bu bilinçli: kalem bir FORM ALANIDIR, defterdeki sütun ise ÜRETİCİNİN bastığı etiketi saklar, sınırı GİRİŞTE koymak sakladığı şeyi daraltmaktan farklıdır; BOŞ TABLO ÖLÇÜMÜ — göç `upgrade`de DE `downgrade`de DE `SELECT count(*) FROM product_lots` yapıyor ve satır varsa `RuntimeError` ile GÜRÜLTÜLÜ ÖLÜYOR, VARSAYMIYOR: üç değişikliğin (NOT NULL depo, tekilin değişmesi, bileşik anahtar) HİÇBİRİ mevcut satırlar için anlamlı bir varsayılan üretemez ve "varsayılan depo" yazmak defteri YALAN SÖYLETİRDİ; `downgrade` tarafındaki gerekçe DAHA SERT ve simetrik değil — eski üçlü tekil iki farklı depodaki aynı kodu ARTIK REDDEDER, yer açmak için satır silmek geri çağırma defterini geri alma adına YOK ETMEK olurdu ve hangi satırın silineceği seçilemez; DİYALEKT AYRIMI AÇIK YAZILDI ve İKİ DERSİ BİRDEN uyguluyor: 0072'de ÖLÇÜLDÜ ki `batch_alter_table` içindeki kısıt yaratımı yeniden kurulum YAPILMAYAN diyalektte (PostgreSQL) SESSİZCE hiçbir DDL üretmiyor, 0071'de ÖLÇÜLDÜ ki SQLite'ta İKİ AYRI batch tabloyu iki kez yeniden kurar ve arada yansıtılan eski tekil `OperationalError` verir — bu yüzden SQLite dalında sütun + eski tekilin düşmesi + yeni tekil + yabancı anahtar HEPSİ TEK `batch_alter_table` içinde, öteki dalda AÇIK `op.add_column` / `op.drop_constraint` / `op.create_unique_constraint` / `op.create_foreign_key`; `up -> down -> up` turu HEM SQLite'ta HEM gerçek PostgreSQL 16'da KOŞTU; `core_schema.py` DEĞİŞTİRİLDİ ve karar ÖLÇÜLDÜ, VARSAYILMADI: 0067 `core_schema`ya dokunmamıştı ve gerekçesi "bildirim SAYISAL MANİFESTODA bir varlık farkı üretir ve sayısal göç mutabakat kapısını kırar" idi, ama o gerekçe bir NUMERIC sütun (`quantity`) içindi — ölçüm (CPython 3.12.10): `iter_numeric_columns()` `purchase_items` için `(unit_price, vat_rate, line_subtotal, line_vat, line_total, discount_percent, discount_amount, quantity)` veriyor, `lot_code` ve `expiry_date` O LİSTEDE YOK, yani `capture_numeric_snapshot` onları GÖRMÜYOR ve varlık farkı DOĞMUYOR; ikinci yarısı: `20260712_0000` `core_schema.metadata.create_all` çağırıyor, yani TAZE veritabanı sütunları 0000'da alıyor ve 0073 onları VAR bulup atlıyor, MEVCUT veritabanı ise 0073'te alıyor — iki yol AYNI şemada buluşuyor çünkü koşul `_sutunlar` ile SORULUYOR; `test_v2_9_clean_install`, `test_v2_9_runtime_migrations`, `test_v2_9_decimal_contract`, `test_company_id_default_contract` ve `test_platform_maintenance_migration` bu bildirimle YEŞİL koştu; DAVRANIŞ — `POST/PUT /api/purchases` (yol #1, `transactions.py`): parti kodu DOLU her alış kalemi için `_parti_ac` satırı AÇIYOR ya da var olana miktar EKLİYOR (company, product, lot_code, alışın deposu), `stock_movements` satırı o partinin `lot_id`sini TAŞIYOR; kodu BOŞ kalem BUGÜNKÜ davranışını BİREBİR koruyor ve `lot_id` NULL kalıyor — "bu hareket parti öncesindendir" cümlesi 0067'de zaten DOĞRUYDU; SKT ÇATIŞMASI 422 `LOT_SKT_CELISKI` ve sessiz kabul DEĞİL, çünkü var olan partinin SKT'si ile yeni alışın söylediği ayrışıyorsa iki cümleden biri YALANDIR ve hangisi olduğunu depo BİLEMEZ — var olanı ezmek geçmiş hareketleri yeniden yorumlardı, yeni geleni yok saymak operatöre yazdığının kaydedilmediğini SÖYLEMEZDİ; karşılaştırma METİN üzerinde çünkü `expiry_date` PostgreSQL'de `date`, SQLite'ta `str` geliyor ve düz `!=` iki diyalekti AYIRIRDI (0072'nin `_gun_esit` dersi); GERİ ALMA — güncelleme ve silme yollarının İKİSİNDE DE, mevcut `DELETE FROM stock_movements`ten ÖNCE `_parti_geri_al` silinecek hareketlerin `lot_id`li olanlarını okuyup miktarı defterden DÜŞÜYOR; SIRA ZORUNLUDUR ve tersi SESSİZDİR: çağrı DELETE'ten sonraya kayarsa hiçbir satır bulunmaz, hiçbir istisna atılmaz ve defter geri alınmayan miktarı sonsuza kadar taşır — stok geri alınır, defter alınmaz; EKSİYE DÜŞÜRMEZ, 409 `LOT_MIKTARI_EKSIYE_DUSER` atar ve anlamı şudur: o parti başka bir yerde tüketilmiştir (bugün böyle bir yol yok, dilim B getirecek) ve alışı geri almak tüketimi de geri almak demektir, depo bunu kendi başına yapamaz; DELETE'in kendi semantiği (hareketin silinmesi vs. ters kayıt) DEĞİŞMEDİ ve ayrı bir iş olarak PARK EDİLDİ; ÜRETİM KUSURU YERELDE BULUNDU ve DÜZELTİLDİ, ADIYLA: `delete_transaction`ın `except` zincirinde `except HTTPException: raise` dalı YOKTU (kardeşi `save_transaction`da ZATEN VARDI), yani gövdenin içinde ADIYLA atılan her 4xx aşağıdaki `except Exception` tarafından yutulup 500'e çevriliyordu — yalnız bu PR'ın 409'unu değil, 0058'den beri orada duran "Tahsisli satış reversal tamamlanmadan silinemez" 409'unu da; ikisi de operatöre NE YAPACAĞINI söyleyen redlerdi ve 500 hiçbir şey söylemiyor; dal `save_transaction`ınkiyle BİREBİR aynı (rollback + raise) ve hiçbir test 500'ü çivilemiyordu (arandı); OKUMA — `GET /api/products/{product_id}/lots` (depo, parti kodu, SKT, miktar, oluşturma zamanı), izni `read` ve YENİ İZİN AİLESİ AÇILMADI çünkü gösterdiği şey `GET /api/products/{product_id}`in zaten gösterdiği stok bakiyesinin PARTİ KIRILIMIDIR; SIRA FEFO SIRASI DEĞİLDİR ve olmaması karardır — seçici bir DAĞITIM üretir, burası bir LİSTEDİR ve ikisini aynı sıraya bağlamak listenin bir gün seçicinin cevabı sanılmasına yol açardı; miktarı SIFIR olan parti GİZLENMEZ, çünkü tükenmiş parti satırı 0067'de bilinçli olarak silinmiyor ve o satır geri çağırmanın KANITIDIR; ÖLÇEK — `units.resolve` alış yolunda ÇAĞRILMIYOR ve bu bir eksiklik değil KAPSAM kararıdır: `purchase_items.quantity` kullanıcının girdiği HAM sayıdır ve `stock_movements.quantity` de aynı sayıyı taşır, parti defterine ÜÇÜNCÜ bir ölçek sokmak üç sayıdan hangisinin doğru olduğunu sorulamaz yapardı; ÇİVİ ŞUDUR: parti miktarının ölçeği HAREKETİN ölçeğidir ve bu `transactions.py`nin belge dizgisinde ADIYLA duruyor, `test_ALIS_yolu_units_resolve_CAGIRMIYOR_olcek_hareketle_AYNI` ile de AST üzerinden çivili; İKİ YASAK KAPISI EMEKLİ EDİLDİ ve GEVŞETİLMEDİ, DARALTILARAK devredildi — `test_PARTI_MIKTARI_bu_PR_da_HICBIR_YERDEN_guncellenmiyor` (PG ikizinde) ile `test_APP_ALTINDA_app_parti_ITHALI_ve_fefo_sec_REFERANSI_YOKTUR` (SQLite'ta) kaldırıldı, ikisinin de düzyazısı bu günü ADIYLA yazmıştı ("bir çağıran eklendiği gün İKİSİNDEN BİRİ kırmızı olur ve bu DOĞRUDUR"); yerlerine `test_product_lots_YAZICISI_YALNIZ_transactions_py` geldi ve İKİ KATMANLI: (1) `product_lots`a YAZAN (INSERT/UPDATE/DELETE taşıyan) çalıştırılabilir metin YALNIZ `app/routers/transactions.py` içindedir — iki yazıcı olsaydı sessizce ayrışırlardı, ki 0067'nin `app/parti.py` başlığında "iki farklı yerden çıkan mal iki farklı partiden düşerse geri çağırma kaydı YALAN SÖYLER" diye ADIYLA yazılı olan kusur budur, (2) tabloyu ANAN dosyaların kümesi de KAPALIDIR (yazıcı + okuma ucu), üçüncü bir dosya defteri okumaya başlarsa kapı kırmızı olur; ikinci yarı `test_fefo_sec_HALA_CAGIRANSIZ` olarak DEĞİŞMEDEN devam ediyor (`app.parti` ithali ve `fefo_sec` referansı hâlâ YASAK) ve ikisinin AYRI KAPILARA bölünmesi zorunluydu — tek kapıda kalsalardı birinci yarının açılması ikinciyi de GEVŞETİRDİ; arama AST üzerinde ve BELGE DİZGİLERİ DIŞLANARAK, emekli kapının ölçülmüş dersiyle (ham `grep` `app/parti.py`yi yakalıyordu çünkü o dosya tabloyu KENDİ DÜZYAZISINDA tarif ediyor; düzyazıda anmak ÇAĞIRMAK DEĞİLDİR); 0067'nin BAŞLIĞI da güncellendi — düzyazı SİLİNMEDİ (göçün neden böyle yazıldığını anlatıyor) ama İKİ CÜMLESİ ARTIK ESKİ diye adıyla işaretlendi (geri almanın "davranışsal olarak GÖRÜNMEZ" olması ve eski üçlü tekilin "bir firmanın bir ürünü için bir parti kodu TEK satırdır" yorumu); DURUM KAYITLARI APPEND-ONLY olduğu için `docs/durum/0082-pr-0031.md` (0067'nin kaydı) ELLENMEDİ ve "çağıranı yoktur" cümlesinin nerede kapandığı BU kayıtta söyleniyor; TESTLER: `backend/tests/test_1b_a_alis_lot.py` (9 test — 8 statik kapı + gerçek şemalı 8 adımlı davranış smoke'u) ve PG ikizi `backend/test_1b_a_alis_lot_postgresql.py` (9 test) GERÇEK PostgreSQL 16'da KOŞULDU, SIFIR ATLAMA; PG koşumunda HEM `DATABASE_URL` HEM `APP_TEST_DATABASE_URL` verildi artı `REQUIRE_PG=1` (yalnız birini vermek uygulamayı SESSİZCE SQLite'a düşürüyor — #47'nin kaydında ölçülmüş tuzak) ve sürücü `postgresql+psycopg`; PG İKİZİNİN ÖLÇTÜKLERİ (hepsi SQLite'ta GÖRÜNMEZ): iki bileşik yabancı anahtarın GERÇEKTEN ısırması (çapraz kiracı DEPO ve çapraz kiracı ÜRÜN — ikincisi bir REGRESYON kapısıdır, çünkü 0073 tabloyu SQLite'ta YENİDEN KURDU ve yeniden kurulan tablodan bir anahtarın sessizce düşmesi 0071/0072'de ölçülmüş bir kusur sınıfıdır), yeni dörtlü tekilin REDDETMESİ, ESKİ ÜÇLÜ TEKİLİN GERÇEKTEN DÜŞMÜŞ OLMASI (ikisi BİRLİKTE gerekli — yalnız reddi ölçmek, kısıt fazla GENİŞ kalsa da yeşil kalırdı ve o durumda üretici bir partiyi iki şubeye BÖLEMEZDİ; 0072'de ölçüldü ki "düşürdüm" demek YETMEZ), `warehouse_id`in NOT NULL'u, `uq_warehouses_company_id`in varlığı, `purchase_items` sütunlarının NULL kabul ettiği ve BOŞ TABLO ÖLÇÜMÜNÜN gerçek şemada TUTMASI (önce BOŞ OLMAYAN halde `downgrade` denenip GÜRÜLTÜLÜ ÖLDÜĞÜ, sonra defter boşaltılıp turun kapandığı); MUTASYONLAR (sekizi de tek tek uygulandı, adı geçen test KIRMIZI oldu, geri alındı ve dosya BİREBİR eski hâline döndürüldü): parti DEPOSUZ yazılıyor (`warehouse_id` yerine sabit 1) -> SQLite smoke KIRMIZI; geri alma ATLANIYOR (silme yolundaki çağrı kaldırıldı) -> SQLite smoke + sıra kapısı KIRMIZI (2 test); SKT çatışması YOK SAYILIYOR -> SQLite smoke KIRMIZI; `products.py` parti defterine YAZIYOR -> `test_product_lots_YAZICISI_YALNIZ_transactions_py` KIRMIZI; PG'de depo bileşik FK ÇIPLAK yazılıyor -> `test_CAPRAZ_KIRACI_DEPO_referansi_REDDEDILIYOR` KIRMIZI; PG'de eski üçlü tekil DÜŞÜRÜLMÜYOR -> `test_AYNI_KOD_BASKA_DEPO_KABUL_EDILIYOR_eski_tekil_DUSTU` KIRMIZI; PG'de boş-tablo ölçümü `downgrade`den kaldırıldı -> `test_GOC_TURU_BOS_TABLO_OLCUMUYLE_kapaniyor` KIRMIZI; ÇAPRAZ KİRACI MUTASYONU İLK TURDA HAYATTA KALDI ve sebebi ADIYLA yazılıyor: okuma ucunun `l.company_id=:cid` yüklemini kaldırmak SQLite smoke'unda YEŞİL kaldı, çünkü `products.id` KÜRESEL bir birincil anahtardır — komşunun partisi ancak komşunun ÜRÜN kimliği sorulduğunda sızabilir ve o kimlik ürün kapısında ZATEN 404 alır, yani sızıntı DAVRANIŞTA ÜRETİLEMEZ; mutasyonu yakalayan şey `test_tenant_scoping_guard.py::test_every_tenant_table_query_is_company_scoped`tir ve İKİ YÖNDE ölçüldü (kök yüklem kaldırıldı -> KIRMIZI; `warehouses` birleştirmesinin `w.company_id=l.company_id` alias eşitliği kaldırıldı -> KIRMIZI), yani sınır KORUNUYOR ama koruyan kapı bu PR'ın dosyası DEĞİL deponun mevcut kiracı kapısıdır; İĞNELER (hepsi YENİDEN ÖLÇÜLDÜ, ARİTMETİKLE TÜRETİLMEDİ — her biri kapının kendisine ölçtürüldü): göç başı `20260907_0072` -> `20260908_0073` (3 yer: `test_goc_zinciri`in iki iddiası ve CLI çıktısı kapısı), `TENANT_TABLES` DEĞİŞMEDİ (`product_lots` ve `purchase_items` ORADA ZATEN VAR, 110'da SABİT), `transactions.py` dinamik `text()` 18'de SABİT ve parmak izi `5bd758b5` -> `d6f8a68a`, `products.py` dinamik `text()` 6'da SABİT ve parmak izi `5cd5fdfd` -> `a9d91b79`, `core_schema.py` parmak izi `283ae606` -> `866f92a5` (dinamik çağrı sayısı ve HİÇBİR SQL metni kımıldamadı; eklenen şey İKİ CORE SÜTUN BİLDİRİMİDİR), rota sözleşmesi 364/280 -> 365/281 ve parmak izi `a49f58e6` -> `cea742bc`, GET envanteri 174 -> 175 ve parmak izi `39395cc1` -> `ea3ee703` (drift raporu ÖLÇÜLDÜ: `missing`/`stale`/`changed` ÜÇÜ DE BOŞ, artış YALNIZ ekleme), `EXPECTED_AUTHENTICATED` 354 -> 355, `EXPECTED_READ` 93 -> 94, `EXPECTED_UNDENIABLE` 102 -> 103, ÇIPLAK read 67 -> 68, `FARM_HERD_VIEW_OPERATIONS` DEĞİŞMEDİ, alt süreçte SQL taşıyan yüzey 110/177 -> 111/178, PG popülasyonu 105 -> 106 (3 yer: test ADI, iddia ve `ci.yml`in `BEKLENEN_PG_DOSYA_SAYISI`'ı); DİNAMİK `text()` 18'DE SABİT KALMASININ GEREKÇESİ: yeni SQL'lerin ALTISI DA SABİT METİNDİR (`_parti_ac`in üç, `_parti_geri_al`ın üç metni) ve altısının da kök yüklemi AÇIK `company_id=:cid` taşır; mevcut KALEM INSERT'üne iki f-string parçası (`lot_col`/`lot_val`) eklendi ama parçalar `kind` değişkeninden geliyor ve `kind` KAPALI bir kümedir (`_config` yalnız 'sale'/'purchase' bilir), istekten gelen DEĞERLER metne HİÇ girmiyor — `lot_code` ve `expiry_date` BAĞLI PARAMETREDİR ve kalıp aynı fonksiyondaki zaten gözden geçirilmiş `term_col`/`normalized_due_col`ün BİREBİR aynısıdır; `core_query_inventory` DEĞİŞMEDİ çünkü bu PR'ın SQL'lerinin hepsi ham `text()`tir, o kapı yalnız SQLAlchemy Core `select/update/delete` çağrılarını sayar; İKİ KOMŞU KAPI BİLİNÇLİ GÜNCELLENDİ ve ikisi de KAPININ İŞİNİ YAPTIĞININ tanığıdır: `tests/test_e1b_plantback.py::test_goc_turu_up_down_up_SQLitede_KOSUYOR` `command.downgrade(config, "-1")` kullanıyordu ve "-1" GÖREL bir adımdır — 0073, 0072'nin üstüne binince "-1" artık 0073'ü indiriyordu ve 0072'nin nesneleri AYAKTA kalıyordu, yani kapı "geri alma çalışmıyor" diye kırmızı oldu OYSA ölçtüğü şey hiç ÇALIŞTIRILMAMIŞTI; hedef MUTLAK yazıldı (`20260906_0071`) ve artık üstüne kaç göç binerse binsin 0072'nin KENDİ `downgrade`ini sürüyor; aynı dosyadaki baş iddiası da `20260907_0072` -> `20260908_0073` oldu ve gerekçesi yazıldı (kapının ölçtüğü şey başın HANGİ göç olduğu değil TEK olduğudur); SÖZLEŞME ZİNCİRİ: `export_openapi.py -o` -> `npm run types:gen` -> `capture_frontend_fixtures.py`; delta YALNIZ `frontend/src/api/types.gen.ts` (+70), `pos-api.json` DEĞİŞMEDİ, `backend/openapi.json` `.gitignore`da; `npx tsc -b` çıkış 0 (`npm install` gerekti: bu ortamda `node_modules` YOKTU) ve `tsc -b` KULLANILDI, `tsc --noEmit -p tsconfig.json` DEĞİL — ikincisi bu depoda SIFIR dosya denetler ve boş bir yeşildir; ÖLÇÜLMEDİ, ADIYLA: (1) BİRİM DÖNÜŞÜMÜ — alış yolu ham birimle çalışıyor ve parti defteri hareketle AYNI ölçekte yazılıyor; `units.resolve`u buraya sokmak alış yolunun BÜTÜN ölçeğini değiştirmek demekti ve o iş bu dilimin KAPSAMINDA DEĞİL; (2) EŞZAMANLILIK — aynı parti koduna İKİ EŞZAMANLI alış ölçülmedi; `_parti_ac`in SELECT-sonra-INSERT'ü kendi başına bir kilit DEĞİLDİR ve ikisini ayıran şey ŞEMADIR (dörtlü tekil), yani yarışın sonucu ikinci isteğin `IntegrityError` alması olur — bu ölçüldü (PG ikizi) ama uygulamanın o hatayı NASIL SUNDUĞU ölçülmedi; (3) ÜRETİM GÖÇ MALİYETİ — tablo boş olduğu için `ALTER`ların maliyeti ölçülmedi, ama boşluk `upgrade` başında ZORLANIYOR; (4) SATIŞIN PARTİ TÜKETMESİ, STOK DÜZELTMESİ ve DEPO TRANSFERİ — sırasıyla dilim B, C ve D; (5) ÖN YÜZ — yalnız `types.gen.ts` türedi, ekranda gösterim yazılmadı; (6) AYNI BELGEDE BİR ÜRÜNÜN İKİ PARTİSİ — ÖLÇÜLDÜ ve ŞEMA DEĞİL SÖZLEŞME ENGELLİYOR: `TransactionCreate` aynı ürünü bir belgede iki satırda KABUL ETMİYOR ("Aynı ürün belgede birden fazla satırda bulunamaz"), yani bir alışta iki parti ancak İKİ AYRI ÜRÜNLE kurulabilir ve aynı ürünün iki partisi İKİ AYRI BELGE ister; bu kural bu PR'da DEĞİŞTİRİLMEDİ çünkü değiştirmek belge toplamlarının ve indirim dağıtımının tamamına dokunurdu, ama sınır testin içinde ADIYLA yazılı. KAYIT NUMARASI 0097 -> 0098 ÇÜNKÜ #55 BİRLEŞİRKEN 0097'İ ALDI: numara elle seçilmedi, rebase sonrası `python scripts/durum.py --sonraki 56` verdi ve satır içeriği BİREBİR taşındı. REBASE SONRASI BÜTÜN İĞNELER YENİDEN ÖLÇÜLDÜ ve ÜÇÜ KIMILDADI çünkü #55 (outbox yeniden kuyruklama, GÖÇ YOK) aynı sayaçlara dokunmuştu: rota sözleşmesi 365/281 -> 366/282 ve parmak izi `d11a83eb` -> `b80c06e3`, `EXPECTED_AUTHENTICATED` 355 -> 356, alt süreçte SQL taşıyan yüzey 111/184 -> 112/185; `EXPECTED_READ` 94 ve `EXPECTED_UNDENIABLE` 103 rebase ÖNCESİ ölçümle AYNI kaldı (#55'in ucu `farm.manage`e çözülüyor, `read`e değil), GET envanteri 175 ve parmak izi `ea3ee703` KIMILDAMADI (#55 GET AÇMADI), göç başı `20260908_0073` TEK BAŞ olarak DURUYOR (#55 göç AÇMADI). ÜÇ ÇAKIŞMA REBASE'DE ELLE ÇÖZÜLDÜ ve ÜÇÜNDE DE İKİ TARAFIN DÜZYAZISI KORUNDU (sayılar silinip YENİDEN ÖLÇTÜRÜLDÜ, aritmetikle birleştirilmedi): `test_tenant_scoping_guard`ın alt süreç sayacı, `test_route_security_contracts`ın sayaç ve parmak izi blokları, `test_authorization_population_reconciliation`ın üç sabiti.
