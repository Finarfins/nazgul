#145 — PP4b PLATFORM PANELİ HİJYEN (frontend, backend/göç YOK, yeni rota YOK; mercek #142 → H62/H63/H67a/H67b + lint): H62 kuyruk yeniden deneme `changed:false` artık genel "Zaten bu durumda" değil yanıtın kendi sayısı — "Kuyruğa alınacak kayıt yok · kalıcı hata: N" (`EylemDugmesi`e isteğe bağlı `degismediMetni`; vermeyen çağıranlar aynen genel cümlede); H63 hız sınırı "Kilidi temizle" eylem satırı başına değil IP başına TEK düğme (uç `DELETE ?ip=` IP başına; satırlar IP'ye göre gruplanıp düğme grup boyunca uzanan tek hücrede, onay IP'yi ve kapsadığı satır sayısını eylem adlarıyla anıyor); H67b denetim süzgeçlerinde uç kurallarının istemci kopyası `actor_id ≥ 1` / `status_code 100–599` (platform_audit.py:75-76 kaynak satırları filtreler.ts'te yorumda) — geçersizde alan hatası ve istek ATILMAZ (`usePlatformVerisi`e isteğe bağlı `secenekler.etkin`), eskiden 422 tabloyu hata paneline çeviriyordu; H67a operatör-değil durumunda temizleme düğmesinin hiç çizilmediği sınandı; lint süzgeç/doğrulama/gruplama yardımcıları `pages/platform/filtreler.ts`e taşındı, `PlatformSecurity.tsx` yalnız bileşen dışa aktarıyor (react-refresh uyarısı kalmadı, toplam uyarı 883 → 878, hata 0); `ortak.tsx` PP2 notu (`changed:false` sonrası da yenile) korundu + Şef kabulü (mercek #142 3a) yazıldı; testler `filtreler.test.ts` (yeni, sınır değerleri + gruplama) · `PlatformSecurity.test.tsx` (IP gruplama, onay metni, operatör-değil, iki alan için hata+istek yok ve düzeltilince gider) · `PlatformEylemleri.test.tsx` (H62 cümlesi + yenileme); tam vitest 108/108 dosya 849/849 test yeşil; build yeşil; rota envanteri pini 80 oynamadı, `api/types.gen.ts` dokunulmadı — <merge tarihi>
