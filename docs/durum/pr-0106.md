#106 — SEC-10: KÜÇÜK GÜVENLİK SERTLEŞTİRME DEMETİ (GÖÇ YOK — alembic diff BOŞ, alembic başı 20260912_0082 KIMILDAMADI, ROTA SÖZLEŞMESİ 391/301 -> 392/301, types.gen.ts GÜNCELLENDİ); Güvenlik denetiminden gelen 4 sertleştirme maddesi tek PR'da birleştirildi: 1. SecretStr SARMALAMA: backend/app/config.py içindeki bootstrap_admin_password ve turnstile_secret_key alanları SecretStr | None ile sarmalandı; tüketim noktaları (validate_production_security, effective_bootstrap_admin_password, routers/auth.py::_secret_value) .get_secret_value() ile güncellendi; DSN KARARI: "DSN kept as str: engine/alembic init; password exposure via DSN is a deployment concern, see SEC-8"; turnstile_site_key genel/açık anahtar olduğu için str bırakıldı; static test test_no_secret_settings_field_is_bare_str database_url ve turnstile_site_key alanlarını gevşek regex yerine ADIYLA muaf tutarak hiçbir gizli alanın çıplak str kalmadığını doğrular; 2. verify-email AYRIŞTIRMASI: GET /api/auth/verify-email salt-okunur/iniş rotası haline getirildi (token'ı tüketmez veya yakmaz, ardışık GET istekleri idempotently 200 döner; ölen test: test_verify_email_get_is_non_mutating_and_idempotent GET tüketirse KIZARIR); POST /api/auth/verify-email VerifyEmailPayload(token=...) gövdesiyle token'ı tüketen ve e-postayı onaylayan uç olarak eklendi (ikinci POST 400 döner; ölen test: test_verify_email_post_consumes_token_and_second_post_fails); iç içe işlem desteği with (db.begin_nested() if db.in_transaction() else db.begin()): ile sağlandı; frontend/src/pages/VerifyEmail.tsx GET yerine POST atacak şekilde güncellendi; frontend/src/api/types.gen.ts export_openapi ve types:gen ile yeniden üretildi; rota sözleşmesi EXPECTED_OPERATION_COUNT 391/301 -> 392/301, parmak izi 618b656d47a1eba689a0f600d9bd9c9896ce9293e0f11aa71cd46436f8f40dc1 olarak güncellendi; Core sorgu envanteri 175 -> 176 (select 111 -> 112, verify_email_landing eklendi), parmak izi 5adfd8f592cd4458f7895642f96acf92ad3dd77aac977cd02546ee3873360b86 olarak kilitlendi; backend/app/main.py PUBLIC_API gerekçeli girdisi ve backend/tests/test_undeniable_endpoint_population.py PUBLIC_SESSION_WRITE_EXEMPTIONS güncellendi; test_self_service_registration.py (zaman duvarı ölçümü POST üzerine tutuldu) ve test_f0_email_registration.py yeni sözleşmeye uyarlandı; 3. quoteattr İLE UBL-TR XML GÜVENLİĞİ: backend/app/einvoice/ubl_xml.py:303 dosyasındaki gömülü XSLT filename özniteliği quoteattr(f"{invoice_id}.xslt") ile sarmalandı; tırnak ve etiket enjeksiyonu içeren düşmanca fatura kimliğiyle test_ubl_xml_quoteattr_hostile_invoice_id xml.etree.ElementTree üzerinden geçerli XML ayrıştırması doğrulandı; 4. restore_deleted SÜTUN KESİŞİMİ: backend/app/change_history.py::restore_deleted içinde yedek sütunları inspect(db.get_bind()).get_columns(table_name) ile canlı tablo sütunlarıyla kesiştirildi; tablodan düşen sütunlar değer basılmadan YALNIZCA tablo ve sütun adıyla WARNING seviyesinde günlüğe kaydedildi; DYNAMIC_SQL_FILE_ALLOWLIST change_history parmak izi 2f0e1e7a olarak güncellendi; ölen test: test_restore_deleted_drops_extraneous_columns (kesişim mantığı kaldırılırsa SQLite OperationalError ile KIZARIR); kontrol testi: test_restore_deleted_control_identical_payload; TESTLER: backend/tests/test_sec10_hardening_bundle.py (7/7 passed), izole koşucu test_sec10_hardening_bundle, test_self_service_registration, test_route_security_contracts, test_route_get_permission_inventory, test_undeniable_endpoint_population, test_ci_gates, test_core_query_inventory (49/49), test_tenant_scoping_guard (33/33) YEŞİL, vitest 89 dosya 628 test YEŞİL; alembic başı 20260912_0082 sabit; backend/alembic diff BOŞ.
