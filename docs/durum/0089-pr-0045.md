#45 — KANTAR FİŞİ v2 (göç `20260904_0069`): `field_harvest_tickets` + `field_harvest_ticket_deductions`, `GET/POST /api/field-harvest-tickets`, `units.resolve` HER SQL'DEN ÖNCE, `PUT /api/products/{id}` taban birim yazma yolu; DEFTER DEĞİŞMEZ (fiş stok hareketini oynatmaz, outbox'a olay yazmaz — `tests/test_kantar_fisi_defter.py` beş senaryo, `test_kantar_fisi_sozlesme.py` iki statik kapı: tüketici kaynağında fiş tablosunun adı geçmez, `create_harvest_ticket` `_entegrasyon_olayi_yaz` çağırmaz); TABAN `23902cf` (`origin/develop`, PR #43 sonrası; ölçüldü `git rev-parse` işe başlamadan) ve devir başı `2b663ce`; DEVİR NOTU YANLIŞ SHA VERİYORDU: taşınacak testler `a72ece7`de değil (frontend browserslist commit'i), `87db66a`da (`origin/claude/weighbridge-pr1-d9ix7v`, göç 0064 sözlüğü) — 0069 sözlüğüne taşındı (`gross_quantity`→`gross_entered_quantity`+`entered_unit`, `gross_quantity_total`→`base_quantity_total`); DEVİRDE "BİTTİ" DENEN İKİ KUSUR ÖLÇÜLDÜ VE DÜZELTİLDİ: (a) `PUT /api/products/{id}` `base_unit` yolu `log_request_activity(..., "UPDATE", ...)` çağırıyordu ve `ACTION_TYPES` kapalı katalog `{"detail":"Bilinmeyen aktivite tipi: UPDATE"}` ile 4xx veriyordu (ölçüldü, sözleşme smoke'u BACAK 1/2) — `product.base_unit_update` kataloğa eklendi (`backend/app/activity_log.py:85`); (b) `frontend/src/api/types.gen.ts` iki yeni uç için ÜRETİLMEMİŞTİ (ölçüldü: `python backend/export_openapi.py -o backend/openapi.json && (cd frontend && npm run types:gen)` → +180 satır) — contract-drift kapısı kırmızı olacaktı, yeniden üretildi, `frontend/src/test/fixtures/pos-api.json` DEĞİŞMEDİ, `npx tsc -b` çıkış 0; SAHİP KARARLARI (inceleme sonrası, bu PR'da): (1) `entered_unit` KANONİK saklanır (`HarvestTicketWrite.girilen_birim` → `units.turkce_katla`, `products.base_unit` ile AYNI katlama; ham dizgi hiçbir yerde tutulmaz) — pin: POST `"Ton"` → okunan VE saklanan `"TON"`; (2) açık `base_unit: null` ya da katlanınca boşa düşen dizgi → 422 `{"code":"TABAN_BIRIM_SILINEMEZ"}` HER SQL'DEN ÖNCE (`backend/app/routers/products.py` `update_product`), sütun/stok/günlük dokunulmamış; alanı GÖNDERMEYEN PUT sütuna dokunmaz (ölçüldü, günlük 1'de kaldı); ilk yazım `"   "` girdisini sütuna `''` OLARAK YAZIYORDU (ölçüldü: 200 döndü), `or None` ile aynı kapıya alındı; (3) ÖLÇÜLMÜŞ KUSUR, C1 KAPSAMI DIŞI, C2'YE: `POST /api/products` `base_unit`i şemada kabul edip SESSİZCE DÜŞÜRÜYOR — `backend/app/routers/products.py:307` `values = payload.model_dump()` alanı taşır, `:325` `INSERT INTO products(...)` sütun listesinde `base_unit` YOK, text() fazla bağlı parametreyi sessizce yok sayar; tek yazma yolu PUT (sözleşme smoke'u ürünü POST ile açıp taban birimi PUT ile yazıyor, bu yüzden ölçüldü); (4) `sold_exceeds_net` kabul edilmiş görünür bedel — aşağıda ÖLÇÜLMEDİ'de; SONLULUK (c) UÇ KATMANI (`test_kantar_fisi_sonluluk.py`): 4 değer (`NaN`,`sNaN`,`Infinity`,`-Infinity`) × 4 yer (brüt, kağıt neti, kesinti oranı 0 ve 1) → 422, `detail[].loc` alanı adıyla, iki tabloda satır sayısı tabana göre DEĞİŞMEDİ, fikstür canlı (aynı yol 201 verdi); grid İKİ ALAN için GERÇEK sınırlardan (`model_fields[...].metadata`) her koşumda yeniden ölçülür (Pydantic 2.13.4 yerelde / 2.13.5 ilk yazım, aynı sonuç): `allow_inf_nan` varsayılanda dördü `finite_number`, `True`+üst sınır varken NaN/sNaN/Inf `less_than_equal` ve -Inf alt sınır, `True`+üst sınır yokken YALNIZ `Infinity` KABUL — ilk yazımın "NaN alt sınırda düşer" cümlesi DÜZELTİLDİ (Pydantic üst sınırı ÖNCE sorar); "gövde `sebep` taşır" iddiası bu girdiler için ÖLÇÜLMEDİ, DOĞRU DEĞİL: (a) katmanı önde durduğu için gövde Pydantic `detail`dir, `sebep` (b) katmanının dilidir ve uçtaki yüzü sözleşme dosyasında TABAN_BILDIRILMEMIS/BIRIM_TANIMSIZ/BOYUT_UYUSMAZLIGI bacaklarıyla ölçülüyor; MUTASYONLAR (birleşmiş ağaç, her biri uygulandı→kırmızı→`cp` ile geri): `units.resolve` `is_finite()` kapısı → sonluluk `test_COZUCU_*` 4 + `SIZDIRMIYOR` (5 kırmızı), uç katmanı YEŞİL KALIR (şema önde); `_turetilmis_net` sıralı → sözleşme senaryo 1 (`950.6000 != 950.0000`) + defter (1.1412); `_fis_gorunumu` kağıt neti türetime → sözleşme senaryo 3 (`KAGIDIN NETI TURETIME SIZDI`) + defter; `taban_birim or payload.entered_unit` → sözleşme BACAK 1 (`TABAN YOKKEN FIS YAZILDI`); `_FARM_PATH_PREFIXES`ten önek silme → `test_fis_ucu_farm_iznine_bagli` + `test_farm_management_api.py::test_every_farm_endpoint_is_covered_by_the_farm_permission_prefixes`; `girilen_birim`den `turkce_katla` → sözleşme senaryo 5 (`GIRILEN BIRIM KATLANMADI`); açık-null kapısı → BACAK 1 devamı (`ACIK NULL SESSIZCE YAZILDI`, PUT 200, sütun NULL); PG İKİZİ: YENİ DOSYA AÇILMADI (popülasyon 102 üç yerde sabit), üç smoke `backend/test_kantar_fisi_postgresql.py`e importlib ile bağlandı (`test_farm_harvest_revenue_postgresql.py` kalıbı), üç smoke ortak `KantarFisi!123` + aday döngüsü (PR #38 kalıbı) ve TABANA GÖRE FARK sayaçları (aynı firma paylaşılıyor), taze PG 16 şemasında (docker 5433, `DROP SCHEMA public CASCADE`) 15/15 iki kez (kararlardan önce ve sonra); ÇİVİLER (CPython 3.12.10, birleşmiş ağaç, her biri ölçülüp gerekçesiyle yazıldı): `farm.py` dinamik text() 15 SABİT / AST bd42a626→d1541360 (yeni SQL'lerin hepsi sabit metin, `ticket_id IN :ids` `bindparam(expanding=True)`, üç halkalı taban birim SELECT'i kiracı içinde), `products.py` 6 SABİT / dec8ff78→09c36634→99e57d37, `activity_log.py` 3 SABİT / f7e01786→6da9d10e (sözlük girişi), küresel dinamik toplam 254 SABİT, alt süreç SQL 103/166→106/169 (+3 dosya +3 metin: defter, sözleşme, sonluluk smoke'ları; app/ altında yine sıfır), route sözleşmesi 347/268/45a56fbd→349/269/adb27fb5, GET envanteri 166/710073c8→167/0789829a (`GET /api/field-harvest-tickets → farm.view`), kimlikli 337→339, `read` 93 SABİT, undeniable 99→100, farm-view kümesi 32→33, Core envanteri 137 (89/41/7) ve 787d65cf DEĞİŞMEDİ (not düşüldü: dilimin bütün SQL'i text()), alembic başı 0069 ×3 ve PG popülasyonu 102 ×3 devirden; `units.py` doküman düzeltmesi: `f1e8ca4` (PR #25) `caf4114` ile İNDİ ve "kopya silinir" planı ÇÜRÜDÜ — `farm.py` `_bitki_katla` AŞAĞI, `turkce_katla` YUKARI katlar, "Weißkohl"/"I+U+0307" çiftleri ayrışır, ikisi BİLEREK ayrı; `git diff --stat origin/develop..6ec1973` bu PR'ın kendi işi için 14 dosya (+1027/−80: 2 yeni test dosyası, sonluluk 351 satır, types.gen +180, units.py doküman, 6 çivi dosyası, activity_log/products/farm_schemas) — 0069 göçü/uçlar/şemalar devirden (`a5805e8`,`cc4188d`,`2b663ce`); ÖLÇÜM: `python -m pytest tests --ignore=tests/fixtures` → 926 geçti / 0 düştü (7:00), devir başında aynı sweep 901/9 idi (7 çivi + 2 sıra bağımlı `test_line_item_tenancy_slice1`); PR ilk koşusu `33956459087` (`6ec1973`); ÖLÇÜLMEDİ, ADIYLA: `sold_exceeds_net` HASAT BİRİMİ ile FİŞ BİRİMİ ÇEVRİLMEDEN karşılaştırılır (harvest unit vs ticket unit not converted — 980 KG satılan ile 1.14 TON net "aşıyor" der; defter smoke'unda görünür bedel olarak çivili, red değil gösterge); `POST /api/products` `base_unit` düşürmesi (yukarıda, C2); `sold_quantity`nin fiş toplamıyla ilişkisi birden çok fişte; frontend'de fiş ekranı (bu dilimde yok, `types.gen.ts` yalnız üretildi); Firefox/WebKit; CI'ın kendi backend-postgresql koşumu (yerel taze şema ile ölçüldü, CI koşumu bu kaydın yazıldığı anda sürüyordu).
