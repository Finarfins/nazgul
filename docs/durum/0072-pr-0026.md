#26 — `@xmldom/xmldom` 0.9.10 -> 0.9.12: bu PR bir özellik değil, YENİ YAYIMLANMIŞ bir advisory'nin (GHSA-6gmq-8vp8-gcm6, moderate, kırılgan aralık `0.9.0 - 0.9.11`) dayattığı bir bağımlılık yükseltmesidir ve tek işi deponun kendi kapısını yeniden açmaktır; ÖLÇÜLEN KUSUR: `develop` (`7498ab3`) KENDİ `frontend` işini kırıyordu (`AUDIT_POLICY_RED: unapproved advisory GHSA-6GMQ-8VP8-GCM6 (@xmldom/xmldom)`) ve bu kod dalın hatası olmadığı için AÇIK OLAN HER PR'a aynen iniyordu — bu VARSAYILMADI, ÜÇÜ DE TEK TEK ÖLÇÜLDÜ: PR #21 (`33660870361`), PR #24 (`33665941934`) ve PR #25 (`33667802309`) işlerinin üçünde de düşen adım aynı satırı yazıyor, yani kuyruğun tamamı tek bir kilit dosyası satırının ardında bekliyordu ve hiçbiri kendi değişikliği yüzünden kırmızı değildi; advisory kimsenin YAZDIĞI bir şeyden gelmiyor, ZAMANIN GEÇMESİNDEN geliyor: taban değişmeden yeşil olan bir ağaç, yukarı akışta bir kayıt yayımlandığı için kırmızıya döndü ve bu tam olarak audit kapısının VAR OLMA SEBEBİDİR — kapı gürültü yapmadı, işini yaptı; ZİNCİR DEV TARAFINDADIR VE DOĞRUDAN DEĞİLDİR: `@capacitor/cli@8.5.0 -> plist@3.1.1 -> @xmldom/xmldom@0.9.10`, yani paket `package.json`da HİÇ GEÇMEZ ve üretim paketine girmez, ama kapı severity'ye göre değil GHSA KÜMESİNE göre çalıştığı için "dev-only" bir mazeret DEĞİLDİR ve olmamalıdır da, çünkü "zaten dev" cümlesi bir kez kabul edilirse ikinci kez sorgulanmaz; ALLOWLIST REDDEDİLDİ VE GEREKÇESİ PR #23'ÜN KOYDUĞU ÖLÇÜTTÜR — "yaması OLAN bir advisory'yi allowlist'e yazmak o listenin ANLAMINI aşındırır" — ve buradaki olgu tam olarak odur: yama VARDIR (`0.9.12`) ve ERİŞİLEBİLİRDİR, çünkü `plist@3.1.1` zaten `^0.9.10` istiyor, yani yamalı sürüm MEVCUT aralığın içine düşüyor ve ona ulaşmak için ne bir major yükseltme ne bir `overrides` girdisi ne de `package.json`a dokunmak gerekiyor; bu yüzden düzeltme KİLİT DOSYASI YÜKSELTMESİDİR ve `package.json` BAYT BAYT AYNIDIR — bu iddia da ölçüldü, `git hash-object package.json` öncesinde ve sonrasında `729c8b8d0916dead857d228461c5c47a7f9b12e6`; DİFF ALTI SATIRDIR ve yalnız `frontend/package-lock.json` dosyasındadır: `@xmldom/xmldom` 0.9.10 -> 0.9.12 ve `@capacitor/cli` 8.5.0 -> 8.5.1, ikincisi `npm audit fix`in kendiliğinden getirdiği ve mevcut `^8.5.0` aralığının İÇİNDE KALAN bir yama yükseltmesidir, bildirilen bir aralık genişlemesi değildir ve gizlenmesin diye burada AÇIKÇA yazılmıştır; ALLOWLIST'TE KALAN TEK KAYDA DOKUNULMADI: `GHSA-W5HQ-G745-H8PQ` (uuid, `@capacitor/cli -> xcode -> uuid`) yamalı bir zinciri OLMADIĞI için kendi gerekçesiyle listede duruyor ve bu PR onu ne genişletti ne yeniledi — kapı koştuktan sonra `AUDIT_POLICY_GREEN: exact GHSA set matched (1)` diyor, yani liste artık TAM OLARAK gerçekten kabul edilmiş olan tek advisory'yi içeriyor ve fazlası yok, ki kapının "bayat allowlist girdisi de kırmızıdır" kuralı bunu ayrıca doğruluyor; KİLİT DOSYASI DÜZELTMESİ AUDIT'İ YEŞİLE ÇEVİRİP DERLEMEYİ KIRARSA ADVISORY'DEN DAHA KÖTÜDÜR, bu yüzden kapının TAMAMI koşuldu ve yalnız audit adımı değil: `npm ci` (609 paket, 610 denetlendi), `npm run audit:policy` YEŞİL, `npx tsc -b` çıkış 0, `npm run build:bridge` çıkış 0 (4.72s), `npm test` 89 dosya / 593 test tamamı geçti / 0 başarısız; KIRMIZI ÖNCE YERELDE ÜRETİLDİ: düzeltmeden önce aynı ağaçta `npm run audit:policy` CI'ın yazdığı satırın BİREBİR AYNISINI yazdı, yani bu iş uzaktaki bir günlüğe bakıp çıkarım yaparak değil kusuru elde tutarak yapıldı; ÇİVİLERİN HİÇBİRİ OYNAMADI ve oynamamalıydı da: bu dal Python dosyalarına, route yüzeyine ve göç zincirine HİÇ dokunmuyor, `git diff --stat origin/develop...HEAD` tek dosya ve altı satır gösteriyor, dolayısıyla `route_security`, `authorization_population`, `tenant_scoping_guard`, `goc_zinciri`, `pg_population` ve `EXPECTED_UNDENIABLE` sabitlerinin hepsi develop'un değerinde SABİT kalır — bunlardan biri kımıldasaydı bu diff'in tarif ettiğinden başka bir şey yapıldığı anlamına gelirdi; ÖLÇÜLMEDİ: Android/Capacitor mobil derlemesi bu dalda koşulmadı ve `@capacitor/cli` yama yükseltmesinin mobil derlemeye etkisi bu turda ÖLÇÜM DEĞİL, sadece aralık içinde kaldığı gözlemidir; `npm run lint`, `npm run typecheck:e2e` ve backend shard'ları da yerelde koşulmadı, çünkü bu diff onlara dokunmuyor ve doğrulama CI'a bırakıldı.
