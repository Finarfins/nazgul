#44 — KİRACI DIŞA AKTARIMI: `GET /api/company/export` tek firmanın TÜM satırlarını topolojik sırayla akan bir zip olarak verir; TABAN `23902cf1eb717906fe0f58e74967e43473529160` (`origin/develop`, ölçüldü `git rev-parse HEAD` işe başlamadan, oturum açılış anlık görüntüsüne GÜVENİLMEDİ); ayrı worktree `F:\nazgul-disa-aktarim`, `F:\nazgul` İÇİNDE ÇALIŞILMADI; NEDEN YENİ MEKANİZMA: `routers/platform_backups.py` `pg_dump` ile KÜMENİN TAMAMINI alır ve başka kiracıların verisini taşıdığı için bir kiracıya TESLİM EDİLEMEZ, bu uç onu yeniden kullanmaz; ÖLÇÜM (yerelde, keşif devralınmadı): `MetaData.reflect` 115 tablo, `company_id` taşıyan 102 ve `TENANT_TABLES` ile TAM KÜME EŞİTLİĞİ (`cid-not-in-TT` [], `TT-not-cid` []), `sort_tables` 102 benzersiz ad ve 0 sıra ihlali, bağımsız Kahn 7 katman `[46, 18, 14, 13, 5, 4, 2]`, kendine referans 5 MANTIKSAL ilişki (`animals.father_id`/`mother_id`, `payment_allocations` iki sütun, `receivable_charge_documents.reversal_of_document_id`) ama SQLAlchemy 8 FK sütun BACAĞI sayar çünkü 3'ü bileşik `company_id` bacağıdır — görev metnindeki 5 doğru, 8 aynı grafiğin başka birimidir; `app/db.py:38` `create_engine` çağrısında `isolation_level` YOK (doğrulandı) → PG varsayılanı READ COMMITTED, uç kendi seviyesini `SET TRANSACTION ISOLATION LEVEL REPEATABLE READ, READ ONLY` ile bağlantı düzeyinde yükseltir; TABLO SIRASI TÜRETİLİR: uygulama kiracı kümesini ŞEMADAN kurar (`company_id` sütunu olan her tablo) ve `sort_tables` ile dizer, `TENANT_TABLES` üretim koduna KOPYALANMADI (üretim kodu `tests/`ten import etmez), test o frozenset'i İTHAL EDİP türetilen kümeyle eşitliğini doğrular; YETKİ: yeni ROL YOK ve yeni izin adı YOK — `required_permission`a TEK satır eklendi ve `__admin_only__` döndürüyor, yani `auth.py:991`deki deny-by-default nöbetçisiyle AYNI ad (`ROLE_PERMISSIONS["admin"] == {"*"}`, `ROLE_RANK` admin=100 tek başına en tepede; `has_permission` ölçüldü: admin True, yonetici/muhasebe/satis/depo/rapor False); İLK TASARIM GERİ ALINDI: `company.export` adında yeni bir izin yazılmıştı, `test_route_security_contracts`in `known_permissions` kümesi `ROLE_PERMISSIONS` değerlerinden türediği için o ad BİLİNMEYEN düşüyordu ve kabul ettirmek kapıyı GENİŞLETMEK olurdu — mevcut sentinel zaten `test_role_fallback_failclosed` ile kapalı; ÜYELİK ve ROL ayrı ayrı düşer (ara katman): üye olmayan `resolve_company` → 403 `COMPANY_ACCESS_DENIED`, düşük rollü ÜYE → 403 `PERMISSION_DENIED` (ikisi de teste yazıldı); MANİFEST zip'in SONUNA yazılır çünkü `row_counts` ancak satırlar akıtıldıktan sonra bilinir ve manifesti başa koymak tüm gövdeyi bellekte tutmayı gerektirirdi (zip merkezi dizini zaten dosyanın sonundadır); `schema_revision` VERİTABANINDAN okunur (`alembic_version.version_num` = `20260904_0068`), koda GÖMÜLMEDİ; `exported_at` `app.auth.utcnow` (görev metnindeki `app/donmus_saat.py` YOK — `donmus_saat.py` `backend/` kökünde bir TEST yardımcısıdır, uygulama saati `auth.utcnow`tur; `app/tenancy.py` ise VAR ve `resolve_company` oradadır); AKTİVİTE KAYDI kesitten SONRA yazılır (`company.exported` / `backup`, `ACTION_TYPES`a tek girdi) — ÖNCE yazılsaydı satır dışa aktarımın KENDİ `activity_logs` dosyasına düşerdi ve boş firma iddiası yalan olurdu; ÖLÇÜLEN SAPMA, görev metni düzeltildi: "boş firma → 102 boş ndjson + sıfır sayım" İMKÂNSIZ, çünkü dışa aktarımın kendisi üyelik ister ve üyelik satırı `user_company_memberships`tedir — test o tabloyu TAM 1 satır olarak doğrular, diğer 101'in boşluğu aynen aranır; BELLEK TESTİ İKİ KERE YAZILDI: sabit eşikli ilk sürüm YANLIŞTI (ölçüldü `TEPE 14.734.825` ama neredeyse tamamı YÜKTEN BAĞIMSIZ sabit maliyet — 115 tablonun `reflect`i; ayrıca tekrarlı dolgu zip'te eriyip gittiği için `GOVDE` yalnız 43.792 bayt kaldı, yani ölçüm konusuzdu), yerine FARKSAL ölçüm kondu (yüksüz → 5000 satır SIKIŞTIRILAMAZ `os.urandom` notu): `GOVDE1 18.498 / TEPE1 14.480.400`, `GOVDE2 809.402 / TEPE2 11.130.642`, yani `GOVDE_ARTIS +790.904` iken `TEPE_ARTIS -3.349.758` (NEGATİF) — gövde ~791 KB büyürken tepe bellek DÜŞTÜ; eşikler `govde_artis > 700_000` ve `tepe_artis < govde_artis / 2`; TEK İŞLEM kapısı: `engine` `begin`/`commit` olayları dinlendi, bir istekte DÖRT `begin` ve TEK `commit` ölçüldü (oturum belirteci, `resolve_company`, dışa aktarımın tek okuma işlemi, aktivite kaydının yazma işlemi) — kapının anlamı sayının küçüklüğü değil 102 OLMAMASIDIR; TESTLER 13/13 yeşil (24s), her birinin adı konmuş mutasyonu var (izin satırını sil, `memberships` yüklemini kaldır, izni `rapor`a ekle, `schema_revision`ı göm, `_kiraci_tablolari`yı elle listeye çevir, `sort_tables`→`sorted()`, herhangi bir tabloda `.where(company_id)` kaldır → test o tablonun ADINI söyler, `Decimal`→`float`, eksik-dosya dalını `raise` yap, `_gunlukle`yi sil, aktivite kaydını kesitten öncesine al, tüm zip'i `BytesIO`da kur, tablo başına `engine.connect()`); EKLER: diskte VAR olan ek (silinmiş `deleted_at` satırınınki DAHİL, manifest `attachments_include_deleted: true`) zip'e girer, diskte OLMAYAN ek dışa aktarımı DÜŞÜRMEZ ve `missing_attachments`a yazılır, satırın kendisi yine ndjson'dadır (ölçüldü: `attachment_count` 2, `missing_attachments` 1, `work_order_attachments.ndjson` 3 satır); KAPI YENİDEN SABİTLEMELERİ (Python 3.12, her biri tek satır gerekçeli): `test_core_query_inventory` 137→140 sorgu / select 89→92 / parmak izi `787d65cf…`→`36bbb1cf…` ve `UNRESOLVED_ALLOWLIST` BOŞ→3 girdi (satır okuması, `alembic_version`, `companies` tekil satırı — üçü de hedefi çözülemeyen çünkü tablo çalışma zamanında yansıtılıyor; kiracı yüklemi `.where(tablo.c.company_id == cid)` olarak statik GÖRÜNÜR, yalnız tablo NESNESİ değişkendir), ayrıca `test_no_unsupported_expressions` artık `UNRESOLVED_ALLOWLIST`i de onurlandırıyor çünkü aynı üç kayıt iki kapıya birden düşüyordu ve ikincisi yalnız SATIR NUMARASINA bakan `METHOD_FORM_ALLOWLIST`i biliyordu — satır numarası kimliğe girince araya bir satır eklendiğinde SEBEPSİZ kırılırdı, eşleşme hâlâ dosya + PARMAK İZİ ile DAR; `test_tenant_scoping_guard` `app/activity_log.py` parmak izi `f7e01786…`→`1c15afcd…` (SQL METNİNE DOKUNULMADI: dinamik `text()` çağrısı o dosyada 3'te SABİT, metinler BİREBİR aynı, küresel toplam 254'te SABİT — parmak izi kımıldadı çünkü dosya AST'sinin TAMAMINDAN türüyor ve `ACTION_TYPES` bir sözlük LİTERALİDİR) ve alt süreç SQL sayacı 103→104 dosya / 166→168 metin (yeni izole test dosyası; `app.config.Settings` modül düzeyinde TEK KOPYA olduğu için taze bir `DATABASE_URL` ancak ayrı süreçte görülür, alt süreç ZORUNLU); `DYNAMIC_SQL_FILE_ALLOWLIST` ELLE GENİŞLETİLMEDİ ve `TENANT_TABLES`a DOKUNULMADI (C1 hunk'ı); `test_route_get_permission_inventory` 166→167 + parmak izi `710073c8…`→`858acdf4…`; `test_route_security_contracts` 347→348 işlem / 268→269 yol + parmak izi `45a56fbd…`→`200f3c7f…` (ayrı `review_reason` gerekmedi: uç kiracı kapsamlı, platform yedeği gibi router düzeyinde ek operatör listesi kullanmıyor); `test_authorization_population_reconciliation` 337→338; BİLEREK GEVŞETİLEN TEK YASAK, sessizce alınmadı: `test_core_tenant_scoping_guard.py::test_yansimayla_tablo_kurulamaz` bir MEKANİZMA YASAĞIDIR ve `BEKLENEN_YANSIMA_SAYISI = 0` ile çapalıydı ("yansıma tablo adını çalışma zamanına taşır ve statik olarak görülemeyen bir kiracı yüzeyi açar"), bu PR 0→1 yapıyor (tek bulgu: `app/routers/kiraci_disa_aktarim.py:163 .reflect()`), kapının KENDİ docstring'i bunu öngörüyor ("yazılmamış sıfır geri büyür — bu yüzden çapalanıyor ve büyümesi ayrı bir karar oluyor") ve gerekçe kapının yanına yazıldı; TELAFİ EDİCİ DENETİM ÇALIŞMA ZAMANINDA: `test_hicbir_dosyada_baska_firmanin_satiri_yok` iki firmayı da veriyle tohumlar (A: `12.3456`, B: `99.9999`), üretilen zip'teki HER tablonun HER satırını gezer ve `company_id`yi doğrular, tek bir tabloda yüklem düşerse o tablonun ADINI söyleyerek kırılır — statik kapının 102 tabloda veremeyeceği kanıt budur ve sayı 1'de çapalı kaldığı için ikinci bir yansıma yolu yine kapıyı kırar; SÖZLEŞME ZİNCİRİ: `python backend/export_openapi.py -o backend/openapi.json` (dosya `.gitignore:83` ile İZLENMİYOR, üretilen artefakt) → `npm run types:gen` (worktree'de `npm ci` gerekti) → `python backend/tools/capture_frontend_fixtures.py`, sonuç: YALNIZ `frontend/src/api/types.gen.ts` değişti (+49 satır), `frontend/src/test/fixtures/pos-api.json` DEĞİŞMEDİ; `npx tsc -b --force` çıkış 0 (`tsc --noEmit -p tsconfig.json` KULLANILMADI — o çağrı sıfır dosya denetler ve boş yeşil verir); TAM İZOLE TAKIM `python -m pytest tests --ignore=tests/fixtures -q -p no:randomly` → 909 geçti / 0 başarısız / 1 seçilmedi (437.08s = 7m17s) — ilk koşumdaki tek kırmızı `test_yansimayla_tablo_kurulamaz` idi ve yukarıdaki kararla kapatıldı; `deploy/ci-required-contexts.json` ve `.github/workflows/ci.yml` DOKUNULMADI (`git diff origin/develop --` her ikisi için BOŞ) ve `python deploy/ci-gerekli-baglam-kapisi.py .github/workflows/ci.yml deploy/ci-required-contexts.json` → `17 baglam manifest ile tam kume esitliginde`, çıkış 0; `git diff --stat origin/develop` ON İKİ dosya: `backend/app/activity_log.py` (+4), `backend/app/auth.py` (+8), `backend/app/main.py` (+2), `backend/app/routers/kiraci_disa_aktarim.py` (+385, yeni), `backend/tests/test_kiraci_disa_aktarim.py` (+625, yeni), `backend/tests/test_core_query_inventory.py`, `backend/tests/test_core_tenant_scoping_guard.py`, `backend/tests/test_route_get_permission_inventory.py`, `backend/tests/test_route_security_contracts.py`, `backend/tests/test_tenant_scoping_guard.py`, `backend/tests/test_authorization_population_reconciliation.py`, `frontend/src/api/types.gen.ts` (+49); GÖÇ YOK, YENİ TABLO YOK, YENİ PG İKİZ DOSYASI YOK (populasyon iğnesi 101 C1'e aittir); C1 (`feat/kantar-fisi-v2`, `9d71bd7`, işe başlarken AÇIK PR YOKTU — `gh pr list` `[]`) ile beklenen ÖNEMSİZ çakışma İKİ dosyada: `app/auth.py` (C1 bir satır, ben bir satır, farklı konumlar) ve `tests/test_tenant_scoping_guard.py` (C1 `TENANT_TABLES`a 8 satır, ben yalnız `activity_log.py` parmak izi + alt süreç sayaçları — ayrı hunk'lar); C1'in dosyalarına (`farm.py`, `farm_schemas.py`, `products.py`, `ci.yml`, `alembic/versions/*`) DOKUNULMADI; ÖLÇÜLMEDİ, ADIYLA: (1) PG'de `REPEATABLE READ` + `READ ONLY`nin gerçekten uygulandığı — kod `conn.dialect.name == "postgresql"` dalında `SET TRANSACTION`ı çalıştırıyor ama PG'DE KOŞTURULMADI ve SQLite'ta dal hiç girilmiyor, C1 birleştikten sonra ayrı bir düzeltme turunda PG-özel vakalar eklenecek; (2) dışa aktarım sürerken `field_stok_zamanlayici` yazarsa ne olduğu — beklenti PG'de kesitin korunması ve SQLite WAL'da okuyucunun engellenmemesi, DOĞRULANMADI; (3) dışa aktarım BOYUT TAVANI — büyük bir kiracıda zip'in ne kadar büyüyeceği ve isteğin ne kadar süreceği ölçülmedi, zaman aşımı/boyut sınırı YOK; (4) `missing_attachments` listesinin sınırsız büyüyebilmesi (çok sayıda eksik dosyada manifest şişer); (5) uç için frontend YÜZEYİ yazılmadı, yalnız `types.gen.ts` türedi.
