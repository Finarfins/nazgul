#90 — H10: run_isolated_tests.py cp1254 konsolunda UnicodeEncodeError çökmesi giderildi (H1 çocuk çıktısı çözümlemesini düzeltmişti, koşucunun kendi stdout/stderr'i cp1254 kalmıştı); DÜZELTME: koşucu başlangıcında sys.stdout ve sys.stderr configure_runner_streams() ile reconfigure(encoding="utf-8", errors="replace") yapıldı, alt süreç ortamına PYTHONIOENCODING=utf-8 eklendi; 3 yerel kırmızı ölçüldü: test_platform_maintenance_migration, test_v2_9_production_bootstrap_security ve tests/test_donmus_saat_kapisi alt süreçlerinde subprocess.run(text=True) çağrılarına encoding="utf-8", errors="replace" verilmediği için Türkçe Windows cp1254 ortamında torun çıktısının (alembic / python -c) UTF-8 baytları decode edilirken 0x9e gibi tanımsız baytlarda UnicodeDecodeError ve Türkçe harflerde mojibake ("varsayılan parola" -> "varsayÄ±lan parola") ürettiği tespit edildi ve test-tarafında encoding="utf-8" eklenerek düzeltildi; alt_surecte_sql AST envanter sayısı (test_tenant_scoping_guard) ölçüldü, pin kımıldamadı; test_isolated_test_runner.py içine cp1254 üst akış simülasyonu altında FAIL satırında \ufffd yayan çocuğun koşucuyu çökertmeden raporlandığı birim testi eklendi (25/25 yeşil); backend/app SIFIR; göç YOK; TABAN origin/develop 49c2cbd; dal test/h10.
