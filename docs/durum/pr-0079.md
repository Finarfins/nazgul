#79 — 1B-G PARTİ MUTABAKATI: GÖÇ YOK, tablo yok, hiçbir şey YAZILMIYOR. `app/parti_mutabakat.py` `warehouse_stocks` (stok OTORİTESİ, (firma,depo,ürün) başına tek satır; `products.stock` onun TOPLAMI, üçüncü sayı DEĞİL) ile `product_lots`u (İKİNCİ KIRILIM, üçüncü defter DEĞİL) (ürün,depo) başına karşılaştırır; ÜÇ KOVA ve ikisi DEĞİL çünkü iki kova (eşit/eşit değil) bugün partisiz yazan yolların HEPSİNİ tek gürültü yığınına atardı ve içindeki TEK gerçek kusur GÖRÜNMEZ olurdu — gürültüyle dolu bir uyarı uyarı DEĞİLDİR; `LOTSUZ_TASARIM` SAYILIR ama KIRMIZI DEĞİLDİR, kırmızı yalnız `SAPMA`. İKİ KARAR SESSİZ BIRAKILMADI: (a) `LOTSUZ_TASARIM` "parti toplamı 0" DEĞİL "PARTİ SATIRI YOK"tur (`COUNT(*)`), çünkü 1B-B tükenen partiyi 0'a çeker ve SİLMEZ (0067: o satır geri çağırmanın KANITI) ve "toplam 0" mekanizması tükenmiş VE DÜZGÜN KAPANMIŞ bir partiyi "hiç açılmadı" diye gösterip kovanın ADINI YALAN yapardı — doğru cevap `ESIT`; (b) `parti_toplami > stok` dalı KOŞULSUZDUR ve `LOTSUZ_TASARIM`ın ÖNÜNDE durur: parti satırı yokken `0 > stok` ancak NEGATİF stokta doğrudur (`negative_stock_policy='allow'` serbest bırakabilir) ve "eksi bakiye" ile "bu çift bilinçli olarak partisizdir" AYNI CÜMLE DEĞİLDİR — sıra ters olsaydı eksi bakiye "tasarım" etiketiyle GÖMÜLÜRDÜ; `fark = stok - parti_toplami` İŞARETLİ taşınır ve `fark < 0` TEHLİKELİ yöndür (defter ELDE OLMAYAN malı VAR gösteriyor). KISIT NEDEN YOK, üçü de ölçüldü: (1) satır içi `CHECK` başka tabloya BAKAMAZ ve bu depoda tetikleyici YOKTUR (göç zinciri saf DDL); (2) PG'de kurulup SQLite'ta kurulamayan kısıt iki diyalekti İKİ FARKLI ŞEMA yapardı ve testlerin yeşili üretimi TEMSİL ETMEZDİ; (3) kısıt lot-suz yolları REDDEDERDİ ve reddettiği şey BUGÜN ÇALIŞAN iş akışı olurdu — doğru cevap "yaz/yazma" değil GÖRÜNÜR KILMAK ile GİZLEMEK arasındadır; DÜZELTME ucu da YOK çünkü iki defteri uyumlu GÖSTERİR ama malın hangi partiden çıktığını SÖYLEYEMEZ. TEK SQL İKİ DİYALEKT: `FULL OUTER JOIN` YAZILMADI (SQLite 3.39 öncesi tanımıyor; kapı koştuğu MAKİNEYE göre yeşil olurdu), sürücü küme `UNION` (`UNION ALL` DEĞİL), `GROUP BY` seçilen HER sütunu sayıyor (PG'nin işlevsel bağımlılık kuralı SQLite'tan DAR), kova PYTHON'da (SQL `CASE`i kuralın İKİNCİ KOPYASI olurdu ve karşılaştırma diyalektin TİPİNE bağlı kalırdı — `0.1+0.1+0.1 != 0.3` sessiz bir `SAPMA` uydururdu; iki sayı da `money.quantity` ile `NUMERIC(18,4)`e çekiliyor). `GET /api/products/lots/mutabakat` — izin `read` (ÖLÇÜLDÜ, `auth.py`ye satır EKLENMEDİ), kiracı kapsamlı, sayfalı; `counts` SAYFANIN DEĞİL KİRACININ TAMAMININ (ikinci sayfadaki tek `SAPMA`yı ilk sayfaya bakan operatör görmezdi) ve bedeli — kural tek kopya olduğu için tam tarama — belgede YAZILI; yol `/{product_id}/lots`tan ÖNCE yazıldı. KAPILAR: `tests/test_1b_g_mutabakat.py` 1B A–F'nin MUTLU yollarını TEK veritabanında arka arkaya koşturup `SAPMA == 0` ölçüyor (altı dilim AYRI AYRI yeşildi; ölçülmemiş olan BİRLİKTE koştuklarında da yeşil kaldıklarıydı), tükenmiş partinin `ESIT` kaldığını, bilerek partisiz BEŞ yolun (partisiz alış/ayarlama/transfer kaynağı+hedefi/satış, KAYNAKSIZ satış iadesi) `LOTSUZ_TASARIM`a ADIYLA düştüğünü ve `SAPMA`yı KİRLETMEDİĞİNİ, ham `UPDATE product_lots SET quantity=quantity+1` enjeksiyonunun TAM OLARAK BİR sapma üretip o çifti ADIYLA verdiğini (`fark == -1`), komşu firmanın raporunun bizim çiftlerimizle KESİŞMEDİĞİNİ ve sayfalamanın `counts`u BOZMADIĞINI çiviliyor; PG ikizi `test_1b_g_mutabakat_postgresql.py` SQLite betiğini AST ile OKUYOR (KOPYALAMIYOR — iki metin ayrışırsa "ikiz neyi ölçüyor" SORULAMAZDI), `NUMERIC(18,4)`ü `information_schema`dan doğruluyor, üç kez `0.1`in `Decimal('0.3000')` toplandığını ve SAHTE `SAPMA` ÜRETMEDİĞİNİ, dördüncü basamağın (`+0.0001`) GÖRÜLDÜĞÜNÜ ölçüyor. MUTASYONLAR KOŞULDU (varsayılmadı, CPython 3.12): `SAPMA` tamamen `LOTSUZ`a çevrilince DÖRT kapı; `parti_toplami > stok` dalı silinince (VE sıra bozulunca — aynı kusurun iki yüzü, ÖLÇÜLDÜ) adaş kapı; kiracı yüklemi `UNION` kollarının HERHANGİ BİRİNDEN düşürülünce STATİK VE DAVRANIŞ kapıları birden KIRMIZI. ÜÇÜNCÜ esas (parti toplamı alt sorgusu) düşünce YALNIZ statik kırmızı ve bu ÖLÇÜLMÜŞ bir olgu: `lt` kiracıyı `p`den DEVRALIYOR, yani o yüklem doğruluk için gereksiz derinlemesine savunma için VAR — gerekçe yazılı ki "zaten gereksiz" diye silinmesin. İKİ KAPI ADIYLA SORDU, İKİSİ DE CEVAPLANDI (sınır GEVŞETİLMEDİ): (1) 1B-A'nın `product_lots`u ANAN KAPALI dosya kümesine `app/parti_mutabakat.py` üçüncü okuyucu olarak girdi ve kapı bunu KIRMIZIYLA sordu — YAZMA ekseni o an ZATEN YEŞİLDİ (eklenen şey yazıcı DEĞİL) ve küme incelemenin CEVABIYLA genişledi (okuma neden ayrı dosyada: `parti_defteri.py`ye konsaydı yazma tekelinin "YALNIZ parti defterini yönetir" iddiası bulanıklaşırdı, `routers/products.py`ye konsaydı kural bir YÖNLENDİRİCİDE dururdu ve ikinci uç onu İKİNCİ KEZ yazardı); (2) kiracı nöbetçisi — sorgu ÖNCE `text(DEGISKEN)` olduğu için DİNAMİK SQL yüzeyine düşüyordu; metin dinamik DEĞİL (donmuş, tek değişken kiracı ve o da BAĞLI) olduğu için liste BÜYÜTÜLMEDİ, `app/routers/farm.py`nin `_GIRIS_SORGU` kalıbına çekildi (`text` çağrısı MODÜL DÜZEYİNDE literal alıyor; ham metin `str()` ile TÜRETİLİYOR, ikinci kopya olarak yazılmıyor) — ÖLÇÜLDÜ: `parti_mutabakat.py` dinamik yüzeyde DEĞİL ve KÜRESEL DİNAMİK TOPLAM 268'de SABİT. Literal olunca KAPSAM tarayıcısı ısırdı ve sebebi ÖLÇÜLDÜ: `_all_tenant_aliases_are_scoped` aynı tabloyu İKİ KEZ gören sorguda alias grafiğini KURMADAN vazgeçiyor (`len(occurrences) != len(aliases)`), oysa `company_id = :cid` metinde ÜÇ KEZ LİTERAL ve iki `LEFT JOIN` kiracıyı sürücü kümeden DEVRALIYOR (`WHERE`e taşınsaydı dış birleştirme sessizce İÇ birleştirmeye döner ve partisi olmayan çift raporda HİÇ görünmezdi) — `ALLOWLIST` girdisi parmak izi `65c0bd0a` ve gerekçesiyle taşıyor; SORGUYU İKİYE BÖLMEK girdiyi gereksiz kılardı ve REDDEDİLDİ çünkü iki ayrı `SELECT` READ COMMITTED altında İKİ FARKLI ANLIK GÖRÜNTÜ okur ve arada geçen bir yazım VAR OLMAYAN bir `SAPMA` UYDURURDU. ÖN YÜZ: `Ürün Detayı → Partiler` sekmesi (`components/ProductLotsPanel.tsx`), MEVCUT `GET /api/products/{id}/lots` ucundan ve isteği SEKME AÇILINCA yapıyor (ürün gövdesi zaten beş sekmeyi taşıyor; partiyi oraya eklemek partiye HİÇ bakmayan her ziyareti yavaşlatırdı — kapısı var); miktarı SIFIR olan parti GİZLENMEZ, SOLDURULUR ve "Tükendi" ETİKETİ alır (renk tek başına anlam taşımaz) çünkü listeden düşürmek kanıtı veritabanında tutup operatörden SAKLAMAK olurdu; SKT'siz parti "-" değil "SKT yok" der; sekme SONA eklendi (araya girmek `tab===N` koşullarının HEPSİNİ kaydırır ve her panel sessizce YANLIŞ sekmeye bağlanırdı); ROTA ENVANTERİ DEĞİŞMEDİ ve değişmemesi DOĞRUDUR (sekme rota değil PANEL; kapsam sözleşmesi App.tsx'in ROTA kümesine dayanır), e2e adımı VAR OLAN ürün detayı testinin İÇİNE eklendi (ayrı test envanterdeki TEK `spec` test ADInın dışında kalır ve sessizce ölçüsüz olurdu). ÇİVİLER YENİDEN ÖLÇÜLDÜ (aritmetikle taşınmadı): GET izin envanteri 182->183 (parmak izi `cfb171c3`->`070f4e0a`, drift raporu `missing`/`stale`/`changed` ÜÇÜ DE BOŞ), rota güvenlik sözleşmesi 382/293->383/294 (parmak izi `16a0ec60`->`c0b0754c`, `ROUTE_REASONS`daki kiracı kapsamlı OKUMA kümesine; uç ÜRÜN KİMLİĞİ ALMAZ, yani komşu uçların yaslandığı 404 kapısı BURADA YOK ve kapsam TAMAMEN sorgunun yükleminden gelir), kimliklenmiş/read/undeniable 372/98/113->373/99/114, çıplak read 72->73, `products.py` AST parmak izi `ac80411a`->`ff6f0a6c` (dinamik çağrı 6'da SABİT — dosyaya SQL EKLENMEDİ, yalnız import + yönlendirici), pin envanterleri +3 SATIR (`pg_twins.txt` 115->116, `alt_surec_sql.txt` 125->127 — İKİ dosya da sayıldı çünkü SQLite ikizi kadar PG ikizi de alt sürece SQL veriyor; sayı ÖLÇÜLDÜ, tahmin EDİLMEDİ), sıra bu dosyanın kuralına göre izlendi (uç -> izin ÖLÇÜMÜ -> `ROUTE_REASONS` -> sayım -> EN SON parmak izi); TENANT_TABLES DEĞİŞMEDİ, ALEMBIC DEĞİŞMEDİ (`git diff -- backend/alembic/` BOŞ, zincir başı `20260909_0077` KIMILDAMADI). KANIT: `PYTHONUTF8=1 python run_isolated_tests.py --workers 4` -> 419/419 dosya, 2425 test, 0 kırmızı (838.9s); GERÇEK PG16 (16-alpine, HER DOSYA TAZE ŞEMADA — CI'nın `reset_schema`sı dosya BAŞINA koşuyor, ÖLÇÜLDÜ): 1B-A 9, 1B-B 10, 1B-C 9, 1B-D 1, 1B-E 4, 1B-F 5, 1B-G 1 — YEDİSİ DE YEŞİL; `tsc -b`, `npm run build`, vitest 29/29 (ProductDetail), `typecheck:e2e`, sözleşme zinciri yeniden üretildi ve DRIFT YOK (`types.gen.ts` +73 satır, fixtures kımıldamadı), yeni dosyada eslint 0 hata. 17 bağlam SABİT. TABAN `origin/develop` `4d30597` (#78/WA3 birleştikten SONRA): kayıt ilk olarak `0538a4a` üzerinde ölçülmüştü ve WA3 inince O ÖLÇÜM GEÇERSİZ OLDU — dal YENİDEN SÜRÜLDÜ ve ÇİVİLER YENİDEN ÖLÇÜLDÜ, aritmetikle devralınmadı. WA3 rota ve göç GETİRMEDİ ve dinamik SQL yüzeyine GİRMEDİ (ölçüldü: `app/whatsapp/*` dinamik yüzeyde YOK, küresel toplam 268'de sabit, alt süreç envanteri kaymadı), yani sayılar taban değişimini AYNEN geçti; çakışma OLMADI. Dal `feat/1b-g-parti-mutabakat`; `--sonraki 79`
