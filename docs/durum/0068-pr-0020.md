#20 — Kantar fişi PR1 (göç `20260901_0064`, zincir 0062→0064): `field_harvest_tickets` ve `field_harvest_ticket_deductions` tabloları, `GET`/`POST /api/field-harvest-tickets` uçları; net SUNUCUDA türetilir (`net = brüt − Σ(brüt × oran/100)`, TOPLAMSAL — sıralı DEĞİL, owner kararı) ve kağıdın kendi neti `ticket_net_quantity` olarak AYRICA saklanır: ikisi ayrıştığında okuma anında `net_mismatch` yanar, yani alıcı sıralı hesaplıyorsa bu İLK FİŞTE görünür, sessizce sürüklenmez; `gross_quantity` KALİTE KESİNTİLERİNDEN ÖNCEKİ ÜRÜN AĞIRLIĞIDIR, araç+yük DEĞİL (göç başlığında, şema bunu zorlayamaz); `deduction_rate_total`, `derived_net_quantity`, `net_mismatch` ve `sold_exceeds_net` HİÇBİRİ SAKLANMIYOR — hepsi okuma anında türetiliyor; üç değerli bayraklar (`null` = ölçülmedi, `false` = ölçüldü ve uyuşuyor) ve fiş yokken toplamlar `null`, SIFIR DEĞİL; ÖLÇÜLEN ANA İDDİA — STOK DEFTERİ BUGÜNKÜYLE AYNI: dilim outbox olayı YAZMIYOR ve `field_stok_tuketici` DEĞİŞMEDİ, `tests/test_kantar_fisi_defter.py` beş senaryoda ölçüyor (fişsiz taban; fiş yazımı olay ve hareket sayısını artırmıyor; kesintili fişte hareket yine hasat miktarı; fiş tüketiciden ÖNCE girilse de değişmiyor — LEFT JOIN'in 'çalışacağı' sıra; iki bayrak da yanarken fişli ve fişsiz defter satırı alan alan AYNI) ve iki statik kapı destekliyor (tüketici kaynağında fiş tablosunun adı GEÇMİYOR, `create_harvest_ticket` gövdesinde outbox yazıcısı ÇAĞRILMIYOR); `_hasat_kalemleri`ye LEFT JOIN EKLENMEDİ çünkü hasat olayı hasat yazılırken üretilip ilk döngüde tüketiliyor, fiş ise kamyon depoya VARDIKTAN sonra giriliyor — birleştirme her seferinde NULL görür ve kural hiç ateşlenmez; mevcut okuma uçları (`/api/field-harvests`, `/api/field-dashboard`, parsel zaman çizelgesi) gövde gövde AYNI kaldı ve bu da ölçüldü; NUMARALANDIRMA BULGUSU: 0063 develop'ta boş ama AÇIK İKİ PR'da birden ilan edilmiş (#18 BKÜ kataloğu ve #19 tarla kilitleri, ikisi de `revision = "20260901_0063"`, ikisi de `down_revision = "20260827_0062"`) — yani #56/#60'ta ölçülen çakışma şu anda CANLI ve yalnız birleşmede görünür; bu göç 0064 aldı, üçü de 0062'nin çocuğu olduğu için SONRA inen iki dalın `down_revision`ı yeniden işaret etmek zorunda; KABUL EDİLEN ARTIKLAR: (1) fiş `operation_id` TAŞIMIYOR — yeni bir kuyruk türü `ck_farm_operations_kind` CHECK'ini yeniden yazmayı gerektirirdi ve fiş SAHA değil DEPO yolundan girilir; tekrar koruması kağıdın kendi kimliğinde (`UNIQUE(company_id, harvest_id, ticket_no)`) ve NUMARASIZ fiş iki kez girilebilir, bu bedel dondurulmuş bir testtir; (2) fişte `unit` sütunu YOK — miktarlar hasadın biriminde kabul ediliyor, kantarın KG basıp hasadın TON kaydedilmiş olması ÖLÇÜLMEDİ ve bu dilim onu çözmüyor; (3) fiş GÜNCELLENEMİYOR/SİLİNEMİYOR (yalnız yazma ve okuma) ve yanlış girilen bir fişi düzeltmenin yolu YOK; (4) fişin defteri düzeltmesi AYRI iştir ve düzeltici bir ikinci olayı ya da hasat olayının üretim anını değiştirmeyi gerektirir.
