#18 — Tarla tarafına BKÜ kataloğu (`plant_protection_products`, göç `20260901_0063`) eklendi ve PHI (hasat bekleme) gün sayısı ilk kez faaliyet dışında kalıcı bir kayda bağlandı; ÖLÇÜLEN KUSUR: PHI kilidi göç 0046/0048'den beri ÇALIŞIYORDU ama beslendiği sayı `field_activities.preharvest_interval_days` faaliyet başına ELLE giriliyordu ve deponun tamamında arandığında hiçbir yerde BKÜ kataloğu ya da ürün başına PHI değeri BULUNAMADI, dolayısıyla operatör süreyi yazmayı unuttuğunda kilit SESSİZCE hiçbir şey yapmıyordu; DEPO HİÇBİR PHI RAKAMI İDDİA ETMEZ — bu göç tek bir satır veri yazmaz, başlangıç listesi, paketlenmiş bakanlık verisi ve ürün başına varsayılan YOKTUR, sistemdeki her PHI değerini o değerin sahibi firma girer, çünkü PHI yasal bir süredir ve kaynağı BKÜ etiketidir; depoya gömülen bir rakam yanlış olduğunda deponun iddiası olurdu ve bu bir kolaylık tercihi değil hukuki bir duruştur; KATALOG ÖNERİR, OPERATÖR KARAR VERİR: operatörün girdiği değer HER ZAMAN kazanır (katalog sahayı ezseydi etiketi elinde tutan kişi sistemi düzeltemez hâle gelirdi) ve etkin değerin kökeni üç durumda kayıt altına alınır — `preharvest_source` = `CATALOGUE` (operatör boş bıraktı, katalogdan çözüldü) / `OPERATOR` (operatör yazdı, katalog ya sustu ya aynısını söyledi) / `OPERATOR_OVERRIDE` (operatör katalogdan FARKLI bir değer yazdı) — ve katalogun ne dediği AYRI bir sütunda (`catalogue_preharvest_days`) saklanır, bu ayrım göç 0048'in `safety_override_reason` (kullanıcının söylediği) ile `safety_warning` (sistemin bulduğu) arasında kurduğu desenin aynısıdır ve tek sütunda tutulsaydı "operatör katalogdaki 21 günü 7 yaptı" olayı denetimde GÖRÜNMEZ, yani SESSİZ bir üstüne yazma olurdu; uyuşma üstüne yazma SAYILMAZ (her eşitliği ihlal saymak gerçek üstüne yazmaları gürültüde kaybederdi) ve bir faaliyette birden çok girdi varsa EN UZUN bekleme kazanır, çünkü kısa olanı seçmek uzun olanın süresi dolmadan hasada izin verirdi; İLERİYE DÖNÜK: mevcut `field_activities` satırları katalogdan PHI DEVRALMAZ, geri doldurma göçü YOKTUR ve iki yeni sütun NULL açılır — bugün temiz kaydedilen bir hasat yarın da temiz kaydedilmelidir, geriye doldurma geçmiş faaliyetlere sonradan bir PHI takıp bugüne kadar sorunsuz çalışan hasat kayıtlarını kullanıcının yapmadığı bir değişiklik yüzünden aniden ihlale düşürürdü; ayrıca 0044'ten beri geçerli olan "BOŞ SÜRE İHLAL DEĞİLDİR" kuralı (`_bekleme_ihlalleri`) DEĞİŞTİRİLMEDİ, bu göç boşun ANLAMINI değil boş kalma SIKLIĞINI düşürür, çünkü bilinmeyeni ihlal saymak kullanıcıyı gerekçe yazmaya alıştırır ve gerçek uyarıyı değersizleştirirdi; ŞEMA ÜRÜN BAŞINA DEĞİL ÜRÜN + BİTKİ BAŞINA: PHI ürünün tek başına özelliği DEĞİLDİR çünkü aynı etken madde domateste ve elmada farklı bekleme süresi taşır ve ürün başına tek satır tutmak alan gerçeğini şemaya yanlış yazmak olurdu, ancak `crop_seasons.crop` SERBEST METİNDİR (`VARCHAR(120)`, göç 0044) ve her ürün için her bitkiye satır girmeyi zorunlu kılmak kataloğu doldurulamaz hâle getirirdi (firma "Domates" yazarken sezonda "domates" yazılıysa eşleşme kaçar ve katalog sessizce işlevsiz kalır), bu yüzden satır `(product_id, crop)` başınadır ve `crop` BOŞ DİZE olabilir; boş dize "BÜTÜN BİTKİLER" demektir, çözüm sırası bitkiye ÖZEL satırı tercih eder ve yoksa bitkiden BAĞIMSIZ satıra düşer, yani şema ürün+bitkiyi tam olarak ifade edebilirken kullanım tek satırla başlanmasına izin verir; BOŞ DİZE NULL DEĞİLDİR VE BU BİLİNÇLİDİR: SQL'de `UNIQUE` NULL'ları BİRBİRİNDEN FARKLI sayar, dolayısıyla `crop` NULL kabul etseydi aynı ürüne sınırsız sayıda "bütün bitkiler" satırı girilebilir ve çözüm hangisini seçeceğini bilemezdi; boş dize ile `UNIQUE(company_id, product_id, crop)` bu belirsizliği ŞEMA seviyesinde kapatır, ayrıca `product_id` NULL KABUL ETMEZ çünkü çözüm yolu `field_activity_inputs.product_id` -> katalog şeklinde işler ve ürüne bağlı olmayan bir katalog satırı hiçbir faaliyeti çözemez, yani doldurulup hiç kullanılmayan bir alan olurdu (serbest metin girdi — çiftçinin kendi ürettiği gübre — bilerek çözülmez ve boş PHI ile kalır, boş da ihlal değildir, zincir tutarlıdır); İSTANBUL YEREL GÜNÜ KORUNDU VE ÇİVİLENDİ: bekleme günü `_yerel_gun` ile İSTANBUL gününden sayılır, UTC'den değil, ve `test_farm_bku_katalogu.py` içine bu ayrımın düştüğü an kırmızı yanacak bir vaka konuldu — 2021-06-10T01:30+03:00 ilaçlaması UTC'de 2021-06-09T22:30Z'dir, yani İstanbul günü 10 Haziran UTC günü 9 Haziran'dır ve katalogdan çözülen 14 günle güvenli tarih İstanbul'a göre 24 Haziran, UTC'ye göre olsaydı 23 Haziran olurdu; test 23 Haziran'ın REDDEDİLMESİNİ iddia eder, dolayısıyla biri `_yerel_gun`u UTC'ye çevirirse bu satır düşer ve bekleme süresinde bir günlük kayma gerçek bir kalıntı riskidir; BİTKİ KARŞILAŞTIRMASI PYTHON'DA YAPILIR, SQL `LOWER()`INDA DEĞİL (`_bitki_esit`, `casefold`): Türkçe'de İ/ı eşlemesi DİYALEKTE BAĞLIDIR — SQLite'ın `LOWER`ı ASCII dışına dokunmaz, PostgreSQL'inki yerel ayara göre davranır — ve karşılaştırma SQL'de yapılsaydı aynı katalog iki diyalektte FARKLI çözülürdü, bu yüzden karşılaştırma Python'da `casefold()` ile yapılır ve çözüm iki motorda da AYNI kalır; ancak `casefold()` İ→i eşlemesi YAPMAZ (`"İstanbul".casefold()` `'i̇stanbul'`dur — birleştirici noktalı U+0307 — ve `'istanbul'`a eşit değildir), dolayısıyla sezonda "İstanbul" katalogda "istanbul" yazıldığında sunucu eşleşme bulamaz: bu durum motorlar arası tutarlıdır fakat Türkçe'ye duyarlı bir katlama değildir; öte yandan ön yüz önizlemesi (`frontend/src/farm/farmApi.ts`, `catalogueMatch`) `toLocaleLowerCase('tr')` kullandığı için İ→i dönüşümünü YAPAR, dolayısıyla önizleme ile sunucu tam olarak bu karakterde ayrışabilir (önizleme kendi yorumunda otorite olmadığını belirtir); birim testi (`test_katalog_bitki_eslesmesi_dialektten_bagimsiz`) yalnızca ASCII örneklerini kapsar (`Domates`/`domates`) ve İ/ı davranışını çivilemez; `_bitki_esit`in ön yüz ile tek bir katlama kuralında hizalanması ve teste ASCII dışı bir sav eklenmesi ayrı bir küçük PR'ın konusudur; KİRACI: altı yeni sorgunun altısı da kendi `company_id=:cid` yüklemini taşır (`farm.py` `tenancy.tenant_text`ten GEÇMEZ, dolayısıyla yapısal hiçbir şey unutmayı yakalamaz), `list_ppp`in ürün adı birleştirmesi kiracı İÇİNDEDİR (`u.company_id=k.company_id`, olmasaydı başka firmanın ürün ADI bu listeye düşebilirdi), katalog satırı ürüne BİLEŞİK yabancı anahtarla bağlıdır (`fk_ppp_product_same_company`, hedef tekilliği göç 0062'de kurulmuş `uq_products_company_id`), tekil okumalar `_TABLOLAR` beyaz listesinden geçen `_satir` üzerindedir ve uçlar `_FARM_PATH_PREFIXES`e eklenmiştir (okuma `farm.view`, yazma `farm.manage`) — eklenmeseydi genel `read` iznine düşer ve okuma yetkisi olan HERKES yasal bekleme sürelerini değiştirebilirdi; bu tuzağı `test_farm_management_api.py` ilk koşuda yakaladı; DONUK SABİTLER YENİLENDİ (hepsi CPython 3.12.10 ile, yani CI'ın pinlediği yorumlayıcıyla ölçüldü ve TEK dosya kaydığı için gerçek kaynak değişikliği dalıdır, yorumlayıcı imzası dalı DEĞİL): `tenant_scoping_guard` farm.py 13 -> 15 dinamik çağrı ve parmak izi (iki yeni çağrının ikisi de `list_ppp` içindedir ve kök yüklemleri `k.company_id=:cid`tir, interpolasyon yalnız kapalı bir filtre kümesinden gelir, değerler her zaman bağlı parametredir), küresel toplam 251 -> 253, `route_security_contracts` 340 -> 344 operasyon ve 263 -> 265 yol, `route_get_permission_inventory` 162 -> 164, `authorization_population` kimliklenmiş 330 -> 334 / reddedilemez 97 -> 99 / farm_herd 30 -> 32, `goc_zinciri` baş 20260827_0062 -> 20260901_0063, PG popülasyonu 94 -> 95 (`ci.yml` ve `test_ci_gates.py` aynı sayıyı tutar ve birbirini doğrular, biri güncellenip diğeri unutulursa kapı kendi kendisiyle çelişir); PR #7 (aa3d577) tam olarak bu iki dosyaya dokunduğu için birleşme ELDE çözüldü: her iki dosyada DEVELOP'IN sürümü taban alındı ve üzerine 94 -> 95 yeniden uygulandı; birleşme sonrası tree ile aa3d577 arasındaki fark `ci.yml`de yalnız bu sayı artışıdır, `test_ci_gates.py`de ise sayı artışına ek olarak test fonksiyonunun yeniden adlandırılması (`…_exact_94` -> `…_exact_95`) ve docstring ekidir, K9/K10 aile referansı ikisinde de 0 sayıldı ve K5 yeni biçimini (`deploy/artifact-imaj-kimlik-kapisi.sh`) korudu; ÖLÇÜLEN KOŞULAR (birleşme sonrası): zincir `TEK BAŞ VAR` / `HEPSİ ERİŞİLEBİLİR` / `SAYIM TUTUYOR` çıkış 0 ve baş `20260901_0063` ile 64/64, backend `tests/` 764 geçti 5 düştü, temel `aa3d577` ayrı bir worktree'de 760 geçti 7 düştü ve benim 5 başarısızlığım o 7'nin KESİN ALT KÜMESİDİR (`test_field_stok_zamanlayici` üçü, `test_self_service_exemption`, `test_supplier_price_bridge_units::test_zip_bomb_stops_at_uncompressed_entry_limit`), yani YENİ başarısızlık yoktur, frontend 88 dosya / 588 test tamamı geçti, `npm run lint` 0 hata (844 uyarı), `npm run typecheck:e2e` çıkış 0, `npx tsc -b` çıkış 0 ve `npm run build:bridge` çıkış 0, sözleşme yeniden üretimi sonrası `git diff --exit-code` çıkış 0; ÖLÇÜM HATASI DÜZELTİLDİ: ilk turda tip denetimi `npx tsc --noEmit -p tsconfig.json` ile koşulmuştu ve YEŞİL raporlanmıştı, ancak `tsconfig.json` `"files": []` ile yalnız proje REFERANSLARI taşıyor; o komut `--listFiles` ile sayıldığında SIFIR dosya denetliyor, yani yeşil hiçbir şey ifade etmiyordu — gerçek kapı derleme kipidir (`tsc -b`, `build:bridge` bunu koşar) ve ilk CI koşusunda (33485929887) tam da bunu yakaladı: `PlantProtectionProducts.tsx` `ResponsiveTable`ın ZORUNLU `cardFields` özelliğini vermiyordu (TS2741) ve bu TEK hata `frontend`, `container` ve `e2e` işlerinin ÜÇÜNÜ birden düşürdü; eksik özellik eklendi ve derleme yeşile döndü; İKİNCİ KUSURU DA CI YAKALADI ve bu yalnız ÇALIŞMA ANINDA görünen türdendi: `/products` ucu İKİ BİÇİM döndürüyor — `include_meta` verilmedikçe ÇIPLAK DİZİ, verilirse `{items, has_more}` (`routers/products.py`, `list_products` sonundaki iki `return`) — ekran koşulsuz `.items` okuyordu, çıplak dizide `undefined` alıyordu ve ürün adı sözlüğünü kuran `for...of` `TypeError: m is not iterable` ile sayfayı komple çökertiyordu; ne tip denetimi ne birim testi görebilirdi, `e2e`nin KONSOL-TEMİZLİK kapısı (`rota-render-kapisi.spec.ts`) yakaladı ve iddiasını da net kurdu ("işaret sayfa GÖVDESİNDE yok; kenar çubuğunda görünmesi kapsam kanıtı değildir"); iki biçim de kabul edilir hâle getirildi ve `PlantProtectionProducts.test.tsx` ile çivilendi — testin dekoratif OLMADIĞI mutasyonla ölçüldü: düzeltme geri alındığında dört testin üçü aynı `is not iterable` hatasıyla düşüyor; CSV İÇE AKTARMA BU PR'DA YOKTUR ve katalog şimdilik ekrandan TEK TEK SATIR girilerek doldurulur — bu, BKÜ listesi kalabalık olan bir firma için gerçek bir veri girişi yüküdür ve CSV gelene kadar sürer; ayrı bir dilim olarak bırakılmasının nedeni hem bu PR'ın zaten 23 dosya ve +1824/-31 olması hem de yeni bir POST ucunun dört ayrı donuk rota/izin envanterini İKİNCİ kez kaydırıp kiracı ve köken incelemesini zorlaştıracak olmasıdır; ÖLÇÜLMEDİ: PostgreSQL ikizi (`test_farm_bku_katalogu_postgresql.py`) YERELDE KOŞMADI çünkü `APP_TEST_DATABASE_URL` tanımlı değildi ve test `skip` edildi, dolayısıyla ikizin başlığındaki üç iddia — `crop` boş dize varsayılanının iki diyalektte aynı yazılması, `performed_at` tipinin SQLite'ta `str` PostgreSQL'de `datetime` gelmesi ve `(company_id, product_id)` bileşik yabancı anahtarının SQLite'ta `PRAGMA foreign_keys`e bağlıyken PostgreSQL'de her zaman uygulanması — şema ve mevcut güvenlik ikizinin emsali üzerinden AKIL YÜRÜTMEDİR, GÖZLEM DEĞİLDİR ve bu iddiaların ilk gerçek ölçümü CI'ın `backend-postgresql` shard'larında yapıldı ve GEÇTİ: koşum 33485929887'de dört shard'ın dördü de yeşil, ikiz shard 2'de KOŞTU (atlanmadı) ve `POSTGRESQL REPORT GREEN: 24 files, 53 nodes, outcomes={'passed': 53}` ile raporlandı; yani boş dize varsayılanı, `performed_at` tipi ve bileşik yabancı anahtar iddiaları artık AKIL YÜRÜTME değil ÖLÇÜMDÜR.
