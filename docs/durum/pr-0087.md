#87 — E1 e-FATURA/e-ARŞİV SERTLEŞTİRMESİ: e-Arşiv PDF'inin ANAHTARI SAKLANIYOR, durum SAĞLAYICIYA SORULABİLİYOR ve UBL üç yerde ŞEMAYA UYUYOR (göç `20260911_0081`, down_revision `20260910_0080`); ÖLÇÜLEN EKSİK BEŞTİ ve beşi de kaynağın kendi yorumunda YAZILIYDI: (1) `provider.py`nin e-Arşiv `fetch_pdf` dalı çağrıyı HİÇ KURMUYOR, `EARSIV_WEB_KEY_YOK` ile düşüyordu ve gerekçesi kendi satırındaydı — "Bu anahtar gönderim yanıtında (`WEB_KEY`) geliyor ama bugün saklanmıyor"; `GetEArchiveInvoice` UUID DEĞİL `WEB_VALIDATION_KEY` ister, anahtar YALNIZ `WriteToArchiveExtended` yanıtında BİR KEZ döner ve bir daha sorulamaz, yani saklanmadığı sürece e-Arşiv PDF'i KALICI olarak erişilemezdi; (2) `GET .../einvoice/status` YALNIZ yerel veritabanını okuyor, sağlayıcıya HİÇ gitmiyordu — GİB'de ACCEPTED olmuş bir belge bizde PENDING görünmeye devam ediyordu ve durumu ilerletecek TEK yol YENİDEN GÖNDERMEKTİ, yani idempotent OLMAYAN bir yolu OKUMA amacıyla kullanmak; (3) birim kodları UN/ECE Rec.20 değildi, ürün biriminin METNİ (`kg`) `unitCode`a olduğu gibi yazılıyordu; (4) %0 KDV satırı `TaxExemptionReasonCode` taşımıyordu; (5) alıcı vergi dairesi UBL'e HİÇ geçmiyordu (satıcı geçiyordu) ve tüzel kişi alıcıda `PartyTaxScheme/TaxScheme/Name` her zaman "-" oluyordu; GÖÇ ÜÇ SÜTUN ve ÜÇÜ DE NULLABLE — `einvoice_web_key` VARCHAR(255), `einvoice_gib_status_code` VARCHAR(10), `einvoice_pk_alias` VARCHAR(120); nullable bir kolaylık DEĞİL DOĞRU TİPTİR çünkü depoda ZATEN kesilmiş faturalar var ve hiçbiri bu değerleri TAŞIYAMAZ, NOT NULL + varsayılan seçilseydi "değeri boş" ile "değeri bilinmiyor" AYIRT EDİLEMEZ olur ve `fetch_pdf` boş bir anahtarı geçerli sanıp sağlayıcıya anlamsız bir çağrı yapardı; `10` hane KASITLIDIR (kod taşır, hata GÖVDESİ değil — 0080'in `fail_reason` sütunuyla aynı gerekçe) ve PG'de GERÇEKTEN ISIRIYOR, SQLite'ta ise `VARCHAR(n)` bir NİYET beyanıdır ve 500 karakter sessizce yazılır, o yüzden sınırın anlamı YALNIZ PG ikizinde kanıtlanabildi; AÇILIŞ ŞEMASI DENETLENDİ ve VARSAYILMADI: `invoices` tablosu `app/core_schema.py`nin `metadata`sında YOKTUR (ölçüldü, `'invoices' in metadata.tables` -> False, metadata 16 tablo taşıyor ve hiçbiri bu değil), yani tablo YALNIZ Alembic'ten doğuyor ve 0072'de ölçülen kusurun (açılış DDL'i tabloyu göçten ÖNCE kurar, göç onu VAR bulup atlar) bu göçte KARŞILIĞI YOKTUR — `create_all` tarafında ikinci bir çivi yeri AÇILMADI çünkü açılsaydı var olmayan bir tabloya sütun ekleyen ÖLÜ KOD olurdu; YENİ UÇ TEK ve POST: `POST /api/invoices/{invoice_id}/einvoice/sync` SORAR, GÖNDERMEZ (`provider.query_status` → yerel sütunlar + `gib_status_code`) ve GET OLMAMASI ÖLÇÜLMÜŞ BİR KARARDIR — `GET`in envanterdeki anlamı "read"tir (`test_route_get_permission_inventory`) ve orada YAZAN bir uç o sözleşmeyi bozardı, üstelik bu çağrı sağlayıcıda oturum açıp KOTA da tüketiyor; İZİN ÖLÇÜLDÜ VARSAYILMADI: `required_permission("POST", ".../einvoice/sync")` -> "sales" ve YENİ BİR ÖNEK KURALI EKLENMEDİ, gerekmedi de — `app/auth.py`de zaten `path.startswith("/api/invoices")` -> "sales" var ve uç `submit` ile AYNI yetki sınıfında, ki doğrusu budur (ikisi de sağlayıcıya çıkıp yerel satırı yazıyor); DURUM GERİYE YÜRÜYEMEZ: `advance_status` ileri-yönlüdür, ACCEPTED/REJECTED terminaldir ve başarısız bir sorgu `FAILED` YAZMAZ — `FAILED` "gönderim hiç inmedi, ETTN yok" demektir ve elimizdeki ETTN onu yalanlar, o yüzden `UNRESOLVED` kullanılıyor ve belge OLDUĞU YERDE kalıyor; GÖNDERİLMEMİŞ belgede sorgu KURULAMAZ (409) çünkü boş kimlikle gitmek boş bir yanıt üretir ve bu "belge yok" gibi okunurdu; YAZMA KENDİ KİRACI YÜKLEMİNİ TAŞIR (`WHERE id=:id AND company_id=:cid`) ve bu `_invoice()`in zaten yaptığı kapsam denetiminin YERİNE değil YANINA geçen İKİNCİ korumadır (0080'in `_sahip_mi` kaydıyla aynı gerekçe); BU MUTANT HTTP'DEN GÖRÜNMEZ ve bu AÇIKÇA KAYDA GEÇTİ: `_invoice()` satırı zaten `id + company_id` ile okuduğu için başka firmanın kimliğiyle gelen istek UPDATE'e HİÇ ULAŞMADAN 404 alıyor, yani uçtan uca bir test o mutantı YEŞİL bırakırdı ve "kiracı sızıntısını test ediyorum" demek YANLIŞ olurdu — mutant UPDATE metnini DOĞRUDAN ölçen bir kapıyla öldürülüyor; BİLİNMEYEN BİRİM SESSİZCE C62 OLMUYOR ve bu tasarımın kalbidir: 3 ton buğdayı `unitCode="C62"` ile göndermek GİB'e "3 ADET" demektir ve belge SESSİZCE yanlış olur — reddedilmez, KABUL EDİLİR ve YANLIŞ KALIR, o yüzden kod listesinde olmayan birim `UblBuildError` fırlatıyor (eşleme: adet→C62, kg→KGM, lt→LTR, ton→TNE, m→MTR, paket→PA; zaten geçerli bir kod verilmişse olduğu gibi kabul edilir); `UblBuildError`in TANIMI `ubl_xml.py`den `errors.py`ye TAŞINDI çünkü `ubl.py` artık bu hatayı fırlatıyor ve tanım yerinde kalsaydı `ubl -> ubl_xml -> ubl` DÖNGÜSEL içe aktarma doğardı — ad `ubl_xml`den yeniden dışa aktarılıyor, var olan çağrılar KIRILMADI; %0 KDV'de `TaxExemptionReasonCode=351` ("Diğer İstisnalar") HEM belge HEM satır kovasına yazılıyor ve kod DÜŞÜRÜLÜRSE belge ÜRETİLMİYOR (reddedilecek bir belgeyi "gönderildi" saymaktansa hata); 351 bir VARSAYILANDIR ve firma bazlı geçersiz kılma AÇIK BİR EKSİKTİR — GÖÇSÜZ bir firma ayarı AÇILMADI çünkü yeni bir sütun AYRI BİR GÖÇ demektir, ayar bir SABİT olarak yazıldı ve eksik `docs/efatura-adapter-spec.md`e kaydedildi; SIFIRDAN FARKLI ORAN İSTİSNA TAŞIMIYOR — istisna kodu taşıyan %20'lik bir satır da aynı ölçüde yanlıştır; ADRES İLİ HÂLÂ "-" ve bu EKSİK DEĞİL ÖLÇÜMDÜR: `app/core_schema.py`de `customers` tablosunda YALNIZ `address` (Text) var, `city`/`district` sütunu YOK, yani kaynakta il/ilçe olmadığı için UBL'e yazılacak değer de yoktur ve adresi virgülden bölmek UYDURMAK olurdu — yanlış bir il, boş bir ilden DAHA KÖTÜDÜR; sütunların YOKLUĞU teste ÇİVİLENDİ, açıldıkları anda test kırmızı olacak; GERÇEK SANDBOX KOŞULDU ve ÜÇ TEL HATASI ÖLÇÜMLE BULUNDU (kayıt: `docs/izibiz-sandbox-bulgular.md` §7): (a) `IssueDate` GELECEK TARİHLİ OLAMAZ — sabit `2026-09-11` yazılmıştı ve `ERROR_CODE=10003 SCHEMATRON ... günün tarihinden ileri bir tarih olamaz` ile reddedildi, (b) fatura numarasının son DOKUZ hanesi RAKAM olmak ZORUNDA — `uuid4().hex` harf karıştırıyordu ve aynı kapıya takılıyordu, (c) `GetEArchiveInvoiceRequest` YALNIZ `REQUEST_HEADER` + `WEB_VALIDATION_KEY` alır, eklenen `DOCUMENT_TYPE` şema dışıydı (`ERROR_CODE=10013 INVALID XML! cvc-complex-type.2.4b`) ve şema CANLI WSDL'den OKUNDU (`?wsdl` → `?xsd=5`), TAHMİN EDİLMEDİ; SANDBOXTA KANITLANAN: e-Arşiv gönderimi yeni UBL alanlarıyla KABUL EDİLDİ (`SNG2026833641341`, `SNG2026962923391`, `SNG2026466356080`, `SNG2026765340161` — hepsi `status=PENDING`) ve `WEB_KEY` GERÇEKTEN DÖNDÜ, yani göçün dayanağı ölçüyle doğrulandı; `WEB_KEY` çıplak anahtar DEĞİL `webValidationKey=` parametresi taşıyan bir portal URL'idir, o yüzden çağrı anında anahtar URL'den ÇIKARILIYOR (URL'in tamamını göndermek anlamsız bir değer göndermek olurdu) ve ham değer 255 haneye SIĞIYOR; SANDBOXTA KANITLANAMAYAN ve GİZLENMEYEN: TAZE bir e-Arşiv belgesi için `GetEArchiveInvoiceStatus` BOŞ dönüyor ve `GetEArchiveInvoice` PDF vermiyor (`PDF_YOK`) — 0/3/8/15/30/45 sn beklenerek TOPLAM ~100 sn yoklandı ve durum HEP `UNRESOLVED` kaldı, yani ZAMANLAMA DEĞİL; sebep ÖLÇÜLMEDİ ve VARSAYILMIYOR (en olası aday: sorgu bizim İSTEMCİ ETTN'imizle yapılıyor ama İzibiz belgeyi KENDİ kimliğiyle anahtarlıyor olabilir), iki test bunu `xfail(strict=False)` ile taşıyor ki sandbox çözer hâle gelirse XPASS olsun; `fetch_pdf`in e-Arşiv dalı ARTIK ŞEMAYA UYGUN İSTEK KURUYOR (önce HİÇ kurmuyordu) ama PDF döndürdüğü KANITLANMADI ve sessizce boş bayt DÖNMÜYOR, `PDF_YOK` ile gürültülü düşüyor; B2B KAPISI `IZIBIZ_EFATURA_SUBMIT_VERIFIED` DEĞİŞTİRİLMEDİ, `False` KALDI ve gerekçe ÇİFTTİR: kapıyı "denemek için" geçici açmak doğrulanmamış bir gönderimin CANLI BİR BELGE üretmesi riskini alırdı (okuma yollarının aksine burada hata GERİ ALINAMAZ) ve ayrıca durum sorgusunun bugün çözülmediği ÖLÇÜLDÜĞÜ için `GetInvoiceStatus` cevabı alınamadan açılan kapı, gönderilen belgenin akıbetini SORAMAYACAĞIMIZ bir kanal açmak olurdu — kapalı kapının GERÇEKTEN kapalı olduğu ise ÖLÇÜLDÜ: `TICARIFATURA` gönderimi sandbox'ın kendi posta kutusuna (`urn:mail:defaultpk@izibiz.com.tr`) hedeflendi ve ağa HİÇ ÇIKMADAN reddedildi (`status=FAILED`, `external_id=None`); KİMLİK ŞABLONU EKLENDİ (`backend/.env.izibiz.local.example`, DEĞERLER BOŞ) ve ÖLÇÜLDÜ ki `izibiz_smoke.load_credentials` TAM OLARAK üç anahtar arıyor (`IZIBIZ_USER`/`IZIBIZ_PASS`/`IZIBIZ_VKN`), başkası OKUNMUYOR; `.gitignore`a negasyon eklendi ve SIRA ZORUNLUYDU — negasyon `**/.env.izibiz.*` kuralından SONRA gelmeliydi, önce yazılsaydı sonraki desen onu yeniden yok sayardı; YÜKLEYİCİDE İKİ KUSUR DÜZELTİLDİ: (i) ortam değişkeni ezmesi EKSİK ANAHTAR DENETİMİNDEN SONRA koşuyordu, yani kendi yorumunun "CI/geçici deneme için" dediği tam iki durumda İŞLEMİYORDU (dosya yoksa ezme satırına HİÇ ULAŞILMIYORDU) — sıra ters çevrildi ve `IZIBIZ_ENV_FILE` ile dosya BAŞKA BİR YERDEN gösterilebiliyor (kopyalamak yerine göstermek: sır tek yerde kalır), (ii) `<...>` ile sarılmış YER TUTUCU değerler artık ADIYLA reddediliyor çünkü sarmalayıcı gönderildiğinde sağlayıcı `ERROR_CODE=10004 "Kullanıcı adı veya şifre hatalı"` diyordu ve tanı "kimlik YANLIŞ" gibi görünüyordu oysa kimlik DOĞRUYDU — sessizce soymak da YANLIŞ olurdu çünkü gerçek bir parola `<` ile başlayabilir; SANDBOX TESTİ CI'YI BOZMUYOR ve bu ÖLÇÜLDÜ VARSAYILMADI: kanonik koşucu (`run_isolated_tests.py::discover_active_test_files`) TAM OLARAK `backend/test_*.py` ve `backend/tests/test_*.py` glob'larına bakıyor, İKİSİ DE ÖZYİNELEMESİZ, ve eksiksizlik kapısı (`tests/test_karantina_listesi_pini.py`) AYNI iki glob'u kullanıyor — `backend/sandbox/` ikisinin de DIŞINDA, yani dosya ne toplanıyor ne sayılıyor ne de bir pini kımıldatıyor; ikinci emniyet: kimlik yoksa dosya TOPLANMA anında atlanıyor (`allow_module_level=True`), yani çıplak bir `pytest` koşusunda bile sonuç YEŞİL bir SKIP'tir; TESTLER `test_e1_efatura_sertlestirme.py` (21, mock taşıma) + `test_e1_efatura_sertlestirme_uc.py` (3, uç sözleşmesi + alt süreç) + `test_e1_efatura_sertlestirme_postgresql.py` (5, GERÇEK PostgreSQL 16, up→down→up dâhil) + `sandbox/test_e1_izibiz_sandbox.py` (5 geçti/1 atlandı/2 xfail, GERÇEK sandbox, CI DIŞI); KAPILAR: alembic başı 0080→0081 ALTI DOSYADA ve SEKİZ İDDİA SATIRINDA (`test_e1b_plantback`, `test_e2_tedavi_arinma`, `test_e3_karantina`, `test_goc_zinciri` ×3, `test_1b_a_alis_lot_postgresql`, `test_wa3_worker_postgresql` — son ikisi PG ikizidir ve kanonik SQLite koşusunda ATLANIR, o yüzden pin GREP'lenerek bulundu SÜİTE GÜVENİLEREK DEĞİL), rota sözleşmesi 389/299→390/300 ve parmak izi 61223c75→5cadacb3, yetki uzlaştırması 377→378 (`EXPECTED_READ` ve `EXPECTED_UNDENIABLE` KIMILDAMADI çünkü uç POST ve izni "sales"), `app/routers/invoices.py` dinamik-SQL parmak izi c75698af→7d1f2316 (DİNAMİK `text()` sayısı 2'de SABİT — eklenen ifade SABİT bir dizgedir, f-string DEĞİL, o yüzden dinamik sayaca GİRMEZ), `pins/pg_twins.txt` +1, `types.gen.ts` +68 satır (YENİ UÇ OpenAPI'yi büyüttü; `openapi.json` izlenmiyor, üretilen tek çivilenmiş dosya `types.gen.ts`), GET izin envanteri KIMILDAMADI, `TENANT_TABLES` KIMILDAMADI (yeni TABLO yok, yalnız SÜTUN), dışa aktarım KIMILDAMADI, kapalı aktivite kataloğu KIMILDAMADI (`log_invoice_action` serbest metin alır, `ACTION_TYPES` kapalı kümesi DEĞİL — ölçüldü), `alt_surec_sql.txt` KIMILDAMADI ve bu bir KAÇAMAK DEĞİL DÜZELTMEDİR: yeni alt süreç betiği envanterin gözüne SQL'li göründü ama sebebi bir TÜRKÇE YORUMDA geçen `UPDATE` kelimesiydi, betikte SQL YOKTU, o yüzden dosyayı pine EKLEMEK "bu dosyada 1 gömülü SQL var" diye YANLIŞ bir şey iddia etmek olurdu ve yorum yeniden yazıldı; kayıt adı `python scripts/durum.py --sonraki 87`
