#25 — BİTKİ ADI KATLAMASI TÜRKÇE OLDU ve ÖNİZLEME İLE SUNUCU İLK KEZ AYNI KURALI UYGULUYOR (`_bitki_katla` / `bitkiKatla`, göç YOK, yeni uç YOK); ÖLÇÜLEN KUSUR: göç 0063 (PR #18) kataloğu açarken bitki eşleşmesini bilerek SQL `LOWER()`ında değil PYTHON'da bırakmıştı ve gerekçesi doğruydu, ama seçilen Python katlaması `casefold()`TU ve `casefold()` TÜRKÇE BİLMEZ: `"İNCİR".casefold()` = `i` + U+0307 (birleşen üstteki nokta) verir ve bu `incir`e EŞLEŞMEZ, dolayısıyla katalog satırı Türkçe klavyeyle BÜYÜK yazılmış girdiyle BULUNAMIYORDU; ön yüz ise aynı işi `toLocaleLowerCase('tr')` ile yapıyordu, yani ÖNİZLEMENİN GÖSTERDİĞİ SATIR İLE SUNUCUNUN BULDUĞU SATIR tam İ karakterinde AYRILABİLİYORDU ve iki diyalektin aynı kataloğu farklı çözmesi, ikizi koşulan bir testin YAKALAYAMAYACAĞI bir sapmadır; ÖLÇÜLDÜ (bu turda, birleşmiş ağaç üzerinde): Türkçe klavyede BÜYÜK yazılmış 18 gerçek bitki adından `casefold()` ile yalnız 7'si kendi küçük hâlini buluyordu (`MISIR`/`mısır`, `BİBER`/`biber`, `PATLICAN`/`patlıcan`, `İNCİR`/`incir` hepsi ıskalıyordu), yeni katlama ile 18'inin 18'i buluyor ve `İstanbul`/`istanbul` çifti artık EŞLEŞİYOR, bugün develop'ta EŞLEŞMİYOR; KATLAMA `lower()` İLE, `casefold()` İLE DEĞİL ve bu ayrım İKİZLİĞİN TAMAMINI TAŞIR: `casefold()` ön yüzün `toLocaleLowerCase('tr')`ından FAZLA iş yapar, örneğin `ß`yi (U+00DF) `ss`e AÇAR, JavaScript AÇMAZ — küçük harfe çeviren 1616 kod noktası taranarak ÖLÇÜLDÜ (CPython 3.12.10 / Node v24.18.0, her kod noktası tek tek iki tarafa verilerek): `lower()` temelli bu katlama JavaScript'ten 0 KOD NOKTASINDA ayrılıyor, `casefold()` temelli olanı 297 kod noktasında ayrılıyor ve U+00DF o 297'nin İÇİNDEDİR, yani teste seçilen karakter divergans kümesinin GERÇEK bir üyesidir, temsilî değildir; TESTİN KENDİSİ MUTASYONLA ÖLÇÜLDÜ ve bu kaydın var olma sebebi budur: katlamanın sonu `casefold()`a çevrildiğinde `assert not _bitki_esit("Weißkohl", "weisskohl")` satırı DÜŞÜYOR (`AssertionError: assert not True`) ve o satır dosyadaki TEK başarısızlık — üstündeki bütün İ/ı iddiaları mutasyon altında da GEÇİYOR çünkü onları açık `.replace()` zinciri taşıyor, yani `lower()` ile `casefold()` arasındaki farkın TEK nöbetçisi o satırdır; İLK YAZILAN TEST KÖRDÜ ve DEĞİŞTİRİLDİ: iddia önce `WEISSKOHL` (BÜYÜK) biçimindeydi ve ÖLÇÜLDÜ ki o biçim iki katlama altında da `False` verir — içindeki `I` Türkçe katlamada `ı`ya iner ve ß farkını MASKELER — yani test kuralı ayırt etmeden GEÇERDİ; ayırt eden çift (`Weißkohl`/`weisskohl`, BÜYÜK HARF İÇERMEYEN) onun yerine kondu ve o çift `casefold()` altında `True`, `lower()` altında `False` verir; SIRA ÖNEMLİ: `I` + U+0307 dizisi ÖNCE `i`ye indirilir, çıplak `I` `ı`ya inmeden — ters sırada hiçbir yerde bulunmayan `ı` + U+0307 üretilirdi ve bu da teste yazıldı; BİLEREK KAYBEDİLEN, AÇIKÇA: Türkçe'de `I`nın küçüğü `ı`dır, dolayısıyla LATİN yazımlı `I` taşıyan bir bitki adı TERS yönde bozulur — `Iceberg` (marul) ve `Italyan Çimi` bugün `iceberg` / `italyan çimi` satırına EŞLEŞİYOR, bu PR'dan sonra EŞLEŞMEYECEK ve bu ÖLÇÜLDÜ (develop `casefold()`: her ikisi `True`; bu dal: her ikisi `False`); OKUYUCUNUN BİLMESİ GEREKEN TİCARET BUDUR ve iki başarısızlık yönü de AYNI TARAFA, İZİN VERİCİ tarafa düşer: eşleşmeyen bir katalog satırı bitkiye ÖZEL PHI yerine bitkiden BAĞIMSIZ PHI'ye ya da HİÇBİR ŞEYE düşer, yani hasat DAHA ERKEN serbest kalır — kilit sıkılaşmaz, GEVŞER, ve kaybedilen yön de kazanılan yön de bu özelliği paylaşır; kayıp kabul edildi çünkü (a) ürün Türkçe ve baskın girdi Türkçe klavyedir (ölçülen 7/18 -> 18/18 kazanç, iki adlık kayba karşı), (b) ÇEŞİT adları (`Isabella` gibi) `crop` sütununda DEĞİL ayrı `variety` sütununda durur ve bu karşılaştırmaya HİÇ uğramaz; İKİZLİK TEK YERDE: ön yüzdeki kural artık `catalogueMatch` içine gömülü değil, dışa açılan `bitkiKatla` fonksiyonundadır ve testi doğrudan onu çağırır, çünkü çağrı yerine gömülü bir kuralı ikizinden ayrı düşürmek sessizdir; TABANI DEĞİŞEN ÖLÇÜM ÖLÇÜM DEĞİLDİR ve bu dalın ÖNCEKİ turdaki bütün sayıları ATILDI: dal PR #18'in başı (`cfe7362`) üzerine yazılmıştı, ardından #18, #19, #22 ve #23 develop'a indi ve taban `7498ab3` oldu, dolayısıyla katlama ölçümleri de donuk sabitler de birleşmiş ağaç üzerinde YENİDEN alındı, eski sayıların üzerine aritmetik yapılarak DEĞİL — ve yeniden ölçüm ÖNCEKİ TURUN İKİ RAKAMINI DÜZELTTİ: tarama evreni 1775 değil 1616 kod noktası çıktı ve `casefold()` divergansı 207 değil 297, ayrıca `casefold()` ile eşleşen ad sayısı 8 değil 7 — sayılar farklı çünkü evren tanımı ve ad listesi aynı değil, SONUÇ ise değişmedi ve güçlendi (`lower()` tarafı yine TAM SIFIR divergans); BİRLEŞMEDE TEK ÇAKIŞMA `test_tenant_scoping_guard.py`DEYDİ ve iki taraf da AYNI `farm.py` girdisini yazdığı için çıktı: develop'un metni ALINDI (PR #22/#23'ün CSV içe aktarma geçmişini taşıyan ÜST KÜME odur ve bu dalın metnindeki 253 küresel toplamı develop'un kendi notunun söylediği gibi PR #22 ile GEÇERSİZ olmuştu), üstüne bu dilimin notu eklendi ve parmak izi birleşmiş dosyadan YENİDEN HESAPLANDI: `8635fc6d` -> `88cc25ec`; HESAP MAKİNESİ DOĞRULANDI: pin `ast.dump` çıktısına bağlı ve kayıtlar CPython 3.12.3'e çivili, bu tur 3.12.10 ile koşuldu, bu yüzden ölçüm ALINMADAN ÖNCE develop'un KENDİ `farm.py`si ile bu dalın eski `farm.py`si aynı yorumlayıcıdan geçirildi ve kayıtlı iki pin (`8635fc6d…`, `8ba597ee…`) BİREBİR yeniden üretildi, yani yeni pin güvenilirdir; KIMILDAMASI GEREKEN TEK SAYI KIMILDADI, DİĞERLERİ SABİT ve bu AÇIKÇA doğrulandı çünkü bir "geçsin diye" düzenlemeyi yakalayacak olan onlardır: `farm.py` dinamik `text()` sayısı 15'te SABİT, küresel toplam 254'te SABİT (bu dilim SQL'e HİÇ dokunmadı, karşılaştırma SQL'e İNMEDİ), `EXPECTED_UNDENIABLE` 99'da SABİT, `GET_INVENTORY_COUNT` 166'da SABİT (yeni uç YOK), alembic başı `20260902_0065`'te SABİT (göç YOK); SQL `LOWER()` BU İDDİALARI TAŞIYAMAZ ve bu da teste yazıldı (`"İNCİR".lower() != "incir"`, `"MISIR".lower() != "mısır"`): SQLite'ın `LOWER`ı ASCII dışına DOKUNMAZ, yani karşılaştırmanın Python'da durması bir tercih değil ZORUNLULUKTUR ve bu dilim 0063'ün gerekçesini ÇÜRÜTMÜYOR, TERSİNE ONU TESTE YAZIYOR; KIRPMA ASİMETRİSİ BAKILDI ve KUSUR DEĞİL: `bitkiKatla` kırpmayı KENDİ İÇİNDE yapar (`.trim()`), `_bitki_katla` yapmaz ama Python'daki İKİ çağrı yerinin de girdisi çağrı ÖNCESİNDE `.strip()`lenmiştir (`farm.py:2357`, `farm.py:2374`), yani davranış aynıdır; ÖLÇÜLEN KOŞULAR (birleşmiş ağaç üzerinde): `test_farm_bku_katalogu.py` 2/2 geçti, `test_tenant_scoping_guard.py` 28/28 geçti (yeniden hesaplanan pinle), `test_authorization_population_reconciliation.py` + `test_route_get_permission_inventory.py` 15/15 geçti, frontend `farmApi.test.ts` 26/26 geçti; ÖLÇÜLMEDİ: AKSAN KATLAMASI BU DALDA BİLEREK ELE ALINMADI ve alınmamalıydı — depo bugün hem `Buğday` hem `Bugday`, ayrıca `Patlican` tutuyor, yani operatörler ASCII'ye indirgenmiş Türkçe yazıyor ve HİÇBİR harf-durumu kuralı bunu düzeltmez (`Bugday` ile `Buğday` bu PR'dan sonra da eşleşmez); bu ölçüldü ve DOKUNULMADAN bırakıldı çünkü aksan katlaması ayrı bir karardır ve kendi kayıp analizini gerektirir; ayrıca tam CI matrisi (izolasyon koşucusuyla 370 dosyalık backend taraması, PostgreSQL shard'ları, `tsc -b`, `lint`, sözleşme yeniden üretimi) bu turda YEREL olarak koşulmadı ve CI'a bırakıldı.
