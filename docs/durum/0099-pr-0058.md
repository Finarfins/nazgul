#58 — AÇILIŞ KOŞULU 4, CANLILIK/GECİKME SİNYALİ: ÖLÜ THREAD İLE BOŞ KUYRUK ARTIK AYRIŞIYOR; **GÖÇ YOK, YENİ TABLO YOK, YENİ UÇ YOK**, TABAN `e7895a3` (`origin/develop`, `git rev-parse` ile DOĞRULANDI — oturum başı anlık görüntüsü `23902cf` gösteriyordu ve O BAYATTI); açılıştan önce `gh pr list` yalnız #56'yı (1B-A, göç `20260908_0073`) gösteriyordu ve `git ls-remote --heads origin`de `feat/outbox-liveness` YOKTU; worktree `F:/nazgul-liveness`, dal `feat/outbox-liveness`, `F:\nazgul` çalışma ağacına HİÇ DOKUNULMADI. **GÖÇ ÖLÇÜMLE ELENDİ, TERCİHLE DEĞİL**: depoda bir işçi kalp atışı tablosu ARANDI (`grep -i "heartbeat|kalp|liveness|worker_heartbeat" backend/`) ve YOKTUR — `heartbeat` geçen tek şema yeri `platform_maintenance.heartbeat_at`tır (göç `20260728_0034`) ve o sütun BAKIM İŞLEMİNİN (`operation_id`/`operation_kind`/`owner`) kalp atışıdır; bir zamanlayıcı oraya yazsaydı bakım kilidinin SAHİPLİĞİ hakkında yalan söylerdi ve 1B-A `0073`'ü tuttuğu için yeni bir tablo zaten DURMA sebebiydi. Kullanılan şey ZATEN VAR OLAN `settings` anahtar/değer tablosudur: `app/core_schema.py`de tanımlı, şemaya TABAN GÖÇÜYLE giriyor (`20260712_0000` `core_schema.metadata`yı `create_all` ediyor — ÖLÇÜLDÜ, ayrı bir göç dosyasında YOK), yani hem SQLite hem PostgreSQL kurulumlarında VAR; kaynaktaki notu "Platform-global markers only. Never store company-specific data here" diyor ve KALP ATIŞI TAM OLARAK BUDUR — zamanlayıcı SÜREÇ DÜZEYİNDE tek bir thread'dir ve TÜM firmaları TEK döngüde gezer, yani `company_id` taşıyan bir kalp atışı KİRACISI OLMAYAN bir olguya kiracı UYDURMAK olurdu; tek anahtar `field_stok_zamanlayici.heartbeat`, gövde JSON (`started_at`, `finished_at`, `companies_processed`, `companies_total`, `events_processed`, `last_error`) ve UPSERT LEHÇESİZ yazılıyor (önce UPDATE, rowcount 0 ise INSERT, INSERT yarışını kaybeden `SQLAlchemyError`da UPDATE'e düşüyor) — `ON CONFLICT` yazılmadı çünkü tek metnin iki lehçede de koştuğunu VARSAYMAK yerine kararı rowcount'a bırakmak ölçülebilir; **KALP ATIŞI DÖNGÜYÜ ASLA DÜŞÜRMEZ** (`_kalbi_yaz`ın her hatası `logger.exception` ile yutulur ve KENDİ oturumunda yazar: tüketicinin oturumu bir firma düştüğünde KULLANILAMAZ olabilir, aynı işlemde yazmak kalp atışını tam da onu en çok istediğimiz koşumda kaybettirirdi). **YENİ UÇ AÇILMADI, VAR OLAN `summary` BÜYÜTÜLDÜ** ve bu bir tercih: ikinci bir uç "kuyruk" ile "onu boşaltan şey"i iki ekrana bölerdi, 40 gündür bekleyen bir olayı gören kişi tüketicinin ölü olduğunu ancak BAŞKA bir yere bakarsa öğrenirdi; `GET /api/field-integration-events/summary` artık `scheduler` bloğu taşıyor: `enabled`, `alive`, `last_cycle_started_at`, `last_cycle_finished_at`, `seconds_since_last_cycle`, `interval_seconds`, `stale`, `pending_oldest_age_seconds`. **İKİ AYRI KAPSAM, BİLEREK**: `pending_oldest_age_seconds` DIŞINDAKİ her alan PLATFORM düzeyindedir ve HER KİRACI İÇİN AYNIDIR (yukarıdaki gerekçe), o tek alan ise KİRACIYA özeldir ve **İKİNCİ BİR SORGU AÇILMADI** — özet zaten kova başına `MIN(created_at)` seçiyor ve yaş ondan türüyor; ikinci bir sorgu aynı satırları İKİNCİ KEZ okurdu ve iki okuma arasında kayabilirdi. **`alive` İLE `stale` AYRI SORULARDIR ve biri diğerinin yerine geçmez**: `alive` `Thread.is_alive()`tir (yalnız thread'i TAŞIYAN süreçte anlamlı, hiçbir şeyi hayatta kalmaz ama SAHTELENEMEZ — bir kayıt değil, thread nesnesine doğrudan soru), `stale` ise kalıcı satırdan türer ve SÜRECİ AŞAR (bir süreç ölüp yerine yenisi gelmediyse sorulacak thread yoktur, kalan tek delil o satırdır); taze başlamış bir süreçte ilk döngü bitene kadar `alive=True, stale=True` görülür ve bu bir kusur değil TANIMDIR — thread ORADADIR ama tamamlanmış bir döngü KANITI henüz yoktur; `alive` üç dala ayrılır ve üçü de kaynağa yazılı: (a) `_thread` varsa `is_alive() and not _dur.is_set()` (SET bayraklı thread SONLANMAK üzeredir ve canlı sayılmaz — `baslat`ın sahiplenme koşulu da aynı ikiliye bakıyor), (b) bu süreçte başlatılıp DURDURULMUŞSA `False` (`_bu_surecte_baslatildi` bayrağı bunun için var: olmasaydı durdurulmuş bir zamanlayıcı kalp atışı bayatlayana kadar ÜÇ DÖNGÜ BOYU canlı bildirilirdi), (c) bu süreçte hiç thread yoksa karar kalıcı kalp atışına düşer (`acik and not bayat`) — yani uygulamanın web kopyaları koşan bir tüketiciyi ÖLÜ bildirmez. **BAYATLIK EŞİĞİ `3×interval_seconds` ve KATI BÜYÜKTÜR** (bir döngüyü kaçırmak jitter, üçünü birden kaçırmak arıza; çarpan 1 olsaydı normal jitter sürekli alarm üretirdi, çok büyük olsaydı ölü bir thread saatlerce sağlıklı görünürdü) ve **kalp atışı HİÇ yoksa `stale` TRUE'dur**: kanıt yokluğu tazelik değildir. **`last_error` KAYITTA VAR, YÜZEYDE YOK** — kalp atışı satırı düşen bir döngünün istisna metnini taşır (kesilmiş, 500 karakter) ama uç onu DÖNDÜRMEZ; gerekçe deponun kendi kararıdır (`routers/entegrasyon_olaylari._gerekceyi_arindir`): ham metin SQL, kısıt adı ve satır değeri taşıyabilir, adli değeri veritabanında KALIR ve `farm.view` taşıyan salt-okur rollere HTTP üzerinden AÇILMAZ — koşul 2'de kapatılan sızıntı sınıfını koşul 4'te yeniden açmak olurdu; ayrıca **KALP ATIŞI DÜŞEN DÖNGÜDE DE YAZILIR** (yalnız başarılı döngüde yazsaydı sürekli patlayan bir zamanlayıcı kalp atışı bakımından HİÇ KOŞMAMIŞ gibi görünürdü; oysa anlatmak istediğimiz fark tam da bu: thread YAŞIYOR ama İŞ YAPAMIYOR). **ÖLÇÜLEN KISIT, TASARIMI DEĞİŞTİRDİ — `companies_processed` TÜKETİCİNİN DÖNEN SAYACINA EKLENEMEZ**: `tum_firmalari_isle`nin döndürdüğü sözlüğün ANAHTARLARINI SAYAN üç kapı var (`tests/test_field_stok_zamanlayici.py` sözlüğü `BEKLENEN` ile BİREBİR karşılaştırıyor; `tests/test_field_stok_dongu_siniri.py` ve `test_field_stok_tuketici_postgresql.py` ise `girdi` ve `COMPANY_FAILED` DIŞINDAKİ bütün değerleri TOPLAYIP `girdi`ye eşitliyor), yani yeni bir tam sayı anahtar korunum denklemini SESSİZCE bozardı; çözüm İSTEĞE BAĞLI bir ÖLÇÜM KABIDIR (`olcum: dict[str, int] | None = None`, varsayılan `None` — geçmeyen her çağıran için davranış BİREBİR eskisi gibi) ve kap iki anahtar alır: `firma_sayisi` (aktif firma sayısı) ile `firma_islenen` (döngünün GERÇEKTEN girdiği firma sayısı; süre bütçesi kestiğinde ya da bir firma düştüğünde ikisi AYRIŞIR — düşen firma `continue` ile sayılmaz). **İKİ KAPI KIRMIZI YANDI VE İKİSİ DE HAKLIYDI; ADLARIYLA YAZILIYOR**: (1) `test_settings_platform_scope.py` `settings` tablosuna YAZAN üretim dosyalarını çivilliyor (`ALLOWED_SETTINGS_WRITERS`, o güne kadar tek eleman `seed_demo_data.py`) ve kapının koruduğu şey "firma verisi buraya yazılmasın"dır — kalp atışı listeye GEREKÇESİYLE girdi, çünkü yazdığı şey bir firmanın değil SÜRECİN olgusudur; (2) `test_v2_9_decimal_contract.py` `app/` altında `float` ADININ HİÇ geçmesini yasaklıyor ve `yas_saniye`nin dönüş notasyonu `float | None` idi — notasyon `Any` yapıldı ve gerekçe kaynağa yazıldı: değer para DEĞİL bir SÜRE ölçüsüdür ve `timedelta.total_seconds()`ten gelir, ama kapının MUTLAK olması bilinçli bir karardır (bir istisna listesi, paranın da sızabileceği ilk delik olurdu) ve ona UYULDU. **MUTASYONLAR (altısı da tek tek uygulandı, adı geçen test KIRMIZI oldu, geri alındı ve `git diff` ile temizlik doğrulandı)**: bayatlık çarpanı `3 -> 10**9` -> `test_bayatlik_sinirinin_OTESI_BAYAT` + `test_OLU_THREAD_ile_BOS_KUYRUK_AYRISIYOR` + `test_kalp_anahtari_ve_esik_SABIT` (3 failed); kalp atışı KALICI yazılmıyor (yalnız süreç-içi kap) -> `test_kalp_atisi_HER_DONGUDE_yaziliyor`; özetin `WHERE company_id=:cid` yüklemi `:cid IS NOT NULL` yapıldı (gecikme ölçüsü `company_id`'yi yok sayıyor) -> `test_ozet_ucunda_iki_kapsam_AYRISIYOR`; `enabled` YANLIŞ bayraktan (`field_stock_outbox_interval_seconds`) -> `test_BAYRAK_KAPALIYKEN_enabled_FALSE_ve_alive_FALSE` + `test_bayrak_KAPALIYKEN_kalp_atisi_YAZILMIYOR` (2 failed); `alive` her zaman TRUE -> aynı iki test + `test_OLU_THREAD_ile_BOS_KUYRUK_AYRISIYOR` (3 failed). **ALTINCI MUTASYONUN İLK YAZIMI YANLIŞTI VE BU KAYDA ADIYLA GEÇİYOR**: PG ikizinin gerekçesine önce "`_zamani_coz`in `datetime` dalını silmek bu vakayı kırmızı yakar" yazılmıştı; ÖLÇÜLDÜ ve dal `if False:` ile TAMAMEN silindiğinde HİÇBİR ŞEY KIRILMADI (11 passed) — çünkü metin dalı `str(<datetime>)` çıktısını `fromisoformat` ile geri ayrıştırabiliyor, yani o dal bir KISA YOLDUR, şart değil; ŞART OLAN şey çözücünün metin OLMAYAN bir değeri REDDETMEMESİDİR ve doğru mutasyon (`if not isinstance(deger, str): return None`) İKİ KULVARDA BİRDEN ölçüldü: PG ikizi 1 failed, `tests/test_outbox_liveness.py` 10 passed — asimetri PG ikizinin var olma sebebidir ve hem vakanın hem `_zamani_coz`un gerekçesi bu DOĞRU cümleyle yeniden yazıldı. **PG İKİZİNE YENİ DOSYA AÇILMADI**, iki vaka VAR OLAN tüketici ikizine (`test_field_stok_tuketici_postgresql.py`) eklendi ve ikisi de SQLite'ta ÖLÇÜLEMEYEN bir LEHÇE farkını hedefliyor: (a) kalp atışı UPSERT'i — `settings.key` BİRİNCİL ANAHTARDIR, iki döngü sonunda TEK satır kalmalı ve ikinci yazım birinciyi EZMELİ; lehçesiz upsert'in doğru dalı seçtiği ancak gerçek PG'de görünür (SQLite'ta yazmalar seri olduğu için oradaki yeşil kodun değil ORTAMIN özelliği olurdu), (b) ham `text()` sorgusunda zaman damgasının TİPİ — `summary` kova başına `MIN(created_at)` seçiyor ve o sorgu HAM metindir, yani SQLAlchemy tip işleyicisi DEVREDE DEĞİLDİR: ÖLÇÜLDÜ, sqlite3 sürücüsü METİN, psycopg gerçek (saat dilimli) `datetime` döndürüyor; çözücü ikisini de çözmezse `pending_oldest_age_seconds` bir lehçede sessizce `None` olurdu — yani gecikme sinyali tam da ÜRETİM lehçesinde ÖLÜ olurdu; ikinci vaka VAKUM KARŞITIDIR: iki firmaya yaşları çok farklı (40 gün / 10 dakika) birer BEKLEYEN olay yazılıyor ve `company_id` yüklemi düşerse iki yaş AYNI çıkar; ikinci kiracı UYDURULMADI, AÇILDI — `field_integration_events` `companies`e YABANCI ANAHTARLA bağlı (ölçüldü: kimliksiz INSERT önce birincil anahtar, sonra yabancı anahtar ihlaliyle patladı). **KOŞUMLAR**: `tests/test_outbox_liveness.py` 10 test (üçü ALT SÜREÇTE, kendi taze veritabanında — zamanlayıcı ve göç zinciri SÜREÇ DÜZEYİNDE durum taşır: `_kalp`/`_thread`/bayrak modül globalidir ve `app.main` import anında göçleri koşar, aynı süreçte iki senaryo koşturmak birinin bıraktığı kalp atışını diğerinin okuması demekti); PG 16.14 ikizi GERÇEK PostgreSQL'de koşuldu (docker `pglens` :5433, HER KOŞUMDA TAZE veritabanı, sürücü `postgresql+psycopg` — v3, psycopg2 DEĞİL): `test_field_stok_tuketici_postgresql.py` **23 passed, SIFIR ATLAMA** (21'den 23'e). **İĞNELER**: rota envanterleri KIMILDAMADI ve bu bir ÖLÇÜMDÜR — dilim YENİ ROTA GETİRMİYOR, `summary` zaten var olan bir GET'tir, dolayısıyla `test_route_get_permission_inventory.py` ve `test_route_security_contracts.py` develop'unkiyle EŞ; Core sorgu envanteri de SABİT (140/144 satırına dokunulmadı) çünkü eklenen SQL'in tamamı `text()`tir, SQLAlchemy Core `select/update/delete` DEĞİL; `routers/entegrasyon_olaylari.py` DİNAMİK text() sayısı **5'te SABİT** ve BEŞ ÇAĞRININ DA METNİ BİREBİR AYNI (bu dosyaya HİÇBİR SQL EKLENMEDİ — yaş ölçüsü var olan projeksiyondan türüyor), parmak izi `c0fcbafb -> 3e9035fe` yalnızca Python tarafı değiştiği ve parmak izi dosya AST'sinin TAMAMINDAN türediği için kımıldadı; `app/field_stok_zamanlayici.py` ve `app/field_stok_tuketici.py` `DYNAMIC_SQL_FILE_ALLOWLIST`te YOKTUR ve GİRMEDİ — eklenen üç SQL (kalp atışı UPDATE'i, INSERT'ü ve SELECT'i) SABİT METİNDİR, interpolasyon taşımaz ve `settings` bir KİRACI tablosu DEĞİLDİR (`company_id` sütunu YOK), dolayısıyla kiracı yüklemi taşımaması bir eksik değil TANIMDIR; alt süreç SQL sayacı `112/189 -> 113/194` (+1 dosya: `tests/test_outbox_liveness.py`; +5 gömülü metin: üç yeni senaryo + PG ikizine eklenen iki senaryo — o dosya ZATEN sayılıyordu, bu yüzden dosya sayacı yalnız +1) ve sayaç SON kaynak değişikliğinden SONRA ölçüldü (kayıt 0098'in dersi); `ACTION_TYPES`/`RESOURCE_TYPES` BÜYÜMEDİ — bu dilim hiçbir aktivite yazmıyor, kalp atışı bir DENETİM kaydı değil bir GÖZLEM kaydıdır; yetki popülasyonu ve `activity_log.py`/`tenancy.py` parmak izleri SABİT (ikisine de DOKUNULMADI). **SÖZLEŞME ZİNCİRİ** `export_openapi.py -o openapi.json` -> `npm run types:gen` -> `tools/capture_frontend_fixtures.py`, ve **DELTA BOŞ**: `frontend/src/api/types.gen.ts` DEĞİŞMEDİ, fixture DEĞİŞMEDİ, `npx tsc -b` çıkış 0 (`npm ci` gerekti: bu worktree'de `node_modules` YOKTU). SEBEBİ ÖLÇÜLDÜ ve bir BULGUDUR: `summary` ucunun dönüş notasyonu `dict[str, Any]`dir, yani OpenAPI şeması ÇIPLAK BİR NESNEDİR ve yanıta yeni bir anahtar eklemek üretilen istemci tiplerini KIMILDATMAZ — sözleşme kapısı bu ucun GÖVDESİNİ hiç görmüyor; bu dilim o durumu DEĞİŞTİRMEDİ (bir yanıt modeli yazmak bütün `entegrasyon_olaylari` yüzeyini yeniden tiplemek olurdu ve alanın PARAMETRE olması özelliğiyle çakışırdı) ama kaydediliyor. **BELGE GÜNCELLENDİ**: `app/FIELD_STOK_OUTBOX_ACILIS_KOSULLARI.md` koşul 4'ün üzerini çizdi (gerçeklerle: göç yok gerekçesi, iki kapsam ayrımı, `alive`/`stale` ayrımı, 3× eşiği, `last_error`ın yüzeyde olmama gerekçesi, ve NE YAPMADIĞI — kalp atışı bir TARİH SERİSİ değildir, tek satır yalnız SON döngüdür ve "son bir saatte kaç döngü koştu" burada CEVAPLANMAZ; bir ALARM da değildir, uç `stale`i söyler ve birinin BAKMASI gerekir) ve "**dört koşulun dördü de karşılandı; anahtar berkay karar verene kadar KAPALI kalır**" satırı yazıldı; **BAYRAĞA DOKUNULMADI** — `field_stock_outbox_enabled` varsayılanı hâlâ `False` ve bu bir açma emri DEĞİLDİR. AYRICA DÜZELTİLDİ: **koşul 2'nin maddesi bugüne kadar ÜZERİ ÇİZİLMEMİŞTİ** ve hâlâ "uygulamada bu tabloyu okuyan hiçbir ekran/uç yok" diyordu; oysa belgenin BAŞLIĞI 2'yi karşılanmış sayıyor ve koşul 3'ün metni "koşul 2'de eklenen `_FARM_PATH_PREFIXES`" diyerek ona DAYANIYORDU — madde bu turda ölçülerek (iki uç, `farm.view`, `company_id=:cid`) üzeri çizildi, çünkü "dördü de karşılandı" cümlesi bayat bir maddeyle YAN YANA duramazdı. **ÖLÇÜLMEDİ, ADIYLA**: (1) ALARM/BİLDİRİM YOK — `stale`i okuyup birine haber veren hiçbir şey yazılmadı ve bu bilinçli: uç sinyali verir, onu kimin okuyacağı bir OPERASYON kararıdır; (2) TARİH SERİSİ YOK — tek satır, yalnız son döngü; "son bir saatte kaç döngü" bir tablo (yani göç) isterdi; (3) FRONTEND YÜZEYİ YAZILMADI ve `types.gen.ts` bile KIMILDAMADI (yukarıdaki `dict[str, Any]` bulgusu); (4) ÇOK SÜREÇLİ DAĞITIMDA `alive` HÂLÂ BİR TAHMİNDİR — üçüncü dal ("başka süreçte koşuyor") kalıcı kalp atışının TAZELİĞİNE dayanır ve KAÇ kopyanın koştuğunu söyleyemez; süreç kimliği yazılmadı çünkü tek satırlık kayıt onu tutamaz; (5) `RECOVERY_FAILED` sınıfı BU DİLİMDE DE KAPANMADI — o olay veritabanında iz bırakmaz ve kalp atışı da onu göremez, belge bunu üçüncü kez yazıyor; (6) 1B-A (#56, göç `20260908_0073`) DOSYALARINA (`transactions.py`, `parti.py`, `product_lots`) ve `kiraci_*`e HİÇ DOKUNULMADI, yeniden kuyruklama koduna da DOKUNULMADI (`kaydet_yeniden_kuyrukla` develop ile birebir aynı).
