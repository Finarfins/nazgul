#61 — 5.4a MOBİL KİMLİK AKIŞI (GÖÇ YOK): çerezsiz istemciye oturum yolu açıldı ve tarayıcı yolu BİT BİT korundu; TABAN `77aa5b0` (`origin/develop`, #59 + #62 + #63 indikten sonra), dal `feat/54a-mobil-auth`, göç AÇILMADI ve zincirin başı `20260908_0074` (E2 veteriner) olarak DURUYOR — bu dal başa DOKUNMUYOR; ÖLÇÜLEN KUSUR — oturum bugün TAMAMEN çereze bağlı: `/auth/login` refresh ve CSRF tokenlarını YALNIZ `Set-Cookie` ile veriyor, `/auth/refresh` refresh tokenını YALNIZ çerezden okuyup CSRF istiyor; Capacitor kabuğunun origin'i CİHAZIN KENDİSİDİR (`https://localhost`) ve API başka bir alan adındadır, oraya yazılan HttpOnly çerez ya HİÇ yazılmaz ya da üçüncü-taraf çerez engelleriyle SESSİZCE düşer — yani mobil istemcinin bugün `access_token_minutes`=15'ten uzun yaşayan bir oturumu YOKTUR, access token biter ve YENİLENEMEZ; DEĞİŞENLER (dördü de tek bir sözleşme genişletmesidir, hiçbiri var olan bir cümleyi DEĞİŞTİRMEZ): (1) `POST /auth/login` isteğe bağlı `X-Client-Kind: mobile` başlığını tanır — başlık VARSA gövde ham `refresh_token` + `refresh_expires_at` taşır ve HİÇBİR çerez yazılmaz, başlık YOKSA bugünkü davranış AYNEN korunur; başlığın web SPA'sında BULUNMADIĞI ÖLÇÜLDÜ, VARSAYILMADI: `frontend/src/api.ts` istek yorumlayıcısı yalnız `X-Company-ID` ve `X-CSRF-Token` ekler, `AuthContext.tsx`in login çağrısı hiç başlık vermez, yani başlığın YOKLUĞU tarayıcı yolunun TANIMIDIR; (2) `POST /auth/refresh` çerez YOKKEN gövdedeki `{refresh_token}`ı kabul eder, gövde yolu CSRF İSTEMEZ ve yanıtta HEM yeni access HEM yeni refresh döner, çerez varsa bugünkü yol AYNEN koşar; (3) `POST /auth/logout` gövdedeki `refresh_token`ın ailesini düşürür ancak aile ÇAĞIRANA aitse, çerez yolu DEĞİŞMEDİ; (4) `POST /auth/logout-all` (YENİ, 204) kullanıcının TÜM access ve refresh tokenlarını TEK işlemde düşürür; GÖÇSÜZ İSTEMCİ TÜRÜ — var olan `auth_refresh_tokens.user_agent` sütununa `mobile:<ua>` ÖNEKİ yazılır ve web metni DEĞİŞMEZ; sütun ZATEN var ve ZATEN serbest metin, mobil oturumu ayırt etmek için yeni sütun açmak bir GÖÇ demekti ve bu dilim göçsüz olmak ZORUNDAYDI; böylece "hangi oturum telefondan" sorusu ileride ŞEMA DEĞİŞMEDEN yanıtlanabilir; YOL SEÇİMİ ÇEREZE BAKAR, GÖVDEYE DEĞİL ve bu SIRA ZORUNLUDUR: tersi olsaydı — "gövde varsa gövdeyi kullan" — tarayıcıda çalışan bir saldırgan gövdeye KENDİ seçtiği bir token koyarak CSRF kapısını ATLATABİLİRDİ, yani kapı saldırganın YAZABİLDİĞİ bir alanla açılıp kapanır hale gelirdi; bu ADIYLA test ediliyor (`YON-1 govde ile CSRF atlatildi`); GÖVDE YOLUNUN CSRF İSTEMEMESİ BİR GEVŞETME DEĞİLDİR ve gerekçesi ADIYLA yazıldı: CSRF, tarayıcının isteği OTOMATİK kimliklendirmesine karşıdır (çerez, saldırganın sayfasından yapılan isteğe DE iliştirilir); gövdedeki token OTOMATİK İLİŞTİRİLMEZ, saldırgan onu yazabiliyorsa tokenı ZATEN BİLİYOR demektir ve CSRF'in koruduğu şey ÇOKTAN GİTMİŞTİR; `rotate_refresh_token`A DOKUNULMADI ve bu KARARIN KENDİSİDİR: gövde yolu (`_refresh_from_body`) o fonksiyonun TA KENDİSİNİ çağırır, bu yüzden aile rotasyonu ve REPLAY TESPİTİ mobilde de AYNEN işler; ikinci bir rotasyon yolu yazılsaydı o yol bir gün SESSİZCE ayrışır ve replay tespiti mobilde ÖLÜRDÜ — mutasyon 4 tam olarak bunu yapıyor ve KIRMIZI oluyor; GÖVDE İLE ÇIKIŞTA SAHİPLİK YÜKLEMİ — yeni yardımcı `revoke_refresh_family_for_user` yüklemi `token_hash` VE `user_id` üzerine kurar; yüklem olmasaydı bearer ile kimliklenen bir çağıran BAŞKASININ refresh ailesini imha edebilirdi, ki bu `logout`un çerez dalının belge dizgisinde ZATEN adıyla reddettiği tehlikedir ("the refresh cookie can belong to a different principal"); yardımcı True/False döner ki çağıran "bilinmeyen token" ile "başkasının tokenı" arasındaki farkı SIZDIRMASIN; çapraz imha reddi smoke'ta ADIYLA ölçülüyor (`CAPRAZ IMHA — baskasinin ailesi dusurulmus`); `logout-all` ACCESS TOKENLARI DA DÜŞÜRÜR ve bu ZORUNLUDUR: yalnız refresh iptal edilseydi dağıtılmış access tokenları 15 dakika daha CANLI kalırdı (`config.py:access_token_minutes`) ve çalınmış bir telefonda 15 dakika "hemen kapat" düğmesinin vaadini bozmaya YETER; iki yardımcı (`revoke_user_access_tokens`, `revoke_user_refresh_tokens`) BİLEREK commit etmiyor ve router TEK commit atıyor — "access düştü ama refresh yaşıyor" ara durumu böylece İMKANSIZ; `logout-all`DA HIZ SINIRI YOK ve bu ÖLÇÜLDÜ, ATLANMADI: `_consume_ip_limit` IP başına SAATLİK sayaçtır ve KİMLİKSİZ uçlar (kayıt, şifre sıfırlama) içindir; burada çağıran ZATEN geçerli bir access token taşıyor, yani sınır bir saldırganı değil KENDİ hesabını kapatmaya çalışan kullanıcıyı engellerdi ve ucun kötüye kullanımı yalnız çağıranın KENDİ oturumlarını düşürür; `logout-all` `SELF_SERVICE_API`YA EKLENDİ ve MUAFİYET ZORUNLUYDU: uç POST'tur ve `required_permission` içinde eşleşen kuralı YOKTUR, listeye girmezse dosyanın SONUNDAKİ deny-by-default nöbetçisine düşer ve `admin` DIŞINDA hiç kimse cihazını kaybettiğinde oturumlarını düşüremezdi; aynı liste `PASSWORD_CHANGE_ALLOWED_API`dir, yani parolasını değiştirmesi gereken hesap DA çalınmış oturumlarını kapatabilir ve bu DOĞRUDUR; `test_self_service_exemption`ın ELLE YAZILMIŞ çapası ÜÇTEN DÖRDE çıkarıldı ve iki türetim yönteminin İKİSİ DE yeni ucu geçiyor (yol parametresi YOK, başka aktör ADLANDIRMIYOR); TESTLER `backend/tests/test_54a_mobil_auth.py` (9 test — 4 statik kapı + gerçek şemalı alt süreç smoke'u + 4 mutasyon adlandırması); YENİ PG İKİZİ AÇILMADI ve gerekçesi ÖLÇÜLDÜ: depoda `pytest.mark.postgresql` taşıyan HİÇBİR kimlik/oturum ikizi YOKTU (arandı) ve bu dilimin hiçbir iddiası DİYALEKTE bağlı değildir — göç YOK, ham `text()` SQL YOK, `RETURNING`/`FOR UPDATE`/`ON CONFLICT`/`NUMERIC` YOK; tek yeni sorgu SQLAlchemy Core `select`tir ve `rotate_refresh_token`ın PG'ye özgü `with_for_update()` dalı BU PR'DA DEĞİŞTİRİLMEDİ; SMOKE'UN ÖLÇTÜKLERİ: web login DEĞİŞMEDİ (çerez kümesi TAM OLARAK `yhp_access_token`/`yhp_csrf_token`/`yhp_refresh_token`, gövdede `refresh_token` YOK, user_agent'a mobil öneki YAZILMAMIŞ), mobil login gövdede refresh verir ve SIFIR `Set-Cookie` yazar, gövde yenilemesi ROTASYON yapar (yeni access VE yeni refresh, ikisi de eskisinden farklı), REPLAY her İKİ yolda da AİLEYİ öldürür (gövde yolunda tüketilmiş token 401 ve SONRASINDA hâlâ geçerli olan YENİ token da 401; çerez yolunda AYNISI), mobil ailenin imhası WEB ailesine SIZMAZ, `must_change_password` gövde yolunda da 403 `PASSWORD_CHANGE_REQUIRED`, çerezli yenileme HÂLÂ CSRF ister, çerez varken gövdeye token koymak CSRF'i ATLATAMAZ, SPA'nın gerçek çağrısı (boş gövde ve CSRF başlığı) 422'ye DÜŞMEZ, gövde ile çıkış KENDİ ailesini düşürür ve BAŞKASININKİNİ düşürmez, `logout-all` İKİ BAĞIMSIZ oturumun HEM access HEM refresh tokenlarını öldürür ve BAŞKA kullanıcıya DOKUNMAZ, kimliksiz `logout-all` 401; MUTASYONLAR (dördü de tek tek uygulandı, adı geçen iddia KIRMIZI oldu, dosya `cp` ile BİREBİR geri döndürüldü): (1) `_refresh_from_body` rotasyonu ATLAR ve eski tokenı geri döndürür -> `YON-2 ROTASYON YOK`; (2) `logout_all` içindeki `revoke_user_access_tokens` satırı SİLİNİR -> `LOGOUT-ALL access unutuldu` (access token 200 dönüyor); (3) `login` mobil dalında `_set_session_cookies` DE çağrılır -> `YON-2 mobil login CEREZ yazmis` (üç çerez birden); (4) gövde yolu, tüketilmiş/iptal edilmiş tokenı da kabul eden İKİNCİ bir rotasyon kopyası kullanır -> `YON-3 replay kabul edilmis`; İĞNELER — HEPSİ TABAN `77aa5b0` ÜZERİNDE YENİDEN ÖLÇÜLDÜ ve ARİTMETİKLE TAŞINMADI; önceki turun `baadecf` üzerindeki ölçümü (368/284, `62da2fb5`, 358/95/104, 115/196) taban değiştiği anda GEÇERSİZ oldu ve dört çakışan kapı dosyasında İKİ TARAFIN DÜZYAZISI KORUNARAK sayılar SİLİNİP kapının kendisine YENİDEN ölçtürüldü: rota sözleşmesi 374/287 -> 375/288 ve güvenlik parmak izi `411caebd` -> `d3626d88`, `ROUTE_REASONS`a TEK satır (self-servis grubuna) girdi ve SIRA korundu (uç önce `SELF_SERVICE_API`ya yazıldı, izin `required_permission` ile ÖLÇÜLDÜ — "read", gerekçe yazıldı, EN SON parmak izi alındı; dosyanın kendi notu parmak izini YANLIŞ izinle dondurmanın İKİ KEZ yaşandığını söylüyor); `EXPECTED_AUTHENTICATED` 364 -> 365, `EXPECTED_READ` 94 -> 95, `EXPECTED_UNDENIABLE` 107 -> 108, ÇIPLAK read 68 -> 69 ve `NAKED_READ_OPERATIONS` elle yazılan kümesine `POST /api/auth/logout-all` eklendi, self-servis işlem sayısı 3 -> 4 (yani muafiyet YİNE yeni bir açıklık yaratmıyor — o uç da reddedilemeyenlerin İÇİNDE), `FARM_HERD_VIEW_OPERATIONS` KIMILDAMADI; GET izin envanteri 179'DA KALDI çünkü yeni uç POST'tur; `core_query_inventory` toplamı 144 -> 145 ve parmak izi `5cb0ad7b` -> `744f620e`, TEK yeni sorgu `app/auth.py :: revoke_refresh_family_for_user :: select` (`app/auth.py` 25 -> 26), `app/routers/auth.py` 22'DE KALDI çünkü mobil dallar var olan yardımcıları (`issue_token`, `issue_refresh_token`, `rotate_refresh_token`, `revoke_user_*`) çağırır ve YENİ SQL YAZMAZ; drift raporu ÖLÇÜLDÜ: `changed` ve `stale` İKİSİ DE BOŞ, artış YALNIZ EKLEME, `UNRESOLVED_ALLOWLIST` BÜYÜMEDİ; alt süreçte SQL taşıyan yüzey 116/197 -> 117/198 (+1 dosya / +1 metin, `app/` altında yine SIFIR) ve gömülü SQL'lerin kiracı durumu ADIYLA yazıldı — dokunulan tablolar `app_users`/`auth_tokens`/`auth_refresh_tokens`, yani KİMLİK tablolarıdır ve ÜÇÜNDE DE `company_id` sütunu YOKTUR, bu yüzden kiracı yüklemi TAŞIMAZLAR ve TAŞIMAMALIDIRLAR, her okuma bunun yerine `user_id`/`username` eşitliğiyle TEK kullanıcıya daralır; SÖZLEŞME ZİNCİRİ: `export_openapi.py -o` -> `npm run types:gen` -> `capture_frontend_fixtures.py`; delta YALNIZ `frontend/src/api/types.gen.ts`, `pos-api.json` DEĞİŞMEDİ, `backend/openapi.json` `.gitignore`da; `npx tsc -b` ÇIKIŞ 0 ve `tsc -b` KULLANILDI, `tsc --noEmit -p tsconfig.json` DEĞİL — ikincisi bu depoda SIFIR dosya denetler ve BOŞ bir yeşildir; ÖN YÜZ DEĞİŞMEDİ, yalnız `types.gen.ts` türedi; SÜİT KANONİK KOŞUCUYLA ÖLÇÜLDÜ ve bu bir DÜZELTMEDİR: önceki tur `python -m pytest tests` ile ölçmüştü, o da (a) `backend/test_*.py` altındaki ÜST DÜZEY dosyaları HİÇ toplamıyor ve (b) tek süreçte koştuğu için içe aktarma anındaki `DATABASE_URL` yazımlarını dosyalar arasında SIZDIRIYOR; doğru kapı `PYTHONUTF8=1 python run_isolated_tests.py --workers 4`tür (her dosya kendi alt sürecinde ve kendi çalışma alanında) ve bu dalda ÇIKIŞ 0 verdi; ÖLÇÜLMEDİ, ADIYLA: (1) ÖN YÜZ MOBİL İSTEMCİSİ — `X-Client-Kind` başlığını GÖNDEREN ve gövdedeki refresh tokenını güvenli depoya yazan Capacitor tarafı YAZILMADI, bu dilim yalnız SUNUCU sözleşmesini açar; (2) REFRESH TOKENIN CİHAZDA NEREDE DURACAĞI — Keychain/Keystore seçimi ve bunun tehdit modeli bu dilimin KAPSAMINDA DEĞİL, ama sunucu tarafı ham tokenı YALNIZ BİR KEZ (login yanıtında) ve rotasyon başına bir kez verir; (3) OTURUM LİSTESİ UCU — `user_agent`a yazılan `mobile:` öneki "hangi oturum telefondan" sorusunu YANITLANABİLİR kılar ama o soruyu SORAN bir uç bu PR'da AÇILMADI; (4) `logout-all`UN DENETİM KAYDI — uç `log_request_activity` çağırmıyor, çünkü kimlik/oturum uçlarının hiçbiri çağırmıyor ve tek uç için kalıp açmak SÜRÜKLENME olurdu; (5) ESKİ MOBİL OTURUMLARIN GÖÇÜ — bugün mobil oturum YOK (çalışmıyordu), yani geriye dönük veri de YOK; (6) EŞZAMANLI ROTASYON — aynı refresh tokenla İKİ EŞZAMANLI gövde yenilemesi ölçülmedi; ayıran şey `rotate_refresh_token`ın PG'de `with_for_update()` alan SELECT'idir ve o dal BU PR'DA DEĞİŞTİRİLMEDİ, SQLite'ta ise zaten tek yazıcı vardır; (7) BU DALIN #62 (etkinlik kataloğu) VE #63 (1B-B satış FEFO) İLE ETKİLEŞİMİ — ikisi de bu turda tabana indi ve iğneler onların ÜSTÜNDE ölçüldü, ama bu dal ne `ACTION_TYPES` kataloğuna ne de parti tüketimine DOKUNUYOR, yani etkileşim YOK ve aranmadı.
