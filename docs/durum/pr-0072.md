#72 — GENEL İDEMPOTENSİ: `Idempotency-Key` BAŞLIĞI ARTIK TEK YERDE YORUMLANIYOR (5.4b), GÖÇ `20260909_0076` (`idempotency_keys`) ve zincirin BAŞI `20260909_0075` -> `20260909_0076`; TABAN `c5e8b5b` (`origin/develop`, #74/H7 birleşmiş hâli) ve `git rev-parse origin/develop` OTURUM BAŞI ANLIK GÖRÜNTÜSÜNE GÜVENİLMEDEN YENİDEN KOŞTURULDU; BU DAL `fcdaf8f` ÜZERİNDE KURULMUŞTU ve ARADA #61/#64/#70/#71/#73/#74 İNDİ, yani bütün iğneler YENİ TABANDA SIFIRDAN ÖLÇÜLDÜ ve eski daldaki sayılar (111, 120/201) ARİTMETİKLE TAŞINMADI — TAŞINSAYDI ÜÇÜ DE YANLIŞ OLURDU; ÖLÇÜLEN KUSUR ÜÇ PARÇALI VE HER PARÇASI SAYILDI — (1) depoda BEŞ ayrı `*_idempotency` tablosu var (`machine_idempotency`, `pos_idempotency`, `purchase_draft_idempotency`, `payment_idempotency`, `receivable_charge_idempotency`) ve BEŞİ DE FARKLI ŞEKİLDE: üçü `(company_id, idempotency_key)` ikisi `(company_id, operation_id)` anahtarlı, her biri KENDİ ucunun İŞ SONUCUNU tutuyor; (2) HTTP katmanında HİÇBİR ŞEY yoktu — `app/main.py`de `Idempotency-Key` literali SIFIR kez geçiyordu; (3) yazan uç sayısı OpenAPI şemasından sayıldı (198 POST/PUT/PATCH/DELETE) ve bunlardan YALNIZ 11'i korunuyordu, yani ağ zaman aşımından sonra atılan ikinci istek 187 uçta ikinci bir kaydı GERÇEKTEN yazıyordu; NEDEN ALTINCI BİR `*_idempotency` DEĞİL — beş defterin ORTAK kusuru İŞ SONUCUNU saklamalarıdır ve iş sonucundan cevabı YENİDEN KURMAK uca özel bilgi ister (`pos.py`nin `_idempotent_replay`i tam olarak bunu yapıyor ve o yüzden POS'a özeldir); `idempotency_keys` İŞ SONUCUNU DEĞİL HTTP CEVABININ KENDİSİNİ (`response_status` + `response_body`) saklıyor ve TEK bir ara katmanın uca özel hiçbir şey bilmeden çalışabilmesinin tek yolu budur; BEŞ DEFTER YERİNDE KALDI ve bu göç onlara DOKUNMADI; ANAHTARIN KAPSAMI `(company_id, user_id, key)` ÜÇLÜSÜDÜR — `company_id` düşseydi bir firmanın gönderdiği anahtar BAŞKA firmanın isteğini 409'a düşürürdü ve özet tutarsa o firmanın CEVABINI tekrar oynatırdı (kiracı sızıntısının ta kendisi), `user_id` düşseydi anahtarı KENDİ yerelinde üreten iki istemci aynı firmada birbirinin isteğini yutardı; `uq_idempotency_keys_company_user_key` yalnız bir tekillik değil ARA KATMANIN EŞZAMANLILIK SINIRIDIR — iddia bir INSERT'tür ve yarışı veritabanı çözer; `user_id`in YABANCI ANAHTARI ÇIPLAKTIR ve bu 0062'nin bileşik-anahtar kuralına bir istisna DEĞİL, ÖLÇÜLMÜŞ bir olgudur: `app_users` tablosunda `company_id` sütunu YOKTUR, yani bileşik anahtar KURULAMAZ; `UNIQUE(company_id, id)` ileride bileşik yabancı anahtar HEDEFİ olabilmek için açıldı; `status` KAPALI KÜMESİ `processing | completed` ve ÜÇÜNCÜ DEĞER — özellikle `failed` — BİLEREK YOK: 5xx cevaplar SAKLANMAZ, satır SİLİNİR, çünkü saklanmış bir 5xx istemcinin tekrar denemesini SONSUZA KADAR aynı hataya mahkûm ederdi ve idempotensinin amacı tekrarı GÜVENLİ kılmaktır İMKÂNSIZ kılmak değil; ARA KATMANIN YERİ `app/main.py`nin `security_and_audit`i İÇİNDE, `resolve_company` BAŞARDIKTAN SONRADIR ve sıra KEYFİ DEĞİL, üç şey ona bağlı — anahtarın kapsamının İKİ bileşeni firma çözülmeden YOKTUR, `must_change_password` 403'ü İDDİADAN ÖNCE düşmek zorundadır (düşmeseydi zorunlu rotasyona takılan istek anahtarı iddia eder ve kullanıcı parolasını değiştirdikten sonra AYNI anahtarla 409 alırdı), ve kimlik/CSRF/yetki kapıları da önce düşer; sıra `tests/test_54b_idempotency.py::test_ara_katman_KIRACI_COZUMUNDEN_SONRA_kosuyor` ile KAYNAK ÜZERİNDE ve davranış smoke'unun 11. adımında GERÇEK ŞEMADA çivili; İKİ AYRI KISA TRANSACTION ZORUNLUDUR — ara katman işleyicinin transaction'ını PAYLAŞAMAZ (işleyici kendi `SessionLocal`ını `Depends` üzerinden alır ve kendi sınırında commit eder, o sınır ara katmana GÖRÜNMEZ); PENCERE AÇIK VE KAYDA GEÇTİ: işleyici commit ettikten SONRA tamamlama yazımı düşerse satır `processing` kalır ve o anahtar `expires_at`e kadar 409 `IDEMPOTENCY_IN_PROGRESS` döner — alternatifi (satırı düşürmek) TAMAMLANMIŞ bir yazmanın ikinci kez koşmasına izin vermekti ve iki kötüden GÖRÜNÜR olanı seçildi; TTL 24 SAAT ve TEMİZLİK TEMBEL/ANAHTAR BAŞINADIR, ZAMANLANMIŞ SÜPÜRGE BU TURDA YOK ve bu bir EKSİKLİK OLARAK YAZILDI — hiç tekrar edilmeyen anahtarların satırları defterde kalır; `ix_idempotency_keys_expires_at` TAM OLARAK o süpürgenin taraması için BUGÜNDEN açıldı ki süpürgeyi ekleyecek tur ŞEMA GÖÇÜ AÇMAK ZORUNDA KALMASIN; CEVABIN SAKLANMASININ SINIRI VE SINIRIN DIŞI AYRI AYRI KARARA BAĞLANDI — gövde YALNIZ `application/json` ise ve 64 KiB'ı aşmıyorsa saklanır, BOŞ gövde (204) da saklanır ve boş oynatılır, BUNUN DIŞINDAKİ HER CEVAP `completed` yazılır ama `response_body` NULL kalır ve o anahtarın TEKRARI 409 `IDEMPOTENCY_RESPONSE_NOT_STORED` alır — İŞLEYİCİ İKİNCİ KEZ KOŞTURULMAZ, çünkü tekrar koşturmak bu modülün var olma sebebini (yan etkinin BİR KEZ olması) ihlal ederdi; İSTEK GÖVDESİNİN TAMPONLANMASI AYRI BİR KUSURDU VE ÖLÇÜLDÜ — `security_and_audit` ara katmanların EN DIŞTAKİDİR (`app.add_middleware` listeye BAŞTAN ekler; gerçek sıra: bu ara katman -> CORS -> `RequestBodyLimitMiddleware`), yani `await request.body()` çağrıldığında GÖVDE SINIRI KAPISI HENÜZ KOŞMAMIŞTIR ve `Idempotency-Key` taşıyan 10 MiB'lık bir içe aktarma isteği reddedilmeden ÖNCE belleğe alınırdı; çare sınırı BURADA DA sormaktır ve sınır BİLEREK DAR (1 MiB), aşan istek SESSİZCE KORUMASIZ BIRAKILMAZ, 413 `IDEMPOTENCY_REQUEST_TOO_LARGE` ile REDDEDİLİR ve istemci başlığı düşürüp aynı isteği gönderebilir; uzunluğu BİLİNMEYEN gövde (chunked ya da bozuk `Content-Length`) 411 `IDEMPOTENCY_LENGTH_REQUIRED` alır, `Content-Length` YOKLUĞU ise GÖVDESİZLİK demektir ve gövdesiz `DELETE` bu yüzden reddedilmez (smoke'un 13. adımında `DELETE`in 204'ünün tekrar oynatılmasıyla birlikte ölçülüyor); AYNI ANAHTAR FARKLI GÖVDE 422 `IDEMPOTENCY_KEY_REUSED` alır çünkü bir anahtar TEK bir isteği temsil eder; GÖVDENİN İŞLEYİCİ TARAFINDAN YENİDEN OKUNABİLDİĞİ VARSAYILMADI ÖLÇÜLDÜ: Starlette 1.3.1'in `BaseHTTPMiddleware`i isteği `_CachedRequest` ile sarıyor ve tamponlanmış gövdeyi aşağı akışa TEKRAR OYNATIYOR — smoke'un 1. adımı gövdeli bir `POST /api/customers`in işleyicide DOĞRU okunduğunu gerçek şemada gösteriyor; ATLANAN UÇ LİSTESİ ELLE YAZILDI, KAPI ONU KIRDI VE ÖLÇÜM KAZANDI — ilk hâl BEŞ dosya / DOKUZ uç diyordu ve `tests/test_54b_idempotency.py::test_ATLANAN_UCLAR_kendi_basligini_okuyan_YEDI_DOSYAYA_karsilik_geliyor` (kapı `app/routers/` altında `idempotency-key` geçen dosyaları TARIYOR) `app/routers/finance.py` ile `app/routers/work_orders.py`yi ORTAYA ÇIKARDI: birincisi `POST /api/payments`te başlığı `request.headers.get('idempotency-key', '')` ile okuyup `payment_idempotency`ye veriyor, ikincisi `POST /api/work-orders/{id}/receivable/reverse`te `Header(alias="Idempotency-Key")` ile ZORUNLU alıyor — liste YEDİ dosya / ON BİR uca çıktı ve `finance.py`nin VARSAYILANININ BOŞ DİZGİ olması onu listeden ÇIKARMADI çünkü başlığın o yoldaki ANLAMINI o uç tanımlıyor; atlama şablon biçiminde yazıldı ve `{...}` parçası `[^/]+`ya çevriliyor (yol parçası SINIRINI aşamaz — `.+` yazılsaydı `/api/payment-allocations/1/reversal/anything` sessizce korumasız kalırdı, kapı bunu adıyla ölçüyor), şablonların OpenAPI şemasında GERÇEKTEN VAR olduğu smoke'un 0. adımında doğrulanıyor ki bir uç yeniden adlandırıldığında liste SESSİZCE boşa düşmesin; `app/routers/farm.py` ve `app/routers/field.py` LİSTEDE DEĞİL ve olmaması ÖLÇÜLMÜŞ BİR KARARDIR — onların idempotensisi `operation_id` GÖVDE alanındadır ve `Idempotency-Key` başlığını HİÇ OKUMAZLAR, ayrıca `app/herd_schemas.py` `extra="forbid"` kullandığı için gövdeye alan eklemek KAPALI bir sözleşmeyi kırardı — genel çözümün BAŞLIKTA olmasının ölçülmüş sebebi tam olarak budur; DAVRANIŞ OPT-IN'DİR: başlık yoksa hiçbir şey olmaz, `GET`/`HEAD`/`OPTIONS` defterin ADINI BİLE ANMAZ, `PUBLIC_API` uçları kiracı çözümüne HİÇ girmediği için buraya ulaşmaz; İĞNELER `c5e8b5b` ÜZERİNDE SIFIRDAN ÖLÇÜLDÜ — `TENANT_TABLES` 114 -> 115 (`idempotency_keys`, `len(TENANT_TABLES)` ile ölçüldü) ve KİRACI DIŞA AKTARIMININ ÜÇ DONMUŞ SAYISI birlikte kımıldadı (`test_yuz_iki_tablo_dosyasi_tam`, `test_tablo_sirasi_topolojik_ve_tam`, boş firma dışa aktarımı — üçü de 114 -> 115) ve uygulama tarafı DOKUNULMADI çünkü `_kiraci_tablolari` kümeyi ŞEMADAN türetiyor; ALEMBİK BAŞI BEŞ AYRI DOSYADA çivili ve beşi de `20260909_0076` (`tests/test_e1b_plantback.py`, `tests/test_e2_tedavi_arinma.py`, `tests/test_e3_karantina.py`, `tests/test_goc_zinciri.py` — orada ÜÇ ayrı iddia: `zincir.bas`, ata kapısı ve CLI çıktısı — ve `backend/test_1b_a_alis_lot_postgresql.py`, ki bu SONUNCUSU `pytest tests` taramasının HİÇ TOPLAMADIĞI üst düzey dosyalardandır); PG TEST POPÜLASYONU ARTIK BİR TAMSAYI DEĞİL ENVANTERDİR (#74/H7) ve bu dal ONA UYDU: `backend/tests/pins/pg_twins.txt`e SIRALI TEK SATIR eklendi (`test_54b_idempotency_postgresql.py`), dosya 112 -> 113 satır; `ci.yml` sayıyı `wc -l < tests/pins/pg_twins.txt` ile TÜRETİYOR, yani ÇİVİLENMİŞ BİR TAMSAYI ARTIK YOK ve önceki turun `BEKLENEN_PG_DOSYA_SAYISI=113` / `test_pg_test_population_exact_113` düzenlemeleri YENİDEN TABANLAMADA TÜMÜYLE DÜŞÜRÜLDÜ (develop'un tarafı alındı); ALT SÜREÇ SQL YÜZEYİ DE ARTIK ENVANTERDİR (#74/H7): `backend/tests/pins/alt_surec_sql.txt`e `tests/test_54b_idempotency.py	1` SIRALI olarak eklendi (122 -> 123 satır) ve ADET `_alt_surecte_sql()` ile ÖLÇÜLDÜ (CPython 3.12, dönüş artık `dict[str, int]`; ölçülen sözlükte `tests/test_54b_idempotency.py` -> 1, toplam 123 dosya / 204 metin ama TOPLAM TÜRETİLİR, ÇİVİLENMEZ). ADEDİN NEDEN 1 OLDUĞU ÖLÇÜLDÜ, VARSAYILMADI: `tests/test_54b_idempotency.py`de İKİ gömülü betik var ama sayaca YALNIZ BİRİ giriyor — `_SMOKE` defteri ham `SELECT`le okuyor, `_GOC_TURU` ise HİÇ SQL FİİLİ TAŞIMIYOR (yalnız `command.upgrade`/`downgrade` koşturup `sa.inspect()` ile tablo/indeks/tekil adlarını okuyor), yani 1B-C'nin `_TAZE_FK`iyle AYNI kalıp; 5.4b'nin PG İKİZİ ALT SÜREÇ AÇMIYOR ve bu yüzden SAYILMIYOR — 1B-D'nin ikizi açıyordu ve develop'un sayacı o yüzden İKİ artmıştı, bu fark sayıların ARİTMETİKLE DEVRALINAMAYACAĞININ kendisidir; DİNAMİK SQL SAYAÇLARININ HİÇBİRİ KIMILDAMADI (toplam `268`de SABİT) çünkü `app/idempotency.py`nin BEŞ `text()` metninin BEŞİ DE SABİTTİR — kapı ayrıca beşinin de `company_id=:cid` + `user_id=:uid` taşıdığını AST ile çiviliyor; ÇEKİRDEK SORGU ENVANTERİ KIMILDAMADI (modül Core `select/update/delete` KULLANMIYOR); ROTA/İZİN ENVANTERLERİ KIMILDAMADI çünkü bu dilim TEK BİR YENİ UÇ AÇMIYOR; AÇILIŞ DDL'İ KAPISI (0072'de CI'da kırmızı olan, 0074/0075'te tekrarlanan ayrım) bu tur da kuruldu ve `idempotency_keys`in `Table()` olarak BİLDİRİLMEDİĞİ ölçüldü — bildirilseydi göç onu VAR bulup tek `if` dalını atlar ve YEŞİL biterdi; SÖZLEŞME ZİNCİRİ: `Idempotency-Key` OpenAPI'DE DEĞİLDİR ve olamaz — bir ara katman başlığıdır, hiçbir operasyonun parametresi olarak bildirilmez; bu bir iddia olarak bırakılmadı, `backend/export_openapi.py` HEM bu dalda HEM tabanda koşturuldu ve iki `openapi.json` BAYT BAYT AYNI çıktı, dolayısıyla `frontend/src/api/types.gen.ts` ve `frontend/src/test/fixtures/pos-api.json` KIMILDAMADI — `git diff --name-only 1aa8a8b HEAD` ile ölçüldü: `frontend/` altında SIFIR dosya; KANIT: SQLite kapıları `tests/test_54b_idempotency.py` 11 test YEŞİL (dokuz statik kapı + göç turu up->down->up + on üç adımlık davranış smoke'u), PostgreSQL ikizi `backend/test_54b_idempotency_postgresql.py` GERÇEK PG 16.14 üzerinde 15 test YEŞİL ve içinde YİRMİ EŞZAMANLI istek `Barrier` ile aynı anda salınıp TAM BİR taze 201, TAM BİR defter satırı ve TAM BİR müşteri satırı ölçüldü (ölçülen şey durum kodları DEĞİL DEFTERİN KENDİSİDİR — tek turluk bir kod dizisi yarışın hiç tetiklenmediği anlamına da gelebilirdi); PG İKİZİ İLK HÂLİYLE YEŞİLDİ AMA YEŞİLLİĞİ YALAN SÖYLÜYORDU ve bu ÖLÇÜLDÜ: paylaşık veritabanında (önceki koşuların AÇILIŞINI devralarak) 15/15 geçiyordu, oysa CI her `*_postgresql*.py` dosyasını TAZE bir şemaya karşı koşturur — tek kullanımlık kapta (`pglens`, 5433) `DROP SCHEMA public CASCADE` sonrası aynı dosya 13/15 KIRMIZI olmuştu (`NoResultFound`), çünkü `alembic upgrade head` ŞEMAYI kurar AÇILIŞ VERİSİNİ (bootstrap admin + firma) KURMAZ ve ikizin `_kimlik()`i `app_users`ta `admin` satırını arıyordu; çare fixture'ın uygulamanın AÇILIŞINI bir kez koşturmasıdır (`with TestClient(app): pass`) ve BU TABANDA da doğrulandı: `DROP SCHEMA public CASCADE` sonrası TAZE şemada 15/15, AYNI (artık kirli) şemada İKİNCİ koşuda yine 15/15 — yani ikiz kendini de tekrar edebiliyor; KANONİK TARAMA (`python run_isolated_tests.py --workers 4`, `backend/` içinden, `PYTHONUTF8=1`) 413 aktif dosyanın 413'ünü GEÇTİ, 2352 test sonucu kaydedildi, süre 779.3s, çıkış 0; KANONİK TARAMA ÖNCEKİ TABANDA GERÇEK BİR KIRILMA BULMUŞTU ve düzeltmesi bu dalda duruyor: `test_payment_allocation_live_paths.py` bir `Idempotency-Key`i `headers_b` sözlüğüne koyup o sözlüğü ÜÇ AYRI isteğe veriyordu (`POST /api/customers` anahtarı ÖNCE iddia ediyor, sonraki `PUT /api/payments/{id}` AYNI anahtarla BAŞKA bir gövde gönderiyordu) ve ara katman bunu 422 `IDEMPOTENCY_KEY_REUSED` ile reddetti — REDDETMESİ KURALIN KENDİSİDİR, anahtarın gerçek hedefi zaten ATLANAN `/api/payments` ucudur ve anahtar o TEK çağrıya taşındı, testin ölçtüğü çapraz-kiracı davranışı DEĞİŞMEDİ; KAYIT ADI `python scripts/durum.py --sonraki 72` ile TÜRETİLDİ (YENİ BİÇİM, `pr-0072.md`) ve ÖNCEKİ TURUN ESKİ BİÇİMLİ `0108-pr-0072.md` DOSYASI YENİDEN TABANLAMA SIRASINDA DÜŞÜRÜLDÜ — kesme sırası 114'tür ve o ad KESME SONRASI eklendiğinde kapıyı KIRARDI; tarihsel korpustaki `0036-pr-0072.md` YERİNDE KALDI ve çakışma DEĞİLDİR (göç PR numaraları gelecekteki GitHub PR numaralarıyla çakışabilir, `cift_kayit_denetle` bunu adıyla yazıyor).
