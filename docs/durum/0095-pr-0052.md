#52 — YETKİ NÜFUS PİNİNİN İÇE AKTARMA KİRLENMESİ: `tests/test_authorization_population_reconciliation.py` içe aktarmada `os.environ.setdefault("DATABASE_URL", ...)` ile çalışma dizininde paylaşılan bir sqlite açıyordu; aynı süreçte toplanan `tests/test_line_item_tenancy_slice1.py` `DATABASE_URL yok` atlaması yerine o URL ile KOŞUYORDU (ölçüldü 2026-09-06, develop `185bc44`); DÜZELTME: import-time `setdefault` kalktı, URL `tmp_path_factory` ile modül-kapsamlı tmp sqlite'a bağlanıyor, `Settings`/`app.main` o URL altında içe aktarılıyor, sonra `DATABASE_URL` süreç ortamından SİLİNİYOR — kardeş modülün `os.environ` okuması etkilenmez; izole koşucu zaten URL verdiyse dokunulmaz; `backend/app` SIFIR satır (`git diff --stat 185bc44 -- backend/app` BOŞ); `test_*postgresql*.py` EKLENMEDİ; göç YOK; yeni uç YOK; KANIT (a) `pytest tests/test_authorization_population_reconciliation.py tests/test_line_item_tenancy_slice1.py` → 7 passed / 2 skipped (slice1 yalnızken de 2 skipped), paylaşılan cwd sqlite YOK, `git status --porcelain` yalnız bu kayıt dosyası; (b) dosya yalnız 7 passed (3.05s) ve `run_isolated_tests.py test_authorization_population_reconciliation.py` PASS 7/7 (8.0s); (c) paylaşılan cwd sqlite adı depoda SIFIR kez geçiyor; alt süreç SQL sayacı YENİDEN ÖLÇÜLDÜ 110/177 SABİT ve bu dosya listede YOK (subprocess SQL eklenmedi); kiracı nöbetçisi parmak izi bu dosyayı PINLEMIYOR (DYNAMIC_SQL yalnız `app/`); kayıt adı `python scripts/durum.py --sonraki 52` verdi (`0095-pr-0052.md`); dal `cursor/xdb-import-kirlenmesi-5f00` (Buzz worktree adı `test/xdb-import-kirlenmesi`).
