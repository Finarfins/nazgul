#116 — E4a e-İRSALİYE DÜZ SEVK (göç `20260913_0083`, YENİ kiracı tablosu `despatch_notes`): bir fatura -> BİR irsaliye, tam miktar, tek şoför, tek araç; KISMİ SEVK, bir faturaya ÇOKLU irsaliye ve ReceiptAdvice (ticari yanıt) akışı E4b'nin KAPSAMIDIR ve bu dilim onları ENGELLEMEZ yalnız BUGÜN KURMAZ — dikişler bilerek bırakıldı: `invoices`a ters yönde bir `despatch_id` sütunu EKLENMEDİ (eklenseydi 1:1 ŞEMAYA çivilenir ve ikinci irsaliye o sütunu düşürmeyi gerektirirdi), `UNIQUE(company_id,invoice_id)` E4b'de TEK bir `drop_constraint` ile kalkar, `despatch_lines` defteri AÇILMADI çünkü düz sevkte irsaliye satırı fatura satırının BİREBİR kopyasıdır ve kopyalanan bir şey ayrı bir defter gerektirmez; TABAN `origin/develop` @ `72cfa09` ve sha OTURUM AÇILIŞINDAKİ ANLIK GÖRÜNTÜDEN DEĞİL `git fetch` sonrası ÖLÇÜLDÜ (anlık görüntü `42b6a6b` diyordu, yani BAYATTI); KEŞİF RAPORUNUN HER "DOĞRULANMADI"SI BİR FAIL-CLOSED DALA DÖNÜŞTÜ, HİÇBİRİ TAHMİNLE KAPATILMADI (`docs/e4-eirsaliye-kesif-2026-09-09.md`): (a) `format=pdf` 501 "e-İrsaliye PDF sağlayıcı tarafında doğrulanmadı" — WSDL'de AYRI BİR PDF OPERASYONU YOK ve `SEARCH_KEY/CONTENT_TYPE` bir `xs:string` olduğu için `PDF` değerinin desteklendiği YALNIZ ŞEMADAN ÇIKARILAMAZ (§2.4); 404 DEĞİL çünkü XML sureti VAR, 503 DEĞİL çünkü BEKLEMEK BUNU DÜZELTMEZ, 501 doğru olanı söyler: sunucu bu biçimi UYGULAMIYOR; arayüzde de PDF butonu YOK (görünmeyen bir buton, her basışta hata veren bir butondan iyidir); (b) İPTAL UCU YOK — `CancelDocumentRequest` ŞEMADA VAR ama WSDL portType/binding'de bir OPERASYON DEĞİL (§2.4), `Mark*` işlemleri iptal SAYILMADI ve `IZIBIZ_OP_CANCEL_DESPATCH` diye bir sabit UYDURULMADI (kapı `test_IPTAL_UCU_YOK` ROTA LİSTESİNİ okuyor kaynak METNİNİ değil — ilk yazımda metin taraması KENDİ docstring'imdeki "cancel" kelimesine takıldı ve kapı YANLIŞ SEBEPTEN kırmızı oldu, düzeltildi); (c) PRODUCTION ADRESİ YAZILMADI — iki aday da denenmiş ve 404/502 vermişti (§2.1), yol yalnız TABAN adrese ekleniyor ve canlı allowlist BOŞ olduğu için e-İrsaliye canlı kanalı MEVCUT ortam kilidiyle (`izibiz_endpoint_violation`) kapalı, AYRI BİR KAPI YAZILMASINA GEREK KALMADI ve bu ÖLÇÜLDÜ (`test_PRODUCTION_ADRESI_yazilmamis`); (d) "1 TEMMUZ 2026" HİÇBİR YERE GÖMÜLMEDİ — tarih MÜKELLEF KOŞULUNA bağlıdır (ciro, hesap dönemi, sektör; §4.1) ve dört dosyada dört yazım biçimi taranıyor (`test_TARIH_kodda_SABITLENMEMIS`); DURUM MAKİNESİ AYRI VE e-ARŞİV EŞLEMESİ ÖDÜNÇ ALINMADI — keşfin P0'ı (§1/§5): `endpoints.IZIBIZ_STATUS_ALIASES` içinde `"100" -> PENDING` ve ORADA DOĞRUDUR (e-Arşiv'de 100 "KUYRUĞA EKLENDİ"), ama e-İrsaliye'de AYNI SAYI "durum GÜNCELLENMEDİ" demek ve o tabloyu taşımak HİÇBİR ŞEY OLMAMIŞ bir belgeyi "kuyruğa alındı" diye göstermek olurdu; `app/einvoice/edespatch.py` KENDİ tablosunu taşıyor, `map_provider_status` HİÇ çağrılmıyor ve e-Arşiv tablosuna DOKUNULMADI (kapı iki tarafı da ölçüyor: `kodu_coz("100")==UNKNOWN` VE `IZIBIZ_STATUS_ALIASES["100"]=="PENDING"` VE iki sözlük AYRI NESNELER); `"100"` sözlükte AÇIKÇA yazılı ve AÇIKÇA `UNKNOWN` — boş bırakılsaydı sonuç aynı olurdu ama o zaman satırın YOKLUĞU bir UNUTKANLIKTAN ayırt edilemezdi; `"106"` BİLEREK YOK çünkü sağlayıcı tablosunda `SIGN_PROCESSING` VE `SIGN_FAILED` olarak İKİ KEZ geçiyor (§5) ve çelişkili bir kodun DOĞRU eşlemesi YOKTUR — birini seçmek imzalanmış bir belgeyi "imza başarısız" saymak olurdu; SEKİZ DURUM, TEK TERMİNAL (`DELIVERED`) ve `REJECTED` KÜMEDE YOK çünkü e-İrsaliye'de ret ReceiptAdvice'tan doğar (E4b) — üretemeyen bir durumu sözlüğe yazmak, gelecekte birinin oraya rastgele bir kod eşlemesini kolaylaştırırdı; ONBİR kodun ONBİRİ de tek tek çivili (101/102/103/104/105/107/133/134/135/136/137); TIMEOUT `FAILED` DEĞİL `UNKNOWN` ÜRETİR ve BU DİLİMİN EN ÖNEMLİ DAVRANIŞIDIR: `FAILED` "gönderim İNMEDİ, sağlayıcıda belge YOK" İDDİASIDIR ve bir zaman aşımından sonra bu BİLİNEMEZ; yanlışsa bedeli SOMUT — ilk istek aslında inmişse `FAILED`in açtığı ikinci gönderim AYNI SEVK İÇİN İKİNCİ BİR İRSALİYE KESER; `UNKNOWN` o iddiayı YAPMAZ, `GONDERIM_KAPALI` kümesindedir (gönderim YALNIZ `NONE` ve `FAILED`ten açık) ve rank tablosunda `FAILED`in ÜSTÜNDEdir, yani bir TIMEOUT'tan sonra gelen "gönderim inmedi" iddiası bilgimizi GERİ ALAMAZ; ÇIKMAZ SOKAK DEĞİL — `bilinmeyeni_yok_say` ADIYLA bir çıkış kapısıdır ve YALNIZ `UNKNOWN`da çalışır: sağlayıcı belgenin VARLIĞINI olumsuzlarsa (`raw["belge_yok"]`, `ERROR_CODE=10008` dâhil) durum `FAILED`e döner ve gönderim yeniden açılır; KARAR ADAPTÖRDE DEĞİL UÇTA veriliyor çünkü yerel durumu değiştirmek uç katmanının işidir ve adaptörün "bulamadım"ı ile ucun "gönderim inmemiş" sonucu AYNI ŞEY DEĞİLDİR; SABİT ETTN İKİNCİ SAVUNMA: `despatch_uuid` satırın DOĞUŞUNDA üretilir (gönderimde DEĞİL) ve denemeler boyunca DEĞİŞMEZ — tek adımlı bir uçta bir TIMEOUT kaydı hiç yazamadan dönerdi ve sağlayıcıya gitmiş OLABİLECEK bir belgenin ETTN'i elimizde KALMAZDI, yani sorgulanamaz ve tekrarlanamaz bir belge; uçtan uca ölçüldü (`test_ETTN_denemeler_boyunca_SABIT`): TIMEOUT -> `UNKNOWN`, ikinci gönderim 409 ve SAĞLAYICIYA HİÇ GİDİLMİYOR (sahte sağlayıcının çağrı sayacı 1'de kalıyor), ETTN iki denemede de AYNI; PG İKİZİ GERÇEK BİR KUSUR BULDU VE KUSUR KODDA DÜZELTİLDİ, TESTTE DEĞİL — PostgreSQL zaman damgasını OTURUM SAAT DİLİMİNDE döndürüyor (ÖLÇÜLDÜ: `SHOW TimeZone` -> `Europe/Istanbul`), yani `08:30+00:00` yazılan satır `11:30+03:00` olarak geri geliyor; `_an_coz` geleni OLDUĞU GİBİ bırakıp `strftime` uyguladığı için AYNI SATIR SQLite'ta `ActualDespatchTime=08:30:00`, PG'de `11:30:00` beyan ediyordu — yani MALİ BELGENİN İÇERİĞİ VERİTABANI AYARINA BAĞLIYDI ve bu SQLite ikizinin GÖREMEYECEĞİ bir kusurdur (PG ikizinin var oluş sebebi TAM OLARAK budur); ÇARE: `_an_coz` artık her zaman sabit `BELGE_UTC_OFFSET`e (+03:00) çeviriyor ve `11:30:00` HEM DOĞRU HEM DETERMİNİSTİK — UBL-TR `ActualDespatchTime` bir YEREL saattir (denetçi malın Türkiye saatiyle kaçta yola çıktığını görür) ve 08:30 UTC Türkiye'de 11:30'dur; `ZoneInfo("Europe/Istanbul")` DEĞİL SABİT +03:00 SEÇİLDİ ve gerekçe ölçülü: Windows'ta `zoneinfo` ayrı bir `tzdata` paketi ister ve iki koşu ortamının biri tzdata'lı biri tzdatasız olsaydı AYNI SATIR İKİ FARKLI SAAT beyan ederdi — yani düzelttiğimiz kusurun ta kendisi geri gelirdi; SINIR AÇIK YAZILDI: Türkiye 2016'dan beri KALICI UTC+3'tür ve sabit bugün kesilen her belge için doğrudur, 2016 ÖNCESİNE tarihlenen bir sevk için yanlış olurdu (böyle bir belge bu sistemde üretilmiyor); İKİ İKİZ DE aynı değeri çiviliyor ve SQLite tarafına ayrıca dört farklı offset'le (`+00:00`, `+03:00`, `-05:00`, naive) AYNI SAATİ veren bir kapı eklendi (`test_SAAT_cevirimi_SABIT_ve_girdi_OFFSETINDEN_BAGIMSIZ`); ŞEMA KARARLARI: `despatch_uuid` UNIQUE'i KÜRESELDİR (`company_id` anahtarda YOK) — 0080'in `islem_anahtari` gerekçesiyle aynı: değer bir UUID'dir, kiracı sütunu eklemek tekilliği ZAYIFLATIR ve ETTN GİB nezdinde de küreseldir (PG ikizinde iki AYRI firmada aynı ETTN denendi ve REDDEDİLDİ); TAŞIMA KOLONLARI NOT NULL DEĞİL, `ck_despatch_notes_tasima` İKİ DALLI — GİB kılavuzu taşımayı plaka+şoför VEYA kargo/lojistik firması olarak düzenliyor (§3.2, [G §9-10]) ve dördünü birden NOT NULL yapmak kılavuzun İZİN VERDİĞİ bir sevki şemada İMKÂNSIZ kılardı, dördünü serbest bırakmak ise HİÇBİR taşıma bilgisi taşımayan bir irsaliye üretirdi; E4a'nın DAHA DAR kuralı (plaka+şoför ZORUNLU) UÇ KATMANINDADIR çünkü o bir KAPSAM kararıdır, bir mevzuat kuralı değil — kargo dalı E4b'de ŞEMAYA DOKUNULMADAN açılabilsin; PG'de dört dal da tek tek ölçüldü (şoför dalı GEÇER, kargo dalı TEK BAŞINA GEÇER, hiçbiri yoksa DÜŞER, YARIM dal DÜŞER); `invoice_id` FK'si ÇIPLAK ve bu İHMAL DEĞİL ÖLÇÜLMÜŞ BİR KARAR: `invoices` üzerinde `uq_invoices_company_id` YOKTUR (ölçüldü — `alembic/versions/` altında o adda hiçbir kısıt yok; tablo 0012'de `uq_invoices_company_number` ve `uq_invoices_work_order_type` ile açılıyor) ve bileşik anahtarı kurmak SQLite'ta `batch_alter_table("invoices")` ile ŞEMANIN EN MERKEZİ MALİ TABLOSUNUN YENİDEN İNŞASINI gerektirirdi — yarıçapı bu özelliğin kazandırdığından büyük; KAYBEDİLEN KORUMA YERİNE KONDU ve İKİ KATMANDA ölçülüyor: `UNIQUE(company_id,invoice_id)` (PG'de gerçekten reddediyor) + ucun HER sorgusundaki açık `company_id=:cid` yüklemi (çapraz kiracı faturası 404, 403 DEĞİL — 403 bir VARLIK BİLGİSİ sızdırırdı); `(company_id,delivery_customer_id) -> customers(company_id,id)` ise BİLEŞİK (0062'nin kuralı, hedef 0026'da zaten kurulmuş, bedava geliyor); PİNLER ÖNCE/SONRA HEPSİ ÖLÇÜLDÜ: rota sözleşmesi 392/301 -> 399/307 (YEDİ uç, ALTI yol — liste ile oluşturma AYNI YOLU farklı METOTLA paylaşıyor ve `EXPECTED_PATH_COUNT` metot değil YOL sayar, +7/+6 ayrışımı bu yüzden doğrudur), parmak izi `618b656d -> 67a653b3`; GET izin envanteri 186 -> 190 (dört yeni GET, DÖRDÜ DE `sales`), parmak izi `0e8ce4f3 -> 0f87b6bd`; `TENANT_TABLES` 120 -> 121 ve çivi BEŞ dosyada (`test_sec6_ip_limitleri`, `test_wa1_ingress`, `test_wa2_eslestirme`, `test_wa4_bekleyen` ve `test_kiraci_disa_aktarim` — SONUNCUSUNDA ÜÇ AYRI SATIRDA DÖRT SAYI, dosya bir de ndjson üretimini sayıyor) artı `TENANT_TABLES` frozenset'inin KENDİSİ; `EXPECTED_AUTHENTICATED` 379 -> 386 (+7, bire bir eklenen uç sayısı); `EXPECTED_READ` 80'de SABİT ve bu DÖRT GET İÇİN ANLAMLIDIR — izinleri `sales` olduğu için çıplak `read`e HİÇ düşmüyorlar, önek kuralı genel güvenli-metot kuralının ÜSTÜNE yazılmasaydı bu sayaç 80 -> 84 kayardı; `EXPECTED_UNDENIABLE` 97'de SABİT; `pins/pg_twins.txt` 123 -> 124 satır; alembic head `20260912_0082 -> 20260913_0083` ve çivi YEDİ dosyada (`tests/test_e1b_plantback`, `tests/test_e2_tedavi_arinma`, `tests/test_e3_karantina`, `tests/test_goc_zinciri` [ÜÇ satır], `test_1b_a_alis_lot_postgresql`, `test_e1_efatura_sertlestirme_postgresql` [`BAS` sabiti], `test_wa3_worker_postgresql`) — SAYILMADI, GREPLENDİ; DİNAMİK-SQL ALLOWLIST KIMILDAMADI (269'da sabit) ve gerekçe ölçülü: uçtaki HER `text()` SABİT metindir, `invoice_id` süzgeci bir f-string parçasıyla değil her zaman bağlı olan `(:invoice_id IS NULL OR invoice_id=:invoice_id)` ile kuruldu, yani dosya listeye HİÇ girmiyor ve girmeyen bir dosyanın parmak izi de kayamaz (kapı `test_UC_dosyasinda_DINAMIK_sql_yok`, AST ile tarıyor); YETKİ İKİ YÖNDE DE ÖLÇÜLDÜ VE İKİSİ DE YANLIŞTI — kural yazılmadan ÖNCE `required_permission` AYNI UÇ AİLESİNE metoda göre İKİ FARKLI cevap veriyordu: DÖRT GET "read" (FAZLA GENİŞ — bu satırlar müşteri VKN/TCKN'sini, teslim adresini ve ŞOFÖRÜN TCKN'SİNİ taşıyor; `depo` ve `rapor` bunları görmemeli, `/api/invoices`ın SEC-3'te `read`ten `sales`a çekilmesiyle AYNI gerekçe, hatta daha keskin çünkü fatura bir TÜZEL KİŞİ verisi, şoför TCKN'si bir GERÇEK KİŞİ verisidir) ve ÜÇ POST "__admin_only__" (FAZLA DAR — irsaliye kesmek günlük bir SATIŞ işidir, `satis` rolü faturayı kesip malı sevk edemezdi); ÇARE TEK ÖNEK (`/api/despatch-notes`), `/api/invoices`taki gibi ÖNEK+SONEK DEĞİL ve fark ölçülü: orada `read`te KALMASI gereken uçlar da vardı (liste, iç PDF), burada ailenin YEDİ ucunun YEDİSİ de aynı ticari veriyi taşıyor ve ikinci bir kural ÖLÜ KOD olurdu; KOMŞULAR KIMILDAMADI ve bu ÖLÇÜLDÜ (`/api/invoices`, `.../einvoice/status`, `.../invoices/{id}/pdf` hâlâ `sales`; `/api/customers` hâlâ `read`); ROL MATRİSİ GERÇEK HTTP İLE koşuluyor (5 rol x 6 uç = 30 hücre, GERÇEK kullanıcı + GERÇEK giriş) çünkü sözleşme kapılarının üçü de `required_permission`i DOĞRUDAN çağırır ve middleware'in o değeri GERÇEKTEN uyguladığını HİÇ ölçmez; MATRİSİN İDDİASI ÖLÇÜMLE DÜZELTİLDİ: ilk yazımda `{yonetici, satis}` yazmıştım, ölçüm bunu ÇÜRÜTTÜ — `muhasebe` de `sales` taşıyor, geçenler ÜÇ rol; BEKLENTİ DEĞİL SATIR DÜZELTİLDİ ve sonuç doğrudur (`muhasebe` faturaya zaten erişiyor, irsaliye aynı belgenin sevk yüzü); DÜŞENLER `depo` ve `rapor`dur ve KURALIN AMACI TAM OLARAK ODUR; YAN ETKİ — ZIP BOMBA TAVANLARI TEK YERE ÇIKARILDI: e-İrsaliye XML'i de aynı base64-ZIP sarmalıyla gelebildiği için `_pdf_from_zip`in gövdesi `_zip_uyeleri`ye taşındı ve `_xml_from_zip` onu ÇAĞIRIYOR — İKİ KOPYA OLSAYDI birinde sıkılaştırılan bir sınırın ötekinde unutulması SESSİZ bir güvenlik farkı üretirdi ve bomba unutulan kapıdan girerdi; `_pdf_from_zip`in DAVRANIŞI DEĞİŞMEDİ ve bu İDDİA EDİLMEDİ ÖLÇÜLDÜ: `tests/test_sec7_xml_zip_sertlestirme.py` (16 test) DOKUNULMADAN yeşil kaldı; PG İKİZİNDEKİ İKİNCİ DÜZELTME de bir ÖLÇÜM KUSURUYDU ve saklanmıyor: uzunluk testi başta `edespatch_gib_status_code` ile `edespatch_provider_uuid`i de INSERT ile deniyordu ve YEŞİL YANIYORDU — çünkü o iki sütun INSERT'in SÜTUN LİSTESİNDE HİÇ YOK, yani aşırı uzun değer veritabanına ULAŞMIYORDU; "kısıt çalışıyor" diye okunan şey kısıtın HİÇ SINANMADIĞIYDI, ikisi GERÇEK yazma yollarına (UPDATE — `submit`/`sync`) taşındı ve şimdi gerçekten düşüyorlar; KOŞULAR: SQLite `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4 backend/tests/test_e4a_despatch_notes.py` -> 50 test YEŞİL; PG ikizi (GERÇEK PostgreSQL 16, `REQUIRE_PG=1`, `postgresql+psycopg://lens:lens@127.0.0.1:5433/lens_e4a_test`) `pytest test_e4a_despatch_notes_postgresql.py` -> 10 test YEŞİL; pin kapıları (`test_route_security_contracts`, `test_route_get_permission_inventory`, `test_authorization_population_reconciliation`, `test_goc_zinciri`, `test_ci_gates`, `test_tenant_scoping_guard`, `test_core_tenant_scoping_guard`, `test_kiraci_disa_aktarim`, `test_wa1_ingress`, `test_wa2_eslestirme`, `test_wa4_bekleyen`, `test_sec6_ip_limitleri`, `test_core_query_inventory`, `test_backend_encoding`, `test_sec7_xml_zip_sertlestirme`) hepsi YEŞİL; GÖÇ TURU İKİ DİYALEKTTE DE KOŞULDU — SQLite'ta `upgrade head` -> `downgrade -1` -> `upgrade head` (tablo gitti ve geri geldi, 0081'in üç sütunu `invoices`ta YERİNDE kaldı) ve GERÇEK PG 16'da aynı tur artı kısıt/indeks KÜMESİNİN birebir geri geldiği (`test_GOC_TURU_up_down_up` sütun+unique+check+indeks anlık görüntüsünü karşılaştırıyor); tablo iki diyalektte de 24 sütun, 3 UNIQUE, 2 CHECK, 3 FK (biri BİLEŞİK), 1 indeks ve BEŞ zaman sütununun BEŞİ de tz-farkındalı; frontend `npm test -- --run` -> 91 dosya / 652 test YEŞİL (yeni `DespatchNotePanel.test.tsx` 20 testiyle bu sayıma DÂHİL), `npm run build` YEŞİL, `npx tsc -b` TEMİZ (`tsc --noEmit -p tsconfig.json` bu depoda SIFIR dosya denetleyen BOŞ BİR YEŞİLDİR ve kasten kullanılmadı); `frontend/src/api/types.gen.ts` `python backend/export_openapi.py -o backend/openapi.json` + `npm run types:gen` ile YENİDEN ÜRETİLDİ ve commit edildi (yedi yeni yol içinde); SANDBOX'TA GERÇEK GÖNDERİM YAPILDI VE BELGE KABUL EDİLDİ — bu dilimin en değerli ölçümü budur ve ÜÇ KUSUR BULDU. Kimlik dosyası (`backend/.env.izibiz.local`) BU MAKİNEDE VARDI (ilk yazımda "yok" demiştim; o iddia YANLIŞTI — dosya çalıştığım AĞAÇTA yoktu, MAKİNEDE vardı ve `IZIBIZ_ENV_FILE` ile okundu, kimlik bilgisi hiçbir yere KOPYALANMADI ve hiçbir değeri YAZDIRILMADI). Betik `backend/sandbox/izibiz_edespatch_smoke.py` ve CI'DA KOŞMAZ; `izibiz_smoke.py`den AYRILIYOR çünkü o SOAP'ı kendi kurar, bu ise ÜRETİM ADAPTÖRÜNÜ çağırır — sorulan soru "WSDL ne diyor" değil "bizim yazdığımız gövde KABUL EDİLİYOR MU". Gönderim GERİ ALINAMAZ (iptal operasyonu YOK) o yüzden `--gonder` AÇIK BİR BAYRAK; bayraksız koşu yalnız salt-okunur adımları yapar. ÜÇ TUR, ÜÇÜ DE ŞEMADAN DEĞİL SAĞLAYICIDAN öğrenildi ve ÜÇÜ DE ARTIK BİR KAPIDIR: (1) `ERROR_CODE=10003` "Geçersiz ID elemanı değeri. ID elemanı 'ABC2009123456789' formatında olmalıdır." — türetilmiş `IRS-<fatura no>` REDDEDİLDİ; kural `ubl_xml.py`de `INVOICE_ID_LENGTH = 16` olarak ZATEN YAZILIYDI ama HİÇBİR YERDE UYGULANMIYORDU, artık `edespatch.GIB_BELGE_NO_DESENI` bir DESENDİR ve `belge_numarasi_uret` TEK üreticidir (uç, sandbox ve testler aynı fonksiyonu çağırır); (2) `ERROR_CODE=10003` "Hatalı Posta Kodu :'' ... DeliveryAddress/PostalZone elemanı içermelidir." — OASIS onu OPSİYONEL bırakır, GİB İSTER; posta kodu UYDURULMADI, göce NOT NULL bir sütun olarak eklendi (`delivery_postal_code`, String(10)) ve forma zorunlu alan olarak kondu, çünkü mali bir belgeye sahte adres yazmak beyanı bozar; (3) `ERROR_CODE=10013 XSLT_NOT_FOUND_IN_DOCUMENT` — belge kendi görselleştirme şablonunu taşımalı; kural `ubl_xml.py` başlığında e-Arşiv için (kural 2) ZATEN yazılıydı ama e-İrsaliye'ye TAŞINMAMIŞTI, artık `edespatch.DEFAULT_DESPATCH_XSLT` var (kopya DEĞİL: kök eleman, ad alanı ve basılan alanlar farklı — fatura TUTAR basar, irsaliye MİKTAR ve TAŞIMA basar). DÖRDÜNCÜ TUR YOK: üçüncü düzeltmeden sonra `SendDespatchAdvice` `ERROR_TYPE` TAŞIMADAN döndü, `DESPATCH_ID=IRS2026993046536` geldi ve durum sorgusu HAM `100` verdi — yani belge sağlayıcıda VAR ve `100`un e-İrsaliye'de "durum güncellenmedi" demek olduğu (keşif §5) CANLI OLARAK DOĞRULANDI, eşleme onu `UNKNOWN`a çeviriyor ve `UNKNOWN` yeniden gönderimi kapattığı için çift belge kapısı sandbox'ta da kapalı. `IZIBIZ_EFATURA_SUBMIT_VERIFIED` tarzı bir KAPI e-İrsaliye için YAZILMADI ve buna GEREK KALMADI: o bayrak HİÇ DENENMEMİŞ bir kanal içindir, bu kanal DENENDİ ve GEÇTİ (bayrak bir ara yazılmıştı, belge kabul edilince KALDIRILDI — koşulmayan bir kapı bırakmak, çalışan bir özelliği gerekçesiz kapalı tutmak olurdu). `IZIBIZ_EFATURA_SUBMIT_VERIFIED`e DOKUNULMADI (hâlâ `False`). BU ÖLÇÜM İKİ KUSURU DAHA AÇIĞA ÇIKARDI ve ikisi de saklanmıyor: (a) `test_v2_9_decimal_contract` kırmızı oldu çünkü `_miktar` savunma amaçlı bir `isinstance(deger, <ikili tür>)` kullanıyordu ve depo kuralı o adın `app/**` içinde GEÇMESİNİ bile yasaklıyor; YASAK LİSTESİ İZİN LİSTESİNE çevrildi (`_MIKTAR_TURLERI = (Decimal, str, int)`, `bool` ayrıca eleniyor) ve sonuç DAHA GÜÇLÜ — `numpy.float64` gibi türler yasak listesine takılmazdı ama izin listesine de GİREMEZ; (b) `POST /api/despatch-notes` HER `IntegrityError`ı 409 "zaten var" sayıyordu ve `delivery_postal_code` NOT NULL eklenip INSERT'in sütun listesine yazılmayı unutulunca kullanıcı HİÇBİR irsaliyesi olmayan TAZE bir veritabanında "Bu faturanın e-İrsaliyesi zaten var" gördü — yanlış cevap doğru cevabın yokluğundan KÖTÜYDÜ (operatörü olmayan bir kaydı aramaya gönderirdi); artık YALNIZ tekil ihlali 409'a çevriliyor ve İMZA İKİ DİYALEKTTE AYRI olduğu için İKİSİ DE çivili (PG kısıt ADINI söyler, SQLite SÜTUN ADLARINI — tek imzaya bakan bir kontrol öteki diyalekte hiç eşleşmez ve gerçek bir çakışma 500 olarak kaçardı), kapı `test_TEKIL_IHLALI_DISINDAKI_kisit_409_SAYILMIYOR`; DEĞİŞEN DOSYALAR (32): YENİ -> `backend/alembic/versions/20260913_0083_eirsaliye.py`, `backend/app/einvoice/edespatch.py`, `backend/app/routers/despatch_notes.py`, `backend/tests/test_e4a_despatch_notes.py`, `backend/test_e4a_despatch_notes_postgresql.py`, `frontend/src/pages/DespatchNotePanel.tsx`, `frontend/src/pages/DespatchNotePanel.test.tsx`, `backend/sandbox/izibiz_edespatch_smoke.py` (CI'DA KOŞMAZ); DEĞİŞEN -> `backend/app/einvoice/endpoints.py` (EIrsaliye yolu/ad alanı/üç operasyon/istek kökleri; e-Arşiv tablosuna DOKUNULMADI), `backend/app/einvoice/provider.py` (üç yeni İzibiz metodu + ZIP yardımcısının çıkarılması), `backend/app/auth.py` (tek önek kuralı), `backend/app/main.py` (yönlendirici kaydı), `frontend/src/pages/InvoiceDetail.tsx`, `frontend/src/api/types.gen.ts` (üretilen), artı ONBEŞ pin/çivi dosyası — ve bu kayıt; kayıt adı `python scripts/durum.py --sonraki 116`
