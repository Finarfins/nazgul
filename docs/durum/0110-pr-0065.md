#65 — HİJYEN H1 İZOLE KOŞUCU ÇOCUK ÇIKTI UTF-8: `run_isolated_tests.py` çocuk stdout/stderr'i locale-varsayılan `text=True` ile okuyordu; UTF-8 kipinde (`PYTHONUTF8=1`) çözülemeyen baytlar `UnicodeDecodeError` ile `TestResult` oluşmadan çökertiyordu — dosya Raporlanmıyordu; DÜZELTME: `encoding='utf-8'` + `errors='replace'` + `_decode_captured_output` (None→`""`); `backend/app` SIFIR; göç YOK; KANIT: `PYTHONUTF8=1 python run_isolated_tests.py tests/test_isolated_test_runner.py --workers 4` → 22 passed; run id `950330ff-c711-4136-b0d0-afad22a23bae`; 17 zorunlu bağlam SABİT; dal `test/h1`; kayıt `python scripts/durum.py --sonraki 65`.
