#149 — H59: POST /api/platform/tenant-restore review-reason metni #136'daki yerine kipi kaldırılmasına göre güncellendi (tek kip "yeni"; creates a NEW company only; never writes over an existing one); parmak izi drift çıktısından yeniden çivilendi (cd72be75 -> 05dafb0d; rota sayıları 421/327 ve app dosyaları dokunulmadı); H65: 5.1a kiracı dışa aktarımının (routers/kiraci_disa_aktarim.py) PostgreSQL 16 ikizi yazıldı (backend/test_kiraci_disa_aktarim_postgresql.py, 7 test, 0 skip, taze şema): tüm 125 kiracı tablosu tohumlu olarak manifest row_counts PG sayılarına birebir eşittir, user_emails yalnız aktif üyeleri içerir (ayrılmış üye ve başka firma üyesi hariç), _seri JSON sütunları (supplier_import_profiles) gerçek JSON dict/list yazılır ve Python repr içermez, zip dosya yollarında e-posta sızmaz, firma B satırları sızmaz, Decimal 12.3456 metin ve datetime UTC kalır; pins/pg_twins.txt sıralı güncellendi (135 -> 136); test_ci_gates 26/26 yeşil — <merge tarihi>
