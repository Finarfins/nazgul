2026-09-09 — #70 1B-E: SATIŞ İADESİ ÇIKTIĞI PARTİYE **TERS FEFO** İLE GERİ VERİR (GÖÇ YOK). Taban develop `8758535` (#71/1B-D sonrası); #70 ONUN ÜSTÜNE YENİDEN TABANLANDI ve TÜM sayaçlar birleşmiş ağaçta YENİDEN ÖLÇÜLDÜ. Kaynak belgesi olan iade (`returns.source_type='order'` + `source_id`, bağ ZATEN VARDI) satışın `stock_movements.lot_id` satırlarını OKUYUP malı çıktığı partilere geri veriyor; sıra `id DESC` — SON TÜKETİLEN İLK, çünkü düz FEFO da "toplamı doğru" olurdu ve kısmi iadede yanlış partiyi şişirip toplam bakan gözden KAÇARDI. `parti_defteri._parti_iade` `fefo_sec`i ÇAĞIRMIYOR (iade SEÇMEZ, HATIRLAR) ve defteri KENDİSİ YAZMIYOR, `_parti_ac`ı çağırıyor. Kapasite "satılan" DEĞİL "satılan eksi önceden iade edilen"dir; aşım ve "bu ürün o satışta hiç yok" 422 `IADE_SATISI_ASIYOR` (sayılar gövdede). PARTİSİZ satışın iadesi ve KAYNAKSIZ iade bugünkü davranışını korur (tek satır, `lot_id` NULL) ve partisiz yolda miktar DENETLENMEZ — kapsam sınırı testte adıyla çivili. Geri alma ÇOĞALTILMADI: iade payları POZİTİF, `_parti_geri_al` aynı tek ifadeyle düşüyor ve 409 kapısı pozitif yönde de ısırıyor. 1B-A'nın `_parti_iade` AD YASAĞI, YAZMA TEKELİNE daraltıldı (miktar yazması dört fonksiyonda; ekleme yalnız `_parti_ac`ta) çünkü ad yasağı sözleşmeyi bozmayan bir işi yalnız ADI yüzünden reddediyor ve `_parti_geri_ver` diye yazan birine SESSİZCE yol veriyordu. ÖLÇÜLMEDİ: eşzamanlı iki iade serileştirilmiyor — sonuç stokla TUTARLI olduğu için stok/defter ayrışması değil, iş kuralının yarışta gevşemesi; PG ikizinin başlığında adıyla yazılı. ÇİVİLER: `workflow.py` parmak izi `cacca6359->29fb2506` (dinamik `text()` sayısı 16'da SABİT, toplam 268 değişmedi), alt süreç SQL sayacı 121/202 -> 122/203 (DOSYA +1 / METİN +1: alt sürece SQL veren tek yeni dosya `tests/test_1b_e_iade_lot.py`; PG ikizi alt süreç AÇMIYOR, 1B-D'ninki açıyordu ve develop'un 121/202'si o yüzden İKİ artmıştı), PG popülasyonu 111 -> 112 üç yerde (`ci.yml` sabiti, test adı/iddiası, `ci.yml` grep testi), `_parti_bul` ÇAĞIRANLARI 5'te SABİT (1B-D'nin `warehouses.py`si KORUNDU) ve 1B-A'nın yazma tekeli kapısı onunla BİRLİKTE ölçüyor; `openapi.json` DEĞİŞMEDİ. Sayılar aritmetikle DEVRALINMADI: 1B-D birleştikten sonra hepsi birleşmiş ağaçta YENİDEN ÖLÇÜLDÜ ve alt süreç sayacı bu yüzden beklenen +2 yerine +1 çıktı.
