#98 — E2c: e-ARŞİV PDF'İ BUGÜN İNİYOR ve `UNRESOLVED`IN SEBEBİ SAĞLAYICI DEĞİL BİZİM EŞLEME TABLOMUZDU (GÖÇ YOK — `alembic/` diff'i BOŞ, ROTA YOK, ŞEMA YOK, OpenAPI ve `types.gen.ts` KIMILDAMADI); #95 (§9) İKİ SORUYU AÇIK BIRAKTI ve İKİSİ DE KOD DÜZELTMESİ GEREKTİRİYORDU — §9 istek tarafına bakıp durdu, bu tur cevabın NE OLDUĞUNA ve o cevaba NE YAPTIĞIMIZA baktı; BİRİNCİ BULGU §9.3'ÜN HÜKMÜNÜ GEÇERSİZ KILIYOR ve bu kayda AÇIKÇA geçiyor: §9.3 "PDF'i sağlayıcı ancak GÖNDERİCİ isterse üretir, `EARSIV_PDF_FLAG=Y` yapmak yeni bir GÖNDERİM davranışıdır (geri alınamaz belge üretir) ve AYRI BİR DİLİMİN İŞİDİR" diyordu; ÖLÇÜLDÜ ki PDF `EARSIV_PDF_FLAG=N` ile gönderilmiş belgeler için YENİ GÖNDERİM YAPMADAN iniyor — doğru operasyon `GetEArchiveInvoice` DEĞİL `GetEArchiveInvoiceList`tir ve İKİ ALAN BİRDEN gerekir: `HEADER_ONLY=N` olmadan yanıt içerik düğümü HİÇ taşımaz, `CONTENT_TYPE=PDF` olmadan gelen içerik XML'dir, ve üç varyant AYRI AYRI ölçüldü (`HEADER_ONLY=N` -> `<belge>.xml` `<Invoi`; `+CONTENT_TYPE=PDF` -> `<belge>.pdf` `%PDF-1`; `+READ_INCLUDED=Y` -> aynı); BULGU TAZE BELGEYE ÖZGÜ DEĞİL ve bu AYRICA ölçüldü çünkü olmasaydı "az önce yarattığım belge" itirazı haklı olurdu: BU OTURUMDAN ÖNCE (02:18) gönderilmiş `SNG2026518354588` -> 19184 bayt ve `SNG2026471382557` -> 19168 bayt, İKİSİ DE `%PDF-1` ve İKİSİ DE `STATUS=100`, yani İMZA DA BEKLENMİYOR; `EARSIV_PDF_FLAG` HİÇ DEĞİŞTİRİLMEDİ ve gönderim yolu hâlâ `N` yazıyor — §9.3'ün "biz PDF istemiyoruz" tespiti GÖNDERİM tarafı için DOĞRUDUR, yanlış olan PDF'in YALNIZ o bayrakla alınabileceği SONUCUYDU; İKİNCİ BULGU §9'da HİÇ ARANMAMIŞTI: `query_status`un `UNRESOLVED` demesinin sebebi sağlayıcı DEĞİL BİZİM EŞLEME TABLOMUZDU — sağlayıcı DOĞRU cevap veriyor (bizim ETTN'imiz + `STATUS=100` + `STATUS_DESC=KUYRUĞA EKLENDİ` + `WEB_KEY`), `IZIBIZ_STATUS_ALIASES` `105`/`120`/`130` biliyor ama `100` BİLMİYORDU, `map_provider_status` `None` dönüyor ve `query_status` `reported not in QUERYABLE` dalına düşüp cevabı YERE DÜŞÜRÜYORDU; `STATUS=100` her GERÇEK gönderimin (`SUB_STATUS=NEW`) İLK durumudur, yani bu tek eksik satır HER e-Arşiv faturasını etkiliyordu ve §7.3'ten beri "durum sorgusu boş dönüyor" diye okunan şey DOLU bir yanıttı; BU KUSURUN BİRİM TESTLERİNDEN KAÇIŞ YOLU DA ÖLÇÜLDÜ VE ÇİVİLENDİ: elimizdeki fixture `GetEArchiveInvoiceStatus.200.xml` `SUB_STATUS=DRAFT` ile gönderilmiş bir belgeden alınmış ve `STATUS=105` taşıyor — tablonun BİLDİĞİ bir kod, yani birim testi YEŞİL üretim yolu KIRMIZIYDI; yeni fixture `GetEArchiveInvoiceStatus-queued.200.xml` (`STATUS=100`, gönderimden SIFIR saniye sonra alınmış GERÇEK yanıt) o yolu kapatıyor ve `test_ESKI_FIXTURE_NEDEN_KACIRDI_105_DRAFT_100_GERCEK` iki fixture'ın İKİ FARKLI kod taşıdığını ADIYLA ölçüyor; TAZE BELGE SONDASI GECİKMEYİ DE ELEDİ: `SNG2026252113200` gönderildi ve `t=0/5/15/30/60 sn` yoklandı, HEPSİNDE 1 satır `STATUS=100` — belge gönderildiği SANİYE sorgulanabilir, §8'in "~100 sn boyunca boş" ölçümü bir GECİKME değil bu eşleme eksikliğinin gölgesiydi; ayrıca gönderim yanıtındaki `WEB_KEY` ile durum yanıtındaki `WEB_KEY` AYNI anahtarı taşıyor; DÜZELTMELER ÜÇ DOSYADA ve HEPSİ `app/einvoice/` altında: (1) `IZIBIZ_STATUS_ALIASES` += `"100"` ve `"KUYRUGAEKLENDI"` -> `PENDING` — `SENT`/`ACCEPTED` DEĞİL ve bu bilinçli: GİB'e raporlanmamış bir belgeyi "kabul edildi" göstermek bu dosyadaki en ağır sessiz yanlış olurdu, (2) `IZIBIZ_OP_PDF_EARCHIVE` -> `GetEArchiveInvoiceList` ve eski sabit `IZIBIZ_OP_EARCHIVE_UBL` adıyla KORUNDU çünkü o operasyonun NE döndürdüğü ölçülmüş bir olgudur (bugün çağrılmıyor), `_decode_pdf` ZIP açıyor ama SIKI — yalnız `%PDF-` ile başlayan girdiyi kabul eder ve ZIP'ten çıkan İLK dosyayı körlemesine döndürmek kullanıcıya `.pdf` adıyla bir UBL XML'i indirtirdi, `EARSIV_WEB_KEY_YOK` ön koşulu KALKTI ve bu bir GEVŞETME DEĞİL DÜZELTMEDİR (kapının gerekçesi "anahtarsız istek boş yanıt üretir" YANLIŞ TEŞHİSTİ ve yeni operasyon anahtara HİÇ ihtiyaç duymuyor — bu, gönderim anındaki `WEB_KEY` yakalaması kaçmış ESKİ faturaların PDF'ini de erişilebilir kılıyor), (3) `NOT_FOUND` hata sınıfı + `EInvoiceNotFoundError` ki bu `EInvoiceError`ın ALT SINIFIDIR ve alt sınıf olması ŞARTTI — var olan her `except EInvoiceError` çalışmayı SÜRDÜRÜYOR, yani yeni tür eklenirken hiçbir çağıran sessizce kırılmadı; `10008` -> `NOT_FOUND` ve mesaj SABİT TABLODAN geliyor ("henüz bulunamadı, daha sonra tekrar deneyin"), YENİDEN GÖNDERMEYİ ÖNERMİYOR çünkü belge sağlayıcıya İNMİŞTİR ve yeniden göndermek KOPYA üretirdi; İPTAL SÖZLEŞMESİ DEĞİŞMEDİ: `FAILED`, `CANCELLED` DEĞİL, uç 409 — entegratör kabul etmediği sürece ERP faturayı iptal ETMİYOR; İPTALİN `10008`İ SÜRÜYOR ve SEBEBİ ÖLÇÜLEMEDİ, bu bir eksiklik olarak DEĞİL bir SINIR olarak kayda geçiyor: aday sebep `STATUS=100` bir belgenin iptal edilebilir sayılmaması ama sandbox imzalayıcısı HİÇBİR belgeyi bitirmiyor (bizim belgelerimiz de İzibiz'in KENDİ test belgeleri de `KUYRUĞA EKLENDİ`de duruyor), `RAPORLANDI` durumundaki satırlar BAŞKA entegratörlerin belgeleridir ve ölçüm uğruna başkasının belgesine YAZMA işlemi uygulanmadı; sağlayıcıya sorulacak YENİ soru §10.4'te yazılı; GERÇEK AĞDA DOĞRULANDI ve bu mock'la değil ÜRETİM KODU YOLUYLA yapıldı: `query_status` -> `PENDING` `gib=100` (önce `UNRESOLVED`), `fetch_pdf` -> 19178 bayt GERÇEK PDF (önce `PDF_YOK`); §9.6'nın açtığı ÜÇÜNCÜ İŞ ("iki `xfail(strict=False)` testin gözden geçirilmesi — artık XPASS olabilirler") KAPANDI: ikisi de XPASS oldu ve xfail işaretleri KALDIRILDI, `test_e2_izibiz_iptal_sandbox.py`deki durum-sorgusu xfail'i de kalktı, iptalinki gerekçesi ÖLÇÜMLE düzeltilerek KALDI (eski gerekçe "İzibiz belgeyi bizim ETTN'imizle anahtarlamıyor" diyordu ve §9.2 onu çürütmüştü); DOKÜMAN: §9 TEK BAYT DEĞİŞMEDEN KORUNDU ve ölçümler §10 olarak EKLENDİ, §10 hangi hükmü geçersiz kıldığını ADIYLA söylüyor (yalnız §9.3'ün PDF hükmü; §9'un H1/H2/H3 çürütmeleri ve iptal hükmü AYNEN GEÇERLİ); kod ve test yorumlarındaki 17 atıf tek tek yeniden yönlendirildi (kör `sed` YAPILMADI çünkü bazı atıflar GERÇEKTEN §9'a gitmeli); test dosyası dilimle uyum için `test_e2b_earsiv_arama.py` -> `test_e2c_earsiv_pdf.py` olarak yeniden adlandırıldı; TESTLER `test_e2c_earsiv_pdf.py` (9 YENİ) + düzeltilen dört dosya (`test_e1_efatura_sertlestirme.py` PDF kapıları yeniden yazıldı ve ZIP-içinde-XML negatif kapısı eklendi, `test_e2_ebelge_yasam_dongusu.py`nin `10008` iddiası E2'nin ÖNGÖRDÜĞÜ yönde kımıldadı, `test_efatura_adapter.py` sınıf kümesi, iki sandbox dosyası) + üç GERÇEK SOAP fixture'ı; ALTI MUTASYON KOŞULDU ve HEPSİ ADIYLA KIRMIZI: `"100"` aliasını silmek, `"100"`u `ACCEPTED` yapmak, `10008`i `VALIDATION` yapmak, `CONTENT_TYPE=PDF`i düşürmek, ZIP'ten ilk üyeyi körlemesine döndürmek, operasyonu `GetEArchiveInvoice`a geri almak; SANDBOX BETİĞİ `backend/sandbox/izibiz_earsiv_arama.py` CI DIŞIDIR (kanonik koşucunun iki glob'unun da dışında) ve ham istek/yanıt çiftlerini `SESSION_ID`/`webValidationKey`/VKN maskelenmiş olarak kaydediyor; `IZIBIZ_EFATURA_SUBMIT_VERIFIED` `False` KALDI — bu tur YALNIZ OKUMA yollarını ölçtü; PİNLER YENİDEN ÖLÇÜLDÜ (aritmetik YAPILMADI) ve HİÇBİRİ KIMILDAMADI: rota sözleşmesi, GET izin envanteri, çekirdek sorgu envanteri, dinamik-SQL parmak izleri, `alt_surec_sql.txt`, `TENANT_TABLES`, alembic başı ve `pins/pg_twins.txt`; kayıt adı `python scripts/durum.py --sonraki 98`
