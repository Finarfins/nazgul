#60 — AYARLAMA VE SAYIM PARTİ FARKINDA (FAZ 1B-C): 1B-A'nın açtığı parti defterine İKİNCİ ve ÜÇÜNCÜ çağıran bağlandı, GÖÇ YOK ve bu bir eksiklik değil ölçüm — `stock_movements.lot_id` göç `20260903_0067`ten beri veritabanında ZATEN VAR, bu dilim yeni sütun getirmiyor VAR OLANI yazılabilir yapıyor; TABAN `711f65d` (`origin/develop`, #61 birleşmiş hâli) ve `git rev-parse origin/develop` HER TURDA YENİDEN KOŞTURULDU çünkü oturum başı anlık görüntüsü bayattır — bu dilim DÖRT AYRI TABAN gördü ve HER BİRİNDE İĞNELER SIFIRDAN ÖLÇÜLDÜ, ARİTMETİKLE DEVRALINMADI: `baadecf` (#56/1B-A, dilimin kurulduğu yer) -> `3fa9453` (#59) -> `77aa5b0` (#63, 1B-B'nin kanonik parti defteri) -> `711f65d` (#61, mobil kimlik akışı); KAYIT NUMARASI DA DÖRT KEZ KIMILDADI ve her kımıldayış BİR ÇAKIŞMANIN KAYDIDIR, bir düzeltme değil: `0102` (#59 aldı) -> `0103` (#62 aldı) -> `0104` (#63 aldı) -> `0105` (#61 aldı) -> `0106`, her adımda `scripts/durum.py --sonraki 60` REBASE SONRASI ağaçta YENİDEN SORULDU; ALT SÜREÇ SQL SAYACININ BİRLEŞİK ÖLÇÜMÜ ADIYLA ANILMALI çünkü ÇAKIŞMA SESSİZ OLABİLİRDİ: 5.4a (#61) ile bu dal İKİSİ DE `116/197 -> 117/198` diyordu ve İKİSİ DE KENDİ TABANINDA DOĞRUYDU (biri `tests/test_54a_mobil_auth.py`yi, öteki `tests/test_1b_c_ayarlama_lot.py`yi ekliyor); aynı sayıyı İKİ AYRI dosya için söyleyen iki dal birleşince sayı SEÇİLMEZ, TOPLANIR — `711f65d` üzerinde yeniden ölçüldü ve `118/199` çıktı; KAYIT NUMARASI DA O YÜZDEN KIMILDADI: #59 birleşirken `0102`yi ALDI (`docs/durum/0102-pr-0059.md`) ve bu kaydın ilk numarası `0102` idi, `scripts/durum.py --sonraki 60` rebase SONRASI ağaçta yeniden sorulup `0103`e alındı — #61 (`feat/54a-mobil-auth`, AÇIK) da `0102`yi tutuyor ve o dalın da aynı şeyi yapması gerekecek; İĞNELERİN TAMAMI REBASE SONRASI YENİDEN ÖLÇÜLDÜ, aritmetikle devralınmadı; ÖLÇÜLEN KUSUR — 1B-A alışın parti AÇMASINI getirdi ama stok defterine yazan İKİ yol daha vardı ve ikisi de partiyi GÖRMÜYORDU (`POST /api/products/{id}/stock` elle düzeltme, `POST /api/warehouses/counts` fiziksel sayım), yani bir operatör partili malı elle düşürdüğünde `products.stock` azalıyor `product_lots.quantity` AZALMIYORDU ve "bu partiden ne kadar çıktı" sorusu SESSİZCE yanlış cevap veriyordu; 0067'NİN `core_schema`YA DOKUNMAMA KARARI TERSİNE ÇEVRİLDİ ve gerekçe VARSAYILMADI ÖLÇÜLDÜ, İKİ AYRI SONUÇLA: (1) 0067'nin ilan ettiği sebep (SAYISAL GÖÇ MUTABAKAT KAPISI) BU SÜTUN İÇİN GEÇERSİZDİR — `iter_numeric_columns()` `metadata`dan TÜRETİLMEZ, elle yazılmış iki sütun ADI sözlüğünden (`MONEY_COLUMNS`, `QUANTITY_COLUMNS`) türer; `lot_id` `Integer`dır, ne para ne miktar ailesindendir ve iki sözlüğün hiçbirinde yoktur, ÖLÇÜM: bildirimden ÖNCE de SONRA da TOPLAM 90 sütun ve `stock_movements` için yalnız `('quantity')`, yani `capture_numeric_snapshot` `lot_id`i GÖRMÜYOR — kapı hem SQLite'ta (`tests/test_1b_c_ayarlama_lot.py::test_lot_id_SAYISAL_MANIFESTOYA_GIRMEZ_olculdu`) hem GERÇEK PostgreSQL üzerinde mutabakatın KENDİSİ koşturularak (`test_1b_c_ayarlama_lot_postgresql.py::test_lot_id_SAYISAL_ANLIK_GORUNTUYE_GIRMIYOR`) çivili ve `test_numeric_migration_postgresql.py` gerçek PG'de YEŞİL koşturuldu; (2) AMA GERÇEK BİR BEDEL VARDI VE KİMSE ONU ÖLÇMEMİŞTİ — 0067 `lot_id`i `if "lot_id" not in _sutunlar(...)` koşuluyla ekliyordu ve BİLEŞİK YABANCI ANAHTARI `fk_stock_movements_lot_same_company` `(company_id, lot_id) -> product_lots(company_id, id)` AYNI koşulun İÇİNDE kuruyordu, yani sütun `core_schema`da bildirilince TAZE veritabanında onu artık `20260712_0000`ın `create_all`ı açıyor, koşul YANLIŞ oluyor ve FK HİÇ KURULMUYORDU; ÖLÇÜM (taze SQLite + `alembic upgrade head`, üç tur): bildirimden ÖNCE `stock_movements` üzerinde 1 yabancı anahtar, bildirim + TEK koşul ile 0, koşul İKİYE AYRILDIKTAN sonra yine 1 — sessiz kayıp gerçekti ve hiçbir kırmızı onu söylemezdi çünkü olmayan bir kısıt hiçbir yazmayı reddetmez; ÇARE YENİ GÖÇ DEĞİL, 0067'NİN KOŞULUNU İKİYE AYIRMAKTIR (sütun ayrı sorulur, kısıt ayrı) ve bu, 0067'yi ZATEN GEÇMİŞ veritabanlarında HİÇBİR ŞEY yapmaz (ikisi de vardır, blok atlanır) yalnız taze veritabanında eksik olanı kurar; kısıtın VAR olması ile GERÇEKTEN REDDETMESİ aynı cümle DEĞİLDİR ve ikincisi SQLite'ta SORULAMAZ (temiz şemada `PRAGMA foreign_keys` **0** döner), bu yüzden PG ikizi çapraz kiracı ve var-olmayan parti referanslarının `IntegrityError` ile reddedildiğini ADIYLA ölçüyor; YAZICI SINIRI TAŞINDI GEVŞEMEDİ — `_parti_ac`/`_parti_geri_al` `app/routers/transactions.py`den `app/parti_defteri.py`ye alındı ve 1B-A'nın "yazıcı YALNIZ transactions.py" kapısı EMEKLİ EDİLİP yerine DAHA DAR olanı geldi ("yazıcı YALNIZ `app/parti_defteri.py`" + ÇAĞIRAN KÜMESİ KAPALI), alternatif olan kopyalama REDDEDİLDİ çünkü üç kopya üç ayrı SKT çatışma kuralı ve üç ayrı "eksiye düşmez" eşiği demekti — `app/parti.py`nin başlığında ADIYLA reddedilen kusurun ta kendisi; yeni kapı daha dardır çünkü eski yazıcı bir YÖNLENDİRİCİYDİ ve içine dördüncü bir yazma yolu eklemek kapıyı KIRMIYORDU, yenisinde kırar; ÇAĞIRAN KÜMESİ 1B-B (`feat/1b-b-satis-fefo`, satış/FEFO tüketimi) İLE ORTAK SAHİP KARARIDIR ve `app/workflow.py`yi de İÇERİR — küme İKİ dilimin toplamıdır ve ikinci birleşen onu GENİŞLETMEK zorunda kalmaz, çünkü genişletme anı kapının en kolay sessizce kırıldığı andır; kümenin boş bir izin listesine dönüşmemesi ayrıca `test_CAGIRAN_KUMESI_BOS_DEGIL` ile ölçülüyor (bu dilimin getirdiği ÜÇ çağıranın gerçekten orada olduğu soruluyor) ve `fefo_sec` kapısı da aynı sebeple buraya kondu ("seçiciye YALNIZ parti defteri bağlanabilir", bugün ölçüm SIFIR referanstır ve 1B-A'nın `test_fefo_sec_HALA_CAGIRANSIZ`ı o sıfırı ayrıca çiviliyor); "BEYAN ETMEDİM" İLE "SKT'Sİ YOK" AYRILDI ve ayrımı bir `bool` bayrağı DEĞİL `SKT_SORULMADI` SENTİNELİ taşıyor — sebep ölçülebilir: sayım yolunun SKT ALANI YOKTUR ve ona `expiry_date=None` geçmek "bu partinin SKT'si YOKTUR" BEYANI olurdu, yani SKT'si OLAN her partinin sayımı 422 `LOT_SKT_CELISKI` ile ölürdü ve defteri düzeltmek için var olan yol defterin KENDİSİ yüzünden kapanırdı; bayrak yerine sentinel seçildi çünkü bayrak çağıranın hangi anlamı kastettiğini ÇAĞRI YERİNDE görünmez kılar, sentinel ise argümanı YAZMAMAYI anlamın kendisi yapar ve `tests/test_1b_c_ayarlama_lot.py::test_SAYIM_SKT_BEYAN_ETMEZ_argumani_HIC_yazmaz` sayım çağrısının anahtar kümesini AST ile çiviliyor; AYARLAMA UCU (`POST /api/products/{id}/stock`) isteğe bağlı `lot_code` (+ `expiry_date`) alıyor ve KARARI `diff`in İŞARETİ VERİYOR `payload.mode` DEĞİL — `mode='set'` her iki yönde de fark üretir ve kararı `mode`a bağlamak sayılan bir AZALMAYI partiye EKLERDİ (kapı: `test_ayarlama_ISARETE_bakar_mode_a_DEGIL`); artı/sıfır fark `parti_ac`, eksi fark `parti_dus`, var olmayan partiden düşme SESSİZ AÇILIŞ DEĞİL 409 `LOT_MIKTARI_EKSIYE_DUSER`; SIFIR FARK DA PARTİYİ AÇAR çünkü operatör bir parti kodu YAZDI ve "saydım, doğruydu" ile "hiç bakmadım" aynı şey değildir, `quantity + 0` defterde hiçbir sayıyı kımıldatmaz ama hareket artık hangi partinin sayıldığını SÖYLER; SAYIM UCU (`POST /api/warehouses/counts`) satır başına isteğe bağlı `lot_code` alıyor ve PARTİLİ SATIRIN "SİSTEMDE NE VAR"I DEPO STOĞU DEĞİL PARTİNİN KENDİ MİKTARIDIR — depo stoğundan okunsaydı aynı ürünün ikinci partisi sayıldığında fark BİRİNCİ partinin miktarını da içerir ve düzeltme İKİ KEZ uygulanırdı, üstelik bir partiyi sıfıra saymak o üründeki BÜTÜN stoğu sıfırlardı; tekillik anahtarı ÜRÜN'den (ÜRÜN, PARTİ)'ye GENİŞLETİLDİ kaldırılmadı (aynı çiftin iki satırı iki MUTLAK gözlem üretir ve ikincisi birincinin farkını GÖRMEZ) ve bir ürünün AYNI belgede hem partili hem partisiz sayılması 422 ile REDDEDİLDİ çünkü partisiz satır MUTLAK partili satır PARÇA söyler, ikisi aynı belgede olsaydı toplam partisiz satırın SÖYLEDİĞİ sayı OLMAZDI ve hangisinin kazandığı SATIR SIRASINA bağlı olurdu; "eksiye düşmez, 409 atar" kuralı `parti_dus`ta TEK YERE toplandı (1B-A'da `_parti_geri_al`ın içine gömülüydü, üç çağıranla üç kopyaya bölünürdü) ve çare cümlesi çağırandan gelir kuralın kendisi ve hata KODU gelmez; `parti_geri_al` ayrıca NEGATİF miktarlı hareketi DÜŞMEZ GERİ EKLER, çünkü ayarlama/sayım yolları eksi miktarlı satır yazıyor ve onu "düşmek" defteri ŞİŞİRİRDİ (alış yolunda böyle satır YOKTU, bu dal 1B-C'nindir); TOPLU STOK (`/bulk-stock`) VE EXCEL İÇE AKTARMA BU TURDA ÖLÇÜLMEDİ ve partiyi GÖRMÜYOR — kapsam kararıdır, kusur değil, ve `stock_movements.lot_id` orada NULL kalıyor ("bu hareket bir partiden çıkmadı" cümlesi o yollar için bugün DOĞRU); İĞNELER, REBASE SONRASI DEĞİL BU TABANDA ÖLÇÜLDÜ: DİNAMİK SQL SAYAÇLARININ HİÇBİRİ KIMILDAMADI (products.py 6, warehouse_counts.py 1, transactions.py 18, core_schema.py 3, TOPLAM 262'de SABİT) ve dördünün de PARMAK İZİ yenilendi (core_schema `866f92a5->f764dc37`, transactions `d6f8a68a->c0975ff9`, products `a9d91b79->c2047fcc`, warehouse_counts `2d21e6f9->a6f2a86f`) — transactions.py'ye bu turda SQL EKLENMEDİ TAM TERSİ ÇIKTI (altı sabit metin parti defterine taşındı, geriye yalnız ad değişikliği kaldı), products.py'de değişen tek SQL hareket INSERT'ünün SABİT metnine `lot_id` sütunu ve `:lot` BAĞLI parametresi eklenmesidir, warehouse_counts.py'ye HAM SQL HİÇ EKLENMEDİ (Core `insert(stock_movements).values(..., lot_id=...)`) ve `app/parti_defteri.py` dinamik yüzeye HİÇ GİRMEZ çünkü altı metninin hepsi SABİTTİR ve kök yüklemleri AÇIK `company_id=:cid` taşır; PG TEST POPÜLASYONU 106 -> 107 ve ÜÇ PİN BİRLİKTE hareket etti (`test_pg_test_population_exact_107` ADI, `assert len(all_files) == 107` ve `BEKLENEN_PG_DOSYA_SAYISI=107` hem `ci.yml`de hem onu arayan kapıda); KONTRAT ZİNCİRİ yeniden üretildi (`export_openapi.py` + `types:gen`) ve fark YALNIZ EKLEMELİDİR — `StockAdjust.lot_code/expiry_date` ve `InventoryCountItem.lot_code`, üçü de isteğe bağlı, `pos-api.json` fikstürü KIMILDAMADI; YOL ENVANTERLERİ KIMILDAMADI çünkü bu dilim TEK BİR YENİ UÇ AÇMIYOR, var olan iki ucun gövdesini genişletiyor; EMEKLİ EDİLEN KAPILAR ve yerine geleni: `test_product_lots_YAZICISI_YALNIZ_transactions_py` -> `test_product_lots_YAZICISI_YALNIZ_parti_defteri` + `test_parti_defteri_CAGIRANLARI_KAPALI_KUME` (taşımanın kendisi `test_YAZICI_KAPISI_1B_C_YE_TASINDI_burada_YOK` ile ölçülüyor, ikisi birden silinirse kırmızı olur), `test_parti_geri_alma_HAREKET_SILINMEDEN_ONCE_cagriliyor` YERİNDE KALDI ama beklenen parça sayısı 4 -> 3 düştü çünkü TANIM bu dosyadan çıktı ÇAĞRILAR kaldı.
