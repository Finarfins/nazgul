#60 — AYARLAMA VE SAYIM PARTİ FARKINDA (FAZ 1B-C): elle stok düzeltmesi ve fiziksel sayım parti defterine BAĞLANDI, GÖÇ YOK — `stock_movements.lot_id` göç `20260903_0067`ten beri veritabanında ZATEN VAR ve bu dilim yeni sütun getirmiyor, VAR OLANI yazılabilir yapıyor; TABAN `711f65d` (`origin/develop`, #61 birleşmiş hâli) ve `git rev-parse origin/develop` HER TURDA YENİDEN KOŞTURULDU çünkü oturum başı anlık görüntüsü bayattır — bu dilim DÖRT AYRI TABAN gördü ve İĞNELER HER BİRİNDE SIFIRDAN ÖLÇÜLDÜ, ARİTMETİKLE DEVRALINMADI: `baadecf` (#56/1B-A, dilimin kurulduğu yer) -> `3fa9453` (#59) -> `77aa5b0` (#63, 1B-B'nin KANONİK parti defteri) -> `711f65d` (#61, mobil kimlik akışı); KAYIT NUMARASI DA DÖRT KEZ KIMILDADI ve her kımıldayış BİR ÇAKIŞMANIN KAYDIDIR, bir düzeltme değil — `0102` (#59 aldı) -> `0103` (#62 aldı) -> `0104` (#63 aldı) -> `0105` (#61 aldı) -> `0106`, her adımda `scripts/durum.py --sonraki 60` rebase SONRASI ağaçta YENİDEN soruldu; ÖLÇÜLEN KUSUR — 1B-A alışın parti AÇMASINI getirdi ama stok defterine yazan İKİ yol daha vardı ve ikisi de partiyi GÖRMÜYORDU (`POST /api/products/{id}/stock` elle düzeltme, `POST /api/warehouses/counts` fiziksel sayım), yani bir operatör partili malı elle düşürdüğünde `products.stock` azalıyor `product_lots.quantity` AZALMIYORDU ve "bu partiden ne kadar çıktı" sorusu SESSİZCE yanlış cevap veriyordu; 0067'NİN `core_schema`YA DOKUNMAMA KARARI TERSİNE ÇEVRİLDİ ve gerekçe VARSAYILMADI ÖLÇÜLDÜ, İKİ AYRI SONUÇLA: (1) 0067'nin İLAN ETTİĞİ sebep (SAYISAL GÖÇ MUTABAKAT KAPISI) BU SÜTUN İÇİN GEÇERSİZDİR — `iter_numeric_columns()` `metadata`yı HİÇ OKUMAZ, elle yazılmış iki sütun ADI sözlüğünden (`MONEY_COLUMNS`, `QUANTITY_COLUMNS`) türer; `lot_id` `Integer`dır, ne para ne miktar ailesindendir ve ikisinde de YOKTUR — ÖLÇÜM: bildirimden ÖNCE de SONRA da TOPLAM 90 sütun ve `stock_movements` için yalnız `('quantity')`, yani `capture_numeric_snapshot` onu GÖRMÜYOR; kapı hem SQLite'ta manifestonun İÇİ okunarak (`tests/test_1b_c_ayarlama_lot.py::test_lot_id_SAYISAL_MANIFESTOYA_GIRMEZ_olculdu`) hem GERÇEK PostgreSQL'de mutabakatın KENDİSİ koşturularak (`test_1b_c_ayarlama_lot_postgresql.py`) çivili ve `test_numeric_migration_postgresql.py` gerçek PG 16'da YEŞİL koşturuldu; (2) AMA GERÇEK BİR BEDEL VARDI VE KİMSE ONU ÖLÇMEMİŞTİ — 0067 `lot_id`i `if "lot_id" not in _sutunlar(...)` koşuluyla ekliyordu ve BİLEŞİK YABANCI ANAHTARI `fk_stock_movements_lot_same_company` `(company_id, lot_id) -> product_lots(company_id, id)` AYNI koşulun İÇİNDE kuruyordu, yani sütun `core_schema`da bildirilince TAZE veritabanında onu artık `20260712_0000`ın `create_all`ı açıyor, koşul YANLIŞ oluyor ve FK HİÇ KURULMUYORDU; ÖLÇÜM (taze SQLite + `alembic upgrade head`, ÜÇ TUR): bildirimden ÖNCE `stock_movements` üzerinde 1 yabancı anahtar, bildirim + TEK koşul ile 0, koşul İKİYE AYRILDIKTAN sonra yine 1 — kayıp SESSİZDİ ve hiçbir kırmızı onu söylemezdi çünkü OLMAYAN BİR KISIT HİÇBİR YAZMAYI REDDETMEZ; ÇARE YENİ GÖÇ DEĞİL, 0067'NİN KOŞULUNU İKİYE AYIRMAKTIR (sütun ayrı sorulur, kısıt ayrı) ve bu, 0067'yi ZATEN GEÇMİŞ veritabanlarında HİÇBİR ŞEY yapmaz (ikisi de vardır, blok atlanır), yalnız taze veritabanında eksik olanı kurar; "kısıt duruyor" ile "kısıt GERÇEKTEN REDDEDİYOR" aynı cümle DEĞİLDİR ve ikincisi SQLite'ta SORULAMAZ (temiz şemada `PRAGMA foreign_keys` **0** döner), bu yüzden PG ikizi çapraz kiracı ve var-olmayan parti referanslarının `IntegrityError` ile reddedildiğini ADIYLA ölçüyor; YAZICI: BU DİLİM `app/parti_defteri.py`Yİ KURMADI, ONA BAĞLANDI — modülü ve alt çizgili adlandırmayı (`_parti_ac`, `_parti_geri_al`, `_parti_tuket`) #63 (1B-B, satış/FEFO) KANONİK hâle getirdi, bu dal `77aa5b0` üzerine rebase edilirken kendi yolunu O EVE taşıdı ve yazıcı kapıları da orada, `tests/test_1b_a_alis_lot.py` içinde TEK EVDE toplandı (`test_product_lots_YAZICISI_YALNIZ_parti_defteri_py`, `test_parti_defterini_CAGIRANLAR_kapali_kume`, `test_fefo_sec_CAGIRANI_YALNIZ_parti_defteri_py`); bu dilimin deftere KATTIĞI İKİ FONKSİYON `_parti_bul` (kimlik + eldeki miktar, TEK okumada — ikisi ayrı çağrılsaydı iki okuma arasında satır değişebilirdi) ve `_parti_dus`tur ("eksiye düşmez, 409 `LOT_MIKTARI_EKSIYE_DUSER`" kuralının TEK YERİ; çare cümlesi ÇAĞIRANDAN gelir, kuralın kendisi ve hata KODU gelmez); GERİ ALMANIN YÖNÜ BU DİLİMİN İŞİ DEĞİLDİR ve kayda GEÇMESİ ONU 1B-C'ye MAL ETMEK İÇİN DEĞİL, SINIRI SÖYLEMEK İÇİNDİR: `_parti_geri_al` yönü hareketin İŞARETİNDEN çözüyor (TEK `quantity=quantity-:miktar`; satışta `dusecek` negatiftir ve çıkarma eklemeye döner) ve bu 1B-B'nin ÖLÇÜMÜDÜR, `test_parti_geri_alma_YONU_HAREKETIN_ISARETINDEN_gelir` ile çivilidir — bu dalın bir zamanlar taşıdığı AÇIK `if miktar < 0` dalı `77aa5b0` uyumunda DÜŞTÜ ve düşmesi DOĞRUDUR çünkü aynı kararı İKİNCİ bir yerde yeniden türetirdi; bu dilimin yazdığı eksi miktarlı hareketlerin geri alma helper'ına HİÇ GİRMEDİĞİ de ÖLÇÜLDÜ, varsayılmadı: helper YALNIZ `config["head"]` (alış/satış belgeleri) ile çağrılıyor, oysa bu iki yol `reference_type='manual'` ve `'inventory_count'` yazıyor; BU TURDA `app/routers/transactions.py` DEĞİŞMEDİ — dosya bu dalın farkında HİÇ YOK ve dinamik SQL parmak izi `b1547e6c` develop ile BİREBİR AYNI; "BEYAN ETMEDİM" İLE "SKT'Sİ YOK" AYRILDI ve ayrımı bir `bool` bayrağı DEĞİL `SKT_SORULMADI` SENTİNELİ taşıyor — sebep ölçülebilir: sayım yolunun SKT ALANI YOKTUR ve ona `expiry_date=None` geçmek "bu partinin SKT'si YOKTUR" BEYANI olurdu, yani SKT'si OLAN her partinin sayımı 422 `LOT_SKT_CELISKI` ile ölürdü ve defteri düzeltmek için var olan yol defterin KENDİSİ yüzünden kapanırdı; bayrak yerine sentinel seçildi çünkü bayrak çağıranın hangi anlamı kastettiğini ÇAĞRI YERİNDE görünmez kılar, sentinel ise argümanı YAZMAMAYI anlamın kendisi yapar (`test_SAYIM_SKT_BEYAN_ETMEZ_argumani_HIC_yazmaz` sayım çağrısının anahtar kümesini AST ile çiviliyor); AYNI AYRIMIN İKİNCİ YARISI SONRADAN ÖLÇÜLDÜ VE KUSURLUYDU: `StockAdjust.expiry_date` BOŞ DİZGİ (`""`, `"   "`) geldiğinde alan doğrulayıcısı değeri `None`a çeviriyor ama ANAHTAR `model_fields_set` içinde KALIYORDU, yani `products.py` "beyan var" diyor ve beyanın İÇERİĞİ «SKT'siz» oluyordu — BOŞ BIRAKILMIŞ BİR FORM ALANI, TARİHLİ bir partiye eklemeyi 422 ile ÖLDÜRÜYORDU; tarayıcıların boş alanı `""` göndermesi bu dosyada `TransactionItem.validate_lot_code`ta ZATEN ADIYLA sayılıydı ve ders SKT için UYGULANMAMIŞTI; ÇARE ANAHTARI DÜŞÜRMEKTİR (`StockAdjust.bos_skt_BEYAN_DEGILDIR`, `mode='before'`), değeri değiştirmek ya da `products.py`ye ikinci bir boşluk denetimi koymak DEĞİL — ikincisi "beyan yok" bilgisini İKİ YERDE birden tutar ve ikisi ayrışabilirdi; anahtar düşünce `SKT_SORULMADI` DOĞAL OLARAK devreye girer ve `products.py` DEĞİŞMEZ, çarenin bu ŞEKLİ `test_BOS_SKT_ALANI_ANAHTARI_DUSURULUR_deger_degil` ile ayrıca çivili; AÇIK `null` DOKUNULMADI ve bu KARARDIR: bir istemcinin `"expiry_date": null` yazması BİR BEYANDIR ve tarihli partiyle çelişmesi DOĞRUDUR, boş dizgi ise bir insanın boş bıraktığı alandır — ikisi aynı kefeye konsaydı çelişki kapısı HERKESE açılırdı; 1B-A'nın `TransactionItem`i BU TURDA DEĞİŞTİRİLMEDİ (alış yolunun boş-SKT davranışı ÖLÇÜLMEDİ); AYARLAMA UCU isteğe bağlı `lot_code` (+ `expiry_date`) alıyor ve KARARI `diff`in İŞARETİ VERİYOR `payload.mode` DEĞİL — `mode='set'` her iki yönde de fark üretir ve kararı `mode`a bağlamak sayılan bir AZALMAYI partiye EKLERDİ (`test_ayarlama_ISARETE_bakar_mode_a_DEGIL`); artı/sıfır fark `_parti_ac`, eksi fark `_parti_dus`, var olmayan partiden düşme SESSİZ AÇILIŞ DEĞİL 409; SIFIR FARK DA PARTİYİ AÇAR çünkü "saydım, doğruydu" ile "hiç bakmadım" aynı şey değildir ve `quantity + 0` defterde hiçbir sayıyı kımıldatmaz ama hareket artık hangi partinin sayıldığını SÖYLER; `lot_code` YOKSA defter HİÇ görülmez ve `lot_id` NULL kalır (1B-C öncesi davranışın BİREBİR aynısı); SAYIM UCU satır başına isteğe bağlı `lot_code` alıyor ve PARTİLİ SATIRIN "SİSTEMDE NE VAR"I DEPO STOĞU DEĞİL PARTİNİN KENDİ MİKTARIDIR — depo stoğundan okunsaydı aynı ürünün ikinci partisi sayıldığında fark BİRİNCİ partinin miktarını da içerir ve düzeltme İKİ KEZ uygulanırdı, üstelik bir partiyi sıfıra saymak o üründeki BÜTÜN stoğu sıfırlardı; tekillik anahtarı ÜRÜN'den (ÜRÜN, PARTİ)'ye GENİŞLETİLDİ kaldırılmadı (aynı çiftin iki satırı iki MUTLAK gözlem üretir ve ikincisi birincinin farkını GÖRMEZ) ve bir ürünün AYNI belgede hem partili hem partisiz sayılması 422 ile REDDEDİLDİ çünkü partisiz satır MUTLAK partili satır PARÇA söyler, ikisi bir arada olsaydı toplam partisiz satırın SÖYLEDİĞİ sayı OLMAZDI ve hangisinin kazandığı SATIR SIRASINA bağlı olurdu; TOPLU STOK (`/bulk-stock`) VE EXCEL İÇE AKTARMA BU TURDA ÖLÇÜLMEDİ ve partiyi GÖRMÜYOR — kapsam kararıdır, kusur değil, ve `stock_movements.lot_id` orada NULL kalıyor ("bu hareket bir partiden çıkmadı" cümlesi o yollar için bugün DOĞRU); İĞNELER `711f65d` ÜZERİNDE ÖLÇÜLDÜ: DİNAMİK SQL SAYAÇLARININ HİÇBİRİ KIMILDAMADI (`products.py` 6, `warehouse_counts.py` 1, `core_schema.py` 3, `transactions.py` 18, TOPLAM `266`da SABİT) ve DEĞİŞEN ÜÇ DOSYANIN parmak izi yenilendi — `core_schema.py` `866f92a5->f764dc37` (TEK Core sütun bildirimi, SQL metni kımıldamadı), `products.py` `a9d91b79->ac80411a` (hareket INSERT'ünün SABİT metnine `lot_id` sütunu + `:lot` BAĞLI parametresi; `_ayarlama_partisi` KENDİ SQL'İNİ YAZMAZ, deftere yalnız `parti_defteri` üzerinden dokunur), `warehouse_counts.py` `2d21e6f9->72f09136` (HAM SQL HİÇ EKLENMEDİ, Core `insert(stock_movements).values(..., lot_id=...)`); ALT SÜREÇ SQL YÜZEYİ `117/198 -> 118/199` ve BU SAYININ HİKÂYESİ ADIYLA ANILMALI çünkü ÇAKIŞMA SESSİZ OLABİLİRDİ — 5.4a (#61) ile bu dal İKİSİ DE `116/197 -> 117/198` diyordu ve İKİSİ DE KENDİ TABANINDA DOĞRUYDU (biri `tests/test_54a_mobil_auth.py`yi, öteki `tests/test_1b_c_ayarlama_lot.py`yi ekliyor); aynı sayıyı İKİ AYRI dosya için söyleyen iki dal birleşince sayı SEÇİLMEZ TOPLANIR, ve birleşmede iki düzyazıdan birini ötekinin yerine koymak sayıyı doğru bırakıp GEREKÇEYİ yalan yapardı — iki gerekçe de KORUNDU ve sayı `711f65d` üzerinde YENİDEN ÖLÇÜLDÜ; PG TEST POPÜLASYONU `108 -> 109` ve ÜÇ PİN BİRLİKTE hareket etti (`test_pg_test_population_exact_109` ADI, `assert len(all_files) == 109` ve `BEKLENEN_PG_DOSYA_SAYISI=109` hem `ci.yml`de hem onu arayan kapıda); KONTRAT ZİNCİRİ yeniden üretildi ve fark YALNIZ EKLEMELİDİR — `StockAdjust.lot_code`, `StockAdjust.expiry_date` ve `InventoryCountItem.lot_code`, üçü de isteğe bağlı, `pos-api.json` fikstürü KIMILDAMADI; ROTA/İZİN/ÇEKİRDEK ENVANTERLERİ KIMILDAMADI çünkü bu dilim TEK BİR YENİ UÇ AÇMIYOR, var olan iki ucun GÖVDESİNİ genişletiyor.
