#21 — VADELER SUNUCUNUN SAATİNDEN: `test_v3_harman_vadeli` ve PostgreSQL ikizindeki SON sabit ileri tarihler kaldırıldı, dondurma aleti depoya kondu. TABAN `9c8cb87` (develop; ölçüldü, sığ olmayan klon) ve BU TABAN YEŞİLDİR — bu PR bir kırmızıyı KAPATMAZ. Bu kaydın önceki sürümleri "develop'ta kırmızıydı, bu PR onu kapatıyor" diyor ve değişikliği `2026-08-01`/`2026-09-01` sabitlerinin kaldırılması olarak anlatıyordu; TABANA KARŞI YANLIŞTI. GEÇMİŞ (ölçüldü): FIFO çiftinin vadeleri `2026-08-01`/`2026-09-01` sabitiyken smoke İstanbul 2026-09-02'de kırmızıya döndü (`status=VADESI_GECTI` süzgeci iki kimlik, ödeme sonrası `total_overdue` 800); temiz develop ağaçları aa3d577 ve cfe7362 donmuş 2026-09-01 YEŞİL, 2026-09-02 KIRMIZI. AYNI GÜN Cursor Agent'ın 7443163 kaydı (PR #19, d156d13) develop'ta çifti `2099-01-01`/`2099-06-01` SABİTLERİNE taşıdı; bu dalın ilk birleşmesi 03e9b2c o düzeltmeyi sessizce ezdi ve önceki kayıtlar bunu söylemedi. BU PR'IN TABANA KARŞI FARKI `2099-01-01`/`2099-06-01` çiftini ve `open_future` çapasındaki `2099-12-31`i kaldırmaktır. Neden: sabit ileri tarih fitili uzun bir bombadır — ölçüldü, 9c8cb87 ağacı donmuş 2099-06-01 YEŞİL, 2099-06-02 SQLite ve PostgreSQL 16'da KIRMIZI (`only_overdue` iddiası). ÇARE: üç ileri vade de aynı süreçteki `business_today()`ten türer — `FIFO_ERKEN_VADE` +365, `FIFO_GEC_VADE` +730, `ACIK_UZAK_VADE` +1095 gün; test ile sunucu aynı fonksiyonu okur, sıra (365 < 730 < 1095) aritmetiktir. ÖNCEKİ SÜRÜMÜN (006b44d) ÖLÇÜLMEMİŞ KUSURU: çapa `2099-12-31` sabitinde kalıyordu ve `today+730` ona 2097-12-31'de yetişip `desc_ids[0] == open_id`u düşürüyordu — yani tabandaki 2099-06-01'lik ömrü 2097-12-30'a KISALTIYORDU ve bunu tabana karşı söylemiyordu. Ölçüldü (bu ağaçta çapa sabite geri konarak, iki motor): 2097-12-30 YEŞİL, 2097-12-31 KIRMIZI (smoke satır 159); çapa türetilince aynı günler YEŞİL. TAVAN BULUNAMADI (ölçüldü, `donmus_saat` ile): donmuş 2026-07-01, 2028-01-15, 2050-01-01, 2097-12-30, 2097-12-31, 2098-01-01, 2099-06-02, 2100-01-01, 2150-01-01, 2200-01-01, 2500-01-01, 3000-01-01, 5000-01-01, 9000-01-01, 9990-01-01 SQLite YEŞİL; PostgreSQL 16'da 2026-07-01, 2028-01-15, 2097-12-31, 2099-06-02, 2100-01-01, 2200-01-01, 3000-01-01, 9000-01-01, 9990-01-01 YEŞİL. Taranan aralık budur, ötesi iddia edilmez. TABAN VAR (ölçüldü): donmuş 2026-01-31'de `payment_date='2026-02-01'` tahsilatı uygulanmaz ve `first == 'ODENDI'` (smoke satır 125) düşer; saat tabandan yalnız uzaklaşır, geçmiş sabitler (2019-06-30, 2026-01-05, 2026-02-01) bilerek değiştirilmedi. KIRAN SABİT YALNIZ `second`DI (ölçüldü bu ağaçta, iki motor): `first='2026-08-01'` sabit + `second` göreli YEŞİL/YEŞİL; çiftin ikisi eski sabitte İKİSİ DE KIRMIZI smoke satır 152 (`only_overdue`); vadeler takas İKİSİ DE KIRMIZI smoke satır 125 (`first == 'ODENDI'`). Gerekçe kodda: `_receivable_status` önce `outstanding <= 0` bakıp `ODENDI` döner; FIFO 1200'ü en eski vadeye verdiği için `first` süzgeç okunmadan kapanır. `first`in de göreli olması TERCİHTİR, zorunluluk değil. 365 GÜN, 30 DEĞİL paragrafı AKIL YÜRÜTMEDİR, ÖLÇÜM DEĞİL: gece yarısı sapması bir gündür, iki günden büyük her pay yutar; koşulmadı, hesaplandı. ALET DEPODA: `backend/donmus_saat.py` — `NAZGUL_DONMUS_GUN=YYYY-MM-DD` ile `app.business_time.business_now` dondurulur; `_SMOKE`un İLK satırı çağırır, dolayısıyla SQLite alt sürecinde de PG ikizinin `exec`'inde de aynı alet. İki yönde ölçüldü ve TESTE BAĞLANDI (`test_donmus_saat_ileri_sarilinca_sunucu_rotasi_donmus_gunu_okur`, `test_donmus_saat_yamalayamazsa_kullanici_kodu_kosmadan_97_ile_olur`): PYTHONPATH'i yeniden yazan alt süreçte `transactions.business_today()` donmuş 2097-06-15'i okur; `app` erişilemezken çıkış kodu 97, `DONMUS_SAAT_OLUMCUL` stderr'de, sonraki `print` koşmaz; bozuk değerde de 97. Depoda olmayan bir aletle alınan ölçüm ÖLÇÜM SAYILMAZ; önceki sürümün "çıkış 97" iddiası bu yüzden yeniden alındı. KALDIRILANLAR (ölçülmemişti): "o gece açılan HER PR'ı kırmızıya çevirecekti", "iki ayrı ajan aynı kusuru bağımsız buldu", 53 sabit literal taraması, ve "PR #22'nin sözleşme merceği 33564742779 koşusu" atfı — o koşum bu dalın 34a5469'daki kendi CI koşusudur (iş akışı "CI", başarılı), PR #22'nin merceği değil; kaynak olarak yalnız buradaki ölçüm bırakıldı. DONUK SAYILAR (bu ağaçta yeniden ölçüldü): `EXPECTED_UNDENIABLE` 99, `GET_INVENTORY_COUNT` 166, pg populasyonu 99 (`donmus_saat.py` ve yeni test fonksiyonları `test_*postgresql*.py` globuna girmez), alembic başı `20260902_0065`. `backend/app` altına dokunulmadı. Kayıt develop'un en yükseği 0072 olduğu için 0073'tür; `durum.py --kapi origin/develop`: BAYAT SIRA YOK / YİNELENEN SIRA YOK / GİRDİ VAR.
