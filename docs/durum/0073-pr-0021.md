#21 — ZAMAN BOMBASI: `test_v3_harman_vadeli` ve PostgreSQL ikizi 2026-09-02 İstanbul gününde develop'ta KIRMIZIYA döndü ve düzeltildi. KUSUR: smoke içinde FIFO belgelerinin vadeleri `due='2026-08-01'`/`due='2026-09-01'` SABİT yazılıydı; `_receivable_status` (routers/transactions.py) kararını `due_date < today` ile verir ve `today` `business_today()`ten, yani İSTANBUL takvim gününden gelir — İstanbul 2026-09-02'ye döndüğü (UTC 2026-09-01 21:00) an smoke kırmızıya döndü: `status=VADESI_GECTI` süzgeci bir yerine iki kimlik döndürdü ve ödeme sonrası `total_overdue` 0 yerine 800 çıktı. KUSUR DALIN DEĞİL DEPONUNDU VE BU ÖLÇÜLDÜ: temiz `origin/develop` ağacında (hem aa3d577 hem cfe7362) aynı duvar saatinde AYNEN üretildi; o gece açılan HER PR kendi kusuru olmadan kırmızı görecekti ve iki ayrı ajan (bu oturum ve Cursor) aynı kusuru birbirinden bağımsız buldu. ÇARE: vadeler sunucunun KENDİ saatinden türetiliyor — `FIFO_ERKEN_VADE = business_today() + 365 gün` ve `FIFO_GEC_VADE = business_today() + 730 gün`; test ile sunucu aynı süreçteki AYNI fonksiyonu okuduğu için ayrışamazlar. KAYDIN ÖNCEKİ SÜRÜMÜNDEKİ GEREKÇE YANLIŞTI VE BURADA DÜZELTİLİYOR. Önceki metin "(1) ikisi de AÇIK olmalı ... yalnız birini ileri almak yetmez" diyor ve rebase çözümünü buna dayandırıyordu. PR #22'nin sözleşme merceği 33564742779 numaralı koşuda bunun tutmadığını gösterdi; bu oturum iddiayı 7498ab3 tabanında MUTASYONLA yeniden ölçtü ve mercek DOĞRULANDI: `first`i eski `2026-08-01` SABİTİNDE bırakıp yalnız `second`ı göreli yapan sürüm SQLite'ta da PostgreSQL 16'da da YEŞİL; çiftin ikisi de eski sabitlerindeyken ikisi de `only_overdue` iddiasında KIRMIZI. GEREKÇE KODDA: `_receivable_status` ÖNCE `outstanding <= 0` bakıp `ODENDI` döner, vade karşılaştırmasına ancak AÇIK kalan belge ulaşır; FIFO 1200'lük tahsilatı EN ESKİ vadeye verdiği için `first` süzgeç okunmadan ÖNCE kapanır ve ödenmiş belge hiçbir zaman `VADESI_GECTI` sayılmaz. Yani `first`in vadesi geçmiş olması iki overdue iddiasını da KIRAMAZDI; değişikliği ZORUNLU kılan tek şey `second`ın `VADESI_GECTI`ye düşmesiydi. `first`in de göreli olması bir zorunluluk değil TERCİHTİR: çift tek kaynaktan türeyince sıra (365 < 730) aritmetikle garanti olur, bir sabitin hareketli bir tarihin altında kalmasına güvenmez. Düzeltmenin KENDİSİ değişmedi — yalnız gerekçesi ölçülene getirildi; kendini yanlış anlatan bir kayıt kusurdur. ÜÇ İDDİA: (1) `second` AÇIK kalmalı (`?status=VADESI_GECTI` TEK kimlik döndürmeli, ödeme sonrası `total_overdue` 0) — kısıt YALNIZ `second` üzerindedir; (2) `first < second` KESİN olmalı (FIFO en eski vadeye dağıtır; `first` ÖDENDI, `second` 800 açık); (3) ikisi de `2099-12-31` çapasının altında ve `2019-06-30`un üstünde kalmalı (`desc_ids[0] == open_id`, `desc_ids[-1] == overdue_id`) — alt çapayı ayrıca API koruyor: vade belge tarihinden ÖNCE olamaz (422) ve belge tarihi `2026-01-01`, yani çift 2019 çapasının altına zaten düşemez. 365 gün payı ayrıca koşum İstanbul gece yarısını geçerse oluşacak bir günlük sapmayı yutar. SQLite testi ile PostgreSQL ikizi `_SMOKE` metnini PAYLAŞTIĞI için tek düzeltme ikisini de kapsıyor. BİLİNEN SON KULLANMA TARİHİ — 2097. `FIFO_GEC_VADE = business_today() + 730 gün` 2099-12-31 çapasına 2097-12-31'de YETİŞİR ve `desc_ids[0] == open_id` iddiası o gün düşer. DONMUŞ SAATLE ÖLÇÜLDÜ: 2096-01-01 YEŞİL, 2097-01-01 YEŞİL, 2097-12-30 YEŞİL, 2097-12-31 KIRMIZI, 2098-01-01 KIRMIZI. Yani düzeltme 2097 sonuna kadar geçerlidir; o tarihte 2099-12-31 çapası da ileri alınmalıdır. Bu cümle hem kayıtta hem test dosyasının başlığında duruyor — bilinen ömrü olan bir düzeltme, ömrünü okuyanın bulacağı yerde söylemelidir. MUTASYONLA ÖLÇÜLDÜ, VARSAYILMADI (taban 7498ab3, CPython 3.12.3, PostgreSQL 16.13): eski sabitler geri konduğunda SQLite ve PostgreSQL İKİSİ DE smoke satır 215'te (`only_overdue` iddiası) kırmızı; düzeltme geri konduğunda ikisi de yeşil. Saat DONDURULARAK çapraz ölçüldü (`app.business_time.business_now` yaması): donmuş 2028-01-15'te düzeltilmiş sürüm İKİ MOTORDA DA YEŞİL, sabit tarihli sürüm KIRMIZI — yani düzeltme 'bugün yeşil' değil SAATTEN BAĞIMSIZ. Yamanın alt süreç smoke'una GERÇEKTEN ulaştığı negatif kontrolle kanıtlandı: sabit tarihli sürüm donmuş 2026-07-01'de (dönüm ÖNCESİ) YEŞİL, donmuş 2028-01-15'te (dönüm SONRASI) KIRMIZI. Dondurma yardımcısı YAMALAYAMADIĞINDA GÜRÜLTÜLÜ ÖLÜR (çıkış 97, `app` paketi erişilemezken ayrıca ölçüldü); sessizce geçseydi 'donmuş saatte yeşil' hiçbir şey kanıtlamazdı. FIFO SIRASI iki yönlü ölçüldü: iki vade yer değiştirdiğinde tahsilatın ödediği belge de değişiyor ve iddia SQLite'ta da PostgreSQL'de de smoke satır 188'de (`first ... == 'ODENDI'`) düşüyor — yani iddia gerçekten SIRAYI sınıyor. TARAMA (ölçüldü, DEĞİŞTİRİLMEDİ): backend altındaki (alembic hariç) tüm Python dosyalarında bugünden ileri tarihli 53 sabit literal var; bunları taşıyan 13 dosya donmuş 2028-01-15 saatinde tek tek koşuldu ve TAMAMI YEŞİL geçti — yani kalan literaller 'bugün'e karşı karşılaştırılan vade/son-kullanma değil, tarih ARALIĞI süzgeci, sunucunun ÜRETTİĞİ değerin beklenen karşılığı ya da açıkça enjekte edilen saat (`next_invoice_number(now=datetime(2027,1,1))`). Bu tarama d156d13 tabanında yapıldı ve 7498ab3'te TEKRARLANMADI. TARAMANIN SINIRI: yalnız `business_now` donduruldu; `datetime.now()`/`date.today()`yi DOĞRUDAN okuyan bir yol varsa bu ölçüm onu görmez. Kapsam bilerek dar tutuldu: bu PR yalnız iki vadeyi göreli yapıyor, kantar fişi işini TAŞIMIYOR ve tarla koduna DOKUNMUYOR. REBASE ÖLÇÜMÜ (7498ab3): taban iki merge ilerledi (PR #22 `2745682`, PR #23 `7498ab3`) ve önceki turun BÜTÜN sayıları geçersiz oldu; `git merge origin/develop` ÇAKIŞMASIZ geçti (dalın dokunduğu iki dosyaya develop bu turda dokunmadı) ve dört donuk sayı yeniden ölçülüp KIPIRDAMADIĞI doğrulandı: `EXPECTED_UNDENIABLE` 99, `GET_INVENTORY_COUNT` 166, pg populasyonu 99, alembic başı `20260902_0065`. Kayıt `0070-pr-0021.md`ten önce `0072`ye, sonra `0073-pr-0021.md`ye alındı (`durum.py --sonraki 21`, iki kez): 0070 develop'un PR #22 kaydıyla YİNELENİYORDU, 0071'i PR #23 aldı, 0072'yi de bu turda inen PR #26 (xmldom advisory) aldı. İKİNCİ TABAN HAREKETİ: ölçüm `7498ab3`te yapıldıktan sonra develop `9c8cb87`e ilerledi (PR #26, YALNIZ `frontend/package-lock.json` içinde `@xmldom/xmldom` 0.9.10 -> 0.9.12 ve bir kayıt girdisi). O merge bu dala alındı; ÇAKIŞMA ÇIKMADI ve backend ölçümlerinin HİÇBİRİNE dokunmuyor — `frontend` işini kırmızı tutan GHSA-6gmq-8vp8-gcm6 kapandığı için bu PR'ın tabanı artık yeşil. Dört donuk sayı bu tabanda YENİDEN ölçüldü ve yine kıpırdamadı. Üç ölçüm katmanının TAMAMI bu tabanda sıfırdan tekrarlandı.
