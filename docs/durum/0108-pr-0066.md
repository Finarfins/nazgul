#66 — HİJYEN H2 OUTBOX DATABASE_URL SIZINTISI: `tests/test_field_outbox_writer.py` içe aktarmada `setdefault("DATABASE_URL", "sqlite:///./__outbox_gate.db")` ile cwd sqlite açıyordu; aynı süreçte `test_line_item_tenancy_slice1.py` atlamak yerine o URL ile koşuyordu; DÜZELTME: `_private_sqlite_url` (auth-pop #52 kalıbı) — tmp URL, `app.main` içe aktar, `finally`de pop; `backend/app` SIFIR; KANIT: joint pytest → 19 passed / 2 skipped (slice1 yalnızken de 2 skipped), cwd `__outbox_gate.db` YOK; `PYTHONUTF8=1 python run_isolated_tests.py tests/test_field_outbox_writer.py --workers 4` → 19 passed; run id `4a5b9358-65fd-4ab1-bca0-d1c042b6a2f4`; 17 bağlam SABİT; dal `test/h2`; `--sonraki 66`.
