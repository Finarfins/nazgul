#130 — H43 KODLU HATA GÖVDESİ SAYFALARA ULAŞIYOR (yalnız frontend; backend 0 dosya): kök neden `api.ts` `unwrapApiError` (#128) dict `detail`i sayfalara ulaşmadan `message` metniyle DEĞİŞTİRİYORDU → `Login.tsx` `EMAIL_VERIFICATION_REQUIRED` ve `Pos.tsx` `AMBIGUOUS_BARCODE`/`candidate_count` dalları ölüydü (mevcut Pos testi interceptor'ı atlayan ham taklitle yeşildi); düzeltme: dict gövde metne çevrilmeden `err.apiDetail` olarak saklanır, yeni `apiDetail(err)` (önce `apiDetail`, yoksa ham `response.data.detail` nesnesi; metin/dizide undefined), `response.data.detail`/`err.message` davranışı AYNI (`api.test.ts` #128 vakaları değişmeden yeşil); `detail?.code` okuyan başka sayfa yok (ölçüldü: yalnız bu ikisi); TESTLER interceptor'dan geçmiş hata şekliyle: `Login.test.tsx` (yeni, 2) + `Pos.test.tsx` +1 (409 `candidate_count:3`) — düzeltme öncesi 2 KIRMIZI, sonrası yeşil; lint 0 hata, build yeşil, vitest 102 dosya / 757 test: 753 yeşil, 4 düşen `TransactionDialog`(3)+`Transactions`(1) — `Transactions` yeniden koşumda yeşil, `TransactionDialog` 3/3/1/0 değişen küme (`../api` tümüyle mock'lu, değişiklik yüklenmiyor; findByDisplayValue 1 s bütçe dalgalanması, H37)
