#39 — FİRMA PROFİLLERİ: `companies.profiller` (göç `20260904_0068`), YAZAN İKİ YOL BAĞLI, OKUYAN SIFIR; ÇOĞUL AD BİR ŞEMA İDDİASIDIR: alan `profil` değil `profiller`dir çünkü bir firma aynı anda birden fazla profil taşır ve KARMA olması İSTİSNA DEĞİL KURALDIR (sahibin kendi işi: bayi + servis + tarla + sürü), yani tekil bir AYIRICI sütun bu firmayı KAYDEDEMEZDİ — kaydedebilmek için ya bir değeri "asıl" ilan etmek ya da gerisini atmak gerekirdi ve ikisi de ölçülmemiş bir olgu uydurmaktır; KAPALI KÜME `ciftci`, `pazarci`, `tuccar`, `veteriner` ve firma bunların HERHANGİ BİR ALT KÜMESİNİ taşır; DEPOLAMA `TEXT NOT NULL DEFAULT ''`, içeriği ayıklanmış + TEKİLLEŞTİRİLMİŞ + ALFABETİK SIRALI alt kümenin virgülle birleştirilmiş hâli — KANONİK biçim saklanıyor GİRİLDİĞİ GİBİ DEĞİL, çünkü `"tuccar,ciftci"` ile `"ciftci,tuccar"` AYNI olgudur ve ikisini de saklamak eşitliği SORULAMAZ yapardı; sıralama ALFABETİKTİR ve küme yineleme sırasına BIRAKILMADI (PYTHONHASHSEED'e bağlı olurdu, aynı girdi farklı satırlar yazdırırdı — `test_SIRALAMA_kume_yineleme_sirasina_BIRAKILMAMIS` çiviliyor); `''` "HENÜZ SEÇİLMEDİ"DİR VE BİR OLGU UYDURMAZ: 0066'nın `base_unit` için REDDETTİĞİ şey (`base_unit = unit` kopyalamak) burada YAPILMIYOR, mevcut satırlara profil ATANMIYOR ve boş küme gerçekten de bugün her firmanın durumudur zira sütun bu göçten önce YOKTU; NULL YERİNE `''` seçildi çünkü NULL üç değerli mantığı her çağırana taşırdı ve bu kümede "bilinmiyor" ile "boş" ARASINDA fark YOKTUR, ayrım bir gün gerekirse İKİNCİ bir sütun ister ve o sütunu bugün kimse dolduramaz; JSONB DEĞİL TEXT — SQLite<->PostgreSQL 16 eşliği, 0016'nın `einvoice_payload` gerekçesinin AYNISI; `String(N)` DEĞİL `Text` çünkü dört üyenin 16 alt kümesi var ve kanonik uzunluk üye adlarına bağlı, sabit bir üst sınır beşinci üye eklendiği gün SESSİZ kırpma üretirdi; KISIT ŞEMADA DEĞİL UÇTA ve bedeli ADIYLA konuyor: CSV alt kümesini `CHECK` ile doğrulamak diyalekte bağlı dizgi işlemleri ister (`regexp_split_to_array` PostgreSQL'de VAR SQLite'ta YOK) ve iki motorda iki farklı kısıt yazmak birinin ötekini yalanladığı günü GÖRÜNMEZ yapardı — 0067'nin `NaN` kısıtı diyalekte bağlıydı çünkü orada denenen TEK BİR SAYIYDI, burada denenen bir DİLBİLGİSİDİR; doğrudan SQL ile yazan biri geçersiz belirteç sokabilir ve şema DURDURMAZ, bugün böyle bir yazan YOKTUR (iki yol da `profilleri_coz`dan geçer); KATLAMA YOKTUR VE BU BİLİNÇLİ: `"CIFTCI"` REDDEDİLİR, çünkü bu depoda Türkçe katlama ÜÇ KATMANDA AYRIŞTI ve maliyeti ölçüldü — `app/units.py`in `turkce_katla`sı YUKARI katlar, `app/routers/farm.py`in `_bitki_katla`sı AŞAĞI katlar ve ikisi AYNI DENKLİĞİ ÜRETMEZ; kapalı ve MAKİNEYE bakan bir kümeye dördüncü kopya eklemek o ayrışmayı büyütürdü, `test_KATLAMA_YOK_buyuk_yazim_REDDEDILIR` bunu beş yazımla çiviliyor ve `ÇİFTÇİ` ayrı duruyor (düz `.lower()` eklense ilk üçü SESSİZCE geçer ama o HÂLÂ geçmez, yani yarım esneklik tutarsız bir uç üretirdi); YAZAN İKİ YOL: `POST /api/auth/register` (isteğe bağlı alan; doğrulama Pydantic katmanında yani geçersiz profil FİRMA SATIRI AÇILMADAN reddedilir ve yarım kiracı kalmaz) ve `GET/PUT /api/company-settings` (`CompanyPolicyUpdate` genişletildi, `model_fields_set` kalıbı kardeşlerinin aynısı); `profiller` PUT'ta genel döngünün DIŞINDA ve bu bir HATA DÜZELTMESİDİR, süs değil: döngü `getattr`i olduğu gibi yazıyor ve istemci AÇIKÇA `profiller: null` gönderirse `NOT NULL` sütununa `None` giderdi (500); alan GÖNDERİLMEZSE sütuna DOKUNULMAZ, AÇIKÇA `null` gönderilirse `''` yazılır — MUTASYONLA ÖLÇÜLDÜ: `or ""` kaldırıldığında `test_IKI_YAZAN_YOL_ucdan_uca_sqlite` KIRMIZI oldu, geri konduğunda YEŞİL; KAYIT İDDİASI YANITTA DEĞİL SATIRDA ölçülüyor çünkü `/auth/register` numaralandırma karşıtı bir uçtur ve gövdesi HER durumda aynı 200'dür — yanıta bakan bir test HİÇBİR ŞEY ölçmezdi; OKUYAN YOK VE ÇİVİLENDİ: `\bprofiller\b` `backend/app` altında YALNIZ dört dosyada geçer (`firma_profilleri.py`, `tenancy.py`, `routers/auth.py`, `routers/companies.py`) ve beşinci bir okuyan belirdiği gün `test_APP_ALTINDA_profiller_YALNIZ_DORT_DOSYADA_gecer` KIRMIZI olur ki bu DOĞRUDUR — modül anahtarları geldiğinde bu kapı onları DOĞRULAYAN teste dönüşmelidir (#31'in `app/parti.py` duruşunun aynısı); `\b` sınırı `profilleri_coz`u SAYMAZ, aranan şey SÜTUN ADIDIR; GÖÇ `upgrade -> downgrade -> upgrade` İKİ DİYALEKTTE de koşuldu (SQLite ve gerçek PostgreSQL 16.13, ikisi de temiz) ve sütunun ŞEKLİ ölçüldü: SQLite `('TEXT', notnull=1, "''")`, PostgreSQL `text | not null | ''::text`; ÇİVİLER: alembic başı ÜÇ yerde `20260904_0068`e alındı (`test_goc_zinciri.py` 424/449/511) — `zincir.bas` satırı her yeni göçte BİLEREK kırmızı olur; `tenancy.py` PARMAK İZİ KAYDI VE YENİDEN TÜRETİLDİ (`b50d5fd5...` -> `e81ca586...`), dinamik `text()` sayısı 4'te SABİT ve argümanları develop ile BİREBİR aynı — parmak izi dosya AST'sinin TAMAMINDAN türediği için bir Core SÜTUN BİLDİRİMİ de onu kaydırır (0062'nin `core_schema.py` kaydının aynı gerekçesi), TÜRETME CPython 3.12 ile yapıldı çünkü sabitler 3.12'ye pinlidir ve 3.11'de ALTMIŞ dosya birden kayar (kaynak değişikliği değil YORUMLAYICI imzası); `get_company_settings` sorgu envanteri de kaydı (`165d5888...` -> `5525fded...`) çünkü izdüşüme `companies.c.profiller` eklendi — HEDEF tablo, sorgu ADEDİ ve KİRACI YÜKLEMİ (`companies.c.id == cid`) DEĞİŞMEDİ, değişen tek şey SEÇİLEN SÜTUN LİSTESİDİR; `INVENTORY_FINGERPRINT` o TEK satırla birlikte kaydı; ALT SÜREÇ SQL YÜZEYİ 102->103 dosya ve 165->166 metin oldu, SAYIM ÖLÇÜLDÜ önceki ölçümün ÜZERİNE ARİTMETİK YAPILARAK DEĞİL ve farkın TEK kaynağının yeni test dosyası olduğu AYRICA doğrulandı; `app/` altında SIFIR OLDUĞU YİNE ÖLÇÜLDÜ yani kardeş kapı `test_alt_surecte_sql_uretim_kodunda_yok` HÂLÂ yeşil ve bu dilim ÜRETİM kodunun kör noktasını BÜYÜTMÜYOR; PG İKİZ DOSYASI EKLENMEDİ ve gerekçesi ölçülebilir: bu göçte diyalektin AYRIŞTIĞI hiçbir yapı yok — `CHECK` yok, bileşik yabancı anahtar yok, `NUMERIC` ölçeği yok, JSONB yok — yani 101 dosyalık PG popülasyonu ve `ci.yml`in `BEKLENEN_PG_DOSYA_SAYISI=101` çivisi ELLENMEDİ; göçün kendisi yine de gerçek PostgreSQL 16.13'te koşuldu; SÜPÜRME: `backend/tests` tabanda (24fcf52) 2 kırmızı / 884 yeşil, bu dalda 2 kırmızı / 899 yeşil — AYNI iki kırmızı (`test_line_item_tenancy_slice1.py`in `test_yeniden_kurulum_hicbir_seyi_dusurmuyor` ve `test_geri_alma_tabana_donuyor`), YENİ kırmızı YOK, +15 yeşil bu dilimin testleridir; ÖLÇÜLMEDİ, ADIYLA: ÖN YÜZ dokunulmadı — kayıt ekranı ve firma ayarları ekranı bu alanı GÖSTERMİYOR, uç kabul ediyor ama arayüz göndermiyor; MODÜL ANAHTARLARI yok — hiçbir menü, yetki ya da kapı bu değere bakmıyor ve bakmaması bu dilimin İDDİASIDIR; GEÇMİŞ VERİ GÖÇÜ yok çünkü doldurulacak geçmiş veri YOKTUR; profil değişikliği `activity_log`a AYRI bir satır YAZMIYOR (firma ayarları ucunun mevcut kaydı neyse o).
