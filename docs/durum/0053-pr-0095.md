#95 — Tarla stok outbox TÜKETİCİSİ artık zamanlanıyor: FastAPI lifespan ile süreç-içi periyodik zamanlayıcı (FIELD_STOCK_OUTBOX_INTERVAL_SECONDS, varsayılan 30 sn, 1..3600 sınırlı) mevcut tüm-firma işleyiciyi tüketici mantığına dokunmadan çağırıyor; boş döngüler dahil her tur ve tüm sonuç kovaları loglanıyor, durdurma bayrağı THREAD'e ait, kurtarma yazımı TAZE oturumda kilitle yeniden deneniyor, olay `status` sütunu 26 karakterlik durumu taşıyacak şekilde genişledi ve başlangıç kablolaması mutasyonla kanıtlı: startup çağrısı silinince test_application_startup_reaches_field_stock_scheduler kırmızı
