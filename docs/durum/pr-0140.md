#140 — E4b-2 e-İRSALİYE YANITI (ReceiptAdvice): göç `20260915_0089` (`down_revision=20260915_0088`) `despatch_responses` (UNIQUE `(company_id,response_uuid)` idempotens anahtarı, bileşik FK -> despatch_notes, `raw_xml` denetim için saklanır ve HİÇBİR uçtan dönmez, CHECK tür KABUL/RED/KISMI_KABUL) ve `despatch_response_lines`ı (iki bileşik FK: -> yanıt ve -> despatch_lines; CHECK alınan>=0, reddedilen>=0, toplam>0; UNIQUE (response_id,despatch_line_id)) kurar; `despatch_notes` += `response_status`/`response_received_at`, `ck_despatch_notes_durum` += ACCEPTED/PARTIALLY_ACCEPTED/REJECTED — SQLite batch yeniden inşası 0083/0087'nin iki CHECK'ini, üç FK'sini, iki UNIQUE'ini ve `ix_despatch_notes_company_issue`i KORUDU (yansıma önce/sonra testte karşılaştırılıyor), downgrade yanıt durumunda irsaliye varken ADIYLA durur; DURUM MAKİNESİ: TERMINAL = üç yanıt durumu, DELIVERED 'yanıt bekliyor', yanıt durumuna YALNIZ DELIVERED'dan girilir, DELIVERED->REJECTED açık (Şef) ve nota 'sevk sonrası tam RED — mevzuat kontrolü' düşer, ham `edespatch_gib_status_code` korunur, `kodu_coz` yanıt durumu üretemez; 121 çiftlik tablo testli; E4a'nın TERMINAL/DURUM/rota-sayısı iddiaları BİLİNÇLİ yeniden yazıldı (eski iddia docstring'de); SAĞLAYICI: `get_receipt_advice` -> `GetReceiptAdvice` (SEARCH_KEY UUID=bizim ETTN, DIRECTION=IN — DOĞRULANMADI; uç `DespatchDocumentReference` ile süzer), ayrıştırıcı `edespatch.receipt_advice_coz` (DOĞRULANMADI; eksik RejectedQuantity=0, kimlik/satır referansı/miktar KATI ve adlı hata), eşleme anahtarı `DespatchLineReference/LineID` = bizim `DespatchLine/cbc:ID` = `line_no` (testle çivili, OrderLineReference DEĞİL), sentetik belgeler `tests/fixtures/e4b2/`; SYNC: önce ağ (yanıt sorgusu düşerse 502, HİÇBİR ŞEY yazılmaz), sonra ayrıştır+eşle (422 adlı kod, `edespatch_last_error`a yazılır, yanıt satırı yazılmaz), sonra satır kilidi altında TEK işlem; ikinci sync aynı belgeyle `changed:false`, satır sayıları değişmez; UÇ `GET /api/despatch-notes/{despatch_id}/response` (`sales`, yeni kural YOK; yönetici/muhasebe/satış 200, depo/rapor 403, çapraz firma 404) + `implicit_accept_due_at` = fiili sevk + 7 gün, yalnız DELIVERED'da, OTOMATİK DEĞİL; PİNLER (taban `0885616`): rota 420/326 -> 421/327 (958a9c0e -> cd72be75), GET 200 -> 201 (088b3c51 -> 6fbfbcad), AUTHENTICATED 407 -> 408, READ/GUARDED/UNDENIABLE sabit, TENANT_TABLES 123 -> 125, pg_twins 133 -> 134, baş 0088 -> 0089 (11 dosya + test_goc_zinciri 3 iddia; H17 ikizinin GOC'u 0088'de kaldı), Core envanter 230 -> 236 (select 160/update 64/delete 12; 1c166663 -> ae77cf11), Core kiracı kapısı 183 -> 191, dinamik text() parmak izleri SABİT, E4a PG ikizi despatch_notes 24 -> 26 sütun/3 CHECK/6 timestamptz, sayısal manifesto: yanıt satırı iki miktarı `sonradan_dogan`da (18,4), 5.1c: yeni FK'ler yansımadan eşlendi (0 sınıflandırılmamış), jenerik tohum CHECK'e takıldığı için iki satırlı yanıt elle tohumlandı ve yeni firmada iki bileşik referans DOĞRU satıra eşlendi (SQLite + PG); TESTLER: `tests/test_e4b2_irsaliye_yaniti.py` 43 (ileri/ters/2× karışık yeşil), PG ikizi 7 (taze DB, 0 atlama; iki oturumlu yarışta ikinci oturum UNIQUE'te bekleyip ihlali `False` olarak ele alıyor); MUTASYON: ön okuma+UNIQUE birlikte kaldırılınca idempotens KIRMIZI, yalnız ön okuma YEŞİL (savunma iki katlı), yalnız UNIQUE PG yarışını KIRMIZI yapar, ön okuma despatch_id anahtarlı KIRMIZI, 'yalnız DELIVERED'dan' dalı KIRMIZI, eşleşmeyen satırı atlamak KIRMIZI; tam izole paket 472/472 dosya 3590 sonuç (940s, 4 işçi); PG ikizlerinin TAMAMI dosya başına taze DB'de 134 dosya 536 test yeşil, 0 atlama; değişen 19 SQLite dosyası ters sırada yeşil; types.gen.ts iki üretimde bayt bayt aynı; AÇIK: SEARCH_KEY/UUID süzgeci ve satırlı yanıt sandbox'ta doğrulanmalı, 7 gün zımni kabul zamanlayıcısı yok, alınan+reddedilen ile sevk miktarı mutabakatı yapılmıyor — <merge tarihi>
