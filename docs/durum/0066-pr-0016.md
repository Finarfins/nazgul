#16 — DASHBOARD QUERY-PARAM / ROUTER-STATE AKSIYONLARI: dashboard kisayollari hedef ekranlarda karsiliksiz kaliyordu, ilgili sayfalar parametreyi hic okumadigi icin tiklama filtresiz listeye dusuyordu; Payments `?new=customer` (ve `?new=supplier`) formu dogru islem turuyle acar, Finance router state `accountId`/`transactionId` ile Hareketler sekmesine gecip odak filtresi uygular (rozetten temizlenir), Products `?critical=1` kritik/negatif stok filtresini acar, Transactions `?status=overdue` acik bakiyeli belgeleri suzer ve `?new=1` hem satis hem alis formunu acar, WorkOrders `?new=1` is emri formunu acar, Dashboard'daki Acil Aksiyonlar kartlari artik `/urunler?critical=1` ve `/satislar?status=overdue` uretir (onceden filtresiz `/urunler` ve `/musteriler`e gidiyordu); tuketilen parametre `setSearchParams(...,{replace:true})` ile URL'den dusurulur, desen InventoryCounts/InventoryCountsReport'tan devralindi, yeni gate YOK ve rota/envanter/gate/CI dosyalarina DOKUNULMADI; ETIKET DURUSTLUGU: `/orders` liste ucu `due_date` DONDURMEZ, bu yuzden overdue suzgeci vade degil ACIK BAKIYE olcer (`remaining_amount>0 && status!=='cancelled'`) ve menu etiketi 'Vadesi Gecen' yerine 'Odenmemis (Acik Bakiye)' olarak duzeltildi — gercek vade suzgeci backend `_list` SELECT'ine `h.due_date` eklenmesini gerektirir ve bu PR'in kapsami disindadir; DEGISEN: 6 sayfa + 6 test dosyasi, hepsi `frontend/src/pages/` altinda (Payments/Finance/WorkOrders testleri yeni, Dashboard/Products/Transactions testleri genisletildi); OLCUMLER (yerel, head 504b8a97): `npx vitest run` 580 gecti / 86 dosya / exit 0, `npx tsc -b --force` exit 0, `npm run typecheck:e2e` exit 0, `npm run lint` 0 hata / 844 uyari (degisen 12 dosyada 0 hata), `npm run build` exit 0; CI KOSUMU 33432731057 (ayni head): frontend GECTI (4m33s), e2e GECTI (14m42s), alembic-chain + backend-quality(0-3) + backend-quality-aggregate + backend-quality-canonical + backend-postgresql(0-3) + contract-drift + container + verify-image-artifact GECTI, durum-kaydi DUSTU (GIRDI YOK) — bu girdi tam olarak o kapiyi kapatir. NOT MEASURED: Playwright suiti YERELDE kosulmadi (yalnizca CI kostu); backend pytest suiti bu turda `tests/test_durum_kaydi.py` disinda kosulmadi (degisiklik `frontend/src/pages/` altindadir ve backend kodunun hicbir satirina dokunmaz); akislarin gercek tarayicida elle dogrulanmasi yapilmadi, kanit vitest birim duzeyindedir; bu girdiyi iceren yeni head'in CI kosumu bu satir yazilirken heniz olculmemistir.
