#57 — collect_ignore KARANTİNA PİNİ (yalnız test): `backend/conftest.py` 21 legacy dosyayı `collect_ignore` ile tutuyor (`LEGACY_TEST_MIGRATION_PLAN.md` bunu sınıf olarak anlatır: import anında API çağıran smoke script, paylaşılan `app.main` + `veriler.db`); izole koşucu listeyi AST ile okuyup manifestten düşürür; listeyi çivileyen kapı YOKTU — yeni bir ad eklemek testleri SESSİZCE her koşumdan çıkarırdı; BU PR o kapıyı `backend/tests/test_karantina_listesi_pini.py` ile AÇAR ve BAŞKA HİÇBİR ŞEY YAPMAZ (`conftest.py` DEĞİŞMEDİ, uygulama DEĞİŞMEDİ, plan belgesi DEĞİŞMEDİ); ÇİVİ 21 AD, `frozenset` + dosya-başı gerekçe; PLAN 21 addan HİÇBİRİNİ ANMAZ — gerekçe uydurulmadı, 21'i de `GEREKÇESİZ` (ayrışma RAPOR, belgeye dokunulmadı); her ad diskte VAR (bayat girdi kusur); izole manifest sayısı = keşfedilen `test_*.py` − `len(collect_ignore)` (ikisi de türetilir, 415 − 21 = 394, bu PR +1 dosya ile aktif 395); MUTASYON atılacak kopyada: gerçek aktif ad (`test_bulk_price_contract.py`) eklenince çapa O ADI söyleyerek kırmızı, listedeki bir ad çıkarılınca (`test_e2e_browser.py`) kırmızı, diskte olmayan bayat ad (`test_bayat_karantina_dosyasi_yok.py`) varlık + sayım kapısını kırar; alt süreç SQL sayacı KIMILDAMADI (dosyada subprocess+uzun SQL çifti YOK); TABAN `e7895a3` (`origin/develop`, `git rev-parse` ile doğrulandı); dal `test/collect-ignore-pini`; numara elle seçilmedi, `python scripts/durum.py --sonraki 57` verdi.
