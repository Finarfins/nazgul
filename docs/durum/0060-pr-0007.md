#7 — #6'nın kimlik kapısı bağımsız olarak yeniden ölçüldü ve TUTTU (gerçek Docker daemon, `ci.yml`'den PyYAML ile ayrıştırılmış adım gövdesi, ifadeler runner'ın yaptığı gibi literal ikame edildi): AYNI etikete ve AYNI `org.opencontainers.image.revision` etiketine sahip AMA BAŞKA bir imaj yüklendiğinde eski adım exit 0, yeni adım exit 1 veriyor; beklenti boşken yeni adım fail-closed exit 1 veriyor; `docker save` → `docker load` `.Id`'yi AYNEN koruyor; gerçek runner logu da (`verify-image-artifact`, koşu 33204095633) `expected_image_id="sha256:62794bd1f78d…"` ile `artifact_image_id=sha256:62794bd1f78d…` eşitliğini gösteriyor, yani `needs.container.outputs.tested_image_id` runner'da boş değil; AÇIK OLAN YER KAPI DEĞİL ONU KORUYAN SÖZLEŞMEYDİ — K5 karşılaştırma satırının VARLIĞINI arıyordu ve satır YERİNDE dururken kapı `ci.yml`'de TEK BİR DÜZENLEMEYLE öldürülebiliyordu: ÖLÇÜLDÜ (takas edilmiş artifact yüklenip mutasyonlu adım gövdesi koşturuldu) (1) hata dalı `|| { … exit 1; }` yerine `|| true` yazılırsa adım exit 0 verir ve eski K5 YEŞİL kalırdı — `set -euo pipefail` kurtarmaz, çünkü `[ … ] || true` bir bütün olarak 0 döner; (2) tek satırlık ikinci bir `expected_image_id="$image_id"` ataması eklenirse karşılaştırma kendi ölçtüğü değere bağlanır, adım exit 0 verir ve eski K5 yine YEŞİL kalırdı — yani kapı kendi kopyasına bakar, #6'nın beklentiyi bilerek artifact'in DIŞINDA taşıma gerekçesinin aynısı sözleşmenin kendi içinde açık duruyordu; (3) karşılaştırmanın SİLİNMESİ ise eski K5 tarafından zaten yakalanıyordu, yani #6'nın "sessizce silinemesin" iddiası doğruydu, boşluk silmede değil YERİNDE ETKİSİZLEŞTİRMEDEYDİ; DÜZELTME: K5 artık satırın varlığını değil KIRMIZIYA BAĞLANIŞINI ve beklentinin KAYNAĞINI ölçüyor — karşılaştırmayı İZLEYEN satır işi düşürmek zorunda (`exit 1`), `expected_image_id` TEK kez atanır, ve beklenti `docker save`in girdisiyle AYNI nesneden okunur (K4 save hedefini zaten çiviliyordu; burada kimliğin O hedeften okunduğu çivilendi); MUTASYON ÖLÇÜMÜ (yeni K5): mutasyonsuz dal 78 geçti/0 kaldı YEŞİL, `|| true` 77/1 KIRMIZI, ikinci atama 77/1 KIRMIZI, karşılaştırmanın silinmesi 77/1 KIRMIZI, kimliğin başka nesneden okunması 77/1 KIRMIZI, `container` çıktısının kaldırılması 77/1 KIRMIZI, #6 öncesi `37f7af7` `ci.yml` 77/1 KIRMIZI — her koşumda kırmızıya düşen TEK kapı K5, K1/K2/K3/K4/K6/K7 etkilenmedi; KAPSAM DIŞI ÇÜNKÜ ZATEN KAPALI: `continue-on-error: true` ile fail-open kaçışı denendi ve K1 yakaladı ("yayın bariyerindeki job/step continue-on-error ile fail-open: ['jobs.verify-image-artifact.continue-on-error=True']"), bu yüzden bu tura eklenmedi; KOŞULMAYAN: `ci.yml` bu turda DEĞİŞMEDİ, dolayısıyla gerçek runner'da yeni bir davranış yoktur — ölçülen şey sözleşme betiğinin KENDİSİDİR; bu tur yalnız `deploy/deploy-sozlesme-testi.sh` K5 bloğuna dokunur, `ci.yml` ve dondurulmuş sınır izi hareket etmedi
