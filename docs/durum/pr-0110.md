#110 — SEC-7: GÜVENİLMEZ XML `defusedxml`e ALINDI ve sınırsız ZIP açımına GERÇEK BAYT SAYACI KONDU (GÖÇ YOK — `backend/alembic` diff'i BOŞ ve bu ölçüldü: yama karması `e69de29bb2d1d6434b8b29ae775ad8c2e48c5391`, yani BOŞ BLOB; alembic başı `20260912_0082` KIMILDAMADI, ROTA YOK, ŞEMA DEĞİŞMEDİ, TABLO EKLENMEDİ; `backend/app`+`backend/alembic` birleşik yama karması `1c36800002807fc08e107665b375ba377785f1a9`); AÇIK ÖLÇÜLDÜ İDDİA EDİLMEDİ ve ölçüm develop `d39f743` üzerindedir — yönlendirmede verilen `3d4edb7` bu depoda BULUNAMADI, `git fetch` sonrası develop ucu `d39f743` çıktı ve dal ORADAN kesildi, bu yüzden yönlendirmedeki satır numaraları DEVRALINMADI, hepsi YENİDEN ARANDI (`_parse_xml` gövdesi 1691'de bulundu, `_pdf_from_zip` 1069'da, `exchange_rates` çağrısı 102'de); İKİ AYRI GÜVENİLMEZ XML YOLU vardı ve ikisi de bizim ÜRETMEDİĞİMİZ gövdelerdir: `app/einvoice/provider.py::_parse_xml` İzibiz/NES'in SOAP yanıt gövdesini, `app/exchange_rates.py::_parse_tcmb` ise TCMB'nin kur XML'ini stdlib `xml.etree.ElementTree.fromstring` ile ayrıştırıyordu; stdlib `ElementTree` DIŞ VARLIK ÇÖZMEZ ve bu yüzden klasik XXE (dosya okuma / SSRF) burada ZATEN YOKTU — açığın adı XXE DEĞİL VARLIK GENİŞLETMESİDİR (billion-laughs): `expat` iç varlıkları genişletir ve on katlı iç içe bir tanım birkaç yüz baytlık bir gövdeyi gigabaytlara açar, yani sağlayıcının (ya da MITM'lenmiş bir gövdenin) elinde ÜCRETSİZ BİR BELLEK POMPASIDIR; ÇARE `defusedxml==0.7.1`dir ve iki çağrı da ona alındı, DÖNEN NESNE YİNE STDLIB `Element`TİR — bu ÖLÇÜLDÜ, varsayılmadı (`type(defusedxml.ElementTree.fromstring("<a/>")) is xml.etree.ElementTree.Element` -> `True`) ve bu yüzden `_first_xml_element`/`_child_text` yardımcıları BAYT BAYT DEĞİŞMEDİ; BU DİLİMİN EN ÖNEMLİ ÖLÇÜMÜ İSTİSNA HİYERARŞİSİDİR ve gözden kaçsaydı yama sessizce YANLIŞ olurdu: `defusedxml.common.EntitiesForbidden` bir `ElementTree.ParseError` DEĞİLDİR, MRO'su `EntitiesForbidden -> DefusedXmlException -> ValueError`dır (çalıştırılarak doğrulandı), yani ESKİ `except ElementTree.ParseError` OLDUĞU GİBİ BIRAKILSAYDI bomba `None` döndürmez İSTİSNA olarak yukarı KAÇARDI ve `_business_failure`in fail-closed yolu FAILED demek yerine ÇÖKERDİ — `except` bu yüzden `(ElementTree.ParseError, DefusedXmlException)` oldu ve stdlib `ElementTree` ithali provider'da KASTEN BIRAKILDI çünkü hem `Element` tip açıklamaları hem `ParseError` hâlâ ondan geliyor; `exchange_rates.py`de ise `ElementTree`ye BAŞKA HİÇBİR GÖNDERME KALMADIĞI ölçüldü (yalnız 25 ve 102. satırlar) ve ithal KALDIRILDI, `_parse_tcmb`in "ayrıştırma hatasında FIRLAT" sözleşmesi KORUNDU çünkü çağıran `/api/exchange-rates/refresh` onu `except Exception` ile yakalayıp `MANUAL_OVERRIDE`a düşürüyor (`app/routers/supplier_prices.py:1991-1999`), yani bomba 502 değil DENETİMLİ BİR DEĞRADASYON üretir ve mevcut `test_parse_tcmb_malformed_xml_raises` (`ET.ParseError` bekler) YEŞİL KALDI çünkü defusedxml BOZUK XML'i hâlâ `ParseError` ile reddeder — yalnız DTD/varlık yolunu kesmiştir; İKİNCİ AÇIK ZIP BOMBASIDIR: `_pdf_from_zip` `zipfile.ZipFile(io.BytesIO(raw))` açıp `arsiv.namelist()` üzerinde HER ÜYE için `arsiv.read(ad)` çağırıyordu ve BU ÇAĞRI SINIRSIZDI — bildirilen `file_size` ne ise o kadar bellek TEK SEFERDE ayrılır, `_decode_pdf` de base64'ten çözülmüş gövdeleri doğrudan buraya beslediği için saldırı yüzeyi sağlayıcı yanıtının TAMAMIDIR; YENİ KAPI DÖRT AŞAMALIDIR ve üçü AÇMADAN ÖNCE koşar: `infolist()` üzerinde üye sayısı > `_ZIP_MAX_UYE` (8), herhangi bir üyenin `file_size` > `_ZIP_MAX_BAYT` (25 MiB), bildirilen `file_size` TOPLAMI > 25 MiB, ve bir üyenin `file_size / max(compress_size, 1)` ORANI > `_ZIP_MAX_ORAN` (100) — dördüncü aşamada üyeler `arsiv.open(bilgi)` ile `_ZIP_PARCA` (64 KiB) parçalar hâlinde okunur, OKUNAN GERÇEK BAYT sayılır ve hem üyenin bildirilen boyutunu hem 25 MiB'lık toplamı aşan ilk parçada BIRAKILIR; ihlalde `b""` döner (dosyanın MEVCUT fail-closed üslubuyla AYNI) ve `_zip_reddet` TEK bir WARNING basar — sebep, ölçülen değer ve sınır loglanır, GÖVDE ASLA loglanmaz (`"%PDF" not in mesaj` ayrıca çivilendi) çünkü sağlayıcı belgesi müşteri verisidir; SINIRLAR MODÜL SABİTİDİR (gömülü sihirli sayı değil) ve bir test dördünü de değerleriyle pinler; `EOFError` `except` demetine EKLENDİ çünkü parçalı okuma kesilmiş bir akışta onu üretebilir ve fail-closed sözleşmesi orada da tutmalıdır; DÖRDÜNCÜ ÖLÇÜM NOKTASI `app/routers/supplier_price_bridge.py`TİR ve YÖNLENDİRME "sınır var mı ÖLÇ, yoksa düzelt" diyordu — ÖLÇÜLDÜ ve SINIR ZATEN VARDI, üstelik DOĞRU BİÇİMDE: `_validate_archive` `MAX_ENTRIES` (512), `MAX_ENTRY_BYTES` (100 MiB), `MAX_TOTAL_BYTES` (300 MiB) sınırlarını uygular ve `stream.readinto(buffer)` ile GERÇEK BAYT SAYAR, yani başlığa güvenmez — DOKUNULMADI ve bu dilimin `_pdf_from_zip` yaması ONUN KALIBINI izler; `app/einvoice/ubl_xml.py` de ölçüldü: `escape`/`quoteattr` KULLANIR ama XML ÜRETİR, yani ÇIKTI yardımcısıdır ve ayrıştırma yapmaz — yönlendirmenin dediği gibi ELLENMEDİ, aynı şekilde `app/invoice_pdf.py` ve `app/routers/outputs.py`deki `escape` ithalleri de çıktı tarafındadır; DEPODA BAŞKA `fromstring`/`minidom`/`expat` ÇAĞRISI OLMADIĞI TARANARAK DOĞRULANDI, yani iki yol kapatılınca ayrıştırma yüzeyi BİTMİŞTİR; BAĞIMLILIK İKİ YERE YAZILDI ve bu ÖLÇÜLMÜŞ BİR KARARDIR: `backend/requirements.txt`e `defusedxml==0.7.1` (kesin pin, `pdfplumber`in üslubuyla aynı) VE `backend/requirements.lock`a hash'leriyle birlikte girdi — LOCK ŞART ÇÜNKÜ İKİ KAPI BİRDEN ONA BAĞLIDIR: `Dockerfile:55` `pip install --require-hashes -r backend/requirements.lock` der (yani lock'ta olmayan paket İMAJA HİÇ GİRMEZ ve `import defusedxml` üretimde çökerdi) ve `backend/tests/test_requirements_lock.py::test_lock_pins_every_direct_dependency` her doğrudan bağımlılığın lock'ta pinli olmasını ZORUNLU kılar, yani yalnız `requirements.txt`e eklemek CI'ı KIRAR ve imajı BOZARDI; hash'ler UYDURULMADI, gerçek artefaktlardan alındı (`pip download` ile hem wheel hem sdist indirildi, `pip hash` ile hesaplandı: sdist `1bb3032db185915b62d7c6209c5a8792be6a32ab2fedacc84e01b52c51aa3e69`, wheel `a352e7e428770286cc899e2542b6cdaedb2b4953ff269a210103ec58f6198a61`) ve giriş lock'a ALFABETİK yerine (`cryptography` ile `et-xmlfile` arasına) `# via -r requirements.txt` yorumuyla kondu, `pip-compile` BÜTÜNÜYLE YENİDEN KOŞULMADI çünkü o, ilgisiz onlarca paketi sürüm atlatıp bu PR'ı bir bağımlılık yükseltmesine çevirirdi — diff 4 SATIRDIR ve CRLF satır sonları KORUNDU; `backend/requirements-dev.txt`e AYRICA PİN GEREKMEDİĞİ ölçüldü: dosya ilk satırında `-r requirements.txt` der, yani devralır; İMAJ DEĞİŞTİĞİ İÇİN `container` ve `verify-image-artifact` işleri bu PR'ın ASIL KANITIDIR ve yerelde doğrulanamaz; KAPI `backend/tests/test_sec7_xml_zip_sertlestirme.py` (16 test) ve dosya ALT SÜREÇ ÇAĞIRMAZ, SQL ÇALIŞTIRMAZ, VERİTABANI KURMAZ — tamamen saf fonksiyon ölçümüdür, bu yüzden `pins/alt_surec_sql.txt`e SATIR EKLENMEDİ ve bu iddia dosyanın kendi başlığında da yazılıdır; SOKET TUZAĞI `test_e2_ebelge_yasam_dongusu.py`den DEVRALINDI (`socket.socket` ve `socket.create_connection` patlayan tuzaklarla değiştirilir, `urlopen`ı sahtelemek YANLIŞ NÖBETÇİ olurdu çünkü taşıma `http.client` kullanır) ve NÖBETÇİNİN KENDİSİ AYRICA ÖLÇÜLÜR (`test_SOKET_NOBETCISI_GERCEKTEN_KURULU`) çünkü kurulmamış bir tuzak dosyayı sessizce ağa açardı; testler: (a) billion-laughs gövdesi `_parse_xml`den `None` döner VE bu 2 saniyenin altında olur, ayrıca ÜRETİMDEKİ ZİNCİR ÖLÇÜLÜR — `_business_failure(HttpResponse(200, BOMBA))` `None` DÖNMEZ (yani bomba SESSİZCE BAŞARI SAYILMAZ) ve döndürdüğü sınıf `_fail("submit", ...)`e verildiğinde `EInvoiceResult.status == FAILED` çıkar ve `external_id is None` kalır (başarısız gönderim ETTN UYDURAMAZ), sağlayıcı GERÇEK yapıcısıyla kurulur (`__new__` hilesi değil) ve taşıması çağrılırsa PATLAR; (b) TCMB varlık-genişletmesi gövdesi `ValueError` ailesinden bir istisnayla reddedilir ve DUVAR SAATİ 2 SANİYENİN ALTINDA ölçülür (asılma yok), yanında OLUMLU KONTROL olarak gerçek TCMB gövdesinin hâlâ ayrıştığı çivilenir; (c) ZIP: 9 üyeli paket `b""` + "üye sayısı" uyarısı, 8x5 MiB sıkışmaz üye `b""` + "toplam boyut" uyarısı (her üye TEK BAŞINA sınırın altındadır, yakalayan TOPLAM kapısıdır ve bu testte AYRICA ölçülür), ~24 KB tel gövdesi / 24 MiB açılım `b""` + "sıkıştırma oranı" uyarısı, ve OLUMLU KONTROL olarak 100 KB'lık gerçek bir PDF taşıyan meşru ZIP PDF BAYTLARINI AYNEN döndürür ve TEK BİR UYARI BİLE BASILMAZ — sertleştirme "her şeyi reddet" DEĞİLDİR ve bu ölçülmeseydi tümüyle kapalı bir fonksiyon da yeşil yanardı; ayrıca eski sözleşme korundu (düz `%PDF-` aynen geçer, ZIP olmayan ve boş girdi `b""`) ve ZIP İÇİNDEKİ XML'in hâlâ `b""` döndüğü AMA UYARI BASMADIĞI çivilendi (sebep sınır ihlali değil "içinde PDF yok"); (d) MUTASYON KOŞULDU İDDİA EDİLMEDİ: kaynak `cp` ile yedeklendi, oran kapısı SİLİNDİ, koşuldu, `cp` ile geri kondu ve geri konduğu `grep` ile doğrulandı — mutant altında `test_zip_bombasi_oran_reddedilir_ve_uyari_basilir` VE `test_MUTASYON_oran_kapisi_sokulurse_bomba_gecer` KIRMIZI oldu (2 failed, 14 passed) ve kırmızılığın sebebi tam olarak ölçülen açıktır: `_pdf_from_zip` `b""` yerine 24 MiB'lık açılmış bombayı DÖNDÜRDÜ; MUTASYON HEDEFİ NEDEN ORAN KAPISIDIR — çünkü bu bomba üye sayısı, üye boyutu VE toplam boyut kapılarının ÜÇÜNÜ DE GEÇER (24 MiB < 25 MiB) ve onu durduran TEK ŞEY orandır, testin kendisi bunu ÖNCE ÖLÇER sonra iddia eder; YÖNLENDİRMENİN İSTEDİĞİ "bildirilen boyut YALAN SÖYLÜYOR" TESTİ KURULAMADI VE BU GİZLENMİYOR: CPython'ın `ZipExtFile`ı üyeyi bildirilen `file_size`ta KESER (`_left` defteri) ve tutmayan CRC'de `BadZipFile` fırlatır — hem YEREL BAŞLIK hem MERKEZİ DİZİN yalanı ayrı ayrı denendi (`struct.pack_into` ile merkezi dizindeki `file_size` alanı 1024'e düşürüldü), İKİSİ DE `Bad CRC-32` ile düştü, yani "bildirilen boyuttan FAZLA bayt akıtan" bir ZIP bu çalışma zamanında ÜRETİLEMEZ ve o senaryoyu iddia eden bir test VACUOUS olurdu; bu yüzden bayt sayacı savunmanın SON HALKASI olarak konumlandırıldı (TEK halkası değil) ve yalan başlıklı ZIP'in yine de fail-closed `b""` döndüğü `test_MUTASYON_yalan_baslikli_zip_fail_closed` ile ÖLÇÜLDÜ; SAYAÇLAR ÖLÇÜLDÜ VARSAYILMADI ve HİÇBİRİ KIMILDAMADI: rota sözleşmesi **392/301** (yeni rota YOK — bu dilim sayaca DOKUNMADI; 391 -> 392 hareketi SEC-10'un (#106) verify-email POST ayrımından gelir ve REBASE SONRASI yeniden ölçüldü, yol sayısı 301 KIMILDAMADI), GET izin envanteri **186**, `TENANT_TABLES` **120**, dinamik-SQL envanteri (`provider.py` ve `exchange_rates.py` SQL İÇERMEZ, bu tarandı), alembic başı `20260912_0082`, `frontend/src/api/types.gen.ts` DEĞİŞMEDİ (uç nokta değişmedi, yeniden üretim GEREKMEDİ ve YAPILMADI — `git diff --name-only` çıktısında SIFIR eşleşme); İZOLE KOŞUM `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4` ile iki turda yapıldı: yeni kapı + `test_e2_ebelge_yasam_dongusu` + `tests/test_ci_gates` + `tests/test_route_security_contracts` + BUGÜN exchange_rates'i ÖRTEN DOSYA (`backend/test_v3_fx_rounding_and_offline.py` — `backend/tests/` altında DEĞİL, `backend/` kökündedir ve arayarak bulundu) + `tests/test_requirements_lock` + `test_backend_encoding` + `test_e1_efatura_sertlestirme` -> **8/8 dosya, 100 test YEŞİL**; sayaç kapıları AYRICA koşuldu: `tests/test_route_get_permission_inventory` + `tests/test_tenant_scoping_guard` + `tests/test_core_query_inventory` + `tests/test_alembic_chain_integrity` + `tests/test_core_tenant_scoping_guard` -> **5/5 dosya, 168 test YEŞİL**; MOJIBAKE KAPISI GÖZETİLDİ: `test_backend_encoding.py` `app/**/*.py` içinde dokuz mojibake işaretini arar ve DÜZELTME İŞARETİ (U+00C2) YASAKTIR — yeni Türkçe yorumlar bilerek düzeltme işaretsiz yazıldı ve tarama ayrıca ELLE koşuldu (`clean`); YAZIM NOTU: test dosyası `Write` aracıyla yazıldı çünkü Bash heredoc'u kaçış dizilerini ÇÖKERTİYOR (aynı tuzak lock düzenlemesinde de görüldü ve orada satır-tabanlı ekleme ile aşıldı); ÇALIŞMA AĞACI NOTU: yönlendirme `C:\Users\HHH\nazgul-testler` dedi ama O YOL BU MAKİNEDE YOKTUR (`C:\Users` altında yalnız `berka`), bu oturum `F:\nazgul` makinesidir — talimatın ASIL KURALI ("kendi worktree'n, ASLA paylaşılan checkout") korunarak `F:\nazgul-sec7` worktree'si `origin/develop` üzerine açıldı ve paylaşılan `F:\nazgul`a HİÇ YAZILMADI; REBASE NOTU: dal ilk olarak `d39f743` üzerine kesildi ama iş sırasında develop ilerledi (#106 SEC-10 ve #107 SEC-8 birleşti) ve dal `78f8141` üzerine YENİDEN OTURTULDU; ÇAKIŞMA `backend/requirements.txt` ve `backend/requirements.lock`ta çıktı çünkü SEC-8 `cryptography>=50,<51` ekliyordu — ÇÖZÜM İKİSİNİ DE TUTMAKTIR ve lock'ta SEC-8'in `cryptography==50.0.1` bloğu KORUNUP dalın taşıdığı ESKİ `cryptography==49.0.0` bloğu (48 satır) SİLİNDİ, yani lock'ta tek bir `cryptography` pini kaldı ve bu ÖLÇÜLDÜ (`grep -c '^cryptography=='` -> 1); rebase sonrası diff'in SEC-8'e HİÇ ÇURUK EKLEMEDİĞİ de ölçüldü: lock diff'i hâlâ 4 SATIR, `backend/app`+`backend/alembic` yama karması DEĞİŞMEDİ (`1c36800002807fc08e107665b375ba377785f1a9`); AYRICA SEC-10 İLE İLİŞKİ AÇIKÇA ÖLÇÜLDÜ: `ad3843b` commit'i `tests/test_sec10_hardening_bundle.py`yi `defusedxml`den stdlib `ElementTree`ye ÇEVİRİYOR ve bu bir POLİTİKA DEĞİL, `defusedxml`in O SIRADA BİLDİRİLMİŞ BİR BAĞIMLILIK OLMAMASININ sonucudur — bu dilim onu usulünce bildirdiği için kısıt ORTADAN KALKTI, dosyaya DOKUNULMADI ve rebase sonrası o dosya AYRICA koşuldu (7 test YEŞİL); dal `fix/sec7-defusedxml-zip`; kayıt adı `python scripts/durum.py --sonraki 110`
