#124 — H32 PLATFORM OLAYI YAZICISI (göç YOK, alembic başı 20260914_0084 SABİT): `maintenance._record_recovery` ve `backup_cli._force_clear_activity` `backup.maintenance_recovered` / `backup.maintenance_force_cleared` olaylarını `SELECT id FROM companies ORDER BY id LIMIT 1` ile İLK kiracının `activity_logs`una yazıyordu (#122 merceği, develop 50b0dba) — ikisi de artık `platform_denetim.platform_olayi_yaz(None, …, sistem=True)` ile `security_audit_logs`a firmasız (company_id/user_id/username NULL, `aktor=sistem`) yazıyor; katalog `platform.mt_recover` / `platform.mt_force`; sahte `{id: 0}` aktörü UYDURULMADI, `sistem` ile `request` birbirini dışlar (ValueError); `text()` maintenance.py 18 -> 17, backup_cli.py 1 -> 0, dinamik 1'de ve küresel 269'da SABİT, parmak izi 02ce8076 -> fb67335e; TESTLER: yeni `tests/test_h32_platform_olay_yazici.py` 5 (develop yazıcılarıyla 4 KIRMIZI), SQLite 194 + izole 38 yeşil, PG 16 taze DB backups 9/9, sec4 5/5, pp1 12/12, untenanted 1/1; kapsam dışı: göç 0034 ve `core_schema.py:508` aynı sorguyu olay yazmadan kullanıyor — ve bu kayıt; kayıt adı `python scripts/durum.py --sonraki 124`
