#115 — 5.1c KİRACI GERİ YÜKLEME (Faz 9-1, "yanlışlıkla sildim" cevabı, GÖÇ YOK): `POST /api/platform/tenant-restore` 5.1a zip'ini ÇALIŞAN veritabanına TEK işlemde YENİ bir firma olarak yazar (`mode=yeni`, varsayılan) ya da kapalı ve BOŞ kaynak kimliğini yerinde canlandırır (`mode=yerine`); aktif bir kiracının üstüne HİÇBİR koşulda yazılmaz (409 `RESTORE_SOURCE_ACTIVE`), artık satır 409 `RESTORE_SOURCE_ROWS_PRESENT`; `dry_run` aynı yolu sonuna kadar yürüyüp işlemi BİLEREK geri alır ve raporu döndürür — kuru koşunun yazmaması bir bayrak dalı değil işlemin kendisidir; ekler veritabanı COMMIT'inden SONRA `_depo_koku()/attachments/<yeni cid>/<yeni iş emri>/` altına kopyalanır ve `storage_path` yeniden yazılır; ÇEKİRDEK `backend/app/kiraci_geri_yukleme.py` (saf mantık + SQLAlchemy Core; dışa aktarımın `_kiraci_tablolari/_tablo_sirasi/_yansit/_sema_seviyesi` yardımcıları TAŞINMADI, İTHAL EDİLDİ — export bayt bayt aynı, 30 export+imha testi yeşil) ve `routers/kiraci_geri_yukleme.py` (gövde `MAX_TENANT_RESTORE_UPLOAD_BYTES`=512 MiB tavanına kadar parça parça geçici dosyaya, uç kendi 413'ünü verir, `main.py` yol tavanı +1 MiB; `require_platform_operator`; `company.restored` denetim satırı OPERATÖRÜN firmasına, geri yüklenen firmanın tabloları manifeste birebir kalsın diye); ÖLÇÜLDÜ (0082 şeması, develop `72cfa09`): 120 kiracı tablosu (TENANT_TABLES kımıldamadı), 294 FK kısıt-sütunu (188 kiracıya, 76 `companies`, 30 `app_users`, 60 nullable, 8 kendine) → yeniden yazılan AYRI FK sütunu **113** (yol haritasının "111"i; bileşik FK'ların `company_id` yarısı ayrı sütun değil), FK'sız `*_id` yumuşak referans **94** → 80'i sözlükle haritalanır (`DOGRUDAN_HEDEFLER` + ayırt edici `entity_type/reference_type/source_type/resource_type/converted_type/return_type/party_type/kind/action_type` sözlükleri; `party_type` BÜYÜK harf), 9'u kullanıcı kimliği (platform, olduğu gibi), 12'si metin kimlik, 1'i bilinen çözümsüz (`receivable_charge_periods.installment_id`) ve sınıflandırılmamış sütun **0** (test adıyla çiviler), 4 küresel UNIQUE (ikisi haritalanan kimlik → kendiliğinden çözülür; `whatsapp_pairing_codes.code_digest` ve `whatsapp_pending_actions.islem_anahtari` tek kullanımlık sır → çakışan satır ATLANIR ve `skipped` ile sayılır), 6 bileşik/anahtarsız PK tablosu, PostgreSQL'de serial OLMAYAN tek tamsayı PK **1** (`notifications_archive` — `yeni` kipinde kimliği modül üretir); TASARIM BULGUSU: `sort_tables` FK sırası geri yükleme için YETMEZ — 286 yumuşak kenar var ve DÖNGÜ kuruyor (`delivery_notes`↔`sales_orders`), `extra_dependencies` `CircularDependencyError` veriyor; modül kendi Kahn sıralamasını kullanır (FK KESİN, yumuşak TERCİH) ve döngüde kırılan kenarı satır yazıldıktan sonra UPDATE ile bağlar (`pos_system_customers.customer_id` küresel UNIQUE'i bu yüzden PG'de ısırdı); KAPI `tests/test_kiraci_geri_yukleme.py` (18 test; alt süreç ZORUNLU — Settings tek kopya; betik `-c` ile DEĞİL DOSYADAN koşar çünkü Windows 32 KiB komut satırında `CreateProcess` FileNotFoundError verdi): JENERİK tohum HER kiracı tablosuna yansımadan bir satır yazar (CHECK `IN`/`= ANY(ARRAY[...])`/eşitlik/`>=` ayrıştırılır, tam/asgari/tek-nullable-id denemeleri) ve `BOS_KABUL` **BOŞ** — 120/120 tablo yolculukta; iddialar: yeni firmanın HER tablosu manifeste birebir (iki sır tablosu `skipped`=manifest), yeni firmanın HER FK'sı yeni firmanın içinde (0 kaçak), fatura `totals_snapshot`=kalem toplamı 300.00, tahsis 120+180=ödeme 300.00, parti zinciri (P-1,P-2→A-1; P-3→A-2), Decimal 12.3456 korundu, kaynak firma sayımları ve `is_active=false` DOKUNULMADI, ek kopyalandı/eksik sayıldı ({planned:2,copied:1,missing_in_zip:1}), kuru koşu ve 4 kurcalama (sayı 422 `RESTORE_ROW_COUNT_MISMATCH`, şema 409 `RESTORE_SCHEMA_MISMATCH`, manifest yok 422, zip değil 422) SIFIR yazma, 5. tabloda enjekte RuntimeError → 500 ve firma satırı dahil hiçbir şey kalmadı, operatör olmayan admin 403 / düşük rol `PERMISSION_DENIED` / yabancı `X-Company-ID` `COMPANY_ACCESS_DENIED`, 413; `test_kiraci_geri_yukleme_postgresql.py` (4 test, taze DB `lens:lens@localhost:5433`, `REQUIRE_PG=1`): gerçek FK'larla yuvarlak yolculuk, geri alma, kurcalama, `yerine` kimlik korur VE serial sırayı ilerletir (`setval`; SQLite rowid yeniden kullanımı bunu GİZLİYORDU — üyelik kimliği PG'de 7≠6 çıktı, `_uyelikleri_yaz` kimliği korur hale getirildi); PİNLER: rota 392/301→**393/302** (parmak izi 618b656d→f131e483; `ROUTE_REASONS` platform grubu), GET envanteri **186** kımıldamadı, `EXPECTED_AUTHENTICATED` 379→380, `EXPECTED_READ` **80** ve `EXPECTED_UNDENIABLE` **97** kımıldamadı (izin `__admin_only__` — yedeklerin `read`i BİLEREK taklit edilmedi, gerekçe `auth.py` kuralında), Core sorgu envanteri 176→**184** (+6 select +2 update, hepsi yeni dosyada; `UNRESOLVED_ALLOWLIST` 3→8 yansımalı tablo gerekçesiyle; `VARIABLE_ARG_ALLOWLIST` 4'te SABİT — `values(**)`/`where(*)`/`select_from(<ad>)` bilerek kullanılmadı, satır sözlüğü çalıştırma parametresi), Core kiracı ifadesi 150→**152** (iki `insert(memberships)`), `ACTION_TYPES` 75→**76**, `pg_twins.txt` 123→**124** satır, `alt_surec_sql.txt` +1 dosya (3), dinamik-SQL parmak izi YALNIZ `activity_log.py` için kımıldadı (katalog sözlüğü, SQL değil — önceki altı girdiyle aynı biçim), `YANSIMA_DOSYA_MUAFIYETI` BÜYÜMEDİ (reflect çağrısı bu dosyada değil), `types.gen.ts` yeniden üretildi (+75 satır, tek yol), `.env.example`/`.env.production.example`/`docker-compose.prod.yml` yeni anahtarı taşıyor; KOŞU: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4` TABAN develop `72cfa09` ayrı worktree'de **449/449 dosya, 3031 sonuç, exit 0 (1183 s)**; dal (ccecf9a+603abc0) **451 dosya: 448 yeşil, 3 kırmızı** — `test_backend_encoding.py` (docstring'de `HÂLÂ`, mojibake kapısı), `tests/test_env_example_completeness.py` ve `tests/test_import_route_body_limit.py` (yeni ayar `.env` örneklerinde/konteynerde ve gövde tavanı pininde yoktu) — üçü ff36134/35f6bac/39cde9f ile düzeltildi ve izole koşucuda 3/3 yeşil (23 sonuç); hedefli tur 189 yeşil (yeni + disa_aktarim + imha + tenant_scoping_guard + core envanteri + ci_gates); AÇIK KARARLAR (berkay): (1) `mode=yerine` GERÇEK akışta BUGÜN erişilemez — yumuşak imha satır silmez ve dışa aktarımın KENDİ `company.exported` kaydı bile "artık satır"dır, `activity_logs` DELETE'i ve `company_id` UPDATE'ini tetikleyiciyle reddeder (test 409'u ÖLÇÜYOR, mutlu yolu tetikleyiciyi test DB'sinde düşürerek simüle ediyor): salt-ekleme denetim tablolarındaki artıklara izin verip aynı kimlikli satırı atlamak mı, kipi kaldırmak mı, olduğu gibi bırakmak mı; (2) üyelik eşlemesi E-POSTAYLA YAPILAMIYOR — 5.1a zip'i `app_users`ı taşımaz, yalnız `user_id`; eşleme kimlikle, silinmiş kullanıcı `memberships.skipped_user_ids`te; e-posta için export manifestine kullanıcı e-postası eklenmeli (5.1a'ya dokunulmadı); (3) 5.1a `_seri` JSON sütunlarını Python repr (`str(dict)`) yazıyor (4 sütun ölçüldü) — restore `json.loads`→`ast.literal_eval` ile ikisini de okur, export tarafı ayrı düzeltme; kayıt adı `python scripts/durum.py --sonraki 115`; CI DUZELTMESI (c546a16 sonrasi, `backend-quality-canonical` KIRMIZIYDI): "Degisen SQLite testlerini ters sirada kostur" adimi `tests/test_import_route_body_limit.py`nin 7 `async def` testinde "async def functions are not natively supported" ile dustu; SEBEP SIRA DEGIL - adim `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` veriyor, boylece pytest-asyncio yuklenmiyor ve `pytest.ini`deki `asyncio_mode = auto` hicbir sey yapmiyor; OLCULDU (2026-09-10, c546a16): ileri sira 7 dusen/6 gecen, ters sira 7 dusen/6 gecen, `-p pytest_asyncio.plugin` ile iki sirada da 13/13 gecti - yani kusur ileri sirada da vardi ve ancak `async def` testi olan bir dosya DEGISTIGINDE goruluyordu (bu adim yalniz DEGISEN dosyalari kosar); CARE `.github/workflows/ci.yml` icinde o adimin iki `pytest` cagrisina da `-p pytest_asyncio.plugin` eklemek - `run_isolated_tests.py` ayni bayragi ayni nedenle zaten veriyor (satir 337 ve 592), adim onu atlamisti; TEST DOSYALARINA VE `backend/app`E DOKUNULMADI (`git diff 72cfa09 HEAD -- backend/app backend/alembic` parmak izi b5d9182528b9 KIMILDAMADI); DOGRULAMA: degisen 8 SQLite dosyasinin 208 dugumu ileri/ters/karisik (seed 20260910) UC SIRADA da 208/208 yesil, izole kosucu 451/451 dosya 3053 sonuc exit 0, PG ikizi taze `lens115_fix` DB'sinde 4/4
