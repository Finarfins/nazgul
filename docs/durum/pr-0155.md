#155 — F10-1b ÇİFTÇİ WhatsApp DAĞITIMI + KVKK RIZA KAPISI + EKSTRE/AVANS (GÖÇ VAR: `20260920_0091_whatsapp_message_attempts`, down_revision `20260918_0090`); AÇIK ÖLÇÜLDÜ İDDİA EDİLMEDİ: F10-1a numarayı CARİYE bağlamayı getirmişti ama DAĞITICIYA HİÇ DOKUNMAMIŞTI — bağlanmış bir çiftçi yazdığında `baglam.kimlik_secimi` onu PERSONEL defterinde arıyor, bulamıyor ve `_bagsiz_cevap`a düşüyordu, yani `taraf.taraf_coz` ve `taraf.kod_kullan` KOD OLARAK VARDI ama HİÇBİR gelen mesaj onlara uğramıyordu; DAL SIRASI keşif §5.1'in verbatim niyetiyle kuruldu — `baglam.kimlik_secimi` ÖNCE (SAF OKUMA, `baglam_oku` yalnız SELECT yapar) ve personel kimliği çözülürse çiftçi dalı HİÇ denenmez, gerekçesi DARALTICI OLANIN KAZANMASI (personel zinciri aktif kullanıcı + aktif firma + üyelik taşır), `taraf_coz` personel adayı VARKEN çağrılmıyor yani personelin tek bir kod yolu bile çiftçi dalına sapamaz; `firma_komutu` ÇİFTÇİ DALINDA ÇAĞRILMIYOR ve bu bir atlama değil ÖLÇÜM: o fonksiyon `eslestirme.kimlik_coz`a bakar, yani çiftçiye HER ZAMAN boş liste bulur ve bağlı bir çiftçiye `TANINMAYAN_NUMARA_MESAJI` ("bu numara bir ERP hesabına bağlı değil") derdi; `kimlik_secimi`nin `firma_komutu`dan ÖNCEYE alınması PERSONEL DAVRANIŞINI DEĞİŞTİRMİYOR çünkü `firma_komutu` bir metin döndürürse `secim` kullanılmadan atılıyor ve `FİRMA SEÇ`in yazdığı bağlam zaten bir SONRAKİ mesajda okunuyordu; İKİ ARAÇ KÜMESİ ASLA BİRLEŞMİYOR — `niyet.ARAC_BEYAZ_LISTESI` (yedi personel aracı) DEĞİŞMEDİ ve `ciftci_yurutucu.CIFTCI_BEYAZ_LISTESI` = {ciftci_ekstre, ciftci_avans} onunla KESİŞMİYOR (kapı bunu frozenset kesişimiyle ölçüyor), ayrı modül seçildi çünkü çiftçi köklerini `niyet`e eklemek "hangi beyaz liste?" sorusunun cevabını ÇAĞRI BAĞLAMINA bağlardı ve yanlış listeyle çağıran bir mutant hiçbir davranış testini kırmazdı; `niyet.tahsilat_coz` ve `niyet._terim_cikar` çiftçi modüllerinde HİÇ GEÇMİYOR (AST kapısı, yorum satırları elenerek) yani `whatsapp_pending_actions`e çiftçiden HİÇBİR satır düşemez ve serbest metinden cari adı ÇIKARILMAZ; RIZA KAPISI `consents.evaluate_consent`i HER ERP okumasından ÖNCE, HER mesajda YENİDEN çağırıyor (sözleşme 2: anlık görüntü karar verici değildir) ve `whatsapp_party_links.consent_at` bir İZ, KARAR DEĞİL; dört red gerekçesinin (NO_RECORD/REVOKED/RECIPIENT_CHANGED/RECIPIENT_INVALID) dördü de FAIL-CLOSED, `NO_RECORD` KVKK metnini + EVET/HAYIR sorusunu üretiyor ve kalan ÜÇÜ AYIRT EDİLEMEZ tek bir genel metin alıyor; RIZAYI ÇİFTÇİNİN İLK MESAJI VERİYOR, EŞLEŞTİRME DEĞİL (Şef kararı, F10-1a'nın `kod_kullan` başlığındaki söz) ve `set_consent(source='PHONE')` olay satırını + versiyon artışını KENDİ İÇİNDE yazıyor, ikinci bir defter AÇILMADI; ÜÇ KARAR PR GÖVDESİNE YAZILDI — (a) `HAYIR` bir NO-OP DEĞİL `REVOKED` yazıyor çünkü defter bunu destekliyor (ÖLÇÜLDÜ: `set_consent` kayıt YOKKEN `granted=False` ile `status='REVOKED'`, `version=1` satırını INSERT ediyor) ve no-op bırakılsaydı `evaluate_consent` her mesajda yine `NO_RECORD` der, çiftçi KVKK sorusunu SONSUZA DEK yeniden alırdı — hem rahatsız edici hem Meta mesaj maliyeti üzerinden bir masraf yüzeyi; `EVET` her zaman `GRANTED` yazabildiği için reddeden çiftçi fikrini değiştirebilir ve bu bir adımla ölçülüyor; (b) `EVET` YALNIZ rıza AÇIK DEĞİLKEN, `HAYIR` YALNIZ kayıt YOKKEN yazıyor — aksi hâlde tekrarlanan tek bir kelime sınırsız `version` artışı ve sınırsız olay satırı üretirdi (kapı: üç kez `EVET` sonrası versiyon SABİT); (c) ÇOK FİRMALI ÇİFTÇİNİN SEÇİMİ `FİRMA SEÇ <n>` DEĞİL SIRA ÖNEKİ ("1 EKSTRE") ve gerekçe ÖLÇÜM, tercih değil: `FİRMA SEÇ` seçimi `whatsapp_context`e yazıyor, o tablonun `user_id` sütunu NOT NULL ve `app_users.id`ye GERÇEK bir FK ile bağlı (göç `20260910_0079`), çiftçinin `app_users` satırı YOKTUR (keşif §5.5 bunu aktör sorunu olarak zaten ölçmüştü) — ikinci bir taraf-bağlamı tablosu İKİNCİ bir göç demekti ve §6.2 bu PR'a TEK göç veriyor; durumsuz yol İKİ yan kazanç getiriyor: süresi dolan bir seçimin belirsizliği YOK ve "hangi firmadayım" sorusunun bağlantı defterinden BAŞKA bir cevabı olabileceği ikinci bir yer YOK; RASTGELE SEÇİM HİÇBİR DALDA YOK ve kapı bunu iki firmanın FARKLI açılış bakiyesiyle (10.000 / 77.000) ölçüyor — seçim sorulurken İKİSİNİN DE rakamı cevapta yok, sıra önekiyle her biri KENDİ sayısını veriyor; `DUR`/`İPTAL` rızayı geri çekiyor VE bağlantıyı `is_active=False` yapıyor İKİSİ BİRDEN (keşif §5.4: rıza "mesaj gönderebilir miyiz", bağlantı "bu numara kim" sorusunu cevaplıyor; yalnız rızayı çekmek numarayı hâlâ çözülebilir bırakırdı) ve ADAYLARIN HEPSİNE uygulanıyor — çıkmak isteyene önce firma seçtirmek çıkışı zorlaştırmak olurdu; komutlar TAM EŞLEŞME regex'i (`eslestirme.bagla_ayristir` sınıfı) ve "iptal etmek istemiyorum" bir komut DEĞİL (kapı var) çünkü rızayı geri çeken bir komutun çıkarımla tetiklenmesi, kullanıcının vermediği bir kararı onun defterine yazmak olurdu; HIZ SINIRI K6'nın tavsiyesiyle YENİ PLATFORM TABLOSU (`whatsapp_message_attempts`, `company_id` YOK) — mevcut tabloya `kind` eklemek anahtarı `(phone, window_start)`tan değiştirmek, yani yürürlükteki eşleştirme sınırını bir göç boyunca ZAYIFLATMAK olurdu; `company_id`nin YOKLUĞU İKİNCİ ve DAHA KUVVETLİ bir gerekçe taşıyor: sınırın koruduğu şey firmanın verisi değil BOT NUMARASININ MESAJ BÜTÇESİDİR ve `company_id` taşısaydı iki alım merkezine birden bağlı bir çiftçi sınırı İKİYE KATLARDI (kapı: iki firma, TEK sayaç satırı); pencere 15 dk / sınır 20 / cevap sınırı 15 ve İKİ EŞİK İKİ `last_error` üretiyor — `cevap_siniri` (>15) mesajın TAM İŞLENDİĞİNİ ama cevaplanmadığını, `mesaj_siniri` (>20) mesaja HİÇ dokunulmadığını söylüyor, ikisi de IGNORED çünkü dışarıya hiçbir şey gitmedi; sınır YALNIZ ÇİFTÇİ DALINDA ve bu KAPSAM KARARI — keşif §1.5 personel yolunda sınır OLMADIĞINI ölçtü, personel sınırını değiştirmek bu PR'ın kapsamı dışı ve bu bir kapıyla YAZILI hâle getirildi; EKSTRE `build_statement`i `rol` GEÇİRMEDEN çağırıyor ve bu bir UNUTMA DEĞİL KARARIN KENDİSİ (fonksiyonun başlığı: "varsayılanı MASKELİDİR ... `rol` geçirmeyi unutursa sonuç GİZLİ olur, sızıntı DEĞİL") — çiftçi kendi bakiyesini görmeli, firmanın CARİ KARTI görünümünü değil, ve kapı VKN/adres/e-posta alanlarını DOLDURUP cevapta OLMADIKLARINI ölçüyor, rakamlar `build_statement`in KENDİ çıktısıyla karşılaştırılıyor; ay adı çözümü `ciftci_niyet`te ON İKİ adlık KAPALI bir sözlükle yapıldı ve `niyet`e DOKUNULMADI — `niyet.py:72-76` ay adlarını BİLEREK tanımıyor ("Nisan, Kasım, Ekim aynı zamanda kişi adıdır") ama o gerekçe CARİ ADI ARANAN personel yolunda doğru, çiftçi dalında aranan bir ad YOK; yıl seçimi DETERMİNİSTİK (adı geçen ay bu yıl HENÜZ BAŞLAMADIYSA ÖNCEKİ yıl — Mart'ta ARALIK yazan çiftçi geçen aralığı sorar, gelecekteki bir ayın ekstresi BOŞ dönerdi ve kullanıcı nedenini HİÇ öğrenemezdi); `HESAP` TEK BAŞINA KÖK DEĞİL (yalnız `OZET` ile BİTİŞİK çiftte) çünkü `SORU_SOZLUGU`nda zaten DURAK kelime; `CARI_KOKLER` YENİDEN KULLANILDI (keşif §4a) yani "cari durum <ad>" bir kapsam mesajı DEĞİL çiftçinin KENDİ ekstresini üretiyor ve cümledeki AD sorguya GİRMİYOR — kapı bunu ADIYLA ölçüyor (cevapta çiftçinin KENDİ adı VAR, yazdığı YABANCI ad YOK) ve gerçek personel araç cümleleri ("84993120 stok", "kritik stok", "bu ay ciro", "... nakit tahsilat") KAPSAM MESAJI alıyor, `tahsilat_coz` sayacı SIFIR; AVANS YALNIZ tedarikçi tarafında ve `CUSTOMER` tarafı sorduğunda TARAF TİPİ SIZDIRILMIYOR — cevap "Bu numara için avans kaydı bulunmuyor" ve o metin, kaydı GERÇEKTEN OLMAYAN bir TEDARİKÇİNİN alacağı cevapla BİREBİR AYNI (kapı iki metni `==` ile karşılaştırıyor); `payment_id` ve `note` HİÇ SEÇİLMİYOR (keşif §4b: `note` personel notu olabilir) ve son avans tarihi `payments.payment_date`ten geliyor çünkü `supplier_advances`in KENDİ tarih sütunu YOK (`applied_at` MAHSUP anıdır); toplamlar ucun (`GET /api/suppliers/{id}/advances`) KENDİ satırlarının toplamıyla karşılaştırılıyor (TestClient ile gerçek HTTP), uç ÇAĞRILMIYOR çünkü `Request` ve bir yetki yüklemi ister; ÖLÇÜLEN VE DÜZELTİLEN KUSUR — TEK `BAĞLA` MESAJI SAYACI İKİ YAKIYORDU: dağıtıcı personel defterini, sonra taraf defterini deniyor ve İKİSİ de kendi adım 1'inde `deneme_say`i çağırıyor, yani tek YANLIŞ kod penceredeki sayacı İKİ artırır ve personelin 5'lik sınırı ÜÇ denemede ısırırdı; sayaç artık dağıtıcıda BİR KEZ artırılıp ikisine de `deneme=` ile geçiriliyor ve `None` (varsayılan) İKİ fonksiyonun da BUGÜNKÜ davranışı, yani doğrudan çağıran WA2/F10-1a testleri DEĞİŞMEDİ; H78 — `taraf.kod_kullan` bağlantı satırına `created_by=None` yazıyordu ama sütunun yorumu "Kodu üreten personel" diyordu (yorum ile veri AYRIŞIYORDU ve bağlantıyı hangi personelin açtırdığı denetimde GÖRÜNMÜYORDU), değer artık TÜKETİLEN KOD SATIRINDAN taşınıyor; H77 — `test_DAVRANIS_kod_uret_KULLAN_baglanti_ve_RIZA_aciyor` testin ÖLÇTÜĞÜNÜN TERSİNİ söylüyordu (gövde SIFIR rıza satırı doğruluyor) ve `..._baglanti_acar_RIZA_ACMAZ` oldu — yanlış ad, bu PR'ı yazan kişiye "rıza eşleştirmede açılıyor" diye okunup rıza kapısını HİÇ kurdurmayabilirdi; K2 TOHUM DÜZELTMESİ — `seed_demo_data.py` müşteri telefonu ON ÜÇ rakam üretiyordu ve `consents.normalize_msisdn` (sıkı) onu `None`a düşürüyordu, tedarikçi telefonu AYRICA `212` SABİT HAT önekindeydi (sıkı normalleştirici abonenin `5` ile başlamasını ŞART koşuyor) — yani demo verisiyle WhatsApp rızası HİÇ yazılamıyordu ve F10-1 uçtan uca DENENEMİYORDU; `telefon.normalize_phone` (gevşek) aynı numarayı kabul ettiği için ayrışma SESSİZDİ; ikisi de ON İKİ RAKAMA çevrildi ve kapı OTUZ indisin HER BİRİNDE iki normalleştiricinin AYNI rakamları verdiğini + betiğin GERÇEKTEN bu desenleri taşıdığını ölçüyor; TESTLER SQLite (`tests/test_f10_1b_ciftci_ekstre_avans.py`, 34 test) İLERİ/TERS/KARIŞIK sırada YEŞİL; PG İKİZİ (`test_f10_1b_ciftci_ekstre_avans_postgresql.py`, 10 test) TAZE bir veritabanında 0 SKIP ve ardışık iki koşuda KALINTI SIFIR — ikizin DÖRT gerekçesi de YALNIZ orada görünür: (1) `mesaj_deneme_say` lehçeye göre İKİ AYRI gövdeye dallanıyor ve SQLite hattı PG dalını HİÇ koşturmuyor (yanlış yazılsaydı sayaç ÜRETİMDE hiç artmaz, sınır HİÇ ısırmaz, kapatılan açık KAPALI GÖRÜNÜRKEN AÇIK kalırdı), (2) YİRMİ eşzamanlı UPSERT SQLite'ta ÜRETİLEMEZ ve sonuç TEK satır + `attempt_count=20` + dönen değerler 1..20'nin PERMÜTASYONU (yani `RETURNING` gerçekten atomik okudu), (3) SQLite CHECK'i YANSITMIYOR — `attempt_count >= 0` burada GERÇEKTEN reddediyor, (4) `SUM(NUMERIC)` kuruş farkı yalnız PG'de görünür ve kuruşlu üç satırla 60.000,30 / 6.234,61 / 53.765,69 tam çıkıyor; PG ikizi ayrıca avansın KİRACI yüklemini komşu firmanın 999.999,99'uyla ve taraf tipi denetimini `party_id` KASITLI OLARAK tedarikçinin kimliğiyle AYNI verilerek ölçüyor (`customers.id` ile `suppliers.id` AYRI sözlüklerdir ve aynı sayıyı taşıyabilirler); GÖÇ TURU up->down->up ALT SÜREÇTE koşuyor çünkü Alembic'in `env.py`si bağlantıyı `settings.database_url`den kuruyor — `sqlalchemy.url`i süreç içinde değiştirmek ÇALIŞAN veritabanını DEĞİŞTİRMİYOR (bu önce yazıldı, KIRMIZI görüldü, sonra taşındı) ve tur komşu sayacın (`whatsapp_pairing_attempts`) AYAKTA KALDIĞINI da ölçüyor; PİNLER HER BİRİ TARAYICI KOŞTURULARAK ölçüldü, ARİTMETİKLE DEĞİL: alembic başı 0090 -> 0091 ON DÖRT DOSYA ON BEŞ SİTE (`test_goc_zinciri.py` İKİ site taşıyor), Core sorgu envanteri 250 -> 251 (+1 select `ciftci_yurutucu._firma_adi`; `ciftci_ekstre` `build_statement`i ÇAĞIRDIĞI için kendi Core ifadesi YOK, `ciftci_avans` `text()` ve `mesaj_deneme_say` lehçeye özgü `insert()` olduğu için envantere GİRMİYOR), `select` dağılımı 170 -> 171 ve `update`/`delete` KIMILDAMADI (dal OKUMADIR), envanter parmak izi fa4c5e26 -> a234212e, `taraf.kod_kullan` parmak izi 490f5076 -> 886102c0 ve `taraf.taraf_coz` ae4de317 -> 51b8143a (İKİSİ DE YALNIZ SEÇİLEN SÜTUN KÜMESİYLE kımıldadı, YÜKLEMLER AYNI ve kiracı istisnalarının GEREKÇELERİ de yeniden yazıldı), Core kiracı ifadesi 208 -> 208 DEĞİŞMEDİ (`companies` kiracı tablosu değil), `ACTION_TYPES` 82 -> 84 ve `RESOURCE_TYPES` DEĞİŞMEDİ (`whatsapp_party` F10-1a'da açılmıştı), `activity_log.py` dinamik text() parmak izi 86cdca1d -> 23e84c6f ama text() ÇAĞRISI SAYISI 3'TE SABİT ve HİÇBİR SQL METNİ KIMILDAMADI (eklenen şey bir SÖZLÜK LİTERALİ), `TENANT_TABLES` 127 -> 127 (yeni tablo PLATFORM), `pins/pg_twins.txt` 140 -> 141, rota pinleri (425/331, GET 202, AUTH 412) DEĞİŞMEDİ çünkü YENİ ROTA YOK, `pins/alt_surec_sql.txt` DEĞİŞMEDİ (göç turu betiği tarayıcının SQL desenine GİRMİYOR — ölçüldü, varsayılmadı); SÖZLEŞME SAPMASI KAPISI GERÇEKTEN KOŞTURULDU: `npm ci --no-audit` + `export_openapi.py -o backend/openapi.json` + `npm run types:gen` + `capture_frontend_fixtures.py` ardından `git diff --exit-code -- frontend/src/api/types.gen.ts frontend/src/test/fixtures` TEMİZ — `npm ci` atlanırsa `types:gen` HİÇ koşmaz ve kapı VACUOUS geçerdi; WA2'nin hız sınırı AST kapısı yeni değişken adıyla çivilendi (`deneme` artık PARAMETRE adı, yerel değişken `sayi`) ve kapıya İKİNCİ bir iddia eklendi — sayaç ya ÇAĞIRANDAN gelir ya BURADA artırılır, ÜÇÜNCÜ bir yol YOK; kayıt adı `python scripts/durum.py --sonraki 155`; DÜZELTME 1 (AGY kontrat: avans ortak servis) — `ciftci_yurutucu._AVANS_TOPLAM_SQL` `routers/avans.py::list_supplier_advances` SELECT'inin KOPYASIYDI ve SİLİNDİ, satırlar artık TEK yerden okunuyor: `app/avans_servis.py::tedarikci_avans_satirlari` (tek sayfa, `open_only` iki SABİT metin arasından seçilir, `payments` JOIN'i bileşik yüklemle ve son avans tarihi `payment_date`ten) + `tedarikci_tum_avanslari` (sayfaların birleşimi, sayfa başına en çok 200 satır — tek LIMIT 200+ avanslı çiftçiye eksik rakam söylerdi, mutasyonla KIRMIZI görüldü); uç ve çiftçi aracı İKİSİ DE bu servisi çağırıyor, `app/whatsapp` altında `supplier_advances` geçen SQL dizesi 0 (AST kapısı); ucun cevap biçimi ve durum kodları DEĞİŞMEDİ ve contract-drift 3 adımı frontend'de fark ÜRETMİYOR — ilk denemede ucun docstring'ine yazılan not OpenAPI'ye aktı ve `types.gen.ts`te 5+/2- fark ÖLÇÜLDÜ, not `#` yorumuna taşındı; eşitlik kapısı iki diyalektte: SQLite HTTP cevabıyla, PG ikizi uç fonksiyonun kendi döndürdüğüyle (sayfa boyu 2, üç kuruşlu satır); TARAYICILAR bf5ca3f'e karşı ÖLÇÜLDÜ ve PİN DEĞİŞMEDİ: Core envanteri 251 / parmak izi a234212e SABİT (taşınan sorgu `text()`, Core değil), dinamik `text()` 274 çağrı / 62 dosya SABİT (yeni metinlerin hepsi literal), kiracı nöbetçisinin taradığı literal SQL 728→727 (avans.py 11→9, ciftci_yurutucu 1→0, avans_servis 0→2)
