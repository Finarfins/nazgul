#142 — PP4 PLATFORM PANELİ EYLEM DÜĞMELERİ (frontend, backend/göç YOK, yeni rota YOK): PP3 `PP2Dugmesi` yer tutucuları PP2 uçlarına bağlandı — ortak `EylemDugmesi` (yalnız `can('platform')`ta çizilir; yıkıcı eylemde hedefi adıyla anan onay penceresi; `changed:false` → bilgi "Zaten bu durumda"; hata → `errorDetail` sunucu cümlesi, liste yenilenmez; başarıda `yenile()`) + sayfa düzeyi `useEylemBildirimi`; Şirketler askıya al (onay + sonuç cümlesi) / aç, Kullanıcılar kilitle (onay + sonuç) / kilidi aç / şifre sıfırlat (onay) / doğrulama gönder (yalnız doğrulanmamış; 409/429 cümlesi), Kuyruk kanal başına yeniden dene (yalnız failed>0; onay kanal + sayı; requeued/remaining/not_retryable), Güvenlik IP başına hız sınırı temizliği (onay IP; iki sayı ayrı; 404 cümlesi) + denetimin 7 süzgeci (boş gönderilmez, sayısal olmayan actor_id/status_code gönderilmez, date_to uçta hariç olduğundan ertesi yerel gece yarısı); `PP2Dugmesi`/`TODO(PP2)` 0; `types.gen.ts` yeniden üretildi, fark boş, denetim yanıtı `unknown` → TODO(H38) kaldı; testler `PlatformEylemleri.test.tsx` 18 + mevcut 4 dosyada yer tutucu testleri operatör-değil testlerine çevrildi (platform 7 dosya 45 test); tam vitest 107/107 dosya 804/804 test; lint 0 hata; build yeşil — <merge tarihi>
