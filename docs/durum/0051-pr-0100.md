#100 — TransactionDialog'un boş satır beklemesindeki YARIŞ kaldırıldı; kusur TESTTE, bileşende değil: `waitFor(...toHaveLength(2))` (satır 88/99/112) mount→/products→önseçim→boş satır zincirinin ÜÇ React commit'ini tek 1000 ms bütçeye sıkıştırıyordu — bacaklar p50 48/68/99 ms, bütün zincir p50 223 max 368 ms, pay yalnız 2.7x — artık her geçiş KENDİ koşulu olarak beklenir ve hiçbir timeout değeri değişmedi; RTL bütçesi kısılarak ölçüldü: öncesi 300 ms'de 1, 200 ms'de 3 testi 10/10 düşürüyordu, sonrası 90 ms'e kadar 0/10 ve ancak 30 ms'de kırmızı, yani test körelmedi; useAutoTrailingLine boş satırı eklemeyi bırakınca dört test de kırmızı — <merge tarihi>
