#129 — CS2 ÇEK/SENET ↔ CARİ: KÖPRÜ, EKSTRE, TAHSİS, KARŞILIKSIZ DEKONT (backend; göç 0086): göç `20260914_0086` `receivable_charge_documents` iki CHECK'ini `bounced_check` ile genişletir (şekil: `cek_senet_id` zorunlu, sipariş/dönem/iş emri NULL; eski dallara `cek_senet_id IS NULL`), `cek_senet_id` + bileşik FK + `uq_receivable_bounced_check_active` (posted ∧ tersi yok) ve `companies.ciro_tedarikci_odemesi` (varsayılan KAPALI, admin ayarı) ekler; geri alma `bounced_check` varken REDDEDER; SQLite yeniden kurmada kısmi indeksler BİREBİR korundu (ölçüldü); köprü: `POST /api/payments` çek/senet yönteminde aynı işlemde `portfoyde` evrak (evrak alanı zorunlu/yasak 422, idempotent tekrar tek evrak, bağlı ödeme PUT/DELETE 409 `CEK_BAGLI_ODEME`), ters köprü `POST /api/cek-senetler payment_olustur` (ödeme defterine `cek-senet:` türev anahtar, ATLANAN_UCLAR 11 sabit); geçişler: tahsil -> `finance_transactions` (`reference_type='cek_senet'`), karşılıksız/iade (köprüden doğmuş alınan evrak) -> `bounced_check` belgesi, tahsis KORUNUR, ödemesiz evrak belge AÇMAZ, ciro yalnız anahtar açıkken tedarikçi ödemesi; ekstre `Tahsilat (Çek - <durum>)` + `Karşılıksız/İade Çek Dekontu`, yürüyen bakiye = kapanış = 360; pano `portfolio_checks`, yaşlandırma `portfolio_checks`+`net_risk`, karşılıksız borç vadesi = evrak vadesi; PİNLER (taban `4c3cda8`): rota 0/0, GET/TENANT_TABLES/ACTION_TYPES/alt_surec/manifesto SABİT, pg_twins 129 -> 130, baş 0085 -> 0086 (9 dosya/11 iddia), Core envanter 206 -> 210, Core kiracı 167 -> 171, dinamik text() 269 -> 270 (yalnız statement.py 5 -> 6; 8 dosya yalnız parmak izi), types.gen +94; TESTLER: CS2 25 + PG ikizi 9 (taze DB, motor açık/kapalı, ileri/ters/karışık), CS1 66, mutasyon 7/7 kırmızı, tam izole paket 464 dosya / 3435 sonuç (tek kırmızı `test_platform_backup_security` paralel yük zamanlaması, saf develop'ta da kırmızı — ölçüldü)
