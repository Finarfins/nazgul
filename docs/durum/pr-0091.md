#91 — H11: CI'nin `frontend :: Enforce exact frontend GHSA audit policy` işini düşüren İKİ danışma bildirimi KAPANDI ve İKİSİ DE GERÇEK DÜZELTMEYLE, istisnayla DEĞİL; BULGU: PR #88 (1B-H) yeşil değildi ve sebebi o dalda DEĞİLDİ — `develop` `f6d261c` AYNI işi AYNI adımda düşürüyordu (ölçüldü: koşu 34287033938), yani bildirimler YENİ YAYINLANMIŞTI ve her dalı bloke ediyordu; birinci bildirim `@vitest/mocker` GHSA-82FW-GWWQ-J7X9 ve çaresi düz bir yükseltme: `vitest ^4.1.10 -> ^4.1.11`, sonrasında `npm audit`te `@vitest/mocker` HİÇ GÖRÜNMÜYOR; ikinci bildirim `js-yaml` GHSA-2883-xcg3-v3hh (YÜKSEK) ve BU DİLİMİN ASIL DERSİ BURADA: dalın İLK hâli onu `audit-allowlist.json`a "yukarı akışta düzeltme yok, `openapi-typescript` 7.13.0 henüz `@redocly/openapi-core` v2'yi desteklemiyor" gerekçesiyle ve 2026-12-31 son kullanma tarihiyle YAZIYORDU — GEREKÇE DOĞRU DEĞİLDİ ve yanlışlığı KAYIT DEFTERİNDEN ölçüldü: `npm view js-yaml versions` `4.3.2`yi listeliyor, yani yamalı sürüm AYNI ANA SÜRÜMDE (4.x) VAR ve kırıcı bir yükseltme DEĞİL; NEDEN GÖRÜNMEDİĞİ de ölçüldü: `@redocly/openapi-core@1.34.19` js-yaml'ı `^4.3.1` DEĞİL TAM `4.3.1` olarak sabitliyor, dolayısıyla npm 4.3.2'yi kaldıramıyor ve `npm audit fix --dry-run` KOŞULDU, HİÇBİR ŞEY değiştirmedi (5 zafiyet 5 kaldı); `npm audit --json` yine de `fixAvailable: true` diyor ve bu bayrak İYİMSER — AYRIM ADIYLA ÖLÇÜLDÜ ve bu dilimin okunuşu şudur: js-yaml için `fixAvailable` DÜZ BİR BOOLEAN'dır ("kırıcı olmayan bir çare var"), oysa `uuid`/`xcode`/`@capacitor/cli` zinciri için AYNI alan bir NESNEDİR (`{name: '@capacitor/cli', version: '8.4.3', isSemVerMajor: true}`) yani gerçekten ANA sürüm ister — ikisini aynı kovaya atmak, birini gereksizce istisna listesine yazmak demekti ve tam bu olmuştu; ÇARE `overrides: {"js-yaml": "^4.3.2"}`: tam sabitlemeyi aşmanın standart ve KIRICI OLMAYAN yolu, 4.3.1 -> 4.3.2 bir YAMA sürümü; RİSK UMUT EDİLMEDİ, ÖLÇÜLDÜ ve bu overrides'ın en tehlikeli yanıdır: js-yaml'ı tüketen `@redocly/openapi-core`, `openapi-typescript`in SPEC ÇÖZÜCÜSÜDÜR — override çözücüyü bozsaydı `types.gen.ts` SESSİZCE değişir ve `contract-drift` kapısı düşerdi, yani kusur bu PR'da değil BİR SONRAKİ şema değişikliğinde görünürdü; kanıt olarak `backend/export_openapi.py` + `npm run types:gen` yeniden koşuldu ve `types.gen.ts` çıktısı BAYT BAYT AYNI çıktı (satır sonu normalize edilerek karşılaştırıldı); SONUÇ `npm audit`te YÜKSEK 2 -> 0 (js-yaml VE onun etkilediği `@redocly/openapi-core` düştü), kalan 3 ORTA `uuid`/`xcode`/`@capacitor/cli` zinciridir ve o ZATEN allowlist'te (GHSA-w5hq-g745-h8pq, Android kuran bir depoda iOS proje aracı; Capacitor ANA sürümü mobil kurulum riski demek); `npm run audit:policy` AUDIT_POLICY_GREEN veriyor ve istisna sayısı İKİ DEĞİL TEK — yani bu dilim istisna listesini BÜYÜTMEDİ, bir istisnayı GERİ ALDI; kilit dosyası değiştiği için `audit-allowlist.json` içindeki `$lockfile.sha256` ve `scripts/check-audit-policy.test.mjs` içindeki pin bilerek güncellendi (özet SATIR SONLARI LF'e normalize edilerek hesaplanır — Windows çalışma ağacı CRLF yazar, Linux koşucusu LF okur ve ham bayt haşlamak aynı içerik için iki farklı özet üretirdi); ÖLÇÜMLER SAYIYLA: `node --test scripts/check-audit-policy.test.mjs` 24 test KOŞTU ve sayı ADIYLA okundu (boş eşleşmede `node --test` YEŞİL yanar, yani "0 test geçti" ile "24 test geçti" ayırt edilmezdi), `npx vitest run` 89 dosya / 620 test, `tsc -b` temiz, `npm run build` temiz, `npm run typecheck:e2e` temiz VE gerçekten 32 e2e dosyası GÖRDÜ (`--listFiles` ile sayıldı: `tsc --noEmit -p` sıfır dosyayı da SESSİZCE yeşil sayar); BACKEND'E DOKUNULMADI ve bu ölçüldü (`git diff --name-only HEAD -- backend` BOŞ; `backend/openapi.json` `.gitignore`dadır, yeniden üretilmesi ağacı kirletmez); GÖÇ YOK, ROTA YOK, ŞEMA ALANI YOK; TABAN `develop` f6d261c; dal test/h11; kayıt adı `python scripts/durum.py --sonraki 91`
