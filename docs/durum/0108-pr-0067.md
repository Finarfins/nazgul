#67 — HİJYEN H3 KAYIT ZAMAN BÜTÇESİ: `test_self_service_registration.py` anti-enumeration duvar saati `|Δ| < 0.12` mutlak bütçesi yük altında titreşiyordu; DÜZELTME: 3-örnek medyan + göreli bütçe `|Δ| < max(0.12, 0.35 × slower)`; aynı gövde + her iki yolda hash KORUNDU; `backend/app` SIFIR; KANIT: `PYTHONUTF8=1 python run_isolated_tests.py test_self_service_registration.py --workers 4` ×10 → 10/10; run id `e06cc266-2c38-45b3-8e52-b6d47973b24c`; 17 bağlam SABİT; dal `test/h3`; `--sonraki 67`.
