#92 — E2 e-BELGE YAŞAM DÖNGÜSÜ: gönderilen belgenin SURETİ İNDİRİLEBİLİYOR ve e-ARŞİV FATURASI İPTAL EDİLEBİLİYOR (GÖÇ YOK — yeni sütun yok, yeni tablo yok; `alembic/` diff'i BOŞ ve bu ölçüldü); E1 (#87) belgeyi GÖNDERMEYİ ve durumunu SORMAYI sertleştirmişti, geriye belgenin hayatının İKİ UCU kalmıştı ve ikisi de bu dilimin konusu; YENİ UÇ TEK: `GET /api/invoices/{invoice_id}/einvoice/download?format=pdf|xml`; `format` KAPALI KÜMEDİR (400) ve İKİ BİÇİM İKİ FARKLI KAYNAKTAN gelir, bu bir simetri kaybı değil bir karardır: `xml` AĞA HİÇ ÇIKMAZ — gönderilen belge, gönderim anında dondurulmuş `einvoice_payload`dan `build_invoice_xml` ile bire bir yeniden üretilir (sağlayıcıdan XML istemek, elimizde ZATEN olan ve belgenin gönderilmiş hâlini TANIMLAYAN veriyi ikinci bir kaynaktan sormak olurdu), `pdf` ise sağlayıcıdan gelir çünkü PDF'i BİZ ÜRETMİYORUZ, e-Arşiv/e-Fatura görüntüsü entegratörün mühürlediği sunumdur; ANAHTAR SEÇİMİ KANALA GÖRE ve ikisi birbirinin YERİNE GEÇMEZ: e-Arşiv `GetEArchiveInvoice` belgeyi `WEB_VALIDATION_KEY` ile ister (bizim `einvoice_web_key`imiz — 0081'de saklanmaya başlanan ama O TURDA HİÇBİR YERDEN OKUNMAYAN sütun; bu uç, saklanan anahtarı İLK KEZ KULLANAN yerdir), e-Fatura `GetInvoiceWithType` ise sağlayıcı belge kimliğiyle (`einvoice_external_id`); GÖNDERİLMEMİŞ BELGEDE 404 ve 409 DEĞİL — fark ölçülebilir: 409 "şu an olmaz, durumu değiştir" demektir (`einvoice/sync` orada haklı olarak 409 kullanır, çünkü YAPILACAK bir iş vardır: önce gönder), burada ise indirilecek KAYNAK hiç var olmamıştır; koşul ÇİFTTİR (`einvoice_status` NONE/FAILED/boş DEĞİL **ve** `einvoice_external_id` DOLU) ve ikisi AYRI ŞEY ölçer — durum belgenin bir hayatı olup olmadığını, kimlik sağlayıcıda bir karşılığı olup olmadığını söyler; İZİN `sales` ve BU KEZ AÇIK BİR KURAL GEREKTİ — E1'in TAM TERSİ, ve VARSAYILMADI ÖLÇÜLDÜ: kural yazılmadan önce `required_permission("GET", ".../einvoice/download")` -> "read" veriyordu, çünkü `app/auth.py`de `path.startswith("/api/invoices")` -> "sales" kuralının ÜSTÜNDE genel bir güvenli-metot kuralı var ve her GET'i oraya düşürüyor; `read` YANLIŞ olurdu İKİ SEBEPLE — (a) bu GET DIŞ BİR YAN ETKİ üretir, sağlayıcıda oturum açar ve KOTA tüketir (E1'in `sync` ucunu GET değil POST yapma gerekçesinin AYNISI), (b) indirilen şey RESMİ MALİ BELGEDİR, `GET /invoices/{id}/pdf`in bastığı İÇ faturanın kopyası değil; KURAL ÖNEK+SONEK BİRLİKTE yazıldı (`startswith("/api/invoices/")` **ve** `endswith("/einvoice/download")`) çünkü salt `/api/invoices` öneki fatura LİSTESİNİ ve İÇ PDF'i de yakalar, okuma yetkisi olan rolleri onlardan düşürürdü; komşuların KIMILDAMADIĞI ölçüldü: `.../einvoice/status`, `.../pdf` ve `/api/invoices` hâlâ "read"; `POST /api/invoices/{invoice_id}/cancel` ARTIK e-BELGE DUYARLI ve SIRA SÖZLEŞMENİN KENDİSİDİR: e-belge kapısı, var olan compare-and-set UPDATE'ten ÖNCE koşar — çünkü o UPDATE bir kez koştuğunda fatura YERELDE iptal olmuştur ve sağlayıcı reddederse geri alınacak bir şey kalmaz; YEREL İPTAL ENTEGRATÖRÜ GEÇEMEZ; `einvoice_channel=EARSIV` ve `einvoice_status` üç canlı durumdan birindeyse sağlayıcıda `CancelEArchiveInvoice` çağrılır — BAŞARILIYSA `einvoice_status=CANCELLED` yazılır ve yerel iptal devam eder (İKİSİ AYNI TRANSACTION'da: ya birlikte iner ya hiç), BAŞARISIZSA 409 `EARSIV_IPTAL_BASARISIZ` ve yerelde HİÇBİR ŞEY değişmez; `EFATURA` ise 409 ile REDDEDİLİR ve gerekçe MEVZUATTIR, bir ürün kararı değil: giden bir ticari e-Fatura TEK TARAFLI iptal edilemez, alıcı belgenin kendisine ulaşmasından itibaren SEKİZ GÜN içinde itiraz eder (TTK 18/3) ve iptal o sürecin SONUCUDUR; bu artışta uygulama yanıtı (`ApplicationResponse`) YOK, o yüzden uç sessizce yerelde iptal etmek yerine operatöre ASIL YAPMASI GEREKENİ söylüyor — sessiz yerel iptal, ERP'nin "iptal" dediği bir belgenin GİB'de YÜRÜRLÜKTE kalması demekti; KANALI BİLİNMEYEN ama canlı görünen belge de 409 alır: hangi iptal yolunun geçerli olduğunu BİLMEDEN yerelde iptal etmek, iki yolun da yanlış olabileceği bir tahmindir; yapılandırma yoksa 503 (entegratöre SORULAMIYORSA yerelde iptal etmek, tam da bu kapının engellemek için var olduğu ayrışmayı üretirdi); `CANCELLED` DURUM MAKİNESİNE GİRDİ ve makinedeki TEK BİZE AİT DURUMDUR — diğerleri sağlayıcının cevabından doğar, bu bizim isteğimizin KABULÜNDEN; TEK YÖNLÜ ve YALNIZ `CANCELLABLE`={PENDING,SENT,ACCEPTED}'ten: dışarıda kalan üçü AYRI gerekçelerle dışarıda ve testte de AYRI AYRI ölçülüyor (tek bir "diğerleri" satırı, üçünden birinin sessizce sızmasını göremezdi) — `NONE` gönderilmemiş, entegratörde iptal edilecek zarf YOK; `FAILED` gönderim hiç inmedi ve ETTN yok, aynı çelişki artı meşru yeniden gönderimi (rank 0) KAPATIRDI; `REJECTED` zaten GİB'in kararıdır ve üzerine yazmak o kararı ve `error` gerekçesini SİLERDİ; KAPI, TERMINAL KONTROLÜNDEN ÖNCE koşuyor ve bu ZORUNLUYDU: en önemli iptal `ACCEPTED`ten olandır (operatörün geri çekmesi gereken ASIL belge budur) ve genel rank/terminal makinesine bırakılsaydı `ACCEPTED` geçişi YUTARDI, yerel satır entegratörün az önce iptal ettiği bir zarf için ACCEPTED demeye devam ederdi; `CANCELLED` kendisi TERMINAL (hiçbir sağlayıcı yanıtı geri yürütemez) ve `QUERYABLE` DIŞINDA — bir durum SORGUSU onu ÜRETEMEZ, çünkü bir belgenin iptal edildiğinin TEK kanıtı entegratörün BİZİM iptal çağrımıza verdiği cevaptır; sorguda olsaydı geç gelen bir yoklama belgeyi hiç istemediğimiz bir iptalle işaretleyebilirdi; İPTALİN TEL ŞEMASI TAHMİN DEĞİL: canlı `efaturatest.izibiz.com.tr/EIArchiveWS/EFaturaArchive?wsdl` -> `?xsd=5` üzerinden OKUNDU ve İKİ AYRINTI TÜRETİLEMEZDİ — (a) içerik elemanının adı `CancelEArsivInvoiceContent`, yani sarmalayıcı "EArsiv" yazarken operasyonun kendisi `CancelEArchiveInvoice` diye "EArchive" yazıyor, AYNI İSTEKTE İKİ FARKLI YAZIM ve biri diğerinden çıkarılamaz; (b) zorunlu TEK anahtar `FATURA_UUID`, yani ETTN — sağlayıcı belge kimliğinin karşılığı olan `FATURA_ID` OPSİYONELDİR; OPSİYONEL ALANLARIN HİÇBİRİ GÖNDERİLMİYOR (`UPLOAD_FLAG`/`FATURA_ID`/`EARSIV_CANCEL_EMAIL`/`DELETE_FLAG`/`IPTAL_TARIHI`/`TOPLAM_TUTAR`/`INVOICE_CONTENT`/`IPTAL_NOTU`) ve bu ihmal değil karar: her biri sağlayıcı tarafında FARKLI bir işlem anlamına gelir (silme, yerine belge koyma, geçmişe tarihli iptal) ve hiçbiri ÖLÇÜLMEDİ; `FATURA_ID` de bilerek yok — iki anahtarın çeliştiği durumda hangisinin kazandığını ÖLÇMEDEN varsaymak olurdu; YANITTA DURUM ALANI YOKTUR (şemada yalnız `REQUEST_RETURN` + `ERROR_TYPE`), yani BAŞARININ TEK İŞARETİ hata zarfının YOKLUĞUDUR — bu da okunamayan bir gövdeyi sessizce başarı sayma riskini doğurur ve `_business_failure` fail-closed olduğu için boş/ayrıştırılamayan/zarfsız üç gövdenin ÜÇÜ DE başarısız sayılıyor (test ediliyor); İPTAL OTOMATİK TEKRARLANMIYOR ve gerekçe `submit`inkinden FARKLI: tekrarlanan bir iptal ETKİ olarak zararsızdır (ikincisi belgeyi çoktan çekilmiş bulur) ama 3. denemede zaman aşımına düşen bir retry, entegratörün 1. denemede KABUL ETMİŞ olabileceği bir iptal için `FAILED` bildirirdi — zarf gitmiş, yerel fatura ISSUED kalmış olurdu; raporlanan bir hatada operatör tekrar sorabilir, sessiz bir ayrışmada sorması gerektiğini BİLEMEZ; ETTN'SİZ İPTAL LOGIN BİLE AÇMIYOR (kapı ağdan ÖNCE, `_cancel_precondition`) — kurulamayacak bir istek için oturum açmak, sağlayıcıda karşılığı olmayan bir giriş bırakırdı; e-Arşiv durum sorgusunun ETTN önkoşuluyla AYNI gerekçe; NES'TE İPTAL KAPALI ve kapı `_call`da DEĞİL ondan ÖNCE — bu ÖLÇÜLDÜ VARSAYILMADI: `NesEInvoiceProvider._call` bir if-zinciridir ve SON dalı `check_taxpayer`dır, yani tanınmayan bir operasyon SESSİZCE oraya düşer ve `kwargs["vkn"]` ile `KeyError` verirdi; "Nes zaten yapılandırılmamış, oraya hiç gelinmez" DOĞRUDUR ama bir GÜVENCE DEĞİLDİR, `NES_BASE_URL` dolduğu gün kapı kendiliğinden açılırdı; GERÇEK SANDBOX KOŞULDU ve SONUÇ, E1'in AÇIK BIRAKTIĞI SORUYU CEVAPLADI: iptal BAŞARISIZ oldu ama BAŞARISIZLIĞIN KENDİSİ kanıttır — sağlayıcı `ERROR_CODE=10008 "Belirtilen kritere uygun kayıt bulunamamıştır. Belge ETTN : <bizim gönderdiğimiz ETTN>"` diyor, yani ARADIĞI ANAHTARI GERİ YANKILIYOR; durum sorgusu başarısız olduğunda BOŞ dönüyordu ve "boş" hiçbir şey ayırt ettirmez (anahtarda mı, belgede mi, zamanlamada mı?), iptal ise anahtarı yankıladığı için §7.3'ün "en olası aday" diye yazıp VARSAYMADIĞI şey artık ÖLÇÜLMÜŞTÜR: İzibiz e-Arşiv belgesini bizim uuid5 ile türettiğimiz İSTEMCİ ETTN'iyle ANAHTARLAMIYOR — aynı çağrıda `PENDING` + gerçek `INVOICE_ID` + `WEB_KEY` dönmesine rağmen belge o ETTN ile geri bulunamıyor; SORULACAK SORU YAZILDI ve BELGE KİMLİKLERİYLE birlikte kayda geçti (`docs/izibiz-sandbox-bulgular.md` §8.2, entegrasyon@izibiz.com.tr; VKN ve oturum jetonu İÇERMİYOR): `SNG2026518354588`/`405acba6-ce9c-59c7-bc79-c37e2f141939`, `SNG2026471382557`/`5266133a-7bf6-5e58-85ec-6a9854169dee`, `SNG2026589807117`/`70dfc021-3013-5443-81e7-53e081fbcc45`; PDF HÂLÂ İNMİYOR (`PDF_YOK`) ve E1'in ~100 sn'lik ölçümü YİNELENDİ — E2 bu ölçümü DEĞİŞTİRMEDİ, değiştirdiği tek şey PDF'i isteyen bir UCUN artık VAR olması; sandbox testleri `xfail(strict=False)` ile taşınıyor ki anahtar sorusu çözülünce XPASS olsunlar ve satırlar gözden geçirilsin; `ERROR_CODE=10008` SINIFLANDIRILMADI ve bu BİLİNÇLİ: mevcut sınıf kümesinde (AUTH/VALIDATION/TAXPAYER/DUPLICATE/QUOTA/NETWORK) "kayıt bulunamadı"nın karşılığı YOKTUR ve `VALIDATION` demek YANLIŞ olurdu — istek geçerliydi, aranan KAYIT yoktu, ve o etiket asıl bulguyu (anahtar yanlış) gizlerdi; doğru karşılık yeni bir sınıf (`NOT_FOUND`) olurdu ve o, spec §6 mesaj tablosunu da değiştirir, AYRI BİR DİLİMİN İŞİDİR ve burada SESSİZCE YAPILMADI — bugün kod `UNKNOWN` diyor, kullanıcı KODU görüyor, sağlayıcının cümlesi `raw` içinde denetime kalıyor ve BU EKSİKLİK BİR TESTE ÇİVİLENDİ (`NOT_FOUND` eklendiği gün o iddia kımıldar); BUGÜNKÜ DAVRANIŞ GÜVENLİDİR ve iptal yolu KALDIRILMADI: şemaya uygun istek kuruluyor, sağlayıcının reddi DOĞRU okunuyor (`FAILED`, `CANCELLED` DEĞİL) ve uç bunu 409'a çeviriyor — yani entegratör iptali kabul etmediği sürece ERP de faturayı iptal ETMİYOR; TESTLER `test_e2_ebelge_yasam_dongusu.py` (16, mock taşıma + statik sözleşme) + `test_e2_ebelge_yasam_dongusu_uc.py` (1 uçtan uca + 3 mutant) + `sandbox/test_e2_izibiz_iptal_sandbox.py` (2 geçti/3 xfail, GERÇEK sandbox, CI DIŞI); HİÇBİR TEST SOKET AÇMIYOR ve bu VARSAYILMIYOR ZORLANIYOR: `socket.socket` ve `socket.create_connection` patlayan tuzaklarla değiştiriliyor ve NÖBETÇİNİN KENDİSİ ayrı bir testle ölçülüyor; `urlopen`ı sahtelemek YANLIŞ NÖBETÇİ olurdu — `app/einvoice/transport.py` `urllib.request` DEĞİL `http.client.HTTPSConnection` kullanıyor, yalnız `urlopen`a bakan bir kapı bu dosyadaki HER çağrının ağa çıkmasına izin verirdi; ÜÇ MUTANT ve ÜÇÜ DE SAĞLAM KOŞUMUN GEÇTİĞİ SENARYONUN TA KENDİSİNDE ölüyor (ayrı bir senaryo yazmak "mutant başka bir testte ölüyor" demek olurdu): `YEREL_IPTAL_ONCE` (e-belge kapısı devre dışı ⇒ entegratör REDDETMİŞKEN fatura yerelde iptal olur), `INDIRME_KIRACISIZ` (`_invoice` firma yüklemini düşürür ⇒ BAŞKA firmanın resmî mali belgesi indirilir), `CANCELLED_FAILEDDEN` (`CANCELLABLE` FAILED'i kabul eder ⇒ hiç inmemiş bir gönderim "iptal edildi" diye yazılır ve meşru yeniden gönderim kapanır); ÜÇÜNCÜ MUTANT ÖNCE HAYATTA KALDI ve bu KAYDA GEÇİYOR çünkü DÜZELTİLEN ŞEY TESTTİ, KOD DEĞİL: (i) senaryoda FAILED durumda bir fatura İPTAL EDİLMİYORDU, yani mutantın dokunduğu yol HİÇ KOŞULMUYORDU — senaryoya "gönderimi inmemiş belge iptal edilir, yerel iptal NORMAL akar ama `einvoice_status` FAILED KALIR" adımı eklendi (ve `IPTAL_KABUL` bilerek AÇIK bırakıldı ki kapı yanlışlıkla çalışsaydı yakalansın), (ii) mutant `CANCELLABLE`ı YALNIZ `app/einvoice/status.py`de yeniden bağlıyordu, oysa router `from ..einvoice import CANCELLABLE` ile KENDİ adını bağlıyor ve modül özniteliğini değiştirmek o ada ULAŞMIYOR — tek yeri değiştiren bir mutant, kapının kapalı olduğunu değil yalnızca YANLIŞ YERE BAKILDIĞINI kanıtlardı; BAŞARI GÖVDESİ FIXTURE DİZİNİNE KONMADI ve bu bir sözleşme meselesidir: `tests/fixtures/izibiz/` altındaki her gövde GERÇEK bir yanıttır, sandbox bu iptali bugün kabul etmediği için kaydedilecek bir başarı yanıtı YOKTUR ve uydurup oraya koymak o dizinin anlamını bozardı — başarı gövdesi ŞEMADAN türetilmiş olarak TEST DOSYASINDA duruyor ve öyle etiketli, RED gövdesi ise GERÇEKTİR ve `tests/fixtures/izibiz/CancelEArchiveInvoice-fault.200.xml` olarak kaydedildi; KAPILAR: GET izin envanteri 185→186 ve parmak izi 22fcac03→445ebb4f (SIRA izlendi — uç yazıldı, `auth.py` kuralı eklendi, izin `required_permission` ile ÖLÇÜLDÜ, envantere girdi, sayım yeniden ölçüldü, EN SON parmak izi; `ROUTE_REASONS`a GİRMEDİ ve bu DOĞRU: o kapı yalnız `read`/public GET'leri için gerekçe ister), rota sözleşmesi 390/300→391/301 ve parmak izi 5cadacb3→f4517670 (YOL da arttı: `einvoice/download` var olan bir yolun yeni METODU değil YENİ BİR YOL), yetki uzlaştırması 378→379; `EXPECTED_READ` 99'da SABİT KALDI ve bu bir GET uç için ANLAMLIDIR — sayının KIMILDAMAMASI, `auth.py`ye yazılan açık kuralın GERÇEKTEN ÇALIŞTIĞININ TANIĞIDIR: kural olmasaydı uç genel güvenli-metot kuralından `read`e düşerdi ve sayaç 100 olurdu; `EXPECTED_UNDENIABLE` 114'te SABİT ("sales" çıplak `read` değildir, taşımayan roller VAR); `app/routers/invoices.py` dinamik-SQL parmak izi 7d1f2316→fc899635 ve DİNAMİK `text()` sayısı 2'de SABİT — eklenen tek ifade (`_einvoice_cancel_gate`in UPDATE'i) SABİT bir dizgedir (`ast.Constant`), f-string DEĞİL, o yüzden dinamik sayaca GİRMEZ; o ifade kiracı yüklemini AÇIKÇA taşıyor (`WHERE id=:id AND company_id=:cid`) ve bu `_invoice()`in zaten yaptığı kapsam denetiminin YERİNE değil YANINA geçen ikinci korumadır (E1'in `sync` UPDATE'iyle ve 0080'in `_sahip_mi` kaydıyla aynı gerekçe); `einvoice/download` ucu HİÇ SQL EKLEMEZ, okumasını var olan `_invoice()`ten yapar; `alt_surec_sql.txt` +1 (`test_e2_ebelge_yasam_dongusu_uc.py` 1) ve bu GERÇEKTİR, E1'deki gibi bir yorum yanlış-pozitifi DEĞİL: mutant betiği kiracı yüklemi düşürülmüş bir `SELECT`i BİLEREK gömüyor, çünkü mutasyonun konusu tam da o yüklemdir; `types.gen.ts` +77 satır (yeni uç OpenAPI'yi büyüttü; `openapi.json` `.gitignore`dadır, çivilenen tek üretilen dosya `types.gen.ts` ve `frontend/src/test/fixtures` KIMILDAMADI), alembic başı KIMILDAMADI ve `alembic/` diff'i BOŞ (GÖÇ YOK), `TENANT_TABLES` KIMILDAMADI, dışa aktarım KIMILDAMADI, kapalı aktivite kataloğu KIMILDAMADI (`log_invoice_action` serbest metin alır, `ACTION_TYPES` kapalı kümesi DEĞİL — E1'de ölçülmüştü, bu turda da geçerli), `pins/pg_twins.txt` KIMILDAMADI (PG ikizi EKLENMEDİ: bu dilim ŞEMAYA DOKUNMUYOR, eklenen davranışın tamamı yönlendirme ve sağlayıcı katmanındadır ve hiçbiri lehçeye bağlı değildir); DOKÜMAN: spec §5.1 (`CANCELLED`in tek yönlü diyagramı ve üç dışlamanın gerekçesi), §7.1 (B2B REDDİ AĞA ÇIKTIKTAN SONRA GELİR — ÖLÇÜLDÜ: reddedilen her B2B denemesi İzibiz'de BİR OTURUM + BİR `CheckUser` tüketiyor, `EInvoiceWS`e çağrı YOK; kapı öne alınamaz çünkü girdisi olan KANAL ağdan gelir, öne almak mükellefiyeti sormadan "bu muhtemelen B2B'dir" diye tahmin etmek olurdu), §9.3 güncellendi (PDF YOLU ARTIK VAR ama sandbox'ta PDF HÂLÂ KANITLANMADI — ikisi ayrı ayrı yazıldı ki karışmasın), §9.4 eklendi (e-Fatura iptali `ApplicationResponse` olmadan kapanamaz, ayrı dilim), bulgular §8 eklendi (10008 ölçümü + sorulacak soru); kayıt adı `python scripts/durum.py --sonraki 92`
