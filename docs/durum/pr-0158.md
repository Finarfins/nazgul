#158 — H83 hotfix: `backend/requirements.txt` `sqlalchemy>=2.0,<3` → `>=2.0,<2.1`; CI gevşek `requirements-dev.txt` kurduğu için 2026-09-20'den beri her dal SQLAlchemy 2.1.0 alıyordu ve 2.1.0'da `JSON().python_type` `object` döndüğünden (2.0.54: `dict`) `test_kiraci_disa_aktarim.py::test_str_dalina_dusen_sutun_tipi_yok` dört JSON sütununda (`supplier_import_profiles.column_map`/`page_sections`, `supplier_price_import_lines.raw`, `supplier_price_imports.error_detail`) kırmızıydı; `app/` içinde `python_type` dalı yok (`_seri` değerin tipine göre dallanır), kırılan yalnız testin H49 sınıflandırıcısı, bu yüzden kod düzeltmesi değil pin; `requirements.lock` dokunulmadı (2.0.51); koruma `test_requirements_lock.py::test_sqlalchemy_upper_bound_below_2_1` satırın tam `sqlalchemy>=2.0,<2.1` olmasını ister; kanıt: 2.1.0 venv hedef test kırmızı, pinli (2.0.54) yeşil, dışa aktarım + ci_gates + lock 49 geçti, PG ikizi taze DB'de 7 geçti 0 atlama; göç ve uç değişikliği yok; H84: 2.1.0 altında tam backend + PG-ikiz dökümü, sınıflandırıcıyı tipe bağlama, CI'ın `requirements.lock`'tan kurması.
