#63 — SATIŞ PARTİ TÜKETİR (FAZ 1B-B): 0067'nin FEFO seçicisine İLK ÇAĞIRAN bağlandı ve `product_lots` yazıcıları TEK MODÜLE toplandı; GÖÇ YOKTUR ve bu bir eksiklik değil KAPSAM kararıdır — 0067'nin tablosu ile 0073'ün deposu OLDUĞU GİBİ kullanıldı, tek sütun eklenmedi, dolayısıyla `alembic-chain` kapısı bu PR'da hiçbir şey ölçmüyor ve TABAN `develop@baadecf`tir (#56'nın birleşmesi); ÖLÇÜLEN KUSUR — 0067 seçiciyi ve depoyu kurup İKİSİNİ DE bağlamamıştı, 1B-A alışın parti AÇMASINI getirdi ama TÜKETMESİNİ getirmedi ve `test_fefo_sec_HALA_CAGIRANSIZ` bunu adıyla çiviliyordu, yani depo bir satışta hangi partinin çıktığını SÖYLEYEMİYOR ve geri çağırma kaydı kurulamıyordu; YAZICI TAŞINDI, ÇOĞALMADI: 1B-A'nın kapısı "defterin TEK yazıcısı transactions.py'dir" diyordu ve satış yolu İKİNCİ bir yönlendiriciden (workflow.py'nin irsaliyesi ve alış iadesi) parti tüketince o sınır TUTMADI, iki seçenek vardı ve kapıyı GENİŞLETEN reddedildi ("yazıcılar transactions.py VE workflow.py" demek defteri iki dosyadan yazılabilir yapardı ve `app/parti.py` başlığı o kusuru ADIYLA anıyor: "iki farklı yerden çıkan mal iki farklı partiden düşerse geri çağırma kaydı YALAN SÖYLER"), yerine `app/parti_defteri.py` kuruldu ve `_parti_ac` ile `_parti_geri_al` oraya VERBATIM taşındı, `_parti_tuket` ile `_hareket_notu` eklendi — kapı GENİŞLEMEDİ DARALDI çünkü yazıcı artık bir yönlendirici DEĞİLDİR ve yeni bir uç eklemek defteri kendiliğinden açmaz; MODÜL 1B-C İLE PAYLAŞILIYOR ve sözleşme ORTAK kararlaştırıldı (yazıcı YALNIZ parti_defteri.py, çağıranlar kapalı kümesi {transactions.py, workflow.py, products.py, warehouse_counts.py}, `fefo_sec`in tek çağıranı parti_defteri.py; ÖNCE İNEN modülü kurar, ikincisi rebase edip kendi fonksiyonlarını ekler — bu PR modülü kurdu ve çağıranlar kümesine products.py/warehouse_counts.py'yi ÖNCEDEN yazdı, küme bir ÜST SINIR olduğu için bugün hiçbir şeyi gevşetmiyor ama iki dalın aynı kapıyı ters yönde oynatmasını engelliyor); İKİ BAN KAPISI EMEKLİ EDİLDİ VE YERLERİNE ÜÇ DAHA DAR OLANI GELDİ — `test_product_lots_YAZICISI_YALNIZ_parti_defteri_py` (ad, düzyazı ve iddia BİRLİKTE hareket etti), `test_fefo_sec_CAGIRANI_YALNIZ_parti_defteri_py` ("hiç çağıran yok" yerine "TEK çağıran var ve adı belli", ayrıca çağıranın VARLIĞI da ölçülüyor çünkü kapı aksi halde "hiç çağıran yok" diye de yeşil kalabilirdi) ve YENİ BİR EKSEN olan `test_parti_defterini_CAGIRANLAR_kapali_kume` — üçüncüsü zorunluydu çünkü 1B-B'den sonra transactions.py ve workflow.py `product_lots` LİTERALİNİ HİÇ TAŞIMIYOR (fonksiyon çağırıyorlar), yani yalnız literal tarayan iki kapı ÜÇÜNCÜ bir yönlendiricinin defteri yazmaya başlamasını GÖREMEZDİ ve genişleme SESSİZ kalırdı; TÜKETİM ŞÖYLE BAĞLANDI: stoktan DÜŞEN her yol (satış, irsaliye, alış iadesi) FEFO ile parti tüketiyor ve koşul `kind` ADIYLA değil `stock`/`stock_sign` İŞARETİYLE yazıldı ki CONFIG'e üçüncü bir çıkış türü eklendiği gün ad listesi onu SESSİZCE dışarıda bırakmasın, dağıtımdaki her (lot_id, pay) çifti için AYRI hareket satırı yazılıyor (tek satıra toplamak hangi partiden ne kadar çıktığını GERİ ALINAMAZ yapardı ve geri alma zaten REFERANSTAN anahtarlandığı için N satır onu bozmuyor), workflow.py'nin güncelleme ve silme yollarına `_parti_geri_al` EKLENDİ çünkü o iki yol 1B-A'da parti borcunu HİÇ geri almıyordu, ve `allow_expired_lots` BELGE düzeyinde bir bayrak olarak eklendi (kalem düzeyinde değil: izin operatörün o belge için verdiği TEK karardır) ve izin hareket NOTUNA damgalanıyor — üç aday ölçüldü ve ikisi ADIYLA reddedildi (`policy_override_logs` bir POLİTİKA ihlali sözleşmesidir ve yönetici gerekçesi ZORUNLU kılar, oysa firma ayarlarında süresi geçmiş parti için bir politika sütunu YOKTUR; aktivite paneli ise BELGE düzeyindedir ve hangi SATIRIN izinli olduğunu söyleyemezdi); BRİFİNGDE ÇELİŞEN BİR YORUM KARARA BAĞLANDI — "süresi geçmiş → 422" ile "yetersiz → 409" ayrı maddeler olarak yazılmıştı ve çakışıyordu, seçilen okuma şudur: süresi geçmiş parti VARLIĞI satışı DURDURMAZ, taze partiler isteneni karşılıyorsa satış geçer ve bozuk maldan HİÇBİR ŞEY çıkmaz; 422 yalnız EKSİKLİĞİN SEBEBİ süresi geçmiş partiyse verilir, değilse 409 — gerekçe üç tanedir: (a) düz okuma rafında tek bir bozuk parti duran ürünün BÜTÜN satışlarını kilitlerdi, (b) `ParticiYetersiz.suresi_gecmis` alanını ULAŞILAMAZ yapardı oysa o alan tam olarak "yetmedi ÇÜNKÜ var olan mal süresi geçmiş" cümlesi için var, (c) korunan şey bozulmuyor çünkü süresi geçmiş mal bayrak AÇIKÇA yazılmadan çıkamıyor; ÜÇ ŞEY ÖLÇÜLDÜ, VARSAYILMADI — (1) `_parti_geri_al` satış için ÇOĞALTILMADI ve `_parti_iade` YAZILMADI çünkü `stock_movements.quantity` İŞARETLİ yazılıyor (alışta +, çıkışta -) ve parti deltası o işaretli sayının KENDİSİDİR, yani tek bir `quantity=quantity-:miktar` İKİ YÖNÜ DE çözüyor (satışta çıkarma EKLEMEYE dönüyor); bedeli sözleşmenin ÖRTÜK olmasıdır — işaret bir gün pozitife çevrilip yön `movement_type`a taşınırsa geri alma TERS çalışır ve hiçbir şey bağırmaz, o yüzden sözleşme `test_parti_geri_alma_YONU_HAREKETIN_ISARETINDEN_gelir` ile ADIYLA çivilendi, (2) EŞZAMANLILIK VARSAYIMI YANLIŞ ÇIKTI — brifing (ve ilk yazım) `adjust_warehouse_stock`un `warehouse_stocks` satırını `FOR UPDATE` ile kilitlediğini söylüyordu, ÖLÇÜLDÜ ki `app/inventory.py`de `with_for_update` HİÇ GEÇMİYOR ve koruma KORUMALI ATOMİK UPDATE'tir (koşul yazmanın kendi `WHERE`ünde durur, satır dönmezse çağıran reddeder), bu ayrım tembel bir ayrıntı değil çünkü İKİ AYRI DURUM üretiyor ve ikisi de PG ikizinde AYRI ölçülüyor: negatif stok BLOKLU iken stok UPDATE'i ikinci isteği zaten reddediyor ve parti katmanına SIRA GELMİYOR ([201, 409], ×20 tur), negatif stok SERBEST iken o koruma KAPALI ve tek koruma `_parti_tuket`in `quantity>=:pay` yüklemi — ayrıca o yüklemin KENDİSİ iki oturumlu ayrı bir PG kapısıyla ölçülüyor çünkü ÖLÇÜLDÜ ki HTTP'den HİÇ tetiklenmiyor (kaybeden istek partileri zaten tükenmiş buluyor), yani o kapı olmasaydı yüklem silinse İKİ yarış kapısı da YEŞİL KALIRDI, (3) SESSİZ BİR AYRIŞMA BULUNDU VE GÖRÜNÜR KILINDI — partileri TÜKENMİŞ bir ürünün satışı (negatif stok SERBESTKEN) `lot_id`si NULL bir harekete düşüyordu, yani stok -1 diyor defter 0 diyor ve o -1'in hangi partiden geldiği SORULAMIYOR; davranış brifingin kapsam sınırıdır (`quantity>0` satırlardan seçilir) ve KORUNDU ama artık SESSİZ DEĞİL: `Tuketim.defter_bosaldi` bayrağı "hiç parti satırı yok" ile "partiler tükenmiş" durumlarını AYIRIYOR ve ikincisinde hareket notuna `PARTI DEFTERI KARSILAMADI` damgası basılıyor; MUTASYON BATARYASI 9/9 KIRMIZI (LIFO, süresi geçmişe sessiz izin, N yerine tek hareket satırı, geri alma satış payını atlıyor, depo yüklemi düştü, kiracı yüklemi düştü, yarış yüklemi düştü, defter boşluğu damgasız, `fefo_sec` ikinci çağırandan) ve M5 İLK TURDA SESSİZ GEÇTİ — bu bir BULGUDUR ve kapının kendi kurulumundaki kusuruydu: depo kapısı komşu deponun partisini SÜRESİ GEÇMİŞ yazmıştı, yani yüklem düşse BİLE o parti SKT yüzünden zaten seçime giremiyordu ve kapı ölçtüğünü sandığı şeyi ÖLÇMÜYORDU, kurulum düzeltildi (komşu parti artık TAZE ve SKT'si DAHA ERKEN, yani yüklem düşerse FEFO onu TERCİH EDER) ve ikinci bir yarı eklendi (komşu deponun malı YETERLİLİĞE de sayılmaz); İĞNELER CPython 3.12.10 üzerinde YENİDEN ÖLÇÜLDÜ, aritmetikle devralınmadı: transactions.py dinamik `text()` 18de SABİT (taşınan altı SQL SABİT METİNDİ, dinamik yüzeye hiç girmiyorlardı) parmak izi d6f8a68a->b1547e6c, workflow.py dinamik `text()` 16da SABİT parmak izi b4183717->cacca635, alt süreç SQL sayacı 114/195 -> 115/196 (SQLite davranış ikizi, 1B-A'nın kalıbı; `app/` altında yine SIFIR), PG popülasyonu ÜÇ YERDE 106 -> 107 (`test_ci_gates`in ADI ve iddiası, `ci.yml`in `BEKLENEN_PG_DOSYA_SAYISI` sabiti, ve o sabiti SORAN test), core-query envanteri DEĞİŞMEDİ (yeni kod yalnız `text()` kullanıyor); UÇ SÖZLEŞMESİ değişti (`allow_expired_lots` iki şemaya girdi) ve zincir SONUNA KADAR koşturuldu — `export_openapi.py` -> `npm run types:gen` (types.gen.ts +10 satır) -> `npx tsc -b` TEMİZ, `capture_frontend_fixtures.py` fark üretmedi; KOŞULAR: SQLite davranış turu (11 bölüm) yeşil, PG ikizi GERÇEK PG16 üzerinde 10/10 yeşil ve iki kez üst üste koşturularak paylaşılan veritabanında IDEMPOTENT olduğu doğrulandı (koşu başına benzersiz son ek eklendi çünkü sabit parti kodu ikinci koşuda `MultipleResultsFound` ile düşüyordu — ÖLÇÜLDÜ), kapı süiti yeşil; TEK KIRMIZI `test_isolated_test_runner::test_serial_and_parallel_collect_and_pass_the_identical_test_set` ve bu değişiklikle İLGİSİZ OLDUĞU ölçüldü: aynı ağaçta değişiklik yapılmadan 1 kırmızı 1 yeşil verdi (sentetik geçici dosyalarda koşan bir kapı, `app`i hiç ithal etmiyor), tabanda 3/3 yeşil geldi yani dalgalanma bu dalda ORTAYA ÇIKTI ama ondan DOĞMADI ve not düşülüyor ki CI'da kırmızı olursa yeniden koşturmadan önce bu satır okunsun.
