#37 — FRONTEND ZAMAN BÜTÇESİ ÖLÇÜLEN SÜREYE GÖRE AÇILDI (`timeout-minutes: 12` → `20`), PR #36'nın e2e bütçesi ile AYNI ŞEKİL: yalnız bütçe, süre değil; TABAN `origin/develop` `24fcf52` (PR #31 birleşmesi; ölçüldü `git rev-parse HEAD` işe başlamadan); `deploy/ci-required-contexts.json` DOKUNULMADI (`git diff origin/develop --` o dosya için BOŞ) ve 17 bağlam olduğu gibi; `ci.yml` TEK HUNK (`git diff origin/develop -- .github/workflows/ci.yml | grep -c '^@@'` = 1) ve hunk YALNIZ `frontend` işinin `timeout-minutes` satırındadır (`e2e` 35, `container`/`verify-image-artifact` 12, başka iş YOK); ÖLÇÜLEN KUSUR: `frontend` işi `timeout-minutes: 12` ile (ci.yml ~:626) koşu 33854225188 deneme 1'de 12m15s'te öldürüldü ve GitHub bunu `cancelled` bildirdi — `npm ci` 7m03s sürdü; deneme 2'de `npm ci` 2m15s, iş 8m51s YEŞİL; PR #34 bu işe audit yeniden denemeleri eklemişti (3×30 s, yalnız ağ sınıfı) ve o ek ~90 s, tavanı 12 dk yapan şey DEĞİL; SON 30 TAMAMLANMIŞ CI KOŞUSU (`gh api repos/Finarfins/nazgul/actions/workflows/ci.yml/runs?per_page=40` → her koşu `.../runs/<id>/jobs`, `started_at`/`completed_at`/`conclusion`; in_progress 33859065506 dışarıda, 30. tamamlanan 33809895529; yeniden denenen koşularda denemeler `.../attempts/<n>/jobs` ile ayrı okundu): frontend süreleri min 1m25s, medyan 6m09s, maks 12m19s (tavanda kırpılmış); tamamlanan (success) denemelerde maks 11m51s (33846053464) — tavanın ~28 saniye altında; TAVANDA ÖLDÜRÜLEN 3 / 30 — 33856366292 (12m19s), 33854225188 deneme 1 (12m15s), 33853270963 (12m18s); GitHub timeout öldürmesini `failure` DEĞİL `cancelled` bildirdiği için bu üç arıza toplu görünümde iptal gibi görünüyordu; ÜÇ KOŞUNUN ADIM SÜRELERİ (`npm ci` / lint / tsc=`typecheck:e2e` / vitest=`npm test -- --run` / audit=GHSA politikası): 33854225188/1 7m03s / 25s / 3s / 1m24s(iptal) / 1m36s; 33854225188/2 2m15s / 24s / 3s / 2m47s / 1m35s; 33857237082 47s / 26s / 2s / 2m42s / 1m36s — baskın adım `npm ci` (47s–7m03s), lint/tsc/vitest/audit birlikte ~5 dk ve durağan; ARİTMETİK: gözlenen maks 12m19s tavanda kırpılmış (gerçek süre bilinmiyor, ≥ bu) → yukarı yuvarla 13 dk → 13 × 1.5 = 19.5 → 5'e yukarı yuvarla → 20; `backend/tests/test_ci_gates.py` yeşil (kayıt yazılırken koşulacak); ÖLÇÜLMEDİ, ADIYLA: **npm ci 2-7 dk varyansı — cache stratejisi ÖLÇÜLMEDİ** — `actions/setup-node` `cache: npm` + `cache-dependency-path: frontend/package-lock.json` zaten duruyor, soğuk/sıcak farkının NEDENİ (kayıt defteri, koşucu, hash kaçırma) bu PR'da ölçülmedi; bu PR yalnız bütçeyi ölçer, süreyi değil.
