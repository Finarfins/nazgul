#59 — KARANTİNA GEREKÇELERİ (yalnız test+docs): PR #57 21 `collect_ignore` adını çiviledi ve hepsini `GEREKÇESİZ` bıraktı; BU PR her adı YALNIZ koşturdu (`pytest <file> -x -q`, `pytest-timeout` YOK, `--timeout` YOK; GNU timeout yalnız güvenlik ağı, hiçbir dosya ona çarpmadı) ve İLK başarısızlık sınıfını ÖLÇTÜ, uydurmadı — TABAN `baadecf` (`origin/develop`); `conftest.py` DEĞİŞMEDİ, uygulama DEĞİŞMEDİ, `backend/LEGACY_TEST_MIGRATION_PLAN.md` hâlâ addan ARINIK (sınıf planı); gerekçe `docs/LEGACY_TEST_MIGRATION_PLAN.md` ek tablosunda (dosya · measured failure class · first error line · disposition); pin gerekçesi o tablonun class sütunu; boş / `GEREKÇESİZ` yeni ad KIRMIZI (`test_yeni_ad_gerekcesiz_kirmizi`); on dört `shutil.copy2(..., veriler.db)` dosyası ardışık koşumun ürettiği `veriler.db` silinip TEMİZ AĞAÇTA yeniden ölçüldü — ilk hata `FileNotFoundError` (`missing fixture`); üç import-anında API scripti (`test_detail_workflows.py`, `test_outputs.py`, `test_performance_filters.py`) `executable smoke script`; `test_e2e_browser.py` `ImportError` (`No module named 'playwright'`); `test_transaction_integrity.py` / `test_transaction_warehouse.py` pytest sarmalayıcısı içinde hâlâ smoke (`AUTH_REQUIRED` / çıplak `AssertionError`); BULGU: `test_v2_4_dashboard.py` YALNIZ koşunca `1 passed` — karantinadan ÇIKARILMADI; `delete` önerisi YOK; numara elle seçilmedi, `python scripts/durum.py --sonraki 59` verdi `docs/durum/0102-pr-0059.md`.
