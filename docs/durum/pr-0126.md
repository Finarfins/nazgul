#126 — PP2 PLATFORM YÖNETİM EYLEMLERİ + DENETİM (göç YOK, alembic başı 20260914_0085 SABİT): YEDİ yazma ucu `routers/platform_management.py`de, yedisi `require_platform_operator` arkasında, ara katman izni `__admin_only__` (ÖLÇÜLDÜ; PP1'in `read` kuralı yalnız güvenli metot, genişletilmedi) — firma aç/dondur (`companies.is_active`; dondurma YENİ KOD İSTEMEDİ: `resolve_company` zaten `is_active IS TRUE` birleştiriyor, üye 403 COMPANY_ACCESS_DENIED, testi eklendi), global kilit (`app_users.is_active` + access/refresh süpürgesi, kendini kilitleme 409), doğrulama yeniden gönder (kamu ucuyla aynı yol, doğrulanmış 409, IP başına 5/saat ayrı eylem adı), zorunlu parola rotasyonu (`must_change_password` + `logout-all`un üç süpürgesi), `DELETE /rate-limits?ip=` (yoksa 404), kuyruk retry (brif `FAILED -> PENDING` diyordu, kapalı geçiş tablosu İZİN VERMİYOR — `schedule_retry` gibi `FAILED -> RETRY_SCHEDULED`, tavan 500); her değişiklik TEK firmasız denetim satırı (7 yeni katalog kodu ≤ 20), değişmeyen çağrı `changed:false` ve satır YOK; `GET /api/platform/audit` süzgeçleri PP1'de yoktu, eklendi (username platform olayında `aktor=<id>;` eşler); PİNLER (TABAN `697a3be`): 412/319 -> 419/325, GET 199 SABİT, AUTHENTICATED 399 -> 406, READ 87 / GUARDED 31 / UNDENIABLE 97 SABİT, pg_twins 129 -> 130, Core envanter 206 -> 216, Core kiracı kapısı 167 -> 170 ifade ve lisans 11 -> 14 (üçü `platform_kuyruk_yeniden`), self-servis kapısı `/api/users` sınıfına iki platform yolu, yeni `text()` YOK, types.gen yeniden üretildi; TESTLER: `tests/test_pp2_platform_eylemleri.py` 40 (ileri/ters/2× karışık PP1 ile 92/92; mutasyon: retry kapısı, idempotensi, silah yüklemi, kilit süpürgesi dördü kırmızı), PG ikizi taze DB 3/3 + ikinci koşu 3/3 artık satırsız, tam izole paket 464 dosya (462 ilk koşuda; `test_self_service_exemption` pini düzeltildi, `test_isolated_test_runner` 4 işçi yükünde 180 s zaman aşımı — tek başına 25/25 yeşil); ŞEF KARARLARI (taban develop `4c3cda8`'e birleştirildi): `DELETE /rate-limits` aynı IP'nin `login_attempts` kilitlerini de siler (yanıt `{rate_limit_rows, login_attempt_rows}`, 404 yalnız ikisi 0 ise), `GET /api/platform/audit?actor_id=` (ara katmanın satır sonu `aktor=<id>` biçimi + platform olayının `aktor=<id>;` öneki; `aktor=1` `aktor=17`yi yakalamaz), son aktif yöneticiyi kilitlemek serbest ve denetim notu `son aktif yönetici firma=<id>` taşır, `FAILED -> RETRY_SCHEDULED` aynen, denetim yazıcısının istek oturumu H40'a; testler 44, Core envanter 216 -> 219, kiracı kapısı 170 -> 171 (lisans `list_untenanted_audit` yeni parmak izine), GET envanteri SABİT, PG ikizi taze DB 3/3; PP3 `PlatformSecurity.tsx` PP2'nin null'lı süzgeç tipini `usePlatformVerisi`ne veriyordu (TS2345, frontend/container/e2e kırmızı) — yalnız `limit` gönderir; tam izole paket `57ac594`te 464/465 (`test_v3_absorption_rate` 4 işçi yükünde cp1254 alt süreç çözümü, tek başına 3/3 yeşil; `test_isolated_test_runner` 104 s yeşil) — ve bu kayıt; kayıt adı `python scripts/durum.py --sonraki 126`
