#153 — H52/H53/H66/H68 İRSALİYE HİJYENİ (backend-only, göç YOK, rota envanteri SABİT, taban `5f6d77d`): H52 yılın `IRS<yıl>999999999`u verilmişken otomatik numara `belge_numarasi_uret`in `UblBuildError`ı yakalanmadığı için 500 veriyordu — artık `_belge_numarasi` içinde 409 `IRSALIYE_NUMARA_TUKENDI` (mesajda yıl), istek düştüğü için `document_sequences` ilerlemesi de geri alınıyor (SQLite ve PG'de sayaç satırı KALMIYOR ölçüldü), başka yıl etkilenmiyor; H53 elle `despatch_number` büyük harfe çevriliyordu ama HİÇ tekillik kontrolü yoktu (küçük harfli kopya 201) — ÖLÇÜLDÜ: şemada `(company_id, despatch_number)` UNIQUE'i YOK (0083/0087 yalnız `uq_despatch_notes_uuid` + `uq_despatch_notes_company_id`), hakem ön okuma: firmada tekrar 409 `IRSALIYE_NO_TEKRAR`, numara yılı `issue_date` (verilmemişse bugün) yılıyla uyuşmazsa 422 `IRSALIYE_NO_YIL_UYUMSUZ`, başka firmanın aynı numarası serbest; "alınmış mı" okuması oto yolla ORTAK tek sorgu ve `UPPER()` ile, yeni Core sorgusu YOK; H66 `_gorunum` `*_at` alanlarını `_utc(...).isoformat()` ile UTC ISO-8601 basıyor (`/response`un `response_received_at`iyle birebir; eskiden `str()`: SQLite'ta offset'siz boşluklu, PG'de boşluklu), `issue_date` `YYYY-MM-DD` kalıyor; `invoices.py::_einvoice_view` hâlâ `str()` — fatura sözleşmesi bu dilimin dışında, frontend'de bu alanları ayrıştıran tüketici yok, #143 zaten ISO bekliyor; H68 `GET /api/despatch-notes` öğeleri `lines_count` taşıyor: ilişkili alt sorgu (`company_id` eşli), sayfa başına tek sorgu (test sorgu sayısını 2'de çiviler), satırsız irsaliye 0, liste docstring'i genişledi ve `types.gen.ts` +4 satır yeniden üretildi, ikinci üretim bayt-aynı; PİNLER tarayıcı çıktısından: Core envanteri 233 -> 233, parmak izi `494f7b8b` -> `c3ac55e9` (`_belge_numarasi` "alınmış mı" girdisi değişti), kiracı bekçisi 191 SABİT, `pg_twins.txt` 137 -> 138 (yeni `test_h_irs_numara_zaman_postgresql.py`), rota 421/327, GET 201, AUTH 408, TENANT_TABLES 125, dinamik text() 270 DEĞİŞMEDİ; TESTLER: `test_e4b1_kismi_sevk.py` +7 sonuç, 28/28 (ileri ve ters sıra yeşil), yeni PG ikizi gerçek HTTP 4/4 (tükenme + sayaç geri alma, tekrar/yıl/başka firma, İstanbul oturumunda yazılan TIMESTAMPTZ'nin üç görünümde `/response` ile aynı ISO dizesi, `lines_count` kiracı kapsamı), taze PG veritabanlarında h51 12/12 e4a 10/10 e4b1 6/6 e4b2 7/7 skip YOK; MUTASYON: her düzeltme geri alınınca kendi testi KIRMIZI, SQLite 5/5 ve PG 5/5; tam izole süit 473/474, tek düşen `test_platform_backup_security.py` 5 sn kilit beklemesi (yük altında 2879 sn koşu), tek başına 12/12; REBASE `5f6d77d` (#146/#149/#145/#147): `backend/app` yaması bayt-aynı (patch-id `f03c8e06` önce=sonra), çakışma yalnız `pg_twins.txt` birleşimi; AÇIK: elle numara tekilliği PG'de FARKLI iki faturanın eşzamanlı isteğinde yarışa açık (kilit fatura başına) — kesin çözüm `UNIQUE(company_id, despatch_number)` göçü — <merge tarihi>
