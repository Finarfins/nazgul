#166 — H84 SQLALCHEMY 2.1 DENETİM RAPORU (dok, KOD YOK / GÖÇ YOK / PİN DEĞİŞMEDİ): yeni `docs/hijyen/H84-sqlalchemy-2-1-denetim.md`; ayrı karalama venv'inde SQLAlchemy 2.1.1 altında `run_isolated_tests.py` 494 dosyadan 493 PASS / 1 FAIL (4312 test, 1344.9 sn), tek kırmızı H83 sınıflandırıcısı `test_kiraci_disa_aktarim.py::test_str_dalina_dusen_sutun_tipi_yok` (2.1'de `sa.JSON().python_type` → `object`, 2.0.51'de `dict`; dört JSON sütunu "str dalına düşen" listesine giriyor), uygulama kodunda ölçülen davranış farkı yok; 2.1 değişiklik tablosu kod tabanına eşlendi (Row `._mapping`, `execute` dönüşleri, cursor olayı hata sarmalama etkisiz); önerilen sıra PR-1 sınıflandırıcıyı tipe bağla → PR-2 PG ikizlerini 2.1 altında ölç → PR-3 CI'ı lock'tan kur → PR-4 pini kaldır; pin kaldırma ölçütleri K1 (PR-1 birleşti + 2.1.x altında 0 kırmızı dosya, bugün 1), K2 (bütün PG ikizleri 2.1.x + psycopg 3 + PG16 taze DB'de yeşil, bugün ölçülmedi), K3 (PR-3 birleşti, CI lock'tan kuruyor), K4 (2.1 serisinde kod tabanını etkileyen açık gerileme yok); o zamana kadar `<2.1` pini doğru ve yeterli. — <merge tarihi>
