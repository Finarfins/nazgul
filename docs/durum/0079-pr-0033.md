#33 — `test_security_audit_visibility.py` `uygulama` fikstürü parent `sys.modules` içindeki her `app` / `app.*` anahtarını siliyordu (env taze import için); aynı süreçte kalan `app.main` / `app.routers.auth` referansları ve `sys.modules["app..."]` okuyan testler kırılıyordu; PR #30 üç kurbanı tamir etmişti, zehirleyici duruyordu; bu PR zehirleyiciyi kesti, kurban eklemedi, `backend/app` BOŞ, `test_tenant_scoping_guard.py` dokunulmadı, `test_*postgresql*.py` EKLENMEDİ, #30 tamirleri duruyor (`importlib.import_module` self-service, `bridge_mod._validate_archive` zip-bomb, `_yakala` zamanlayıcı); YALITIM importlib taze kopya takma ad `_denetim_kapi_uygulama` (subprocess değil — tenant-guard SQL literal sayacına dokunmamak için); parent `app` / `app.*` silinmedi; fikstür `DATABASE_URL` / `SECRET_KEY` / `BOOTSTRAP_ADMIN_PASSWORD` / `SUNGUR_PLATFORM_OPERATORS=1` / `SUNGUR_DATA_DIR` env’sini takma paket üzerinde kuruyor, yield hâlâ `(main, engine, client)`; mutasyon testleri `from app.X` yerine takma paketten `auth` / `db` / `config` alıyor; SENTINEL `test_uygulama_fiksturu_app_sys_modules_anahtarlarini_birakmaz` — parent `app*` kümesi önce / sırasında / sonra özdeş, takma artık yok; BLAST (orijinal zehirleyici, poisoner-first, plugin autoload açık, develop `caf41140`): 2 failed / 837 passed / 839 collected (386.85s) — `test_import_route_body_limit.py::test_app_registers_import_override_for_the_import_router` ve `::test_work_order_routes_keep_the_strict_global_body_limit` (`StopIteration` / `RequestBodyLimitMiddleware`); hedefli: `test_authorization_population_reconciliation.py` yedi `KeyError: 'app.routers.auth'` (satır 226), `test_field_outbox_writer.py::test_the_core_anchor_is_not_vacuous` kırmızı (`app.*` altında MetaData yok); autoload kapalı süpürme ~60 kırmızı (pytest-asyncio düşer) — o sayı kullanılmadı; SÜPÜRME SONRA (düzeltme) 840 passed / 0 failed (385.88s, +sentinel); İKİ YÖN: düzeltmeyi geri alınca body-limit iki `StopIteration` kırmızısı geri geldi (2 failed / 22 passed); dosya yalnız 11 → 12 passed (orijinal iddialar aynı); ÖLÇÜLMEDİ: Windows süpürme, yerel isolated dört-parça koşucu, A5 vacuous-yalnız (kurban; tetikte tam süpürmede yeşil çünkü başka dosya `app.main` yükler), bu PR’nin durum commit’inden sonraki CI 17 bağlam (önceki koşu `33814925428` durum-kaydi kırmızı — beklenen; frontend `AUDIT_TIMEOUT` npm audit 30s — bu backend test dosyasıyla ilgisiz).
