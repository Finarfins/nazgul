#112 — H18: e-posta doğrulama POST'u artık TOKENE GÖRE TEK KEZ atılıyor ve StrictMode'un çift efekti BAŞARILI bir doğrulamanın üstüne HATA basamıyor (BACKEND'E DOKUNULMADI — `git diff origin/develop HEAD -- backend` BOŞ, GÖÇ YOK, `types.gen.ts` DEĞİŞMEDİ, uç noktanın kendisi KIMILDAMADI; değişen tek şey İSTEMCİNİN o uca KAÇ KEZ gittiğidir); AÇIK ÖLÇÜLDÜ İDDİA EDİLMEDİ ve ölçen MERCEKTİR (#106 SEC-10 mercek turu): SEC-10 sonrası `frontend/src/pages/VerifyEmail.tsx` isteği `params` bağımlı bir `useEffect` içinden atıyor, `frontend/src/main.tsx:44` uygulamayı `<React.StrictMode>` altında kuruyor ve React 18 geliştirme modunda her efekti BİLEREK iki kez çağırıyor — doğrulama tokeni TEK KULLANIMLIK olduğu için ikinci POST **400** döner, `.catch` çalışır ve kullanıcı doğrulama BAŞARILI olmasına rağmen "E-posta doğrulanamadı." görür; KUSURUN ŞEKLİ BURADA: hata GERÇEK BİR BAŞARININ ÜSTÜNE yazılır, yani kullanıcı işe yaramış bir işlemi başarısız sanır ve tokeni bir daha kullanamayacağı için (harcanmıştır) tekrar denemesi de İŞE YARAMAZ, sayfayı yenilemek durumu DÜZELTMEZ SABİTLER; ÜRETİMDE ÇİFT ÇAĞRI YOKTUR ve bu SUSULMUYOR ADIYLA YAZILIYOR — StrictMode'un ikilemesi yalnız geliştirme derlemesinde olur, yani üretimde kullanıcı bunu bu yoldan GÖRMEZ; ama düzeltme yine de gereklidir çünkü YARIŞ TEK BAŞINA StrictMode'a AİT DEĞİLDİR: efekt `[params]`e bağlıdır ve `useSearchParams`in döndürdüğü nesnenin KİMLİĞİ değiştiği her render'da efekt yeniden koşar, yani aynı ikinci-POST'u üreten ikinci bir yol AÇIKTIR ve o yol üretimde de açıktır — geliştirme kipi kusuru YARATMAZ, GÖRÜNÜR KILAR; İKİNCİ ÖLÇÜLEN BOŞLUK: sayfanın HİÇ BİRİM TESTİ YOKTU, yani ne mevcut davranış ne de düzeltmesi herhangi bir kapının arkasındaydı — `frontend/src/pages/` altında 90 test dosyası var ve `VerifyEmail` bunların hiçbirinde geçmiyordu; ÇARE tüketilen tokeni `useRef` içinde tutmaktır (`const consumed=useRef<string|null>(null)`; `if(consumed.current===token)return; consumed.current=token;`) ve `useRef` SEÇİMİ KEYFİ DEĞİLDİR: React 18 StrictMode simüle ettiği unmount/remount turunda BİLEŞENİN STATE VE REF'LERİNİ KORUR, yani ref ikinci efekt çağrısında DOLU gelir ve koruma tam olarak ölçülen yarışın üstüne oturur — `useState` ile kurulan bir bayrak fazladan bir render turu getirir ve ilk efektin ATIŞI ile bayrağın GÖRÜNÜRLÜĞÜ arasında aynı yarışı yeniden açardı, bir modül düzeyi değişken ise test dosyaları arasında SIZINTI olurdu; koruma TOKENE GÖREdir (boolean değil) çünkü kullanıcı BAŞKA bir doğrulama bağlantısına tıkladığında `params` değişir ve YENİ tokenin isteği ATILMALIDIR — düz bir "bir kez koştu" bayrağı o meşru ikinci doğrulamayı SESSİZCE YUTARDI ve bu, düzelttiğimizden daha kötü bir kusur olurdu; İSTEK GÖVDESİ, BAŞARI VE HATA METİNLERİ, YERLEŞİM ve "token yok" DALI AYNEN KORUNDU — bu bir davranış değişikliği değil bir TEKRAR BASTIRMADIR, `api.post('/auth/verify-email',{token})` çağrısı harfi harfine aynıdır; KAPI `frontend/src/pages/VerifyEmail.test.tsx` (YENİ, 3 test) ve depo standardı kalıpla kuruldu (`vi.hoisted` + `vi.mock('../api')` + dinamik `import()`, `ForgotPassword.test.tsx` ile AYNI iskelet — kalıp İCAT EDİLMEDİ); TEST KURULUMU KUSURUN ŞEKLİNİ TAŞIR: bileşen testte de `<React.StrictMode>` ALTINDA kurulur, yani kapı çift efekti SİMÜLE ETMEZ GERÇEKTEN KOŞAR — StrictMode sarmalayıcısı kaldırılırsa kapı ölçtüğü şeyi kaybeder ve bu yüzden testin gerekçesine `main.tsx:44` referansıyla yazıldı; (1) `?token=abc` ile TEK `api.post('/auth/verify-email',{token:'abc'})` ve başarı mesajı ekranda, ARDINDAN aynı token'la yeniden render edilip çağrı sayısının HÂLÂ 1 olduğu ölçülür — ikinci yarış yolu (`params` kimliği) da böylece çivilenir, yalnız StrictMode değil; (2) API **400** → "E-posta doğrulanamadı." metni görünür ve çağrı YİNE TEK — hata dalının kendisi korunmalıdır (gerçek bir bayat token gerçekten hata göstermelidir), kapı bunu doğrular; (3) token YOK → HİÇ POST atılmaz ve "Doğrulama bağlantısında token bulunamadı." basılır — bu dal düzeltmeden ETKİLENMEDİĞİ için çivilendi, gelecekte bir refactor onu ref korumasının içine düşürüp erken dönüşü kaybederse kapı kırmızı olur; MUTASYON KOŞULDU İDDİA EDİLMEDİ: `VerifyEmail.tsx`ten `if(consumed.current===token)return;` TEK SATIRI çıkarıldı ve İKİ test kırmızı oldu — `StrictMode çift çağrısına rağmen tokeni tek kez harcar` ve `sunucu 400 dönerse hata metnini basar ve isteği tekrarlamaz`, ikisi de AYNI iletiyle: `AssertionError: expected "vi.fn()" to be called 1 times, but got 2 times`; yani mutant TAM OLARAK ölçülen açığı geri getirir ve kapı onu iletiyle birlikte yakalar; satır geri konunca 3/3 yeşil; mutasyon sırasında COMMIT YAPILMADI ve kaynak `cp` ile geri yazıldı (in-place mutasyon paralel koşuda güvenli değildir, tek dosya tek koşu olarak yapıldı); KOŞU `npm ci && npm test -- --run` → **90 dosya / 631 test YEŞİL** ve `npm run build` → yeşil; TİP KONTROLÜ AYRICA KOŞULMADI ve bu bir atlama DEĞİL ölçülmüş bir gözlemdir: `frontend/package.json` `build` betiği `tsc -b && vite build`tir, yani `tsc -b` derlemenin İÇİNDE koştu — `tsc --noEmit -p tsconfig.json` bu depoda SIFIR dosya denetleyen BOŞ BİR YEŞİLDİR ve kasten kullanılmadı; DEĞİŞEN DOSYALAR: `frontend/src/pages/VerifyEmail.tsx` (+8/-1) ve `frontend/src/pages/VerifyEmail.test.tsx` (yeni) — artı bu kayıt; kayıt adı `python scripts/durum.py --sonraki 112`
