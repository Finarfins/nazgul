#46 — KANTAR NETİ STOĞA (C2): kantar fişi artık stok defterini oynatıyor, ama SENKRON DEĞİL ve bir MİKTAR değil bir FARK yazarak; TABAN `9e02fb8` (`origin/develop`, PR #44 kiracı dışa aktarımı indikten SONRA), ayrı worktree, `F:\nazgul` İÇİNDE ÇALIŞILMADI; kayıt numarası ELLE SEÇİLMEDİ: `python scripts/durum.py --sonraki 46` → `0091-pr-0046.md` (D1 `feat/mustahsil-makbuzu` bu turda AÇILMAMIŞTI — `gh pr list` boş, `git ls-remote --heads origin` içinde ne kantar ne müstahsil dalı vardı); GÖÇ YOK ve gerekmedi — yeni terminal kova sabiti 27 karakter, `field_integration_events.status` göç 0061'den beri `VARCHAR(64)` (ÖLÇÜLDÜ, varsayılmadı); D1'in dosyalarına (`purchases`, `transactions.py`, `finance_engine.py`, `document_engine.py`) TEK SATIR DOKUNULMADI; **NE DEĞİŞTİ**: `POST /api/field-harvest-tickets` fiş başına TAM BİR outbox olayı yazıyor (kaynak tipi `field_harvest_ticket`, anahtar `field_harvest_ticket:<fiş id>:stock`), fiş ve kesintileriyle AYNI işlemde ve olay KESİNTİLERDEN SONRA — POST'un kendisi `stock_movements`a hiçbir satır yazmıyor; **İDDİA DARALDI, SİLİNMEDİ**: C1'in "fiş defteri HİÇ oynatmaz"ı bugün "fiş yazımı SENKRON yolda hiçbir hareketi değiştirmez", ve senkron bağlamak HÂLÂ yanlış olurdu çünkü fiş ile hasat aynı işlemde değil — POST içinde yazılan bir düzeltmenin düzelteceği satır (hasadın kendi olayının hareketi) henüz ortada olmayabilir, sıra bu yüzden outbox'a bırakıldı; **FORMÜL**: `delta = Σ(fiş netleri, TABAN birimde) − hasadın TABAN miktarı − BU HASAT için daha önce yazılmış fiş düzeltmeleri`, üçüncü terim olmadan ikinci fiş birinciyi TEKRAR sayar (mutasyonla ölçüldü: terim düşürülünce delta 500 yerine 1200); fark sıfırsa olay `SENT` biter ve SATIR YAZILMAZ (`_FARK_KAYNAKLARI`); hareket ASLA UPDATE/DELETE edilmiyor — düzeltme YENİ BİR SATIRDIR (sahip kuralı 1) ve bu `test_tuketici_stok_hareketini_ASLA_guncellemez_silmez` statik kapısıyla çivilendi; net formülü KOPYALANMADI, `routers.farm._turetilmis_net` İTHAL EDİLİYOR (okuma yüzeyinin `derived_net_quantity`si ile defter aynı formülden türüyor; ters yönde içe bağ OLMADIĞI ölçüldü, döngü yok); ÖLÇÜLEN VE TESTE YAZILAN AYRIM: okuma yüzeyinin neti GİRİLEN birimde (`1.7000` TON), defterinki TABAN birimde (`1700` KG) — aynı formül, ayrı ölçek, ikisi yan yana çivilendi; hareket olaya `reference_id` ile bağlı ve kaynağa DEĞİL, bu yüzden `already_adjusted` toplamı `field_integration_events` ÜZERİNDEN geçiyor (ölçüldü: başka yol yok), süzgeç `source_type='field_harvest_ticket'` çünkü hasadın KENDİ olayının hareketi bu toplama girseydi hasat miktarı iki kez çıkarılırdı; **MEVCUT HASAT YOLUNDAKİ 1000× RİSKİ KAPATILDI**: `_hasat_kalemleri` `field_harvests.quantity`yi HAM yazıyordu ve hareket defteri birim TAŞIMADIĞI için miktarın ürünün TABAN biriminde olduğu VARSAYILIYORDU — `unit="ton"` giren bir hasat için 1000× sapma, hatasız ve sadece yanlış bir CEVAP; artık `units.resolve` uygulanıyor, bayrak üretimde HİÇ AÇILMADIĞI için CANLI VERİ YOK (bu bir düzeltme değil bir KAPI) ve `"ton"`/`"kg"` ile teste çivilendi; **YENİ TERMİNAL KOVA** `SKIPPED_TABAN_BILDIRILMEMIS` (4 → 5): ürün BELLİ ama `products.base_unit` yok; `SKIPPED_NO_PRODUCT` ile BİRLEŞTİRİLMEDİ çünkü ÇARELERİ FARKLI (biri sezona ürün bildirmek, öteki ürün kartına taban birim yazmak) ve tek kova `last_error` okuyanı hangi kaydı düzelteceğini bilmeden bırakırdı; kova okuma yüzeyinin `failed_only` kümesine de eklendi (`_TARLA_BASARISIZ` 3 → 4) — eklenmeseydi olay YEŞİL bir kapıdan geçerek görünmez olurdu, yani yüzeyin kapatmak için var olduğu kusurun ta kendisi; `BirimCozulemedi` ailesi genel `except` kolundan ÖNCE yakalanıyor (yoksa red `RECOVERY_*`/yeniden deneme yoluna düşer ve üç deneme sonunda `DEAD` olurdu — oysa taban birimin bildirilmemesi bir ARIZA değil bir VERİ OLGUSUDUR ve denemek onu düzeltmez), `UrunTemsilEdilemez` → `DEAD`; HER KOL KENDİ SABİTİNİ DÖNDÜRÜYOR ve bu ÖLÇÜLEREK öğrenildi: ilk yazımda ortak bir `return kova` vardı ve `test_field_stok_korunum_denklemi` onu `AttributeError: module has no attribute 'kova'` ile KIRDI — korunum denklemi dönüş ADLARINI statik çözüyor, yerel bir ad kovayı denklemin GÖRÜŞ ALANINDAN çıkarırdı; **EK, KAPSAM İÇİ** `POST /api/products` taban birimi YAZIYOR: `ProductCreate` `ProductUpdate`ten türediği için `base_unit` gövdede KABUL EDİLİYOR ama INSERT'in sütun listesinde YOKTU ve değer SESSİZCE düşüyordu — istemci 201 alıyor, kart taban birimsiz doğuyor ve o ürünün İLK kantar fişi `TABAN_BILDIRILMEMIS` ile reddediliyordu; kurallar PUT ile AYNI (`turkce_katla` ile kanonikleştirme, açık `null` ve katlanınca boşa düşen dizgi → 422 `TABAN_BIRIM_SILINEMEZ`), alan HİÇ gönderilmezse sütun NULL kalır ve `unit`ten KOPYALANMAZ (kopyalansaydı `test_kantar_fisi_sozlesme.py`nin taban-birim bacağı SESSİZCE ölürdü — o test yeşil kaldı, doğrulandı); etkinlik kaydı YAZILMADI ve bu PUT'tan bilinçli farkı: PUT bir DEĞİŞİKLİKTİR ("kim, ne zaman değiştirdi" sorulabilir olmalı), oluşturmada önceki bir değer YOKTUR; **KAPI EMEKLİLİĞİ**: `test_tuketici_fisin_adini_bile_gecirmiyor` SİLİNDİ — C1'in KAPSAM ÇİTİYDİ ("bu dilim tüketiciye dokunmuyor", `field_stok_tuketici` fiş tablolarının ADINI bile geçirmesin) ve C2 tam olarak o işi yaptığı için TASARIM GEREĞİ obsolet; yerine bir şey KONMADI çünkü çitin koruduğu şey (yanlışlıkla `LEFT JOIN field_harvest_tickets` eklenmesi) artık farkın SAYISIYLA ölçülüyor, bir adın geçip geçmediğiyle değil; tek kullanıcısı o kapı olan `FIS_TABLOLARI` sabiti de kaldırıldı; `test_fis_yazimi_outbox_olayi_uretmiyor` → `test_fis_yazimi_TAM_BIR_outbox_olayi_uretir` (yön değişti ama kapı KALDIRILMADI, ve `in cagrilar` yerine `== 1` çünkü asıl risk çağrının VARLIĞI değil SAYISI: iki çağrı aynı fiş için iki düzeltme olayı doğurur ve farkı İKİ KEZ uygular); **ÖLÇÜM**: `python run_isolated_tests.py --workers 4` → `Tüm 385 aktif test dosyası geçti; 2030 test sonucu kaydedildi. Toplam süre: 633.7s`, çıkış 0 (CPython 3.12.10, ayrı venv — depo 3.11 sistem Python'ıyla gelmiyor); yeni `backend/tests/test_kantar_neti_stoga.py` SEKİZ senaryo ve aritmetiği BAŞLIKTA açık açık: (1) hasat 1000 KG fişsiz → +1000, (2) fiş 2 TON %10+%5 → taban 2000, net 1700, delta **+700**, (3) ikinci fiş 500 KG %0 → netler 2200, zaten 700, delta **+500** ve DEFTER TOPLAMI 2200 = fişlerin neti, (4) aynı olay ELLE `PENDING`e çekilip yeniden tüketiliyor (at-least-once taklidi) → delta 0, SATIR YOK, (5) `base_unit` NULL → `SKIPPED_TABAN_BILDIRILMEMIS` ve 0 satır (fiş ucu da 422 veriyor — outbox yolu ile etkileşimli yolun AYNI olguyu reddettiği yan yana), (6) hasat 2 `"ton"` taban `"kg"` → 2000 (ham yazılsaydı 2 çıkardı), (7) A'nın bekleyen fiş olayı B için koşturulan tüketicide `girdi=0` ve B'nin defteri SIFIR, (8) bayrak KAPALI → olay `PENDING`, hareket YOK; PG vakası MEVCUT `backend/test_kantar_fisi_postgresql.py`e EKLENDİ, YENİ `*_postgresql.py` dosyası AÇILMADI (PG popülasyonu üç yerde 102'ye çivili ve bu dilim onu oynatmıyor) — bunun için `_smoke` yardımcısının `test_kantar_fisi_{dosya}` KALIBI gevşetilip TAM AD alacak hâle getirildi, çünkü C2'nin dosyası kalıba uymuyor ve kalıbı zorlamak dosyayı yanlış ada göre adlandırmak olurdu; ALTI MUTASYON KIRMIZI ÖLÇÜLDÜ ve her biri ADIYLA kırıldı: `already_adjusted` düşürüldü → `IKINCI FIS BIRINCIYI TEKRAR SAYDI`, hasat miktarı HAM yazıldı → `HASAT MIKTARI TABAN BIRIME CEVRILMEDI (1000x riski)`, sıfır fark satırı yazıldı → tekrar teslim ikinci satır yazdı, kova `SKIPPED_NO_PRODUCT`a çevrildi → kova sayımı, kesintiler SIRALI hesaplandı → delta 700 ≠ 710, outbox olayı kaldırıldı → `Fiş yazımı outbox yazıcısını 0 kez çağırıyor`; hepsi geri alınınca yeşil; **İĞNELER, hepsi YENİDEN ÖLÇÜLDÜ (hiçbiri devralınmadı)**: `app/routers/farm.py` parmak izi `d1541360`→`46734f13` (dinamik `text()` sayısı 15'te SABİT, YENİ dinamik SQL YOK — değişen tek SQL zaten gözden geçirilmiş ve SABİT metinli `_entegrasyon_olayi_yaz`ın ikinci bir yerden ÇAĞRILMASI, `company_id` yine açık sütun olarak `:cid` ile bağlı; gerisi saf Python ve yorum: dilim başlığı ile `create_harvest_ticket` docstring'i daraltılmış iddiaya taşındı, parmak izi kımıldadı çünkü dosya AST'sinin TAMAMINDAN türüyor), `app/routers/products.py` `99e57d37`→`5cd5fdfd` (dinamik 6'da SABİT; sabit INSERT metnine `base_unit` sütunu ve `:base_unit` yer tutucusu, değer BAĞLI PARAMETRE ve metne girmiyor, kök yüklem değişmedi), `app/routers/entegrasyon_olaylari.py` `a2c5ed3d`→`644dd6ad` (dinamik 3'te SABİT ve HİÇBİR SQL METNİ DEĞİŞMEDİ — değişen tek şey `_TARLA_BASARISIZ` demeti, değerleri `:kova0..` ile BAĞLI PARAMETRE olarak gidiyor); alt süreç SQL sayacı 107/171 → **108/172** (tek yeni test dosyası; `app/` ÜRETİM yüzeyi BÜYÜMEDİ); `test_field_stok_tuketici.py` `BEKLENEN_TERMINAL_DURUMLAR` 4 → 5 ve `test_entegrasyon_olaylari_api.py` `BEKLENEN_BASARISIZ_KOVALAR` 3 → 4; `test_field_stok_zamanlayici.py`de üç sayaç sözlüğüne yeni kova eklendi; `field_stok_tuketici.py` dinamik-SQL izin listesine GİRMEDİ çünkü eklenen sorguların HEPSİ sabit metin (kiracı kapsamlı, birleştirmeler kiracı İÇİNDE); Core envanteri (`test_core_query_inventory`) DEĞİŞMEDİ — bu dilim hiç Core sorgusu eklemedi; contract-drift zinciri KOŞULMADI ve gerekçesi ölçüldü: OpenAPI değişmedi, `base_unit` ZATEN `ProductCreate` şemasındaydı (yalnız yazılmıyordu), yani `types.gen.ts` türetmesinin görebileceği bir fark yok; `deploy/ci-required-contexts.json` ve `.github/workflows/ci.yml` DOKUNULMADI; **BEKLENEN FALLOUT ve nasıl karşılandı**: `test_field_stok_tuketici.py`nin sentetik ürünleri `base_unit`siz kuruluyordu ve hasat yolu artık çevirdiği için `SKIPPED_TABAN_BILDIRILMEMIS` kovasına düşüyorlardı — kurulum ürüne `base_unit='KG'` verildi (`unit`le AYNI, yani katsayı 1 ve o dosyanın ölçtüğü sayılar DEĞİŞMEDİ), taban birim UYDURULARAK değil o dosyanın konusu (yön ve kova sayımı) korunarak; `test_kantar_fisi_defter.py` beş senaryosu "SENKRON" cümlesine yeniden yazıldı ve hasat satırları ile fiş düzeltmesi notlarına göre AYRILDI (fişli/fişsiz hasadın satırı hâlâ alan alan AYNI — fiş, hasadın satırını GERİYE DÖNÜP DEĞİŞTİRMEDİ, düzeltme AYRI bir satır); `backend/app/FIELD_STOK_OUTBOX_ACILIS_KOSULLARI.md`ye iki OLGU satırı eklendi (kuyrukta artık fiş olayları da var, birikim ölçümü üç kaynak tipini kapsıyor; yeni terminal kova ve açmadan önce `base_unit IS NULL` ürün sayımı) — açılış KOŞULU eklenmedi, yalnız ölçülmüş gerçekler; **ÖLÇÜLMEDİ, ADIYLA**: (1) ÜRETİM BAYRAĞI HÂLÂ KAPALI (`field_stock_outbox_enabled=False`) — bu dal onu açmıyor ve birikmiş kuyruğun gerçek boşalması ölçülmedi; (2) İKİ TÜKETİCİ İŞÇİSİNİN AYNI HASAT ÜZERİNDE EŞZAMANLILIĞI — tek olayın çift uygulanması `uq_stock_movements_field_event` kısmi indeksiyle veritabanında kapalı, ama iki FARKLI fiş olayının aynı anda `already_adjusted` okuması ölçülmedi ve teorik sonuç ikisinin de ESKİ toplamı görüp fazla düzeltme yazmasıdır; bayrak kapalı olduğu için üretimde erişilebilir değil, açılmadan önce ölçülmeli; (3) PostgreSQL ikizi bu koşumda SKIP (`APP_TEST_DATABASE_URL` yok, 16 test skipped) — PG vakası YAZILDI ama gerçek PG'de KOŞTURULMADI, yani `NUMERIC(18,4)` ölçek dayatması, kısmi indeksin gerçekten ısırması ve çapraz kiracı bileşik yabancı anahtarları bu turda SQLite'ta kaldı; (4) fiş SİLME/DÜZELTME yolu YOK (uç zaten yok), silinen bir fişin düzeltmesinin nasıl geri alınacağı tasarlanmadı; (5) çok sayıda fişi olan bir hasatta `_fis_kalemleri`nin okuma maliyeti ölçülmedi (hasat başına tüm fişler + kesintileri her olayda yeniden okunuyor).
