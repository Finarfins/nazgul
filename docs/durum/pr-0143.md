#143 — E4b-3 KISMİ SEVK ARAYÜZÜ: E4b-1 (#127) ve E4b-2 (#140) arka ucunun ÖN YÜZÜ, YALNIZ `frontend/src/pages/DespatchNotePanel.tsx` (+2 test dosyası) — arka uç dosyası ve göç YOK, `types.gen.ts` taze `openapi.json`dan yeniden üretildi ve diff 0 SATIR, YENİ ROTA/SAYFA YOK (`ROTA_ENVANTERI` pini önce 80 / sonra 80, 13/13 yeşil; `navigation.consistency` 36/36); DURUM BİRLİĞİ 8 -> 11: ACCEPTED (success) / PARTIALLY_ACCEPTED (warning, çünkü iş BİTTİ ama EKSİK bitti — yeşil reddedilen satırı görünmez kılardı, kırmızı alınan malı yok sayardı) / REJECTED (error) eklendi, DELIVERED etiketi 'teslim edildi — yanıt bekleniyor' oldu (E4b-2'de SON DURAK DEĞİL; eski 'alındı' kabul edilmiş gibi okunuyordu), `EDESPATCH_STATUSES` birliğin ÇALIŞMA ZAMANI karşılığı ve test onu `edespatch.py::BILINEN`in ON BİR adına çakıyor — şema `edespatch_status`ı düz `string` ürettiği için (`types.gen.ts`te numaralandırma YOK) kapı BURADA, her ad için ayrıca 'adı konmuş rozet' testi var (sözlükte olmayan durum `NONE`a düşerdi: 'gönderilmedi' yazan bir REDDEDİLDİ); KISMİ SEVK DİYALOGU: `GET /api/invoices/{id}/despatchable-items` ile satır satır miktar (varsayılan = kalan, max = kalan, adım 0.0001), gövdeye `decimalPayload` ile DECIMAL DİZESİ gider `Number` DEĞİL (dört haneli ölçek float'a çevrilseydi ikili kalıntı sunucunun kalan hesabına sızardı), sıfır/boş satır GÖNDERİLMEZ, hizmet kalemi süzgeci SUNUCUDA kalır (arayüz ikinci kural yazmıyor), şoför/plaka alanları düz sevkle AYNI diyalogda (iki kopya iki kez kayardı) ve düz sevkte `lines` HİÇ gönderilmez (gövdesiz istek = E4a davranışı); HATA EŞLEMESİ KODA BAKAR, METNE DEĞİL: SEVK_MIKTAR_ASIMI / HIZMET_SATIRI_SEVK_EDILMEZ / FATURA_KALEMI_YOK / SEVK_SATIRI_TEKRAR `apiDetail(...)`in `invoice_item_id`siyle SUÇLU SATIRIN altına düşer (aşımda `remaining` de yazılır), SEVK_KALEMI_YOK istemcide de kapalı (gereksiz bir 422 turu), 409 IRSALIYE_TAMAMLANDI toast + tazeleme (istek GEÇERLİ, değişen KAYNAĞIN DURUMU — diyalogda kırmızı satır yanlış olurdu), `complete` olduğunda İKİ BUTON DA kapalı ve ipucu 'Tüm kalemler sevk edildi' (kapalı ama sessiz buton 'bozuk' diye okunur); ÇOKLU İRSALİYE: E4a'nın `note?.` varsayımı düştü (göç 0087 UNIQUE'i kaldırdı), her satır kendi numarası/tarihi/rozeti/satır sayısı ve KENDİ submit/sync/XML eylemleriyle, satır sayısı için irsaliye başına detay isteği atılıyor (liste ucu satır TAŞIMIYOR; sınır FATURA BAŞINA irsaliye sayısı, sayfa boyu değil); YANIT BÖLMESİ `can('sales')` kapısında (uç `sales` istiyor, depo/rapor 403 alırdı — görünüp hata veren bölme yerine GÖRÜNMEYEN bölme, depo testinde `/response` isteği HİÇ atılmıyor), yanıt türü rozeti + tarih + not + satır tablosu (sevk/alınan/reddedilen/gerekçe) ve REDDEDİLEN SATIR VURGULU, zımni kabul YALNIZ yanıt yokken (yanıt geldikten sonra 'hâlâ bekleniyor' diye okunurdu), `responses_count`>1 için not, istek bölme AÇILINCA atılıyor (peşin sorulsa liste N+1 olurdu); SYNC sonrası liste VE o irsaliyenin yanıtı tazeleniyor ve `changed`/`recorded`/`skipped` toast'ta — `submit`te tazeleme YOK (yanıt gövdesi zaten kaydın kendisi, kalanlara dokunmuyor); tazelemeler SESSİZ (tam ekran iplikçik az önce basılan uyarıyı silerdi); biçimler MEVCUT yardımcılardan: miktar `utils/documentMoney.quantityDecimal(...).toFixed()` (`PartiMutabakati` ile AYNI, sondaki sıfırları taşımaz), tarih `ReceivablesAging.formatDate` (`Transactions` ile AYNI), sunucu mesajları `errorDetail(...)`; TESTLER: mevcut `DespatchNotePanel.test.tsx` 20/20 (sahte `api.get` artık URL'e BAKIYOR — liste + `despatchable-items` + detay üç ucu birden yanlış beslenemez; `AuthContext` sahtesi eklendi), yeni `DespatchNotePanel.kismi.test.tsx` 26/26 (yalnız sevk edilebilir kalemler, gövde BİREBİR dize miktarlarla ve sıfır satır YOK, aşım mesajı DOĞRU satırda ve temiz satır temiz, 409 toast + tazeleme, tamamlandığında iki buton kapalı, çoklu irsaliye, KISMI_KABUL satırları `data-redli` ile, yanıt varken zımni kabul GİZLİ, depo bölmeyi görmüyor, sync özeti + yanıt yeniden sorulması, üç yeni rozetin RENGİ, birlik kapısı); KAPILAR: `npm run lint` 0 HATA, `npm run build` temiz, `tsc --noEmit` temiz, tam `npx vitest run --testTimeout=30000` 107 DOSYA / 812 TEST YEŞİL 0 KIRMIZI; AÇIK: liste ucuna `lines_count` eklenirse irsaliye başına detay isteği düşer, `EDESPATCH_STATUSES` ihracı bir `react-refresh/only-export-components` UYARISI ekliyor (hata değil, sabit ayrı modüle taşınabilir), kısmi sevkte `despatch_number` elle verilemiyor (E4a'daki gibi sunucu üretiyor), miktar gösteriminde dört hane istenirse biçimlendirici tek yerde değişir, kargo firması dalı bu ekranda hâlâ YOK — <merge tarihi>
