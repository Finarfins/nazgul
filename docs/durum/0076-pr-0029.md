ÖLÇÜLDÜ (develop f623190): production runtime-base kalıtımıyla 110 `backend/tests/test_*.py`, 290 kök `backend/test_*.py` ve `backend/donmus_saat.py` taşıyordu; temizlenmiş ağaçta `app.main` import'u YESİL. PR #29 production stage'i root iken test varlıklarını siliyor, `test` stage'i koruyor, imaj/Dockerfile/CI kapısı ekliyor.
