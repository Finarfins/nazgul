#114 — SEC-3b: cari (müşteri/tedarikçi) hassas alanları artık ROLE GÖRE maskeleniyor ve hangi rotanın hangi alanı döndürdüğü ÜRETİLEN bir pinle çivilendi (GÖÇ YOK — şema kımıldamadı, `0083` E4a'ya ait; FRONTEND'E DOKUNULMADI); İŞİN KAYNAĞI SEC-3'ün KENDİ YAZDIĞI SINIRDIR, sonradan icat edilmiş bir gerekçe değil: `backend/app/auth.py` SEC-3 bloğunun içinde "Cari LİSTESİ ve DETAYI (`/api/customers`, `/api/customers/{id}`) BİLEREK `read`te KALIYOR — günlük iş yüzeyi; alan maskeleme ayrı bir iş olarak (SEC-3b) açıldı" diyor, yani #102 KİMİN gireceğini daraltmış ama GİREN'in NE GÖRECEĞİNİ daraltmamıştı ve `depo` ile `rapor` rolleri cari listesini ve kartını TAM okuyordu — telefon, e-posta, adres ve VERGİ NUMARASI dahil; MEKANİZMA TEK TABLO + TEK DİKİŞ: kurallar `backend/app/alan_maskeleme.py::MASKELENEN_ALANLAR` içinde tek yerde ve modül SAF (I/O yok, DB yok, `Request` yok — girdisi `dict`, çıktısı YENİ `dict`, girdi ASLA yerinde değişmiyor çünkü aynı satır maskeli yanıtta VE maskesiz denetim kaydında `change_history.record_change` ile kullanılabiliyor), maskeler `phone`→`05** *** ** 12` (ilk iki + son iki hane; yalnız 11 hanede TR 4-3-2-2 gruplaması), `email`→`a***@ornek.com` (alan adı AÇIK kalıyor çünkü kurumsal alan adı zaten cari ADINDAN çıkarılabilir, gizlenen KİŞİNİN kimliği), `tax_number`→`*******890` (son 3, uzunluk korunuyor), `address`→ilk satır 24 KARAKTERE (bayta değil — Türkçe adres ortasından bölünmüyor) kırpılıp `…` (ucnokta YALNIZ gerçekten içerik düştüğünde ekleniyor, kısa tek satırlık adres olduğu gibi dönüyor), `iban`→son 4; DEĞİŞEN 4 ÇAĞRI SİTESİ: (1) `entity_detail.cari_liste_satirlari` YENİ ve BİRLEŞTİRİCİ — `GET /api/customers` (`routers/customers.py`) ile `GET /api/suppliers` (`routers/finance.py`) risk hesabını HARFİ HARFİNE AYNI döngüyle İKİ AYRI YERDE yapıyordu ve maskeleme eklerken tehlikeli olan tam buydu (biri maskelenip diğeri unutulsa aynı VKN ikinci uçtan sızardı ve hiçbir test bunu söylemezdi), döngü tek yere taşındı ve RİSK HESABINDA TEK KARAKTER DEĞİŞMEDİ, (2) `entity_detail.entity_detail` — cari KARTI satırı yanıtta İKİ anahtar altında dönüyor (`customer`/`entity`) ve İKİSİ de aynı maskeden geçiyor (yoksa `depo` maskeli `customer` nesnesini görüp ham `entity` nesnesinden VKN'yi okurdu; kapı `test_maskeli_rol_cari_kartinda_IKI_anahtari_da_maskeli_gorur`), `contacts` de maskeleniyor çünkü `entity_contacts` cari YETKİLİSİNİN telefon/e-postasını taşıyor, `notes`/`tasks` maskelenMİYOR ve bu bilinçli (serbest metin alanı, yapılandırılmış iletişim verisi değil), (3) `statement.build_statement`a keyword-only `rol` eklendi ve ekstre BAŞLIĞI maskeleniyor — JSON ile PDF (`routers/outputs.py::_statement_pdf`) AYNI çıktıyı paylaştığı için maske modele DÖNÜŞMEDEN ÖNCE uygulanıyor, varsayılan `rol=""` yani MASKELİ (yarın `rol` geçirmeyi unutan bir çağıran GİZLİ sonuç alır, SIZINTI değil), (4) `routers/search.global_search` — altyazı ham telefonu basıyordu, satır önce maskeden geçiyor ve `subtitle` SONRA maskeli değerden kuruluyor; İŞİN EN KOLAY KAÇIRILAN HÜCRESİ ÖLÇÜLDÜ VE AYRI TESTLE ÇİVİLENDİ: `depo` rolü `purchases` iznini TAŞIR (`auth.ROLE_PERMISSIONS`), yani `/api/suppliers/{id}/statement` ona 403 DEĞİL **200** döner ve o ekstrenin başlığı VKN + adres + telefon + e-posta taşır — `statement.py` maskelemeye bağlanmasaydı SEC-3b `depo` için HİÇBİR ŞEY DEĞİŞTİRMEMİŞ olurdu (`test_depo_tedarikci_ekstresine_GIRER_ama_maskeli_gorur`, karşı hücresi `test_maskesiz_rol_tedarikci_ekstresinde_tam_veri_gorur`); ROL MATRİSİ (F=tam, M=maskeli, D=403): `/api/customers` ve `/api/customers/{id}` (`read`) → F,F,F,**M,M**; `/api/customers/{id}/statement` (`sales`) → F,F,F,D,D; `/api/suppliers*` (`purchases`) → F,F,**D**,**M**,D (dikkat: `satis` maskesiz bir rol ama tedarikçiye HİÇ giremiyor); `/api/search` (`read`) → F,F,F,**M,M**; `admin` MASKESİZ roller listesinde ve bu ÖLÇÜME dayanıyor: izni `"*"` jokeri ve `ROLE_RANK`ta 100 ile `yonetici`nin (80) ÜSTÜNDE, maskelemek yetki sıralamasını TERSİNE çevirirdi — liste MASKESİZ olanları sayıyor, yani yarın eklenen bir rol otomatik MASKELİ başlıyor (deny-by-default) ve bilinmeyen/boş rol de maskeli (`test_maskele_cari_bilinmeyen_rol_maskeler`); YENİ PİN `backend/tests/pins/cari_alan_envanteri.txt` **16 satır** ve ELLE YAZILMADI ÜRETİLDİ: uygulama gerçekten kuruluyor, **393 GET rotası** geziliyor, her rotanın uç fonksiyonundan başlayarak `app` içindeki çağrılar AST ile BFS'le takip ediliyor (derinlik 3) ve hassas alan döndüren her rota `METOT YOL<TAB>izin<TAB>alanlar<TAB>matris` biçiminde sınıflandırılıyor — üretim `ast.dump` KULLANMIYOR (parmak izi yok, 3.12'nin `type_params` alanı çıktıyı etkilemez), her küme `sorted()` ile yazılıyor ve `PYTHONHASHSEED`den bağımsız (`test_envanter_uretimi_deterministik` aynı koşuda iki üretimin BYTE BYTE aynı olduğunu ölçüyor); GEZİNTİ AYRI BİR TUZAKTI VE ÖLÇÜLEREK ÇÖZÜLDÜ: FastAPI bu sürümde `include_router` rotalarını `app.routes` içine DOĞRUDAN koymuyor, araya `_IncludedRouter` giriyor ve naif `isinstance(r, APIRoute)` döngüsü 393 rotanın YALNIZ 4'ünü görüyordu (ölçüldü — envanter SESSİZCE boş üretilir ve pin testi "hiçbir şey eksik değil" deyip YEŞİL kalırdı), `_rotalar()` her iki biçimi de geziyor ve `test_rota_gezintisi_bos_degil` gezintinin gerçekten rota bulduğunu AYRICA ölçüyor; iki sınıf gerekçeli: **S** = `response_model` süzüyor — `GET /api/work-orders/{id}/invoice` gövdesinde `customer_tax_number`/`customer_address` SEÇİYOR (`billing_service.build_invoice_summary`) ama `InvoiceParty` yalnız `id`+`name` taşıdığı için FastAPI onları yanıta ÇIKARMADAN süzüyor, **H** = cari değil — 4 rota (`auth/verify-email` kullanıcının KENDİ e-postası, `finance/accounts`+`finance/summary` FİRMANIN KENDİ banka hesabı, `whatsapp/links` kullanıcının KENDİ numarası); `test_maskeli_rol_hicbir_cari_rotasinda_tam_veri_gormuyor` ayrıca HİÇBİR maskeli rolün `F` hücresi olmadığını envanterin TAMAMI üzerinden iddia ediyor ve `test_maskelenen_alanlar_semayla_ayni` maskeleme tablosunun `customers`/`suppliers` ŞEMASINI gerçekten kapsadığını şemadan OKUYARAK doğruluyor; ÖLÇÜLDÜ VE DEĞİŞTİRİLMEDİ — **IBAN/banka sütunu cari tablolarında YOKTUR**: `customers` ve `suppliers` yalnız `phone`, `email`, `address`, `tax_number` taşıyor, depodaki tek `iban` sütunu `finance_engine.finance_accounts` üzerinde ve o FİRMANIN KENDİ kasa/banka hesabı, bir cari DEĞİL — `maskele_iban` yine de yazıldı ve tabloya bağlandı ki cariye böyle bir sütun eklendiği gün maskeleme kendiliğinden devreye girsin, BUGÜN CARİ YÜZEYİNDE ÇAĞRILANI YOKTUR VE BU BİLİNÇLİDİR; **dışa aktarım GERÇEKTEN admin-only** — `auth.py:891` `/api/company/export` → `__admin_only__`, hiçbir rol tablosunda yazılı değil ve yalnız `admin`in `"*"` jokeri taşıyor, maskesiz bırakıldı ve **BULGU YOKTUR** (brief'in "non-admin bir rol export'a ulaşabiliyorsa bu bir bulgudur" maddesi ölçüldü ve olumsuz çıktı); **`musteri_satirlari`nin SQL gövdesi ELLENMEDİ** çünkü onu WhatsApp kanalı da çağırıyor (`whatsapp/yurutucu.py::cari_durum`) ve o yüzey yalnız ad/bakiye/son hareket döndürüyor, cari iletişim alanı DÖNDÜRMÜYOR — maskeleme sorgunun değil UCUN işi; ARAMA ORAKÜLÜ BİLİNÇLİ SINIR OLARAK KABUL EDİLDİ VE YAZILDI: `q` bugün `name`/`phone`/`email`/`tax_number` üzerinde eşleşiyor ve maskeli roller için DARALTILMADI (telefonla müşteri aramak günlük iştir; daraltma `musteri_satirlari`nin SQL'ini role göre değiştirmeyi gerektirir ve o fonksiyonu WhatsApp da çağırıyor), KALAN SINIR AÇIKÇA KABUL EDİLİYOR — ham VKN'yi ZATEN BİLEN bir `depo` kullanıcısı `?q=<VKN>` yazıp o VKN'nin BU müşteriye ait olduğunu DOĞRULAYABİLİR, doğrulama elde etmek değildir ama sıfır da değildir ve bu MEKANİZMANIN değil MATRİSİN kararıdır (daraltma istenirse `musteri_satirlari`na tek bir `maskeli` bayrağı yeterli), test HER İKİ yarıyı da ölçüyor (arama gerçekten çalışıyor VE yanıt gerçekten maskeli) ve `SORTS`un hassas sütuna göre sıralama SUNMADIĞI ayrıca çivilendi (`test_maskeli_rol_siralama_ile_ham_deger_alamaz`); KİRACI YALITIMI: `test_capraz_kiraci_hala_404` komşu kiracının carisinin MASKELİ 200 değil 404 döndüğünü beş rol için ölçüyor — maskeleme bir gün kiracı kontrolünün ÖNÜNE geçseydi sızıntının en sinsi biçimi doğardı; TESTLER `backend/tests/test_sec3b_cari_maskeleme.py` (**97 test** — beş maske fonksiyonunun birim testleri None/boş/kısa/Unicode dahil, beş rollük GERÇEK HTTP matrisi, arama orakülü, kiracı yalıtımı) ve `backend/tests/test_sec3b_cari_alan_envanteri.py` (**6 test**), PG ikizi `backend/test_sec3b_cari_maskeleme_postgresql.py` — ikizin sorduğu TEK soru "gerçek PostgreSQL satırı maskeleme dikişinden geçince matris aynı mı": cari kartı ve ekstre başlığı satırı `SELECT *` ile çekiyor (alan adları kaynakta HİÇ GEÇMİYOR) ve maskeleme alan ADIYLA çalıştığı için sütun kümesi iki lehçede ayrışsaydı maskeleme PostgreSQL'de SESSİZCE kapanırdı — hata yok, sadece ham VKN; PİNLER: rota sözleşmesi **392/301 DEĞİŞMEDİ** (yeni rota yok), GET izin envanteri **186 DEĞİŞMEDİ**, `EXPECTED_READ` **80** / `EXPECTED_UNDENIABLE` **97 DEĞİŞMEDİ**, dinamik SQL allowlist SAYILARI değişmedi, `pg_twins.txt` **123 → 124**, yeni `cari_alan_envanteri.txt` **16 satır**; `test_tenant_scoping_guard` parmak izleri **7 dosyada** yenilendi (`entity_detail.py`, `routers/customers.py`, `routers/finance.py`, `routers/outputs.py`, `routers/search.py`, `statement.py`, `tenancy.py`) ve TANI DEPODAKİ KURALA GÖRE YAPILDI: 62 dosyanın TAMAMI değil TAM OLARAK düzenlenen 7 dosya kaydı, yani yorumlayıcı sürümü uyuşmazlığı DEĞİL gerçek kaynak değişikliği — her dosyada dinamik `text()` SAYISI SABİT (9/1/7/4/2/5/4, tek tek ölçüldü) ve HİÇBİR SQL METNİ DEĞİŞMEDİ, parmak izi dosya AST'sinin TAMAMINDAN türediği için kımıldadı ve gerekçeler pin girdilerine yazıldı; `customers.py`de `HUNDRED`/`money` ve `finance.py`de `HUNDRED` importları risk döngüsü taşınınca ÖLÜ kaldı ve aynı değişiklikte kaldırıldı (import satırı da AST'nin parçası olduğu için iki dosyanın parmak izi İKİNCİ kez kımıldadı, o da yazıldı); KOŞUM CPython **3.12.10** venv ile yapıldı ve bu bir tercih değil ZORUNLULUK: bu makinede `python` 3.14.6'dır ve parmak izi pinleri 3.12'de üretilmiştir (`ast.dump` 3.12'de `type_params` kazandı, 3.14'te HER hash kayar) — 3.14'ten pin YENİLENMEDİ ve yenilenmemelidir; BAZ ÖLÇÜMÜ İZOLE EDİLDİ: `origin/develop` (72cfa09) AYRI bir `git worktree`ye (`C:\Users\HHH\nazgul-baseline`) çıkarıldı çünkü ilk baz koşusu düzenlenen dosyaları okumaya başlamıştı ve kirlenmişti — temiz baz koşusunda **449 dosyanın 347'si tamamlandığında SIFIR FAIL** ve tek `TIMEOUT` (`test_v3_machine_cards.py`, 180 s sınırı, 0 test) BU MAKİNENİN 4 işçili yük altındaki bilinen davranışıdır, dalın kendi koşusunda da benzer bir TIMEOUT (`test_kiraci_disa_aktarim.py`) görüldü ve TEK BAŞINA koşturulunca **16 test / 115.89 s YEŞİL** oldu, yani gerileme değil YAVAŞLIK; DEĞİŞEN DOSYALAR: `backend/app/alan_maskeleme.py` (yeni), `backend/app/entity_detail.py`, `backend/app/statement.py`, `backend/app/tenancy.py` (`istek_rolu`), `backend/app/routers/{customers,finance,outputs,search}.py`, `backend/tests/test_sec3b_cari_maskeleme.py` (yeni), `backend/tests/test_sec3b_cari_alan_envanteri.py` (yeni), `backend/test_sec3b_cari_maskeleme_postgresql.py` (yeni), `backend/tests/pins/cari_alan_envanteri.txt` (yeni), `backend/tests/pins/pg_twins.txt`, `backend/tests/test_tenant_scoping_guard.py` — artı bu kayıt; BRIEF'İN BİR VARSAYIMI ÖLÇÜLDÜ VE YANLIŞ ÇIKTI, RAPOR EDİLİP BIRAKILMADI DÜZELTİLDİ: brief "maskeleme yalnız okuma yolundadır, bu rollerin yazması SEC-3 ile zaten reddediliyor — DOĞRULA, VARSAYMA" diyordu ve doğrulandığında varsayım YARI YARIYA yanlış çıktı — MÜŞTERİ yazması için doğru (`PUT /api/customers/{id}` → `sales`, `depo`/`rapor` DIŞARIDA) ama TEDARİKÇİ yazması için YANLIŞ: `PUT /api/suppliers/{id}` → `purchases` ve `depo` o izni TAŞIYOR, yani MASKELİ bir rol tedarikçi kaydını YAZABİLİYOR; okuma maskesi TEK BAŞINA eklendiğinde bu SESSİZ BİR VERİ KAYBI kapısı açıyor ve kapıyı AÇAN ŞEY BU PR'IN KENDİSİDİR (bu PR olmadan `depo` gerçek değeri görüyordu ve form turu ZARARSIZDI): `frontend/src/components/EntityDialog.tsx` formu `GET /suppliers/{id}` ile DOLDURUYOR ve kaydederken gördüğü nesnenin TAMAMINI geri PUT ediyor (`api.put(path,v)`), maskelemeden sonra `depo`nun formuna `tax_number` `"*******890"` olarak geliyor ve YALNIZCA ADI değiştiren bir `depo` kullanıcısı o maskeli değeri geri gönderip GERÇEK vergi numarasının ÜSTÜNE YAZIYOR — hata yok, 200 dönüyor, ham değer hiçbir yerde kalmıyor, GERİ DÖNÜLMEZ; ÇARE `alan_maskeleme.maskeyi_geri_al`: gelen değer MEVCUT değerin maskesine BİREBİR eşitse kullanıcı o alana DOKUNMAMIŞTIR ve ham değer KORUNUR, eşit değilse GERÇEKTEN yeni bir değer yazılmıştır ve YAZILIR — karşılaştırma MEVCUT değerin maskesiyle yapılıyor, "maskeye benziyor mu" SEZGİSİYLE değil (sezgisel kontrol, `"*******890"` metnini gerçekten yazmak isteyen mesru bir çağrıyı yutardı) ve maskesiz roller için fonksiyon HİÇBİR ŞEY yapmıyor; kapılar `test_yazma_izinleri_olculdu_varsayilmadi` (`depo`nun `purchases` taşıdığını donduruyor — izin haritası değişirse korumanın gerekçesi yeniden okunur), `test_depo_tedarikciyi_duzenleyince_ham_vergi_no_KORUNUR` (arayüzün GERÇEK turunu koşuyor: `depo` gözüyle OKU, gövdeyi OLDUĞU GİBİ geri YAZ, `admin` gözüyle ham VKN/telefon/e-posta/adresin YERİNDE olduğunu doğrula), `test_depo_gercekten_yeni_deger_yazabilir` (KARŞI hücre — koruma MEŞRU düzenlemeyi engellemiyor) ve `test_maskeyi_geri_al_birim` (kuralın dört dalı); bu yüzden `customers.py`/`finance.py` parmak izleri AYNI PR içinde ÜÇÜNCÜ kez yenilendi — UPDATE metni ve bağlı parametre adları HARFİ HARFİNE aynı, eklenen şey saf Python; FRONTEND'E BİR DOSYA DOKUNULDU VE YALNIZCA GÖSTERİM YOLU: brief "frontend değişikliği gerekmez; maskeli bir değer bir UI ayrıştırıcısını bozarsa YALNIZ gösterim yolunu düzelt ve SÖYLE" diyordu — bozuluyor, ölçüldü ve söyleniyor: `frontend/src/pages/EntityDetail.tsx` cari kartı başlığındaki ÜÇ kısayol ham değeri bir PROTOKOL BAĞLANTISINA çeviriyor ve maskeli değerle SESSİZCE YANLIŞ çalışıyor — `tel:${e.phone}` → `tel:05** *** ** 12` GEÇERSİZ, `https://wa.me/${String(e.phone).replace(/\D/g,'')}` rakam dışını attığı için `05** *** ** 12` → **`0512`** olup BAŞKA BİR NUMARAYA gidiyor, `mailto:${e.email}` var olmayan `a***@ornek.com` adresini açıyor; üçü de GÖRÜNÜŞTE ÇALIŞIR SONUCU YANLIŞTIR (sessiz yanlış, görünür hata DEĞİL) ve maskeli değer taşıyan alanlarda kısayol artık GİZLENİYOR — alanın kendisi kartta MASKELİ olarak GÖRÜNMEYE DEVAM EDİYOR, yani veri kaybolmuyor yalnız tıklanabilir eylem kalkıyor; ROL TABLOSU İSTEMCİYE KOPYALANMADI: maskenin varlığı VERİDEN okunuyor (`deger.includes('*')`), böylece maskesiz roller için hiçbir şey değişmiyor ve rol matrisi değişirse gösterim kendiliğinden uyuyor; `npm run build` (`tsc -b && vite build`) YEŞİL; KOŞUM SONUÇLARI: baz (`origin/develop` 72cfa09, AYRI worktree) **449 dosya / 447 PASS / 0 FAIL / 2 TIMEOUT**, dal tam süiti **452 dosya / 450 PASS / 0 FAIL / 2 TIMEOUT** ve iki TIMEOUT BAZDAKİYLE AYNI İKİ DOSYADIR (`test_v3_machine_cards.py`, `tests/test_isolated_test_runner.py`) yani bu makinenin 4 işçili yük davranışıdır; SEC-3b PG ikizi GERÇEK PostgreSQL 16 üzerinde **7/7 PASS** (Docker `postgres:16`, 5433) ve frontend `npm run build` YEŞİL; DİĞER 122 PG İKİZİ BU MAKİNEDE KOŞTURULAMADI VE BU BİR GERİLEME DEĞİLDİR — `run_isolated_tests.py:182` alt süreç ortamında `DATABASE_URL`i KOŞULSUZ olarak workdir'e özel SQLite'a çeviriyor, yani ikizler URL'yi CI'ın kurduğu KENDİ değişkenlerinden (`APP_TEST_DATABASE_URL`, `CARI_PARITY_TEST_DATABASE_URL`, `WORKFLOW_TEST_DATABASE_URL` ... yirmi adet) alıp uygulamayı import etmeden ÖNCE `os.environ`a yazmak zorunda ve nöbetçi (`conftest._require_postgresql_engine`) aksi hâlde "REQUIRE_PG=1 ancak aktif motor 'sqlite'" diyerek kuruluşta düşüyor — AYNI ÜÇ İKİZ DOKUNULMAMIŞ `origin/develop` WORKTREE'SİNDE DE BİREBİR AYNI HATAYLA DÜŞÜYOR (ölçüldü), yani fark yerel kurulum ile CI'ın PostgreSQL servisi arasındadır, bu PR değil; YAN GÖZLEM: 124 dosyalık koşumda PASS eden İKİ dosyadan biri bu PR'ın ikizidir (diğeri `test_wa4_bekleyen_postgresql.py`) — ikisi de URL'yi kendi fixture'ında `os.environ`a yazıp uygulamayı ONDAN SONRA import ediyor, yani yeni ikiz nöbetçinin tarif ettiği kalıba uyuyor; kayıt adı `python scripts/durum.py --sonraki 114`; ═══ DÜZELTME TURU (10 Eylül 2026, SAHİPLİK DEVRİ) ═══ d7b8930'a iki mercek NO-GO dedi ve ikisi de HAKLIYDI; şef kararıyla dal sahipliği Claude Code B'den (diğer PC; yerel düzeltmesi HİÇ push edilmedi, o worktree döndüğünde ATILACAK ve asla push etmeyecek) Claude Code D'ye (ana PC, `F:\nazgul-sec3b`) devredildi, düzeltme origin d7b8930'dan YENİDEN yapıldı ve dal önce `origin/develop` c66e232 (#113 docs, #115 5.1c) üzerine rebase edildi (çakışma ÇIKMADI; `pg_twins.txt` 124 → **125** iki satırıyla, `test_tenant_scoping_guard` parmak izleri develop ile uyumlu kaldı, rebase sonrası kapı 33/33); BU SEGMENTİN ÜSTÜNDEKİ İKİ CÜMLE ARTIK GEÇERSİZDİR ve BİLEREK silinmedi (tarihçe): (a) "ÇARE `alan_maskeleme.maskeyi_geri_al`: gelen değer MEVCUT değerin maskesine BİREBİR eşitse ... eşit değilse GERÇEKTEN yeni bir değer yazılmıştır ve YAZILIR" ve (b) "ARAMA ORAKÜLÜ BİLİNÇLİ SINIR OLARAK KABUL EDİLDİ ... doğrulama elde etmek değildir"; NO-GO 1 (sözleşme merceği, Antigravity) — YAZMA YOLU AÇIKTI: eşitlik kuralı yalnız "dokunmadı"yı yakalıyordu, "yazamaz"ı SAĞLAMIYORDU; ÖLÇÜLDÜ: `depo` `PUT /api/suppliers/{id}` `tax_number="*******891"` → 200 ve saklanan değer `"*******891"` (maskeden TEK HARF sapan her değer gerçek VKN'yi eziyordu); ŞEF KARARI ve ÇARE: maskeli rol maskeli alana HİÇ yazamaz — `maskeyi_geri_al` artık "SAKLANAN KAZANIR": rol maskeliyse `MASKELENEN_ALANLAR` içindeki HER anahtar (ve `customer_`/`supplier_` önekli eşleri) gelen değer NE OLURSA OLSUN mevcut satırdaki değerle değiştirilir, saklanan `None` ise gelen de `None` olur (boş alan DOLDURULAMAZ da), maskelenmeyen alanlar (`name`, `owner_name`, `notes`…) serbestçe yazılır, 4xx DÖNMEZ (form nesnenin tamamını gönderdiği için reddetmek adı bile düzenletmezdi), `update_customer`/`update_supplier` ikisi de aynı fonksiyondan geçiyor (müşteri PUT'u `sales` iznine bağlı olduğundan maskeli rol oraya HTTP ile ulaşamaz — `test_yazma_izinleri_olculdu_varsayilmadi` bunu dondurmayı sürdürüyor — kural yine de iki uçta simetrik), eski eşitlik kuralı fonksiyonun docstring'inde TARİHÇE olarak duruyor; NO-GO 2 (çalışma zamanı merceği, Claude Code C) — `q` TAM ÇIKARMA ORAKÜLÜYDÜ: `LIKE %q%` İÇERİR süzgeci telefon/e-posta/VKN üzerinde eşleştiği için `depo` maskeli `*******596`yı görüp `q=1596`, `q=31596`, … `q=4820731596` ile **80 gerçek istekte TAM VKN'yi geri çıkardı**, her adımda tek eşleşme; ŞEF KARARI ve ÇARE: maskeli roller için `q` YALNIZ `name` ve `owner_name` üzerinde eşleşir, ÜÇ yüzeyde de — (1) `customers.musteri_satirlari` keyword-only `maskeli: bool = False` bayrağı aldı ve WHERE'deki süzgeç `{arama_sql}` ile KAPALI iki metinden birini seçiyor (maskesiz dalın metni önceki sürümle KARAKTERİ KARAKTERİNE aynı; `q` iki dalda da BAĞLI PARAMETRE; kök yüklem `c.company_id=:cid` değişmedi), `list_customers` bayrağı `maskelenecek_mi(istek_rolu(request))` ile geçiyor; (2) `finance.suppliers` aynı kalıpla (`depo` `purchases` taşıdığı için tedarikçi listesinde AYNI yürütmeyi koşabilirdi); (3) `search.global_search` — mercek VKN'de eşleşmediğini ölçmüştü ama `_like` İÇERİR kalıbıyla telefon/e-posta'da eşleşiyordu (aynı orakül: `05** *** ** 12`den `q=012`, `q=4512`…), şef bu daraltmayı da onayladı ve sorgu role göre seçilen İKİ AYRI SABİT metin olarak yazıldı (f-string DEĞİL) ki `test_tenant_scoping_guard` sabit metinleri ayrı ayrı denetlesin ve dosyadaki dinamik `text()` sayısı 2'de kalsın; WHATSAPP ÇAĞIRANI ÖLÇÜLDÜ, ROL GÜDÜMLÜ DEĞİL: `whatsapp/yurutucu.py::cari_durum` `musteri_satirlari`yi AÇIKÇA `maskeli=False` ile çağırıyor çünkü `eslestirme.Kimlik` yalnız `(company_id, user_id)` taşır, rol alanı YOK, ve kanalın cevabı yalnız ad/bakiye/son hareket döndürür, cari iletişim alanı DÖNDÜRMEZ — rol bilinmediğinden eski (tam) süzgeç korunuyor, daraltma HTTP uçlarında role göre yapılıyor (KALAN GÖZLEM, DEĞİŞTİRİLMEDİ: kanal kullanıcısı `musteri=<VKN>` yazıp yalnız AD alabilir; `Kimlik.user_id`den rol çözülebilir ama şefin talimatı `maskeli=False` idi ve bu turun kapsamı dışında bırakıldı); DOCSTRING DÜZELTİLDİ: `test_maskeli_rol_arama_ile_ham_deger_alamaz` artık yeni davranışı anlatıyor ve eski "doğrulama, elde etmek değildir" cümlesinin NEDEN yanlış olduğunu (içerir kalıbı + sonek yürütmesi) yazıyor; TESTLER `backend/tests/test_sec3b_cari_maskeleme.py` **101 → 119 test**: yazma — `test_depo_maskeli_alana_HIC_yazamaz` (5 hücre: `tax_number="*******891"` MERCEĞİN DEĞERİ, `tax_number="9999999999"`, `phone`, `email`, `address` → hepsi 200, saklanan değer DEĞİŞMEDİ, aynı PUT'taki ad değişikliği YAZILDI yani 200 gerçek yazma), `test_yonetici_maskeli_alani_yazabilir` (karşı hücre, kendi tedarikçisini açar), `test_maskeyi_geri_al_birim` (8 dal: maskeli / bambaşka / None / önekli / tablonun tamamı / maskesiz / mevcut yok / girdi değişmez), `test_depo_tedarikciyi_duzenleyince_ham_vergi_no_KORUNUR` (form turu, korundu); arama — `test_maskeli_rol_arama_ile_ham_deger_alamaz` (depo+rapor: VKN/telefon/e-posta/adres ile ARANMAZ; `SEC3B`/`sec3b müşteri`/`Yetkili` ile bulunur ve MASKELİ; tohuma `owner_name="Ahmet Yetkili"` eklendi ki süzgecin ikinci sütunu gerçekten ölçülsün), `test_maskesiz_rol_vkn_ve_telefonla_arayabilir` (yonetici/muhasebe/satis tam süzgeç, ham satır), `test_sonek_yurutmesi_ilk_adimda_sifir_eslesme` (MERCEĞİN SALDIRISI, düzeltmeden sonra: `*******890` → on aday `0890`…`9890`, on istek → depo **0/10**, rapor **0/10**, yürütme İLK ADIMDA BAŞLAYAMIYOR; KONTROL HÜCRESİ aynı testte: `yonetici` için aynı adım TAM OLARAK `["7890"]` = 1/10, yani adım gerçek bir oraküldür ve sıfır daraltmadan geliyor), `test_depo_tedarikci_aramasi_daraltildi` (`/api/suppliers?q=`: depo VKN/telefon/e-posta bulamaz, ad/yetkili bulur maskeli, sonek ilk adım **0/10**, yonetici kontrolü `["7890"]` ve `q=<VKN>` tam satır), `test_global_arama_maskeli_rolde_telefon_ve_eposta_eslesmez` (`/api/search`: depo+rapor telefon/e-posta/`234512` bulamaz, `SEC3B`/`Yetkili` bulur ve `subtitle` maskeli, telefon soneki `12` ilk adım **0/10**, yonetici `q=<telefon>` bulur), `test_global_arama_vkn_ile_hic_eslesmez` (BEŞ rol: `/api/search` VKN'de eşleşmez — merceğin ölçümü donduruldu); FRONTEND `frontend/src/pages/EntityDetail.maskeli.test.tsx` **6 test** (merceğin yazıp sildiği test; kod tabanında `maskeli` adına dokunan başka test YOKTU): ham telefon+e-posta → `tel:05551234512`, `https://wa.me/05551234512`, `mailto:ahmet@ornek.com` TAM OLARAK; maskeli telefon → tel/wa.me GİZLİ, değer başlıkta metin olarak DURUYOR, `mailto` etkilenmiyor (alan bazlı); maskeli e-posta → mailto gizli, telefon kısayolları duruyor; `***` → üçü de gizli; `wa.me/0512` tuzağı ASLA türetilmiyor; boş alanlar kısayol üretmiyor; MUTASYON DENETİMİ (dört mutasyon, her biri `tests/test_sec3b_cari_maskeleme.py` tam dosya; kaynak `cp` ile geri alındı ve `git diff` hash'i öncesi=sonrası doğrulandı): M1 stored-wins → eski eşitlik kuralı: **6 kırmızı** (`test_depo_maskeli_alana_HIC_yazamaz`×5 + `test_maskeyi_geri_al_birim`); M2 `customers.py` daraltma kapalı: **4 kırmızı** (`test_maskeli_rol_arama_ile_ham_deger_alamaz`×2 + `test_sonek_yurutmesi_ilk_adimda_sifir_eslesme`×2); M3 `finance.py`: **1 kırmızı** (`test_depo_tedarikci_aramasi_daraltildi`); M4 `search.py`: **2 kırmızı** (`test_global_arama_maskeli_rolde_telefon_ve_eposta_eslesmez`×2) — dördü de yalnız HEDEF testlerce yakalandı; KOŞUM SIRASI: iki sec3b dosyası İLERİ / TERS (`--collect-only -q | tac` ile aynı sıra) / MODÜL-İÇİ KARIŞIK ×3 tohum (20260910, 7, 31337) → **125/125 beşinde de**; ters sırada d7b8930'dan gelen GİZLİ bir sıra bağımlılığı çıktı ve düzeltildi: yazma-tuzağı testi tedarikçinin adını değiştiriyor, `test_depo_tedarikci_ekstresine_GIRER_ama_maskeli_gorur` özgün adı bekliyordu — iki yazma testi artık değiştirdiği adı aynı test içinde GERİ YAZIYOR ve geri yazıldığını ölçüyor; MODÜLLER ARASI karışık sıra BİLEREK koşulmadı ve bu bir sınır değil pytest'in yaşam döngüsüdür: modül-kapsamlı fixture'lar (TestClient + tek seferlik açılış parolası değişimi) modül sınırında finalize edilir, iki modülü iç içe karıştırmak ikinci girişi 401 yapar (ölçüldü: 64/72 sahte hata), izole koşucu zaten dosya başına ayrı süreç kullanır; Windows'ta 125 node id'yi komut satırına yazmak "Argument list too long" verdiği için sıralama pytest içinde küçük bir koleksiyon eklentisiyle yapıldı; PİNLER: rota sözleşmesi **393/302 DEĞİŞMEDİ** (yeni rota yok), GET izin envanteri **186 DEĞİŞMEDİ**, `EXPECTED_READ` **80** / `EXPECTED_UNDENIABLE` **97 DEĞİŞMEDİ**, `pg_twins.txt` **125** (124 + develop'un `test_kiraci_geri_yukleme_postgresql.py` satırı), `cari_alan_envanteri.txt` **16 DEĞİŞMEDİ** (rota ve alan kümesi aynı); `test_tenant_scoping_guard` parmak izleri **4 dosyada** yenilendi (`routers/customers.py`, `routers/finance.py`, `routers/search.py`, `whatsapp/yurutucu.py`) ve dinamik `text()` SAYILARI ÖLÇÜLDÜ, SABİT: customers **1**, finance **7**, search **2**, yurutucu **1** (entity_detail 9, statement 5, outputs 4, tenancy 4 bu turda dokunulmadı) — kapı yalnız parmak izi uyuşmazlığı gösterdi, sayı uyuşmazlığı YOK, gerekçeler pin girdilerine yazıldı; PG İKİZİ `test_sec3b_cari_maskeleme_postgresql.py` GERÇEK PostgreSQL **16.4** üzerinde **7/7 PASS** (yerel 5433, `sec3b_d_ikiz` adlı TAZE veritabanı, `REQUIRE_PG=1` + `DATABASE_URL` + `APP_TEST_DATABASE_URL`, sürücü psycopg v3); FRONTEND `npm test -- --run` **91 dosya / 638 test PASS** ve `npm run build` (`tsc -b && vite build`) YEŞİL; TAM İZOLE SÜİT (`run_isolated_tests.py`, 4 işçi, CPython 3.12.10): **454 dosya / 454 PASS / 0 FAIL / 0 TIMEOUT, 3185 test sonucu, 1056 s** (rebase sonrası dal ağacı; d7b8930 kaydındaki iki TIMEOUT bu makinede görülmedi); izole koşucu SON ağaçta ayrıca dokunulan iki sec3b dosyası + `test_tenant_scoping_guard` + üç pin dosyasını (`test_route_security_contracts`, `test_route_get_permission_inventory`, `test_authorization_population_reconciliation`) koştu: **6/6 dosya, 185 test PASS**; DEĞİŞEN DOSYALAR (bu tur): `backend/app/alan_maskeleme.py`, `backend/app/routers/{customers,finance,search}.py`, `backend/app/whatsapp/yurutucu.py`, `backend/tests/test_sec3b_cari_maskeleme.py`, `backend/tests/test_tenant_scoping_guard.py`, `frontend/src/pages/EntityDetail.maskeli.test.tsx` (yeni) — artı bu kayıt; GÖÇ YOK (`backend/alembic` diff'i boş); `git diff origin/develop HEAD -- backend/app backend/alembic | git hash-object --stdin | cut -c1-12` = **1a779277dc65**; düzeltme commit'i e55abe2, sıra bağımsızlığı e735952
