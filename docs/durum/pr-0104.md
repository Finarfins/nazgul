#104 — CI: H15 — e2e Playwright font hazırlığı üçüncü taraf apt kaynaklarından (dl.google.com vb.) bağımsızlaştırıldı; KUSUR ÖLÇÜLDÜ (2026-09-09, PR #102 koşu 34384954373 ve PR #103 koşu 34382994237): `.github/scripts/playwright_hazirla.py` 2. aşamasında `npx playwright install-deps chromium` kök kullanıcı ile `apt-get update` çalıştırırken runner üzerindeki `/etc/apt/sources.list.d/google-chrome.list` deposundaki Packages.gz özet uyumsuzluğu (`E: Failed to fetch https://dl.google.com/linux/chrome-stable/deb/dists/stable/main/binary-amd64/Packages.gz Hash Sum mismatch`) nedeniyle exit 100 ile düştü ve e2e testleri atlandı; AYNI BAŞARISIZLIK SINIFI: #82'deki apt asılmasının aynısı, e2e işimiz Google Chrome veya Microsoft/Azure apt depolarına hiçbir zaman ihtiyaç duymaz — tarayıcı Chrome for Testing olarak Playwright CDN'inden iner, kurulan dokuz bağımlılık ise yalnızca Ubuntu aynalarındaki font paketleridir (`eksik_fontlar()`); ÇARE: 2. aşamada font bağımlılık komutu çalıştırılmadan hemen önce ilgisiz üçüncü taraf apt kaynakları (`google-chrome*.list`, `microsoft-prod.list`, `azure-cli.list`) `.disabled` uzantısıyla kenara alınır (`sudo mv X X.disabled`), dosyalar yoksa no-op çalışır ve `apt kaynağı yok` loglar, Ubuntu temel aynalarına dokunulmaz, fontlar zaten kuruluysa apt'e dokunulmayan hızlı yol ve 300 sn sınır korunur; ALTERNATİF: Ubuntu paket listeleri hazır olduğundan `sudo apt-get install -y --no-install-recommends <9 font>` ile apt-get update tamamen atlanabilirdi ancak daha dar etki alanı (blast radius) için kaynak devredışı bırakma tercih edildi; DOĞRULAMA: yerel Windows koşusunda 2. aşama no-op olarak `apt kaynağı yok` logladı, `backend/tests/test_ci_playwright_hazirlik.py` 13/13 yeşil; UYGULAMA VE TEST KODU DEĞİŞMEDİ (`git diff origin/develop HEAD -- backend frontend` BOŞ); GÖÇ YOK; kayıt adı `python scripts/durum.py --sonraki 104`
