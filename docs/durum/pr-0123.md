#123 — CS1 ÇEK/SENET PORTFÖYÜ: MODEL, GÖÇ 0085, DURUM MAKİNESİ (backend; muhasebe YOK, CS2): göç `20260914_0085` `cek_senetler`i §2.2 birebir kurar (25 sütun, `tutar NUMERIC(18,2)`, CHECK tur/yon/tutar>0/`ck_cek_senetler_yon_taraf`/6 değerli `portfoy_durumu`, 4 indeks, `uq_cek_senetler_company_id`) ve beş bileşik FK; hedefler ÖLÇÜLDÜ — customers (0026), suppliers (0070), payments (0071) VAR, `finance_accounts`ta (company_id,id) UNIQUE YOKTU ve `uq_finance_accounts_company_id` bu göçte 0071 kalıbıyla kuruldu (downgrade koşullu düşürür); `UNIQUE(company_id,seri_no,tur)` KONMADI (seri no karneye aittir); up/down/up SQLite (`PRAGMA foreign_keys=ON`: çapraz firma, yon_taraf, tutar, durum, tur REDDEDİLDİ) ve PG16 (`convalidated=true`, beş FK'nin beşi çapraz firmayı reddetti); DURUM MAKİNESİ 6x6=36 çift, 7 izinli, 29 izinsiz -> 409 CEK_GECIS_GECERSIZ (29'u da HTTP'den denendi), ciro yalnız alınan (409 CEK_CIRO_YALNIZ_ALINAN); beş uç `/api/cek-senetler` Core ile, `text()` yok; izin `payments` bütün metotlar — kural öncesi ölçüm 2 GET `read`, 3 POST `__admin_only__`; `payments` = admin/yönetici/muhasebe/satış (satış YAZAR), depo/rapor GET'te de 403; `hesap_no` SEC-3b maskesinde (son 4 hane) ama bugün hiçbir rolde TETİKLENMİYOR (payments taşıyan dört rol maskesiz); 5.1c: TENANT_TABLES 121 -> 122, iki FK'sız bağ DOGRUDAN_HEDEFLER'de (beş FK sütunu brifin aksine YAZILMADI: `_Plan` yansıtılan FK'den kendisi eşliyor, ölçüldü), 2 satırlık yuvarlak yolculuk yeni firmanın müşteri/tedarikçi/kasasına eşledi; PİNLER (#122 sonrası TABAN `2e35393`, ölçüldü): 407/315 -> 412/319, GET 197 -> 199, AUTHENTICATED 394 -> 399, READ 87 / GUARDED 31 / UNDENIABLE 97 SABİT, pg_twins 128 -> 129, alt_surec 136 -> 137, baş 0084 -> 0085 (8 dosya/10 iddia), ACTION_TYPES 76 -> 78 ve RESOURCE_TYPES 22 -> 23 (sayı pini VARDI: `test_activity_log_panel.py`), cari_alan_envanteri 17 -> 19 (liste+tekil; `phone` tarayıcının yanlış pozitifi, pinde gerekçeli), sayısal manifesto 90 sabit (`cek_senetler.tutar` 0000 tabanından sonra doğar, manifestoya GİRMEZ — girseydi PG sayısal göç mutabakatı varlık farkı verirdi; tipi `test_v2_9_hardening`te ölçülür), Core kiracı kapısı 159 -> 167 ifade (lisans 4'te sabit), Core envanter 199 -> 206, kiracı parmak izi yalnız `activity_log.py` 7b0f1212 -> 6ae3e797; TESTLER: `tests/test_cs1_cek_senet.py` 66 (lens düzeltmesi: kimlik/liste süzgeci/gövde kimlikleri ve `offset` int4 üstünde 500 yerine 422, `karsiliksiz` isteğe bağlı not kabul eder; ileri/ters/3× karışık; mutasyon: izin kuralı, kiracı yüklemi, bordro atomikliği, maske, matris hepsi kırmızı), PG ikizi 16 (int4 üstü GET 422 dahil; taze DB + ikinci koşu + ters + karışık), tam izole paket 462 dosya / 3401 sonuç yeşil (taban 50b0dba)
