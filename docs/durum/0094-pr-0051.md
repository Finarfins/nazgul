#51 — AVANS, BORSA TESCİLİ, NET ÖDEME ve STOPAJ DEFTERİ (D2): D1 makbuzu KESER ve bir BORÇ doğurur, D2 o borcun KAPANMASIDIR; göç `20260906_0071`, down_revision `20260905_0070`, TABAN `91feaf8` (`origin/develop`; ilk oturuş `9cc9edb` üzerindeydi ve #50 İŞLEM SIRASINDA indi — REBASE EDİLDİ, çakışma YOK; oturum başı anlık görüntüsü `23902cf` gösteriyordu ve O DA BAYATTI, `git rev-parse` ile DOĞRULANDI); ÜÇ YENİ TABLO: `supplier_advances` (11 sütun), `tax_liabilities` (9 sütun), `producer_receipt_exchange_registrations` (9 sütun) — üçü de `TENANT_TABLES`ta; artı `producer_receipts`e TEK sütun (`advance_applied_total`) ve `payments`a `uq_payments_company_id`; `uq_payments_company_id` BU GÖÇTE KURULDU ve gerekliliği ÖLÇÜLDÜ, VARSAYILMADI: `app/core_schema.py`de `payments` bildiriminde (sütunlar + iki `Index`) hiçbir `UniqueConstraint` YOK, yani avansın ve yükümlülüğün bileşik yabancı anahtarlarının HEDEFİ EKSİKTİ — 0058/0070'in `batch_alter_table` kalıbıyla eklendi; `downgrade` bu kısıtı DÜŞÜRÜR (0070'in `uq_suppliers_company_id`yi BIRAKMA gerekçesinin TERSİ DEĞİL, TAMAMLAYICISI: orada kısıt başka göçlerin de isteyebileceği ORTAK bir hedefti, burada onu isteyen tek şey bu göçün kendi iki tablosudur ve onlar düşerken kısıt öksüz kalır) ama düşürme KOŞULLUDUR (adı VARSA), yani başka bir göç aynı kısıtı kurarsa körlemesine silinmez; AVANS BİR `payments` SATIRIDIR: `supplier_advances.payment_id` NOT NULL, çünkü avans PARANIN KASADAN ÇIKMASIDIR — ayrı bir tabloda tutmak kasadan çıkmış ama defterde görünmeyen para bırakırdı ve tedarikçi bakiyesi (açılış + alımlar − ödemeler; `finance.py:136`, `statement.py:214`, `dashboard.py:127`) avansı SAYMAZDI; TAVUK-YUMURTA YOK ve bu ÖLÇÜLDÜ: `payments.reference_id` bir yabancı anahtar TAŞIMIYOR, bu yüzden sıra (1) INSERT payments `reference_type='supplier_advance'` + `reference_id` NULL, (2) INSERT supplier_advances `payment_id`=1'in kimliği, (3) UPDATE payments SET `reference_id`=2'nin kimliği — ÜÇÜ DE TEK İŞLEMDE; `reference_id=0` yer tutucu YAZILMADI çünkü var olmayan bir avansı gösterirdi ve o satır bir daha ASLA "hiç bağlanmamış" olarak AYIRT EDİLEMEZDİ; `advance_applied_total`ın CHECK'i İLK TURDA ATLANMIŞTI ve GEREKÇESİ YANLIŞTI — bu kayda ADIYLA yazılıyor: "SQLite'ta CHECK eklemek tabloyu yeniden kurar, yeniden kurulum `producer_receipts`in KISMİ benzersiz indeksinin `WHERE` yüklemini TAŞIMAZ ve numara tekilliği sessizce gevşer" diye düşünülmüştü; ÖLÇÜLDÜ ve ÇIKMADI (alembic 1.18.5 / SQLAlchemy 2.0.51 / SQLite 3.49.1: batch sonrası `ux_producer_receipts_company_receipt_no` `WHERE receipt_no IS NOT NULL` ile KORUNDU, yabancı anahtar sayısı 4'te DEĞİŞMEDİ, `advance_applied_total=-5` INSERT'i `IntegrityError` aldı), bu yüzden kısıt EKLENDİ ve ölçümün kendisi göçün başlığına düşüldü; İKİNCİ GERÇEK KUSUR, YERELDE BULUNDU: `downgrade`de yalnız `drop_column` çağırmak SQLite'ta `OperationalError: no such column: advance_applied_total` veriyordu — batch yeniden kurulumu YANSITILAN CHECK'i yeni tabloya taşıyor ve o CHECK az önce düşürülen sütunu ADIYLA anıyor; kısıt AYNI BATCH'te ÖNCE düşürülüyor ve `up -> down -> up` turu SQLite 3.49.1'de KOŞULDU (kısmi indeks SAĞ, FK 4, üç tablo geri geldi); UÇLAR `POST`+`GET /api/suppliers/{id}/advances`, `POST /api/producer-receipts/{id}/pay`, `POST /api/producer-receipts/{id}/exchange-registration`, `GET /api/tax-liabilities` — BEŞİ DE `purchases` iznine bağlı, GET'LER DAHİL; iki yeni kural `auth.py`de temel `read` kuralının ÜSTÜNE yazıldı çünkü avans çiftçiye ödenen PARADIR ve stopaj yükümlülüğü makbuzun kesintisidir, ikisi de tedarikçi maliyetini AÇIK EDER; avans yolu için bu ÖZELLİKLE gerekliydi: `/api/suppliers` kuralı ZATEN var ama `read`in ALTINDA, yani yazılmasaydı avans listesi `read`e düşer ve çiftçiye ne ödendiği okuma yetkisi olan HERKESE görünürdü — yerleştirmenin TANIĞI `EXPECTED_READ`in 93'te SABİT kalmasıdır; AVANS MAKBUZA DEĞİL TEDARİKÇİYE BAĞLANIR (`/suppliers/{id}/advances`, `/producer-receipts/{id}/advances` DEĞİL): avans verildiğinde ORTADA HENÜZ MAKBUZ YOKTUR ve avansın sebebi zaten makbuzun kesilmemiş olmasıdır; makbuzun altına asmak, kaydı ancak belge kesildikten sonra girilebilir yapardı, oysa para çoktan kasadan çıkmıştır; MAHSUP `issue` ANINDA FIFO (en eski avans önce, `ORDER BY id` — `created_at` DEĞİL, çünkü aynı saniyede yazılmış iki avansta berabere kalır ve sıra diyalektin iç sırasına düşerdi); kanca `issue`ın CAS İŞLEMİNİN İÇİNDE, `issued` yazıldıktan SONRA ve commit'ten ÖNCE — ayrı işleme alınsaydı numarası verilmiş ama stopajı yazılmamış bir makbuz ARA HÂL olarak var olabilirdi; BİLİNEN SINIR, ADIYLA: KISMİ mahsupta avans AÇIK kalır ve `receipt_id` NULL DURUR, yani "B avansının 150'si R makbuzuna gitti" olgusu SATIR BAZINDA OKUNAMAZ; makbuz tarafında yalnız TOPLAM (`advance_applied_total`) durur; tam kayıt bir MAHSUP SATIRI tablosu ister (avans × makbuz × tutar) ve D2'nin kapsamı DIŞINDA bırakıldı — göçün ve motorun başlıklarında da yazılı; SIFIR TUTARLI YÜKÜMLÜLÜK SATIRI YAZILMAZ (kesinti yoksa yükümlülük de yoktur; göçün `amount > 0` CHECK'i de reddederdi) ama SIFIR KESİNTİLİ MAKBUZ YİNE AVANS TÜKETİR — mahsup NET ÖDENECEĞE bakar, kesintiye DEĞİL; `due_period` makbuzun KESİLDİĞİ aydır (`YYYY-MM`), ödemenin yapıldığı ay DEĞİL; İPTAL DÖRT ENGELLE KORUNUYOR (ödeme, borsa tescili, mahsup edilmiş avans, KAPANMIŞ yükümlülük -> 409) ve engel geçilirse KAPANMAMIŞ yükümlülükler AYNI İŞLEMDE SİLİNİR — kural budur, istisna değil: o satırlar `issue` tarafından TÜRETİLMİŞTİ ve hiçbir yere beyan edilmemişlerdi, kalsalardı iptal edilmiş bir belgeden doğan vergi borcu defterde ASILI kalırdı; engel takıldığında `rollback` makbuzu `issued` hâline geri getiriyor ve reddedilen iptal yükümlülükleri SİLMİYOR (ikisi de teste yazıldı); iptal edilen makbuzun NUMARASI DURUYOR (D1 kuralı BOZULMADI); D1'İN `test_kaynak_defterin_ADINI_bile_gecirmiyor` KAPISI YEŞİL KALDI ve SEBEBİ KAPININ BAŞLIĞINA YAZILDI: ödeme yazımı `routers/avans.py`de, `routers/mustahsil.py`de DEĞİL; kapının yeşilliği artık ŞUNU ölçüyor — `issue` hâlâ ödeme satırı YAZMIYOR (doğurduğu şey yükümlülük ve mahsuptur, para hareketi DEĞİL) ve `cancel`ın ödeme denetimi `avans_engine.iptal_engelleri` üzerinden OKUYOR (kapı dolaylı okumayı ZATEN iddia dışı bırakıyor: "NE İDDİA ETMEZ"); YANLIŞ OKUNMASIN diye kapıya AÇIKÇA yazıldı: bu "müstahsil makbuzu ödeme defterine dokunmuyor" DEMEK DEĞİLDİR — `/pay` ve `/advances` `payments`a YAZAR, dokunmayan şey D1 router'ının KENDİSİDİR; D2'NİN YAZDIĞI ÖDEME SATIRLARI GENEL UÇTAN DÜZENLENEMEZ VE SİLİNEMEZ ve bu MEVCUT davranıştır, D2 ona YASLANIYOR (yeniden yazmıyor): `finance.py:_validate_payment` `reference_type`i yalnız `order`/`purchase`e izin veriyor, `update_payment` (:353) ve `delete` (:416) `reference_type`i NULL OLMAYAN her ödemeyi 409 ile reddediyor — smoke'ta ölçüldü; HAREKET HARİTASI İKİ GİRİŞ BÜYÜDÜ (`(supplier, supplier_advance)` -> `(supplier_advances, supplier_id)`, `(supplier, producer_receipt)` -> `(producer_receipts, supplier_id)`) ve kapının DAR kaldığı da ölçüldü (müşteri carisi + makbuz -> 422); BÜTÜN D2 SQL'İ SABİT METİN: `avans_engine.py` ve `routers/avans.py`de hiçbir `text()` f-string/birleştirme ALMIYOR, kiracı yüklemi her sorguda LİTERAL — bunun SONUCU ÖLÇÜLDÜ: dinamik SQL envanterine bu iki dosyadan HİÇ giriş DÜŞMEDİ ve TOPLAM KIMILDAMADI (bu bir OLGUDUR, unutma DEĞİL); TESTLER: `backend/tests/test_d2_avans_tescil.py` (12 test — 11 statik kapı + alt süreçte gerçek şemalı 8 senaryoluk davranış smoke'u) ve PG ikizi `backend/test_d2_avans_tescil_postgresql.py` (18 test) GERÇEK PostgreSQL 16.14'te (docker `pglens` :5433, HER KOŞUMDA yeni veritabanı) KOŞULDU, SIFIR ATLAMA; D1 ikiziyle birlikte aynı taze şemada 38/38; İKİ KUSUR TESTTEYDİ, KODDA DEĞİL ve ikisi de ADIYLA yazıldı: (1) kiracı kapısı yalnız `company_id=:cid` arıyordu ve `tax_liabilities` INSERT'inde KIRMIZI oldu — INSERT'te yüklem OLMAZ, kiracı SÜTUN LİSTESİNDE durur; kapı iki biçimi de kabul edecek şekilde genişletildi ve ayrım kapının başlığına yazıldı ki sonraki okuyucu INSERT'i "yüklemsiz diye muaf" SANMASIN; (2) sıfır kesintili makbuzun da avans tükettiği atlanmıştı ve senaryo 5'in aritmetiği tutmadı — beklenti düzeltildi ve tüketim artık AYRICA çivili; SÜRÜCÜ psycopg2 DEĞİL `psycopg` (v3): ilk PG koşumunda `postgresql+psycopg2://` yazılmıştı ve 18 test `ModuleNotFoundError` verdi; `ci.yml`in URL'i `postgresql+psycopg://` ve doğrusu odur; PG koşumunda HEM `DATABASE_URL` HEM `APP_TEST_DATABASE_URL` verildi artı `REQUIRE_PG=1` (yalnız birini vermek uygulamayı SESSİZCE SQLite'a düşürüyor — #47'nin kaydında ölçülmüş tuzak); MUTASYONLAR (yedisi de tek tek uygulandı, adı geçen test KIRMIZI oldu, geri alındı ve `git status` BOŞ doğrulandı): FIFO -> LIFO (`ORDER BY id DESC`) -> smoke senaryo 2; kısmi mahsup ATLANDI -> senaryo 2; aşırı ödeme SERBEST -> senaryo 5; yükümlülük YAZILMAZ -> senaryo 3; iptal ödemeyi GÖRMEZDEN GELİR -> senaryo 7; ikinci tescil SERBEST -> senaryo 6 AMA UYGULAMA KAPISI DEĞİL ŞEMA YAKALADI (`IntegrityError: UNIQUE constraint failed`), yani uygulama kontrolü kaldırılsa bile SON SAVUNMA DURUYOR ve bu kayda ADIYLA geçiyor; `issue` kancası HİÇ ÇAĞRILMAZ -> ÖNCE statik kapı `test_D1_kancalarinin_ikisi_de_CAS_ISLEMININ_ICINDE`, SONRA smoke (tasarlanan sıra: statik kapı davranıştan ÖNCE ve SEBEBİYLE kırılır); İĞNELER (hepsi YENİDEN ÖLÇÜLDÜ, ARİTMETİKLE TÜRETİLMEDİ — her biri kapının kendisine ölçtürüldü): göç başı `20260905_0070` -> `20260906_0071` (3 yer), `TENANT_TABLES` 106 -> 109, dışa aktarım tablo sayısı 106 -> 109 (3 yer), alt süreç SQL sayacı 109/176 -> 110/177, rota sözleşmesi 355/274 -> 360/278 ve parmak izi `647736cf` -> `8f3eb86b` (DÖRT yol, BEŞ işlem: ilk yol İKİ işlem taşır), GET envanteri 170 -> 172 ve parmak izi `d64b5cb3` -> `bd1b5dd1` (drift raporu ÖLÇÜLDÜ: `missing`/`stale`/`changed` ÜÇÜ DE BOŞ, artış YALNIZ ekleme), `EXPECTED_AUTHENTICATED` 345 -> 350 ile `EXPECTED_READ` 93 ve `EXPECTED_UNDENIABLE` 100 SABİT, `movement_references.py` parmak izi `2d8c02a5` -> `4ca0a7fc` (çağrı sayısı 1'de SABİT), `routers/mustahsil.py` parmak izi `7020cd85` -> `ce05a0b2` (3'te SABİT), PG popülasyonu 103 -> 104 (3 yer: test adı, iddia ve `ci.yml`in `BEKLENEN_PG_DOSYA_SAYISI`'ı — SAYIM ÖLÇÜLDÜ: `ls backend/test_*postgresql*.py | wc -l` -> 102, yani 102 adlı + 2 özel = 104); SÖZLEŞME ZİNCİRİ: `export_openapi.py -o` -> `npm run types:gen` -> `capture_frontend_fixtures.py`; delta YALNIZ `frontend/src/api/types.gen.ts` (+348), `pos-api.json` DEĞİŞMEDİ, `backend/openapi.json` `.gitignore`da; `npx tsc -b --force` çıkış 0 (`npm ci` gerekti: bu worktree'de `node_modules` YOKTU); ÖLÇÜLMEDİ, ADIYLA: (1) YÜKÜMLÜLÜK KAPATMA — `/tax-liabilities` YALNIZ OKUR, `settled_at` NULL doğar ve D2'de kapanmaz; kapatma bir ödemeyi vergi dairesine bağlamayı ister ve `payments.entity_type` kapalı kümesinde böyle bir cari YOKTUR (`ACCOUNT_TYPES` `{cash,bank,pos}` de vergi borcunu temsil etmez — D1'in ölçtüğü engelin AYNISI); `settlement_payment_id` sütunu ŞİMDİ açıldı ama hiçbir yazma yolu onu doldurmuyor — sütunun varlığı kapatmanın bir `payments` satırına BAĞLANACAĞINI şemaya yazıyor ve o kararı ileriye taşıyor; `test_yukumluluk_kapatma_ucu_YOK` sessizce eklenmesini ENGELLİYOR; (2) MÜSTAHSİL MAKBUZUNUN TEDARİKÇİ EKSTRESİNE GİRMESİ — ÖLÇÜLDÜ ve DEĞİŞTİRİLMEDİ: bakiye açılış + alımlar − ödemeler; avans ZATEN bir `payments` satırı olduğu için bakiyeyi KENDİLİĞİNDEN düşürüyor, AMA makbuzun `net_payable`ı bir ALIM DEĞİL, yani bakiyeyi ARTIRMIYOR — sonuç olarak makbuz ödemesi bakiyeyi KARŞILIKSIZ düşürüyor ve çiftçi FAZLA ÖDENMİŞ görünüyor; `statement.py` D2'de DEĞİŞTİRİLMEDİ (görev metni açıkça yasakladı) ve bu bir D3 ADAYIDIR; (3) e-MÜSTAHSİL (e-MM) SAĞLAYICI ENTEGRASYONU — D2 de GİB'e ya da bir entegratöre HİÇBİR ŞEY göndermiyor; (4) MAHSUP SATIRI TABLOSU (yukarıdaki bilinen sınır); (5) AVANSIN GERİ ALINMASI — avansı iptal eden ya da iade eden bir uç YOK, verilmiş avans ancak bir makbuzla kapanır; (6) FRONTEND YÜZEYİ yazılmadı, yalnız `types.gen.ts` türedi; (7) YASAL ORANLAR — D1'deki gibi kodda YOK ve D2 tutarları makbuzdan OKUYOR, yeniden HESAPLAMIYOR (`test_kodda_YASAL_ORAN_ya_da_VERGI_SABITI_YOK` bunu çiviliyor); KAYIT NUMARASI 0093 -> 0094 ÇÜNKÜ #50 BİRLEŞİRKEN 0093'Ü ALDI: numara elle seçilmedi, `python scripts/durum.py --sonraki 51` verdi ve satır içeriği BİREBİR taşındı; çakışmayı CI'ın `durum-kaydi` kapısı ve `test_CONTROL_gercek_kayit_YESIL` YAKALADI (ikisi de AYNI kök sebep: YİNELENEN sıra 0093), rebase sonrası ikisi de YEŞİL; BİRLEŞMİŞ AĞAÇTA İĞNELER YENİDEN ÖLÇÜLDÜ ve HİÇBİRİ KIMILDAMADI (#50 göç AÇMADI ve D2'nin dokunduğu hiçbir yüzeye değmedi): 161 kapı iddiası yeşil; D2 DALI PR'IN TEK DALIDIR (`feat/d2-avans-tescil`, worktree `F:/nazgul-d2`); açılıştan önce `gh pr list` BOŞTU ve `git ls-remote --heads origin`de D2 dalı YOKTU. IKINCI TUR — SAHIP KARARI: TEDARIKCI EKSTRESI DUZELTILDI ve bu artik OLCULMEDI DEGIL, YAPILDI. Kural: bakiye = acilis + SUM(alimlar) + SUM(makbuz net_payable) - SUM(odemeler). UC OKUMA YERI de degistirildi: `statement.py` (`_makbuz_borcu` artı ekstre SATIRI), `routers/finance.py` GET /suppliers `current_balance`, `routers/dashboard.py` `supplier_payables` (SEKIZINCI CTE). NET, BRUT DEGIL: stopaj ve SGK ciftciye degil VERGI DAIRESINE borctur (`tax_liabilities`), brutu yazmak firmanin ciftciye borcunu kesintiler kadar FAZLA gosterirdi. YALNIZ `issued`: taslak borc dogurmamistir, `cancelled` dogmus borcu KALDIRIR, `issuing` CAS'in ARA DURUMUDUR — bu yuzden OLUMLU liste kullanildi, `purchases` tarafindaki `NOT IN ('draft','cancelled')` kalibi DEGIL: olumsuz liste `issuing`i SESSIZCE borc sayardi. EKSTRE SATIRI DA EKLENDI (yalniz toplam degil): makbuz `_totals`a girip satirlarda GORUNMESEYDI ekstrenin YURUYEN BAKIYESI kendi kapanisini TUTMAZDI ve okuyucu farki hicbir satirda goremezdi. `finance.py`de alt sorgu ONCEDEN TOPLANMIS olarak baglandi (`pay` kalibi), duz LEFT JOIN DEGIL: satir coklayan bir birlestirme `purchases` ile kartezyen uretir ve alim toplamini makbuz sayisi kadar SISIRIRDI. KANIT TESTI (SQLite smoke senaryo 9): avans 100 -> makbuz brut 400 / stopaj 80 / sgk 20 / net 300 (mahsup 100, cash_due 200) -> odeme 200 => UC SITEDE DE bakiye 0; borc terimi olmasaydi ayni akis -300 okunurdu. Brut ile net BILEREK ayrisiyor (400 vs 300): borc terimi yanlislikla brutu toplasaydi bakiye 100 fazla cikardi. Ayrica IPTAL BORCU KALDIRIR ciftiyle civili (issued -> bakiye 100, cancel -> 0). SITE BAZLI MUTASYONLAR (ucu de tek tek, geri alindi): finance.py borc terimi dustu -> 'finance.py bakiyesi 0 degil: -300'; statement.py dustu -> 'statement.py kapanisi 0 degil: -300.00'; dashboard.py dustu -> 'dashboard.py supplier_payables deltasi -300.0' — HER MUTASYON KENDI SITESINI ADIYLA SOYLUYOR. PG IKIZINE IKI TEST EKLENDI (18 -> 20) ve GEREKCESI OLCULDU: `issued_at` bir `timestamptz`tir, oteki tarih sutunlari GUN dizgisidir; pencereyi onlarla ayni bicimde yazmak (`COALESCE(issued_at,'')`) PostgreSQLde TIP HATASI verir, SQLitede SESSIZCE gecer — bu yuzden gun `SUBSTR(CAST(r.issued_at AS TEXT),1,10)` ile metne indiriliyor. IDDIA OLCULDU: naif bicime geri donuldugunde SQLite 12/12 YESIL KALDI, PostgreSQL 2 test KIRMIZI oldu — yani kusur gelistirme diyalektinde GORUNMEZDI ve ikiz yerini HAK EDIYOR. ENVANTER: cekirdek Core sorgu envanteri KIMILDAMADI ve bu OLCULDU — uc site de ham `text()` kullaniyor, Core ifadesi DEGIL; kimildayan sey DINAMIK SQL envanteridir: `statement.py` cagri sayisi 4 -> 5 ve parmak izi c9a2d509 -> 6bc90e86, `routers/dashboard.py` parmak izi b87074fb -> 7176ad90 (cagri 2de SABIT), `routers/finance.py` parmak izi 6e4fdec8 -> e56aa3c1 (cagri 7de SABIT), kuresel toplam 257 -> 258; ayrica dashboard'un ALLOWLIST parmak izi 760c7fcc -> 6853bd18 (yedi CTE -> SEKIZ CTE) ve gerekcesi orada yazili. YEREL KUSUR, ADIYLA: `tests/test_entity_detail_accounting_totals.py` elle kurulan MINIMAL semada `producer_receipts` YOKTU ve kosulsuz sorgu 'no such table' ile dustu; dosyanin ZATEN VAR OLAN `_create_auxiliary_tables` yardimcisina tablo eklendi — sorguyu 'tablo varsa' diye SARMALAMAK, tablo bir gun gercekten eksik oldugunda borcu SESSIZCE sifirlardi. IKI KUSUR YINE TESTTEydi: ekstre ucunun yolu `/api/finance/suppliers` degil `/api/suppliers`; ve ekstrenin VARSAYILAN donemi AY BASI -> BUGUN oldugu icin ileri tarihli odemeler disarida kaliyor, makbuz sayilip odemeler sayilmayinca kapanis 300 okunuyordu — pencere artik ACIKCA veriliyor (2026-01-01..2026-12-31), yani test TARIHE GORE KAYMIYOR.
