#83 — WA2 EŞLEŞTİRME: numara → ERP kimliği defteri, FİRMA SEÇ ve dört yönetim ucu (göç `20260910_0079`, down_revision `20260910_0078`); ÖLÇÜLEN EKSİK: WA1 (#80) PLATFORM kuyruğunu açtı ve başlığında "numara hiçbir kullanıcıya BAĞLANMAZ" diyordu — ölçüldü, `alembic/versions/` altında `whatsapp_links` YOKTU ve `app/whatsapp/` altında kimlik çözen tek bir fonksiyon bile YOKTU, yani `whatsapp_inbound.sender_phone`daki rakam dizisini bir firmaya çeviren hiçbir şey yoktu; ÜÇ yeni tablo (`whatsapp_links`, `whatsapp_pairing_codes`, `whatsapp_context`) ÜÇÜ DE KİRACI TABLOSU — WA1'in kararının TERSİ ve gerekçe iki göçün başlığında karşılıklı yazılı: WA1'in tabloları "bu mesaj henüz kimseye ait değil" diyordu, bunlar tam olarak "bu numara KİMİN" sorusunun cevabıdır, sütun düşseydi cevap küresel olurdu; üçünde de `UNIQUE(company_id, id)` (0062'nin kuralı) ve `whatsapp_pairing_codes.consumed_link_id` BİLEŞİK anahtarla bağlı — bir firmanın kodu BAŞKA firmanın bağlantısını tüketmiş görünemez ve bu PG'de GERÇEK istekle kanıtlandı; KAYNAKTAN AYRILAN TEK YER aktif numara tekilidir: kaynak (`nazgul_website`) onu KÜRESEL yazıyor ("WhatsApp mesajında güvenilir bir firma SEÇİCİ yoktur") ama bu PR seçicinin kendisini getiriyor, o yüzden tekil `(company_id, phone) WHERE is_active` — küresel tekili korumak, muhasebecisi iki firmaya bakan kullanıcının İKİNCİ firmasına HİÇ bağlanamaması demekti; tekil KISMİDİR ve düz `UNIQUE(company_id, phone, is_active)` DEĞİL çünkü o hâlde pasif satırlar da anahtara girer ve aynı numara aynı firmada İKİ KEZ kapatılamazdı; yüklem DİYALEKTE GÖRE ayrı yazıldı (`is_active = 1` / `is_active = true`) ve gerekçe ölçüldü: SQLite'ın boolean'ı bir TAMSAYIDIR, tek metin yazmak indeksin bir diyalektte HİÇ kurulmamasına yol açardı ve kurulmamış bir tekil HİÇBİR ŞEYİ reddetmez — İKİ DİYALEKTTE DE hem RED hem İZİN ölçüldü (aynı firmada ikinci aktif satır → RED, başka firmada aynı numara → KABUL, pasif satır → KABUL); `kimlik_coz` kaynağın tersine LİSTE döner ve bu bir gevşetme DEĞİL: kaynağın "belirsizlikte rastgele seçim yanlış tenant'ın verisini dönmek demektir" cümlesi HALA uygulanıyor, değişen yalnız ÇARE — belirsizlik sessizce kapatılmıyor, kullanıcıya soruluyor (`FİRMA LİSTELE` / `FİRMA SEÇ <n>`, bağlam `whatsapp_context`te 30 dakika yaşıyor ve her okumada aday listesine karşı YENİDEN doğrulanıyor, yani üyeliği düşmüş bir seçim satır dursa bile çözülmez); UÇLAR +4 (POST/DELETE `/api/whatsapp/pairing-codes`, GET/DELETE `/api/whatsapp/links`), yetki `/api/whatsapp/` öneği üzerinden `users` ve bu ÖLÇÜLDÜ, varsayılmadı — kural yazılmadan ÖNCE `required_permission` çağrıldı ve aynı uç ailesinin metoda göre İKİ FARKLI kapıdan geçtiği görüldü (GET `read`e, POST/DELETE `__admin_only__`e), yani okuma yetkisi olan HER rol firmanın bota bağlı numaralarını görürdü; webhook uçları ETKİLENMEDİ ve bu da ölçüldü (ikisi de `PUBLIC_API`de, `required_permission` onlar için HİÇ çağrılmaz) — WA1'in `test_MUAFIYET_TAM_YOL_ve_ONEK_DEGIL` kapısının işe yaradığı ilk tur budur; düz kod YALNIZ POST cevabında bir kez döner (`no-store`), veritabanına yalnız SHA-256 özeti gider ve `GET /links` telefonu MASKELİ döndürür — ham numara hiçbir uçtan çıkmaz; İŞÇİ, META CEVABI ve GİDEN MESAJ BU PR'DA YOK: `komut_isle` bir DİZE döner ve hiçbir yere göndermez, kuyruğu bağlayan işçi WA3'ün işidir; kaynağın İKİ SÜPÜRÜCÜSÜ bu tura ALINMADI ve gerekçe ölçüldü — çağıranı yok, süre denetimi zaten `kod_kullan`da ve `sureleri_gecenleri_kapat` kiracı yüklemi taşıyamadığı için nöbetçide AYRI bir güvenlik istisnası harcardı; KAPILAR: `TENANT_TABLES` 116→119 (WA1'in iki tablosu HALA dışarıda — envanter şemadan türediği için üyelik `company_id`nin VARLIĞIDIR), dışa aktarım ×3, alembic başı 5 dosyada 0078→0079, GET envanteri 184→185 ve rota sözleşmeleri 385/295→389/299 (yeni `ROUTE_REASONS` grubu: kiracı kapsamlı eşleştirme yönetimi — üstteki "public Meta webhook" grubuna GİREMEZ çünkü o metin oturum ve kiracı bağlamı OLMADIĞINI vaat ediyor), yetkilendirme mutabakatı 373→377 ve `read`/REDDEDİLEMEYENLER SABİT kaldı — bu, önek kuralının TANIĞIDIR (kural olmasaydı `read` 1 artardı), Core sorgu envanteri 145→160 (ON BEŞ sorgu, ÜÇ dosya; `changed`/`stale` BOŞ, `UNRESOLVED_ALLOWLIST` BÜYÜMEDİ), kiracı nöbetçisi 122→139 ve istisna 1→3, `activity_log.py` dinamik-SQL parmak izi (SQL EKLENMEDİ, üç `ACTION_TYPES` literali — `log_activity` bilinmeyen tipi `ValueError` ile reddettiği için o üç satır uçların ÖNKOŞULU) ve KAPALI AKTİVİTE KATALOĞU 67→70 (izole süit buldu, tahmin değil; `RESOURCE_TYPES` 21'de SABİT çünkü üçü de bir KULLANICIYA erişim aracı verir/geri alır — DÖRDÜNCÜ eylem, yani KODUN TÜKETİLMESİ, BİLEREK açılmadı ve gerekçe kapıya yazıldı: o olayın aktörü ERP kullanıcısı değil WhatsApp'tan yazan numaradır ve `log_activity` `user_id`yi OTURUMDAN bekler, oturum YOKTUR; iz `consumed_at` + `consumed_link_id` + `whatsapp_links.created_at` üçlüsünde duruyor ve işçi gerçek bir aktör kimliği getirdiğinde sayı kımıldamak ZORUNDA), `alt_surec_sql.txt` ve `pg_twins.txt` +1'er satır, `types.gen.ts` yeniden üretildi (+238 satır) ve fixture'lar KIMILDAMADI; NÖBETÇİ İSTİSNASI SAYISI ÖLÇÜMLE DÜŞÜRÜLDÜ — ilk hâlde BEŞ ihlal vardı, `_denemeyi_artir` ve CAS'e `company_id` yüklemi eklendi ve `kimlik_coz` üyelik JOIN'ini bırakıp her adayı KİRACI YÜKLEMLİ `_hedef_dogrula`dan geçirir hâle geldi (böylece `test_memberships_ifadeleri_KULLANICI_anahtariyla_gecer` DEĞİŞMEDEN yeşil kaldı), kalan İKİSİ AYNI SINIFTAN: sorgunun ÜRETTİĞİ şey `company_id`nin KENDİSİDİR ve ikisi de hiçbir kiracı VERİSİ okumaz; WA1'İN ÜÇ KAPISI DARALTILDI çünkü PREMİSLERİ DEĞİŞTİ — `company_id` araması dosyanın TAMAMI yerine WA1'in İKİ TABLOSUNUN AST gövdesine indirildi, `create_all` yasağı "whatsapp.schema dizesi geçmesin"den "bu MetaData'yı kuran bir ÇAĞRI olmasın"a çevrildi (WA2'nin uçları o tabloları içe AKTARMAK zorunda ve import hiçbir şema kurmaz), kiracı envanteri iddiası 116'dan 119'a taşınıp WA1'in iki tablosunun HALA DIŞARIDA olduğu ayrıca çivilendi; TESTLER `tests/test_wa2_eslestirme.py` (31) + `test_wa2_eslestirme_postgresql.py` (9, GERÇEK PostgreSQL 16'da koştu); YARIŞ TESTİ ÖLÇÜMLE İKİ KEZ DÜZELTİLDİ ve ikisi de kayıtta — (1) ilk kurgu yirmi işçiye AYNI numarayı veriyordu ve CAS'i HİÇ ölçmüyordu (ikinci bağlantı zaten kısmi tekilde ölüyor, kodun iki kez tüketilmiş olması GÖRÜNMÜYORDU), yirmi AYRI numaraya geçildi; (2) o kurguda bile YALNIZ CAS'i düşüren mutant SAĞ KALDI ve sebep ölçüldü: `kod_kullan` bu değişmezi ÜÇ BAĞIMSIZ katmanla koruyor (PG satır kilidi, okuma sonrası durum denetimi, CAS) ve HER BİRİ TEK BAŞINA yetiyor, ÜÇÜ BİRDEN düşünce test 20 bağlantı görüp kırmızı oluyor — tablo testin başlığında, ve üç yerdeki "PG ikizi bu mutantı öldürür" iddiası ÖLÇÜLEN gerçekle DEĞİŞTİRİLDİ (katmanların TEK TEK çivilenmesi AST kapılarının işidir); KANONİK İZOLE SÜİT (`run_isolated_tests.py`) 425 aktif test dosyasının TAMAMINDA geçti, 2593 test sonucu; MUTASYON BATARYASI (SQLite, altı mutant, ALTISI DA ADIYLA ÖLDÜ): digest karşılaştırması `==`, CAS yüklemi düşürüldü, hız sınırı kapatıldı, bağlam yok sayıldı, kısmi tekil küresel yapıldı, kısmi tekil düz tekil yapıldı; `WhatsAppAsistan.tsx` SAYFA PORTU BU TURA ALINMADI (isteğin açıkça İSTEĞE BAĞLI bıraktığı parça) — dolayısıyla `frontend/e2e/rota-envanteri.ts` KIMILDAMADI ve `tsc -b` + rota kapsam sözleşmesi + `npm run build` üçü de yeşil; REBASE: #81 (`tests/pg_ikiz_yardimci.py` + `product_lots` teardown) ve #82 (31 ikize `_acilisa_cek`) indikten SONRA TABAN `27916d7`→`f53339f` olarak yeniden kuruldu; ÇAKIŞMA ÇIKMADI ve yamanın DEĞİŞMEDİĞİ ölçüldü — `git diff 27916d7 3be0b79 -- backend/app backend/alembic` ile `git diff f53339f HEAD -- backend/app backend/alembic` AYNI nesne özetini veriyor (`d38995e8`), yani üretim kodu ve göç BİT BİT aynı. 1B-A'nın alembic baş pini İKİ TARAFI DA korudu (bizim `20260910_0079` + develop'un `_acilisa_cek` eklemeleri) ve `pins/pg_twins.txt` 117→118 (tek satır, sıralı). PAYLAŞILAN YARDIMCI BENİMSENDİ: `test_wa2_eslestirme_postgresql.py` artık `pg_ikiz_yardimci.acilisa_cek`i İKİ UÇTAN (setup + teardown) çağırıyor — BU DOSYA İÇİN BUGÜN NO-OP ve bu ÖLÇÜLDÜ (bu ikiz `admin` olarak HİÇ giriş yapmıyor; kendi firmalarını ve kullanıcılarını ham SQL ile açıyor, `_temizle` yalnız KOŞU ÖNEKLİ kullanıcıları siliyor), çağrının bugünkü tek görünür etkisi yardımcının `_sync_sequences` adımıdır; iki uçtan çağrılmasının gerekçesi DOLAYLI YOLDUR — bu ikiz WA3'ün işçisiyle büyüyecek ve bir oturum smoke'u eklendiği an dosya sessizce paylaşılan şifreye bağımlı hâle gelir. PG ikizi rebase sonrası ÖZEL bir veritabanında (`lens_wa2_test`, paylaşılan `lens_test` DEĞİL) yeniden koştu: 9/9. TABAN `origin/develop` `f53339f`; kayıt adı `python scripts/durum.py --sonraki 83`
