#125 — PP3 PLATFORM YÖNETİM PANELİ ARAYÜZÜ (frontend, salt-okunur; backend/göç YOK): `/platform/*` altında altı ekran (`src/pages/platform/`) — Özet (PlatformOzeti, 60 sn tazeleme, 403 -> "yetki yok" paneli, boş sayfa değil), Şirketler (limit/offset, q + active süzgeci), Platform Kullanıcıları (limit/offset, q + verified, üyelik çipleri, bekleyen doğrulamalar /verifications ilk 20), Kuyruk Sağlığı (/outbox/health kanal başına + saha zamanlayıcısı), Güvenlik (/rate-limits pencere 1..168 sa + retention_hours notu; /platform/audit yalnız `limit` — ölçüldü, types.gen.ts'te tek parametre, yanıtı `unknown`, satır tipi auth.py audit_logs kolonlarından daraltıldı), E-Belge Sağlığı (/edocuments/health, sütunlar durumların birleşimi); tipler types.gen.ts `components`/`operations`tan; PP2 eylemleri devre dışı yer tutucu (`TODO(PP2)`, ipucu "PP2 ile gelecek"), yeni uç ÇAĞRILMAZ; Yedekler `/platform/yedekler`e taşındı, `/yedekler` adlandırılmış `<Navigate>` rotası olarak kaldı (izni `platform`); "Platform Yönetimi" nav grubu yalnız `can('platform')`ta (AppShell NAV_GROUPS okur, kod değişmedi); PİNLER: rota envanteri 71 -> 78 (brifteki 77 değil: yönlendirme App.tsx'te ayrıştırıcının saydığı bir `<Route path>`; gizlemek G1 körlüğü olurdu) — 8 platform girdisi `muaf`, gerekçe /yedekler ile AYNI metin (G5 kümesi 5 -> 12, eşitlik pini), menü 51 -> 57, grup 9 -> 10, BOOKMARKED 51 -> 57 (/yedekler çıktı, yönlendirme testi bekçi); test `can` kopyaları AuthContext kuralına çekildi (`*` platformu VERMEZ); TESTLER: 6 sayfa + App yönlendirme + nav/AppShell; tam vitest 99 dosya / 691 test: 674 yeşil, 16 kırmızının hepsi 5 sn zaman aşımı (8 dosya --testTimeout=30000 ile 74/74 yeşil), 1 kırmızı H37 Transactions (tek yeniden koşuda 8/8 yeşil); lint 0 hata, build yeşil
