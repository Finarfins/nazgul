#96 — SEC-4 (P1, C incelemesi bulgu 1) AÇILIŞTA GÖÇ SÜRMEK BİTTİ: üretimde uygulama şemaya HİÇ YAZMIYOR, açılış SALT OKUNUR bir KAPIYA dönüştü (GÖÇ YOK — `alembic/` diff'i BOŞ ve bu ölçüldü, alembic başı KIMILDAMADI); KUSUR: `app/main.py` MODÜL İTHALİNDE ve KOŞULSUZ olarak `database_bootstrap_lock` + `run_database_migrations` + `seed_bootstrap_data` koşuyordu, yani uygulamayı ithal eden HER süreç — `WEB_CONCURRENCY>1` ile açılan her uvicorn işçisi, her replika — cluster genelindeki TEK advisory kilit için (`POSTGRES_MIGRATION_LOCK_KEY`) sıraya giriyor ve kilidi `settings.migration_lock_timeout_seconds` (120 sn) içinde alamayan işçi `TimeoutError` ile AÇILIŞTA ÇÖKÜYORDU; kilit yalnız PostgreSQL'de var olduğu için SQLite yolu bu serileştirmeyi HİÇ yapmıyor, yani aynı anda açılan iki süreç aynı göçü birlikte sürebiliyordu; ÜRETİM O GÜN AUTO_MIGRATE=true İLE KOŞUYORDU ve bu VARSAYILMADI ÖLÇÜLDÜ: `docker-compose.prod.yml` AUTO_MIGRATE'i HİÇ yazmıyordu, taban `docker-compose.yml` ise `AUTO_MIGRATE: ${AUTO_MIGRATE:-true}` diyor ve Compose `environment` haritalarını -f dosyaları arasında BİRLEŞTİRİYOR — üretim app'i taban değeri devralıyordu; BU DİLİMİN EN ÖNEMLİ CÜMLESİ NE YAPILMADIĞIDIR: "bloğu lifespan'a taşımak" TEK BAŞINA HİÇBİR ŞEY ÇÖZMEZ, çünkü lifespan da işçi BAŞINA koşar — N işçi, N lifespan, aynı kilit, aynı yarış; yarışı bitiren şey bloğun YERİ değil üretimde YAZMAYI BIRAKMASIDIR, ve bulgunun (1) maddesinin ASIL yarısı olan "READ-ONLY by default" tam olarak budur; İKİ YOL ve İKİSİ AYNI ŞEYİ YAPMAZ — AUTO_MIGRATE=true (geliştirme/CI/testler) İTHALDE YAZAR (`_semayi_hazirla`: kilit + `alembic upgrade head` + bootstrap tohumu) ve İTHALDE KALDI, bu bir ihmal değil ÖLÇÜLMÜŞ BİR KARAR: depodaki alt-süreç testlerinin 98 dosyası `TestClient(app)`i lifespan'a GİRMEDEN kuruyor (`with` YOK) ve şemayı ithalden başka hiçbir yerden alamaz, ayrıca bulgunun (5) maddesindeki "two processes importing concurrently with auto_migrate" cümlesi de zaten ithal-zamanı göçü VARSAYAR; AUTO_MIGRATE=false (ÜRETİM) ise İTHALDE HİÇBİR ŞEY yapmaz — kilit YOK, DDL YOK, DML YOK — ve lifespan'da yalnız `_semayi_dogrula` koşar: `alembic_migration_status` okunur, güncel değilse `RuntimeError`; KAPI KİLİT ALMAZ ve bu bir ayrıntı değil KAPININ KENDİSİDİR — kilit alsaydı N işçi yine tek kilit için sıraya girerdi ve dilim hiçbir şey değiştirmemiş olurdu (iddia AST ile çivilendi: `_semayi_dogrula` içinde `database_bootstrap_lock` çağrısı SIFIR, `_semayi_hazirla` içinde BİR); HATA METNİ OPERATÖRE KOMUTU VERİR ve komut İKİ YERDE AYRI AYRI YAZILMADI: `ALEMBIC_UPGRADE_ARGV` sabiti hem hata metnine hem `deploy/sunucu-deploy.sh`e girer ve bir test ikisinin AYNI dize olduğunu ölçer — ayrı yazılsalardı biri değiştiğinde diğeri bir üretim olayında sessizce YANLIŞ komutu göstermeye devam ederdi; `docker-compose.prod.yml`de `AUTO_MIGRATE: "false"` SABİTTİR, `${AUTO_MIGRATE:-false}` DEĞİL ve bu bilinçli: `${...}` yazmak `.env.production`a tek satır ekleyerek üretimde auto-migrate'i geri açmayı mümkün kılardı, yani kapının tamamı tek bir operatör satırıyla kaybolurdu (kapı, arkasında açık kalan bir kapı bıraktığı sürece kapı değildir); `deploy/sunucu-deploy.sh` 7/7 -> 8/8 oldu ve YENİ 7/8 ADIMI `up -d app`ten ÖNCE koşuyor: `run --rm --no-deps app` ile önce `cd /app/backend && python -m alembic upgrade head`, SONRA `python -m app.bootstrap_data`; İKİ AYRI KOMUT ve bu SÖZLEŞMEDİR — tek komutta birleştirilseydi yarım kalmış bir göçün üstüne bootstrap DML'i yazma ihtimali açılırdı, `-e` yürürlükte olduğu için göç düşerse betik durur ve tohum HİÇ koşmaz; `python -m app.bootstrap_data` YENİ bir giriş noktasıdır ve `app.main`i İTHAL ETMEZ (testle çivilendi): etseydi tohumlama yönlendirici/ara katman kablolamasının tamamına ve AUTO_MIGRATE'e göre ithal-zamanı göçe bağımlı olurdu, yani deploy'un "önce göç sonra tohum" sırası kendi içinde ikinci kez ve GİZLİCE kurulurdu; TOHUMUN ETKİSİZLİĞİ (idempotency) BULGUNUN İSTEDİĞİ GİBİ KANITLANDI, BEYAN EDİLMEDİ: `app/bootstrap_data.py` okundu ve her `INSERT`'ün önünde onu koruyan bir `SELECT` olduğu görüldü (yönetici, firma, şube, üyelik, depo, depo stoğu, üç finans hesabı), ÜSTÜNE tohum ikinci kez koşturulup YEDİ TABLONUN sayımı TABLO BAZINDA karşılaştırıldı — tek bir toplam, bir tabloda artıp diğerinde eksilen bir sapmayı gizleyebilirdi; AUTO_MIGRATE KOD VARSAYILANI `True` KALDI ve CI'nın ona dayandığı ÖLÇÜLDÜ (`ci.yml` duman testi `-e AUTO_MIGRATE=true`, `deploy/veri-kaliciligi-kabul-testi.sh` aynısı, taban compose `${AUTO_MIGRATE:-true}` — üçü de KIMILDAMADI); TESTLER: `backend/tests/test_sec4_acilis_migrasyon_kapisi.py` (15 — davranış: AUTO_MIGRATE=false iken ithal BAŞARILI ve veritabanı ŞEMASIZ kalıyor [`alembic_version` ve `app_users` tablolarının YOKLUĞU ölçülüyor, bir niyet beyanı değil], güncel OLMAYAN şemada lifespan DURUYOR ve komutu basıyor, GÜNCEL şemada lifespan normal AÇILIYOR [kapı bir DUVAR değil], tohum idempotent; statik: prod compose sabit `"false"`, taban compose `true` KIMILDAMADI, `Settings.auto_migrate` varsayılanı `True`, ci.yml LAFZEN okunuyor, deploy betiğinde göç < tohum < `up -d app` SIRASI indeksle ölçülüyor, hata metnindeki komut betiktekiyle AYNI, modül gövdesinde KORUMASIZ şema çağrısı YOK ve tam olarak BİR `settings.auto_migrate` koşulu var) + `backend/test_sec4_acilis_migrasyon_postgresql.py` (2, PG İKİZİ — kusur SQLite'ta GÖRÜNMEZ çünkü `database_bootstrap_lock` PostgreSQL DIŞINDA no-op'tur: boş bir şemada iki süreç `app.main`i AYNI ANDA ithal ediyor ve İKİSİ DE 0 ile çıkıp `alembic_version` TEK satır, firma/depo TEK kopya, finans hesabı ÜÇ kalıyor; ayrıca "TAM OLARAK BİRİ sürüyor" iddiası AYRI ölçülüyor çünkü ilk ölçüm onu SÖYLEYEMEZ — her süreç kilidi ALDIKTAN SONRA `current_revision`ı okuyup bildiriyor ve tam bir `SURDUM` + bir `BULDUM` bekleniyor); İKİZ VACUOUS DEĞİL ve bu ÖLÇÜLDÜ: `database_bootstrap_lock`un PostgreSQL dalı devre dışı bırakıldığında İKİ TEST DE KIRMIZI yandı, sonra dosya bayt bayt geri kondu; İKİ MUTANT ve İKİSİ DE SAĞLAM KOŞUMUN GEÇTİĞİ SENARYONUN TA KENDİSİNDE ölüyor — `BLOK_ITHALE_GERI` (`if settings.auto_migrate:` -> `if True:`, yani kusurun ta kendisi: AUTO_MIGRATE=false olan üretim işçisi yine ithalde kilidi alıp göç sürerdi) ve `DURUM_KONTROLU_DUSURULDU` (lifespan'daki `_semayi_dogrula()` çağrısı düşürülür, yani şeması OLMAYAN bir süreç trafik almaya başlar); mutasyonlar dosyayı YERİNDE değiştirip `finally`de BAYT BAYT geri koyuyor ve geri koymanın kendisi de assert ediliyor; KAPILAR: `ci.yml`e çözümlenmiş `.services.app.environment.AUTO_MIGRATE == "false"` kapısı eklendi ve METİN GREP'İ DEĞİL JSON ALAN KARŞILAŞTIRMASIDIR (dosyadaki satır `${...}` biçimine geri döndürülseydi grep'e aynı görünürdü ama `.env.production`dan geri açılabilir olurdu), `pins/pg_twins.txt` 122 -> 123 (yeni PG ikizi) ve `SEC4_TEST_DATABASE_URL` `backend-postgresql` işine eklendi; DOKÜMAN: `docs/ops/DEPLOY.md`e "Şema göçü — uygulama DEĞİL, deploy sürer" bölümü eklendi (neden, bugün ne oluyor, ve "açılış şema güncel değil diye durursa" — o durumda elle `AUTO_MIGRATE=true` verip işçilere göç sürdürmek YAPILMAZ, kapının kapattığı yarışın ta kendisidir) ve betikteki adım sayacı değiştiği için 2/7 atfı 2/8 olarak düzeltildi; kayıt adı `python scripts/durum.py --sonraki 96`
