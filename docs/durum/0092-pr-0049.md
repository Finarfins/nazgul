#49 — AYNI HASADIN FİŞ OLAYLARI PG'DE SERİ: mercek iki `field_harvest_ticket` olayını aynı hasatta READ COMMITTED altında eşzamanlı işledi, ikisi de `_zaten_duzeltilmis`i 0 okudu ve 25/25 koşum +2400 yazdı (doğru fark +1200: hasat 1000 KG, fiş 2 TON %10+%5 net 1700, fiş 500 KG net 500, Σ net 2200); `_talep_et` OLAYI kilitler HASADI değil — iki ayrı PENDING, iki ayrı talep, tek hasat; Şef: birleşmeden ÖNCE kapat, sonra değil; TABAN `feat/kantar-neti-stoga` `83643e7` (PR #46 C2) / `origin/develop` `9e02fb8`; kayıt `python scripts/durum.py --sonraki 49` → `0092-pr-0049.md`; GÖÇ YOK; **NE DEĞİŞTİ**: `_bir_olayi_isle` içinde yalnız fiş olaylarında, `_fis_kalemleri`/`_zaten_duzeltilmis` OKUNMADAN ÖNCE `_hasati_serilestir` — PG `SELECT id FROM field_harvests WHERE id=:hid AND company_id=:cid FOR UPDATE` (önce tickets'tan harvest_id, ikisi de sabit `text()`, `:cid` bağlı; kilit mevcut tek commit'e kadar), SQLite tek yazar no-op (SQL KOSMAZ, `FOR UPDATE` sözdizimi reddedilir) ve bu dal `test_hasat_serilestirme_SQLITE_NOOP_FOR_UPDATE_KOSMAZ` ile çivilendi; **TEST**: mevcut ikiz `test_mercek_iki_fis_olayi_ayni_hasatta_eszamanli_zaten_0_ASIRI_DUZELTME_yazmiyor` — bariyerli iki işçi, işçi başına `_bir_olayi_isle` (olaylari_isle bir işçinin ikisini yemesine izin verirdi), ×10, her seferinde `DUZELTME 1200.0000` / `DURUM SENT SENT`; yeni `*_postgresql.py` YOK (küme 102); **MUTASYON** (yerel, commit edilmedi): `FOR UPDATE` düşürülünce kosum=0 `DUZELTME 2400.0000 SATIR 2 DURUM SENT SENT`, ifade geri konunca mercek yeşil; **ÖLÇÜM** (CPython 3.12.3, PG 16.15): izole `run_isolated_tests.py --workers 2` dört dosya 54/54 (test_field_stok_tuketici 20, tenant_scoping_guard 28, kantar_neti_stoga 2, field_stok_korunum_denklemi 4); PG `REQUIRE_PG=1` taze şema `test_field_stok_tuketici_postgresql.py` **18 passed / 0 skipped** (146s), `test_kantar_fisi_postgresql.py` **16 passed / 0 skipped**; `field_stok_tuketici.py` dinamik-SQL izin listesine GİRMEDİ (sabit metin, parmak izi YOKTU türetilmedi); alt süreç SQL 108/172 → **108/175**; `deploy/ci-required-contexts.json` ve `ci.yml` DOKUNULMADI; koşu `bc-faa41a0e-2fe1-41b8-86cb-0fd212922efc`.
