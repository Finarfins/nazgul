#76 — 5.4c PUSH CİHAZ DEFTERİ: göç `20260909_0077` (`push_devices`; tekil `(company_id,token)` — aynı jeton İKİ firmada AYRI satır, `platform` CHECK'i `('android','ios','web')`, `session_family_id` FK DEĞİL çünkü 5.4a'nın "ailesi" bir TABLO değil `auth_refresh_tokens.family_id` TEKİL OLMAYAN sütunudur ve kapı o OLGUYU ölçüyor); ÜÇ uç `POST/GET /api/push/devices` + `DELETE /api/push/devices/{id}`, izin `/api/push/` önek kuralından `read` (alternatifler tek tek elendi: kural yazmamak POST+DELETE'i `__admin_only__`e düşürürdü, `SELF_SERVICE_API` tam-eşleşmedir ve `{device_id}` giremez), DELETE'in sınırı ROL DEĞİL SAHİPLİK (başkası 403, başka firma 404); `logout-all` cihazları da düşürüyor ve süpürge FİRMA FİRMA dönüyor (kiracı yüklemi her yazımda AÇIK); kanal `schema.PUSH` — `CONSENT_REQUIRED_CHANNELS`e BİLEREK girmedi (rıza defteri yalnız CUSTOMER/SUPPLIER tanır, girseydi her push doğduğu an REJECTED olurdu) ve `rules.CHANNELS`e de girmedi (kural motoru alıcıyı müşteriden çözer), sağlayıcı KANALA ÇİVİLİ ayara DEĞİL (aksi hâlde smtp ayarlı kurulumda cihaz jetonuna MAIL atılırdı), `PushNotificationProvider` AĞA ÇIKMAZ ve `SENT` YAZMAZ — GERÇEK TESLİMAT ÖLÇÜLMEDİ, `SIMULATED` bunu söylüyor; `test_undeniable_endpoint_population`a ÜÇÜNCÜ çapa kategorisi `SELF_SCOPED_WRITE_EXEMPTIONS` ("evrensel izinli yazma serbest ANCAK VE ANCAK yazılan satırın öznesi çağıranın kendisiyse") ve GERÇEK istekle kanıtı YÖN C (satis rolü KENDİ cihazını kaydedip düşürüyor) + YÖN D (başkasınınkini düşüremiyor, 403), iki çapa AYRIK ve ayrıklık ayrı kapıda; ÇİVİLER YENİDEN ÖLÇÜLDÜ: TENANT_TABLES 115->116, dışa aktarım ×3 115->116, alembic başı 5 dosyada `20260909_0076`->`20260909_0077`, kimliklenmiş/read/undeniable 369/95/110->372/98/113, çıplak read 69->72, GET izin envanteri 181->182 (parmak izi `1a615fb1`->`cfb171c3`), rota güvenlik sözleşmesi 379/291->382/293 (parmak izi `25fa635c`->`16a0ec60`, `ROUTE_REASONS`a AYRI grup: üç uç kiracı kapsamlıdan DAHA DAR çünkü `user_id` de OTURUMDAN), dinamik-SQL parmak izleri `notifications/service.py` `39f5416b`->`837a1cd4` ve `notifications/templates.py` `227df912`->`f2113486` (dinamik çağrı sayıları ve KÜRESEL TOPLAM 268 SABİT), pin envanterleri +2 satır (`alt_surec_sql.txt`, `pg_twins.txt`), sözleşme zinciri yeniden üretildi (fixture drift YOK); KANIT: `PYTHONUTF8=1 python run_isolated_tests.py --workers 4` → 415/415 dosya, 2393 test, 0 kırmızı (830.2s) ve PG16 ikizi `test_54c_push_devices_postgresql.py` → 16 passed (20 eşzamanlı kayıt TEK SATIR ve HİÇ 5xx yok, tekil/CHECK/7 NOT NULL GERÇEKTEN reddediyor, `is_active` gerçek BOOLEAN, `last_seen_at`/`created_at` TIMESTAMPTZ, göç turu up->down->up GERÇEK PostgreSQL'de), komşu PG ikizleri taze şemada 5.4b 15 + bildirim ×4 ve F0-email 14, SQLite ikizi 15, `tsc -b` ve `npm run build` temiz; 17 bağlam SABİT; TABAN develop `2a22212` (#75/1B-F birleştikten SONRA `origin/develop` üzerine yeniden sürüldü; çakışma YALNIZ `tests/pins/*.txt`te bekleniyordu ve git İKİ SATIRI DA sıralı biçimde birleştirdi — ÖLÇÜLDÜ: iki dosya da sıralı, sondaki satır sonu yerinde, `pg_twins.txt` 115 ve `alt_surec_sql.txt` 125 satır. ÇİVİLER YENİDEN ÖLÇÜLDÜ ve HİÇBİRİ KIMILDAMADI: 1B-F göç AÇMADI ve rota EKLEMEDİ, yani TENANT_TABLES 116, rota envanterleri 372/98/113 ve 382/293, GET envanteri 182 AYNEN geçerli. Benim yamam BİREBİR AYNI: `git diff 77be889 3881ccd` ile `git diff 2a22212 397795f` (`app/field_stok_tuketici.py` hariç — o 1B-F'nin dosyası ve bu dal ona HİÇ dokunmadı) AYNI nesne özetini veriyor: `8748d21e`. Önceki TABAN `77be889`); dal `feat/54c-push-devices`; `parti_defteri`/`field_stok_tuketici` (1B-F) ELLE SÜRÜLMEDİ; `--sonraki 76`
