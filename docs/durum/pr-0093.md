#93 — SEC-1 (P1, guvenlik incelemesi A/1) KAPANDI: WhatsApp eslestirme kodu artik bir HEDEF NUMARAYA bagli ve YALNIZ o numaradan kullanilabilir (GOC VAR: `20260912_0082_pairing_target_phone`, down_revision `20260911_0081`); ACIK OLCULDU IDDIA EDILMEDI: 0079'un actigi `whatsapp_pairing_codes` satiri "hangi firma, hangi kullanici" sorusunu cevapliyordu (`company_id` + `user_id`) ama "HANGI NUMARA" sorusunu HIC sormuyordu ve `eslestirme.kod_kullan` satiri YALNIZ `code_digest` ile bulup baglantiyi CAGIRANIN numarasiyla aciyordu — yani B firmasinin kodunu ele geciren biri (ekran goruntusu, iletilmis mesaj) KENDI numarasini B'nin kullanicisina baglayabiliyor ve o numaradan gelen her mesaj B'nin borc/stok/tahsilat verisini goruyordu; kod bir SIRDIR ama TEK BASINA bir KIMLIK DEGILDIR ve ayrim tam olarak buydu; SUTUN `target_phone VARCHAR(20) NOT NULL` ve GENISLIK BIR SUS DEGIL: `whatsapp_links.phone` ile BIREBIR ayni, cunku `kod_kullan` iki degeri KARSILASTIRIYOR; saklanan bicim kanonik `telefon.normalize_phone` ciktisidir (ulke kodlu, ISARETSIZ) ve `+90...` SAKLANMIYOR — E.164 saklansaydi karsilastirma her seferinde bir donusum daha ister, o donusumu unutan bir yol HICBIR kodu eslestiremez ve acigin TERSI bir kusur dogardi; UCTA `phone` ZORUNLU (`POST /api/whatsapp/pairing-codes`, `KodGirdisi`) ve VARSAYILANI YOK — varsayilan verilseydi alani yazmayan eski bir istemci sessizce HEDEFSIZ kod uretmeye devam eder, kapatilan acik KAPALI GORUNURKEN ACIK kalirdi; bicim `telefon.e164` ile DOGRULANIYOR ("0540 599 59 59" kabul, "abc"/"12345" 422) ve dogrulama `_hedef_dogrula`dan SONRA kosuyor ki hedef reddi (BES yolun AYNI metni) bicim hatasindan ONCE gelsin ve envanter sizmasin; CEVAP MASKELI (`***5959`) ve TAM NUMARA CEVABA GIRMIYOR: yonetici numarayi ZATEN kendisi yazdi, geri yazmak `GET /links`in maskeleme kararini (defterin amaci "kim bagli", "hangi numaradan" degil) bu uctan delerdi; SIRA SOZLESMEDIR: hedef denetimi BAGLANTI INSERT'INDEN ONCEDIR ve durum/sure denetimlerinden SONRA — sonraya alan bir mutant DAVRANISTA AYNI gorunur (SAVEPOINT geri alir, `basarili=False`, baglanti yok) yani HICBIR davranis testi onu olduremez ve kapi tam bu yuzden AST'dedir (`tests/test_wa2_eslestirme.py::test_HEDEF_NUMARA_DENETIMI_BAGLANTI_INSERTINDEN_ONCE`, WA1'in `compare_digest` ve WA2'nin CAS kapilariyla AYNI kalipta); kapi UC AYRI SEY olcuyor — kiyasin VARLIGI (AST'de `target_phone` alt agaci, `str(...)` sarmalayicisini de goren yuruyus), OPERANDLARI (`normal` GERCEKTEN kiyasin icinde) ve SATIR NUMARASIYLA INSERT'ten ONCE oldugu; dizge aramasi kullanilmadi cunku `target_phone` kelimesi fonksiyonun KENDI BASLIGINDA da geciyor ve `parca.index(...)` orayi bulup kapinin tamamini anlamsiz kilardi (bu once YAZILDI, sonra KIRMIZI GORULDU, sonra AST'ye tasindi); RET AYIRT EDILEMEZ: yanlis numaradan gelen DOGRU kod ile HIC VAR OLMAMIS kod BIREBIR AYNI `EslestirmeSonucu`yu alir (test iki nesneyi `==` ile karsilastiriyor, alan alan degil) — ayrilsaydi saldirgan elindeki kodun GECERLI oldugunu ogrenirdi, yani bir KAHIN; kapinin ucuncu assert'i dalin govdesinde `RED_MESAJI`/`raise` OLMADIGINI da olcuyor; SAYACLAR YANIYOR: yanlis numara kodun KENDI `attempt_count`unu artiriyor (`_denemeyi_artir`, AYRI deyim, geri alinmaz) ve saldirganin telefon+pencere sayacina da yaziliyor; tavan dolunca kod KILITLENIYOR ve DOGRU numara bile gecemiyor — bu bir KAYIP DEGIL, kodun sizdiginin GORUNUR kaydidir ve care ucuz (yeni kod eskisini deterministik olarak iptal eder); MEVCUT SATIRLAR: tablo 0079 ile (2026-09-10) dogdu ve URETIMDE SATIRI YOK, ama goc "satir yok" VARSAYIMIYLA yazilmadi — (1) sutun `server_default=''` ile aciliyor ve BOS DIZE FAIL-CLOSED'dur cunku `kod_kullan` cagiranin numarasini `normalize_phone`dan geciriyor ve BOS sonucta HEMEN donuyor, yani `''` tasiyan satir HICBIR cagiranla eslesemez; (2) buna RAGMEN mevcut butun `PENDING` satirlar `EXPIRED` yaziliyor — (1) tek basina yeterdi, (2) niyeti SEMAYA yaziyor; gecis 0079'un `ck_wpc_expired_temiz` CHECK'ini KIRMIYOR cunku `ck_wpc_pending_temiz` ucunun de (`consumed_at`,`cancelled_at`,`consumed_link_id`) NULL oldugunu ZATEN zorluyor, yani iki CHECK ayni satir kumesinde ayni sarti istiyor ve gecis TANIMLI; `server_default` DUSURULMEDI ve gerekce OLCULDU: PG'de `DROP DEFAULT` mumkun, SQLite'ta DEGIL (ayni sonuc icin tablo YENIDEN KURULMALI) ve bu tablonun `sqlite_where` yuklemli KISMI TEKIL indeksleri var (`uq_wpc_aktif_kod`) — yeniden kurma onlari sessizce kaybetme riskini tasir ve kazanc YOK (varsayilan `''`dir ve `''` fail-closed'dur); birinde dusurulup otekinde birakilsaydi Alembic'in kurdugu sema IKI LEHCEDE AYRISIR ve `schema.py`nin Core tanimi ikisinden YALNIZ BIRIYLE ortusurdu, o yuzden Core tanim da AYNI `server_default`u tasiyor ve goc ile `metadata` arasinda fark YOK; `downgrade()` sutunu dusuruyor ve up->down->up GERCEK PG 16'da kosuldu; SURESI DOLDURULAN SATIRLAR GERI GELMIYOR ve bu bir eksiklik DEGIL karardir — onlari `PENDING`e dondurmek tam da bu gocun kapattigi acigi GERI ACMAK olurdu, `downgrade` bir SEMA islemidir guvenlik olayini geri sarmaz; TESTLER SQLite (`tests/test_wa2_eslestirme.py` 31 -> 38): AST kapisi + CAPRAZ KIRACI adimi (incelemenin senaryosu birebir; UC AYRI IDDIA — yanlis numara reddediliyor ve baglanti YOK, kod HALA `PENDING` yani ret bir HIZMET KESINTISI degil, DOGRU numara AYNI kodu kullanip BAGLANIYOR; ucuncusu olmasaydi `kod_kullan`i hep `False` donduren bir mutant SAG CIKARDI) + YANLIS NUMARA DENEMEYI YAKIYOR + HEDEFSIZ KOD HICBIR NUMARADAN KULLANILAMIYOR + HEDEF KANONIKLESIYOR ("0540 599 59 59" -> `905405995959`) + GECERSIZ HEDEF KOD URETTIRMIYOR (satir da YAZILMIYOR) + UC adimi (`phone` yok -> 422, bozuk -> 422, dogru -> 201 + MASKELI + saklanan KANONIK); IKI MEVCUT ADIM ONARILDI ve onarimin sebebi KAYDA GECIYOR cunku ikisi de DUZELTMENIN KENDISI yuzunden kirmiziya dondu: (a) `test_DAVRANIS_kod_uret_KULLAN_baglanti_aciyor`un "ayni kod ikinci kez" adimi `IKINCI_NUMARA` kullaniyordu ve artik hedef denetiminde duserdi — yani adim "kod tukendi"yi degil "numara tutmadi"yi olcerdi; `NUMARA`ya cevrildi (durum denetimi hedef denetiminden ONCE kosuyor, yani ret GERCEKTEN tukenmislikten geliyor); (b) hiz siniri adiminin son satiri `IKINCI_NUMARA`ya `NUMARA` icin uretilmis kodu veriyordu — artik `IKINCI_NUMARA` icin KENDI kodu uretiliyor (BASKA kullaniciya, cunku `uq_wpc_aktif_kod` ayni kullanicida ikinci PENDING koda izin vermez) ve olculen sey yine SINIRIN TELEFON BASINA oldugu; GOC TURU BETIGI BUYUDU (alt surec, SQLite): `target_phone` NOT NULL + genislik 20 pinlendi ve 0081'e INILIP GERCEK bir HEDEFSIZ `PENDING` satir yazilip goc kosuluyor — yani gocun kapattigi acigin ta kendisi uretiliyor — sonra satirin `status='EXPIRED'` ve `target_phone=''` oldugu olculuyor; kaynagi grep'lemek YETMEZDI, `upgrade` govdesindeki UPDATE'i OKUMAK onun KOSTUGUNU soylemez; PG IKIZI (`test_wa2_eslestirme_postgresql.py` 10 -> 13): `target_phone` GERCEK PG'de NOT NULL + VARCHAR(20) ve `whatsapp_links.phone` ile AYNI genislikte (SQLite uzunlugu ZORLAMAZ, olcum yalniz burada anlamli) + `server_default` GERCEKTEN SUNUCUDA (sutunu HIC yazmayan bir INSERT NOT NULL'i geciyor; varsayilan yalniz Python tarafinda olsaydi `NotNullViolation` duserdi) ve bos hedef FAIL-CLOSED + CAPRAZ KIRACI senaryosu (SQLite'ta da var ama BURADA TEKRARLANMASININ GEREKCESI SATIR KILIDIDIR: `kod_kullan` PG'de `SELECT ... FOR UPDATE` ile okuyor ve o dal SQLite'ta HIC KOSMAZ, yani "denetim kilitsiz yolda var kilitli yolda yok" ayrismasi YALNIZ burada gorulebilirdi) + 0082 goc turu (0081'e inip hedefsiz satir yazip head'e cikmak; PG'YE OZGU OLAN `ck_wpc_expired_temiz`in GERCEKTEN yansitilmasidir — SQLite CHECK'i yansitmaz, 0072'de olculdu); YARIS ADIMI YENIDEN KURULDU ve ESKI GEREKCE SILINMEDI KAYDEDILDI: eski kurgu YIRMI AYRI NUMARA kullaniyordu ve o secim de bir olcumle gelmisti (daha eskisi ayni numarayi veriyordu ve kodun iki kez tuketilmis olmasini HIC GOREMIYORDU, cunku ikinci baglanti zaten `uq_whatsapp_links_aktif_numara`da oluyordu); 0082'den sonra o kurgu IMKANSIZ — yirmi ayri numaranin ON DOKUZU YARISA HIC GIRMEZ, hedef denetiminde CAS'e varmadan duserler, yani bugun yazilsaydi "yirmi isci yarisiyor" diye okunur GERCEKTE TEK ISCI yarisirdi; yeni kurgu YIRMI ISCI TEK NUMARA; YENI KURGUNUN IKI BEDELI ACIKCA YAZILDI, GIZLENMEDI: (1) hiz siniri artik YARISIN ICINDE — sinir telefon basinadir (`PAIRING_PENCERE_SINIRI`=5) yani yirmi iscinin BESI CAS'e kadar ilerliyor, on besi `sinirda` dalinda donuyor; sinirı test icin GEVSETMEK, uretimde ASLA olusamayacak bir yarisi olcmek olurdu (tek numara zaten pencere basina bes kez deneyebilir) ve `for_update` sayaci YINE 20 cunku sinir denetimi KILITLI OKUMADAN SONRA geliyor; (2) ayni numarayla `uq_whatsapp_links_aktif_numara` yine bir HAKEM, yani bu test TEK BASINA "kod iki kez tuketildi" halini artik ayirt edemez — ayirt eden sey `cas_denemesi == 1` olcumudur ve o SURUCU SEVIYESINDEN (`before_cursor_execute`) sayiliyor, uygulamanin kendi raporundan DEGIL; ayrica YENI bir iddia eklendi: kazanan baglantinin `phone`u ile kodun `target_phone`u AYNI (hedefi denetleyip baglantiyi BASKA degerle acan bir mutant yalniz burada gorunur); MUTASYON BATARYASI KOSULDU (dordu de KIRMIZI, ADIYLA): M1 hedef kiyasi SILINDI -> AST kapisi + UC davranis adimi; M2 kiyas BAGLANTI INSERT'INDEN SONRA -> AST kapisi + DENEME YAKIYOR adimi ama CAPRAZ KIRACI adimi YESIL KALIYOR (tam olarak beklenen: SAVEPOINT geri aldigi icin davranis AYNI gorunuyor — AST kapisinin var olma sebebi bu olcumdur, iddia degil); M3 hedef uyusmazligina AYRI cevap -> AST kapisi + CAPRAZ KIRACI'nin "ayirt edilemez" iddiasi; M4 ucta `phone` ISTEGE BAGLI -> TELEFON ZORUNLU adimi; batarya sirasinda COMMIT YAPILMADI ve kaynaklar `cp` ile geri yazildi; PG16 PARITE: WA2 ikizi ARDISIK IKI KOSUDA 13/13 (paylasilan semada kalinti YOK) ve head pinini tasiyan komsular AYRI TAZE veritabanlarinda ayri ayri yesil; ALEMBIC BASI 0081 -> 0082 ve PIN YEDI YERDE, ALTISI GREP'LE BULUNDU YEDINCISI CI'DAN GELDI: `tests/test_goc_zinciri.py` (uc satir — bas, "bas kendi atasinin atasi olamaz", CLI ciktisi), `tests/test_e1b_plantback.py`, `tests/test_e2_tedavi_arinma.py`, `tests/test_e3_karantina.py`, `test_1b_a_alis_lot_postgresql.py`, `test_wa3_worker_postgresql.py` ve YEDINCISI `test_e1_efatura_sertlestirme_postgresql.py` — orada pin `GOC = "20260911_0081"` sabitinin ICINDE gizliydi ve grep onu "gocun KENDI kimligi" diye okuttu; sabit IKIYE AYRILDI (`GOC` bu dosyanin KONUSU olan gocun kimligi ve up->down->up turunun hedefi, `BAS` semanin o an bulundugu yer) cunku ikisi 0082'ye kadar TESADUFEN ayni degerdi ve tek sabitte kalsaydi basi guncelleyen biri bu dosyanin GOC TURUNU da farkinda olmadan BASKA bir goce cevirirdi; ayni dosyanin tur testine kapanis `command.upgrade(head)` EKLENDI — 0082'ye kadar gerek yoktu (0081 zaten BASTI) ama artik yok ve semayi 0081'de birakmak CI'da ayni konteyneri paylasan KOMSU dosyalara eksik sema birakirdi (WA2 ikizinin AYNI sozlesmesi); DIGER PINLER: core sorgu envanteri `kod_kullan::select` parmak izi d45429e0 -> 6b51156b ve envanter parmak izi 3ae3e6f6 -> 732a6bde — SORGU SAYISI (160) ve HEDEFI KIMILDAMADI, degisen tek sey SECILEN SUTUN KUMESI (`target_phone` eklendi) ve yuklem (`code_digest == :ozet`) AYNI; `insert(...)` bu kapinin KAPSAMI DISINDA oldugu icin `kod_uret`in yeni sutunu envantere HIC dokunmadi (dosya basliginda yazili); ayni parmak izi `test_core_tenant_scoping_guard.py`nin kiraci ISTISNASINDA da anahtardi ve orada DA guncellendi — istisnanin GEREKCESI de yeniden yazildi cunku SEC-1 onu DARALTTI: ozeti bilmek artik TEK BASINA YETMIYOR; KIMILDAMAYANLAR OLCULDU: `TENANT_TABLES` (yeni tablo yok, yalniz sutun), rota envanterleri 391/301 ve parmak izi (yol ve metot DEGISMEDI — degisen yalniz GOVDE), GET izin envanteri, yetki uzlastirmasi, kapali aktivite katalogu (`user.whatsapp_pairing_code_created` ZATEN vardi), `pins/pg_twins.txt` (ikiz BUYUDU, YENISI EKLENMEDI), `pins/alt_surec_sql.txt`, `frontend/src/test/fixtures`; URETILENLER: `types.gen.ts` +7 satir (`KodGirdisi.phone: string` ZORUNLU olarak ve ucun aciklamasi) ve `backend/openapi.json` `.gitignore`dadir; frontend fixture'lari YENIDEN URETILDI ve DEGISMEDI; DOKUMAN: `docs/whatsapp/WA3_BILINEN_SINIRLAR.md`e "KAPANDI — SEC-1" bolumu (acik/sonuc/duzeltme/ayirt edilemezlik/sayaclar/mevcut satirlar/sabitleyen tablosu + sira sozlesmesi ve dort mutantin olcumu) ve CAS bolumune 0082'nin kurguyu DEGISTIRDIGI notu (eski olcumun HANGI kurguya ait oldugu kaybolmasin diye), `docs/whatsapp/WA4_KANALLAR.md` §2.2'ye riza gerekcesinin ARTIK DAHA GUCLU oldugu notu (eslestirme artik "kodu bilen biri" degil "kodu bilen VE yoneticinin yazdigi numaradan yazan" kisi tarafindan tamamlanabiliyor); WA2'nin kendi notlari `app/whatsapp/eslestirme.py` modul basliginda ve `kod_kullan` sozlesmesinde guncellendi; kayit adi `python scripts/durum.py --sonraki 93`
