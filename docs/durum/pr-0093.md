#93 — E2b ÖLÇÜM KOŞUSU YAPILAMADI ve bu DÜRÜSTÇE KAYDA GEÇTİ: tek değişiklik `docs/izibiz-sandbox-bulgular.md` §8 başlığındaki GELECEK TARİHİN (`2026-09-11`) koşunun gerçekten işlendiği tarihe (`2026-09-09`, commit `24a6478`) çekilmesidir; §7.2'deki `2026-09-11` alıntılarına DOKUNULMADI çünkü onlar sağlayıcının `ERROR_CODE=10003 SCHEMATRON` ile reddettiği DEĞERİN KENDİSİDİR ve düzeltilseydi ölçümün kaydı yalanlanırdı; §9 (hipotez→istek→yanıt kodu→hüküm) YAZILMADI ve bu bir ihmal DEĞİL bir karardır — koşuyu yapan ortamda `efaturatest.izibiz.com.tr:443` çıkışı ağ politikasınca 403 ile reddediliyor (proxy `connect_rejected` kaydıyla ÖLÇÜLDÜ) ve `IZIBIZ_ENV_FILE` tanımsız, yani H1/H2/H3/H4'ün hiçbiri için GERÇEK bir SOAP istek/yanıt üretilemedi; ölçülmemiş bir satırı 'ölçüldü' diye yazmak `izibiz-sandbox-bulgular.md`nin var oluş sebebinin TERSİ olurdu; ağa çıkmadan okunabilenler raporda kaldı ve KODA İŞLENMEDİ: ETTN'in TEK kaynağı var (`ubl.py:159` `uuid5`, `ubl.py:283` payload, `ubl_xml.py:283` `<cbc:UUID>`) ve sorgu/iptal submit'te saklanan `invoices.einvoice_uuid`i okuyor (`routers/invoices.py:133,249,398`), yani H1 için ikinci bir üretim noktası YOKTUR; KOD DEĞİŞMEDİ, GÖÇ YOK, `IZIBIZ_EFATURA_SUBMIT_VERIFIED` `False` KALDI, hiçbir pin kımıldamadı; kayıt adı `python scripts/durum.py --sonraki 93`
