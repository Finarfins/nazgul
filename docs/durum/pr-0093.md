#93 — SEC-2 (P1): reportlab `Paragraph` GÖVDESİNE giren kiracı verisi ARTIK KAÇIŞLI; ÖLÇÜLEN AÇIK `app/invoice_pdf.py`de: `Paragraph`ın ilk argümanı DÜZ METİN DEĞİLDİR, reportlab onu bir mini-XML belgesi olarak ayrıştırır (`reportlab.platypus.paraparser`) ve `build_invoice_pdf` ON BİR kiracı dizgesini (firma adı, firma vergi no, müşteri adı, makine marka/model/seri, iş emri no, iş emri durumu, para birimi — İKİ kez —, garanti türü, ödeme koşulları, notlar, teknisyen adı) bu gövdeye KAÇIŞSIZ gömüyordu; ÜÇ SONUÇ AYRI AYRI ÖLÇÜLDÜ, VARSAYILMADI: (a) `<` -> `ValueError: paraparser: syntax error: parse ended with 1 unclosed tags` -> `GET /api/invoices/{id}/pdf` 500 ve bu 500 KALICIDIR — "Ac<me" adlı bir müşteriye kesilen faturanın PDF'i bir daha HİÇ inmez, çünkü hatayı üreten şey istek değil KAYITLI VERİDİR; (b) `<b>x</b>` YORUMLANIR, yani kiracı verisi belgenin BİÇİMİNİ ele geçirir; (c) `<img src='/etc/hosts'/>` -> reportlab `ImageReader` o yolu GERÇEKTEN AÇAR (`OSError ... fileName='/etc/hosts' identity=[ImageReader@... filename='/etc...`) — bu bir YEREL DOSYA OKUMASIDIR ve en ağır olanıdır: yol var olsaydı sunucu dosyası faturaya GÖMÜLÜRDÜ; ÇARE, DEPODA ZATEN OLAN KALIBIN AYNISI: `app/routers/outputs.py:304`in `safe_note = escape(str(head["note"]))` kalıbı — gövdeye giren HER değer `xml.sax.saxutils.escape(str(...))` ile sarıldı; `escape` yalnız `&`, `<`, `>` çevirir ve BU YETERLİDİR çünkü değerlerin HEPSİ öğe METNİNE girer, hiçbiri ÖZNİTELİK değeri değildir (öznitelik olsaydı `quoteattr` gerekirdi ve bu ayrım kayda geçiyor); YARDIMCI FONKSİYON YA DA LAMBDA TAKMA AD KULLANILMADI ve bu bir üslup tercihi değil bir KAPI KARARIDIR: kısaltma (`e = lambda v: escape(str(v))`) diff'i güzelleştirir ama AST kapısını `escape` adına DEĞİL keyfi bir yerel ada bakmak zorunda bırakırdı, yani kapının kendisi zayıflardı; SÜPÜRME TAMDIR VE ADIYLA ÖLÇÜLDÜ: `app/**` altında `Paragraph(` geçen dosyalar SADECE İKİSİDİR (`app/invoice_pdf.py`, `app/routers/outputs.py`) ve `outputs.py` ZATEN TEMİZDİ — üç enterpolasyonu (`safe_note`, `company_name`, `company_tax`) kaçışlıydı, kalan tek f-string enterpolasyonu `{len(statement.lines)}` yani DİZGE ÜRETMEYEN bir sayaç, ve tek sabit-olmayan-görünümlü argümanı `Paragraph(config["title"], ...)` bir MODÜL SABİTİDİR (`"SATIŞ BELGESİ"` / `"ALIŞ BELGESİ"`) — o boşluk `test_outputs_baslik_sabiti_MODUL_SABITI` ile ayrıca çivilendi, sabit olmaktan çıktığı gün kapı kırmızıya döner; `app/labels.py` SÜPÜRMENİN DIŞINDADIR VE BU BİR ATLAMA DEĞİL: orası `Paragraph` HİÇ KULLANMAZ, `canvas.drawString` ile çizer (satır 341) ve düz metin API'si mini-XML AYRIŞTIRMAZ, kaçış GEREKMEZ; bu "bakmadık" değil ÖLÇÜLDÜ — `test_labels_Paragraph_KULLANMIYOR_drawString_ile_ciziyor` hem `Paragraph(` YOKLUĞUNU hem `drawString(` VARLIĞINI çiviliyor, yani o dosyaya bir gün bir `Paragraph` eklenirse dosya kaçış sözleşmesine O AN girer; KAPI BİR AST TARAYICISIDIR, GREP DEĞİL: `app/**/*.py` içindeki her `Paragraph(...)` çağrısının İLK argümanı incelenir — f-string ise her `FormattedValue` `escape(...)` (ya da `saxutils.escape(...)`), `len/abs/round/int/float` gibi dizge ÜRETMEYEN bir çağrı, sabit, veya AYNI KAPSAMDA `ad = escape(...)` ile bağlanmış bir ad olmak ZORUNDADIR; `.format(...)` ve `%` ile kurulan gövdeler DOĞRUDAN İHLALDİR (kaçışın konabileceği bir yer yoktur); ihlal `file:line` ve `ast.unparse` edilmiş ifadeyle raporlanır; DOLAYLI AD KURALI DAR TUTULDU — ad YALNIZ DOĞRUDAN `escape(...)`tan geliyorsa güvenli sayılır, `s = str(x)` sayılmaz, yani `outputs.py`de `safe_note = escape(...)` satırını `safe_note = str(...)` yapmak kapıyı KIRMIZIYA çevirir; KAPININ KENDİSİ DE ÖLÇÜLÜYOR ve bu ZORUNLUYDU: bir AST kapısı sessizce boş bir döngüye indirgenebilir (dosya süzgeci hiçbir şey bulmaz, ya da tarayıcı her şeye güvenli der) ve o hâlde SONSUZA KADAR YEŞİL yanar — `test_KAPI_KENDINI_DOGRULUYOR_sentetik_ihlali_yakaliyor` tarayıcıyı SENTETİK girdilerle iki yönde de sınıyor (kaçışsızı BULMALI, kaçışlıyı GEÇİRMELİ, dolaylı temizi geçirmeli, dolaylı kirliyi bulmalı, `.format` ve `%` gövdelerini bulmalı) ve `test_KAPI_appte_kacissiz_Paragraph_YOK` süzgecin `invoice_pdf.py` ile `outputs.py`i GERÇEKTEN gördüğünü ayrıca iddia ediyor; DAVRANIŞ İKİ KATMANDA ÖLÇÜLDÜ, biri diğerinin yerine geçmiyor: (1) `build_invoice_pdf` DOĞRUDAN — "Ac<me & Söhne" müşteri adıyla PDF ÜRETİLİYOR ve `pdfplumber` ile çıkarılan metinde `Ac<me` ve `& Söhne` HARFİ HARFİNE duruyor, `<img src='/etc/hosts'/>` notu 200 veriyor ve metinde `/etc/hosts` ile `<img` DÜZ METİN olarak görünüyor (yorumlansaydı metinde HİÇ görünmezlerdi) ve belgedeki GÖMÜLÜ GÖRÜNTÜ SAYISI 1'de kalıyor (o tek görüntü QR koddur; `<img>` yorumlansaydı 2 olurdu — bu sayı, "etiket metne düştü" iddiasının negatif tarafını da kapatıyor), `<b>x</b>` ödeme koşulu metinde `<b>x</b>` olarak duruyor, ve on bir alanın HER BİRİ ayrı bir parametrik satırla `<` taşıyabildiğini gösteriyor (tek bir toplu satır, alanlardan birinin sessizce kaçışsız kalmasını GÖREMEZDİ); (2) UÇ — `GET /api/invoices/{id}/pdf` GERÇEK ŞEMADA, ALT SÜREÇTE (taze `DATABASE_URL` ancak ayrı süreçte görülür, `app.config.Settings` modül düzeyinde TEK KOPYADIR; `test_kiraci_imha.py` ile AYNI gerekçe), düşmanca anlık görüntülerle yazılmış bir fatura satırı üzerinde 200 + `application/pdf` dönüyor ve PDF metni düşmanca değerleri DÜZ olarak taşıyor — ÖLÇÜLEN KUSUR UÇTAYDI, doğrudan çağrı ONU KANITLAMAZDI; bootstrap parolası KODA GÖMÜLMEDİ (`settings.effective_bootstrap_admin_password` üzerinden, sabit `admin123` yazan bir giriş parolayı değiştiren başka bir smoke'tan SONRA koştuğunda düşerdi); MUTASYONLAR KOŞULDU VE ADIYLA ÖLDÜ: `customer['name']`in `escape(...)` sarmalını düşürmek -> `test_KAPI_appte_kacissiz_Paragraph_YOK` (file:line ile), `test_MUSTERI_ADINDA_kucuktur_PDF_URETIYOR` (paraparser `ValueError`) ve `test_UC_dusmanca_anlik_goruntuyle_PDF_200` (alt süreç sıfırdan farklı dönüyor) ÜÇÜ BİRDEN KIRMIZI; KAPIYI ETKİSİZLEŞTİRMEK (tarayıcının `Paragraph` çağrısı denetimini atlamak) -> `test_KAPI_KENDINI_DOGRULUYOR_sentetik_ihlali_yakaliyor` KIRMIZI (12 test yeşil kalırken YALNIZ o düşüyor, yani kapı gerçekten kapıdır); GÖÇ YOK (`alembic/` diff'i BOŞ, alembic başı KIMILDAMADI), ROTA YOK (yeni uç yok, var olan ucun imzası kımıldamadı), ŞEMA YOK, `types.gen.ts` KIMILDAMADI (OpenAPI değişmedi), `TENANT_TABLES` KIMILDAMADI, kapalı aktivite kataloğu KIMILDAMADI, `pins/pg_twins.txt` KIMILDAMADI (PG ikizi EKLENMEDİ ve gerekçe: bu dilim tek bir SQL ifadesi bile değiştirmiyor, değişen şey bir dizge kaçışıdır ve LEHÇEDEN BAĞIMSIZDIR); TEK PIN OYNADI: `tests/pins/alt_surec_sql.txt` +1 satır (`tests/test_pdf_paragraf_kacisi.py` 1) — yeni uç smoke'u alt süreçte gömülü SQL taşıdığı için envanter dosya-başı büyüdü, toplam TÜRETİLİR çivilenmez; TESTLER `backend/tests/test_pdf_paragraf_kacisi.py` (14: 5 kapı/statik + 8 davranış + 1 uçtan uca alt süreç), ÜRETİM DEĞİŞİKLİĞİ TEK DOSYA (`backend/app/invoice_pdf.py`, +15/-11: bir `import` ve on bir `escape(str(...))` sarmalı — mantık, sıralama, biçim ve çıktı düzeni HİÇ değişmedi); kayıt adı `python scripts/durum.py --sonraki 93`
