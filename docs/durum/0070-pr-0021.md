#21 — ZAMAN BOMBASI: `test_v3_harman_vadeli` ve PostgreSQL ikizi 2026-09-02 İstanbul gününde develop'ta KIRMIZIYA döndü ve düzeltildi. KUSUR: smoke içinde FIFO belgesinin vadesi `due='2026-09-01'` SABİT yazılıydı; `_receivable_status` (routers/transactions.py) kararını `due_date < today` ile verir ve `today` `business_today()`ten, yani İSTANBUL takvim gününden gelir — İstanbul 2026-09-02'ye döndüğü (UTC 2026-09-01 21:00) an o belge `ACIK`tan `VADESI_GECTI`ye geçti. İKİ iddia birden düştü: `status=VADESI_GECTI` süzgeci bir yerine iki kimlik döndürdü ve ödeme sonrası `total_overdue` 0 yerine 800 çıktı. KUSUR DALIN DEĞİL DEPONUNDU VE BU ÖLÇÜLDÜ: temiz `origin/develop` ağacında (hem aa3d577 hem cfe7362) aynı duvar saatinde AYNEN üretildi; o gece açılan HER PR kendi kusuru olmadan kırmızı görecekti ve iki ayrı ajan (bu oturum ve Cursor) aynı kusuru birbirinden bağımsız buldu. ÇARE: vade sunucunun KENDİ saatinden türetiliyor — `FIFO_ERKEN_VADE = business_today() + 365 gün` ve `FIFO_GEC_VADE = business_today() + 730 gün`; test ile sunucu aynı süreçteki AYNI fonksiyonu okuduğu için ayrışamazlar. ÇİFTİN İKİSİ DE TAŞINDI, BİRİ DEĞİL: d156d13 ile inen PR #19 (7443163) aynı satırlara dokunmuş ve çifti `2099-01-01`/`2099-06-01` SABİTLERİNE almıştı — Cursor'un kendi deyişiyle “bombayı erteledi, etkisiz kılmadı”. O taban üstüne rebase'de çakışma TARAF SEÇEREK değil İDDİALARDAN TÜRETİLEREK çözüldü: `2026-08-01` de İstanbul 2026-09-02'de vadesi geçmişe düşüyordu, yani çiftin İKİSİ de fitildi; yalnız birini göreli yapmak `only_overdue` iddiasını kurtarmaz. Üç iddia aynı anda tutmalı — (1) ikisi de AÇIK kalmalı (`?status=VADESI_GECTI` TEK kimlik döndürmeli, ödeme sonrası `total_overdue` 0), (2) `first < second` KESİN olmalı (FIFO en eski vadeye dağıtır; `first` ÖDENDI, `second` 800 açık), (3) ikisi de `2099-12-31` çapasının altında ve `2019-06-30`un üstünde kalmalı (`desc_ids[0] == open_id`, `desc_ids[-1] == overdue_id`). 365 < 730 sırası her saatte sabit olduğu için üçü de duvar saatinden BAĞIMSIZ tutar; ileri bir sabit (1) ile (3)'ü bugün sağlar ama anlamı yine saate emanet eder. 365 gün payı ayrıca koşum İstanbul gece yarısını geçerse oluşacak bir günlük sapmayı yutar. SQLite testi ile PostgreSQL ikizi `_SMOKE` metnini PAYLAŞTIĞI için tek düzeltme ikisini de kapsıyor. MUTASYONLA ÖLÇÜLDÜ, VARSAYILMADI: sabit tarih geri konduğunda SQLite ve PostgreSQL İKİSİ DE smoke satır 171'de kırmızı; düzeltme geri konduğunda ikisi de yeşil. Ayrıca saat DONDURULARAK çapraz ölçüldü (`app.business_time.business_now` yaması, `test_istanbul_business_date.py`nin kurduğu desen): donmuş 2027-06-15'te düzeltilmiş sürüm YEŞİL, sabit tarihli sürüm KIRMIZI — yani düzeltme 'bugün yeşil' değil SAATTEN BAĞIMSIZ. Yamanın alt süreç smoke'una GERÇEKTEN ulaştığı ayrıca negatif kontrolle kanıtlandı: sabit tarihli sürüm donmuş 2026-08-01'de (dönüm ÖNCESİ) YEŞİL, donmuş 2028-01-15'te (dönüm SONRASI) KIRMIZI. TARAMA (ölçüldü, DEĞİŞTİRİLMEDİ): backend altındaki (alembic hariç) tüm Python dosyalarında bugünden ileri tarihli 53 sabit literal var; bunları taşıyan 13 dosya donmuş 2028-01-15 saatinde tek tek koşuldu ve TAMAMI YEŞİL geçti — yani kalan literaller 'bugün'e karşı karşılaştırılan vade/son-kullanma değil, tarih ARALIĞI süzgeci, sunucunun ÜRETTİĞİ değerin beklenen karşılığı ya da açıkça enjekte edilen saat (`next_invoice_number(now=datetime(2027,1,1))`). Geçmişte kalan sabitler ATIL sayıldı ve taranmadı. TARAMANIN SINIRI: yalnız `business_now` donduruldu; `datetime.now()`/`date.today()`yi DOĞRUDAN okuyan bir yol varsa bu ölçüm onu görmez (deponun kendi kapısı iş günü okumalarının `business_today()` kullanmasını zaten şart koşuyor). ZAMAN DONDURMA ALTYAPISI VAR: `app/business_time.py` başlığının bildirdiği gibi `business_now` yamalanabilir ve bu alt süreç smoke'larında da işler; bu dosya onu KULLANMIYORDU. Kapsam bilerek dar tutuldu: bu PR yalnız bir vadeyi göreli yapıyor, kantar fişi işini TAŞIMIYOR ve tarla koduna DOKUNMUYOR. REBASE ÖLÇÜMÜ: taban d156d13'e taşındıktan sonra üç katmanın TAMAMI sıfırdan tekrarlandı ve FIFO SIRASI ayrıca iki yönlü ölçüldü — iki vade yer değiştirdiğinde tahsilatın ödediği belge de değişiyor, yani iddia gerçekten SIRAYI sınıyor.
