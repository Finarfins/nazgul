#19 — TARLA YAZMA KİLİTLERİ (ÇKS tek ürün + tarlaya giriş yasağı) tek PR: ikisi de `farm.py` yazma yoluna girer, ayrı ajanlar `tenant_scoping_guard` parmak izinde çarpışırdı; KANIT (okuma merceği, develop `farm.py:1199-1201`): re-entry verisi "V1'de toplanıyor ama HİÇ KULLANILMIYORDU" — PHI kilidi yalnız hasat tarafına indi, boşluk tasarım değil; KİLİT A `create_season` (`_sezon_urunu_dogrula` sonrası / INSERT öncesi) + `update_season` yalnız parsel/yıl/ürün değişince (not-only denetimi silmez); KİLİT B `create_activity` (`_makine_dogrula` sonrası / oran kopyası öncesi), tarih `_yerel_gun(performed_at)`, pencere `[yapıldığı gün, güvenli)`; şekil `_hasat_guvenlik_dogrula`: iki raise (block, require_reason) iki dönüş (warn, gerekçe verilmiş), "Sert ret DEĞİL — gerekçe isteniyor.", dönen metin `monoculture_warning` / `reentry_warning`; POLİTİKA `farm_monoculture_policy` ve `farm_reentry_policy` = warn|require_reason|block, `allow` YOK (uyum/kalıntı kontrolünü kapatmak sessiz düğme olurdu), varsayılan require_reason; göç `20260901_0064`; KIRMIZI: çağrılar yorumlanınca 3. yıl Domates 201 (id=3) ve yasağın içindeki sulama 201 (id=2); YEŞİL: çağrılar geri, SQLite duman + mevcut farm/herd testleri geçti; PIN (birleşmiş ağaç, CPython 3.12.3, develop `cfe7362` üzerine): farm.py dinamik 15 SABİT AST 5b93084f→a708e84b, tenancy.py 4 SABİT AST b50d5fd5, route_security 344/265/ebf0189b Δ0, GET envanter 164/3aa237d4 Δ0, AUTH 334/READ 91/UNDENIABLE 99 Δ0, get_company_settings 165d5888, INVENTORY_FINGERPRINT 099ecb9d, alembic baş 20260901_0063→20260901_0064, dinamik toplam 253 SABİT; NOT MEASURED: Windows `wip-tarla-bku` stash (bu klon boş, stash DÜŞÜRÜLMEDİ), Windows develop 70 geride FF (bu klon zaten 0/0), monokültür/giriş önizleme GET, types.gen yanıt `*_warning`.
