#48 — MÜSTAHSİL ISSUE/CANCEL COMPARE-AND-SET: runtime lens NO-GO kapanışı — aynı taslağa eşzamanlı iki `issue` ikisi de 200 alıp iki numara tüketiyordu (`routers/mustahsil.py` status okuyup predicate'siz UPDATE); düzeltme diyalekt-nötr CAS: (1) `UPDATE … SET status='issuing' WHERE status='draft'` rowcount==1 yoksa 409 `MAKBUZ_TASLAK_DEGIL`, (2) `next_document_no` YALNIZ kazanan, (3) `UPDATE … SET status='issued', receipt_no WHERE status='issuing'` — hepsi tek işlem, istisnada rollback taslağı geri getirir; `cancel` aynı kalıp (`WHERE status='issued'`); göç `20260905_0070` YERİNDE (`issuing` CHECK + `ck_…_no_follows_status` numarasız `issuing` kabul; 0071 YOK); upgrade/downgrade SQLite+PG16 ölçüldü; PG ikizi: aynı taslak 2×20 → tam bir 200/bir 409 ve `document_sequences` delta 1, ayrı taslaklar → iki AYRI numara, cancel CAS 200+409; router dinamik SQL iğnesi yeniden ölçüldü (sayı 3 SABİT, parmak izi `1e8e6cd4`→`7020cd85`); `ci-required-contexts.json` 17 bağlam DOKUNULMADI; head `f336ce96f945`.
