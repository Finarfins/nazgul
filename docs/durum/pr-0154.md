#154 — F10-1a WhatsApp taraf bağlantısı: numarayı bir CARİYE bağlayan defter (göç 20260918_0090: `whatsapp_party_links` + `whatsapp_party_pairing_codes`, kısmi tekil `(company_id, phone) WHERE is_active`, 0079'un yedi CHECK'i, bileşik FK `(company_id, consumed_link_id)`); `app/whatsapp/taraf.py` (kod üret/kullan/çöz/kapat, hedef numara bağı SEC-1, CAS'li tüketim, sabit süreli özet kıyası, hız sayacı personel yoluyla ORTAK) ve dört uç `/api/whatsapp/party-*`; izin `party_type`tan türüyor (CUSTOMER -> sales, SUPPLIER -> purchases; keşif §7 K4) — ara katman `read` verir, GERÇEK kapı handler'dadır, bu yüzden GUARDED_READ 31 -> 35 ve UNDENIABLE 97'de SABİT. RIZA EŞLEŞTİRMEDE YAZILMAZ (Şef kararı): rızayı çiftçinin ilk mesajı verir (F10-1b) ve `notification_consents.status` CHECK'i yalnız GRANTED/REVOKED taşıdığı için bekleyen satır biçimi YOK; `consent_at` NULL kalır ve `evaluate_consent` NO_RECORD ile fail-closed davranır. Tam suite bir SIZINTI buldu ve kapatıldı: `{id}` taşıyan iki DELETE'in izni satırın `party_type`ından türediği için sıra 'önce 404, sonra 403'tü ve iki izni de taşımayan bir rol satır kimliklerini sayabiliyordu; ön kapı `_herhangi_taraf_izni` ile cevap id'den BAĞIMSIZ 403 oldu ve dördüncü çapa sınıfı `PARTY_ROLE_WRITE_EXEMPTIONS` gerçek isteklerle kanıtlandı (YÖN H/I: `rapor` üç uçtan da 403, `satis` CUSTOMER'da 201 / SUPPLIER'da 403). Dilim TABAN 6ad974f (#152/H46 sonrası) üzerine yeniden oturtuldu; uygulama yükü BİT BİREBİR AYNI (backend/app+alembic yama kimliği 46e338026e8c, rebase ÖNCESİ ve SONRASI), çakışma YALNIZ iki envanter çivisindeydi ve ikisi de TARAYICIYLA yeniden ölçüldü. Pinler TABAN 6ad974f üzerinde yeniden ölçüldü: alembic başı 0090 (14 çivi), TENANT_TABLES 127, rota 425/331, GET 202, kimlik doğrulamalı 412, read 91, GUARDED_READ 35, Core envanteri 250 (parmak izi b1a3216d), kiracı ifadesi 208, kiracı istisnası 16, ACTION_TYPES 82, RESOURCE_TYPES 24, pg_twins 138, cari_alan_envanteri 57; types.gen.ts yeniden üretildi (ikinci koşu bayt-birebir). Kanıt: izole suite 479/479 dosya ve 3738 test sonucu, 0 başarısız; PG 16 ikizi 12 test taze şemada (kısmi tekilin PG yüklemi, CHECK'ler, bileşik FK, yirmi işçilik yarışta tam bir kazanan) IKINCI REBASE: #153 (H-IRS) develop'a inince TABAN d6b07ca oldu; tek cakisma YINE `tests/test_core_query_inventory.py`nin parmak izi civisiydi (develop ff03df02, dilim b1a3216d — birlesmeden sonra IKISI DE YANLIS) ve ARITMETIKLE DEGIL TARAYICIYLA cozuldu: `_belge_numarasi`nin H53 `UPPER()` okumasi develop'tan alindi, dilimin ON BES sorgusu korundu, `scan_sources` merged agac uzerinde YENIDEN kosuldu ve ciktisi civilendi — toplam 250 (select 170 / update 68 / delete 12), sapma 0/0/0, parmak izi ff03df02 -> fa4c5e26. Uygulama yuku YINE BIT BIREBIR AYNI: backend/app+alembic yama kimligi 46e338026e8c, rebase ONCESI (vs 6ad974f) ve SONRASI (vs d6b07ca). Oteki civiler kimildamadi: alembic basi 0090 (ON UC dosyada ON DORT civi); `npm ci` sonrasi openapi.json + types.gen.ts + fixture'lar yeniden uretildi, `git diff --exit-code` BAYT-BIREBIR temiz (CRLF gurultusu bile yok); F10-1a PG 16 ikizi TAZE bir veritabaninda 12 test, 0 atlama; dort SQLite dosyasi 49+69+26+47 = 191 gecti. — <merge tarihi>
