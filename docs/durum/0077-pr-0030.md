#30 — Yerel tam süpürmede (`backend/` içinden `python -m pytest tests --ignore=tests/fixtures -q -p no:cacheprovider`, taze `DATABASE_URL=sqlite:///<tmp>/x.db`) develop `f623190` üzerinde beş test kırmızıydı, CI `backend-quality` yeşildi; üçü bağımsız mercek aynı kümeyi gördü ve bu PR hepsini ORDER-DEPENDENT olarak ölçüp YALNIZ test dosyasında kapattı (`backend/app` SIFIR satır, `test_*postgresql*.py` EKLENMEDİ); TABAN BU KAYIT YAZILIRKEN OYNADI: `origin/develop` `f623190` → `23b11ef` (PR #28 indi, `0076-pr-0028.md` aldı), kayıt `python scripts/durum.py --sonraki 30` ile `0077-pr-0030.md` oldu; SÜPÜRME ÖNCE (`f623190`, düzeltmesiz) 5 failed / 829 passed (834 collected, 391.72s) — beş ad: `test_field_stok_zamanlayici.py::test_cycle_logs_every_outcome_bucket`, `::test_empty_cycle_is_logged_explicitly`, `::test_shutdown_keeps_reference_to_a_stuck_thread` (üçü de `IndexError: list index out of range` `caplog.records[-1]`), `test_self_service_exemption.py::test_no_fourth_self_service_route_outside_the_auth_prefix` (`KeyError: 'app.routers.auth'` satır 115), `test_supplier_price_bridge_units.py::test_zip_bomb_stops_at_uncompressed_entry_limit` (`DID NOT RAISE HTTPException`); SÜPÜRME SONRA (düzeltme, `f623190` ağacı) 834 passed / 0 failed (381.97s); her biri ALONE yeşil ve FILE yeşil, yani kusur dosya-içi değil süreç-paylaşımlı; (1) ÜÇ ZAMANLAYICI — mekanizma `app.main:109` `logging.getLogger("yerel_hesap").propagate = False` (satır 102-109 stdout handler + propagate kesişi): tam süpürme KOLEKSİYONUNDA başka bir dosya `app.main`i içe aktarır, çocuk logger `yerel_hesap.field_stok_zamanlayici` kaydı ebeveyne verir, kökteki caplog BOŞ kalır; kanıt: `test_self_service_exemption.py::test_declared_set_matches_the_hand_written_anchor` ile birlikte koleksiyon `IndexError` üretir ve stdout'ta log SATIRI vardır (`Field stok outbox dongusu tamamlandi; ...` / `olay bulunmadi` / `DURMADI`) yani üretim loglar, caplog görmez; düzeltme `_yakala` ile handler'ı üretim logger'ının KENDİSİNE takmak; PLATFORM-DEPENDENT DEĞİL (Linux'ta aynı beş, aynı IndexError); (2) SELF-SERVİS — mekanizma `test_security_audit_visibility.py:43-44` ve `:60-61` `for name in ... app.*: del sys.modules[name]`, bu dosyanın `app` referansı koleksiyon-anı nesnesinde kalır, `functions()` `sys.modules[module_name]` der, `KeyError: 'app.routers.auth'`; `security_audit` hemen önce (alfabe), araya giren yeniden-import YOK; düzeltme `importlib.import_module`; (3) ZIP-BOMB — aynı `app.*` silme; modül-düzeyi `from ... import _validate_archive` BAYAT kalır, `monkeypatch.setattr("app.routers.supplier_price_bridge.MAX_ENTRY_BYTES", 1024)` YENİ modüle yazar, bayat fonksiyon `__globals__`ında 100MB görür, 4096 bayt sınırın altında kalır, HTTPException ATMAZ; düzeltme aynı import nesnesine `setattr` + `bridge_mod._validate_archive`; CI NEDEN YEŞİL — `run_isolated_tests.py` `_execute_test_file` her dosyayı `subprocess.run(..., cwd=workdir)` ile ayrı süreçte koşturur, `_subprocess_environment` taze `DATABASE_URL`/`TMP`/`PYTHONPATH`/`PYTHONPYCACHEPREFIX` ve `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` verir, shard `select_test_shard` dosyayı tek başına alır: koleksiyon-anı `app.main` sızıntısı, `sys.modules` silmesi ve bayat `__globals__` AYNI SÜREÇTE yaşamaz, bu yüzden per-file yalıtım süpürmenin açtığı sıra bağımlılığını MASKELER; develop `f623190` koşusu 33791566923, 17 zorunlu bağlam: alembic-chain success, backend-postgresql (0)(1)(2)(3) success, backend-quality (0)(1)(2)(3) success, backend-quality-aggregate success, backend-quality-canonical success, container FAILURE (`pip install --require-hashes` PyPI `Read timed out` — bu beş testle ilgisiz), contract-drift success, durum-kaydi success, e2e success, frontend success, verify-image-artifact SKIPPED (`needs: container`); İKİ YÖN: zehir-eşlemesi düzeltmeden sonra 17 passed (self-service koleksiyonu + üç zamanlayıcı + security_audit dosyası + self-service kapısı + zip-bomb); mutasyon (app geçici, `git checkout --` ile geri): (a) `kovalar = ""` → cycle_logs KIRMIZI `assert 'girdi=4' in 'Field stok outbox dongusu tamamlandi; '`, (b) `"olay bulunmadi"` → `"olay VARDI"` empty_cycle KIRMIZI, (c) takılı dalda `_thread = None` shutdown KIRMIZI `assert None is StuckThread`, (d) `list_products` gövdesine `issue_token` adı → self-service KIRMIZI `{/api/products, /api/users}`, (e) `opened > MAX_ENTRY_BYTES or` silindi → zip-bomb KIRMIZI DID NOT RAISE; beş mutasyon geri alındı, `git diff --stat backend/app` BOŞ; SÜPÜRME SONRA (#28 birleşmiş ağaç, `23b11ef` + düzeltme) 839 passed / 0 failed (391.51s; +5 `test_donmus_saat_kapisi`); ÖLÇÜLMEDİ: bu PR'ın KENDİ CI koşusu (kayıt commit'in içindedir), Windows `F:\nazgul-kirmizi` süpürmesinin yeniden koşulması (mekanizma Linux'ta aynı beşle ölçüldü, cp1254/path ayırıcıya bağlı DEĞİL), `run_isolated_tests.py` dört shard'ının yerelde tam koşusu, frontend, PostgreSQL ikizleri.
