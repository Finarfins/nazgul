#94 — SEC-5 (P2, review A bulgu 6): .env.production.example (39 eksik) ve backend/.env.example (22 eksik) Settings tamlığı sağlandı, çalışma zamanında okunan 30 ayar docker-compose.prod.yml environment haritasına geçirildi ve tamlık kapısı eklendi; ÖLÇÜLEN KUSUR: `_settings_env_names() - _documented_keys(".env.production.example")` tam 39 anahtar eksik veriyordu — en kritiği `PUBLIC_APP_URL` (`config.py:432` üretim ortamında https:// ile başlamasını zorunlu kılar, eksikliği açılışta doğrudan boot crash üretir), `COOKIE_SECURE` (`config.py:388`), `SUNGUR_DATA_DIR`, `WHATSAPP_*` (WA1-WA5) ve `HARMAN_KOPRU_*` (PR #77); `backend/.env.example` da ölçüldü ve 22 anahtar eksik bulundu; DÜZELTME: `.env.production.example`a 39 anahtar, `backend/.env.example`a 22 anahtar Settings varsayılan değerleri veya güvenli yer tutucularla (opsiyonel/boş değerler `# KEY=` yorumlu) ve tek satırlık Türkçe açıklamalarla eklendi, hiçbir mevcut satırın değeri değiştirilmedi; `PUBLIC_APP_URL=https://harmanzamani.com` açık yazıldı; COMPOSE AKTARIMI: taşınmayan gruptaki 31 anahtar incelendi — `EINVOICE_SENDER_VKN` ölü ayardır (firma profilinden okunur, `test_production_deployment.py:164` compose'a sızmasını açıkça yasaklar) ve `MUAF_URETIM` muafiyet listesine gerekçesiyle eklendi, kalan 30 ayarın tamamı `docker-compose.prod.yml` app servisi `environment:` bloğuna `${KEY:-default}` biçimiyle geçirildi; KAPI: `backend/tests/test_env_example_completeness.py` dosyasına iki yeni tamlık testi eklendi (`test_env_production_example_covers_all_settings` ve `test_backend_env_example_covers_all_settings`), her iki örnek dosya için `_settings_env_names() - _documented_keys(path) == set()` garanti edildi; MUTASYON: `.env.production.example`dan `PUBLIC_APP_URL` silindiğinde testin adıyla KIRMIZI yandığı kanıtlandı (`AssertionError: .env.production.example missing Settings keys: ['PUBLIC_APP_URL']`); İZOLE KOŞUM: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4 backend/tests/test_env_example_completeness.py backend/test_production_deployment.py` 2 dosya, 17/17 test geçti; GÖÇ YOK, ROTA YOK, UYGULAMA KODU DEĞİŞİKLİĞİ YOK; dal fix/sec5-env-production-tamlik; kayıt adı `python scripts/durum.py --sonraki 94`
