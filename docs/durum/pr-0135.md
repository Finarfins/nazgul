#135 — H48: çek/senet durum makinesinin riskli hedefleri (`ciro_edildi`, `karsiliksiz`, `iade`) uç İÇİNDE yalnız admin/yonetici/muhasebe'ye daraltıldı, `satis` evrak girer/okur/tahsile verir/tahsil eder; tek kaynak `cek_senet_engine.MUHASEBE_HEDEFLERI` (+ `MUHASEBE_ROLLERI`, `rol_hedefe_gidebilir_mi`), 403 `CEK_DURUM_ROL_YETKISIZ` routerın 409 zarfıyla ve satır okunup kilitlenmeden ÖNCE (sıra 403->404->409->422, ev kuralı `work_order_labor_lines`/`companies`; yetkisiz rol evrakın varlığını öğrenemez, yetkili rolün 404'ü değişmedi); uç izni `payments`, yol ve `auth.py` önek kuralı DEĞİŞMEDİ (yalnız yorum), göç yok; uç docstring'i değiştiği için `types.gen.ts` açıklaması güncellendi (contract-drift yalnız açıklama farkında da kırmızı yanar — ilk push'ta ölçüldü); 8675dbe'ye (#127 E4b-1, #134 1B-H sonrası) yeniden taban alındı ve pinler YENİ tabanda koşularak ölçüldü, hiçbiri bu farkla oynamadı — route 420/326, GET 200, AUTHENTICATED 407 (READ 87/GUARDED 31/UNDENIABLE 97), TENANT_TABLES 123, pg_twins 132, Core sorgu envanteri 230 (155/63/12), Core kiracı kapısı 183, dinamik SQL izin listesi 270, alembic başı 20260915_0087, güvenlik parmak izi 989b735'te `35f45088…50f2` -> 8675dbe ve başta `958a9c0e…d178`; testler: üç hedefte satis 403 ve ÖDEMELİ evrakta satır+payments+finance_transactions+receivable_charge_documents+activity_logs değişmez (aynı evrakı muhasebe geçirince borç belgesi açılıyor — sayım canlı), admin VE yonetici üç hedefte UÇTAN yeşil (şef: kuralın izin verip uçtan hiç koşmadığı rol sessiz gerilemenin saklandığı yerdir), satis tahsil yeşil, depo/rapor 403 regresyonu, 403-önce-404, AST ile ikinci liste yok; iki kendi test kusurum bulunup düzeltildi (ast.walk genişlik-önce sırası; Bravo'da açılan fazladan evrak geri yükleme testinin "iki satır" sayımını bozuyordu); yerel izole süit 469/469 dosya 3529 sonuç yeşil, PG ikizleri 20260915_0087'ye taze göçlü DB'de CS1 16/16 CS2 9/9, 0 atlama (ikiz `DATABASE_URL` VE `APP_TEST_DATABASE_URL` ister); açık: frontend Ciro/Karşılıksız/İade düğmelerini satis'e hâlâ gösteriyor — <merge tarihi>
