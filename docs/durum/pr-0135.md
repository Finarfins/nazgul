#135 — H48: çek/senet durum makinesinin riskli hedefleri (`ciro_edildi`, `karsiliksiz`, `iade`) uç İÇİNDE yalnız admin/yonetici/muhasebe'ye daraltıldı, `satis` evrak girer/okur/tahsile verir/tahsil eder; tek kaynak `cek_senet_engine.MUHASEBE_HEDEFLERI` (+ `MUHASEBE_ROLLERI`, `rol_hedefe_gidebilir_mi`), 403 `CEK_DURUM_ROL_YETKISIZ` routerın 409 zarfıyla ve satır okunup kilitlenmeden ÖNCE (sıra 403->404->409->422, ev kuralı `work_order_labor_lines`/`companies`; yetkisiz rol evrakın varlığını öğrenemez, yetkili rolün 404'ü değişmedi); uç izni `payments`, yol ve `auth.py` önek kuralı DEĞİŞMEDİ (yalnız yorum), göç yok; pinler taban 989b735'te ölçülüp başta aynı bulundu — route 419/325, parmak izi `35f45088…50f2`, GET 199, AUTHENTICATED 406, READ 87/GUARDED 31/UNDENIABLE 97, TENANT_TABLES 122, pg_twins 131; testler: üç hedefte satis 403 ve ÖDEMELİ evrakta satır+payments+finance_transactions+receivable_charge_documents+activity_logs değişmez (aynı evrakı muhasebe geçirince borç belgesi açılıyor — sayım canlı), admin yeşil, satis tahsil yeşil, depo/rapor 403 regresyonu, 403-önce-404, AST ile ikinci liste yok; iki kendi test kusurum bulunup düzeltildi (ast.walk genişlik-önce sırası; Bravo'da açılan fazladan evrak geri yükleme testinin "iki satır" sayımını bozuyordu); yerel izole süit 467/467 dosya 3497 sonuç yeşil, PG ikizleri taze göçlü DB'de CS1 16/16 CS2 9/9 (ikiz `DATABASE_URL` VE `APP_TEST_DATABASE_URL` ister); açık: frontend Ciro/Karşılıksız/İade düğmelerini satis'e hâlâ gösteriyor — <merge tarihi>
