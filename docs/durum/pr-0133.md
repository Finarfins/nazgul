#133 — TransactionDialog'un yük altındaki kırmızısı BEKLEME BÜTÇESİ payına bağlandı; kusur ne bileşende ne çağrı yerinde: zincirde GERÇEK ZAMANLAYICI YOK (mock'lar çözülmüş promise döndürür, tek bir `await act(async()=>{})` turu render→/customers+/warehouses+/payments→/products→önseçim→boş satır zincirinin tamamını bitiriyor, 16/16 örnekte tur=1), gecikmenin tamamı ağır MUI diyalogunu jsdom'da commit etmenin CPU maliyeti ve RTL'in `findBy`/`waitFor` bütçesi DUVAR SAATİ; pay bu kutuda bütçe süpürülerek ölçüldü — 600 ms ve üstü 11/11 yeşil, 400 ms'te ilk düşen ÜÇLÜ tam olarak bildirilen testler (satışta önseçim / alışta önseçim / son satır dolunca boş satır), 200 ms'te 4, 150 ms'te 5 — yani varsayılan 1000 ms'e kalan pay yalnız ~1.7-2.5x; `src/test/setup.ts` içinde TEK YERDE `configure({asyncUtilTimeout:3000})`, `testTimeout` VARSAYILANDA (5000) bırakıldı ki bozuk bekleme RTL'in eksik elemanı ADIYLA söyleyen hatasıyla düşsün (denenip ATILAN `act` ile bekleme yaklaşımı bütçeyi testTimeout'a taşıyıp 9/11 testi `Test timed out in 1200ms` diye kördüğüm ediyordu); DÜRÜSTÇE kapı koşulunda bugün kırmızı YOK — tam süit varsayılan işçiyle 757/757 yeşil ve ikinci bir vitest süreci koşarken dosya ayar VARKEN de YOKKEN de 20/20 yeşil, kırmızı yalnız ~9x aşırı abonelikte üretilebildi: aynı kutuda arka arkaya ayarlı 0/8, ayarsız 5/8 kırmızı — <merge tarihi>
