#27 — BİRİM DÖNÜŞÜMÜ PR 1: çözücü (`app/units.py`) ile deposu (göç `20260902_0066`), HİÇBİR ŞEYE BAĞLANMADAN — bu PR'da çözücüyü çağıran ne bir yol, ne bir ekran, ne bir yazma yolu VARDIR ve bu bilinçlidir: bir çözücünün ŞEKLİ ve ÖLÇEĞİ, onu unutacak bir çağıran ortaya çıkmadan ÖNCE çivilenmelidir; ÖLÇÜLEN KUSUR: `products.unit` (`VARCHAR(40)`, `server_default='Adet'`) SERBEST METİNDİR ve yalnız EKRANDA ne yazacağını söyler, stok sayısının HANGİ birimde olduğunu SÖYLEMEZ, `stock_movements.quantity` ise birimi hiçbir yerde YAZILI OLMAYAN bir sayı taşır — bugün doğru görünüyor çünkü bugün dönüşüm YOK, dönüşüm eklendiği AN yanlış olur ve yanlışlığı GÖRÜNMEZ olur; ŞEMA ÜÇ AYRI OLGU AÇIYOR ve tek sütunda birleşmemeleri 0063'ün ve 0065'in kaçındığı hatanın aynısıdır (birleşik bir sütunun BOŞ olması iki şeyi birden söylerdi): `products.base_unit VARCHAR(40)` NULL kabul eder, `server_default` YOKTUR ve GERİYE DOLDURULMAZ — `products.unit`ten kopyalamak cazip ve YANLIŞ olurdu çünkü `unit` DOĞRULANMAMIŞ bir görüntü etiketidir ve kopyalamak onu doğrulanmış bir olguya SESSİZCE terfi ettirirdi; `product_unit_factors` firmanın "1 X = N taban" beyanlarını EKLEMELİ tutar (`effective_from` DATE, `CHECK factor > 0`, `UNIQUE(company_id, product_id, unit_code, effective_from)`, bileşik yabancı anahtar `(company_id, product_id) -> products(company_id, id)` yani 0062'nin kuralı); `stock_movements.entered_quantity NUMERIC(18,4)` · `entered_unit VARCHAR(40)` · `entered_factor NUMERIC(24,10)` ÜÇÜ DE NULL ve geriye doldurma YOK, çünkü mevcut hareketlerin girildiği birim KAYITTA YOKTUR (ölçülen kusur zaten budur) ve `entered_unit = products.unit` yazmak kaybolmuş bir olguyu UYDURMAK olurdu; SAHİP KARARI 1 ŞEMAYA GİRDİ: yanlış çıkan katsayı ASLA yeniden hesaplanmaz ve satır ASLA `UPDATE` edilmez, düzeltme daha yeni `effective_from` ile YENİ BİR SATIRDIR — gerekçe iki tanedir ve ikisi de geri alınamaz olanı korur, (a) geçmişi yeniden hesaplamak MUTABAKATI YAPILMIŞ faturaları geçersiz kılardı, (b) `UPDATE` 50'nin bir zamanlar doğru sanıldığına dair TEK KANITI silerdi, çünkü denetimin sorusu "doğru sayı neydi" değil "o gün neye inanıldı"dır; fiziksel stok gerçekten yanlışsa çare TARİHLİ BİR STOK DÜZELTME HAREKETİDİR ve fark CARİ DÖNEME düşer, sahip bunu açıkça kabul etti; yasak tetikleyiciyle DAYATILMADI (bu depoda kurulmamış bir desen olurdu) ama `effective_from`un varlığıyla ŞEMADA GÖRÜNÜR — tek geçerli satır olsaydı o sütun gereksiz olurdu; ÇÖZÜCÜNÜN OKUMA SIRASI ÜRÜN SATIRI -> EVRENSEL HARİTA -> RED'dir ve ÜÇÜNCÜ ADIM DOSYANIN VAR OLMA SEBEBİDİR: bilinmeyen bir birimi 1.0 katsayısıyla geçirmek ÖLÇÜLMEMİŞ bir sayıyı ÖLÇÜLMÜŞ gibi stoka yazardı ve bu dönüşümün HİÇ OLMAMASINDAN daha kötüdür çünkü hata artık görünmez olurdu; RED SENTINEL DEĞİL İSTİSNADIR (`BirimCozulemedi.sebep`) çünkü bir sentinel çağıran tarafından sessizce sayı yerine konabilirdi, istisna ise YAKALANMAK ZORUNDADIR ve sahip kararı 2'nin iki farklı çağıran davranışını (outbox'ta ADI KONMUŞ atlama kovası, etkileşimli yazmada RET) aynı sebepten türetmeyi mümkün kılar; SAHİP KARARI 2 UYGULANDI: `base_unit IS NULL` iken taban dışı birim `TABAN_BILDIRILMEMIS` ile REDDEDİLİR ve girileni taban SAYMAK reddedildi çünkü bu bir OLGU UYDURMAK olurdu — ayrıca `base_unit is None` denetimi "aynı birim" kısayolundan ÖNCE yapılır, sonra yapılsaydı tabanı bildirilmemiş bir üründe `entered_unit='KG'` sessizce geçer ve "KG cinsinden" olduğu UYDURULMUŞ olurdu; TÜRKÇE KATLAMA BURADA YAZILDI VE BU KOPYA GEÇİCİDİR — ÖLÇÜLDÜ: `git log --oneline origin/develop | grep -i bitki` BOŞ döndü, yani `claude/bitki-esitligi-tr` (`f1e8ca4`, PR #25) bu iş başlarken develop'a BİRLEŞMEMİŞTİ, bu yüzden `turkce_katla` (`metin.strip().replace("i","İ").upper()`, `.upper()`tan ÖNCE değiştirilir çünkü sonra yapılsaydı `i` çoktan `I` olup `ı`dan ayırt edilemez hale gelirdi) burada kuruldu ve O DAL İNDİĞİNDE BU KOPYA SİLİNMELİ, ortak olan çağrılmalıdır — gerekçe tarihseldir ve bu deponun KENDİ hatasıdır: İ/ı katlaması üç katmanda AYRIŞTI ve ayrışmanın başlangıcı tam olarak "ikinci bir kopya" idi; `unit_code` SERBEST METİN KALDI ve kapalı liste GEREKMEDİ çünkü çözücü zaten ne evrensel ne de beyan edilmiş olan her şeyi reddediyor; `units_per_box` VE `price_per` ÖLÜDÜR ve burada ADLARI KONUYOR ki sonraki okuyucu `units_per_box`ı katsayı SANMASIN: `units_per_box` (`core_schema.py:88`, `QUANTITY`) ile `price_per` (`core_schema.py:89`, `String(40)`, `server_default='Adet'`) bu PR'da HİÇ DOKUNULMADAN bırakıldı, katsayı defteriyle ilişkileri YOKTUR ve `products.base_unit` + `product_unit_factors` onların yerine GEÇMEZ — onlar hâlâ ölü, sadece artık yanlarında canlı bir mekanizma var; İKİZ AYIRT EDİCİ OLDUĞU ÖLÇÜLEREK GÖSTERİLDİ ve bu PR #24'ün üç kez ölçtüğü dersin (bir test ADINI TAŞIDIĞI mekanizmayı çalıştırmadan da geçebilir) doğrudan sonucudur, üstelik burada başka hiçbir kapsam YOKTUR: saf bir "değer doğru mu" karşılaştırması BURADA AYIRT EDİCİ OLMAZDI çünkü PostgreSQL NUMERIC(18,4) yazarken KENDİSİ yarım-yukarı yuvarlar, yani `quantize` tamamen kaldırılsa bile `0.00005` veritabanında yine `0.0001` olurdu — ayrım (a) PYTHON değeriyle VERİTABANI değerinin BİRBİRİNE karşı ölçülmesinden, (b) karşılaştırmanın `str()` üzerinden yapılmasından (`Decimal("1.12") == Decimal("1.1200")` SAYISAL OLARAK DOĞRUDUR, yanlış kuantumu yalnız metin yakalar) ve (c) `0.00005` satırının Python varsayılanı HALF_EVEN ile HALF_UP'ı ayırmasından geliyor; ÖLÇÜLEN MUTASYONLAR (ikinci turda `9c8cb87` ile birleşmiş ağaçta YENİDEN koşuldu, hiçbiri önceki turdan devralınmadı; birim testi + PG ikizi birlikte, `--tb=line`): (1) `.quantize(...)` tamamen kaldırıldı -> `test_birim_donusumu_postgresql.py:336` KIRMIZI (`Decimal('0.00005')` verdi, PG `0.0001` sakladı; `1.123456789`/`1.1235`; `33.3333333`/`33.3333`; `2500.0`/`2500.0000`), ayrıca `tests/test_birim_donusumu.py:197`, `:240`, `:325` ve PG `:385`, `:505`, `:589` — 16 kırmızı; (2) kuantum `0.01` yapıldı -> PG `:336` üç parametrede KIRMIZI (`'1.12' == '1.1200'` — SAYISAL eşitlik geçerdi, metin yakaladı), `:385`, `:505`, `:589` ve birim `:209`, `:224`, `:240`, `:251`, `:325` — 18 kırmızı; DİKKAT: bu mutasyonda `2 KG -> TON` (`0.002 -> 0.00`) ve `0.00005 KG -> KG` artık SIFIR-ÜRÜN REDDİYLE de kırmızı oluyor (`app/units.py:313`), yani yanlış ölçek sessiz bir yuvarlama olarak değil GÜRÜLTÜLÜ bir red olarak görünüyor; (3) `rounding=ROUND_HALF_UP` silindi -> 3 KIRMIZI ve ÜÇÜ DE `app/units.py:313`teki `UrunTemsilEdilemez`den: `0.00005 KG -> KG` HALF_EVEN'da `0.0000` olur ve red onu `test_birim_donusumu.py:196`, `:301` (50 gram sınır satırı) ve PG `:324` (`0.00005` parametresi) noktalarında yakalar — önceki turda `:336`daki `'0.0000' == '0.0001'` karşılaştırmasına kadar gidiyordu, şimdi red DAHA ERKEN düşürüyor; (4) `_katsayi`nin `BIRIM_TANIMSIZ` `raise`'i `return Decimal("1")` yapıldı -> `tests/test_birim_donusumu.py:109` ve PG `:423` KIRMIZI (`DID NOT RAISE BirimCozulemedi`); (5) OKUMA SIRASI TERS ÇEVRİLDİ (evrensel harita `_katsayi`nin başında ürün satırından ÖNCE okunacak şekilde) -> `tests/test_birim_donusumu.py:81` KIRMIZI (`evrensel harita ürünün beyanını EZDİ`, 900 yerine 1000) ve PG `:588` KIRMIZI (`evrensel harita defteri EZDİ`) — önceki turda bu mutasyon İDDİA edilmiş ama KOŞULMAMIŞTI, bu turda koşuldu; (6) SIFIR-ÜRÜN REDDİ KALDIRILDI (`if entered_quantity != 0 and urun == 0` bloğu silindi) -> `tests/test_birim_donusumu.py:273`, `:299` ve PG `:464` KIRMIZI (`DID NOT RAISE UrunTemsilEdilemez`); ALTISI DA AYIRT EDİCİ, her biri geri konulduğunda 49/49 YEŞİL ve `git diff --stat app/units.py` TEMİZ; red testi yalnız istisnayı değil VERİTABANININ DEĞİŞMEDİĞİNİ de ölçüyor çünkü bir çağıranın istisnayı yakalayıp yine de yazması mümkündür — o denetim yukarıdaki mutasyonda ÇALIŞMAZ (`pytest.raises` daha önce düşer) ve bu, testin İÇİNDE açıkça yazıldı, başka bir mutasyonu hedeflediği belirtilerek; İKİ ÇİVİ TAHMİN EDİLMEMİŞTİ VE İKİSİ DE OYNADI — BU KAYDIN EN DEĞERLİ SATIRI BUDUR: (1) `tests/test_goc_zinciri.py:424` `zincir.bas == "20260902_0065"` çivisi ELLE GÜNCELLENİR ve öyle TASARLANMIŞTIR ("bu satır her yeni göçte KIRMIZI olur ve yeni başın BİLİNÇLİ bir karar olmasını zorlar"), üç yerde 0066'ya alındı (`:424`, `:449`, `:511`); (2) `tests/test_tenant_scoping_guard.py:1367` `TENANT_TABLES` envanteri `tenant table inventory drift: missing=['product_unit_factors'], stale=[]` ile KIRMIZI oldu — yeni tablo `company_id` taşıdığı için BEYAN EDİLMEK ZORUNDA ve nöbetçi tam olarak bunu yapmak için var, `TENANT_TABLES`a gerekçesiyle eklendi; TAHMİN EDİLİP TUTAN ÇİVİLER (hepsi ÖLÇÜLDÜ, hiçbiri oynamadı): `INVENTORY_FINGERPRINT`, `TOTAL_CORE_QUERIES`, `BEKLENEN_CORE_IFADE_SAYISI` (134 test yeşil), `operation`/`path`/`authenticated`/`read`/`undeniable` route ve authorization sayıları (78 test yeşil), `test_v2_9_hardening` + `test_v2_9_decimal_contract` (10 yeşil); `pg_population` 99 -> 100 İKİ YERDE de güncellendi (`tests/test_ci_gates.py:57` ve `:65`, `ci.yml:468`) ve sayım ÖLÇÜLDÜ: 98 PG glob + 2 adlandırılmış = 100; `core_schema.py`, `inventory.py` VE `numeric_manifest.py` DEĞİŞTİRİLMEDİ (`git status` ile ölçüldü) ve bu, `core_schema.py`de `reorder_point` için yazılmış NOT'un aynı gerekçesidir: 0015 sonrası her sütun yalnız alembic'te yaşar, bildirmek onları sayısal manifestoya kaydettirir ve manifestonun "0000 tabanında mevcut" sözleşmesi bir VARLIK FARKI üretip mutabakat kapısını kırardı — `post_hardening`e de DOKUNULMADI; ZİNCİR TEK BAŞLI: `alembic heads` -> `20260902_0066 (head)`, `scripts/goc_zinciri.py --kontrol` -> `DÜĞÜM/DOSYA 67/67`, `BİRLEŞME 1`, `ÇATALLANMA 1`, `TEK BAŞ VAR`, `HEPSİ ERİŞİLEBİLİR`, `SAYIM TUTUYOR`, ata `20260902_0065`; GÖÇ GERÇEK PostgreSQL 16.14'TE KOŞTURULDU ve şema `information_schema`dan doğrulandı — `base_unit` `character varying` NULL varsayılansız, `entered_quantity` numeric(18,4) NULL, `entered_factor` numeric(24,10) NULL, `entered_unit` varchar NULL, `product_unit_factors` üzerinde `ck_product_unit_factors_factor_positive CHECK (factor > 0::numeric)`, `fk_product_unit_factors_product_same_company FOREIGN KEY (company_id, product_id) REFERENCES products(company_id, id)` ve `uq_product_unit_factors_effective`; SAHİP KARARI 3 — SIFIR OLMAYAN GİRİŞ SIFIR ÜRÜNE DÜŞERSE RED: birinci turun bulgusu (`1 GRAM -> TON` 4 basamakta `0.0000`, ölçeğin doğrudan sonucu, ama sıfır olmayan bir girişin sıfır ürüne dönüşmesi başlıktaki görünmez-hata sınıfına YAKIN) sahibe götürüldü ve karar REDDİR; dar koşul `entered_quantity != 0 AND base_quantity == 0 -> raise`, istisna `UrunTemsilEdilemez` — `BirimCozulemedi` AİLESİNDE (aynı `except` onu da yakalar, `.sebep == URUN_TEMSIL_EDILEMEZ` ile de ayrılır) ama KENDİ ADIYLA, çünkü "birimi bilmiyorum" ile "birimi biliyorum, sayı 4 basamağa sığmıyor" farklı hatalardır ve PR 2'nin çağıranı ikisini farklı yönlendirecektir (outbox için adı konmuş atlama kovası, etkileşimli yazma için 4xx — `base_unit IS NULL` için zaten verilmiş kararın aynı şekli); istisna girilen miktarı ve kullanılan katsayıyı YUVARLANMADAN taşır; GEREKÇE beş maddedir ve kayıt talimatı değil akıl yürütmeyi taşısın diye yazıldı: (a) düşman SESSİZLİKTİR, hassasiyetsizlik değil — sıfır olmaması gereken bir sıfır sessiz bir kayıptır ve bu proje aynı oturumda o sınıftan üç kez ısırıldı (1000x hasat hatası, tabana yuvarlanan PHI günü, yarıda kesilen ifadeden sonra kaybolan satırlar), (b) çözücüyü henüz kimse çağırmıyor, bugün raise etmenin maliyeti sıfır, (c) red kararı KORUR (çağıran yakalar ve seçer), (d) sessiz sıfır kararı KAPATIR (kurtarma yok; red kurtarılabilir, operatör temsil edilebilir birimle yeniden girer), (e) sözleşme DEĞİŞMEDİ — "ürün 4 basamaktır" duruyor, red o sözleşme uygulanmadan önce, 4 basamağın girdiyi hiç temsil edemediği durumda ateşler; GERÇEK SIFIR GEÇER — ÖLÇÜLDÜ: `resolve(0, "KG", "KG")` -> `(Decimal('0.0000'), Decimal('1'))`, red YOK; `resolve(1, "GRAM", "TON")` -> `UrunTemsilEdilemez` (`sebep=URUN_TEMSIL_EDILEMEZ`, `isinstance(..., BirimCozulemedi)` True); dört gerçek-sıfır durumu (`0 KG->KG`, `0.0 KG->KG`, `0 GRAM->TON`, `0.000 ÇUVAL->KG` katsayılı) parametreli testte geçiyor; SINIR TAM KUANTUMDA: `49 GRAM -> TON` (`0.000049`) RED, `50 GRAM -> TON` (`0.00005`, HALF_UP) `0.0001` GEÇER — red ölçekten ayrı bir eşik uydurmuyor; `test_KABA_tabana_cevrim_SIFIRA_yuvarlanabilir_BILINEN_DAVRANIS` SİLİNDİ ve yerine `test_SIFIR_OLMAYAN_giris_SIFIR_urune_DUSERSE_REDDEDILIR_sessiz_sifir_YOK`, `test_SIFIR_URUN_reddinin_SINIRI_tam_kuantumdadir` ve `test_GERCEK_SIFIR_giris_GERCEK_SIFIR_urun_verir_RED_YOK` geldi; `test_KATSAYI_yuvarlanmadan_DONER_urun_olcegine_cekilmez` miktarı `1`den `1000000`e alındı çünkü `1 x 0.0000012345 = 0.0000` artık DOĞRU OLARAK reddediliyor ve o testin iddiası katsayıdır, ürün değil (PG ikizi zaten 1000000 kullanıyordu); PG İKİZİNE İKİ TEST EKLENDİ: `test_SIFIRA_DUSEN_urun_HAREKET_yazdirmaz_stok_DEGERI_bunu_GOREMEZDI` — burada `products.stock`un değişmediğini ölçmek KÖRDÜR (`5 + 0.0000 = 5`, red kaldırılsa da stok aynı kalır ve "stok değişmedi" yeşil geçerdi), sessiz sıfırın görünür izi `quantity=0.0000` ile `entered_quantity=1` taşıyan TUTARSIZ hareket satırıdır, bu yüzden ölçülen HİÇBİR HAREKET SATIRININ yazılmamış olmasıdır — ve `test_GERCEK_SIFIR_giris_PG_uzerinde_de_REDDEDILMEZ` (sıfır giriş hareket yazar: `0.0000 / 0.0000 / 0.0000010000`); İKİNCİ TUR AYRICA `9c8cb87` (PR #26 birleşmiş `origin/develop`) İLE BİRLEŞTİRDİ: `git merge origin/develop` ÇAKIŞMASIZ (`4a81174`nin ağacına göre birleşme commit'i SIFIR dosya değiştirdi, çünkü `2ea58c1` zaten develop'un atasıydı), `scripts/durum.py --kapi origin/develop --pr 27` -> `base girdi sayısı: 72 / ölçülen ağaçtaki girdi sayısı: 73 / BAYAT SIRA YOK / YİNELENEN SIRA YOK / GİRDİ VAR (#27 kendi girdisini ekliyor)` exit 0 — bu dosya `0073` olarak `base_max + 1`dir ve #21/#24/#25 de 0073'e gidiyor, kapı taban ile bitişikliği zorlar, dallar arası tekilliği DEĞİL; ÇİVİLER BİRLEŞMİŞ AĞAÇTA YENİDEN ÖLÇÜLDÜ, hepsi TUTTU: `INVENTORY_FINGERPRINT` · `TOTAL_CORE_QUERIES` · `BEKLENEN_CORE_IFADE_SAYISI` · route/authorization `operation/path/authenticated/read/undeniable` · `test_v2_9_hardening` · `test_v2_9_decimal_contract` · `test_ci_gates` · `test_goc_zinciri` · `test_tenant_scoping_guard` birlikte 217 yeşil; `pg_population` 100 iki yerde de (`tests/test_ci_gates.py:57`/`:65`, `ci.yml:468`) ve sayım 98 glob + 2 adlandırılmış = 100; `alembic heads` -> `20260902_0066 (head)`; `goc_zinciri.py --kontrol` -> `67/67`, `TEK BAŞ VAR`, `SAYIM TUTUYOR`; `TENANT_TABLES` `product_unit_factors` içeriyor; `core_schema.py`, `inventory.py`, `numeric_manifest.py` `origin/develop`a göre DEĞİŞMEMİŞ (`git diff --stat` boş); `app.units`i içe aktaran YALNIZ göç dosyası ve iki test — çağıran hâlâ YOK; YEREL ÖLÇÜMLER (ikinci tur, birleşmiş ağaç): 30 birim testi + 19 PG ikizi = 49 yeşil, `tests/` süpürmesi (`--ignore=tests/fixtures`) 805 geçti / 7 düştü — birinci turda 800/7 idi ve fark tam olarak bu turun 5 yeni birim testidir; YEDİSİ DE TABANDA AYNEN DÜŞÜYOR — bu VARSAYILMADI, değişiklikler `git stash` ile çıkarılıp temiz tabanda AYNI süpürme koşuldu ve AYNI yedi ad çıktı (775 geçti, fark tam olarak bu PR'ın 25 birim testi), yani hiçbiri bu dala ait değil: üçü `test_field_stok_zamanlayici` iş parçacığı testleri, ikisi `test_line_item_tenancy_slice1` (izole koşulduğunda ATLANIYOR, süpürmede başka bir testten SIZAN `DATABASE_URL` yüzünden koşuyor — göçüm ONLARI KIRMIYOR, temiz bir veritabanı ve `DATABASE_URL` verildiğinde İKİSİ DE GEÇİYOR, bu AYRICA ölçüldü), biri `test_self_service_exemption` ve biri `test_supplier_price_bridge_units` (ikisi de İZOLE koşulduğunda GEÇİYOR, yani sıra/artık-veritabanı bağımlılığı); DEVRALINAN KIRMIZI: `develop` `frontend` işinde `AUDIT_POLICY_RED: GHSA-6GMQ-8VP8-GCM6 (@xmldom/xmldom)` ile kırmızıydı (develop'un kendi koşusu `33655135774`) ve bu dal PR #26'nın (`2ea58c1`) ÜZERİNE kurulduğu için o düzeltmeyi ZATEN TAŞIYOR — bu PR o kapıyı ne açtı ne kapattı, ona HİÇ dokunmadı; CI ÖLÇÜLDÜ — birinci turun başı `587a575` için PR #27 koşusu `33677940055`: 17 zorunlu bağlamın 17'si YEŞİL (`alembic-chain`, `backend-postgresql` 0-3, `backend-quality` 0-3, `backend-quality-aggregate`, `backend-quality-canonical`, `container`, `contract-drift`, `durum-kaydi`, `e2e`, `frontend`, `verify-image-artifact`; `publish-image` ATLANDI, zorunlu değil), develop'tan devralınan `frontend` kırmızısı PR #26 ile kapandığı için burada YOK; ÖLÇÜLMEDİ: bu ikinci turun başının (birleşme + sıfır-ürün reddi) KENDİ CI koşusu bu kayıt yazılırken YAPISAL OLARAK ölçülemez — kayıt commit'in içindedir ve koşu push'tan sonra başlar, sonuç PR'daki rapora düşer, buraya değil, `frontend` tarafının hiçbir adımı yerelde koşulmadı (bu diff frontend'e dokunmuyor), PG ikizi yalnız YEREL PostgreSQL 16.14'te koşuldu (CI'ın 16-alpine servisinde değil), 0066'nın KENDİ `downgrade`i PostgreSQL'de AYRICA koşulmadı (SQLite tarafında `test_line_item_tenancy_slice1` üzerinden dolaylı koşuldu), ve `product_unit_factors`ın `effective_from` sıralamasıyla GEÇERLİ SATIRI SEÇME mantığı bu PR'da YAZILMADI — ikizdeki sorgu bir ÖRNEKTİR, ürün kodu değildir, çünkü seçiciyi yazacak çağıran PR 2'dedir.
