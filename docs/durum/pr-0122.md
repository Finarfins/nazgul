#122 — PP1 PLATFORM PANELİ: KİRACI MUAFİYETİ + SALT-OKUNUR YÖNETİM API'Sİ (göç YOK, alembic başı 20260914_0084 SABİT): `/api/platform/*` kiracı çözümünden MUAF — tek yüklem `platform_access.platform_yolu`, muaf yolda `resolve_company` koşmaz, `X-Company-Id` ve idempotensi defteri yok sayılır, asıl kapı `require_platform_operator`; ÖNCE/SONRA ÖLÇÜLDÜ (taze SQLite, develop 6442794'e karşı): sıfır üyelikli operatör `GET /api/platform/backups` 403 COMPANY_ACCESS_DENIED -> 200, `POST` 403 + CHECK ck_security_audit_logs_untenanted_only_preauth ihlali -> yedek çıkış, `/api/ready` 503 -> `POST` 201 ve `/api/ready` 200; DENETİM SATIRI TAŞINDI: üyeli operatörün `X-Company-Id: 1` ile aldığı yedek önce `activity_logs(company_id=1,user_id=1,'backup.created')` idi, şimdi `security_audit_logs(company_id NULL, user_id NULL, username NULL, action 'platform.bk_create', failure_reason 'aktor=<id>; olay=backup.created | …')` ve `activity_logs`ta 0 satır — kimlik CHECK yüzünden NULL, aktör not sütununda (TAVİZ, PR gövdesinde); kiracı geri yükleme olayı da aynı yazıcıya taşındı (muafiyet sonrası `int(None)` ile işlem BİTTİKTEN sonra 500 verirdi); YEDİ GET (`overview`, `companies`, `users`, `verifications`, `outbox/health`, `rate-limits`, `edocuments/health`) yalnız sayı + meta veri, SEC-3b envanteri LİSTELEMİYOR (17 satırda sabit); izin `/api/platform/` + YALNIZ GÜVENLİ METOT -> `read`; PİNLER: 400/308 -> 407/315, GET 190 -> 197, AUTHENTICATED 387 -> 394, GUARDED_READ 24 -> 31, READ 80 -> 87 (brief 80 diyordu; guarded ⊂ read olduğundan aritmetik zorunluluk, çıplak read 56 ve UNDENIABLE 97 SABİT), TENANT_TABLES 121, pg_twins 127 -> 128, alt_surec 136; Core kiracı kapısı 152 -> 159 ifade ve lisans 4 -> 11, Core envanter 184 -> 199; dinamik text() sayısı değişmedi, literal text() -1 (`platform_backups`un ilk-firmaya-yaz yedek yolu silindi); TESTLER: `tests/test_pp1_platform_paneli.py` 50 (ileri/ters/3× karışık 50/50; mutasyon: muafiyet dalı 28, kimlik taşıma 2, önek 1 kırmızı), PG ikizi taze DB 12/12, tam izole paket 4×115 dosya yeşil; AÇIK BULGU (kapsam dışı): platform DIŞI yolda sıfır üyelikli kullanıcının yazma 403'ü hâlâ CHECK'i ihlal edip `/api/ready`i 503'e düşürüyor (ölçüldü) — ve bu kayıt; kayıt adı `python scripts/durum.py --sonraki 122`
