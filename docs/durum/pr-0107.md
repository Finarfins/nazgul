#107 — SEC-8: dağıtım sertleştirme (deployment hardening) — `cryptography>=50,<51` pini ve 50.0.1 kilit hash'leri, Caddy imajının sha256 digest ile sabitlenmesi (`caddy:2.8-alpine@sha256:af32e973...`), üretim CORS/CSP/HSTS güvenceleri ve statik dağıtım kapıları (`backend/tests/test_sec8_deploy_kapilari.py`); KUSUR VE ENVANTER ÖLÇÜLDÜ (develop 8cfa7a0): (1) `cryptography`: `backend/requirements.txt` dosyasında açık bir girdi yoktu, `pdfplumber==0.11.10` -> `pdfminer.six==20260107` -> `cryptography>=36.0.0` zinciri üzerinden geçişli (transitive) geliyordu; `requirements.lock` dosyasında 49.0.0 kilitliydi ve CI `container` işinde `cryptography==49.0.0` kuruluyordu (koşu 34390710694 logu kanıtı), oysa standart koşucuda pip doğrudan 50.0.1 çözüyordu; bağımlı kırılmadı (50.0.1 `pdfminer.six`in `>=36.0.0` kuralını sağlar, testler yeşil), `backend/requirements.txt`ye `cryptography>=50,<51` eklendi, `backend/requirements.lock` 50.0.1 sürümünün PyPI'daki 46 sha256 hash'iyle güncellendi ve `test_requirements_lock.py` 2/2 yeşil; (2) CORS: `docker-compose.prod.yml` ve `.env.production.example` ölçüldü — üretim yapılandırması `CORS_ORIGINS`i `https://${APP_DOMAIN...}` ile güvenli HTTPS olarak türetiyor, varsayılanlarda hiçbir `*` veya `http://` yok; `app/config.py` zaten `validate_cors_origins` ve `cors_origin_list` ile wildcard'ı reddediyor, `test_sec8_deploy_kapilari.py` içinde `environment == "production"` durumunda `cors_origin_list`in asla `*` içermediği ve `cors_origins="*"` durumunda `Settings` başlatımının reddedildiği başlangıç bekçisi (startup guard) ile doğrulandı; (3) CSP & HSTS: `main.py:650-656` ölçüldü — `default-src 'self'; script-src 'self' https://challenges.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://challenges.cloudflare.com; frame-src 'self' https://challenges.cloudflare.com; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'` değerinde script-src içinde `'unsafe-inline'` YOKTUR, `frame-ancestors 'none'` mevcuttur, MUI satır içi stilleri için `style-src 'unsafe-inline'` korunmuştur, HSTS `Strict-Transport-Security: max-age=31536000; includeSubDomains` hem Caddyfile hem FastAPI seviyesinde etkindir; frontend `tsc -b && vite build` sıfır hatayla derlendi; (4) Caddy / Compose: `docker-compose.prod.yml` servisleri incelendi — `app` servisi `ports: !override []` ile ana makine portunu düşürür, `db` servisi `profiles: ["gate"]` altındadır ve hiçbir port yayınlamaz, `deploy/Caddyfile` içinde `admin off` tanımlıdır (admin API portu 2019 kapalıdır), dışa açılan tek portlar Caddy'nin 80 ve 443 (TCP/UDP) portlarıdır; Caddy imajı multi-arch manifest list digest'iyle `caddy:2.8-alpine@sha256:af32e97399febea808609119bb21544d0265c58a02836576e32a2d082c262c17` olarak sabitlendi (CI `container` işindeki `sed` çıkarma kuralıyla uyumlu); uygulama imajı `ghcr.io/finarfins/nazgul:${APP_IMAGE_TAG}` sürüm akışı (release flow) gereği etiket tabanlı kaldı; (5) Ağ yapısı ve çalışma zamanı: hiçbir üretim servisi 0.0.0.0 ana makine portu bağlamaz, :147 sağlık kontrolü konteyner içi yerel yoklamadır (`127.0.0.1:5050/api/ready`); çalışma zamanı kullanıcısı repo kökündeki Dockerfile içinde tanımlı 'app' (USER app, uid 10001) kullanıcısıdır (backend/Dockerfile veya 'appuser' değil); KAPILAR VE TESTLER: `backend/tests/test_sec8_deploy_kapilari.py` (8 test: 5 kapı + 3 mutasyon testi) ile Caddy digest sabitlemesi, DB/app port yokluğu, admin API kapalılığı, üretim CORS https/wildcardsızlığı ve pydantic CORS startup guard'ı statik olarak mühürlendi; `_yorumsuz(metin)` yardımcısı ile tüm dosya içeriği kapıları #... yorumlarından arındırılmış metin üzerinde çalışır, yorumlu+değiştirilmiş 3 varyant mutasyon testleriyle doğrulanır; ÇİVİLER VE SAYACLAR SABİT: rota sözleşmeleri 391/301, GET izinleri 186, TENANT_TABLES 120, alembic başı 0082, pins (`pg_twins.txt` 123, `alt_surec_sql.txt` 127) DOKUNULMADI; `test_ci_gates.py` 20 passed, `test_requirements_lock.py` 2 passed, `test_sec8_deploy_kapilari.py` 8 passed; GÖÇ YOK (`backend/alembic` diff'i BOŞ); `backend/app` diff'i TAMAMEN BOŞ (CORS bekçisi ve CSP FastAPI'de zaten güvenliydi, kod değişikliği gerekmedi); tek mercek (Claude Code); kayıt adı `python scripts/durum.py --sonraki 107`
