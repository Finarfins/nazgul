#100 — TEST/DOC: H12 anti-enumeration zaman duvarı dalgalanması kapandı + WA2 PG ikizi docstringi güncellendi + docs/prompts .gitignore'a eklendi; (1) H12: `backend/test_self_service_registration.py` içindeki zaman duvarı ölçümünde ilk kayıt isteğinin soğuk başlama (cold-start) maliyetini (FastAPI yönlendirme, Pydantic doğrulama, DB havuzu ve Argon2 bytecode ısınması) örnekleme havuzunun dışına almak için örnekleme öncesi warm-up isteği (`register("isinma@example.com", ...)` ) eklendi; tekil işletim sistemi zamanlayıcı ve çöp toplayıcı duraklamalarının medyanı saptırmasını önlemek için örnek sayısı 3'ten 5'e çıkarıldı (`range(5)`); olası regresyonlarda telemetrinin yakalanabilmesi için `print(TIMING_BUDGET_MS)` deyimi `assert` öncesine alındı; bütçe formülü (`|Δ| < max(0.12, 0.35 × slower)`) ve ayırt edilemezlik değişmezi korunarak 30/30 koşunun tamamı yeşil, maks |Δ| = 16.00 ms (120.00 ms bütçe içinde, 22/30 koşu tam 0.00 ms); uygulama koduna (`backend/app`) dokunulmadı; (2) WA2 PG İKİZİ: `backend/test_wa2_eslestirme_postgresql.py` modül docstringindeki yarış kurgusu SEC-1 (#99, göç `20260912_0082`) sonrası geçerli olan tek kurguyu (yirmi işçi, aynı hedef numara, bir kazanan) birincil açıklama yapacak şekilde yeniden yazıldı; 0082 öncesi yirmi ayrı numara kurgusu tek kısa tarihçe paragrafına indirildi; `git diff`te sıfır kod satırı değişti; (3) .gitignore: orkestrasyon taslakları için kök `.gitignore` dosyasına `docs/prompts/` eklendi; GÖÇ YOK (`alembic head` 20260912_0082 korundu), UYGULAMA KODU DEĞİŞİKLİĞİ YOK (`git diff origin/develop -- backend/app backend/alembic` boş); kayıt adı `python scripts/durum.py --sonraki 100`
