#78 — WA3-core DÜZELTME TURU (#77 sonrası, aynı dal): runtime lens `E164_EN_AZ_RAKAM` 10->9 mutantı HİÇBİR testi öldürmüyordu; `tests/test_wa3_niyet.py` geçersiz kümesine 9 rakamlı iki girdi eklendi (`"1"*9` ve `"+90"+"1"*7`) ve mutant ÖLÇÜLDÜ: `test_e164_gecersiz_numara_yukseltir[111111111]` ve `[+901111111]` KIRMIZI, kaynak geri alındı (commit `f20ffd6`); `docs/whatsapp/WA3_BILINEN_SINIRLAR.md` eklendi — (1) okuma akışında soyadı katlaması: `Yılmaz`→`YIL` durak kökü soyadını DÜŞÜRÜR (kaynak davranışı, `test_OKUMA_akisinda_YILMAZ_soyadi_YIL_kokune_yaslanir_OLCULDU` ile adıyla sabit, düzeltme WA3-full), aynı sınıf ÖLÇÜLDÜ: `Günay`/`Sonat` düşer, `Aydın` KORUNUR (`AY` iki harf, kök ≥3) — ilk yazım `Aydın`ı yanlış listeliyordu, `31e1665`te düzeltildi; (2) çıplak `tahsilat` → `donem_ozeti` (tutar yokken `tahsilat_coz` None, `TAHSILAT` `DONEM_KOKLER`de); KOD DEĞİŞMEDİ (yalnız test + belge), çiviler AYNEN; #77 bu commit'ler dalda dururken birleştiği için ayrı PR; TABAN `origin/develop` `87adec6`; kayıt adı `python scripts/durum.py --sonraki 78`
