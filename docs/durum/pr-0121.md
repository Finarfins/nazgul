#121 — SEC-3b CARI ALAN ENVANTERI: e-İRSALİYE SURETİ PİNLENDİ (göç YOK, kod YOK, uç YOK — TEK veri satırı): `develop` @ `474e2ec` KENDİ BAŞINA KIRMIZIYDI ve bu İDDİA EDİLMEDİ ÖLÇÜLDÜ — temiz bir worktree (`git worktree add --detach origin/develop`, hiçbir dalım karışmadan) `tests/test_sec3b_cari_alan_envanteri.py::test_cari_alan_envanteri_pinle_ayni` ile 1 failed / 5 passed veriyor: "Cari HASSAS ALAN dondururen ve pinde OLMAYAN rota(lar): GET /api/despatch-notes/{despatch_id}/edespatch/download"; SEBEP BİRBİRİNİ GÖRMEMİŞ İKİ PR'DIR ve zamanlama ölçüldü (`gh pr view --json mergedAt`): #116 (E4a e-İRSALİYE) 17:42'de indi ve `GET /api/despatch-notes/{id}/edespatch/download` ucunu EKLEDİ, #114 (SEC-3b) 19:02'de indi ama `pins/cari_alan_envanteri.txt`i o ucun VAR OLMADIĞI bir ağaçta ölçüp pinlemişti; İKİSİ DE TEK BAŞINA YEŞİLDİ ve git ÇATIŞMA VERMEZ — pin ANLAMSAL olarak bayatladı, METİNSEL olarak değil, yani bu kusur ancak BİRLEŞMİŞ ağaçta görünür ve iki PR'ın da kendi CI'ında GÖRÜNMEZDİ; MASKELEME DEĞİL PİNLEME SEÇİLDİ ve kapının kendi sorusu ("maskeleme gerekiyorsa dikişten geçirin; gerekmiyorsa gerekçesiyle pine ekleyin") ÜÇ ÖLÇÜMLE cevaplandı: (1) ALANLAR MALİ BELGENİN ZORUNLU YAPISAL PARÇASIDIR — uç `edespatch.build_despatch_xml` ile bir UBL-TR e-İrsaliye BELGESİ üretiyor ve `_taraf` alıcının VKN/TCKN'sini `cac:PartyIdentification/cbc:ID[@schemeID]`, adresini `cac:PostalAddress/cbc:StreetName` olarak yazıyor (`app/einvoice/edespatch.py:378-394`), yani ikisi de GÖSTERİM SÜSLEMESİ DEĞİL belgenin iskeletidir ve maskelenmiş bir belge GEÇERSİZ olurdu; (2) MASKELEME ZATEN ÇALIŞMAZDI — maskeli `tax_number` `"*******890"` olur (`app/alan_maskeleme.py:245`) ve `_kimlik_semasi` TAM 10 ya da 11 HANE ister (`edespatch.py:363-373`), yıldızlı değer ne VKN ne TCKN sayılır ve uç HATA VERİR; yani maskeleme yalnız hukuken yanlış değil İŞLEVSEL OLARAK DA imkânsızdır; (3) YENİ BİR AÇIKLIK SINIFI DEĞİL — eklenen satır, pinde ZATEN DURAN İKİ e-Fatura kardeşinin BİRLEŞİMİDİR: `invoices/{id}/einvoice/download` (sales, address, F,F,F,D,D) ve `invoices/{id}/pdf` (sales, tax_number, F,F,F,D,D); yeni satır `despatch-notes/{id}/edespatch/download  sales  address,tax_number  F,F,F,D,D`, yani İZİN de GÖRÜNÜRLÜK VEKTÖRÜ de onlarla BİREBİR AYNI ve bu vektör ÜRETİLİR, elle yazılmaz; vektörün anlamı da ölçüldü (`envanter_uret` gövdesi): `D` = rol o izni TAŞIMIYOR, `F` = tam görünür, `M` = maskeli, `S` = süzülmüş, `H` = muaf — yani `depo` ve `rapor` `sales` taşımadığı için rotayı HİÇ GÖREMEZ (D) ve kalan üç rol (`yonetici`, `muhasebe`, `satis`) ZATEN `MASKESIZ_ROLLER` kümesindedir (`alan_maskeleme.py:53`), yani maskeleme onlara HİÇBİR durumda uygulanmazdı ve `M` hücresi hiç doğamazdı; GEREKÇENİN TAMAMI pin dosyasına `#` YORUMU olarak yazıldı çünkü `_pin_oku()` `#` satırlarını ATLIYOR (`test_sec3b_cari_alan_envanteri.py:409`) — gerekçeyi commit mesajında bırakmak, pini okuyanın onu görmemesi demekti; SEKMELER LİTERAL doğrulandı (`cat -A` -> `^I`, dosyada 17 sekmeli satır: 16 eski + 1 yeni); KOŞULAR KANONİK İZOLE KOŞUCUYLA yapıldı ve bu GEREKLİYDİ — düz `pytest tests/...` bu beş dosyayı birlikte koştuğunda `test_e4a_despatch_notes.py` 14 ERROR veriyor, sebebi dosyalar arası İMPORT-ZAMANI `DATABASE_URL` sızıntısıdır (envanter testi import anında `os.environ.setdefault("DATABASE_URL", ...)` yapıyor) ve UYDURMA bir hatadır: `run_isolated_tests.py` ile BEŞİ DE GEÇİYOR — `tests/test_e4a_despatch_notes.py` 50, `tests/test_route_get_permission_inventory.py` 8, `tests/test_route_security_contracts.py` 12, `tests/test_sec3b_cari_alan_envanteri.py` 6, `tests/test_sec3b_cari_maskeleme.py` 119 -> TOPLAM 195 test YEŞİL; DEĞİŞEN DOSYA (1): `backend/tests/pins/cari_alan_envanteri.txt` — BİR veri satırı artı gerekçe yorumu; KOD, GÖÇ, UÇ, ŞEMA, İZİN DEĞİŞMEDİ, yani alembic başı / rota sözleşmesi / GET envanteri / TENANT_TABLES çivilerinin HİÇBİRİ bu dilimde kımıldamaz; BU DÜZELTME #120'yi (SEC-9) DE AÇAR: onun CI'ı BİRLEŞME commit'ini denediği için bu kırmızıyı DEVRALDI (#120'nin kendi 15 işi YEŞİL, tek kırmızısı `backend-quality (2)` ve içindeki tek hata BU dosyaydı); — ve bu kayıt; kayıt adı `python scripts/durum.py --sonraki 121`
