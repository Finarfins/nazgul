#111 — SEC-4b: `app.main` İTHALİ ARTIK SALT OKUNURDUR ve bu dilimin kapattığı şey SEC-4'ten (#96) GERİYE KALAN TEK YAZMA PENCERESİDİR (GÖÇ YOK — alembic başı `20260912_0082` KIMILDAMADI, ROTA YOK, ŞEMA DEĞİŞMEDİ, TABLO EKLENMEDİ, `frontend/src/api/types.gen.ts` DEĞİŞMEDİ çünkü uç noktaların hiçbiri kımıldamadı); AÇIK ÖLÇÜLDÜ İDDİA EDİLMEDİ ve ölçüm develop `ef4639d` üzerindedir: SEC-4 ithalden GÖÇÜ kaldırmıştı (`if settings.auto_migrate: _semayi_hazirla()`) ama modül gövdesinde HEMEN ARDINDAN duran satır KOŞULSUZDU — `app/main.py:200` düz `maintenance_status(engine)` diyordu ve o çağrının imzası `recover_stale: bool = True`dır, yani ithal PostgreSQL'de SESSİZCE bir kurtarma yolu açıyordu; `app/maintenance.py:357-379` şunu yapar: `platform_maintenance` 1 numaralı satırı AKTİF ise ve dialect `postgresql` ise `_recover_stale_row` çağrılır, o da `pg_try_advisory_lock(MAINTENANCE_LOCK_KEY)` ile satırın SAHİPSİZ olup olmadığını sınar (kilidi alabiliyorsa onu yazan süreç ÖLMÜŞTÜR), alabilirse satırı `UPDATE` eder ve `_record_recovery` hem restore günlüğüne hem `activity_logs`a bir `INSERT` atar — yani `AUTO_MIGRATE=false` ile açılan ÜRETİM işçisi ithalde İKİ DML koşuyordu ve `app/bootstrap_data.py:244`ün "Üretimde AUTO_MIGRATE kapalıdır ve `app.main` ithalinde artık hiçbir DML koşmaz" cümlesi LAFZEN YANLIŞTI; ÖLÇÜM DÜZENEĞİ VE SAYILAR: PG 16.4 üzerinde tek kullanımlık bir şemaya göç sürüldü, 1 numaralı satır `active=true, operation_id='OLCUM-1', heartbeat_at=now()-interval '2 hours'` ile bayatlatıldı, ardından `AUTO_MIGRATE=false` ile AYRI BİR ALT SÜREÇTE `import app.main` koşuldu ve motora `before_cursor_execute` kancası takılarak sürücüden geçen HER ifade yakalandı — sonuç `YAZMA=['UPDATE platform_maintenance SET active=false, ...', 'INSERT INTO activity_logs (company_id, user_id, action_type, ...)']`, satır `{'active': True, 'operation_id': 'OLCUM-1'}` -> `{'active': False, 'operation_id': None}`, kurtarma günlüğü `0 -> 1`; AYNI DÜZENEK DÜZELTMEDEN SONRA: `YAZMA=[]`, satır KIMILDAMIYOR, günlük `0`; ADVİSORY KİLİT SORUSU DA AYRICA SORULDU VE CEVABI ÖLÇÜLDÜ: `AUTO_MIGRATE=false` ithalinde `database_bootstrap_lock` (`POSTGRES_MIGRATION_LOCK_KEY = 79500329`) ALINMIYORDU — SEC-4 onu zaten kaldırmıştı ve bu dilim orayı KIMILDATMIYOR — ama `_recover_stale_row` KENDİ kilidini (`MAINTENANCE_LOCK_KEY = 0x53554E475552424B + 1`) alıp bırakıyordu, yani "ithal kilit almıyor" cümlesi de YARIM doğruydu; ARTIK O YOLA HİÇ GİRİLMİYOR ve kapı bunu `pg_locks` ÖRNEKLEYEREK DEĞİL İFADE DÖKÜMÜNDEN ölçüyor, çünkü kilit ithalin İÇİNDE alınıp yine ithalin İÇİNDE bırakılıyor ve ithalden SONRA bakan bir `pg_locks` sorgusu onu GÖREMEZDİ — `before_cursor_execute` ise sürücünün gönderdiği metnin TAMAMINI görür, `advisory_lock` geçen ifade sayısı SIFIR ölçüldü; ÇÖZÜM TEK SATIRDIR ve o satırın SEÇİMİ asıl karardır: `maintenance_status(engine, recover_stale=settings.auto_migrate)`; KURTARMA KAYBOLMUYOR, YERİ SABİTLENİYOR ve bu dilimin en önemli cümlesi budur: `_readiness_probe` (`app/main.py:787`) AYNI çağrıyı `recover_stale`i AÇIKÇA VERMEDEN yapar, yani varsayılan `True` orada YÜRÜRLÜKTEDİR — o çağrının gerçekten kurtardığı OKUNARAK DOĞRULANDI (`status = maintenance_status(engine)`; kurtarma sonrası `status['active']` `False` döner ve prob 200 verir), bu yüzden hazırlık yolunu `recover_stale=True` diye YENİDEN YAZMAK GEREKMEDİ; hazırlık probu ithalden ÜSTÜN BİR YERDİR ve gerekçe üç katlıdır: prob süre bütçelidir (`readiness_timeout_seconds`), İŞÇİ BAŞINA değil İSTEK BAŞINA koşar (N işçi tek kilit için sıraya girmez) ve düştüğünde AÇILIŞI değil TEK BİR PROBU düşürür — ithalde patlayan bir kurtarma ise işçiyi hiç açtırmazdı; `AUTO_MIGRATE=true` DAVRANIŞI DEĞİŞMEDİ ve bu VARSAYILMADI, ÖLÇÜLDÜ (aşağıdaki (b) kapısı); KAPILAR İKİ DOSYADADIR ve bölünme dialect sınırının TA KENDİSİDİR: DAVRANIŞ PostgreSQL'e AİTTİR çünkü `maintenance_status` `engine.dialect.name != "postgresql"` ise kurtarmaya HİÇ girmez, yani kusur SQLite'ta GÖRÜNMEZ; (1) `backend/test_sec4_acilis_migrasyon_postgresql.py` "SEC-4b" bölümü — dosya `backend/tests/pins/pg_twins.txt`de ZATEN VARDIR ve PİN DOSYASI KIMILDAMADI, yeni bir ikiz dosyası açmak yerine mevcut ikizin ucuna yazmanın sebebi tam olarak budur; (a) `test_AUTO_MIGRATE_KAPALI_ITHAL_BAYAT_SATIRI_KURTARMIYOR_prob_KURTARIYOR` TEK testte İKİ YARIYI ZİNCİRLER ve bu BİLİNÇLİDİR — "ithal yazmıyor" TEK BAŞINA bir REGRESYON olurdu, sahipsiz satır hiç temizlenmiyorsa örnek SONSUZA DEK bakım modunda kalır ve TRAFİK ALMAZDI, yani iddia yazmanın KAYBOLMASI değil YER DEĞİŞTİRMESİDİR: bayat satır + `AUTO_MIGRATE=false` ithal -> satır DEĞİŞMEMİŞ, kurtarma günlüğü +0, ifade dökümünde SIFIR INSERT/UPDATE/DELETE ve SIFIR advisory kilit; ARDINDAN aynı ayarlarla `GET /api/ready` -> 200, satır TEMİZ, günlük +1; (b) `test_AUTO_MIGRATE_ACIK_ITHAL_BAYAT_SATIRI_HALA_KURTARIYOR` regresyon kapısıdır ve gerekçesi somuttur: kwarg'ı `False` SABİTİNE bağlamak (a)'yı YİNE yeşil bırakır ve geliştirme/CI yolundaki kurtarmayı SESSİZCE kaldırırdı; (d) `test_MUTANT_ITHALDE_KOSULSUZ_KURTARMA_kirmizi_yanar` mutasyonu KAYNAK AĞACA DEĞİL tek kullanımlık bir KOPYAYA (`app/` + `alembic/` + `alembic.ini`) uygular — kanonik koşucu dosyaları PARALEL koşturur ve yerinde mutasyon komşu dosyaları NEDENSİZCE kırmızıya çevirirdi (aynı gerekçe SEC-4 kapısında zaten yazılıdır); MUTASYON AYRICA ELLE DE KOŞULDU, İDDİA EDİLMEDİ: `app/main.py` `cp` ile yedeklenip satır düz `maintenance_status(engine)`e GERİ ALINDI ve (a) KIRMIZI yandı, üstelik iki DML'i ADIYLA raporlayarak (`AssertionError: AUTO_MIGRATE=false iken ithal DML koştu` + `UPDATE platform_maintenance` + `INSERT INTO activity_logs`), sonra kaynak `cp` ile geri kondu ve `recover_stale=settings.auto_migrate` sayımı `1` olarak doğrulandı; (2) `backend/tests/test_sec4b_ithal_salt_okunur.py` (YENİ, 7 test, SQLite şeridi) ve bu dosyanın en önemli cümlesi ne ÖLÇTÜĞÜ değil ne ÖLÇEMEDİĞİDİR — kusurun KENDİSİ burada GÖRÜNMEZ, bu yüzden dosya iki BAŞKA şey ölçer: (c) dialect ERKEN DÖNÜŞÜ DURUYOR mu (`AUTO_MIGRATE` KAPALI da AÇIK da olsa SQLite'ta bayat satır kurtarılmaz; erken dönüş kalksaydı SQLite yolu `pg_try_advisory_lock`ta patlar ve `maintenance_status`un dış `except`i FAIL-CLOSED davranıp HER SQLite açılışını bakım modunda gösterirdi) ve YAPISAL kapılar: modül gövdesinde TAM BİR `maintenance_status` çağrısı ve kwarg'ı AST ile `settings.auto_migrate`e eşitlenmiş, `_readiness_probe`daki çağrının `recover_stale`i AÇIKÇA VERMEDİĞİ (yani varsayılanı KORUDUĞU), `maintenance_status` imzasının `KEYWORD_ONLY` ve varsayılanının `True` kaldığı, `_recover_stale_row`un dialect kapısının ARDINDA olduğu, `bootstrap_data` sözleşme cümlesinin YERİNDE durduğu; YAPISAL KAPILARIN GEREKÇESİ DAVRANIŞ TESTİNİN KÖRLÜĞÜDÜR: iki çağrı da varsayılana dönseydi PG ikizi kırmızı yanardı, ama `recover_stale`i PROBDAN silmek (ya da orada `False` yapmak) HİÇBİR SQLite testini kımıldatmaz ve kurtarma SESSİZCE KAYBOLURDU; kapılar METİN GREP'İ DEĞİL AST TARAMASIDIR ve bu ayrım SEC-4'ün kendi dersinden alındı — kaynakta geçen bir dize (docstring dahil) düz aramayı yanlış yeşile boyardı; SAYAÇLAR ÖLÇÜLDÜ VARSAYILMADI: rota sözleşmesi **391/301** KIMILDAMADI, GET izin envanteri **186** KIMILDAMADI, `TENANT_TABLES` **120** KIMILDAMADI, alembic başı `20260912_0082` (yedi yerdeki pin DOKUNULMADI), core sorgu envanteri YENİDEN ÖLÇÜLDÜ ve KIMILDAMADI — `main.py` DEĞİŞTİĞİ hâlde kımıldamaması BEKLENENDİR çünkü envanter `select`/`update`/`delete` Core sorgularını pinler ve bir kwarg eklemek yeni bir Core sorgusu YARATMAZ (`tests/test_core_query_inventory.py` 49 test YEŞİL), `pins/pg_twins.txt` KIMILDAMADI; TEK PİN HAREKETİ `pins/alt_surec_sql.txt`tir ve +1 SATIRDIR (133 -> 134 dosya, toplam 217 -> 218) — SEBEBİ ÖLÇÜLDÜ, TAHMİN EDİLMEDİ: `_alt_surecte_sql()` 120 karakterden UZUN, içinde SQL anahtar kelimesi geçen VE `execute(`/`create_engine`/`import app` içeren dize sabitlerini sayar ve yeni dosyanın MODÜL DOCSTRING'İ bu sezgisele TAKILIR (kusuru anlatırken hem "UPDATE" hem "import app.main" yazıyor), dosyada ayrıca `subprocess.run` vardır; ölçüm farkı TAM OLARAK `{'tests/test_sec4b_ithal_salt_okunur.py': 1}` çıktı, aritmetikle türetilmedi ve PG ikizine eklenen `_ITHAL_IZLEYICI` dizesi SAYILMADI (SQL anahtar kelimesi taşımıyor); İZOLE KOŞUM `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4` ile: yeni kapı + SEC-4 kapısı -> 2/2 dosya, **24 test YEŞİL**; `tests/test_ci_gates` + `tests/test_core_query_inventory` + `tests/test_tenant_scoping_guard` -> 3/3 dosya, **102 test YEŞİL**; `test_backend_encoding` + `tests/test_route_security_contracts` + `tests/test_sec6_ip_limitleri` -> 3/3 dosya, **24 test YEŞİL**; `tests/test_route_get_permission_inventory` -> **8 test YEŞİL**; bakım satırına dokunan KOMŞULAR AYRICA koşuldu (`test_settings_platform_scope` + `test_platform_backup_security` + `test_platform_backups` + `test_platform_maintenance_migration`) -> 4/4 dosya, **32 test YEŞİL**; PG İKİZİ ÖZEL BİR VERİTABANINDA koşuldu (`REQUIRE_PG=1`, PG 16.4, `sec4b_test`, her test KENDİ ŞEMASINDA) -> **5 test YEŞİL** (SEC-4'ün iki eski ölçümü + SEC-4b'nin (a), (b), (d)'si); TEK YEREL KIRMIZI NİTELENİYOR, SİLİNMİYOR: `test_platform_backups_postgresql.py::test_pg_dump_and_pg_restore_smoke` bu makinede `BackupError: pg_dump bulunamadı; PostgreSQL istemci araçlarını kurun` ile düşüyor — ORTAM EKSİĞİDİR, dalla ilgisi YOKTUR ve bu PR o yola DOKUNMUYOR (aynı dosyanın diğer 8 testi YEŞİL); ÇAKIŞMA NOTU: bu dilim `app/main.py`de TEK BİR satırı (200) ve etrafındaki yorumu değiştirir, `app/maintenance.py`ye HİÇ DOKUNMAZ, `backend/test_sec4_acilis_migrasyon_postgresql.py`de yalnız `_ortam`a `auto_migrate` ANAHTAR KELİMESİ ekler (varsayılan `"true"`, mevcut iki çağrı BAYT BAYT aynı kaldı) ve dosyanın ucuna yazar; dal `fix/sec4b-import-time-write`, taban `origin/develop` `ef4639d`; kayıt adı `python scripts/durum.py --sonraki 111`
