#53 — EKİM-ARASI BEKLEME (PLANT-BACK) KİLİDİ ve TARLAYA GİRİŞ YASAĞININ KÖKENİ (E1b): iki ölçülmüş boşluk tek göçte kapandı; göç `20260907_0072`, down_revision `20260906_0071`, TABAN `0d601ad` (`origin/develop`; ilk oturuş `185bc44` üzerindeydi ve #52 İŞLEM SIRASINDA indi — REBASE EDİLDİ, çakışma YOK; açılıştan önce açık PR YOKTU ve `git ls-remote --heads origin`de plant-back dalı YOKTU, yani göç numarası çakışmıyor ve #52 GÖÇ AÇMADI, `20260907_0072` hâlâ TEK baş); BOŞLUK 1 — PLANT-BACK ŞEMADA YOKTU: DEVİR notunun kendi cümlesi "herbisit ekim-arası 10-18 ay" ne PHI'dir ne giriş yasağıdır, ilacın toprakta kalan etkisi SONRAKİ SEZONUN EKİMİNİ yakar; ÖLÇÜLDÜ — `plant_protection_products`ta yalnız `preharvest_interval_days` ve `reentry_interval_days` var ve ikisi de AYNI sezona bakar; BOŞLUK 2 — GİRİŞ YASAĞININ KÖKENİ KAYITTA YOKTU: E1a (#50) değeri katalogdan çözüyordu ama kimin koyduğunu yazmıyordu ve `_giris_yasagi_coz`un KENDİ BAŞLIĞI bunu adıyla söylüyordu ("sütunu yok ve açmak göç olurdu; bu dilim göçsüzdür"); SÜTUN DEĞİL AYRI TABLO ve karar ÖLÇÜLDÜ, VARSAYILMADI: görev metninin ilk tasarımı plant-back'i katalog tablosuna İKİ SÜTUN olarak koyuyordu, ama o tablonun tekilliği `UNIQUE(company_id, product_id, crop)`tır (0063) ve plant-back'in ihtiyacı ÜRÜN+UYGULANDIĞI BİTKİ başına BİRDEN ÇOK satırdır (aynı herbisit ardından ekilecek ayçiçeği için 12 ay, mercimek için 4 ay bekletir) — ölçüm (CPython 3.12, SQLite): `(1,7,'buğday',...,365,'ayçiçeği')` GEÇTİ, `(1,7,'buğday',...,120,'mercimek')` `IntegrityError: UNIQUE constraint failed` ALDI, yani sütun tercihi ikinci satırı ŞEMA SEVİYESİNDE imkânsız kılıyordu ve firma ikinci bitkiyi ancak birinciyi SİLEREK girebilirdi; KARAR: ayrı tablo `plant_protection_plantbacks`, tekilliği `UNIQUE(company_id, product_id, crop, next_crop)`; 0063'ün UNIQUE'i DEĞİŞTİRİLMEDİ çünkü o kısıt PHI çözümünün belirsiz kalmamasını sağlıyor ve gevşetmek bu göçün ihtiyacı için bir GÜVENLİK kuralını bozmak olurdu; gerekçenin kendisi PG ikizinde ÇİVİLENDİ (`test_KATALOG_TABLOSU_ayni_seyi_YAPAMIYOR_gocun_gerekcesi` — kapı yeşil kaldığı sürece ayrı tablo bir tercih değil ZORUNLULUKTUR); GÖÇÜN DÖRT PARÇASI: (a) `plant_protection_plantbacks` — `crop` ('' = hangi bitkide atılırsa atılsın), `next_crop` ('' = ardından ne ekilirse ekilsin), `interval_days` NOT NULL (tablonun VAR OLMA SEBEBİ; süresi boş bir satır hiçbir ekimi kesmez — 0063'ün `preharvest_interval_days`i NOT NULL yapan gerekçenin aynısı), `CHECK 0..3650`, BİLEŞİK FK `(company_id, product_id) -> products(company_id, id)`, `UNIQUE(company_id, id)`, `ix_ppb_company_product`; iki BOŞ DİZE iki AYRI soru ve NULL DEĞİL boş dize seçilmesi 0063'ün gerekçesiyle AYNI (SQL'de UNIQUE NULL'ları farklı sayar, NULL'lu tekillik aynı ürüne sınırsız "her bitki" satırı bırakırdı); süre GÜN cinsindendir çünkü takvim ayı sabit uzunlukta değil ve mevcut iki kilit (`_bekleme_ihlalleri`, `_giris_ihlalleri`) GÜN topluyor, ay birimi aynı tarih aritmetiğinin İKİNCİ bir sürümünü doğururdu; (b) `field_activities.reentry_source VARCHAR(20)` + `catalogue_reentry_days INTEGER`, ikisi de NULL açılıyor ve NULL "bu satır köken çağından önce yazıldı" demek — 0063'ün `preharvest_source`/`catalogue_preharvest_days` çiftinin BİREBİR eşi ve AYNI SÖZLÜK (`CATALOGUE`|`OPERATOR`|`OPERATOR_OVERRIDE`), çünkü ikinci bir sözlük uydurmak denetçiye aynı olguyu iki dilde okuturdu; (c) `crop_seasons.plantback_warning` + `plantback_override_reason` (ikisi `Text`; uçtaki sınır kardeşleriyle aynı 255'te kaldı) — 0048'in kuralı ÜÇÜNCÜ kez: birincisi SİSTEMİN bulduğu, ikincisi KULLANICININ söylediği, aynı sütunda tutmak "warn" seviyesinde sistemin ürettiği metni kullanıcının gerekçesi gibi gösterirdi; (d) `companies.farm_plantback_policy VARCHAR(20) NOT NULL DEFAULT 'require_reason'` + `CHECK IN ('block','require_reason','warn')` — "allow" YOK (0048/0064 sınırı: plant-back ihlali bir sonraki ürünü yakar, kontrolü tamamen kapatabilen bir ayar sessiz bir güvenlik düğmesi olurdu) ve BU, SEVİYE KÜMESİNİ ŞEMAYA YAZAN İLK POLİTİKA SÜTUNUDUR: ÖLÇÜLDÜ, bugün `UPDATE companies SET farm_reentry_policy='kapali'` veritabanı tarafından KABUL EDİLİR ve tek kapı uçtaki `Literal`dır; ESKİ İKİ SÜTUNA CHECK EKLENMEDİ çünkü mevcut satırlarda ölçülmemiş bir değer varsa göç PATLARDI ve bu göçün konusu o değil; İKİ GERÇEK GÖÇ KUSURU YERELDE BULUNDU ve İKİSİ DE ADIYLA GÖÇÜN BAŞLIĞINA YAZILDI: (1) `downgrade`de koşulsuz `batch.drop_constraint("ck_companies_farm_plantback_policy")` `ValueError: No such constraint` verdi — sebep ÖLÇÜLDÜ (alembic 1.19.2 / SQLAlchemy 2.0.52 / CPython 3.12): SQLite yansıtması bu CHECK'i GÖRMÜYOR (aynı tablodaki `ck_companies_service_parts_mode`i GÖRÜYOR), görülmeyen kısıt yeniden kuruluma da TAŞINMIYOR, bu yüzden düşürme KOŞULLU yapıldı ve koşul iki dünyayı da kapsıyor — bir gün yansıtma kısıtı görmeye başlarsa düşürme KENDİLİĞİNDEN devreye girer ve 0071'in kusuru (yansıtılan CHECK düşürülmüş sütunu adıyla anıyor) geri gelmez; (2) sütun ve CHECK'i TEK `batch_alter_table`e koymak SQLite'ta ÇALIŞTI ama PostgreSQL'de CHECK'i SESSİZCE KURMADI (`\d companies` yalnız eski kısıtı gösterdi ve `UPDATE ... ='allow'` KABUL EDİLDİ), yani batch'in `create_check_constraint`i yeniden kurulum YAPILMAYAN diyalektte hiçbir DDL üretmiyor — SESSİZ olduğu için tehlikeli, göç yeşil biter ve kısıt yoktur; ayrım artık AÇIK yazılı (SQLite batch ile CREATE TABLE metnine, ötekiler `ALTER TABLE ... ADD CONSTRAINT` ile) ve ikinci kusuru PG ikizindeki `test_firma_politikasi_KAPALI_KUMEDE` YAKALADI; `up -> down -> up` turu SQLite'ta ve PostgreSQL 16.13'te KOŞTU, SQLite turu `test_goc_turu_up_down_up_SQLitede_KOSUYOR` olarak TESTE GİRDİ ve gerekçesi yazıldı: kaynağı grep'lemek YETMEZDİ çünkü `downgrade` tablo adını SABİTTEN (`PLANTBACK`) okuyor ve dizge araması onu göremezdi, dahası `drop_column`ların SQLite'ta GERÇEKTEN çalıştığını hiç ölçmezdi; DAVRANIŞ 1 — E1a KÖKENİ: `_giris_yasagi_coz` artık `(katalog, etkin, köken)` döndürüyor ve `_phi_coz`un KARARLARINI birebir yansıtıyor, ETKİN DEĞER DAVRANIŞI DEĞİŞMEDİ (operatör hâlâ kazanır, katalog yalnız boşu doldurur, boş hâlâ ihlal değil); ikisi AYNI ŞEYİ söylüyorsa `OPERATOR`, farklıysa `OPERATOR_OVERRIDE` — her uyuşmazlığı üstüne yazma saymak denetimde GERÇEK üstüne yazmaları görünmez kılardı; `create_activity` çifti PHI ile AYNI yerde, girdiler yazılmadan ÖNCE çözüp yazıyor; DAVRANIŞ 2 — PLANT-BACK KİLİDİ `POST /api/crop-seasons` ve `PUT`ta (faaliyet yolunda DEĞİL: plant-back'i ihlal eden şey ilaçlama değil ONDAN SONRAKİ EKİMDİR, faaliyet tarafına koymak henüz var olmayan bir sezonu yargılamak olurdu); parselin ÖNCEKİ sezonlarındaki faaliyetlerin GİRDİLERİNE bakılıyor, kural `(crop = o sezonun bitkisi ya da '')` ve `(next_crop = yeni bitki ya da '')` ile eşleşiyor, `earliest_allowed = _yerel_gun(performed_at) + interval_days`; İKİ AYRI SEÇİM KARIŞTIRILMIYOR — TEK bir (faaliyet, ürün) çifti için EN ÖZEL kural GÖLGELER (bitki özel+ardıl özel > bitki özel+ardıl '' > bitki ''+ardıl özel > bitki ''+ardıl ''), FARKLI faaliyet/ürünler arasında EN UZUN SÜRE KAZANIR (`_phi_coz` ve `_giris_yasagi_coz` ile AYNI kural); SINIR GÜNÜ İZİNLİDİR (`ekim == earliest_allowed` ihlal DEĞİL — süre "kaç gün BEKLENECEĞİ"dir ve `<=` yazmak beklemeyi sessizce BİR GÜN uzatırdı); `started_on` BOŞSA SUSULUR (bilinmeyeni ihlal saymak `_bekleme_ihlalleri` ile `_giris_ihlalleri`in ikisinin de reddettiği şeydir); bitki eşleştirmesi SQL'de DEĞİL Python'da (`_bitki_esit`) çünkü SQL'in `lower()`ı Türkçe katlamayı ÜRETMEZ ve katalog çözümünün üç yerdeki sözleşmesi TEK olmalı; `PUT`ta sezon KENDİ faaliyetlerini DIŞLIYOR (`haric_sezon_id`) ve gerekçesi ÖLÇÜLDÜ: sezonun kendi ilaçlaması ekim gününden SONRA yapılmıştır, yani `ekim < yapıldığı + süre` HER ZAMAN doğrudur ve sezon KENDİ KENDİNİ bloke ederdi; `PUT`un tetikleyicisi monokültürünkinden AYRI ve bu bilinçli — `started_on` değişimi monokültür kırılımını KIMILDATMAZ ama ekim günü öne çekilmiş bir sezon plant-back'i YENİDEN ihlal edebilir, tek bayrağa bağlamak o değişikliği SESSİZ geçirirdi; gün karşılaştırması `_gun_esit` ile BİÇİMDEN BAĞIMSIZ çünkü SQLite satırı dizge, PostgreSQL satırı `date` verir ve düz `!=` iki diyalekti AYIRIRDI; RET GÖVDESİ SÖZLÜKTÜR (`BIRIM_COZULEMEDI` kalıbı): `{sebep: PLANTBACK_SURESI_DOLMADI, message, blocking[ürün/faaliyet/tarih]}` — çağıranın yapacağı şey HANGİ ilacın ve HANGİ tarihin kestiğine göre değişir; DAVRANIŞ 3 — `GET /api/field-safety` ÜÇÜNCÜ LİSTE ALDI (`plantback_blocks`) ve yuva UYDURULMADI, VARDI: `harvest_blocks` hasadı planlayanı, `reentry_blocks` tarlaya girecek kişiyi, `plantback_blocks` ekim planını yapanı ilgilendirir — hasat ya da giriş serbestken ekim yasak olabilir; liste ADAY BİTKİ SORMUYOR ve bu ucun SÖZLEŞMESİDİR (rapor "yürürlükteki kısıtlar"dır, "şunu eksem olur mu" değil), her satır kendi `next_crop`unu TAŞIYOR ve aday bitki başına tekil cevap `POST /api/crop-seasons`un işidir; rapor kipinde gölgeleme YALNIZ `crop` ekseninde çalışıyor çünkü farklı `next_crop`lar FARKLI SORULARIN cevabıdır ve birini ötekinin altında gizlemek raporu YANILTIRDI; DAVRANIŞ 4 — KATALOG CRUD `GET/POST /api/plant-protection-plantbacks` + `GET/PUT /{plantback_id}`, önek `_FARM_PATH_PREFIXES`e AYRICA yazıldı ve gerekliliği ÖLÇÜLDÜ: "...-plantbacks" mevcut "...-products" önekiyle EŞLEŞMİYOR ('l'/'r' harfinde ayrılıyorlar), yani satır olmasaydı dördü de genel `read` iznine düşerdi ve bekleme sürelerini OKUMA yetkisi olan herkes DEĞİŞTİREBİLİRDİ (0063'ün katalog için yazdığı tuzağın aynısı); YENİ İZİN AİLESİ AÇILMADI; DEPO HİÇBİR SÜRE İDDİA ETMEZ: göç TEK SATIR VERİ YAZMAZ, başlangıç listesi ve ürün başına varsayılan YOK, kodda yasal sabit YOK (`test_kodda_YASAL_PLANTBACK_SABITI_YOK` çiviliyor); STOK, OUTBOX ve YASAL SABİT KAPSAM DIŞI; TESTLER: `backend/tests/test_e1b_plantback.py` (9 test — 8 statik kapı + gerçek şemalı 15 senaryoluk davranış smoke'u) ve PG ikizi `backend/test_e1b_plantback_postgresql.py` (10 test) GERÇEK PostgreSQL 16.13'te KOŞULDU, SIFIR ATLAMA; PG koşumunda HEM `DATABASE_URL` HEM `APP_TEST_DATABASE_URL` verildi artı `REQUIRE_PG=1` (yalnız birini vermek uygulamayı SESSİZCE SQLite'a düşürüyor — #47'nin kaydında ölçülmüş tuzak) ve sürücü `postgresql+psycopg`; PG ikizinin `acilis_sifresi` fikstürü D2'den DEVRALINDI ve İKİ UÇTAN da çalışıyor (testten ÖNCE de SONRA da `admin123` + `must_change_password`), yani hangi sırada koşarsa koşsun kendi girişini garanti eder ve veritabanını BULDUĞU GİBİ bırakır; PG İKİZİNİN ÖLÇTÜKLERİ (hepsi SQLite'ta GÖRÜNMEZ): bileşik FK'nın gerçekten ısırması (SQLite'ta `PRAGMA foreign_keys` KAPALI doğar, çapraz kiracı kural orada SESSİZCE yazılırdı), `UNIQUE(company_id,product_id,crop,next_crop)`, göçün TASARIM GEREKÇESİ, `interval_days` aralığı, `farm_plantback_policy` seviye kümesi ve `:haric IS NULL` PARAMETRE TİPİ — `_PLANTBACK_SORGU` `POST /api/crop-seasons` yolunda HER ZAMAN `haric=None` ile çağrılıyor, tip düşerse PostgreSQL `42P08 AmbiguousParameter` verir ve o uç 500 döner, SQLite ise NULL'u yutar (`_GIRIS_SORGU`nun `pid`inde ölçülmüş AYNI tuzak, ama burada kilidin İLK çağrısı bu yoldan geçtiği için kusur ÜRETİMDE HER sezon yazımını düşürürdü); ölçüm İKİ YÖNLÜ: gerçek sabit GEÇİYOR, tipi düşürülmüş kopyası HATA ALIYOR; MUTASYONLAR (altısı da tek tek uygulandı, adı geçen test KIRMIZI oldu, geri alındı ve dosya BİREBİR eski hâline döndürüldü): en uzun yerine EN KISA kazanır -> smoke 7; `next_crop` YOK SAYILIYOR -> smoke 4; sınır gününde bir gün kayma (`<` -> `<=`) -> smoke 5; politika `block` WARN gibi davranıyor -> smoke 9; köken YAZILMIYOR -> smoke 1; çapraz kiracı okuma -> `test_plantback_sorgusu_parsel_kapsamli_ve_kiraci_bagli` AMA DAVRANIŞ SMOKE'U DEĞİL STATİK KAPI YAKALADI ve bu ADIYLA yazılıyor: B firmasının kendi faaliyeti olmadığı için sızıntı DAVRANIŞTA görünmüyordu, kiracı yüklemini kaldıran değişikliği yakalayan şey kaynak kapısı ile `test_tenant_scoping_guard`ın alias grafiğidir; MUTASYON 1 İLK TURDA HAYATTA KALDI ve sebebi bir TEST KUSURUYDU, kod kusuru DEĞİL: "en uzun kazanır" iddiası yalnız TEK bir kuralın ısırdığı bir senaryoda ölçülüyordu, yani sıralama sonucu DEĞİŞTİRMİYORDU; smoke'a İKİ kuralın AYNI ANDA ısırdığı bir adım (60 ve 30 gün) eklendi ve sıra ADIYLA çivilendi (`engeller[0]['interval_days'] == 60`), mutasyon ondan sonra kırmızı oldu; İĞNELER (hepsi YENİDEN ÖLÇÜLDÜ, ARİTMETİKLE TÜRETİLMEDİ — her biri kapının kendisine ölçtürüldü): göç başı `20260906_0071` -> `20260907_0072` (3 yer), `TENANT_TABLES` 109 -> 110, dışa aktarım tablo sayısı 109 -> 110 (3 yer), `farm.py` dinamik `text()` 15 -> 17 ve parmak izi `2f0ba1f9` -> `24791db0`, küresel dinamik SQL toplamı 258 -> 260, `tenancy.py` parmak izi `e81ca586` -> `45d7b295` (çağrı 4'te SABİT), rota sözleşmesi 360/278 -> 364/280 ve parmak izi `8f3eb86b` -> `a49f58e6` (İKİ yol, DÖRT işlem), GET envanteri 172 -> 174 ve parmak izi `bd1b5dd1` -> `39395cc1` (drift raporu ÖLÇÜLDÜ: `missing`/`stale`/`changed` ÜÇÜ DE BOŞ, artış YALNIZ ekleme), `EXPECTED_AUTHENTICATED` 350 -> 354, `EXPECTED_UNDENIABLE` 100 -> 102, `FARM_HERD_VIEW_OPERATIONS` 33 -> 35, `EXPECTED_READ` 93'te SABİT, PG popülasyonu 104 -> 105 (3 yer: test adı, iddia ve `ci.yml`in `BEKLENEN_PG_DOSYA_SAYISI`'ı); DİNAMİK `text()` 15 -> 17'NİN GEREKÇESİ: iki yeni çağrının İKİSİ DE `list_plantbacks` içindedir ve 0063'te zaten gözden geçirilmiş `list_ppp`in BİREBİR İKİZİDİR, interpolasyon YALNIZ `kosul` değişkeninden gelir ve o KAPALI bir kümeden kurulur (`product_id`, `status`), istekten gelen DEĞERLER her iki durumda da BAĞLI PARAMETREDİR; bu turda eklenen diğer SQL'lerin HEPSİ SABİT METİNDİR (`_PLANTBACK_SORGU` modül sabiti — dört birleştirmenin DÖRDÜ de kiracı İÇİNDE, `create_plantback`/`update_plantback`, `field_safety`in parsel listesi); `EXPECTED_READ`İN 93'TE SABİT KALMASI ÖNEK KARARININ TANIĞIDIR (dört uç önek listesine yazılmasaydı sayaç 93 -> 95 olurdu) ve `EXPECTED_UNDENIABLE` 100 -> 102 çünkü İKİ OKUMA ucu `farm.view` istiyor ve `farm.view` tanımlı rollerin hepsinde var — 0063'ün katalog okuma uçlarında ölçülen hareketin AYNISI, YAZMA uçları (`farm.manage`) bu kümede DEĞİL; `plant_protection_plantbacks`ın `TENANT_TABLES`a girmesi yalnız bir sayaç değil ÖLÇÜLMÜŞ bir gereklilik: girmeseydi kiracı kapısı `_PLANTBACK_SORGU`nun `products` birleştirmesini alias grafiğinden ULAŞILAMAZ sayar ve sorguyu KAPSAMSIZ diye reddederdi; SÖZLEŞME ZİNCİRİ: `export_openapi.py -o` -> `npm run types:gen` -> `capture_frontend_fixtures.py`; delta YALNIZ `frontend/src/api/types.gen.ts` (+245), `pos-api.json` DEĞİŞMEDİ, `backend/openapi.json` `.gitignore`da; `npx tsc -b --force` çıkış 0 (`npm ci` gerekti: bu ortamda `node_modules` YOKTU); ÖLÇÜLMEDİ, ADIYLA: (1) PLANT-BACK MESAJININ ÖN YÜZÜ — yalnız `types.gen.ts` türedi, ekranda gösterim yazılmadı; (2) KATALOG ÇAĞINDAN ÖNCEKİ FAALİYETLER — geriye doldurma YOK ve bu 0063'ün duruşunun tekrarıdır, geçmiş faaliyetlere sonradan bir plant-back takmak bugüne kadar sorunsuz kaydedilmiş ekimleri kullanıcının yapmadığı bir değişiklik yüzünden ihlale düşürürdü, kilit YALNIZ yeni yazılan/değiştirilen sezonda işliyor; (3) PLANT-BACK'İN DOSYADAN İÇE AKTARILMASI — 0065'in `import_ppp` ucunun karşılığı bu tabloda YOK, katalog tek tek formdan doldurulur; (4) AY BİRİMİ — devir notu "10-18 ay" diyor, şema GÜN tutuyor ve dönüşümü kullanıcı yapar. KAYIT NUMARASI 0095 -> 0096 ÇÜNKÜ #52 BİRLEŞİRKEN 0095'İ ALDI: numara elle seçilmedi, `python scripts/durum.py --sonraki 53` verdi (dosya geçici olarak kaldırılıp yeniden ölçüldü ki sayı KENDİ varlığından etkilenmesin) ve satır içeriği BİREBİR taşındı; `python scripts/durum.py --kapi origin/develop --pr 53` ÇIKIŞ 0 (base 95 girdi, ölçülen ağaç 96, BAYAT SIRA YOK, YİNELENEN SIRA YOK). REBASE SONRASI BÜTÜN İĞNELER YENİDEN ÖLÇÜLDÜ ve HİÇBİRİ KIMILDAMADI (#52 göç AÇMADI ve bu PR'ın dokunduğu hiçbir yüzeye değmedi; 171 kapı iddiası + bu PR'ın 10'u yeşil). CI KIRMIZI TURU ve İKİ KÖK SEBEP, ADIYLA: (1) İLK İTİŞ `durum-kaydi` kapısını kırdı çünkü kayıt İKİNCİ commit'te geldi — kapı doğru çalıştı, iddia yanlış değildi. (2) ASIL KUSUR: `backend-postgresql (0)` ve `(2)` ile üç SQLite kapısı kırmızı oldu. PostgreSQL tarafının kökü ÖLÇÜLDÜ ve göçün başlığına yazıldı: `app/tenancy.py` `companies`i `Table()` olarak bildiriyor ve uygulamanın AÇILIŞ DDL'i tabloyu alembic'ten ÖNCE kurabiliyor; sütun o bildirime de eklendiği için göç 0072 sütunu VAR bulup TEK `if` dalını ATLADI ve `ck_companies_farm_plantback_policy` HİÇ KURULMADI — ölçüm: taze şema + açılış + `upgrade head` sonrası `UPDATE companies SET farm_plantback_policy='allow'` KABUL EDİLDİ. Bu, aynı göçte zaten kayda geçmiş ikinci kusurun (batch'in yeniden kurulum yapmayan diyalektte sessizce DDL üretmemesi) İKİZİDİR: farklı yol, AYNI sonuç — göç yeşil biter, kısıt yoktur. Sütun ve CHECK artık AYRI AYRI soruluyor. AYNI KUSUR SINIFI ÖTEKİ ÜÇ NESNE İÇİN DE ARANDI ve ÜRETİLEMEZ olduğu ÖLÇÜLDÜ: açılış DDL'i (`tenancy.py`, `core_schema.py`, `auth.py`, `inventory.py`, `finance_engine.py`, `workflow.py`) YALNIZ ÜÇ tablo bildiriyor — `companies`, `branches`, `user_company_memberships`; `plant_protection_plantbacks`, `crop_seasons` ve `field_activities` HİÇBİRİNDE YOK, yani onların TEK yaratıcısı göçtür. Bu olgu `test_ACILIS_DDLi_GOCUN_ONUNE_GECMIYOR` ile ÇİVİLENDİ ve kapı İKİ YÖNDE mutasyona uğratıldı (göç tek koşula döndürüldü -> KIRMIZI; `crop_seasons` açılış DDL'ine eklendi -> KIRMIZI). ÜÇ SQLite KAPISI da BİLİNÇLİ güncellendi: `test_farm_pesticide_safety.py` `GET /api/field-safety`in ANAHTAR KÜMESİNİ donduruyor ve `plantback_blocks` oraya AÇIKÇA yazıldı (kapı işini YAPTI: üçüncü liste sessizce eklenemezdi); `test_e1a_rei_katalog.py` `_giris_yasagi_coz`un skaler beklentilerini ÜÇLÜYE çevirdi — ETKİN DEĞER terimlerinin HEPSİ E1a'daki sayının BİREBİR aynısıdır, eklenen şey KÖKENDİR, artı İKİ yeni durum (operatör katalogla AYNI şeyi söylüyorsa `OPERATOR`, üstüne yazma DEĞİL; katalog susarken operatör yazdıysa `(None, n, OPERATOR)`); `test_core_query_inventory.py` `get_company_settings` projeksiyonu bir sütun büyüdüğü için karma `5525fded` -> `2a91c7d8` ve envanter parmak izi `36bbb1cf` -> `16f1e954` olarak YENİDEN ÖLÇÜLDÜ. SÜREÇ KUSURU, ADIYLA: ilk itişten ÖNCE yalnız KIMILDAYACAĞINI TAHMİN ETTİĞİM kapılar koşturulmuştu, izole süit KOŞTURULMAMIŞTI; dördü de o süitle bulunabilirdi ve ilk itiş kırmızı gitmemeliydi.
