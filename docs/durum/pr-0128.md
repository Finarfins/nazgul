#128 — CS3 ÇEK/SENET PORTFÖYÜ ARAYÜZÜ (yalnız frontend; backend/göç 0 dosya, CS2'ye bağımlılık yok): `/cek-senet-portfoyu` Finans grubunda, `NAV_LABELS.cheques`, izin `payments` (backend `/api/cek-senetler` açık önek kuralıyla aynı; `/nakit-yonetimi` `finance`ta KALDI — satis çek portföyünü açar, hazineyi açmaz; depo/rapor rota korumasında durur, doğrudan 403'te sayfa uyarır); `pages/cek-senet/`: portföy tablosu sunucu tarafı sayfalama (`limit/offset`, vade artan) + süzgeçler tür/yön/durum çipleri/cari (müşteri+tedarikçi Autocomplete — ortak bir cari bileşeni YOK, ölçüldü; Payments.tsx deseni)/vade aralığı/`q`; TOPLAM ÇUBUĞU sayı = sunucu `total`, tutar = istemcide YALNIZ ekrandaki sayfa (etiketli; tek sayfada "süzgeç toplamı") — yeni uç uydurulmadı; VADE TAKVİMİ açık durum başına bir `GET ?portfoy_durumu=..&vade_to=bugün+30&limit=200` (sunucu tek durum süzgeci alır), gecikmiş (vurgulu)/bugün/1–7/8–30 ayrık kovaları, kova tıklaması tabloyu süzer; PENCERELER Yeni Evrak, Bordro (≤200, hep-ya-hiç; `N. satır`/`Satır N:` hatasında satır işaretlenir), Tahsile Ver, Bankadan Tahsil (`/api/payments/accounts` — `payments` izni; `/api/finance/accounts` `finance` olduğu için KULLANILMADI — yalnız aktif cash|bank, POS yok), Ciro Et (yalnız alınan; verilen evrakta hiç çizilmez), Karşılıksız, İade, Portföye Geri Al — her biri TEK `durum-degistir`, yük `_IZINLI_ALANLAR` ile birebir, boş not gönderilmez; 7/36 matrisi istemcide aynalı, 409/422 sunucu mesajı pencerede; `satis` `purchases` taşımadığı için `/api/suppliers` ÇAĞRILMAZ, tedarikçi seçicileri çizilmez, Ciro devre dışı; `hesap_no` olduğu gibi; Nakit Yönetimi "Çek / Senet" sekmesi bağlantı oldu, `prompt()` + eski `/finance/instruments` listesi/formu + müşteri/tedarikçi yüklemesi kaldırıldı (özet kartları ve e2e işareti "Alınan Çek/Senet" duruyor); `api.ts` `errorDetail`: `{code,message}` gövdesinin mesajı (önceden 409'da axios ham metni sızıyordu) + bordro `satirlar` satır numarası; PİNLER (#125 PP3 sonrası taban `4c3cda8`, ölçüldü): menü `ALL_NAV_ITEMS`/`BOOKMARKED_MENU_URLS`/AppShell href 57 -> 58, grup 10 ve rol üst düzey sayıları SABİT, rota envanteri 78 -> 79 (`kapi`, işaret "Vade Takvimi", muaf DEĞİL; `rota-kapsam-sozlesmesi` pini güncellendi); TESTLER: `cekSenet.test.ts` (36 çift/7 izinli, 6 durum × 2 yön eylem tablosu) + `CekSenetPortfoyu.test.tsx` (süzgeç parametreleri, offset, çip renkleri, 6 durum menüsü, her pencerenin tam yükü, 409/422, bordro satır işareti, boş durum, 403, maskeli hesap no, satis), Finance +3, api +3; lint 0 hata, build yeşil, vitest 101 dosya / 754 test yeşil