PR #14 (head f32c44aac37f1fcb3843bec9724732f8e4f0e308) — MUAF 8 -> 5 VE RENDER KONTRATI: `/tedarikciler/:id`, `/urunler/:id` ve `/depolar/:id` muafiyetten `spec`e taşındı ve pozitif render kontratına girdi; envanter artık `olcum:'positive'` + sayfaya özgü `isaret` taşıyor, raportör R5 bu üç rota için ziyaret kanıtını render kanıtıyla BİRLİKTE istiyor; muaf kümesi 8 -> 5 indirildi, donmuş G5 listesi güncellendi, yeni G12 parametreli/yakalayıcı bir rotanın `kapi` olmasını engelliyor, G13 render kontratının tam kümesini ve boş olmayan işaretleri çiviliyor, R6 kapı testlerinin ziyaret kanıtını ölçüyor; yeni `rota-muaf-kapsami.spec.ts` üç rotayı gerçek API fikstürüyle, gerçek pathname, `rota-govdesi` kökü, sayfaya özgü işaret, render attestation ve R5 visit evidence ile açıyor, yeni `rota-render-kaniti.ts` render kanıtını `testInfo` eki olarak bırakıyor, helper'lara `createSupplier` ve `createWarehouse` eklendi; GERİLEME: depo detay testi `createWarehouse()` ile shared seeded tenant'a üçüncü depoyu yazıyordu, `serve.py` her Playwright çağrısında tek SQLite açıyor, `workers=1` ve `fullyParallel=false` olduğundan B-before `touch-targets.spec.ts:2442`'de `Expected: 2 / Received: 3` ile kırıldı; BEFORE FIX: A `touch-targets` alone 47 passed / 0 failed / exit 0, B route spec + touch-targets 40 passed / 1 failed / 9 did not run / exit 1, C aynı B üçüncü warehouse satırı olmadan 50 passed / 0 failed / exit 0; FIX: fixture'ı zayıflatmak yerine `createCompany()` + `provisionUser()` ile taze izole tenant açıldı, supplier/product/warehouse `X-Company-ID` ile bu tenant'a yazıldı, browser `yonetici` kullanıcısıyla bu tenant'a bağlandı, bağ uçtan uca ölçüldü: shell'deki aktif firma seçicisi izole firma adını gösteriyor ve detail GET aynı tenant'taki kaydı bulup işareti çiziyor; `touch-targets.spec.ts`'in `toHaveCount(2)` iddiasına dokunulmadı; AFTER FIX: A 47 passed / 0 failed / exit 0, B 50 passed / 0 failed / exit 0 ve `Expected: 2 / Received: 3` görülmedi; üç rota tek başına 3 passed / 0 failed / exit 0; MUTASYONLAR: M1 parameterized kapi RED, M2 splat kapi RED, M3 render-contract entry removed RED, M4 unexpected render-contract member RED, M5 empty marker RED, M6 muaf freeze drift RED, M7 render attestation removed RED via R5 ve M7 kapı dosyasını içeren geçerli full-suite yapılandırmasında ölçüldü, mutasyon ağaçları restore edildi; FULL PLAYWRIGHT: 146 passed / 0 failed / 0 skipped / 0 flaky / exit 0, reporter 0 ihlal, envanter 70 rota, kapi 32, spec 33, muaf 5, koşan spec dosyası 21; STATIC: `npx vitest run` 564 passed / 0 failed / exit 0, `npx vitest run src/rota-kapsam-sozlesmesi.test.ts` 13 passed / exit 0, `npx tsc -b` exit 0, `npm run typecheck:e2e` exit 0, `npm run lint` 0 error / exit 0, `npm run build` exit 0; REMAINING MUAF: `/depo-transferleri/:id`, `/hayvancilik/hayvanlar/:id`, `/saha/:id`, `/stok-sayimlari/:id`, `/yedekler`; NOT MEASURED: signed/test-run-linked receipt zinciriyle bağlanmadı, CI'ın bu ağacı gerçekten çağırdığı yerelde ölçülemez, M7 dışında mutasyon ağaçlarında tam Playwright süiti ayrıca koşulmadı ve HTTP middleware `X-Company-ID` davranışı beyaz kutu olarak ayrıca doğrulanmadı — tenant binding UI + detail GET ile ölçüldü.
