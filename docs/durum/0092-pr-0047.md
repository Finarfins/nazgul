#47 — MÜSTAHSİL MAKBUZU + STOPAJ (D1): çiftçiden alımın kağıdı ve iki kesintisi (gelir vergisi stopajı, sosyal güvenlik); göç `20260905_0070`, down_revision `20260904_0069`, TABAN `9e02fb8` (`origin/develop`); DEFTERE HİÇBİR ŞEY YAZILMIYOR — stok, ödeme ve finans defterleri birebir aynı, `test_kaynak_defterin_ADINI_bile_gecirmiyor` kaynak metni üzerinden çiviliyor; İKİ TABLO: `producer_receipts` (15 sütun) + `producer_receipt_items` (19 sütun), ikisi de `TENANT_TABLES`ta; `uq_suppliers_company_id` BU GÖÇTE KURULDU ve gerekliliği ÖLÇÜLDÜ, VARSAYILMADI: temiz şemada `inspect(...).get_unique_constraints` → `suppliers: []` ama `purchases: [uq_purchases_company_id]` ve `products: [uq_products_company_id]`, yani bileşik yabancı anahtarın hedefi EKSİKTİ — 0058'in `batch_alter_table` kalıbıyla eklendi ve `downgrade`de DÜŞÜRÜLMÜYOR (0066/0067'nin `uq_products_company_id` gerekçesinin aynısı: fazladan bir UNIQUE zararsız, eksik bir UNIQUE kiracı sınırını deler); BEŞ bileşik yabancı anahtar, hepsi `(company_id, id)` hedefli; KISMİ benzersiz indeks `ux_producer_receipts_company_receipt_no` (`WHERE receipt_no IS NOT NULL`) — düz bir UNIQUE de çalışırdı (iki diyalekt de NULL'ları çakıştırmaz) ama kısmi indeks NİYETİ kayda geçirir ve o davranışa BAĞIMLI OLMAYI bırakır; `ck_producer_receipts_no_follows_status` numarayı DURUMLA birlikte hareket ettiriyor (taslakta numara olmaz, kesilmişte numarasızlık olmaz), yani `issue` yolundaki bir hata numarasız `issued` satır BIRAKAMAZ; KDV SÜTUNU YOK ve bu bir eksiklik DEĞİL: satıcı çiftçi KDV mükellefi değildir ve `transactions.py`in FİYATA DAHİL alım formülü buraya KOPYALANMADI — kopyalansaydı olmayan bir vergi düşülür ve `net_payable` sessizce eksik çıkardı; alan eklenip sıfır bırakılsaydı bir gün doldurulması ŞEMACA mümkün olurdu; ORANLAR KULLANICIDAN gelir ve kodda YASAL SABİT YOKTUR (`withholding_rate`, `social_security_rate` satır başına, şemada varsayılansız, şema yalnız 0..100 ARALIĞINI zorlar) — gerekçe: oran tebliğe ve ürün cinsine göre değişir, koda gömülen bir sayı tebliğ değiştiği gün hangi satırın hangi oranla yazıldığını OKUNAMAZ yapardı (0066'nın `entered_factor` duruşunun aynısı); `test_kaynakta_yasal_oran_sabiti_YOK` bunu AST üzerinden arıyor ve `_ORAN_TAVANI`yı MUAF tutuyor çünkü o bir ORAN değil ARALIK SINIRIDIR; ARİTMETİK `app/mustahsil.py`de SAF, SQL'siz ve oturumsuz (`line_gross = taban × fiyat`, kesintiler YUVARLANMIŞ brütten, `line_net` ÇIKARMAYLA) — yuvarlama SATIRDA BİR KEZ (ROUND_HALF_UP, `MONEY_QUANTUM`), başlık satırların toplamıdır ve YENİDEN YUVARLANMAZ; `line_net` çarpımla değil çıkarmayla bulunuyor ki `net + stopaj + sgk == brüt` eşitliği KORUNSUN (makbuzun okuyucusu bunu gözle denetler); UÇLAR `/api/producer-receipts` (POST, GET liste, GET tekil, `issue`, `cancel`) ve BEŞİ DE `purchases` iznine bağlı — GET'LER DAHİL, çünkü makbuz çiftçiye ödenen birim fiyatı ve stopajı taşır, yani TEDARİKÇİ MALİYETİDİR; kural `auth.py`de temel `read` kuralının ÜSTÜNE yazıldı ve bunun TANIĞI `EXPECTED_READ`in 93'te SABİT kalmasıdır (altında kalsaydı 93 → 95 olur ve alım fiyatları her role açılırdı); SİLME UCU YOK (kesilmiş makbuz vergi belgesidir; `cancel` onu `cancelled` yapar, SATIRLAR ve NUMARA DURUR); `issue` TEKRARI 409 verir ve İKİNCİ NUMARA ÜRETMEZ — sessizce geçseydi bir makbuz iki numara taşırdı, ilk numarayı geri veren bir `idempotent` sürüm ise seriden numara harcayıp hiçbir belgeye yazmazdı (seride açıklanamayan delik); numara `document_sequences`ten, önek `MM`, `DOCUMENT_TABLES`a ve `DOCUMENT_NUMBER_COLUMNS`a birer giriş (`receipt_no`, çünkü sütun `document_no` DEĞİL — bildirilmeseydi `next_document_no`nun tekrar denetimi OLMAYAN bir sütuna bakardı); KANTAR FİŞİ BAĞI isteğe bağlı ve `farm._turetilmis_net` İTHAL EDİLİYOR, KOPYALANMIYOR (ikinci bir kopya, bileşim kuralı toplamsal/sıralı bir gün düzeltildiğinde makbuzu fişten AYIRIRDI); kullanıcı öneriyi ezerse İKİSİ DE saklanıyor (`ticket_net_snapshot`) çünkü `fiş ne diyordu` ile `kağıda ne yazıldı` AYRI iki olgudur; STOPAJ YÜKÜMLÜLÜĞÜ DEFTERE YAZILAMADI ve SEBEBİ ÖLÇÜLDÜ — tasarım `issue` anında yeni bir `withholding` kategorisiyle `finance_transactions`a yazmayı öngörüyordu, görev metnindeki DURMA KOŞULU tam olarak gerçekleşti: `finance_transactions.account_id` NOT NULL, `ACCOUNT_TYPES` `{cash, bank, pos}` (hiçbiri vergi dairesine olan BORCU temsil etmez) ve satır okuma yolunda gerçek bir hesaba İÇ BİRLEŞTİRİLİP o hesabın hareket dökümünde ve gösterge panelinde GÖRÜNÜYOR, yani yükümlülüğü bir kasa/banka hesabına yazmak bakiyeyi GERÇEKLEŞMEMİŞ bir para hareketiyle kirletirdi; yeni bir hesap TÜRÜ uydurmak bu PR'ın yetkisi DIŞINDA (küme finans modülünün her doğrulama kapısından geçiyor) — seçenekler PR gövdesinde üç madde olarak yazıldı ve karar SAHİBİNİN; `test_stopaj_defteri_YAZILMADI_ve_sebebi_olculdu` YOKLUĞU çiviliyor VE engelin hâlâ gerçek olduğunu ölçüyor (`account_id` nullable olursa KIRMIZI olur ve karar yeniden verilir); YERELDE BULUNUP DÜZELTİLEN GERÇEK KUSUR: tarih süzgeci ham dizgi olarak `timestamptz` ile karşılaştırılıyordu ve ÖLÇÜLDÜ (PostgreSQL 16.13) çözülemeyen değerde sürücü `InvalidDatetimeFormat` atıp uç 500 döndürüyordu, SQLite'ta ise aynı sorgu SESSİZCE boş liste veriyordu — yani kullanıcı hatası üretim diyalektinde SUNUCU HATASI gibi görünüyor, geliştirmede hiç görünmüyordu; `_tarih_suzgeci` eklendi (ISO-8601, `Z` kabul, saat dilimsiz değer UTC sayılır) ve artık 422 `TARIH_COZULEMEDI`, her iki diyalektte de teste yazıldı; OKUMA YOLUNDA ÖLÇEK NORMALİZASYONU: `_metin` değerleri sütun ölçeğine (`0.01` / `0.0001`) quantize ediyor çünkü ÖLÇÜLDÜ — PostgreSQL `NUMERIC(18,2)`yi `12500.00` olarak, SQLite ölçeği TAŞIMADIĞI için `12500` olarak veriyordu ve ikizler AYRI iddialar yazmak zorunda kalırdı; TESTLER: `backend/tests/test_mustahsil_makbuzu.py` (40 test; aritmetik tablosu 8 satır — 0 oran, %100 oran, iki oran toplamı 100, 0.005 yuvarlama ucu iki kez, sıfır fiyat — artı NaN/Inf/-Inf/sNaN her alanda, statik kapılar ve ALT SÜREÇTE gerçek şemalı davranış smoke'u) ve PG ikizi `backend/test_mustahsil_makbuzu_postgresql.py` (16 test) GERÇEK PostgreSQL 16.13'te KOŞULDU, ATLANMADI; davranış smoke'u HER İKİ diyalektte koşuldu; senaryo 9 (fiş önerisi/ezme) KOŞULSUZ kuruluyor — `varsa ölç` biçiminde bir kontrol tablo boş olduğu gün senaryoyu SESSİZCE atlardı; MUTASYONLAR (dokuzu da tek tek uygulandı, adı geçen test KIRMIZI oldu, geri alındı): yuvarlama satırdan başlığa taşındı → `test_baslik_toplami_satirlarla_BIREBIR` + iki aritmetik satırı; brüt KDV-dahil sayıldı → dört aritmetik satırı + smoke; oran modül sabitinden alındı → dört satır + `test_kaynakta_yasal_oran_sabiti_YOK`; tedarikçi FK'sı bileşikten TEKİLE indirildi → PG ikizinde YALNIZ `test_baska_firmanin_tedarikcisi_VERITABANINDA_reddedilir` (öteki 15 YEŞİL kaldı, yani kapı DAR); `issue`ın `draft` kapısı kaldırıldı → smoke senaryo 5; `create` `status='issued'` yazdı → senaryo 1; router'a `stock_movements` sorgusu eklendi → `test_kaynak_defterin_ADINI_bile_gecirmiyor`; `ticket_net_snapshot` ezmede None yazıldı → senaryo 9; router `finance_transactions`a değdi → `test_stopaj_defteri_YAZILMADI_ve_sebebi_olculdu`; ÖLÇÜLDÜ AMA KIRMIZI OLMAYAN ve dosyaya ADIYLA yazıldı: `makbuz_topla` toplamlarını `money()` ile SARMAK hiçbir testi kırmaz ve kırmaması DOĞRUDUR (girdiler zaten 0.01'in katı, ikinci `quantize` kimliktir) — tehlike ikinci yuvarlamada DEĞİL, yuvarlamanın SATIRDAN KALKMASINDADIR, bu yüzden 1. mutasyon İKİ parçalıdır; İĞNELER (hepsi YENİDEN ÖLÇÜLDÜ, devralınmadı): göç başı `20260904_0069` → `20260905_0070` (3 yer), `TENANT_TABLES` 104 → 106 (keşifteki 64 rakamı BAYATTI, ölçüm 104 verdi), dışa aktarım tablo sayısı 104 → 106 ve topolojik katman 8 → 9 (zinciri UZATAN şey `producer_receipt_items`tir: `producer_receipts`e, o da 0069'un `field_harvest_tickets`ine bağlı), GET envanteri 168 → 170 ve parmak izi `e0436744` → `d64b5cb3` (drift raporu ÖLÇÜLDÜ: `missing`/`stale`/`changed` ÜÇÜ DE BOŞ, artış YALNIZ ekleme), rota sözleşmesi 350/270 → 355/274 ve parmak izi `43d9e7d8` → `647736cf` (YOL sayısı 273 değil 274: ilk yol İKİ işlem taşıyor, ilk yazımda yanlış sayılmış ve kapı düzeltti), `EXPECTED_AUTHENTICATED` 340 → 345 ile `EXPECTED_READ` 93 ve `EXPECTED_UNDENIABLE` 100 SABİT, dinamik SQL toplamı 254 → 257 artı `routers/mustahsil.py` girdisi (ÜÇ çağrı; kiracı yüklemi ÜÇÜNDE DE SABİT METİNDE, interpolasyon yalnız modül sabiti sütun listeleri ve KAPALI süzgeç parçaları, değerler hep BAĞLI PARAMETRE), `document_engine.py` parmak izi `1861cf82` → `9da37689` (SQL METNİNE DOKUNULMADI: eklenen şey iki KAPALI HARİTAYA birer giriş, dinamik çağrı 9'da SABİT), alt süreç SQL sayacı 107/171 → 108/172, PG popülasyonu 102 → 103 (3 yer; SAYIM ÖLÇÜLDÜ: `ls backend/test_*postgresql*.py | wc -l` → 101, yani 101 + 2 özel = 103); TABAN DOĞRULAMASI: `9e02fb8`de ayrı bir worktree kurulup aynı 13 kapı koşuldu ve HEPSİ YEŞİL çıktı (96 test), yani kırmızıların TAMAMI bu dalın eseriydi — hiçbiri devralınmış bir kırmızı değil; SÖZLEŞME ZİNCİRİ: `export_openapi.py` → `npm run types:gen` → `capture_frontend_fixtures.py`, sonuç YALNIZ `frontend/src/api/types.gen.ts` (+307), `pos-api.json` DEĞİŞMEDİ; `backend/openapi.json` `.gitignore:83` ile İZLENMİYOR; `npx tsc -b --force` çıkış 0; `deploy/ci-gerekli-baglam-kapisi.py` → 17 bağlam manifest ile TAM KÜME EŞİTLİĞİNDE, çıkış 0 ve `deploy/ci-required-contexts.json` DOKUNULMADI; YEREL DOĞRULAMA CI'IN KENDİ KOMUTUYLA: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python run_isolated_tests.py --timeout 180 --workers 4` → `Tüm 386 aktif test dosyası geçti; 2084 test sonucu kaydedildi. Toplam süre: 968.6s`, çıkış 0 (`pytest tests/` KULLANILMADI: o komut `backend/` KÖKÜNDEKİ test dosyalarını HİÇ görmez ve #44'ün kaydı bu tuzağı adıyla yazmıştı); C2 (#46, `feat/kantar-neti-stoga`) DOSYALARINA DOKUNULMADI — `routers/farm.py`, `farm_schemas.py`, `field_stok_tuketici.py`, `routers/products.py`, `test_kantar_fisi_*.py` hepsinde `git diff` BOŞ; C2 PR'ını ÖNCE açtı (#46), bu yüzden kayıt numarası ELLE SEÇİLMEDİ: `python scripts/durum.py --sonraki 47` çalıştırıldı ve `0091-pr-0047.md` verdi; beklenen çakışma yalnız rota envanteri sayı/parmak izi iğnelerinde ve durum numaralandırmasında, İKİNCİ BİRLEŞEN yeniden oturur ve iğneleri BİRLEŞMİŞ AĞAÇTA YENİDEN ÖLÇER (aritmetik YAPMAZ); ÖLÇÜLMEDİ, ADIYLA: (1) e-MÜSTAHSİL (e-MM) SAĞLAYICI ENTEGRASYONU — D1 kağıdı KAYDEDER, GİB'e ya da bir entegratöre HİÇBİR ŞEY göndermez, belgenin elektronik hâli/imzası/gönderim durumu bu PR'ın DIŞINDA; (2) YASAL ORANLAR — hangi ürün için stopajın kaç olduğu ölçülmedi ve kodda YOK, bu bir mevzuat sorusudur; (3) NET ÖDENECEĞİN ÖDENMESİ — çiftçiye ödeme ve avans mahsubu D2'dir, D1 yalnız BORCU doğurur; (4) stopaj yükümlülüğünün deftere işlenmesi (yukarıdaki sahip kararı bekliyor); (5) FRONTEND YÜZEYİ yazılmadı, yalnız `types.gen.ts` türedi; (6) ÜRÜN KARTSIZ KALEM — `product_id` sütunu NULL kabul ediyor ama yazma yolu taban birim isteyip 422 ile reddediyor, ürünsüz bir kalemin ne anlama geleceği AYRI bir karar; ISSUE/CANCEL COMPARE-AND-SET (runtime lens NO-GO kapanışı): aynı taslağa eşzamanlı iki `issue` İKİSİ DE 200 alıp İKİ NUMARA tüketiyordu — `routers/mustahsil.py` durumu OKUYUP predicate'siz UPDATE ediyordu; düzeltme diyalekt-nötr CAS ve durum `draft` → `issuing` → `issued` olarak İLERLİYOR: (1) `UPDATE … SET status='issuing' WHERE status='draft'`, `rowcount == 1` DEĞİLSE 409 `MAKBUZ_TASLAK_DEGIL`, (2) `next_document_no` YALNIZ kazanan çağırır (seriden numara harcayan kaybeden YOK), (3) `UPDATE … SET status='issued', receipt_no=… WHERE status='issuing'` — ÜÇÜ DE TEK İŞLEMDE, istisnada rollback taslağı geri getirir; `cancel` AYNI KALIP (`WHERE status='issued'`); `issuing` ara durumu 0070'in status CHECK'ine YERİNDE eklendi — YENİ GÖÇ AÇILMADI (0071 YOK) çünkü 0070 henüz birleştirilmemiş bu dalın kendi göçüdür, `ck_producer_receipts_no_follows_status` numarasız `issuing`i KABUL eder, upgrade/downgrade SQLite ve PostgreSQL 16.13'te ÖLÇÜLDÜ; PG YARIŞ TESTİ: aynı taslağa 20 eşzamanlı `issue` → TAM BİR 200 ve BİR 409, `document_sequences` deltası 1 (Δseq=1), AYRI taslaklar İKİ AYRI numara alır, `cancel` yarışı 200+409; router dinamik SQL iğnesi yeniden ölçüldü (çağrı sayısı 3 SABİT, parmak izi `1e8e6cd4` → `7020cd85`); `deploy/ci-required-contexts.json` 17 bağlam DOKUNULMADI; izole süit yeniden koşuldu: 386 dosya / 2088 test geçti, çıkış 0. İKİNCİ OTURUŞ (#46 develop'a indi, TABAN `9e02fb8` → `0c27c6d`): yukarıdaki iğnelerin ÖNCEKİ TABANDA ölçülmüş hâlleri KAYITTA BIRAKILDI, aşağıdakiler BİRLEŞMİŞ AĞAÇTA YENİDEN ÖLÇÜLDÜ ve öncekileri GEÇERSİZ KILAR — hiçbiri aritmetikle türetilmedi, her biri kapının kendisine ölçtürüldü; KAYIT NUMARASI 0091 → 0092 çünkü #46 birleşirken 0091'i ALDI (numara elle seçilmedi, `python scripts/durum.py --sonraki 47` verdi, satır içeriği BİREBİR taşındı) ve `durum.py --kapi origin/develop --pr 47` çıkış 0 (taban 91, ağaç 92, bayat/yinelenen sıra YOK); ÇAKIŞAN İĞNELER: alt süreç SQL sayacı 108/172 → 109/176 (develop 108/175'e bu dalın +1 dosya / +1 metni bindi; TOPLAM ölçüldü, toplanmadı), `routers/products.py` parmak izi `99e57d37` → `5cd5fdfd` DEVELOP'IN değeridir ve KORUNDU — bu dal o dosyaya DOKUNMUYOR (C2'nin POST `base_unit` türetmesi), `routers/mustahsil.py` `7020cd85` (CAS) DEĞİŞMEDİ; ÇAKIŞMAYAN ama YİNE ÖLÇÜLEN iğneler DEĞİŞMEDEN yeşil: GET envanteri 170, rota sözleşmesi 355/274, `EXPECTED_AUTHENTICATED` 345 / `EXPECTED_READ` 93 / `EXPECTED_UNDENIABLE` 100, PG popülasyonu 103 (ÜÇ yer: test adı, iddia ve `ci.yml`in `BEKLENEN_PG_DOSYA_SAYISI`'ı — ölçüm 101 adlı + 2 özel), göç başı `20260905_0070` develop'ın `0069`'unun ÜSTÜNDE, çakışma YOK; SÖZLEŞME DRİFTİ KIRMIZIYDI ve düzeltildi: `types.gen.ts` CAS docstring değişiminden sonra yenilenmemişti; `export_openapi -o` → `types:gen` → `capture_frontend_fixtures` koşuldu, delta YALNIZ YORUM (iki `@description`; tip/şema/uç değişimi YOK), `pos-api.json` DEĞİŞMEDİ, `backend/openapi.json` `.gitignore`da, `npx tsc -b --force` çıkış 0; PG İKİZİ TAZE PostgreSQL 16.14'te (docker `pglens` :5433, `lens`, HER KOŞUMDA yeni veritabanı) 20 test — 16 değil, çünkü CAS dört yarış testi EKLEDİ — SIFIR ATLAMA, çıkış 0; İLK DENEME YANLIŞTI ve ADIYLA YAZILIYOR: yalnız `APP_TEST_DATABASE_URL` verilince `app.config.Settings` modül düzeyinde TEK KOPYA olduğu için uygulama SQLite'a düştü ve 17 hata verdi — `ci.yml`in SAHTE PARİTE uyarısının tarif ettiği tuzağın ta kendisi; `DATABASE_URL` iş genelinde verilip `REQUIRE_PG=1` eklenince 20/20 yeşil; İZOLE SÜİT CI'ın kendi komutuyla yeniden koşuldu: 386/2088 → `387 aktif test dosyası geçti; 2092 test sonucu kaydedildi. Toplam süre: 720.6s`, çıkış 0; `deploy/ci-gerekli-baglam-kapisi.py` 17 bağlamla TAM KÜME EŞİTLİĞİNDE, çıkış 0, `deploy/ci-required-contexts.json` DOKUNULMADI; C2/#46 DOSYALARINDA `git diff origin/develop...HEAD` BOŞ: `routers/farm.py`, `farm_schemas.py`, `field_stok_tuketici.py`, `routers/products.py` — birleşme hiçbirini geri almadı.
