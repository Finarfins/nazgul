#34 — ZORUNLU `frontend` BAĞLAMI ARTIK NPM KAYIT DEFTERİNİN AYAKTA OLMASINA BAĞLI DEĞİL: politika KORUNDU, değişen yalnız ARIZA ŞEKLİ (`frontend/scripts/check-audit-policy.mjs` + testi, `frontend/audit-allowlist.json`, `.github/workflows/ci.yml` YALNIZ frontend işi bölgesi, yeni `.github/workflows/audit-gunluk.yml`); taban `origin/develop` = `caf41140f5bf32e028b03b7ca2cd213df3e0f465` ve ÖLÇÜLDÜ ki bu kusur develop'ın KENDİSİNİ düşürüyordu — koşu 33807936220 (headSha `caf4114`, 2026-09-03T21:26:16Z) YALNIZ `frontend` işinde düştü ve düşme satırı birebir `AUDIT_TIMEOUT: npm audit did not return within 30000ms; no audit policy decision was made` (22:49:13Z), yani bütün kod testleri geçerken zorunlu bir bağlam ÜÇÜNCÜ BİR TARAFIN erişilebilirliği yüzünden kırmızıydı ve brifingin bildirdiği PR #31 (3 yeniden koşu) ile PR #32 aynı sınıftır; BİR KAPI, ÜÇÜNCÜ TARAFIN ÇALIŞMA SÜRESİNİ ÖLÇTÜĞÜNDE KAPI DEĞİLDİR: sinyal değil gürültü üretir ve 'yeniden koşarak yeşil kazanmayı' öğretir, bu yüzden POLİTİKA (tam GHSA küme eşitliği, iki yön: onaysız advisory KIRMIZI + bayat allowlist girdisi KIRMIZI) OLDUĞU GİBİ BIRAKILDI ve yalnız ulaşılamama durumunun sonucu değiştirildi; YEREL ÖLÇÜM (`frontend/`, Node v24.18.0): `npm audit --json` SOĞUK ilk çağrıda 5 DAKİKADA DÖNMEDİ (5 dk sınırında kesildi), ısınmış npm önbelleğiyle üç ardışık koşu 2691 ms / 2271 ms / 2014 ms sürdü, üçü de ÇIKIŞ 1 ve 2168 baytlık geçerli yük verdi — yani 30 sn'lik bütçe SICAK yol için fazlasıyla yeterli, SOĞUK yol için hiçbir bütçe garanti değildir; YENİ ARIZA ŞEKLİ: üç deneme (her biri 30 sn, aralar 1 sn ve 4 sn) ve YALNIZ AĞ SINIFI hata yeniden denenir (`ETIMEDOUT ENOTFOUND EAI_AGAIN ECONNRESET ECONNREFUSED ENETUNREACH ENETDOWN EHOSTUNREACH EPIPE ERR_SOCKET_TIMEOUT ENOTCACHED` + `request to https://… failed` / `getaddrinfo` / `socket hang up` kalıpları — liste BİLEREK dar, genişledikçe bir ihlalin ağ arızası kılığına girme ihtimali doğar), tükenirse zorunlu iş `--degrade-on-unavailable` ile `::warning::AUDIT_UNAVAILABLE` basıp ÇIKIŞ 0 verir; ÖLÇÜLEN VE DÜZELTİLEN GERÇEK KUSUR (provanın kendisi buldu, tasarım değil): npm kayıt defterine ulaşamadığında da ÇIKIŞ 1 verir — 'açık bulundu' ile AYNI KOD — ve betiğin ilk hâli çıkış kodunu yükten önce elediği için gerçek bir kesinti `npm audit output is not valid JSON` diye KIRMIZI yanıyordu, yani bozulmuş geçiş yolu HİÇ ÇALIŞMIYORDU; karar sırası tersine çevrildi (önce yük okunabiliyor mu, değilse ağ sınıfı mı) ve bu bir gerileme testiyle çivilendi (`a registry outage that exits 1 with empty stdout is unavailable, not a broken payload`); FAIL-OPEN DEĞİL, DAR BİR AĞIZ ve iki bağımsız çivi tutuyor: (a) bir POLİTİKA İHLALİ bu yoldan ASLA geçmez — `--degrade-on-unavailable` AÇIKKEN ihlal içeren yük verildiğinde çıkış 1 ve `AUDIT_POLICY_RED: unapproved advisory GHSA-9999-8888-7777 (evil)` + `stale allowlist entry GHSA-W5HQ-G745-H8PQ` ölçüldü, (b) ÇEVRİMDIŞI SİNYAL YERİNDE KALDI: `package-lock.json`ın sha256'sı (`0de0d540ff11abdf7f88f31addcf313e45f5db18b592a8235d8933302d1a08d2`) allowlist içinde ayrılmış `$lockfile` anahtarında beyan edilir, kontrol yaşayan denetimden ÖNCE ve BOZULMUŞ GEÇİŞ YOLUNDA DA koşar, yani ağa hiç ulaşılamasa bile bağımlılık ağacındaki her değişim kapıyı kırmızı yakar; ÜÇ YÖN DE UÇTAN UCA KANITLANDI (gerçek CLI, stub `npm_execpath` ile): ihlal yükü → çıkış 1 KIRMIZI; zorlanmış kesinti + degrade → üç `AUDIT_RETRY n/3` satırı ardından `::warning::AUDIT_UNAVAILABLE` ve ÇIKIŞ 0; AYNI kesinti degrade OLMADAN (günlük katı yol) → ÇIKIŞ 1; kilit dosyasına sahte bağımlılık eklenip beyan güncellenmeyince → çıkış 1 ve `AUDIT_POLICY_RED: package-lock.json sha256 mismatch: declared 0de0d540… actual 7d8c1659…`, mutasyon geri alındı ve tekrar `AUDIT_POLICY_GREEN` ölçüldü; GÜNLÜK KATI DENETİM AYRI VE ZORUNLU DEĞİL: `audit-gunluk.yml` her gün 06:17 UTC (tam saat bilerek seçilmedi — GitHub'ın zamanlanmış kuyrukları tam saatlerde yığılıyor) ve `workflow_dispatch` ile, `--degrade-on-unavailable` OLMADAN koşar; `deploy/ci-required-contexts.json` DEĞİŞTİRİLMEDİ ve 17 OLARAK KALDI (`git diff --name-only` o dosya için BOŞ) — günlük iş zorunlu olsaydı, kaçınmak istediğimiz uçtan-bağlılık geri gelirdi; TESTLER `node:test`E EKLENDİ, VITEST'E DEĞİL ve BU BİR DÜZELTMEDİR: brifing 'vitest testleri ekle' dedi, ama ÖLÇÜLDÜ ki mevcut 11 alt-test `node --test scripts/check-audit-policy.test.mjs` ile koşuyor ve `vitest.config.ts`in `include`u YALNIZ `src/**/*.{test,spec}.{ts,tsx}` — bu dosyayı vitest'e taşımak ya da orada ikizlemek, kapıyı koşan koşucudan AYIRIRDI, bu yüzden mevcut süite EKLENDİ: 11 → 22 alt-test, 22/22 geçti; MEVCUT BİR ALT-TESTİN SABİTLEDİĞİ METİN KORUNDU: `AUDIT_TIMEOUT: …within 17ms…` teşhis sözcüğü aynı bırakıldı, değişen tek şey artık ağ sınıfı olarak işaretlenip yeniden denenebilmesidir; ÖLÇÜLEN KOŞULAR (`F:/nazgul-audit`, `npm ci` sonrası): `npm run test:audit-policy` 22/22 geçti, `npx tsc -b --force` çıkış 0, `npx vitest run` 89 dosyada 611/611 geçti (42.12s), `node scripts/check-audit-policy.mjs` `AUDIT_POLICY_GREEN: exact GHSA set matched (1): GHSA-W5HQ-G745-H8PQ` verdi ve `npm ci` SONRASINDA kilit özeti hâlâ eşleşti; ci.yml'in YALNIZ frontend işi bölgesine dokunuldu ve iki iş akışı dosyası da `yaml.safe_load` ile ayrıştırıldı (ci.yml 12 iş, audit-gunluk.yml tek iş `frontend-audit-kati`); DEPODA BAŞKA BİR ÇİVİ YOK: `audit:policy`/`audit-allowlist` metnini harfi harfine bekleyen bir test ARANDI ve BULUNAMADI (PG nüfusundaki ci.yml-literal grep testi sınıfının bir eşi burada yok), yani adım metnini değiştirmek başka bir kapıyı sessizce bozmuyor; ÖLÇÜLMEDİ: bu PR'ın KENDİ CI koşusu ve 17 bağlamın sonucu (kayıt commit'in içindedir, koşu numarası bu satır yazılırken yoktur); ÖLÇÜLMEDİ: `audit-gunluk.yml`in İLK ZAMANLANMIŞ koşusu — `workflow_dispatch` taşıyor ama zamanlanmış iş akışları YALNIZ varsayılan dalda çalışır, dolayısıyla bu dal develop'a İNMEDEN ne zamanlanmış ne elle tetiklenmiş bir koşusu olabilir; ÖLÇÜLMEDİ: kayıt defteri kesintisinin GERÇEK bir CI koşucusunda bozulmuş geçişi tetiklediği — kesinti yerelde stub `npm_execpath` ile ve enjekte edilen spawn ile SİMÜLE edildi, gerçek bir ağ arızası ısmarlanamaz; ÖLÇÜLMEDİ: 30 sn'lik deneme bütçesinin ve 1 sn/4 sn geri çekilmenin GERÇEK kesinti süresine göre kalibrasyonu — üç deneme bir HEURİSTİKTİR, kanıt değil: uzun süren bir kesinti üç denemeyi de yer ve bozulmuş geçişe düşer (TASARIM BUDUR), ama kısa bir titremenin üç denemeyi doldurup doldurmayacağı ölçülmedi; ÖLÇÜLMEDİ: SOĞUK `npm audit`in neden 5 dakikada dönmediği (npm'in meta veri çözümlemesi mi, ağ mı) kök nedene inilmedi — bu PR o soruyu yanıtlamıyor, yalnızca cevabın zorunlu bir kapıyı düşürmesini engelliyor; ÖLÇÜLMEDİ: backend, e2e, container ve PostgreSQL ikizleri bu turda yerelde koşulmadı ve CI'a bırakıldı — bu dilim `frontend/` dışına ve ci.yml'in frontend işi dışına HİÇ dokunmuyor.
