#2 — Hayvancılığın test edilmeyen ÜÇ ekranı — modülün iki en büyük yazma yüzeyi ve iniş ekranı — kendi ALAN KURALLARINA bağlandı; her test bileşenin kendi yorumunda ya da koşulunda YAZILI bir kuralı çiviliyor, hiçbiri "çizildi" demiyor: Animals'ta küpe uyarısı kaydı ENGELLEMEZ (yanıt `warnings` ile iniyor, kutu `info` — `error`a çevrilmesi 2026'da değişen standarttaki geçerli bir küpeyi girilemez yapardı), bireysel kayıtlı sürüde baş sayısı GİRİLEMEZ ve liste beyan edilen 40'ı değil bireysel 2'yi gösterir (karşı yön de sınandı: bireysel kaydı olmayan sürüde alan AÇIK, yoksa kapı her sürüde kapalı olur ve kimse fark etmezdi), yazma düğmeleri `herd.manage`e bağlı ama DETAY yolu değil, düzenleme mevcut durumu KORUR ve `expected_updated_at` gönderir — satılmış hayvan ad düzeltmesiyle sürüye dönmez; AnimalDetail'de yaş sunucunun `age_days` alanından okunur (çapa bilerek çelişkili: doğum tarihi 2022-04-01 ama yaş 1000 gün, cihaz saatinden hesaplansaydı dört yılı aşardı), altı kaynak TEK çizelgede EN YENİ ÜSTTE birleşir ve doğumlar `mother_id` ile çekilir, sürüden ÇIKARAN hareket önceden uyarır — çıkarmayan UYARMAZ, kayıttan sonra durum istemcide tahmin edilmez SUNUCUDAN tazelenir, tartım noktalı STRING gider; HerdDashboard'da bireysel sayı ile grup başı TOPLANMAZ (12 ve 30 ayrı durur, toplamları 42 ekranda YOK — toplamak aynı hayvanın iki kez sayılma riskini tek sayının arkasına gizlerdi), doğum OLAYI ile canlı yavru ayrı, geciken aşı listesi yalnız vadesi geçmiş VE sürüdeki hayvanı en eski gecikme önce gösterir, sayının NE OLMADIĞI ekranda yazılı; yirmi mutasyon atılır bir ağaçta ölçüldü ve yirmisi de koruduğu testi kırmızıya düşürdü — uyarı gösterilmeyince küpe testi, `disabled` kalkınca baş sayısı testi (ve `disabled` sabitlenince karşı yön testi), izin kapısı kaldırılınca izin testi, `status: row.status` silinince durum testi, yaş doğum tarihinden hesaplanınca yaş testi, sıralama ters çevrilince ve SÜT kaynağı düşünce çizelge testi, `mother_id` yerine `animal_id` yazılınca yine çizelge testi, uyarı her türde çıkınca hareket testi, `yukle()` silinince tazeleme testi, ağırlık `Number()`e çevrilince tartım testi, iki sayı toplanınca pano testleri, `ACTIVE` süzgeci ve vade süzgeci kalkınca ve sıra ters çevrilince geciken aşı testi, kapsam cümlesi silinince kapsam testi
