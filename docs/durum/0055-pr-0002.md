#2 — `crop_seasons.product_id` bileşik yabancı anahtarı artık CI'da ARDINDA DURAN testi var: 0062'nin `(company_id, product_id) -> products(company_id, id)` güvencesi BİR KEZ elle ölçülmüştü ve bu dosyada `field_harvest` hiç geçmiyordu, yani özellik doğruydu ama kırılsa hiçbir yerde kırmızı görünmezdi; `test_field_stok_tuketici_postgresql.py`ye altı test eklendi — AYNI kiracının ürünü KABUL (probun geçerlilik koşulu: kısıt her yazmayı reddediyor olsa çapraz-red iddiası hiçbir şey ölçmezdi), BAŞKA kiracının ürünü RED, NULL ürün KABUL (MATCH SIMPLE; `SKIPPED_NO_PRODUCT` kovası ERİŞİLEBİLİR kalmak zorunda) ve kısıt `downgrade`+`upgrade` zincirinden sonra HÂLÂ ısırıyor — zincir probu kendi NEGATİF KONTROLÜNÜ taşıyor, geri almanın GERÇEKTEN indiğini (`kisit=False sutun=False`) ölçmeden yeniden kurulum iddiasına geçmiyor; red iddiası "bir şey fırladı" DEĞİL, `ForeignKeyViolation` SINIFINA ve `fk_crop_seasons_product_same_company` KISIT ADINA bağlı çünkü sezon satırı NOT NULL, CHECK ve parsel kısıtlarını da taşıyor ve başka bir kısıta takılan prob bu güvenceyi ölçmüş sayılmazdı; tüketici yolu da PG'de çivilendi (ürünlü sezonun hasadı TEK hareket üretiyor, miktar `50.0000` ve tipi `Decimal` — sqlite3 aynı sütunu float verir ve orada `50.0` ile `50.0000` ayırt edilemez, ürünsüz sezonunki `SKIPPED_NO_PRODUCT`ta terminalleşip gerekçesinde `crop_seasons.product_id` diyor); TEST KODU + BU GİRDİ dışında hiçbir şeye dokunulmadı, `backend/app/` ve `backend/alembic/` değişmedi; gerçek PostgreSQL 16.13'te ölçüldü ve mutasyonla kanıtlı: atılabilir bir kopyada kısıt çıplak `product_id -> products.id`ye zayıflatılınca prob `CAPRAZ RED sinif=ForeignKeyViolation kisit=fk_crop_seasons_product_same_company` yerine `CAPRAZ KABUL urun=960302` yazdı ve tam iki test kırmızı oldu — test_sezon_urunu_BASKA_firmada_REDDEDILIYOR_pg ve test_bilesik_kisit_GERI_ALMA_ve_YENIDEN_KURULUMDAN_sonra_ISIRIYOR_pg
