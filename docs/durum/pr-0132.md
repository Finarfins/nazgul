#132 — CS4 ÖDEME FORMLARINDA ÇEK/SENET → PORTFÖY DİYALOĞU (yalnız frontend; backend/göç 0 dosya, yeni rota YOK, rota envanteri SABİT): CS2 köprüsü `POST /api/payments`i check|promissory_note yönteminde evrak alanı ZORUNLU (422), diğer yöntemlerde YASAK (422) yaptı; ölçülen üç form (`pages/Payments.tsx`, `components/EntityQuickActions.tsx`, `pages/EntityDetail.tsx`) o yöntemleri sunup evrak alanı GÖNDERMİYORDU, yani çıplak çek ödemesi artık geçersiz istek; Şef kararı: yöntem listesinde çek/senet KALDI ama Kaydet `/api/payments` ÇAĞIRMIYOR — CS3'ün Yeni Evrak penceresi ön dolgulu (cari, tutar, tarih, not) açılıyor ve evrak `payment_olustur:true` ile yazılıyor, ödeme satırını TERS KÖPRÜ doğuruyor, liste yenileniyor; AYNALANAN ALANLAR (`CekBilgisi`, refs/pull/129/head `schemas.py`; `CekSenetOlustur` ile doğrulandı) zorunlu `vade` (date) + `seri_no` (1–100, strip sonrası boş olamaz), isteğe bağlı `keside_tarihi` (<=vade), `banka_adi` 160, `sube_adi` 120, `hesap_no` 100, `kesideci` 200, `notlar` 2000; yöntem eşlemesi backend `cek_senet_cari.py` ile birebir (`CEK_YONTEMLERI`, `YONTEM_TUR` check->cek / promissory_note->senet); UYGULAMA: `YeniEvrakDialog`a `baslangic` propu (form ÇOĞALTILMADI — CS3 portföy sayfası propu hiç geçmiyor, davranışı ve testleri DEĞİŞMEDİ, 62 test yeşil), yeni `pages/cek-senet/odemeKoprusu.ts` yöntem sabitleri + ön dolgu tek yerde, `payment_olustur`/`odeme_tarihi` yüke YALNIZ köprülü girişte giriyor (CS3'ün köprüsüz yükü o anahtarları taşımıyor; `odeme_tarihi` tek başına 422), not EVRAKIN `notlar`ına gidiyor çünkü ters köprü ödeme notunu kendisi `Çek <seri_no>` yazıyor (`_cek_odemeyle_olustur`), `payments` taşımayan rol çek/senet seçeneğini FORMDA görmüyor, kilitli tarafta `/suppliers` listesine hiç bakılmadığı için `purchases` taşımayan rol de kendi formundan verilen evrak girebiliyor; types.gen.ts CS2 ucundan yeniden üretildi (`CekBilgisi`, `CekSenetOlustur.payment_olustur`, `PaymentCreate.cek_senet`) — develop'ta bu dosya CS2 tabanından beri değişmediği için üretim temiz, CS2 birleşince fark kendiliğinden kayboluyor; BİLEREK YAPILMAYAN: düzenleme (PUT) yolunda istemci kapısı YOK — bağlı ödemeyi sunucu 409 `CEK_BAGLI_ODEME` ile reddediyor ve metin formda gösteriliyor, kapıyı istemciye koymak o davranışı ÖLÇÜLEMEZ kılardı; AÇIK BOŞLUK (backend, bu PR dışı): CS2 `update_payment` `_cek_bilgisi` çağırmıyor, köprüsüz bir ödemenin yöntemi PUT ile check'e çevrilerek hâlâ çıplak çek ödemesi yazılabiliyor; TESTLER: yeni `Payments.cek.test.tsx` 10, `EntityQuickActions.cek.test.tsx` 6, `EntityDetail.cek.test.tsx` 6 — form başına Çek seçimi pencereyi ön dolgulu açıyor, `POST /api/payments` ÇAĞRILMIYOR, başarılı kayıt listeyi/cariyi yeniliyor, Vazgeç hiçbir şey yazmıyor, `payments`siz rol seçeneği görmüyor, 422 evrak metni pencerede, 409 `CEK_BAGLI_ODEME` düzenleme VE silmede sunucu metnini gösterip satırı BIRAKIYOR; mevcut Payments/EntityDetail/EntityQuickActions/CS3 testleri DEĞİŞTİRİLMEDİ (hiçbir eski iddia gevşetilmedi); lint 0 hata (872 uyarı, taban ile aynı), build yeşil, tam vitest `--maxWorkers=1` 105/105 dosya 779/779 test YEŞİL (1021 s); `--maxWorkers=2` koşumunda 776 yeşil / 3 kırmızı — üçü de YALNIZ `TransactionDialog.test.tsx`, dosya tek başına 11/11 yeşil, CS4'ün dokunduğu hiçbir modülü kullanmıyor (import listesi ölçüldü), imza `vite.config.ts`te belgelenen işçi doygunluğu flake'inin aynısı (bu makine oradaki 8 çekirdekli ölçümden yavaş; taban 368cb5d)
