#84 — WA4 BEKLEYEN İŞLEM DEFTERİ: taslak → açık ONAY → TEK uygulama, ve WhatsApp GİDEN kanalı (göç `20260910_0080`, down_revision `20260910_0079`); ÖLÇÜLEN EKSİK: WA3-core (#77) `niyet.tahsilat_coz`u ve `TahsilatNiyeti`yi getirdi — "Şaban Korkmaz 175.000 nakit verdi" cümlesi artık deterministik olarak tutara, yönteme ve müşteri terimine çözülüyordu ama o niyetin GİDECEĞİ BİR YER YOKTU: ölçüldü, `alembic/versions/` altında `whatsapp_pending_actions` adında bir tablo YOKTU ve `app/whatsapp/` altında taslak açan tek bir fonksiyon bile YOKTU; dahası eksik ZATEN YAZILIYDI — WA2'nin (#83) `app/whatsapp/schema.py` başlığı "TAŞINMAYAN TEK TABLO ve gerekçesi (bu dilimin KAPSAMI DIŞINDA): `whatsapp_pending_actions` — iki adımlı yazma taslakları; bu PR hiçbir finansal yazma yapmıyor" diyordu ve bu PR finansal yazmayı GETİRDİĞİ için tablo da onunla birlikte doğdu, yani kaynaktaki ALTI tablonun ALTISI da artık bu depoda ve taşıma TAMAMLANDI; TEK KİRACI tablosu (`company_id` TAŞIR, `TENANT_TABLES` 119→120) ve bağlantıya BİLEŞİK yabancı anahtarla bağlı (0062'nin kuralı): `(company_id, whatsapp_link_id) -> whatsapp_links(company_id, id)` — çıplak bir `whatsapp_link_id` YETMEZDİ, bir firmanın taslağı BAŞKA firmanın bağlantısına asılı görünebilirdi ve bu PG'de GERÇEK istekle kanıtlandı; FK "satır VAR" der "satır SENİN" DEMEZ, o yüzden servis ayrıca AYNI kullanıcı + AYNI kanonik numara + AKTİF denetimi yapıyor (`_baglanti_dogrula`) ve iki koruma AYNI şeyi ölçmüyor; `islem_anahtari` KÜRESEL UNIQUE ve bu bir görünüm alanı DEĞİL ÖDEME İDEMPOTENSİSİNİN KÖKÜDÜR: satırla birlikte doğar ve taslak ömrü boyunca SABİT kalır, uygulama anında üretilen taze bir UUID kullanılsaydı işçi ödemeyi yazıp `APPLIED` damgasını yazamadan çöktüğünde lease devralmasından sonraki ikinci deneme YENİ bir anahtarla gider ve MÜŞTERİYE İKİNCİ KEZ TAHSİLAT İŞLENİRDİ; kapsamda TEK AKTİF TASLAK kuralının hakemi uygulama sorgusu DEĞİL kısmi UNIQUE indekstir — `(company_id, user_id, phone) WHERE status IN ('PENDING','APPLYING')` — ve kural olmasaydı kullanıcının yazdığı `ONAY` kelimesinin HANGİ taslağa ait olduğu BELİRSİZ kalırdı (sohbette seçim arayüzü yoktur), tekil KISMİDİR ve düz DEĞİL çünkü o hâlde terminal satırlar da anahtara girer ve aynı kullanıcı aynı numaradan İKİNCİ KEZ tahsilat YAPAMAZDI; yüklem İKİ diyalekt parametresine de AÇIKÇA verildi ve 0079'un `is_active` yükleminden AYRILDIĞI nokta kayda geçti: orada yüklem BOOLEAN olduğu için diyalekte göre ayrı yazılmak ZORUNDAYDI (SQLite'ın boolean'ı tamsayıdır), burada METİN karşılaştırması olduğu için iki diyalektte AYNI sözdizimi — yine de ikisi de yazıldı, birini unutmak indeksin o diyalektte HİÇ kurulmaması demektir ve kurulmamış bir tekil HİÇBİR ŞEYİ reddetmez; `SUPERSEDED` YOK ve bu kaynaktan (`nazgul_website/backend/app/whatsapp/bekleyen.py`, 479 satır) BİLİNÇLİ olarak AYRILAN yerdir: kaynak yeni taslağı eskisinin ÜZERİNE yazıyordu, burada ikinci taslak REDDEDİLİYOR (`BekleyenIslemSurmekte`) çünkü sessizce değiştirilen bir taslak, kullanıcının ekranında HALA duran ESKİ özeti onaylamasına ve BAŞKA bir tutarın yazılmasına yol açabilirdi; BİR TASLAK TAM OLARAK BİR KEZ uygulanır ve bunu ÜÇ BAĞIMSIZ katman BİRLİKTE sağlar (PENDING→APPLYING CAS'i, `payment_idempotency` defteri, `claim_token` sahipliği) — 1 olmasaydı yirmi işçi ödeme yoluna girerdi, 2 olmasaydı lease devralması ikinci ödeme yazardı, 3 olmasaydı devralınmış satırı eski sahibi kapatabilirdi; ÖDEME BU DEPONUN GERÇEK YOLUNDAN yazılıyor ve yol ÖLÇÜLDÜ, varsayılmadı: `app/routers/finance.py:273` (`POST /api/payments`) tahsis motoru açıkken `payment_allocation_engine.create_payment_with_allocation`ı çağırıyor ve idempotency anahtarını `Idempotency-Key` başlığından veriyor — WA4 AYNI fonksiyonu, AYNI defteri (`payment_idempotency`, anahtar `(company_id,'create_payment','payment','create',key)`) ve AYNI sözleşmeyi kullanıyor, tek fark anahtarın KAYNAĞIDIR (HTTP başlığı yerine taslağın kendi `islem_anahtari`si, `wa:` önekiyle); ayrı bir yazma yolu (ham `INSERT INTO payments`) açmak MÜMKÜNDÜ ve YANLIŞ olurdu — tahsis, cari senkronu ve idempotency defteri o yolda YENİDEN yazılmak zorunda kalır ve iki yol zamanla ayrışırdı; TAHSİS MOTORU KAPALIYKEN WA4 BİLEREK YAZMIYOR (`TahsisMotoruKapali` → taslak `FAILED`a kapanır, kullanıcı web paneline yönlendirilir) ve gerekçe ölçüldü: motor kapalıyken `POST /api/payments` ödemeyi HAM `INSERT`le yazıyor ve `payment_idempotency` defterine HİÇ dokunmuyor, yani o yolun TAM OLARAK BİR KEZ garantisi YOKTUR ve WA4'ün bütün tasarımı o deftere dayanıyor — sessizce zayıf yola düşmek, işçi çökmesinden sonraki ikinci denemede müşteriye ikinci kez tahsilat işlerdi; GİDEN TARAF — TAŞIYICI WA4'ÜN DEĞİL ve bu REBASE'DE ÖLÇÜLEREK DEĞİŞTİ: ilk hâlde WA4 `cloud_api.metin_gonder` adında KENDİ urllib göndericisini getiriyordu; #85 (WA3-full) develop'a inerken `app/whatsapp/saglayici.py` ile AYNI uca POST atan İKİNCİ bir gönderici getirdi ve rebase'de görüldü ki aynı depoda Meta'ya çıkan İKİ yol, iki FARKLI hata sınıflandırması ve `config.py`de İKİ KEZ tanımlanmış `whatsapp_access_token` vardı (Pydantic sessizce SONUNCUSUNU alırdı — git otomatik birleştirdi çünkü hunk'lar çakışmıyordu); WA4'ün göndericisi ve ayar alanı SİLİNDİ, WA3'ünki KALDI (önce indi ve daha zengin: hatayı yeniden-denenebilir / kalıcı / kota olarak SINIFLANDIRIYOR) ve `WhatsAppNotificationProvider` artık "wiring-only stub" DEĞİL gerçek adaptördür ama taşıyıcıyı KENDİSİ YAZMAZ — `saglayici.saglayici_al()` çağırır: jeton ya da numara kimliği BOŞKEN `NONE` (ağa HİÇ çıkılmaz — varsayılan kurulumda KOŞAN yol budur, yani bu PR tek başına hiçbir kanalı AÇMAZ), `notification_provider="simulation"` iken `SIMULATED` (terminal, `PushNotificationProvider`dan devralınan kural) — ve SİMÜLASYON DENETİMİ YAPILANDIRMA DENETİMİNDEN ÖNCE koşar, sırası ÖLÇÜLDÜ: tersi olsaydı jetonu olmayan bir kurulumda `simulation` ayarı HİÇ ETKİ ETMEZ ve `NONE` dönerdi, yani akışı ağa çıkmadan koşturmak için tam da jeton koymak gerekirdi, yapılandırılmışsa gerçek gönderim ve ardından `SENT`; `external_id` BOŞTUR ve bu daralma BİLİNÇLİDİR — WA3'ün sağlayıcısı yanıt gövdesini okur ama AYRIŞTIRMAZ, yani `wamid` bu sınıra ulaşmıyor; iki seçenekten (WA3'ün sözleşmesini WA4 uğruna genişletmek / `SENT`in anlamını daraltmak) İKİNCİSİ seçildi ve `SENT` artık "Meta POST'u 2xx ile KABUL ETTİ" demektir, bir TESLİMAT KANITI değil — dar ama DOĞRU bir iddia, geniş ve yanlış olana yeğlendi; `KANAL_SAGLAYICILARI["WHATSAPP"]` ÇİVİLENDİ ve 5.4c'nin "bu kanal TARİHSEL olarak ayardan seçiliyor ve o davranış BU TURDA KIMILDAMADI" kaydını kımıldatan şey kanalın artık GERÇEK bir adaptörünün olmasıdır: çivilenmeseydi `notification_provider="smtp"` ayarlı bir kurulumda WHATSAPP satırı SMTP adaptörüne gider ve o adaptör alıcı alanındaki TELEFON NUMARASINI bir e-posta adresi sanıp ona mail atmaya çalışırdı (PUSH için ölçülen kusurun birebir aynısı); `supports_idempotency = False` ve bu ÖLÇÜLMÜŞ bir karardır — Meta'nın `messages` ucunda istemci tarafı tekilleştirme anahtarı YOKTUR, `True` demek süresi dolmuş lease'in geri alınmasını açar ve aynı mesajı İKİ KEZ gönderirdi; ASİSTAN CEVAPLARI OUTBOX'TAN GEÇMEZ ve gerekçesi `docs/whatsapp/WA4_KANALLAR.md`de tam yazılı: rıza defteri (`notification_consents`) DB kısıtı seviyesinde YALNIZ `CUSTOMER`/`SUPPLIER` tanıyor (göç 0033 `ck_notification_consents_party_type`), asistan cevabının alıcısı ise bir `app_users` satırıdır ve orada kaydı OLAMAZ — outbox'tan geçirmek ya rıza denetimini atlamayı (defterin var olma sebebini bütün satırlar için zayıflatır) ya `PARTY_TYPES`e `USER` eklemeyi (rıza bir PAZARLAMA iznidir; kullanıcının kendi sorduğu sorunun cevabı için "izin vermesi" kavramsal olarak boştur) ya da sahte bir müşteri kaydı uydurmayı gerektirirdi; ayrıca Meta'nın 24 saat kuralı iki yolu ZATEN ayırıyor (asistan cevabı her zaman pencerenin İÇİNDE, bildirim çoğu zaman DIŞINDA ve şablon ister) ve ŞABLON DESTEĞİ BU TURDA YOKTUR — açıkça söyleniyor; BU PR HİÇBİR ROTA EKLEMEZ (kapı `test_WA4_HICBIR_ROTA_EKLEMEDI`: `routers/whatsapp.py`de `bekleyen`/`pending` geçmiyor ve servis modülü hiçbir FastAPI sembolü içe aktarmıyor), HİÇBİR ZAMANLAYICI BAĞLAMAZ (`suresi_gecenleri_kapat` bir süpürücüdür ama onu ÇAĞIRAN YOKTUR — işçi WA3'ün işi) ve `app/whatsapp/service.py` ile `niyet.py` DEĞİŞTİRİLMEDİ, yalnız OKUNDU; İKİ KAPI ÖLÇÜMLE KAZANILDI ve ikisi de kod şeklini DEĞİŞTİRDİ: (1) `_kapsam(kimlik, telefon)` yardımcısı KALDIRILDI çünkü kiracı kapsam nöbetçisi ÇAĞRININ ARDINI GÖREMİYOR ve yardımcıyı kullanan DOKUZ ifadeyi birden "bağlı company_id yüklemi yok" diye işaretliyordu — nöbetçiyi istisnayla susturmak YANLIŞ olurdu (o listede her kayıt AYRI bir güvenlik kararıdır ve burada verilecek karar YOKTU: yüklem gerçekten vardı, yalnız GÖRÜNMÜYORDU), yüklem her sorguya AÇIKÇA yazıldı ve `_sahip_mi` ile `_jetonla_kapat` da artık `kimlik` ALIYOR (önce yalnız satır kimliği + jetonla daralıyorlardı; kapsam yüklemi eklenince sızmış bir jeton BAŞKA firmanın satırını kapatamaz hâle geldi, yani kapı yalnız denetlenebilirliği değil GÜVENLİĞİ de artırdı), (2) tablo `as wpa` KISALTMASIYLA içe AKTARILMADI ve `_jetonla_kapat` `**degerler` yayılımı yerine AÇIK SÜTUN KÜMESİ kullanıyor — ikisi de Core sorgu envanterinin hedefi ve dokunulan sütunları STATİK olarak görebilmesi içindir, kısaltma yazılsaydı DOKUZ sorgunun hedefi "çözülemedi" sayılır, yayılım kalsaydı `desteksiz` listesine WA4'ten BİR satır girerdi (`_finalize` için daha önce yapılan düzeltmenin AYNISI); ALT SÜREÇ SQL ENVANTERİ DE KAÇAMAKLA DEĞİL DÜZELTMEYLE GEÇİLDİ: göç turu betiği başta bir f-string'di ve envanter gömülü SQL'i yalnız `ast.Constant` dizgelerinde aradığı için (`JoinedStr`da ARAMIYOR) içindeki `SELECT` kapının gözünden KAÇIYORDU — betik MODÜL DÜZEYİNDE SABİT METNE çevrildi ve parametreler interpolasyonla değil ORTAM DEĞİŞKENİYLE geçiyor, böylece kapı yeşil değil GERÇEKTEN yeşil oldu (`alt_surec_sql.txt` +1); KAPILAR: `TENANT_TABLES` 119→120, dışa aktarım ×3 (119→120 ve bellek yorumundaki sayı), alembic başı 5 yerde 0079→0080 (`test_e1b_plantback`, `test_e2_tedavi_arinma`, `test_e3_karantina`, `test_goc_zinciri` ×3), Core sorgu envanteri 160→170 (select 103→109, update 47→51, delete 10→10; ON sorgu TEK dosya, `changed`/`stale` BOŞ, `UNRESOLVED_ALLOWLIST` BÜYÜMEDİ, `desteksiz` BÜYÜMEDİ), kiracı nöbetçisi 139→150 ve istisna 3→4 (TEK yeni kayıt: `suresi_gecenleri_kapat`, KÜRESEL işçi süpürücüsü — WA2'nin iki kaydından FARKLI SINIFTAN, orada sorunun cevabı `company_id`nin kendisiydi, burada sorunun `company_id` ile HİÇ İLGİSİ YOK; kapsam kaçağı DEĞİL çünkü dönüş değeri yalnızca KAPATILAN SAYIDIR ve okunan her satır için yazılan UPDATE kiracı yüklemi TAŞIR), `activity_log.py` dinamik-SQL parmak izi (SQL EKLENMEDİ — dinamik `text()` çağrısı 3'te SABİT; eklenen tek şey İKİ KAPALI KÜME LİTERALİDİR), KAPALI AKTİVİTE KATALOĞU 70→75 ve `RESOURCE_TYPES` 21→22 (`whatsapp_pending`; BEŞ eylemin hiçbiri ötekinin yerine geçemez — bir taslağın İPTAL EDİLMESİ ile SÜRESİNİN DOLMASI panelde ayırt edilebilmelidir, birincisi kullanıcının KARARI ikincisi SESSİZLİĞİDİR; kaynak tipi `payment` DEĞİL çünkü dördü henüz VAR OLMAYAN bir ödemeye bağlanmak zorunda kalırdı), `pins/pg_twins.txt` ve `pins/alt_surec_sql.txt` +1'er satır, ROTA ENVANTERLERİ KIMILDAMADI (uç eklenmedi: `test_route_get_permission_inventory`, `test_route_security_contracts`, `test_authorization_population_reconciliation` üçü de DEĞİŞMEDEN yeşil) ve `types.gen.ts` KIMILDAMADI (OpenAPI yalnız rota ve şemadan türer; bu turda `app/routers/` ve `app/main.py`ye HİÇ dokunulmadı), `test_backend_encoding.py` mojibake nöbetçisi `Â`yi işaretledi ve deponun yerleşik yazımına (`HALA`, `DAHIL`) geçildi — nöbetçi meşru Türkçe düzeltme işaretini ayırt edemez ve ETMEMELİ, etmeye çalışan bir kural gerçek mojibake'i de kaçırırdı; TESTLER `tests/test_wa4_bekleyen.py` (27) + `test_wa4_bekleyen_postgresql.py` (10, GERÇEK PostgreSQL 16'da koştu); YARIŞ TESTİ ÖLÇÜMLE DÜZELTİLDİ ve düzeltme kayda geçti — testin ilk hâlinin başlığında "CAS düşerse bu test KIRMIZI olur" YAZIYORDU ve o cümle YANLIŞTI: mutasyon bataryası hem CAS hem idempotency-anahtarı mutantını YEŞİL bıraktı, sebep ölçüldü (değişmez ÜÇ BAĞIMSIZ katmanla korunuyor ve HERHANGİ BİRİ TEK BAŞINA yetiyor; CAS düşünce yirmi işçi claim alıyor ama tamamlama CAS'i yalnız sonuncuyu geçiriyor ve defter ödemeyi tek tutuyor), bunun üzerine katmanları TEK TEK ölçen İKİ YENİ PG adımı eklendi — `test_YIRMI_ESZAMANLI_CLAIM_TEK_JETON_VERIYOR` (öteki iki katmanı devreden çıkarıp DAĞITILAN JETON SAYISINI sayar; CAS mutantını ÖLDÜRÜR) ve `test_COKME_PENCERESI_IKINCI_ODEME_YAZMIYOR` (CAS'i devreden çıkarıp yalnız defteri ölçer; anahtar mutantını ÖLDÜRÜR) — ve SQLite ikizindeki mutasyon tablosunun yanlış iddiası ÖLÇÜLEN gerçekle DEĞİŞTİRİLDİ; PG İKİZİNİN TEARDOWN'I `activity_logs`in APPEND-ONLY tetikleyicisine ÇARPTI (göç 0030, `trg_activity_logs_no_delete`, KOŞULSUZ) ve üç seçenekten ikisi reddedildi (satırları bırakmak → paylaşık şemada biriken firma satırı komşu dosyayı kırar, `product_lots` artıklarıyla ZATEN bir kez ölçülmüş hata sınıfı; tetikleyiciyi kalıcı düşürmek → bu ikizin KENDİ testleri o koruma AÇIKKEN koşuyor), seçilen yol tetikleyiciyi YALNIZ teardown transaction'ının içinde kapatıp aynı transaction'da geri açmaktır ve GÜVENLİ olmasının sebebi PostgreSQL'de DDL'in TRANSACTION'A dâhil olmasıdır — blok herhangi bir sebeple düşerse `DISABLE TRIGGER` de GERİ ALINIR ve koruma hiç kapanmamış olur; MUTASYON BATARYASI DOKUZ MUTANT, DOKUZU DA ADIYLA ÖLDÜ — SQLite'ta ALTI (CAS yüklemi düşürüldü, idempotency anahtarı taze UUID yapıldı, kısmi tekil göçten silindi, kanal çivisi kaldırıldı, sağlayıcı `SENT` yazdırıldı, jeton `==` ile kıyaslandı) ve PG'de ÜÇ (CAS, anahtar, kısmi tekil — göç mutantı için şema SIFIRDAN kuruldu, yoksa indeks ZATEN kurulu olduğu için mutant görünmezdi); REBASE: #85 (WA3-full: işçi, zamanlayıcı, giden sağlayıcı) indikten SONRA TABAN `e78a466`→`acd3738` olarak yeniden kuruldu; İKİ çakışma çıktı (`pins/pg_twins.txt` — iki satır da korundu; `test_core_query_inventory.py` — toplamlar ve parmak izi) ve `app/whatsapp/schema.py` OTOMATİK birleşti: WA3'ün lease sabitleri (`LEASE_DAKIKA`, `MAX_DENEME` — WA1'in AÇTIĞI sütunların POLİTİKASI, göç GEREKTİRMEZ) ile WA4'ün bekleyen işlem tablosu YAN YANA duruyor, İKİSİ DE korundu; PİNLER ARİTMETİKLE TAŞINMADI YENİDEN ÖLÇÜLDÜ ve ölçüm gerekliydi: Core sorgu envanteri `e78a466` üzerinde 160→170 iken `acd3738` üzerinde 165→175 (select 111, update 54, delete 10), çıkarma yapmak WA3'ün sorgularından hiçbirinin WA4'le ÇAKIŞMADIĞINI VARSAYMAK olurdu; YAMA ÖZETİ (`git diff <taban> <baş> -- backend/app backend/alembic`) rebase ÖNCESİ `59b402df` iken SONRASI `15bc4cb2`dir ve bu fark BEKLENEN bir farktır, taşımanın bozulduğunun kanıtı DEĞİL: hunk başlıklarını normalleştirip karşılaştırınca kalan tek ayrım (a) blob kimlikleri, (b) `config.py` ve `schema.py`de WA3'ün eklediği BAĞLAM satırlarıdır — WA4'ün EKLEDİĞİ/SİLDİĞİ satırlar birebir aynıydı; yamanın BİLEREK değiştiği tek yer, yukarıda gerekçesi yazılı taşıyıcı tekilleştirmesidir (`cloud_api` göndericisi ve `config.py` alanı SİLİNDİ, adaptör `saglayici`ye bağlandı); ALEMBİC BAŞ PİNİ SAYISI 6→7: #85 kendi PG ikizinde (`test_wa3_worker_postgresql.py::test_GOC_TURU_up_down_up_GERCEK_PostgreSQLde`) bir baş pini daha açtı ve o dosya kanonik izole koşucuda ATLANIYOR (koşucu SQLite'a zorluyor, atlanan test PASS sayılıyor) — pin, eski baş kimliği depoda GREP'lenerek bulundu, süite güvenilerek DEĞİL; kayıt adı `python scripts/durum.py --sonraki 84`
