#84 — WA3-FULL: gelen kuyruğun İŞÇİSİ + niyet YÜRÜTÜCÜSÜ + giden SAĞLAYICI (GÖÇ YOK, ROTA YOK); ÖLÇÜLEN EKSİK: WA1 (#80) PLATFORM kuyruğunu açtı, WA2 (#83) numarayı ERP kimliğine bağladı, WA3-core (#77/#78) niyeti çözdü — üçünü birbirine bağlayan HİÇBİR ŞEY yoktu ve `whatsapp_inbound` satırları sonsuza dek `RECEIVED` kalıyordu; DÖRT YENİ MODÜL ve hiçbiri göç açmıyor: `service.py` (kirala → cevapla → damgala; kira alanları `lock_token`/`locked_until`/`attempt_count` WA1'in göçünde ZATEN VAR, bu dilim yalnız POLİTİKALARINI yazıyor: `LEASE_DAKIKA=5`, `MAX_DENEME=3`), `yurutucu.py` (WA3-core'un `NiyetYurutucu` protokolünün GERÇEK gerçekleştirimi), `saglayici.py` (Meta Cloud API `/messages`, stdlib `urllib` — `httpx` bu depoda çalışma zamanı bağımlılığı DEĞİL) ve `zamanlayici.py` (süreç içi thread, `field_stok_zamanlayici` deseni, VARSAYILAN KAPALI); YEDİ ARACIN ALTISI UÇLARIN KENDİ FONKSİYONLARINI ÇAĞIRIYOR ve bu bir kolaylık değil ölçülmüş bir karar: `customers.musteri_satirlari`, `products.urun_satirlari`, `reports.ozet_verisi`, `reports.yaslandirma_verisi`, `dashboard.kritik_urunler` bu turda `Request` bağımlılığından ARINDIRILDI (gövde modül düzeyine taşındı, SQL METNİ KARAKTERİ KARAKTERİNE aynı, `cid` artık `company_id(request)` yerine PARAMETRE) ve uçlar onları ÇAĞIRIYOR — kopyalanmış bir bakiye formülü, iki yüzeyin AYNI müşteri için FARKLI sayı söylediği güne kadar sessiz kalırdı ve o gün hangisinin doğru olduğu BİLİNEMEZDİ; TEK İSTİSNA `parca_satis_gecmisi`: "şu parça bu dönemde kaç adet satıldı" sorusunu soran bir uç ARANDI ve YOK (`reports/summary`nin `top_products`ı SIRALAMA döndürür, tek parça sorgusu değil), sorgusu yürütücüde yazılı ve kök yüklemi AÇIK ve KOŞULSUZ (`o.company_id=:cid`), interpolasyona giren ÜÇ parça da KOD SABİTİ (lehçeye göre tarih normalizasyonu, merkezî muhasebe durumu yüklemi, aynı tarih ifadesinin yeniden kullanımı) ve kullanıcının yazdığı terim ile dönem sınırları BAĞLI PARAMETRE; KAYNAKTAN AYRILAN ÜÇ YER, üçü de ölçülmüş gerekçeyle modül başlıklarında yazılı: (1) SATIRA KİRACI YAZILMIYOR — kaynağın `_sonlandir`ı terminal durumla birlikte `company_id`/`user_id` de yazıyordu çünkü ORADAKİ `whatsapp_inbound` o sütunları taşıyor, bu depoda TAŞIMIYOR (göç `20260910_0078` başlığı: platform tablosu; `company_id` taşıyan her tablo `TENANT_TABLES`a girer ve her sorgusundan `company_id=:cid` yüklemi ister) ve bedel AÇIKÇA kabullenildi: kuyruk satırı hangi firmaya cevap verildiğini SÖYLEMEZ, satırda duran tek iz `status` + `last_error`dır ve "bu numara o an hangi firmadaydı" sorusu `whatsapp_links` + `whatsapp_context`ten cevaplanır; (2) HIZ SINIRI SAYACI AYRI VE KISA BİR İŞLEMDE — MERCEK BULGUSU ve kapatılan kusur şudur: `eslestirme.deneme_say` ÇAĞIRANIN transaction'ında yazar, kaynağın `_bagla_akisi`ı gönderim hatasında `db.rollback()` çağırır ve o rollback SAYACI DA GERİ ALIYORDU, yani düşen bir sağlayıcıyla sınırsız `BAĞLA` denemesi hız sınırına HİÇ takılmadan tekrarlanabilirdi — sınır tam da saldırının işe yaradığı koşulda kayboluyordu; çare `_sayaci_kalicilastir`dır ve yalnız GERİ ALINAN dallarda çağrılır, net etki HER ZAMAN TEK ARTIŞTIR (rollback içerideki artışı siler, ayrı oturum birini geri koyar) ve bu SQLite'ta da PostgreSQL'de de `== 1` olarak ölçüldü, ">= 1" değil; (3) DANIŞMAN DEĞİL KÖPRÜ ve YAZMA YOK — bağsız numaraya kaynak kendi `danisman`ını soruyordu, burada aynı yeri WA3-core'un `kopru.KopruIstemcisi`si tutuyor ve köprü KAPALIYKEN (varsayılan) kullanıcı `BAĞLA <KOD>` rehberliği alıyor (SESSİZLİK DEĞİL, çünkü bu depoda somut bir çıkış yolu VAR), tahsilat (yazma) niyeti ise `WA4_MESAJI` alıyor çünkü taslak/onay defteri (`whatsapp_pending_actions`) bu depoda YOK ve onay adımı olmadan bir tahsilat, kullanıcının onaylamadığı bir para kaydı demekti; NİYETTE İKİ DÜZELTME ve İKİSİ DE `docs/whatsapp/WA3_BILINEN_SINIRLAR.md`de adıyla yazılı sınırların KAPANIŞI: (a) `_terim_cikar` sözlük kökünü artık YALNIZ TAM EŞLEŞMEYLE eliyor (`katli in SORU_SOZLUGU`), kök+ek ile DEĞİL — `YILMAZ` = `YIL` + üç harflik ek olarak okunup DURAK KELİME sayılıyor ve soyadı arama teriminden DÜŞÜYORDU; ölçüldü: `bakiye ahmet yılmaz` → `musteri = "ahmet yılmaz"`, aynı sınıftan `Günay`/`Sonat` da korunuyor ve WA3-core'da da korunan `Aydın` BOZULMADI; niyet kökleri HALA kök+ek eşleşiyor (`borcu`/`stoğundan`/`cirosu` niyetin KENDİSİDİR) ve bu ayrım bilinçli; ödenen bedel de ölçüldü ve kabul edildi (yalnız sözlükte olup niyet köklerinde olmayan bir kelimenin ekli hâli arama terimine karışabilir — yön güvenlidir: fazla bırakmak "bulunamadı", fazla eleme YANLIŞ MÜŞTERİNİN BAKİYESİ demekti); (b) çıplak `tahsilat` artık REHBERLİĞE gidiyor, sessizce dönem özetine DEĞİL — ÜÇ koşul birden aranıyor (açık `tahsilat`/`tahsil` kökü VAR, açık dönem YOK, `TAHSILAT`ın kendisi dışında niyet kökü YOK) ve üçü de mevcut akışları KORUMAK için: `geçen ay tahsilat` hâlâ dönem özetidir (WA3-core'un pini), `tahsilat ve ciro` hâlâ dönem özetidir; ORTAK GÖVDE DIŞARI ALINDI, KOPYA YAZILMADI: `baglam.firma_komutu` (yalnız `FİRMA LİSTELE`/`FİRMA SEÇ`) ayrıldı ve `komut_isle` de artık onu çağırıyor — işçi `komut_isle`ı kullanamazdı çünkü o `BAĞLA`yı da işler ve bağlı kullanıcıya `KOMUT_ANLASILMADI_MESAJI` döner, yani niyet akışına yer bırakmaz; KAPILAR: Core sorgu envanteri 160→165 (BEŞ sorgu, TEK dosya `service.py`; select 103→105, update 47→50, delete 10 SABİT, parmak izi `0a32c53d`→`e899452d`, `UNRESOLVED_ALLOWLIST` BÜYÜMEDİ), `VARIABLE_ARG_ALLOWLIST` 3→4 (`_sonlandir`ın `**degerler` yayılımı; `notifications/service.py::_finalize` ile AYNI desen ama gerekçe DAHA GÜÇLÜ: genişletilecek bir kiracı sınırı YOK çünkü hedef bir platform tablosudur, yerine açıkta duran yüklem KİRA CAS'idir), dinamik SQL toplamı 268→269 ve dosya listesine `app/whatsapp/yurutucu.py` girdi (1 çağrı), DÖRT UÇ DOSYASININ dinamik-SQL parmak izi yenilendi (`customers` `27b663df`→`e1f239d5`, `dashboard` `7176ad90`→`825802e2`, `products` `ff6f0a6c`→`2cd36a62`, `reports` `8bec3fd7`→`efec49d6`) ve gerekçe her birinde AYNI: SQL METNİ KIMILDAMADI, değişen SORGUNUN YAŞADIĞI FONKSİYONDUR, `pins/pg_twins.txt` 118→119, `test_settings_platform_scope.py` yazıcı listesi 2→3 (`app/whatsapp/zamanlayici.py`; gerekçe `field_stok_zamanlayici` ile aynı sınıftan ve burada DAHA GÜÇLÜ — gezilen kuyruk zaten platform tablosudur, kalp atışı için bir kiracı UYDURULAMAZ bile), `test_v2_9_report_mixed_date_contract.py::test_top_customer_join_is_tenant_consistent` kaynağı `summary`den `ozet_verisi`ye çevrildi (uçtan okumaya devam etseydi kapı, yüklem düşürülse bile YEŞİL kalırdı: uçta artık hiç SQL yok) ve ucun o gövdeyi GERÇEKTEN çağırdığı ayrıca çivilendi; ROTA ENVANTERLERİ KIMILDAMADI ve bu bekleniyordu — işçi İÇSELDİR, yeni uç YOK: GET 185, rota sözleşmeleri 389/299, `TENANT_TABLES` 119 (yeni tablo yok), `types.gen.ts` ve `frontend/` HİÇ DOKUNULMADI; TESTLER `tests/test_wa3_worker.py` (33: beş STATİK kapı + kira/CAS + akış + yürütücü + sağlayıcı + zamanlayıcı) ve `test_wa3_worker_postgresql.py` (6, GERÇEK PostgreSQL 16'da koştu); PG İKİZİNİN ÜÇ GEREKÇESİ ve üçü de yalnız orada görünür: (i) kira/CAS GERÇEK eşzamanlılıkta — yirmi işçi AYNI satıra giriyor, TAM BİRİ jeton alıyor ve `attempt_count` TAM BİR artıyor (kaybeden UPDATE'ler satıra HİÇ dokunmuyor; bu üçüncü iddia olmasaydı CAS'i düşüren mutant sayacı yirmi artırıp deneme tavanını TEK TURDA tüketir ve mesaj DEAD olurdu), (ii) yürütücünün SQL'i PostgreSQL lehçesinde de koşuyor — tarih normalizasyonu (`substr` / `SUBSTRING FROM..FOR`), boolean karşılaştırması (`COALESCE(active, TRUE)=TRUE`) ve `GROUP BY` İŞLEVSEL BAĞIMLILIĞI SQLite'ta GÖRÜNMEZ ve yedi araç orada GERÇEK satır görerek koşuyor (stok 1, kritik 1, satılan adet 3, belge 1), (iii) hız sınırı sayacı GERÇEKTEN ayrı bir bağlantıda — SQLite'ta "ayrı oturum" aynı dosyayı açar; H9 — WA2'NİN YARIŞ TESTİ ISINMA TURUYLA YENİDEN ÖLÇÜLDÜ ve SONUÇ İSTENENDEN FARKLI ÇIKTI, kayıt bu yüzden burada: her thread bariyerden ÖNCE bir gidiş-dönüş yapıyor ve yarışın KENDİ motoru var (yirmi bağlantılık havuz), çünkü `motor` varsayılan havuzla kuruluyor (5+10=ON BEŞ) ve ısınma o havuzla bariyeri `BrokenBarrierError` ile düşürüyor (ÜÇ koşuda ÜÇÜ DE ölçüldü); ama "CAS kaybı ≥ 15" İDDİASI ÖLÇÜLDÜ ve DOĞRU DEĞİL: PostgreSQL 16'da DÖRT koşuda (ısınmalı ve ısınmasız) `FOR UPDATE` **20**, tüketim `UPDATE`i **1**, CAS kaybı **0** — `SELECT ... FOR UPDATE` yirmi thread'i SIRAYA sokuyor ve kazanan commit ettikten sonra sıradaki thread satırı YENİDEN okuyup `CONSUMED` görüyor, yani B KATMANINDA dönüyor ve CAS deyimini HİÇ GÖNDERMİYOR; hiçbir zaman doğrulanamayacak bir eşik yazmak yerine ÖLÇÜLEBİLİR OLAN çivilendi (yirmi yarış girişi, tam bir tüketim) ve sezgiye aykırı sonucun TAMAMI hem testin gövdesine hem `WA3_BILINEN_SINIRLAR.md`ye yazıldı; ısınmanın sonucu DEĞİŞTİRMEDİĞİ de dürüstçe kaydedildi (eski kurguda da 20/1) — değişiklik bir kusuru KAPATMIYOR, testin ölçtüğü şeyi ÖLÇÜLEBİLİR kılıyor; MUTASYON BATARYASI, DÖRT MUTANT, İKİ KULVARDA DA ADIYLA ÖLDÜ — (1) sayacı yalnız dış transaction'da bırakmak → `test_HIZ_SINIRI_SAYACI_DIS_ROLLBACKI_ASIYOR` (SQLite) ve `test_HIZ_SINIRI_SAYACI_GERCEK_AYRI_ISLEMDE_KALICI` (PG), (2) yürütücüde kiracı yüklemini kaydırmak → `test_KIRACI_YALITIMI_AYNI_AD_KOMSU_FIRMADA` (iki firmada AYNI ADLI müşteri, FARKLI bakiye — adlar farklı olsaydı yüklemsiz sorgu "bulunamadı" der ve testi GEÇERDİ), (3) `_claim`den `lock_token` yazımını düşürmek → SQLite'ta ON BİR adım, PG'de yirmi işçilik yarış KIRMIZI, (4) deneme tavanını yok saymak → `test_DENEME_TAVANI_ISIRIYOR_ve_DEAD` (SQLite) ve `test_SURESI_DOLMUS_KIRA_DEVRALINIYOR_ve_TAVAN_ISIRIYOR` (PG); KANONİK İZOLE SÜİT (`run_isolated_tests.py`, 427 dosya) 806 saniyede koştu: 2221 geçen, 409 atlanan, DÜŞEN İKİ DOSYA yukarıdaki iki KAPIDIR ve ikisi de aynı turda düzeltilip yeniden ölçüldü; BU TURDA BİLEREK YAPILMAYAN YEDİ ŞEY `WA3_BILINEN_SINIRLAR.md`de gerekçesi ve sabitleyen testiyle yazılı (tahsilat yazma yok, numaralı SEÇİM yok, medya yok, satırda kiracı yok, `toplam_tahsilat` yok, işçi ayrı süreç değil, köprü yalnız bağsız numaralar için); TABAN `origin/develop` `e78a466`; kayıt adı `python scripts/durum.py --sonraki 84`
