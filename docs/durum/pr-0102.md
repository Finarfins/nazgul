#102 — SEC-3 FAZ 2: `read` izni ONDOKUZ ticari okuma ucunda DARALDI (GOC YOK — `alembic/` diff'i BOS, alembic basi `20260912_0082` KIMILDAMADI, ROTA YOK, SEMA YOK, `types.gen.ts` KIMILDAMADI); #100'un OLCUM raporu (`docs/SEC-3-olcum-2026-09-09.md`) 91 GET ucunun `read`e dustugunu olcmustu ve bu tur o raporun KABUL EDILEN satirlarini UYGULUYOR: yedisi `purchases` (`/api/purchases`, somut `/api/purchases/{id}`, `last-purchase-price`, `/api/suppliers` ve DORT alt ucu), sekizi `sales` (BES fatura ucu, musteri ekstresi JSON+PDF, `/api/orders/last-sale-price`), ucu `payments` (tahsis defterinin `/payments|/orders|/charges` ucluleri), biri `stock` (`/api/warehouses/replenishment`); KURALIN YERI KURALIN KENDISI KADAR ONEMLIDIR VE BU OLCULDU: alti kural genel guvenli-metot kuralinin (`auth.py`: `if method in {"GET","HEAD","OPTIONS"}: return "read"`) USTUNE, `/api/producer-receipts` + `/api/tax-liabilities` + `/api/suppliers/.../advances` ile AYNI bolgeye yazildi — dosyanin DIBINDE duran `/api/purchases`, `/api/suppliers` ve `/api/invoices` kurallari ZATEN VARDI ama `read`in ALTINDA kaldiklari icin GET'i HIC gormuyorlardi, yani kusur "kural yok" degil "kural gec" idi; blok e-BELGE SURETI kuralinin ALTINDA duruyor ve bu da bir karar: `/api/invoices` onegi ustune konsaydi `.../einvoice/download`in AYRI gerekcesini (dis yan etki + resmi mali belge) sessizce OLU KODA cevirirdi, izin sonucu ayni kalir GEREKCE kaybolurdu; her kural `SAFE_METHODS` ile SINIRLI ve yazma cozumunun HIC degismedigi OLCULDU; MUSTERI EKSTRESI `payments` DEGIL `sales` ve izin adi SECILMEDI DEPODAN OKUNDU: `statement.py:46-68` musteri icin `sales` tedarikci icin `purchases` diyor ve `outputs.py:875` bunu PDF'te ZATEN uyguluyordu — yani bugune kadar AYNI belgenin JSON'i `depo`ya 200, PDF'i 403 donuyordu ve `EntityStatementDialog.tsx:36`/`:44` ikisini YAN YANA cagiriyordu; `payments`a baglamak depoda yazili karari SESSIZCE ters cevirirdi; `{kind}` DARALTMASI SOMUT ONEKE yazildi sablona OGRETILEREK DEGIL ve bu ayrimin OLCULEBILIR sonucu su: envanter `required_permission`i HAM yolla cagirir, `/api/{kind}/{transaction_id}` hicbir oneke uymaz, yani YIRMI yol daraldi ama envanterde `changed=19` — eksik olan satir bir TUTARSIZLIK DEGIL, kanitin YER DEGISTIRMESIDIR ve o kanit `DYNAMIC_PERMISSION_CASES` (`orders/1 -> read`, `purchases/1 -> purchases`) ile GERCEK HTTP istegindedir; `read`te BILEREK KALANLAR: `/api/customers` ve `/api/customers/{id}` (gunluk is yuzeyi; alan maskeleme SEC-3b'ye ayrildi), `/api/orders` LISTESI, `/api/products`, `imports/*/template.xlsx` (uc kardes ucun ucu de veritabanina HIC dokunmuyor), `exports/producer-logbook*` (handler'da `farm.view`; tasimak DARALTMA degil YER DEGISTIRME olurdu ve tarla rollerini vururdu), TUM `notifications/*` (handler-gated; `depo`/`rapor` BUGUN DE 403 aliyor, yani erisim kazanimi SIFIR ve tasimak denetim durusunu iki kapidan bire indirirdi) ve `/api/payment-allocations/engine-state` (tek yapilandirma boolean'i; `payments`a baglamak arayuzun "hic tahsis yok" ile "ozellik kapali"yi ayirt etmesini geri kirardi — kurali tahsis kuralinin USTUNDE, yoksa onek onu da yutar); SAYACLAR OLCULDU ARITMETIKLE TASINMADI: `EXPECTED_READ` 99 -> 80 (-19), `EXPECTED_UNDENIABLE` 114 -> 97 (-17), ciplak read 73 -> 56 (-17), korumali read 26 -> 24 (-2), `EXPECTED_AUTHENTICATED` 379 SABIT, `FARM_HERD` 41 SABIT, GET envanteri 186 SABIT (`changed=19`, `missing`/`stale` UCU DE BOS), rota sozlesmesi 391/301 SABIT, `GET_INVENTORY_FINGERPRINT` 445ebb4f -> 0e8ce4f3 ve `EXPECTED_SECURITY_FINGERPRINT` f4517670 -> d4ad9f24; -19 ile -17 ARASINDAKI IKI FARK KAZAI DEGIL: tasinan 19 ucun IKISI (`customers/{id}/statement.pdf`, `suppliers/{id}/statement.pdf`) ZATEN `GUARDED_READ`teydi ve `naked`de HIC DEGILDI, yani reddedilebilirligi HIC degistirmiyorlar — erisimleri de degismedi, degisen yalniz kapinin handler'dan MIDDLEWARE'e tasinmasi; §3.2'NIN TAHMINIYLE FARK (82/99 dedi, 80/97 olculdu) DA KAZAI DEGIL KAPSAM FARKIDIR ve ADIYLA yazildi: §3.2 ON YEDI yol icin simule edilmisti, kabul edilen tabloya SONRADAN `GET /api/orders/last-sale-price` (§5.1d simetrisi — alis ikizi `purchases`a baglandiktan sonra satis fiyatinin `read`te kalmasi ayni gerekceyi iki kardes ucta iki farkli sonuca goturuyordu) ve `GET /api/warehouses/replenishment` (E-2 bayragi) EKLENDI; ikisi de CIPLAK read'deydi, yani 82-2 = 80 ve 99-2 = 97, `guarded` icin tahmin (24) ile olcum (24) AYNI, BASKA SAPMA YOK; PARMAK IZLERININ DEGISMESI BIR KAYMA DEGIL KAPININ ISLEVIDIR: yuk `permission` + `review_reason` + `permission_cases`ten kuruluyor ve UCU DE bilerek degisti (19 izin, 19 kaldirilan `ROUTE_REASONS` girdisi, bir `permission_cases`); `ROUTE_REASONS` KALDIRMASI BIR HIJYEN KARARIDIR ve bu kayda gecti: `_build_contract` gerekceyi YALNIZ izin `read`/public/platform ise ISTER, `read` olmayan bir rotada duran gerekce HATA DEGILDIR — ama orada birakilsaydi "bu uc baseline okuma roluna acik" diyen bir metin artik DOGRU OLMAYAN bir vaat olarak dosyada dururdu; `/api/{kind}/{transaction_id}`in gerekcesi KALDI ve bu da olculdu: `has_read_case` hala DOGRU cunku `orders` kolu `read`te; CALISMA ZAMANI KANITI YENI BIR DOSYADIR (`backend/tests/test_sec3_read_daraltma.py`, 125 test) ve VARLIK SEBEBI FAZ 1'IN KENDI YAZDIGI SINIRDIR (§7: "hicbir rol icin GERCEK HTTP istegi atilmadi") — mevcut UC kapinin UCU DE `required_permission`i DOGRUDAN cagirir, yani `required_permission` dogru cevabi verip middleware onu KULLANMASA ucu de YESIL kalirdi; yeni dosya tohumlanmis bir uygulamaya BES ROLUN BESIYLE de GERCEKTEN giris yapip 19 yolun her birine GERCEK GET atiyor (5x19 parametrize matris) ve TOHUMLAMA GERCEKTIR: musteri, tedarikci, depo, urun, ALIS belgesi, SATIS belgesi, tahsilat, makine, is emri ve ONDAN URETILEN GERCEK FATURA aciliyor, izinli hucrelerin 19/19'u GERCEK 200 (hicbir hucre 404 uzerinde kosmuyor ve bu AYRICA civilendi, cunku beklenenlerin HEPSI 404 olsaydi matris yine yesil kalirdi); beklenen 200'ler ELLE YAZILMADI `admin` (`*`) ile OLCULDU ve her kosuda yeniden dogrulaniyor; matrisin izinleri `EXPECTED_GET_PERMISSIONS` ile UC UCA karsilastiriliyor, yani iki dosya BAGIMSIZ kayamaz; KONTROL SATIRLARI (`/api/customers`, `/api/orders`, `/api/products`, `/api/work-orders`) `depo` ve `rapor` icin 200'de CIVILENDI ve gerekcesi ACIK: FAZLA DARALTMA en kolay yanlisti ve HICBIR SAYAC onu gosteremezdi — `EXPECTED_READ` yine duserdi, yani "iyi" gorunurdu; `{kind}` kaniti GERCEK istekle (`rapor` -> `GET /api/purchases/1` 403, `GET /api/orders/1` 200) ve `/api/documents/{kind}/...` handler kapisinin GERCEKTEN atesledigi ayrica olculdu (`depo` alis ciktisini alir, satis ciktisini ALAMAZ); UC MUTASYON CANLANDIRILDI (`app.main.required_permission` yamalanarak — `main.py:27` adi KENDI ad alanina import ediyor ve `:447`de oradan cagiriyor, `app.auth`i yamalamak middleware'i HIC etkilemezdi ve mutant SESSIZCE olmezdi) ve her biri HANGI matris hucresini oldurdugunu ADIYLA yaziyor: M1 `/api/invoices` kurali SILINIR -> `rapor` fatura listesini 200 okur (`[/api/invoices-rapor]`), M2 `/api/purchases` kurali genel `read` kuralinin ALTINA kayar -> `satis` 200 (`[/api/purchases-satis]`) ve `depo` hucresinin OLMEMESI mutantin KENDI KANITIDIR cunku `depo` `read` de `purchases` de tasiyor, yani "hala 200" olmak kuralin calistigini KANITLAMAZ, M3 `sales` kuralindan `/statement` soneki DUSER -> `depo` musteri ekstresini 200 okur (`[/api/customers/{id}/statement-depo]`); ucu uc AYRI kirilma bicimini kapsiyor: kuralin SILINMESI, YERI, KAPSAMI; URUN KARARLARI (berkay onayi, orkestrator uzerinden): A-1 `/odemeler` — tedarikci odemesi bir ALIS-TARAFI eylemidir, tedarikci entity turu ve `/suppliers` cagrisi `can('purchases')` arkasina alindi, `satis`e `purchases` VERILMEDI ve backend `POST /api/payments` `payments`ta KALDI; `?new=supplier` kisayolu da AYNI kapidan geciyor cunku menuden gizlenip URL'den tetiklenebilen bir cagri sayfayi yine 403'e sokardi — `/alacaklar`da bir kez yasanan olu-madde kusurunun aynisi; A-2 `rapor` `/alislar`, `/tedarikciler*` ve `/urunler/:id`in tedarikci panelini KAYBEDIYOR, yuzeyi `/raporlar/*` altinda ve FAZ 1'de olculmustu ki `PurchaseComparison.tsx:57` tedarikci listesini `/api/suppliers`den DEGIL kendi rapor yanitindan okuyor, yani `/raporlar/tedarikci-karsilastirma` ETKILENMIYOR; C-1 `/is-emirleri/:id` — SAYFA DEGIL YALNIZ FATURA PANELI `can('sales')` arkasina alindi, `depo`/`rapor` is emrini fatura paneli OLMADAN acar ve parca/iscilik/ek/durum akisina DOKUNULMADI; DURUST NOT: `findInvoiceForWorkOrder` bugun YALNIZ `generateInvoice`den cagriliyor ve o zaten `canWrite=can('sales')` ile korunuyordu, yani raporun "depo/rapor fatura panelini 403 ile kaybeder" ifadesi o cagri icin FAZLA GENISTI — kapi yine de fonksiyonun BASINA konuldu, ileride baska bir yerden cagrilirsa kapisiz kalmasin diye; N+1 sayfalama (liste sayfalanip her kalem icin ayrica `/invoices/{id}`) bu isin KAPSAMI DISINDA ve `TODO(SEC-3)` olarak birakildi; NAV LOCKSTEP: `/alislar` + `/tedarikciler(+/:id)` -> `purchases`, `/faturalar(+/:id)` -> `sales`, `/tahsis-defteri` -> `payments`; `/musteriler*` BILEREK `read`te KALDI ve ayrisma SAYFA KODUNDA DEGIL ROTA KAYDINDA ifade edildi — `Entities.tsx` ve `EntityDetail.tsx` TEK bilesendir ve ucu `type` proposundan secer (`Entities.tsx:100`, `EntityDetail.tsx:67`), yani ayni kod iki AYRI rota kaydiyla iki farkli izne baglaniyor ve bilesen icine bir `can()` dali YAZILMADI; `satis` `/musteriler`de 403 ALMAZ ve bunun bekcisi eklendi; OLCULEN NAV SONUCLARI (tahmin edilmedi; bir grup ICINDEKI HERHANGI bir madde acikca gorunur, yani "iki madde kaybettim" ile "grubu kaybettim" arasinda dogrudan iliski YOK): `satis` 11 -> 10 ALIS grubunu kaybetti (bes maddesinin hicbiri acik degil), `depo` 9 -> 8 FINANS grubunu kaybetti — depo'ya acik TEK Finans maddesi `/tahsis-defteri` (`read`) idi ve kaybolan sey OLU BIR MENU BASLIGIYDI cunku depo kasa/banka/alacak maddelerinin HICBIRINI zaten acamiyordu, `rapor` 10'DA SABIT ve bu ANLAMLI cunku o da dort sayfayi kaybetti ama IKI GRUBU DA kaybetmedi (Alis grubunda iki `reports` maddesi, Finans grubunda `/raporlar/alacak-yaslandirma` duruyor), yani rapor rolunun yuzeyi `/raporlar/*` altina CEKILDI SILINMEDI ve A-2'nin gerekcesi tam olarak buydu; `muhasebe`/`yonetici`/`admin` 11'de SABIT (uc izni de tasiyorlar); `AppShell.test.tsx`in "baslik o rolde gorunur ILK maddeye gider" testi SILINMEDI TASINDI: senaryo `depo` + Finans grubuyla yazilmisti ve grup ortadan kalkinca senaryo da kalkti, AYNI OLGUYU hala sergileyen `rapor` + Finans grubuna gecirildi (olculen davranis degismedi, olculdugu yer degisti); `rota-kapsam-sozlesmesi.test.ts` DEGISMEDEN geciyor (rota kapsami e2e modelini olcuyor, izinleri degil); TESTLER `backend/tests/test_sec3_read_daraltma.py` (YENI, 125) + guncellenen uc kapi (`test_route_get_permission_inventory.py` 19 satir + sayim notu + parmak izi, `test_route_security_contracts.py` 19 `ROUTE_REASONS` girdisi + `DYNAMIC_PERMISSION_CASES` + parmak izi, `test_authorization_population_reconciliation.py` iki sayac + `NAKED`/`GUARDED` kumeleri + bolunme iddialari) + `test_ci_gates.py` => izole kosucuda 5/5 dosya 172 test YESIL; frontend `npm test -- navigation rota-kapsam` 2/2 dosya 46 test, `Payments.test.tsx` (+2 A-1 testi: secenek GORUNMUYOR ve istek ATILMIYOR — ikincisi olmadan birincisi yetmezdi) ve `AppShell.test.tsx` 27 test, `npx tsc -b` TEMIZ; URETIM DEGISIKLIGI DORT DOSYA (`backend/app/auth.py` +134/-9, `frontend/src/navigation.tsx`, `frontend/src/pages/Payments.tsx`, `frontend/src/pages/WorkOrderDetail.tsx`); PINLER YENIDEN OLCULDU: rota sozlesmesi 391/301, GET envanteri 186, `TENANT_TABLES` 120, cekirdek sorgu envanteri, dinamik-SQL parmak izleri, alembic basi `20260912_0082`, `pins/pg_twins.txt` ve `pins/alt_surec_sql.txt` HICBIRI KIMILDAMADI (SQL EKLENMEDI ve yeni test alt surec KULLANMIYOR — uygulama AYNI surecte `TestClient` ile ayaga kalkiyor), `types.gen.ts` DEGISMEDI (OpenAPI degismedi: izin degisikligi sema uretmez); kayit adi `python scripts/durum.py --sonraki 102`
