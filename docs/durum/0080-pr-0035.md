#35 — İMAJ KAPISI SHARD'DAN ÇIKTI, `container` JOB'INA GİRDİ: gerekli bir shard artık 60-180 s'lik docker build'e BAĞLI DEĞİL. TABAN `40921fb8` (develop; PR #29 indikten SONRA ölçüldü: `git rev-parse origin/develop` -> `40921fb8` ve worktree `HEAD` -> `40921fb8`, yani ŞİMDİ aynı). SEBEP ÖLÇÜLDÜ: koşu `33812772540` attempt 1'de shard 0 `[94/94] TIMEOUT tests/test_uretim_imaji_kapisi.py (180.1s, 0 test)` bastı — yani kapı yalnız yavaş değil, `--timeout 180` altında SIFIR test raporlayarak kırmızı yanıyordu; aynı dosya koşu `33806477095`'te 62,2 s sürdü. Dosya iki imaj kuruyordu ve geride ~1 GB etiket bırakıyordu (ölçüldü, yerel: `yerel-hesap-pro:uretim-imaji-kapisi` 500 MB + `yerel-hesap-test:uretim-imaji-kapisi` 526 MB). ÜÇ DOSYA DEĞİŞTİ (+ bu kayıt): `backend/pytest.ini`, `backend/tests/test_uretim_imaji_kapisi.py`, `.github/workflows/ci.yml`. (1) DESEÇİM TEK SİTEDEN YAPILDI: `backend/pytest.ini`'de `addopts` -> `--strict-markers -p no:ddtrace -m "not docker_imaj"` ve `markers`'a `docker_imaj` kaydı (`--strict-markers` açık olduğu için kayıt ŞART, yoksa HER koşu hata verir). SİTE SEÇİMİ KEYFİ DEĞİL: `run_isolated_tests.py`'nin İKİ ayrı pytest çağrısı vardır — per-file koşum (~:303) ve canonical `--collect-only` (~:555) — ve `aggregate_isolated_test_reports.py` `shard node union mismatch; missing=...` ile FAIL-CLOSED'dır, yani canonical manifest'in düğüm kümesi shard raporlarınca TAM kapsanmak zorundadır. YALNIZ per-file çağrısına `-m` eklemek iki docker düğümünü canonical'da bırakıp hiçbir shard raporuna sokmaz ve `backend-quality-aggregate`'i KIRMIZI yapardı; `pytest.ini` her iki çağrının da miras aldığı TEK yerdir. SİMETRİ ÖLÇÜLDÜ: `python -m pytest --collect-only -q tests/test_uretim_imaji_kapisi.py` -> `1/2 tests collected (1 deselected)`, per-file koşum da `1 passed, 1 deselected` — iki taraf AYNI tek düğümü görür. KOMUT SATIRI `-m`'in `addopts`'u EZDİĞİ AYRICA ÖLÇÜLDÜ (izole dizinde, sonra gerçek depoda): `addopts` ile 1 passed / 2 deselected, `-m docker_imaj` eklenince 2 passed / 1 deselected — son `-m` kazanır, bu yüzden `container` job testi geri seçebilir. (2) `test_production_image_contains_no_test_assets` SİLİNDİ, TAŞINMADI: aynı iddiayı `ci.yml`'deki `Verify production image has no test assets` adımı ZATEN ölçüyor ve bunu job'ın ZATEN KURDUĞU `yerel-hesap-pro:${{ github.sha }}` imajı üzerinde, YENİ BİR BUILD OLMADAN yapıyor; iki tarafın iddiası birebir aynıdır (aynı yedi yasaklı yol + aynı `find /app/backend -name 'test_*.py' | wc -l == 0`). YANİ SHARD'DAKİ KOPYA, ÜRETİM İMAJINI SIFIRDAN İKİNCİ KEZ KURAN DAHA YAVAŞ VE DAHA KIRILGAN BİR İKİZDİ. GÜVENDİĞİM KAPSAM VARSAYILMADI, İKİ YÖNDE ÖLÇÜLDÜ: `ci.yml`'deki kabuk birebir kopyalanıp gerçek imaja karşı koşuldu -> `exit=0`; `FROM yerel-hesap-pro:uretim-imaji-kapisi` üstüne `RUN echo "def test_x(): pass" > /app/backend/test_sizinti.py` konarak üretilen doctored imajda -> `Uretim imajinda 1 adet test_*.py kaldi`, `exit=1`. `YASAKLI_URETIM_YOLLARI` sabiti bu silme sonrası ÖLÜ kaldı (tek geçen yer kendi tanımıydı, ölçüldü) ve kaldırıldı; liste `ci.yml` adımının içinde YAŞAMAYA DEVAM EDER. (3) `test_stage_still_contains_tests` `@pytest.mark.docker_imaj` ile işaretlendi ve `container` job'ında geri seçilir. O JOB'IN İKİ EKSİĞİ ÖLÇÜLDÜ VE KAPATILDI: `container`'da `defaults.run.working-directory` YOKTUR (adıma `working-directory: backend` konuldu) ve PYTHON KURULU DEĞİLDİR (`actions/setup-python@v5` eklendi); ayrıca `pytest.ini` `asyncio_mode = auto` istediği için kurulum `pytest pytest-asyncio`dur — dosyanın kendi importları yalnız `shutil`, `subprocess`, `pathlib`, `pytest` olduğu için TAM `requirements-dev.txt` GEREKMEZ. `--target test` imajını TESTİN KENDİSİ kurar (sabit etiketle, tek kurulum); ayrı bir build adımı ve `TEST_IMAJI_ETIKETI` env geçişi EKLENMEDİ çünkü o imaja ihtiyaç duyan tek test artık budur. ZAMANLAR (yerel, docker 29.6.1): shard'da dosya ÖNCE 17,63 s -> SONRA **0,11 s** (`1 passed, 1 deselected`; duvar saati 0,605 s), `container` adımı 7,79 s yeşil. YEREL ÖNCE-SAYISI CI İLE KIYASLANAMAZ VE KIYASLANMADI: yerel docker katman önbelleği SICAKTI (iki etiket zaten diskteydi, ölçüldü), CI'nın SOĞUK gerçeği 62,2 s / 180,1 s TIMEOUT rakamlarıdır ve otorite onlardır. KAPI İKİ YÖNDE GÖSTERİLDİ: `Dockerfile`'ın `test` stage'ine `RUN rm -rf /app/backend/tests` enjekte edilince adım `AssertionError: test stage'de /app/backend/tests yok ya da sayım çalışmadı` + `assert 1 == 0` ile KIRMIZI, gerçek ağaçta YEŞİL; `Dockerfile` `cp` ile geri alındı ve ağaç temiz doğrulandı (`git status --porcelain` boş). TEMİZLİK: adım `if: always()` ile İKİ etiketi de `docker rmi -f` eder; ölçüldü, sonrasında `docker images | grep uretim-imaji-kapisi` HİÇBİR SATIR döndürmez (`temiz: kapi etiketi kalmadi`). Adım grep bir şey bulursa `exit 1` ile fail-closed'dır. ÇİVİLER YEŞİL, ÖLÇÜLDÜ: `tests/test_ci_gates.py` -> 16 passed; `deploy/ci-required-contexts.json` DEĞİŞMEDİ (`git diff origin/develop --stat` sıfır satır) ve 17 bağlam 17 kalır — YENİ JOB EKLENMEDİ, yalnız mevcut `container` job'ına ADIM eklendi, bu yüzden `test_ci_gates.py:80` (tam 17) ve `:173` (shard matrisi `[0,1,2,3]`) tasarımca etkilenmez; `backend/test_production_deployment.py` -> 9 passed. `ci.yml` KAPSAMI DAR TUTULDU, ÖLÇÜLDÜ: `git diff origin/develop -- .github/workflows/ci.yml | grep -c '^@@'` -> **1**, tek hunk `@@ -983,6 +983,29 @@` ve tamamı `container` job'ının içindedir (job :890'da başlar); başka hiçbir bölgeye dokunulmadı. `backend/app` ve `Dockerfile` DEĞİŞMEDİ. ÖLÇÜLMEDİ, AÇIKÇA: (a) yeni `container` adımının CI'daki SOĞUK süresi — `--target test` build'i runner'da ilk kez kurulacak, yerel 7,79 s SICAK önbellekten gelir ve o sayı CI için GEÇERLİ DEĞİLDİR; (b) `container` job'ının 12 dakikalık `timeout-minutes` bütçesinin yeni adımla hâlâ yeterli olduğu — bütçe artırılmadı, ilk koşuda izlenmelidir; (c) `pip install pytest pytest-asyncio`nun pinlenmemiş olması (job'da pip cache yok, sürüm serbest); (d) `e2e` ve `publish-image` job'ları; (e) shard sürelerinin TOPLAM etkisi — yalnız bu dosyanın maliyeti ölçüldü, shard 0'ın yeni toplam süresi ÖLÇÜLMEDİ.
