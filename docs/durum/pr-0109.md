#109 — H16 IS EMRI FATURASI TEK SORGUYLA BULUNUR (N+1 KAPANDI), GOC YOK ve bu OLCULDU (`git diff origin/develop HEAD -- backend/alembic` BOS, alembic head `20260912_0082` KIMILDAMADI): `frontend/src/pages/WorkOrderDetail.tsx` yinelenen fatura yolunda (`POST /invoices/generate` -> 409 "Bu is emri icin fatura zaten olusturulmus." `invoice_service.py:116` -> mevcut faturaya yonlendir) `findInvoiceForWorkOrder` BUTUN `/api/invoices` listesini `page_size=200` ile sayfa sayfa geziyor, SONRA HER kalem icin ayrica `/invoices/{id}` cagiriyordu — 5 000 faturali bir firmada tek bir sayfayi acmanin bedeli 25 liste + 5 000 detay istegiydi; sebep UCTAYDI: `work_order_id` ne sorgu parametresiydi ne de liste yanitinin bir alani, yani istemcinin tek caresi her kaydi tek tek okumakti; `GET /api/invoices` (`routers/invoices.py::list_invoices`) artik istege bagli `work_order_id` suzgeci tasiyor ve suzgec ucun `status`/`invoice_type`/`currency` icin ZATEN kullandigi KAPALI mekanizmadan geciyor — `conditions.append("work_order_id=:work_order_id")`, sutun adi KAYNAKTA SABIT bir dizgedir (f-string bile DEGIL, yani yeni bir interpolasyon noktasi ACILMADI) ve deger BAGLI PARAMETREDIR, istek verisi SQL METNINE HIC girmez; kiraci yuklemi `company_id=:cid` `conditions`in ILK ogesi olarak DURUYOR ve dokunulmadi; `work_order_id:int|None=Query(None,gt=0)` sifir/negatif kimligi 422 ile SORGUDAN ONCE keser; endeks ZATEN VARDI (`ix_invoices_company_work_order`, `company_id`+`work_order_id`, goc `20260718_0012`) yani yeni bir indeks de gerekmedi; arayuz tarafinda `findInvoiceForWorkOrder` ve `TODO(SEC-3)` notu SILINDI, yerine TEK cagri geldi (`api.get('/invoices',{params:{work_order_id,page:1,page_size:1}})` -> `items[0]`) ve `can('sales')` kapisi SEC-3'un biraktigi gibi KALDI (`canWrite`, `canSeeInvoice`, fatura paneli kapisi — hicbirine dokunulmadi, C-1 yorumunun panel gerekcesi duruyor, silinen yalniz `findInvoiceForWorkOrder`i anlatan iki paragraf ve N+1 TODO'sudur); REDDEDILEN ALTERNATIF ve GEREKCESI: mevcut faturanin kimligini 409'un `detail` metnine gommek — makine tarafindan okunacak bir degeri INSAN icin yazilmis bir hata dizgesine karistirir ve metin degistigi gun bu yol SESSIZCE bozulurdu, oysa bir sorgu parametresi kontratin kendisidir ve `types.gen.ts` uzerinden derleyiciye gorunur; KAPILAR: yeni `backend/tests/test_h16_fatura_is_emri_filtresi.py` (8 test, ALT SUREC ACMIYOR — `tests/pins/alt_surec_sql.txt` DEGISMEDI ve bu dogrulandi) gercek uygulamayi ayaga kaldirip IKI firma aciyor, her birinde GERCEK is emri COMPLETED'a getirilip GERCEK fatura uretiliyor ve kimlikler UMUT EDILMIYOR yanitlardan OKUNUYOR: (a) suzgec YALNIZ o is emrinin faturasini donduruyor (2 is emri / 2 fatura, suzgecsiz `total==2` suzgecli `total==1`), (b) baska firmanin `work_order_id`si -> BOS liste ve simetrik yonde de bos, ayrica B kendi kimligiyle sordugunda faturasini GORUYOR (yani bos liste "suzgec hic calismiyor"un degil KIRACI SINIRININ sonucu), (c) bilinmeyen kimlik -> 200 + BOS (404 DEGIL: liste ucu bir KOLEKSIYONdur), (d) `0`/`-1`/`-999`/metin kimlik -> 422; MUTASYONLAR ELLE KOSULDU ve agacin BIR KOPYASINDA (calisma agaci in-place DEGISTIRILMEDI, izole kosucu dosyalari es zamanli kosuyor): `conditions`ten `"company_id=:cid"` dusurmek 2 failed verdi ve ISTENEN kapiyi `test_baska_firmanin_is_emri_kimligi_BOS_liste_donduruyor` ADIYLA oldurdu, `work_order_id` kosulunu hic eklememek 4 failed verdi ve `test_suzgec_YALNIZ_o_is_emrinin_faturasini_donduruyor` oldu, `gt=0`i kaldirmak 3 failed verdi ve `test_sifir_ve_negatif_kimlik_422[0/-1/-999]` oldu; `frontend/src/pages/WorkOrderDetail.invoice.test.tsx` duplicate yolunda `/invoices` GET'inin TAM BIR KEZ ve `work_order_id` suzgeciyle atildigini VE HICBIR `/invoices/{id}` GET'i atilmadigini birlikte olcuyor (ikincisi olmadan listenin tek cagriya inmesi bir detay firtinasini gizleyebilirdi), fatura bulunamazsa ozgun 409 hatasinin gosterildigi ikinci senaryo da eklendi; SAYACLAR OLCULDU, ARITMETIKLE DEVRALINMADI: rota kontrati 391/301 KIMILDAMADI (ayni operasyon, yalniz yeni bir sorgu parametresi — yeni yol ya da yeni operasyon YOK), GET izin envanteri 186 SABIT, `TENANT_TABLES` 120 SABIT (WA1/WA2/WA4 pinleri yesil), alembic head pinleri `20260912_0082`, dinamik-SQL envanterinde `backend/app/routers/invoices.py` CAGRI SAYISI 2 -> 2 ve TOPLAM 269 -> 269 (eklenen sey bir `text()` cagrisi DEGIL, var olan `conditions` listesine BIR KOSULDUR) — kimildayan TEK sey dosya AST'sinin TAMAMINDAN turetilen parmak izidir, `7fbc9c1f75fe7bc59a9a2376bbcf9f5c1d6c5e8f6f12c880a0b41b68690215d9` -> `8ecf3bf692472989540c2fcc9b0522be24577f24309d8297fcef96aacb86c204` (CPython 3.12.10) ve gerekcesi pin girdisine YAZILDI; `frontend/src/api/types.gen.ts` deponun KENDI ureticisiyle yeniden uretildi (`python backend/export_openapi.py -o backend/openapi.json` -> `npm run types:gen` -> `python backend/tools/capture_frontend_fixtures.py`) ve diff TEK SATIR: `work_order_id?: number | null` — contract-drift isi bunun kanitidir; YEREL KOSULAR: `run_isolated_tests.py --workers 4` ile bes dosya (yeni H16 testi + `test_enterprise_invoices.py` + rota kontrati + GET envanteri + `test_ci_gates.py`) 5/5 PASS 50 test, ayrica ikinci bir turda `test_tenant_scoping_guard.py` + `test_backend_encoding.py` + `test_sec3_read_daraltma.py` + `test_core_query_inventory.py` + `test_alembic_chain_integrity.py` 5/5 PASS 218 test ve WA1/WA2/WA4 3/3 PASS 85 test; `npm test -- --run` 89 dosya / 629 test PASS, `npm run build` (`tsc -b && vite build`) temiz, `npm run lint` 0 hata; IS KENDI WORKTREE'MDE yapildi (`F:\nazgul-h16`, `git worktree add ... origin/develop`) ve `F:\nazgul` calisma agacina DOKUNULMADI — orada baska bir canli oturum var; taban `origin/develop` @ 8cfa7a0 (#104 birlesmesi), head `ddf28bb`, `git diff origin/develop HEAD -- backend/app backend/alembic | git hash-object --stdin` = `8b096eddff55d201a4835f6494be77af388818d7`, `git diff --stat origin/develop HEAD` ALTI dosya 344 ekleme 27 silme; kayit adi `python scripts/durum.py --sonraki 109`
