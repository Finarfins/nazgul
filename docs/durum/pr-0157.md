#157 — H71 platform ortak.tsx BÖLME (frontend-only, davranış değişikliği yok, backend/types.gen.ts/rota envanteri 80 DOKUNULMADI): 9.6 KB'lık `pages/platform/ortak.tsx` altı modüle bölündü — `ortak/veri.ts` (usePlatformVerisi, useGecikmeliDeger, tipler), `cerceve.tsx` (PlatformBaslik, YetkiYok, HataPaneli), `bildirim.tsx` (useEylemBildirimi), `eylem.tsx` (EylemDugmesi + can('platform') kapısı, ZATEN_BU_DURUMDA), `bicim.ts` (tarihSaat, yasMetni), `tablo.tsx` (Sayfalama); gövdeler harfi harfine (140 kod satırının 139'u aynen, tek fark ithalatçısız `SAYFA_BOYUTLARI`nın dışa aktarımının kalkması); yedi ithalatçı doğrudan modüllere geçti, barrel YOK (ithalatçı kalmadı); modül sınırı react-refresh kuralına göre çizildi, lint uyarısı 883 -> 877, 0 hata; yeni test yalnız doğrudan sınanmayan iki modüle (`bicim.test.ts` 7, `tablo.test.tsx` 3); tam vitest 111 dosya / 893 test yeşil (develop 109/883), tsc -b ve typecheck:e2e temiz — <merge tarihi>
