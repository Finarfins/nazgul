#167 — H86 PLATFORM tarihSaat SAAT DİLİMİ + PlatformEylemleri TEST BAŞLIĞI (yalnız frontend, backend/göç YOK, types.gen.ts dokunulmadı): `ortak/bicim.ts` `tarihSaat` makine TZ'si yerine Europe/Istanbul duvar saatiyle yazar (önce TZ=Etc/GMT-12 altında 15.06 12:00Z → "16.06.2026 00:00:00" kırmızıydı; çağıranlar Companies/Security/Users yalnız tablo hücresi); `bicim.test.ts` desen yerine tam dize, TZ=UTC/Etc/GMT-12/Etc/GMT+12 it.each (önce 3/9 kırmızı, sonra 9/9); `PlatformEylemleri.tsx` hiç olmadı — test başlığı sınananı (`ortak/eylem.tsx` EylemDugmesi, dört sayfa) yazar, H71 ile bölünen `ortak.tsx` göndermesi düzeltildi; tam vitest --testTimeout=30000 115/115 dosya 927/927 test; lint 0 hata; tsc -b yeşil — <merge tarihi>
