#13 — ROTA KAPSAMI ARTIK BEYAN DEĞİL SÖZLEŞME: #8 kapsamı 32/70'ten 62/70'e taşımıştı ama kendi KABUL EDİLEN ARTIK bölümünde iki deliği adıyla yazmıştı — (1) `OTURUMLU_ROTALAR` listesinden bir rota eksiltmek süiti YEŞİL bırakıyordu (30 geçti) ve (2) üretim döngüsüne `test.skip` koymak da YEŞİL bırakıyordu (28 atlandı, 3 geçti); ikisinin de sebebi aynıydı: kapsamın kendisi hiçbir yerde SAVUNULMUYORDU, yalnız bir dosyanın içindeki bir dizide BEYAN ediliyordu; bu tur o beyanı sözleşmeye çevirir ve mekanizmayı İKİYE böler — STATİK yarı `frontend/src/rota-kapsam-sozlesmesi.test.ts` (G1-G7, vitest) envanterin App.tsx ve diskteki spec dosyalarıyla tutarlılığını ölçer, ÇALIŞMA ZAMANI yarısı `frontend/e2e/rota-kapsam-raportoru.ts` (R1-R4, Playwright raportörü) envanterin bildirdiği testlerin GERÇEKTEN koştuğunu ve GEÇTİĞİNİ ölçer; ayrım zorunlu, çünkü statik bir test "bu test koştu mu" diyemez ve bir raportör de App.tsx'i saymak için tarayıcı açamaz; TEK KAYNAK `frontend/e2e/rota-envanteri.ts`: App.tsx'in 70 adlandırılmış rotasının HEPSİ burada bir kez görünür ve ÜÇ tasniften TAM OLARAK BİRİNİ alır — `kapi` {tur, isaret, oturum} (32 rota; testi bu dosyadan ÜRETİLİR, üç ölçüm korunur: sayfaya özgü işaret görünür + `networkidle` + pathname aynı), `spec` {tur, dosya, testAdi, gerekce} (30 rota; başka bir spec'in içinde ziyaret edilir, dosya ve TAM test adı — iç içe describe başlıkları ` > ` ile — yazılır), `muaf` {tur, gerekce} (8 rota; kapsamın ÖLÇÜLMÜŞ sınırı); `rota-render-kapisi.spec.ts`teki `KAPSANMAYAN` sözlüğü mekanizma olarak envantere TAŞINDI, korumasız bırakılmadı: G5 muaf kümesini dondurur ve `/yedekler` gerekçesinin metnini harfi harfine tutar; DONMUŞ SAYISAL TABAN YOK VE BU BİLİNÇLİDİR — hiçbir kapı "kapsam >= N" demez, G1 KÜME EŞİTLİĞİ ister (App.tsx rota kümesi == envanter rota kümesi, İKİ YÖNDE); naif bir eşik sabiti olduğu yerde bırakıp testleri silerek yenilebilirdi, küme eşitliği yenilemez: yeni rota kapsamı düşürmez, envanteri EKSİK bırakır; G1 ayrıca AYRIŞTIRMANIN KENDİSİNİ ölçer (`<Route` etiketi sayısı == adlandırılmış rota + düzen rotası + `path="*"`), yoksa regex bir biçimi sessizce kaçırdığında kapı "eşit" deyip hiçbir şey ölçmemiş olurdu; G2 parametreli 12 rotanın ikisinde de aynı olduğunu, G3 her `spec` tasnifinin GERÇEK bir dosyayı ve o dosyada BEYAN EDİLMİŞ bir testi adlandırdığını (şablon literalleri `${...}` yerine `.+` konarak desene çevrilir, sabit parçalar harfi harfine tutulur), G4 kapı işaretlerinin boş olmadığını ve BİRİCİK olduğunu, G6 bir rotanın birden fazla tasnifte görünemediğini, G7 raportörün `playwright.config.ts` içinde HEM CI HEM yerel listede bildirildiğini ölçer; DARALTILMIŞ KOŞUM AYRIMI — CI Playwright'ı İKİ kez çağırır ve ikinci çağrı (`e2e/touch-targets.spec.ts`, tahsis motoru AÇIK) kapsam kapısını hiç koşturmaz; raportör bu MEŞRU daralmayı kapının SESSİZCE KAYBOLMASINDAN diskten ayırır: eksik = (diskteki *.spec.ts) \ (koşuda testi olan dosyalar); eksik BOŞ ise sözleşme kurulur, eksik YALNIZ kapının kendisiyse "her şey koştu ama kapı koşmadı" demektir ve R4 KAPALI DÜŞER, eksik başka dosya da içeriyorsa koşum gerçekten daraltılmıştır ve raportör bunu SÖYLEYEREK susar; koşuda HİÇ test yoksa (ör. webServer ayağa kalkmadı) bu daralma değil ÖLÇÜMSÜZLÜKTÜR ve yine kapalı düşer — bu son kural inşa sırasında ÖLÇÜLDÜ, uydurulmadı: ilk yerel koşumda backend süreci düştü, sıfır test koştu ve raportör onu "meşru daralma" saymıştı; İNŞA SIRASINDA ÖLÇÜLEN İKİNCİ KUSUR ve dersi: raportör dosya yollarını `config.rootDir`e göre alıyordu; Playwright `rootDir`i projelerin `testDir`lerinin ORTAK ATASI olarak hesaplıyor ve bu depoda iki projenin de `testDir`i `./e2e` olduğu için `rootDir` = `frontend/e2e` çıkıyor, görece yollar `e2e/` önekini KAYBEDİYOR; sonuç 63 sahte ihlaldi — kusur ölçülen ağaçta değil ÖLÇÜMDEYDİ; kök artık `config.configFile`in dizininden alınır ve içinde `package.json` yoksa kapı yine kapalı düşer; ÖLÇÜMLER (gerçek Chromium + gerçek backend, `e2e/serve.py`, simülasyon yok) — NİHAİ AĞAÇ: `npx playwright test` 143 geçti / 20 dosya, 10.4 dk, exit 0 ve raportör 0 ihlal bildirdi (envanter 70, kapi 32, spec 30, muaf 8, koşan spec dosyası 20); `npx vitest run` 558 geçti / 83 dosya, exit 0 (taban 551/82 -> yeni sözleşme dosyası 7 test ekledi); `npm run typecheck:e2e` exit 0, `npx tsc -b` exit 0, `npm run lint` exit 0, `npm run build` exit 0; DONMUŞ SÖZLEŞME KIPIRDAMADI: `frontend/e2e` lint uyarısı 8 -> 8, dolayısıyla `ci.yml`e DOKUNULMADI; test sayısı 142 -> 143 (donmuş `KAPSANMAYAN` testi kaldırıldı çünkü işini G5 devraldı, `/nakit-yonetimi` ve `/depolar` kapı olarak eklendi: -1 +2); CI'IN İKİNCİ ÇAĞRISI KIRILMADI — `PAYMENT_ALLOCATION_ENGINE_ENABLED=true npx playwright test e2e/touch-targets.spec.ts --project=chromium-utc` 45 geçti / 2 atlandı, exit 0 ve raportör "KURULMADI — daraltılmış koşum (koşan 1, koşmayan 19)" diyerek doğru sınıfladı; MUTASYONLAR — BEŞİ DE KIRMIZI, hepsi exit kodu ile: M1 App.tsx'e envanter girdisi OLMAYAN geçici bir rota eklendi -> vitest exit 1, G1 rotayı ADLANDIRDI (`/gecici-mutasyon-rotasi`); M2 envanterden `/analizler` silindi -> vitest exit 1, G1 yine rotayı adlandırdı; M3 üretilen tek bir kapı testi `test.skip` ile atlandı -> Playwright'ın KENDİSİ "1 atlandı, 142 geçti" ile exit 0 verirdi, raportör R2 ile exit 1 yaptı (#8'in birinci artığı KAPANDI); M4 `/aktivite`nin `spec` tasnifi var olmayan bir viewport adına (`tablet-800: ...`) çevrildi — G3 statik olarak YEŞİL kaldı (7/7, çünkü ad dosyadaki şablon desenine uyuyor) ama Playwright "143 geçti" derken raportör R3 ile exit 1 yaptı: envantere yazılmış bir ad, koşmuş bir testin yerine geçmiyor; M5 kapsam kapısı dosyası DİSKTE BIRAKILIP hiç test üretmez hale getirildi -> Playwright "111 geçti" ile exit 0 verirdi, raportör R4 + 32 R1 ile exit 1 yaptı (#8'in ikinci artığı KAPANDI); tüm mutasyonlar atılacak bir git worktree kopyasında koşuldu ve ağaç temiz bırakıldı; ÖLÇÜMÜN DÜRÜST SINIRLARI: (a) yerel Playwright koşumları, `cdn.playwright.dev` bu ortamda ağ politikasıyla KAPALI olduğu için sabitlenmiş Chromium 1228 yerine ortamda hazır 1194 yapısına sürüm-kaydırmalı bir sembolik bağla koşuldu — CI sabitlenmiş yapıyı kullanır, dolayısıyla yukarıdaki yeşiller CI'ın tarayıcı yapısında DEĞİL yakın bir yapıda ölçülmüştür; (b) `kapi` tasnifinin 32 rotası ve `spec` tasnifinin 27'si sayfaya ÖZGÜ bir işaretle çizim kanıtı taşır (toplam 59/70), ancak `/tanitim`, `/kayit` ve `/eposta-dogrula` YALNIZ OLUMSUZ ölçülür (girişe fırlatılmadı, giriş formu yok) ve bu sınır gerekçelerinde yazılıdır; (c) `/saha` ziyareti `context.newPage()` ile açılan İKİNCİL sekmededir ve `page` fixture'ının konsol-temiz sözleşmesi o sekmeyi kapsamaz; (d) `/odemeler` `satis` ROLÜNDE ölçülür, admin görünümü değil; ÖLÇÜLMEYEN: backend pytest süiti bu turda yerelde koşulmadı (değişiklik `frontend/` altındadır ve backend kodunun hiçbir satırına dokunmaz); raportör Playwright'ın gördüğü koşuyu ölçer, CI'ın o koşuyu gerçekten ÇAĞIRDIĞINI ölçmez — o `ci.yml`in ve `test_ci_gates.py`nin işidir ve ikisine de DOKUNULMADI; `/yedekler`in platform operatörü AÇIKKEN nasıl çizildiği; kapsanan rotaların boş veritabanı DIŞINDAKİ durumları (dolu liste, hata, yükleme, izin kısıtlı roller); sekiz `muaf` rotanın tohumlanmış hali; ve envanterdeki GEREKÇE metinlerinin doğruluğu — G3/G5 gerekçenin BOŞ olmadığını zorlar, DOĞRU olduğunu zorlayamaz. ||| İKİNCİ TUR — ÜÇ BLOCKER (bağımsız Cursor + Antigravity incelemeleri, c42aaf4 NO-GO): üçünün de ortak yanı aynıydı — kapı YEŞİL yanıyordu çünkü ölçtüğünü sandığı şeyi ÖLÇMÜYORDU. B1 AYRIŞTIRMA: G1 App.tsx'i tek bir regexle (`/<Route\s+path="([^"]+)"/g`) okuyordu ve desen `path`in İLK SIRADA ve ÇİFT TIRNAKLI olmasına bağlıydı; ÖLÇÜLDÜ — envanter girdisi olmayan bir `<Route element={<X/>} path="y"/>` satırı eski regexte 70 rotanın içinde GÖRÜNMÜYOR ve eski parite hesabı da (`rota + element= + 1 == <Route sayısı`) 73==73 ile TUTUYORDU, yani kaçış tam bir kaçıştı; tek tırnaklı varyantta ise eski regex rotayı yine kaçırıyor ama parite 72 vs 73 ile kırılıyordu — o yüzden tek tırnak TAM bir bypass DEĞİL, rotayı ADLANDIRAMAYAN bir kırmızıydı; standart biçimli yeni rota zaten eski kapıda da kırmızıydı. Ayrıştırma artık TypeScript'in KENDİ AST'sinden okunur (`e2e/rota-ayristirici.ts`; `typescript` zaten devDependency, yeni ağır bağımlılık YOK): AST'de öznitelik sırası ve tırnak biçimi diye bir kavram yoktur, `path={degisken}` ve `{...spread}` FAIL-CLOSED istisna fırlatır, `index` -> `/` ve `path="*"` yakalayıcı ayrımı korunur; G1 ayrıca rota beyanının YALNIZ src/App.tsx'te olduğunu da ölçer (birim testlerinin kendi MemoryRouter ağaçları hariç tutulur) ve G9 ayrıştırıcının kendi sözleşmesini çivilerler. B2 İŞARET: `page.getByText(isaret).first()` SAYFANIN TAMAMINDA arıyordu; işaretlerin çoğu aynı zamanda menü etiketidir ve AppShell aktif grubu DAİMA açık gösterir. ÖLÇÜLDÜ — `/alislar`, `/analizler` ve `/islem-gecmisi` rotalarının sayfa gövdesi boş bir bileşenle değiştirildiğinde (kenar çubuğu aynen yerinde) c42aaf4'ün iddiası ÜÇÜNÜ DE GEÇİRDİ (3 passed, 11.5s), yeni iddia ÜÇÜNÜ DE DÜŞÜRDÜ (3 failed) — yani yanlış pozitif gerçekti ve kapandı. İşaret artık rota gövdesinin KÖKÜNÜN altında aranır (`data-testid="rota-govdesi"`: oturumlu rotalarda AppShell'in `<Outlet/>`ü saran kutusu, oturumsuz kapı rotalarında sayfa bileşeninin kendi kökü) ve kök TAM OLARAK BİR kez bulunmak zorundadır; G8 kapıda `page.getByText(`, `page.locator(`, `page.getByRole(` kaçış yollarının kalmadığını ve kökün uygulamada bildirildiğini ölçer. B3 `spec` TASNİFİ: G3 dosya + test adının varlığını, R3 o testin koşup geçtiğini ölçüyordu; ikisi de doğruydu ve ikisi de ROTAYI ölçmüyordu. Kanıt artık TARAYICININ KENDİ gezinti olaylarından gelir (`e2e/rota-ziyaret-kaydi.ts`): bağlam fixture'ı her sayfaya bir başlangıç betiği kurar, `history.pushState`/`replaceState` çağrılarını ve her belge yüklenişini bir testInfo EKİ olarak kaydeder, raportör (R5 `spec`, R6 `kapi`) kaydı okur ve gerçek URL'yi rota desenine indirger (`/musteriler/7` -> `/musteriler/:id`; sabit segment parametreye yeğlenir, hiçbir desene oturmayan yol kanıt değildir). OLUMSUZ YÖNLENDİRME KANIT SAYILMAZ ve ayrım ZAMANLAMAYA DEĞİL YAPIYA dayanır: bir kayıt, aynı sekmedeki bir sonraki kayıt farklı yola giden bir `replace` ise VE arada sayfaya hiç kullanıcı girdisi gitmediyse kalıcı değildir. Etkileşim koşulu UYDURULMADI, ÖLÇÜLDÜ: ilk kural yalnız "sonraki kayıt replace mi" diye soruyordu ve gerçek bir ziyareti — zorunlu şifre rotasyonu testinin `/sifre-degistir` ekranını açıp üç alanı doldurup düğmeye bastığı ziyareti — yanlışlıkla eledi; ayrımı taşıyan şey süre değil ETKİLEŞİMdi. Bir test birden fazla `spec` rotası kanıtlıyorsa özet bunu ADIYLA ve kaç/kaç olarak yazar. EK — RAPORTÖRÜN KENDİSİ KAPATILABİLİYORDU: `--reporter=list` raportörü hiç yüklemeden süiti yeşil bitiriyordu ve G7 yalnız yapılandırma metnini saydığı için o da yeşil kalıyordu; raportör artık `onBegin`de süreç kimliği taşıyan bir makbuz yazar, yapılandırmadan gelen (ve komut satırından ezilemeyen) `globalTeardown` onu arar, bulamazsa koşuyu düşürür — makbuz `onEnd`de YAZILAMAZ çünkü Playwright'ta teardown `onEnd`den ÖNCE koşar. ÖLÇÜMLER (gerçek Chromium + gerçek backend, simülasyon yok): TABAN c42aaf4 ağacı `npx playwright test` 143 geçti / 20 dosya / 9.7 dk / exit 0; NİHAİ ağaç `npx playwright test` 143 geçti / 10.3 dk / exit 0, raportör 0 ihlal ve üç çoklu-kapsam satırı (3/3, 3/3, 4/4) bildirdi; `npx vitest run` 561 geçti / 83 dosya / exit 0 (c42aaf4 tabanı 558; G8, G9, G10 eklendi); `npx tsc -b`, `npm run typecheck:e2e`, `npm run lint`, `npm run build`, `npx playwright test --list` (143 test / 20 dosya) hepsi exit 0; `frontend/e2e` lint uyarısı 8 -> 8, dolayısıyla `ci.yml`e DOKUNULMADI. MUTASYONLAR, YEDİSİ DE BEKLENEN RENKTE (hepsi atılacak git worktree kopyalarında, ağaç temiz bırakıldı): M1 ters öznitelik sıralı envantersiz rota -> vitest exit 1, G1 `/mutasyon-sira-ters`i ADLANDIRDI (eski regex: 70 rota, kaçış görülmez, parite 73==73); M2 tek tırnaklı envantersiz rota -> vitest exit 1, G1 `/mutasyon-tek-tirnak`ı adlandırdı; M3 standart biçimli envantersiz rota -> vitest exit 1; M4 üç sidebar-etiketli rotanın gövdesi boşaltıldı -> yeni iddia 3 failed / EXIT 1, c42aaf4 iddiası AYNI ağaçta 3 passed; M5A `/alislar` tasnifi `spec`e çevrilip `/` açan GERÇEK ve GEÇEN bir teste bağlandı -> Playwright'ın kendisi 142 GEÇTİ (statik yarı da 10/10 YEŞİL kaldı, G3 dahil) ama raportör R5 ile exit 1 yaptı ve `Testin kalıcı ziyaretleri: /, /giris` diyerek rotayı adlandırdı, çoklu kapsam satırı `1/2 rota kanıtladı: / | KANITSIZ: /alislar` yazdı; M5B AYNI yeniden bağlama ama rotayı GERÇEKTEN açan bir teste -> raportör 0 ihlal (karşıt kontrol: kapı "her yeniden bağlamaya kırmızı" demiyor, ZİYARETİ ölçüyor); M6 CI'ın meşru ikinci çağrısı + `--reporter=list` -> aynı 45 geçti / 2 atlandı ama `globalTeardown` ile exit 1; M7 CI'ın meşru ikinci çağrısı (`PAYMENT_ALLOCATION_ENGINE_ENABLED=true npx playwright test e2e/touch-targets.spec.ts --project=chromium-utc`) -> 45 geçti / 2 atlandı / exit 0 ve raportör "KURULMADI — daraltılmış koşum (koşan 1, koşmayan 19)" diyerek doğru sınıfladı. ÖLÇÜMÜN DÜRÜST SINIRLARI VE ÖLÇÜLMEYEN: yerel Playwright koşumları yine sürüm-kaydırmalı bir Chromium ile yapıldı (`cdn.playwright.dev` ağ politikasıyla kapalı; sabitlenmiş 1228 yerine ortamda hazır 1194 / 141.0.7390.37 yapısı yeni dizin düzenine sembolik bağlarla takıldı) — CI sabitlenmiş yapıyı kullanır; M5B koşumunda `machine-360.spec.ts :: "Sayaç Geçmişi: uç 500 dönerse..."` testi 20 sn `toPass` penceresinde düştü, aynı dosya tek başına yeniden koşulduğunda 12/12 geçti ve bu test dört ayrı tam koşumda da geçmişti — düşüşün ölçülen sebebi o sırada aynı makinede paralel koşturduğum vitest'in CPU baskısıdır, kod değil, ama ölçüm kirlendiği için burada ADIYLA yazılıyor; M4 koşumlarının EXIT kodu raportörün daraltılmış koşum tepkisini de taşır, kapının kendi ölçüsü test SONUÇLARIDIR (3 passed vs 3 failed); R5/R6 yalnız `helpers.ts` bağlam fixture'ını kullanan testleri ölçebilir — `@playwright/test`ten doğrudan `test` alan bir spec'e bağlanan bir `spec` tasnifi kanıt üretemez ve KAPALI DÜŞER (bugün hiçbir `spec` girdisi öyle bir teste bağlı değil); makbuz raportörün YÜKLENDİĞİNİ ölçer, ne ölçtüğünü değil (verdict `onEnd`in işidir); backend pytest süiti bu turda yalnız durum kaydı kapısıyla koşuldu; `ci.yml`in bu koşumları gerçekten ÇAĞIRDIĞI hâlâ ölçülmüyor — o `test_ci_gates.py`nin işidir ve ona DOKUNULMADI; `/yedekler`in platform operatörü açıkken çizimi, kapsanan rotaların boş veritabanı dışındaki durumları ve envanterdeki gerekçe metinlerinin DOĞRULUĞU hâlâ ölçülmüyor. ||| ÜÇÜNCÜ TUR — İKİ SÖZDIZIMI KAÇIŞI KAPATILDI (Cursor final runtime review, cc10c27 sonrası): cc10c27 ağacında G1 YEŞİL kalıyordu çünkü AST gezgini yalnız JSX açma etiketlerini (`ts.isJsxSelfClosingElement`, `ts.isJsxElement`) arıyordu; `React.createElement(Route, {path: "kacak-create-element", element: <Login/>})` ve `useRoutes([{path: "kacak-use-routes"}])` biçimleri `CallExpression` düğümleri üretir ve gezgin bunları görmeden geçiyordu. ÖLÇÜLEN KAÇIŞ, UYDURULMADI: her iki satırı App.tsx sonuna ekleyip `npx vitest run src/rota-kapsam-sozlesmesi.test.ts` çalıştırmak G1'i YEŞIL bıraktı. MİMARİ KURAL artık yorumda değil makinede: "Uygulama rota beyanları YALNIZ src/App.tsx içindeki JSX <Route> hiyerarşisi olabilir; React.createElement(Route, ...) ve useRoutes([...]) FAIL-CLOSED." DEĞİŞEN DOSYALAR (2): `frontend/e2e/rota-ayristirici.ts` — gezginin `jsx === null` dalına `ts.isCallExpression` kontrolü eklendi: `createElement` + ilk argüman `Route` tanımlayıcısıysa veya çağrılan fonksiyon `useRoutes` ise `hata()` fırlatılır; ayrıca G11 için metin tabanlı `yasakBeyanVarMi(kaynak: string): boolean` yardımcısı eklendi. `frontend/src/rota-kapsam-sozlesmesi.test.ts` — mevcut G9 `it` bloğuna iki yeni mutasyon `expect` eklendi (createElement ve useRoutes kaçışları ROTA AYRIŞTIRILAMADI fırlatmalı); yeni `it('G11: src/ altında useRoutes ve createElement(Route, ...) beyanı yasak')` bloğu eklendi: `src/` altını metin taramasıyla tüm kaynak uzantılarında (`test/spec` dosyaları hariç) tarar, `yasakBeyanVarMi` true döndüren her dosyayı ihlaller listesine ekler ve liste boş olmasını bekler. ÖLÇÜMLER (throwaway node + parser inline transpile, App.tsx'e dokunulmadan): M8 `React.createElement(Route,{path:"mutasyon-create-element"})` → ROTA AYRIŞTIRILAMADI, satır ve sebep mesajda, exit 1. M9 `useRoutes([{path:"mutasyon-use-routes"}])` → ROTA AYRIŞTIRILAMADI, exit 1. NEG1 değiştirilmemiş App.tsx → YEŞİL. NEG2 ters öznitelik sırası + tek tırnak + `index` + `path="*"` + iç içe çocuk → YEŞİL. `yasakBeyanVarMi` temiz → false, createElement+Route → true, useRoutes → true. `npx vitest run` 562 geçti / 83 dosya / exit 0 (cc10c27 taban 561; G11 +1). `npx tsc --noEmit` exit 0. `npm run lint` 0 hata (iki değişen dosya kendi başına 0 uyarı). `npm run build` exit 0. Playwright bu turda yeniden koşulmadı — değişiklik yalnız AST ayrıştırıcı ve statik sözleşme katmanında, çalışma zamanı raportörüne dokunulmadı. COMMIT: b4e928a6e0d603d81887b9e980a0c003aaa34261. ÖLÇÜLMEYEN: `yasakBeyanVarMi` metin tabanlıdır — yorum ve dize değişmezlerini gerçek beyanlardan ayırt etmez; mevcut `src/` ağacında ihlal yok (G11 ölçtü, geçti) ama teorik yanlış pozitif mümkündür; Playwright mutasyonları (M8/M9) yalnız AST-tabanlı unit düzeyinde ölçüldü, gerçek e2e koşumunda değil.
