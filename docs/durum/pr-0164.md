#164 — HİJYEN H88/H89/H90/H94/H96 (göç YOK, yeni rota YOK, base ad36c64 = #163 sonrası): H88 GET /api/despatch-notes/{id}/response'a response_model (IrsaliyeYanitiGorunumu/Belgesi/Satiri/Uyarisi) — gövde önce SQLite+PG 13'er gövdeyle ölçüldü, tip kümeleri özdeş, product_id/notes şemada NULL olabildiği için isteğe bağlı; types.gen.ts 3 adımla yeniden üretildi (uç unknown → IrsaliyeYanitiGorunumu, yeniden üretim bayt bayt aynı); E4b-3 DespatchResponseView/Line üretilen şemadan türer, şemadan item_name silinince tsc -b DespatchNotePanel.tsx:235'te kırmızı; H89 party-links created_at utc_iso'dan geçer (listenin tek iki *_at alanı), SQLite HTTP + PG ikizi +00:00 ve saklanan anla eşitlik; H90 evaluate_consent GRANTED satırın recipient_snapshot'ı normalize olmayınca ALLOWED dönüyordu (iki lehçede ölçüldü, çiftçi bakiyesini alıyordu) → BLOCKED/RECIPIENT_INVALID, çiftçi genel kapalı metni alır, F1 SQLite 4 + F1 PG 3 + F10-1b 3 test önce kırmızı sonra yeşil; seed_demo_data rıza satırı açmıyor (0 satır), tohumdaki 35 telefonun hepsi normalize oluyor; H94 conftest PGOPTIONS'a -c timezone=Europe/Istanbul ekler + REQUIRE_PG'de SHOW TimeZone kapısı (çıplak psycopg — app içe aktarmak ikizin AUTO_MIGRATE=false'ını ezip sec3b ikizini 7 hataya düşürdü, ölçüldü ve düzeltildi), kanıt test_enterprise_invoices_postgresql utc_iso mutantı UTC oturumda eski conftest'le yeşil yenisiyle kırmızı (created_at +03:00), 143 PG ikizi taze DB'de İstanbul oturumunda kırılmadı (platform_backups yalnız pg_dump PATH ortamsal, PATH'le 9/9); H96 _katli sızıntısı ölçüldü (401 GET, admin, SQLite+PG aynı): 5 uç (müşteri/tedarikçi kartı, ürün detayı, finans hareketleri, satış/alış detayı) → tek yardımcı arama_katli.katli_gizle 4 SELECT * noktasında, sonrası 0, email_katli maskesi ikinci savunma olarak kalır ve birimde çakılı, altı tablo liste+detay testi SQLite + PG ikizi, mutasyon iki lehçede kırmızı; pin: DYNAMIC_SQL_FILE_ALLOWLIST 6 dosya parmak izi (sayı 271 aynı), rota/pg_twins/alembic head değişmedi; run_isolated_tests 492 dosya, tek kırmızı o pin, yeniden pinle yeşil.
