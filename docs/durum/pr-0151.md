#151 — H47 HİJYEN: statement.py CS2 sorgusu Core (backend, göç YOK, yeni uç YOK): CS2'nin (#129) eklediği tek `text()` `_cek_dekont_borcu` (karşılıksız/iade çek borç belgesi toplamı) SQLAlchemy Core `select`e çevrildi — dar `receivable_charge_documents` Core `Table`ı (yalnız okunan sütunlar, `create_all` edilmez, tipler göçle aynı ve test ediliyor), hedef açık `.select_from()`, toplam `NullType` (ham değer `money()`ye text() ile aynı yoldan); diğer beş `text()` CS2 öncesi, DEĞİŞMEDİ; eşdeğerlik eski text() ile yeni Core aynı tohumda 40/40 AYNI (SQLite + PG16 taze DB), text() silindi, testler sabit beklenenle kaldı (SQLite `test_H47_*` kendi firmasında — firma `a`ya yazılan ilk tohum ters sırada pano testini 18.00 ile kırmıştı; PG `test_H47_dekont_borcu_Core_PG`); pinler tarayıcı çıktısından: dinamik text() statement.py 6→5, toplam 270→269, Core envanteri 233→234 (494f7b8b→31ca10d5), Core kiracı kapısı 191→192 (+`receivable_charge_documents`); tam izole koşu 471/473 (iki kırmızı tabanda da kırmızı / bilinen yerel zaman aşımı), CS2 PG ikizi 10/10 0 skip, değişen 4 dosya ileri+ters yeşil, mutasyonlar kırmızı — <merge tarihi>
