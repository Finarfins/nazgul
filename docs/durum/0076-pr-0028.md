#28 — `donmus_saat` SERTLEŞTİRME: aletin güvenliği gelenekti, artık KAPI. TABAN `f623190d` (develop; ölçüldü, `git rev-parse origin/develop`; önce PR #24 `b29a289d`, sonra PR #27 `f623190d` indi ve her ikisi de `--no-ff` ile birleştirildi, ÇAKIŞMA YOK — PR #27'nin getirdiği dokuz dosyanın bu PR'ın üç dosyasıyla KESİŞİMİ BOŞ, ölçüldü) ve TABAN YEŞİL DEĞİLDİR — bu PR o kırmızıları kapatmaz, onlara dokunmaz. AŞAĞIDAKİ BÜTÜN ÖLÇÜMLER `c3be7934` TABANINDA ALINDI, `b29a289d`DE DEĞİL: PR #24 yalnız dört tarla testi ve kendi durum kaydını getirdiği, `donmus_saat`e, `app/`a ve `Dockerfile`a dokunmadığı için yeniden alınmadılar — bu bir AKIL YÜRÜTMEDİR, yeni tabanda yeniden ölçüm DEĞİLDİR. ÖLÇÜLEN KUSUR 1 (YARI DONMUŞ SAAT): taban aleti `app.business_time.business_now`u yamalar ama ADI erken bağlayan çağrı yerlerine ULAŞMAZ; `app/labels.py:31` ve `app/routers/analytics.py:6` `from ..business_time import business_now` ile adı bağlar. Ölçüldü (taban aletinin kopyası, geçici dizinden, `PYTHONPATH=backend`, `NAZGUL_DONMUS_GUN=2097-06-15`, `ENVIRONMENT=test`; önce `app.routers.analytics` ve `app.labels`, SONRA `donmus_saat.uygula()`): stderr `DONMUS_SAAT_ETKIN ... 2097-06-15T12:00:00+03:00`, çıkış kodu 0, buna rağmen `analytics.business_now().date()` ve `labels.business_now().date()` `2026-09-03` — yani GERÇEK saat. Alet "donmuş" diye bağırırken iki çağrı yeri duvar saatini okuyordu ve hiçbir şey bunu söylemiyordu. Bu dalda aynı senaryo ikisi için de `2097-06-15` döndürür. ÖLÇÜLEN KUSUR 2 (ÜRETİMDE SESSİZ ÇALIŞMA): taban aleti `ENVIRONMENT=production` iken de yamalar; ölçüldü, aynı yolla çıkış kodu 0, `DONMUS_SAAT_ETKIN` yazılır ve `KULLANICI KODU KOSTU` basılır. Bunun neden önemli olduğu AKIL YÜRÜTME DEĞİL, ÖLÇÜMDÜR: `Dockerfile:57` `COPY --chown=app:app backend/ ./backend/` satırı aleti `runtime-base`e taşır, `production` hedefi (`Dockerfile:66`) ondan türer ve `.dockerignore` hiçbir test dosyasını dışlamaz — yani alet üretim imajında DİSKTE DURUR. ÇARE ÜÇ PARÇA. (1) ÜRETİMDE RED: `uygula()` ilk iş olarak HAM `ENVIRONMENT` değişkenini okur; `production` ise hiçbir şey yamalamadan 97 ile ölür. Karar `app.config.settings` ile DEĞİL ham değişkenle verilir — gerekçe docstring'de: `app.config` import edilseydi alet uygulama yapılandırmasına bağlanırdı, `Settings` nesnesi süreç içinde değiştirilebildiği için kapı olarak güvenilmez, ve `ENVIRONMENT` tanımsızken `settings.environment` zaten `development` olduğu için ek bir `settings` denetimi HİÇBİR yeni red üretmez. (2) TAM ÖZ-DENETİM: yamadan sonra `sys.modules` içindeki `app`/`app.*` modülleri taranır, yamadan ÖNCEKİ fonksiyon nesnesine `is` ile eşit her öznitelik donmuş fonksiyona yeniden bağlanır ve bulunan her takma ad ÇAĞRILARAK doğrulanır; biri donmuş günü döndürmüyorsa alet ölür. Takma ad listesi `DONMUS_SAAT_TAKMA_ADLAR` satırıyla stderr'e yazılır (ölçüldü, bu dalda: `app.labels.business_now app.routers.analytics.business_now`). (3) KAPI TESTİ `backend/tests/test_donmus_saat_kapisi.py`, beş test, her biri İKİ YÖNLÜ. `test_app_import_grafigi_donmus_saat_cekmez`: alt süreçte `import app.main` sonrası `donmus_saat` `sys.modules`'te YOK ve `app/` altındaki her `.py`nin AST'sinde `donmus_saat` importu yok; `test_statik_tarama_mutasyonda_kirmizi_olur` aynı taramayı `app/` kopyasına tek satır (`import donmus_saat`) eklenmiş hâlde koşar ve KIRMIZI olduğunu gösterir — hiç kırmızı gösterilmemiş bir tarama, hiçbir şey taramayan bir taramadan ayırt edilemez. `test_production_ortaminda_uygula_97_ile_olur`: `ENVIRONMENT=production` ile 97 + `DONMUS_SAAT_OLUMCUL` + `KULLANICI KODU KOSTU` YOK, KARŞI YÖN aynı testte `ENVIRONMENT=test` ile 0 + `DONMUS_SAAT_ETKIN` + kullanıcı kodu koşar. `test_erken_baglanan_business_now_da_donar` iki erken bağlanan çağrı yerini doğrudan okur; `test_takma_ad_yamasi_kapatilinca_97_ile_olur` yalnız yeniden bağlama adımını `NAZGUL_DONMUS_SAAT_ALIASSIZ=1` ile atlatır (takma adlar yine BULUNUR ve doğrulanır) ve öz-denetimin 97 ile öldüğünü gösterir — denetim eksik olsaydı burada "ETKIN" yazıp 0 ile çıkardı. Bayrak YALNIZ bu test içindir ve üretim reddinden SONRA okunur. ÖLÇÜMLER (bu dal, `backend/`den): `python -m pytest tests/test_donmus_saat_kapisi.py tests/test_ci_gates.py test_v3_harman_vadeli.py -q` -> 25 passed; `test_v3_harman_vadeli`in iki mevcut `donmus_saat` testi davranış değiştirmeden yeşil kalır. TAM SQLITE TAKIMI İKİ AĞAÇTA DA KOŞULDU, TABANA KARŞI: dal `13 failed, 1627 passed, 169 skipped`, taban `c3be7934` `14 failed, 1621 passed, 169 skipped` — 10 kırmızı ORTAK. Komut LİTERAL `python -m pytest -q` DEĞİLDİR: o hâliyle her iki ağaçta da `tests/fixtures/canonical_collection/test_broken_import.py` (deponun BİLEREK bozuk fixture'ı) toplamayı kesiyor ve HİÇBİR test koşmuyor, bu yüzden iki koşu da `--ignore=tests/fixtures` ile ve taze bir `DATABASE_URL` ile alındı (yerel `backend/veriler.db` başka bir dalın `20260902_0066` göçüne damgalı olduğu için alembic toplamayı düşürüyordu). FARK EDEN KIRMIZILAR SIRA/DURUM BAĞIMLIDIR, BU PR'IN ESERİ DEĞİLDİR — VE BU BİR AKIL YÜRÜTME DEĞİL, ÜÇÜNCÜ BİR ÖLÇÜMDÜR: `test_v2_9_demo_summary.py::test_demo_summary_reports_seeded_counts` yalnız dalın tam koşusunda, `tests/test_isolated_test_runner.py::test_serial_and_parallel_collect_and_pass_the_identical_test_set` yalnız tabanın tam koşusunda kırmızıydı; ikisi de TEK BAŞINA koşulduğunda iki ağaçta da YEŞİL. ATIF KOŞUSU (aynı dal, YALNIZ `--ignore=tests/test_donmus_saat_kapisi.py` eklenerek): `13 failed, 1622 passed, 169 skipped` — geçen sayı tam olarak 5 azalıyor, yani yeni dosyanın katkısı SADECE kendi beş testidir; buna rağmen `test_demo_summary_reports_seeded_counts` HÂLÂ kırmızı ve üstelik önceki iki koşunun İKİSİNDE DE yeşil olan `test_f0_email_dispatch.py::test_t4b_smtp_credentials_are_redacted_from_logs` ile `test_registration_hardening.py::test_development_without_turnstile_secret_warns_and_allows` bu koşuda kırmızıya döndü. Yani kırmızı KÜMESİ bu takımda koşudan koşuya kendiliğinden oynuyor; yeni dosya çıkarıldığında da oynamaya devam ediyor. DONUK SAYILAR (BİRLEŞMİŞ AĞAÇTA, YANİ `f623190d` TABANINDA YENİDEN ÖLÇÜLDÜ): pg populasyonu 100 — `ls backend/test_*postgresql*.py | wc -l` 98 döndürür, artı 2 adlı özel dosya; bu PR'ın yeni test dosyası o globa GİRMEZ, 97 -> 98 artışı PR #27'nin `test_birim_donusumu_postgresql.py` ikizinden gelir ve kapının adı da `test_pg_test_population_exact_100`tür. Alembic başı `20260902_0066` (PR #27'nin birim dönüşümü göçü; deponun kendi zincir testi `tests/test_goc_zinciri.py` ile doğrulandı, 23 passed). Bu iki pin ÖNCEKİ sürümde `b29a289d` tabanının değerleriyle (99 ve `20260902_0065`) yazılıydı; taban oynayınca ikisi de bayatladı ve BURADA YENİDEN ÖLÇÜLEREK yazıldı, eski değerin üzerine aritmetik yapılarak DEĞİL. `test_ci_gates.py` yeşil. `backend/app` altına DOKUNULMADI — ölçüldü, `git diff --name-only origin/develop..HEAD -- backend/app/` SIFIR satır döndürür; bu kaydın önceki sürümü aynı iddiayı YANLIŞ bir parantezle destekliyordu ("yalnız iki dosya döndürür"), oysa komut ÜÇ yol döndürür: `backend/donmus_saat.py`, `backend/tests/test_donmus_saat_kapisi.py` ve bu kaydın kendisi `docs/durum/0075-pr-0028.md` — başlık doğruydu, dayanağı değildi. Alembic göçü EKLENMEDİ. ÜRETİM REDDİ GÜN GEREKTİRMEZ: `ENVIRONMENT=production` iken `NAZGUL_DONMUS_GUN` VERİLMESE BİLE alet 97 ile ölür (ölçüldü: değişken tanımsız, `DONMUS_SAAT_OLUMCUL: ENVIRONMENT=production iken saat dondurulamaz`, çıkış 97) — yani red, "değişken yoksa `None`" kuralının istisnası değil ONDAN ÖNCE gelen kuraldır ve modül docstring'i artık bu önceliği açıkça yazar. ÖLÇÜLMEDİ: PostgreSQL 16 ikiz takımı, CI iş akışının kendisi, ve `ENVIRONMENT=production` reddinin gerçek üretim imajı içinde koşumu. Kayıt develop'un en yükseği 0075 (PR #27, `0075-pr-0027.md`) olduğu için 0076'dır; bu kayıt İKİ KEZ yeniden adlandırıldı — önce 0074 olarak inmişti, PR #24 aynı sırayı alınca `git mv` ile 0075 oldu (ölçüldü, adlandırmadan önce kapı `YİNELENEN sıra 0074: 0074-pr-0024.md, 0074-pr-0028.md` diye KIRMIZIYDI), sonra PR #27 0075'i alınca aynı sebeple 0076 oldu. Sıra bir MÜLK değil, iniş anında boş olan bir yerdir.
