#68 — HİJYEN H4 E3 SÜRÜ KURULUM ADI: smoke'ta grup sağımı kilidinden ÖNCE `GET /api/animals/{id}` ile `animals.group_id` persist'i doğrulanıyor — üyelik kurulmazsa kilit kaçağı gibi görünen yanlış sinyal ADIYLA kurulmamış kurulum olur; SQLite smoke + PG ikizi; `backend/app` SIFIR; KANIT: `PYTHONUTF8=1 python run_isolated_tests.py tests/test_e3_karantina.py --workers 4` → 14 passed; run id `c4926ab5-85ce-4d37-948f-b61a671afa4c`; 17 bağlam SABİT; dal `test/h4`; `--sonraki 68`.
