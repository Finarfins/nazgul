#137 — H49 DIŞA AKTARIM MANİFESTİ (backend-only, göç YOK, rota YOK; karar #115 (2)+(3)): `_seri`nin son çare `str()` dalına düşen tip METADATADAN sayıldı — SQLite ve PostgreSQL 16.14'te AYNI sonuç, tek tip `JSON`→`dict`, dört sütun (`supplier_import_profiles.column_map`/`page_sections`, `supplier_price_import_lines.raw`, `supplier_price_imports.error_detail`), UUID/Enum 0 sütun — ve `dict`/`list` artık Python repr'i değil gerçek JSON yazılıyor, `test_str_dalina_dusen_sutun_tipi_yok` yeni bir tip gelince sütunu ADIYLA kırar; geri yükleme `ast.literal_eval` okuyucusunu KORUDU (eski arşiv, gerçek zip'in JSON değerleri repr'e çevrilip geri yüklenerek birebir eşit ölçüldü) ve `manifest_dogrula` gevşek olduğu için DEĞİŞMEDİ; manifest `user_emails` taşıyor — YALNIZ `id`+`email`, YALNIZ dışa aktarılan firmanın ÜYELERİ: `app_users` çekirdek tablo, firma sınırı YOK, tek koruma kimlik kümesi; ilk ölçülen "A satırlarında anılan kimlik" filtresi (31 FK + 10 yumuşak = 41 sütun) bozuk bir A satırındaki B kullanıcısını sızdırırdı, kesişim "anılan ∩ üyeler" üyelik satırı da referans olduğu için tam üyelik kümesine eşit çıktı ve toplama kodu kaldırıldı; kapsam SQL düzeyinde ölçüldü (tek `app_users.email` ifadesi, `IN` parametreleri tam A üyeleri, `password_hash` yok, boş kümede sorgu yok), B kullanıcısı A'nın iki satırına ELLE EKİLDİ ve haritada yok, ayrılmış üye e-postasız ama kimliği satırda; mutasyonlar 4/4 KIRMIZI (ilk filtre geri konunca harita `[1,2,3,4]`); pinler: Core envanteri 223→224 (select 150→151, `72b2110c`→`2eb1e8ac`), SEC-3b'ye ölçülen `GET /api/company/export __admin_only__ email D,D,D,D,D` satırı gerekçesiyle (MUAFLAR değil: zip 5.1a'dan beri cari alanları taşıyor, beş rol de görmüyor), rota 419/325, GET 199, AUTHENTICATED 406, TENANT_TABLES 122, pg_twins 131 SABİT; tam süit 467/467 (3492), PG ikizi taze şemada 4/4; merge sırası #127 → H23 → H49, rebase sonrası pinler tarayıcıdan yeniden türetilecek — <merge tarihi>
