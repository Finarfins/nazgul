#148 — H57+H58 arama hijyeni (backend, göç YOK): tek ortak dikiş `app/arama.py` — `arama_deseni` (katlama + `\` `%` `_` kaçışı, `q` hep bağlı parametre) ve `katli_sql` (kolonu aynı tabloyla katlayan SQL ifadesi); TEK KURAL `ARAMA_ESLESME` tablosunun karakter başına uygulanmasıdır (Türkçe + Latin-1 aksanları + ASCII a-z→A-Z, `UPPER` YOK, SQL tarafı TEK `translate(kolon,kaynak,hedef)` çağrısı — PG'de yerleşik, SQLite'a `app/db.py`nin `Engine` SINIFI kancasında aynı anlamda kaydedilir) — düzeltme turu (mercek NO-GO 0580dcc): Python `.upper()` ile SQL `UPPER()` tablo dışı aksanlarda ayrışıyor, "Kâzım/hâlâ/Café" tabanda bulunup HEAD'de boş dönüyordu; `/customers` ve `/suppliers` arama parçaları modül sabitine alındı (`katli_sql` istek başına ÇAĞRILMAZ, AST pini 18 modül düzeyi çağrı), `barcode` da katlanır; `/api/customers`, `/api/suppliers`, `/api/payments`, `/api/finance/transactions` ve `/api/search` bu dikişi kullanır, her LIKE `ESCAPE` taşır, kiracı yüklemleri aynı; "yılmaz/YILMAZ/Yilmaz/yilmaz" ve "Kâzım Hâlâ Tarım", "Café Deniz", "Émile"nin her yazımı beş uçta bulur, her adın her birebir alt dizgisi adı hâlâ getirir (gerilemezlik), `%` 0 satır, `_` ve `\` harfi harfine, B firması görünmez, maskeli rol aynı katlamayla arar; PG'de `katli_sql` öntanımlı, C, POSIX, und-x-icu, tr-TR-x-icu harmanlamalarında `arama_katla` ile aynı; dinamik text() 269→274 (search.py 2→7, customers 1 / finance 7 sabit, üç parmak izi yeniden türetildi), pg_twins 137→138; iç içe `REPLACE` zinciri CI'nın Linux SQLite yapısında `parser stack overflow` veriyordu (koşu 35473305307; yığın derinliği YAPIYA bağlı, yereldeki 3.49.1 kaldırıyordu) — tek çağrı derinlikten bağımsızdır ve SQLite bedeli TABANIN ALTINA indi; `test_h57_h58_arama` 225 test ileri+ters yeşil, PG ikizi taze veritabanında 218/218 ileri+ters 0 atlama, izole süit 479/479 (4099 sonuç), 14 mutasyonun 14'ü öldü (M11 `translate` silme kuralı ilk turda HAYATTA kaldı, sözleşme testi eklendi); bedel: 20k satır × 2 kolon SQLite 63→52 ms, PG 39→176 ms; H47 (#151, `statement.py` Core) sonrasi develop 82254b2 uzerine rebase: catisan TEK satir toplam pindi ve TARAYICI KOSTURULARAK olculdu (269→274), aritmetikle DEGIL; 62 dosyanin 62'si eksiksiz, `statement.py` 5 / `search.py` 7; backend/app yama kimligi 68bdcd84dbf2 DEGISMEDI (iki mercek GO'su tasinir); OpenAPI tabanla bayt-özdeş; 2026-09-20 ikinci rebase, develop 6ad974f uzerine (H51 #152 + H47 #151 sonrasi): catisan TEK satir `routers/finance.py` parmak iziydi ve iki tarafin pini de ATILDI — develop'ta H46 (PUT /payments cek/senet yontem ve evrak kilidi), dalda H57 katlamasi, yani dogru deger ancak BIRLESMIS AGACTA tarayici kosturularak bulunur: 3cca3886→b8519168, dinamik text() cagri sayisi 7de SABIT ve toplam 274 KIMILDAMADI; `backend/app`+`alembic` yama kimligi rebase ONCESI ve SONRASI AYNI (30e19e912b5a, `git diff <taban> <bas> -- backend/app backend/alembic | git patch-id --stable`); taze veritabaninda PG ikizi 218/218 ileri+ters 0 atlama, `test_h57_h58_arama` 225 ileri+ters, kiraci kapisi 33, Core envanteri 118, SEC-3b 157, ci_gates 26, kontrat drifti (openapi+types.gen.ts+fixture) FARKSIZ — <merge tarihi>
