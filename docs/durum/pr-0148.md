#148 — H57+H58 arama hijyeni (backend, göç YOK): tek ortak dikiş `app/arama.py` — `arama_deseni` (katlama + `\` `%` `_` kaçışı, `q` hep bağlı parametre) ve `katli_sql` (kolonu aynı tabloyla katlayan SQL ifadesi); TEK KURAL `ARAMA_ESLESME` tablosunun karakter başına uygulanmasıdır (Türkçe + Latin-1 aksanları + ASCII a-z→A-Z, `UPPER` YOK, SQL tarafı yalnız `REPLACE` zinciri) — düzeltme turu (mercek NO-GO 0580dcc): Python `.upper()` ile SQL `UPPER()` tablo dışı aksanlarda ayrışıyor, "Kâzım/hâlâ/Café" tabanda bulunup HEAD'de boş dönüyordu; `/customers` ve `/suppliers` arama parçaları modül sabitine alındı (`katli_sql` istek başına ÇAĞRILMAZ, AST pini 18 modül düzeyi çağrı), `barcode` da katlanır; `/api/customers`, `/api/suppliers`, `/api/payments`, `/api/finance/transactions` ve `/api/search` bu dikişi kullanır, her LIKE `ESCAPE` taşır, kiracı yüklemleri aynı; "yılmaz/YILMAZ/Yilmaz/yilmaz" ve "Kâzım Hâlâ Tarım", "Café Deniz", "Émile"nin her yazımı beş uçta bulur, her adın her birebir alt dizgisi adı hâlâ getirir (gerilemezlik), `%` 0 satır, `_` ve `\` harfi harfine, B firması görünmez, maskeli rol aynı katlamayla arar; PG'de `katli_sql` öntanımlı, C, POSIX, und-x-icu, tr-TR-x-icu harmanlamalarında `arama_katla` ile aynı; dinamik text() 270→275 (search.py 2→7, customers 1 / finance 7 sabit, üç parmak izi yeniden türetildi), pg_twins 135→136; `test_h57_h58_arama` 224 test ileri+ters yeşil, PG ikizi taze veritabanında 214/214 ileri+ters 0 atlama, 10 mutasyonun 10'u öldü; bedel: 20k satır × 2 kolon SQLite 63→409 ms, PG 36→147 ms; OpenAPI tabanla bayt-özdeş — <merge tarihi>
