#148 — H57+H58 arama hijyeni (backend, göç YOK): tek ortak dikiş `app/arama.py` — `arama_deseni` (`niyet.tr_katla` ASCII katlaması + `\` `%` `_` kaçışı, `q` hep bağlı parametre) ve `katli_sql` (kolonu aynı eşlemeyle katlayan lehçe-güvenli SQL ifadesi); `/api/customers`, `/api/suppliers`, `/api/payments`, `/api/finance/transactions` ve `/api/search` bu dikişi kullanır, her LIKE `ESCAPE` taşır, kiracı yüklemleri aynı; "yılmaz/YILMAZ/Yilmaz/yilmaz" beş uçta da bulur, `%` 0 satır, `_` ve `\` harfi harfine, B firması görünmez, maskeli rol aynı katlamayla arar; dinamik text() 270→275 (search.py 2→7, customers/finance sayısı sabit, parmak izleri tarayıcıdan), pg_twins 135→136; SQLite 82 test ileri+ters yeşil, PG ikizi taze veritabanında 70/70; OpenAPI tabanla bayt-özdeş — <merge tarihi>
