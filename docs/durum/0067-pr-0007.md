#7 — `verify-image-artifact` işi bağımsız bir kimlik karşılaştırma betiğine (`deploy/artifact-imaj-kimlik-kapisi.sh`) bağlandı; çağrı yeri YAML yapısı `deploy/ci-verify-cagri-kapisi.py` (K5) ile fail-closed kilitlendi: (1) adım-seviyesi `if: false`, (2) yinelenen `run:` anahtarı, (3) `BASH_ENV` enjeksiyonu, (4) `PATH` ezilmesi / sahte docker ikilisi, (5) yanlış/hook'lu run komutu, (6) workflow kökü `env:`/`defaults:` kaçışları parse düzeyinde yakalanır; gate emekliliği politikası ("Gate konusu kapanmıştır. Yeni gate eklenmeyecek") gereği K9 ve K10 gate aileleri tamamen geri çekildi; KABUL EDİLEN ARTIKLAR (ACCEPTED HOLES): (1) K5 yalnız çağrı yerini garanti eder, kimlik betiğinin iç mantığını doğrulamaz; (2) deploy-sozlesme-testi.sh için çalışma-anı denetim kümesi yayma kapısı (K10) yoktur; (3) `deploy/artifact-imaj-kimlik-kapisi.sh` veya `deploy/deploy-sozlesme-testi.sh` üzerindeki tekil dosya değişiklikleri CI gate'leri ile değil, PR incelemesi ve repo sahipliği süreçleriyle korunur.

