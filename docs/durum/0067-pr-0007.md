#7 — `verify-image-artifact` işi bağımsız bir kimlik karşılaştırma betiğine (`deploy/artifact-imaj-kimlik-kapisi.sh`) bağlandı; çağrı yeri YAML yapısı `deploy/ci-verify-cagri-kapisi.py` (K5) ile fail-closed kilitlendi: (1) adım-seviyesi `if: false`, (2) yinelenen `run:` anahtarı, (3) `BASH_ENV` enjeksiyonu, (4) `PATH` ezilmesi / sahte docker ikilisi, (5) yanlış/hook'lu run komutu, (6) workflow kökü `env:`/`defaults:` kaçışları parse düzeyinde yakalanır; gate emekliliği politikası ("Gate konusu kapanmıştır. Yeni gate eklenmeyecek") gereği K9 ve K10 gate aileleri tamamen geri çekildi; GERÇEK SAHTE ARTIFACT ÖLÇÜMÜ (PR #17 deneyi): `verify-image-artifact` ilk kez gerçek bir sahte artifact değişiminde kırmızı gözlemlendi; negatif kontrolde (koşum 33443836851, 128518344 bayt tarball) container ve verify işi yeşil geçerken, pozitif kontrolde (koşum 33444328333, 1578 bayt sahte tarball) container yeşil geçti ve verify-image-artifact işi 'sha256:57e38c8710c6f2d32b767cf03fd33b51237681128b2c5ee7d3d77dccc45d0324' yüklenen ile 'sha256:0ca6f80c00b05fb2cafa79f86e51da9da33899038ec1435ec59458fc5a4627e1' beklenen uyuşmazlığında exit 1 ile kırmızıya düştü; gerçek Docker daemon ile betiğin 8 girdi durumunda (yanlış kimlik, yanlış revizyon, her değişkenin boş olması, tümünün tanımsız olması, eksik imaj, PATH'te docker olmaması) fail-closed olduğu ölçüldü; KABUL EDİLEN ARTIKLAR (ACCEPTED HOLES): (1) ÖLÇÜLDÜ: K5 yalnız çağrı yerini kilitler; betik gövdesindeki 5 tek satırlık değişiklik (erken `exit 0`, `trap 'exit 0' EXIT`, karşılaştırmaya `|| true`, değeri kendisiyle karşılaştırma, tersine karşılaştırma) yanlış kimlikte exit 0 üretebilir; betik içi mantık K5 dışındadır ve inceleme ile savunulur; (2) deploy-sozlesme-testi.sh için çalışma-anı denetim kümesi yayma kapısı (K10) yoktur; (3) `deploy/` betik değişiklikleri CI gate'leri ile değil PR incelemesi ve repo sahipliğiyle korunur; (4) `actions/download-artifact@v4` K5 tarafından şart koşulan `if-no-files-found` girdisini yok sayar ('Unexpected input(s)'), pin atıldır ancak zararsızdır.

