#54 — 5.1b KİRACI YUMUŞAK İMHASI: firma KAPATILIR, veri SİLİNMEZ; GÖÇ YOK, YENİ TABLO YOK, TABAN `a2c5f61` (E1b #53 ve YENİDEN KUYRUKLAMA #55 indikten SONRA; dal ÜÇ KEZ tabanını değiştirdi — ilk oturuş `185bc44`, sonra `0d601ad`, şimdi `a2c5f61` — ve HER SEFERİNDE kayıt yeniden numaralandı: 0095 -> 0096 -> 0098, numara elle seçilmedi, `python scripts/durum.py --sonraki 54` verdi; SON REBASE'de `git rebase` DÖRT commit'i sırayla oynatmayı denedi ve ALTI dosyada çakıştı — ara commit'lerin bir kısmı zaten SONRAKİ commit tarafından geri alınıyordu (durum ucu, ayrı hata kodu), yani çakışmaları çözmek ölü durumları diriltmek olurdu; dal bunun yerine `git checkout -B feat/kiraci-imha origin/develop` ile TAZE tabana kuruldu ve NİHAİ durum yeniden uygulandı, iğnelerin TAMAMI yeni ağaçta YENİDEN ÖLÇÜLDÜ) (`origin/develop`, `git rev-parse` ile DOĞRULANDI — oturum başı anlık görüntüsü `23902cf` gösteriyordu ve O BAYATTI); SERT İMHA DENENMEDİ ve bu bir TERCİH DEĞİL ÖLÇÜM: `activity_logs` (göç `20260727_0030`) ve `notifications_archive` (`20260728_0033`) üzerinde `BEFORE DELETE` tetikleyicileri var, silme veritabanı düzeyinde reddediliyor ve bir kaskad denemesi işlemin ORTASINDA patlayıp YARIM silinmiş bir kiracı bırakırdı; UÇ TEK: `POST /api/company/erase` (`__admin_only__`, dışa aktarımla AYNI ad). İlk turda bir de `GET /api/company/status` (`read`) vardı ve SAHİP KARARIYLA KALDIRILDI — ÖLÜ YÜZEY: kapalı firma ara katmanda düştüğü için o ucun gövdesine HİÇ varılamaz, yani `is_active: false` DÖNDÜREMEZ ve sabit `true` yazan bir gövdeyle HTTP üzerinden ayırt edilemezdi; `auth.py`deki `read` satırı da onunla birlikte gitti; ETKİ TEK İŞLEMDE, TEK COMMIT: `companies.is_active=false` + bu firmanın `user_company_memberships` satırları + TEK `company.erased` aktivite satırı — `get_db` commit ETMİYOR (ÖLÇÜLDÜ: yalnız `close()`) ve `log_activity` de etmiyor, yani üç yazma da uçtaki TEK `commit()`e bağlı; ilk turda o `commit()` YOKTU ve smoke dışa aktarım yanıtını 200 zip olarak okuyup UnicodeDecodeError verdi — kusur TESTTE DEĞİL KODDAYDI ve bu kayda adıyla geçiyor; KİLİT TEK YERDE — `tenancy.resolve_company`nin ZATEN VAR OLAN `companies.is_active.is_(True)` yüklemi — ve bu dilim oraya HİÇBİR ŞEY EKLEMEDİ; uçlara tek tek kapı TAKILMADI: gözden kaçan tek bir uç kilidi yalan yapardı, üstelik sonradan eklenen her uç aynı riski yeniden doğururdu; kilit DÖRT AYRI ROUTER'ın rotasında ölçülüyor (products, customers, dashboard, work_orders) ve dördü de AYNI `COMPANY_ACCESS_DENIED` gövdesini veriyor; KİLİDİ GERÇEKTEN TAŞIYAN ŞEY ÜYELİK SİLME DEĞİL `is_active` YÜKLEMİDİR ve bu ÖLÇÜLDÜ: smoke üyeliği doğrudan veritabanından GERİ VERİYOR ve aynı istek yine 403 alıyor — yüklem kaldırılınca o satır 200 ölçüyor; SÖZLEŞME DEĞİŞTİRİLMEDİ — İLK TURDA DEĞİŞTİRİLMİŞTİ, SAHİP KARARIYLA GERİ ALINDI ve `app/tenancy.py` develop ile BİREBİR AYNI kaldı (ölçüldü: `git diff origin/develop -- backend/app/tenancy.py` BOŞ). İlk tur `resolve_company`ye kapalı firmayı ayrı bir kodla (`COMPANY_INACTIVE`, `FirmaPasifError`) düşüren bir dal eklemişti; gerekçe, imhadan sonra üyelik silindiği için kullanıcının "kiracı kapandı" ile "kimliği yanlış yazdım"ı ayırt edememesiydi. KARAR: o ayrım, kimliği doğrulanmış HERHANGİ bir kullanıcıya, açıkça `X-Company-ID` yazarak bir kimliğin "var olan ama kapatılmış firma" olduğunu öğrenme yolu açıyor ve kiracı yaşam döngüsünün dışarıya sızmaması bu bilgiden daha değerli; dal, `FirmaPasifError` sınıfı ve ara katmanın `getattr(exc, kod, ...)` okuması TAMAMEN kaldırıldı — kapalı firma artık üye olmayanla AYNI `COMPANY_ACCESS_DENIED` gövdesini alıyor; `tests/test_tenancy_inactive_company.py`nin ÖZGÜN sözleşmesi (`detail == 'Bu firmaya erişim yetkiniz yok'`) GERİ GELDİ ve dosya develop'unkine eş; ilk turda eklenen üye-olmayan kapısı SİLİNMEDİ, çünkü GEREKSİZ OLMADIĞI ÖLÇÜLDÜ: yeniden yazılan `test_non_member_cannot_tell_active_from_inactive_from_absent` üye OLMAYAN için ÜÇ hedefin (yabancı AKTİF firma, yabancı KAPALI firma, hiç var olmayan kimlik) TİP, DURUM KODU ve METİN olarak AYIRT EDİLEMEDİĞİNİ ölçüyor ve ayrımı YALNIZ üye olmayan için yapan bir mutasyon (M11b) denendiğinde dosyanın diğer ÜÇ testi YEŞİL kalıp yalnız bu kapı KIRMIZI yandı — yani sızıntının en zararlı biçimi ancak burada görünüyor; ÜYELİKLER SİLİNİR HESAPLAR SİLİNMEZ: `app_users` GLOBALDİR (`company_id` taşımaz, ölçüldü) ve tek üyeliği bu firma olan kullanıcı GİRİŞ YAPABİLİYOR ama hiçbir firmaya çözülemiyor (`companies` listesi BOŞ) — hesabı silmek onu kiracı sınırının DIŞINDA yok etmek olurdu ve bu ucun yetkisi tek kiracı kadardır; JETONLAR SİLİNMİYOR ve gerekçesi ÖLÇÜLDÜ: `auth_tokens`/`auth_refresh_tokens` KULLANICI başınadır (ikisinde de `company_id` YOK), silmek kullanıcıyı BAŞKA firmalardaki oturumundan da atardı ve gerekmiyor — geçerli jetonla gelen istek de kiracı çözümünde düşüyor; ONAY ADI BİREBİR (`turkce_katla` YOK, `strip()` YOK, `lower()` YOK) yoksa 422 ve reddedilen deneme HİÇBİR yan etki bırakmıyor (bayrak, üyelik, aktivite kaydı üçü de ölçüldü); İKİNCİ ÇAĞRI için adı konmuş `FirmaZatenKapaliError` -> 409 `COMPANY_ALREADY_INACTIVE` (`disa_aktarim_errors` ailesinin AYNI biçimi, `main.py`de tek işleyici) ve BU YOL BUGÜN HTTP'DEN ERİŞİLEMEZ — ikinci çağrı ara katmanda düşer; dal yine de yazılı çünkü tek denetim kaydı garantisini kilit gevşerse O tutar, ve ölçmenin TEK yolu fonksiyonu DOĞRUDAN çağırmaktır (`test_ikinci_cagri_dogrudan_409_uretir`); STATİK KAPI — router kaynağında her `delete()` çağrısının argümanı `memberships` ve yürütülen hiçbir metin sabitinde silme/düşürme/boşaltma sözcüğü YOK (docstring'ler AST'ten ayıklanıyor, kapı AÇIKLAMAYI değil YÜRÜTÜLEN metni arıyor) artı adın GERÇEKTEN `user_company_memberships` olduğu içe aktarılarak doğrulanıyor; ÖLÇÜLEN KUSUR KAPSAMDA DÜZELTİLDİ — `field_stok_tuketici.tum_firmalari_isle` firma listesini YÜKLEMSİZ okuyordu: bu döngü HTTP'den GEÇMEZ, uygulama sürecinde koşar ve kiracı çözümünü HİÇ görmez, yani kapatılmış bir kiracının bekleyen outbox olayları işlenmeye devam eder ve `stock_movements`e YENİ satır yazardı — kapatıldı iddiası tam da DENETLENMEYEN tek yazma yolunda yalan olurdu; TEK PREDİKAT eklendi ve BAĞLI PARAMETRE olarak yazıldı (`WHERE is_active = :aktif`), sabit doğruluk METNİ DEĞİL: PG'de boolean, SQLite'ta 1 bağlanır ve iki lehçe de aynı satırları döndürür; MUTASYONLAR (on ikisi de tek tek uygulandı, adı geçen test KIRMIZI oldu, geri alındı ve `git diff` ile temizlik doğrulandı): `is_active` yüklemi düştü -> `test_kilit_kiraci_cozumunde_dort_ayri_rotada` + iki tenancy testi; üyelik silme firma yüklemsiz -> `test_uyelikler_yalniz_bu_firmadan_dustu`; onay adı `strip().lower()` -> `test_yanlis_onay_adi_422_ve_yan_etkisiz`; zamanlayıcı yüklemi düştü -> `test_zamanlayici_kapatilmis_firmayi_atlar`; `log_activity` silindi -> `test_tek_aktivite_kaydi`; bayrak `False` -> `True` -> üç test; 409 dalı silindi -> `test_ikinci_cagri_dogrudan_409_uretir`; erase izni `read` -> iki test; durum ucu tek `commit()` silindi -> modül fikstürü 6 hata; `delete()` satırı tamamen silindi -> statik kapı + üyelik testi; kaldırılan sızıntı GERİ KONDU (kapalı firmaya ayrı kod) -> `test_explicit_inactive_company_selection_is_rejected` + `test_non_member_cannot_tell_active_from_inactive_from_absent`; AYNI sızıntı YALNIZ üye olmayan için kondu (M11b) -> SADECE `test_non_member_cannot_tell_active_from_inactive_from_absent` — bu ölçüm, o kapının gereksiz OLMADIĞINI kanıtlıyor ve silinmemesinin sebebidir; İĞNELER (SAHİP KARARINDAN SONRA HEPSİ YENİDEN TÜRETİLDİ, İLK TURUNKİLER DEVRALINMADI): `ACTION_TYPES` küme+sayı 61 -> 62 (`company.erased`; TABAN 61'dir çünkü #55 `field_event.requeued`i EKLEDİ — çakışan tek satır buydu ve iki giriş de KORUNDU; kaynak tipi dışa aktarımla AYNI backup, `RESOURCE_TYPES` 18'de SABİT, yeni kaynak tipi AÇILMADI), GET izin envanteri 172'de SABİT ve parmak izi `bd1b5dd1`de SABİT — dosya develop'unkine EŞ, çünkü dilim artık HİÇBİR GET ucu getirmiyor (ilk turdaki 173/`2dde5236` GERİ ALINDI), rota güvenlik sözleşmesi 365/281 -> 366/282 ve parmak izi `d11a83eb` -> `cec015e0` (TEK yol, TEK işlem; TABAN E1b ve #55 ile 360/278'den 365/281'e çıkmıştı, bu satırlar YENİ AĞAÇTA ölçüldü, önceki turun 361/279 / `acecff24` değerleri ARİTMETİKLE TAŞINMADI), Core sorgu envanteri 140 -> 144 (select 92->94, update 41->42, delete 7->8) ve parmak izi `16f1e954` -> `6031430b` — DÖRDÜ DE `routers/kiraci_imha.py`de, `app/tenancy.py`ye HİÇBİR sorgu EKLENMEDİ; TABAN 140'ta KALDI ve bu bir ÖLÇÜMDÜR: E1b ve #55 Core envanterini KIMILDATMADI, ikisinin de SQL'i `text()` ile yazıldı, Core kiracı kapsamı 120 -> 122, yetki popülasyonu 355/93/102/67 -> 356/93/102/67 (YALNIZ kimliklenmiş sayaç kımıldadı; `read` 93'te SABİT KALMASI iznin `__admin_only__` olduğunun TANIĞIDIR ve UNDENIABLE 102'de SABİT çünkü o izin admin dışındaki HER rolü reddeder; TABAN E1b/#55 ile 350/93/100'den 355/93/102'ye çıkmıştı ve ÜÇÜ DE yeniden ölçüldü), alt süreç SQL sayacı 111/184 -> 112/188 (TABAN #55 ile 110/177'den 111/184'e çıkmıştı; YENİ AĞAÇTA ölçüldü), `activity_log.py` parmak izi `3a0bd7a2` -> `0a19ef72` (dinamik `text()` 3'te SABİT, YENİ DİNAMİK SQL YOK; parmak izi yalnız ACTION_TYPES sözlük LİTERALİ büyüdüğü ve dosya AST'sinin TAMAMINDAN türediği için kımıldadı) ve `tenancy.py` parmak izi `45d7b295`te SABİT — o dosyaya bu dilim HİÇ DOKUNMADI (E1b onu `farm_plantback_policy` sütunu için değiştirdi, bu dilim DEĞİL); GET izin envanteri de SABİT (dilim hiçbir GET ucu getirmiyor) ve `test_route_get_permission_inventory.py` develop'unkine BİREBİR EŞ; `UNRESOLVED_ALLOWLIST` BÜYÜMEDİ ve bu bir ÖLÇÜM: ilk turda okunabilirlik için konan `UYELIKLER = memberships` takma adı `test_core_query_inventory` tarayıcısının HEDEF TABLOYU ÇÖZEMEMESİNE yol açtı ve iki sorgu güvenli sayılamaz kovasına düştü — takma ad KALDIRILDI, gerekçesi kaynağa yazıldı; `auth.py`deki İKİ İZİN SATIRI da BUGÜN SONUCU DEĞİŞTİRMİYOR ve bu ÖLÇÜLDÜ (silinince imha yine deny-by-default ile `__admin_only__`, durum yine genel kuralla `read`) — ilk turda yazılan "yazılmasaydı nöbetçiye düşerdi" gerekçesi YANLIŞTI ve DÜZELTİLDİ: satırlar bir gün /api/company önekli bir kural eklenirse sessiz kaymayı önlemek için duruyor, ve TAM EŞLEŞME zorunlu çünkü o önek `/api/company-settings`i de yakalar; KOŞUMLAR: KANONİK KOŞUM `python run_isolated_tests.py --workers 4` (her dosya KENDİ alt sürecinde ve KENDİ çalışma alanında) — 394/394 aktif test dosyası GEÇTİ, 2172 test sonucu, 823.6s, ÇIKIŞ 0 — TABAN `a2c5f61` ağacında; İLK koşum `tests/test_tenant_scoping_guard.py` ile KIRMIZI döndü (alt süreç metin sayacı 188 ölçüldü, 189 gerçekleşti) ve sebebi ADIYLA yazılıyor: sayaç PG ikizine vaka EKLENMEDEN ÖNCE çivilenmişti, eklenen gömülü betik o yüzeyi bir artırdı; ders, iğnenin SON kaynak değişikliğinden SONRA ölçülmesi gerektiğidir — 189 yazıldı ve koşum tekrarlandı; `tests/test_kiraci_imha.py` 14 test; ilk turda bildirilen `pytest backend/tests` (1015 passed) BU KOŞUMUN YERİNE GEÇMEZ ve `test_v2_7_tenant_security.py` hakkındaki "taban hatası" ifadesi EKSİKTİ — ÖLÇÜLDÜ: o dosya `conftest.py`nin `collect_ignore` listesindeki 21 legacy scriptten biridir (`LEGACY_TEST_MIGRATION_PLAN.md` bunu "başarı iddiası değil, dönüşüm backlogu" diye adlandırıyor), izole koşucu listeyi conftest'ten AST ile okuyup dosyayı manifestine HİÇ ALMIYOR (manifest 391, dosya İÇİNDE DEĞİL), ve `collect_ignore` yalnız DİZİN toplamasında geçerli olduğu için dosya ancak AÇIKÇA ADIYLA çağrıldığında toplanmaya çalışılıp düşüyor — yani CI yeşilliği bir maskeleme DEĞİL, bildirilmiş bir karantinadır; BULGU (bu dilimde DÜZELTİLMEDİ, kapsam dışı): karantina listesinin BOYUTUNU ya da İÇERİĞİNİ çivileyen HİÇBİR kapı yok (`collect_ignore` yalnız `run_isolated_tests.py` tarafından okunuyor, hiçbir test onu iddia etmiyor), yani listeye yeni bir dosya eklemek testleri SESSİZCE koşumdan düşürebilir; PG 16 ikizi GERÇEK PostgreSQL'de koşuldu (docker `pglens` :5433, HER KOŞUMDA taze veritabanı, sürücü `postgresql+psycopg` — v3, psycopg2 DEĞİL): `test_field_stok_tuketici_postgresql.py` 21 passed, SIFIR ATLAMA; O DOSYAYA BU TURDA BİR VAKA EKLENDİ ve gerekçesi ÖLÇÜLDÜ: zamanlayıcı yüklemi HAM `text()` SQL'dir ve `is_active`i BAĞLI parametreyle sorar — SQLite `is_active`i 0/1 TAMSAYI, PostgreSQL gerçek `boolean` saklar, yani `= TRUE` ya da `= 1` METNİ yazılsaydı lehçelerden BİRİ sessizce yanlış kümeyi döndürürdü ve iddia yalnız geliştirme lehçesinde doğrulanmış olurdu; yeni vaka AYNI PENDING olayı İKİ TUR koşuyor (firma KAPALI -> `PENDING`/0 hareket/0 girdi, firma AÇIK -> `SENT`/1 hareket/1 girdi) — tek tur yetmezdi, çünkü "hiçbir şey işlenmedi" bulgusu bozuk kurulumla da açıklanabilirdi ve ikinci tur ilk turdaki sessizliğin YÜKLEMDEN geldiğini kanıtlıyor; bayrak `finally` içinde GERİ ALINIYOR, aynı veritabanını paylaşan sonraki testler kapalı firmayla başlamasın diye; MUTASYON GERÇEK PG'DE DE ÖLÇÜLDÜ: yüklem kaldırıldığında vaka `KAPALI DURUM SENT HAREKET 1` ile KIRMIZI yandı; GÖREV METNİ "#55 ile ikiniz de bu dosyaya dokundunuz, iki vakayı da birleştirin" diyordu — ÖLÇÜLDÜ ve DÜZELTİLDİ: dosyaya YALNIZ #55 dokunmuştu (`git diff 0d601ad pre-rebase -- <dosya>` BOŞ), bu dal onu HİÇ DEĞİŞTİRMEMİŞTİ; birleştirilecek bir çakışma YOKTU, eksik olan şey benim vakamın PG'de HİÇ OLMAMASIYDI ve o eksik bu turda kapatıldı; `test_field_stok_0061_postgresql.py` 2 passed; YENİ BİR PG İKİZ DOSYASI AÇILMADI ve gerekçesi 5.1a ile AYNI: dilim şema açmıyor ve yeni testlerin hiçbiri SQLite'ta ATLANMIYOR, PG popülasyonu SABİT kaldı; SÖZLEŞME ZİNCİRİ `export_openapi.py -o` -> `npm run types:gen` -> `capture_frontend_fixtures.py`, delta YALNIZ `types.gen.ts`, fixture DEĞİŞMEDİ, `npx tsc -b` çıkış 0 (`npm ci` gerekti: bu worktree'de `node_modules` YOKTU); ÖLÇÜLMEDİ, ADIYLA: (1) PII ANONİMLEŞTİRME YAPILMADI — kapatıldı ile silindi AYNI ŞEY DEĞİL ve 5.1 keşfinde adı konmuş PII taşıyan tablolar (`customers`, `suppliers`, `entity_contacts`, `work_orders`) OLDUĞU GİBİ duruyor; uç bu tablolara dokunmuyor ve `test_pii_anonimlestirme_bu_dilimde_YOK` hem kapsam notunun kaynakta KALMASINI hem uçun o adlara DOKUNMAMASINI çiviliyor; (2) TAZE YEDEK ŞARTI YOK — aranmıyor, çünkü tazenin tanımı burada ölçülemez; yanıt bunun yerine `export_hint` taşıyor ve dışa aktarımın imhadan SONRA erişilemeyeceğini AÇIKÇA söylüyor; (3) GERİ ALMA UCU YOK — kapatılmış firmayı yeniden açan bir yol yazılmadı; (3b) DURUM OKUMA UCU YOK — `GET /api/company/status` yazıldı ve KALDIRILDI (yukarıda); firmanın açık olduğunun sinyali, ucun CEVAP VEREBİLMESİDİR; (4) FRONTEND YÜZEYİ yazılmadı, yalnız `types.gen.ts` türedi; DAL `feat/kiraci-imha`, worktree `F:/nazgul-51b`; açılıştan önce `gh pr list` BOŞTU ve `git ls-remote --heads origin`de bu dal YOKTU; `farm.py` ve bitki koruma dosyalarına DOKUNULMADI (E1b).
