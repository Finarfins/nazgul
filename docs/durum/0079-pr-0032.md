#32 — POISONER DÜZELTİLDİ, kurban değil: `tests/test_security_audit_visibility.py` fikstürü `uygulama` `sys.modules`ten her `app` / `app.*` anahtarını siliyordu (eski satır ~43-44 ve ~60-61) ki `DATABASE_URL` / `SECRET_KEY` / `BOOTSTRAP_ADMIN_PASSWORD` / `SUNGUR_PLATFORM_OPERATORS` taze içe aktarmada işlesin; TABAN `caf41140` (develop; ölçüldü `git rev-parse HEAD` işe başlamadan); `backend/app` SIFIR satır (`git diff --stat caf41140 -- backend/app` BOŞ), `test_*postgresql*.py` EKLENMEDİ, PR #30'un üç onarımı (`test_field_stok_zamanlayici.py` / `test_self_service_exemption.py` / `test_supplier_price_bridge_units.py`) DOKUNULMADI; SÜPÜRME ÖNCE (zehir yerinde, poisoner-first: `-p no:randomly -p no:cacheprovider -q tests/test_security_audit_visibility.py` sonra geri kalan `tests` `--ignore=tests/fixtures`, taze `DATABASE_URL=sqlite:////tmp/poisoner-sweep/blast.db`) 2 failed / 837 passed (839 collected, 381.70s) — iki ad: `test_import_route_body_limit.py::test_app_registers_import_override_for_the_import_router` ve `::test_work_order_routes_keep_the_strict_global_body_limit` (ikisi de `StopIteration`: koleksiyon-anı `RequestBodyLimitMiddleware` ile yeniden içe aktarılmış `app.main.app.user_middleware[].cls` `is` karşılaştırması); `test_authorization_population_reconciliation.py:226` ve `test_field_outbox_writer.py:248/290/304` bu sırada YEŞİL kaldı (ölçüldü, araya giren dosyalar `app.main`i yeniden yükler; field_outbox zaten `importlib.import_module("app.main")` ile doldurur); DOSYA YALNIZ ÖNCE 11 passed (15.43s); FİKSTÜR ŞEKLİ: `_alt_surec` / `_temiz_ortam` (`test_donmus_saat_kapisi` ve `test_tenant_scoping_guard` canlı-envanter çocuğu ile aynı: `sys.executable -c`, kopya env, `cwd=backend`, `PYTHONIOENCODING=utf-8`), `_cocukta_sema_kur` çocuğa `import app.main` verir (göç+tohum; çocuk metninde SQL YOK — `test_alt_surecte_sql_sayisi_donduruldu` 102/165 SABİT, ölçüldü), ebeveyn anlık görüntüyü `_ebeveyn_app_modullerini_geri_yukle` ile geri koyar; SENTINEL `test_uygulama_fiksturu_app_sys_modules_anahtarlarini_birakmaz` (`set(k for k in sys.modules if k=='app' or k.startswith('app.'))` önce/sonra eşit); SÜPÜRME SONRA (aynı poisoner-first komut, `after2.db`) 840 passed / 0 failed (403.00s; +1 sentinel); İKİ YÖN: `git show caf41140:backend/tests/test_security_audit_visibility.py` geri kondu, aynı komut 2 failed / 837 passed (379.97s) — AYNI iki ad, AYNI `StopIteration`; düzeltme `git checkout --` ile geri alındı ve `git status --porcelain` BOŞ; DOSYA YALNIZ SONRA 12 passed (38.96s; 11 eski iddia + sentinel, eski assert'ler değişmedi); `git diff --stat caf41140 -- backend/app` yine BOŞ; ÖLÇÜLMEDİ: bu PR'ın KENDİ CI koşusu (kayıt commit'in içindedir), `run_isolated_tests.py` dört shard'ının yerelde tam koşusu, frontend, PostgreSQL ikizleri, Windows süpürmesi. ÖLÜ MEKANİZMA ÇIKARILDI: `_cocukta_sema_kur` ve yalnız onun çağırdığı `_alt_surec`, lensin ölçtüğü gibi (çağrı silinince dosya 12/12 yeşil) çünkü ebeveynin geçici silme+içe aktarması `denetim.db` üzerinde şemayı zaten kuruyor; `_temiz_ortam` hâlâ `DATABASE_URL` için duruyor. ENCODING DÜZELTMESİ YOK: `subprocess.run` kalmadığı için `encoding="utf-8", errors="replace"` eklenmedi.
