#55 — OUTBOX YENİDEN KUYRUKLAMA (FIELD_STOK_OUTBOX AÇILIŞ KOŞULU 3): **GÖÇ YOK**, TABAN `bb5663c` (`origin/develop`; ilk oturuş `185bc44` üzerindeydi, İŞLEM SIRASINDA ÖNCE #52 (`0d601ad`) SONRA E1b/#53 (`bb5663c`) indi — İKİ KEZ REBASE EDİLDİ; oturum başı anlık görüntüsü `23902cf` gösteriyordu ve O DA BAYATTI, her tur `git rev-parse origin/develop` ile DOĞRULANDI); dal `feat/outbox-requeue`, worktree `F:/nazgul-requeue`; KOŞULUN CÜMLESİ: "Tüketici yalnız `PENDING` seçer; `SKIPPED_*`/`DEAD` yazılan satır bir daha ASLA seçilmez ve onu `PENDING`e döndüren hiçbir mekanizma yok" — bu iş o mekanizmadır ve BAŞKA HİÇBİR ŞEY YAPMAZ (tüketici davranışı, `FIELD_STOCK_OUTBOX_ENABLED` varsayılanı, `farm.py`/`tenancy.py`/`kiraci_*` DOKUNULMADI); UÇ `POST /api/field-integration-events/{olay_id}/requeue`, izni `farm.manage` ve bu ÖLÇÜLDÜ VARSAYILMADI (`required_permission("POST", ...)` -> `farm.manage`; yol koşul 2'de `auth._FARM_PATH_PREFIXES`e ZATEN eklenmişti, eklenmemiş olsaydı `/api/field` genel kuralı onu SESSİZCE `field_service`e indirirdi — bu depoda iki kez yaşanmış tuzak) ve koşul 2 kaydının kendi şartını KARŞILIYOR ("yeniden kuyruklama ... geldiği gün kendi YAZMA iznini gerektirir — `farm.view` ona yetmez"); İZİN VERİLEN KÜME terminal durumlar EKSİ `SENT`tir (`SKIPPED_SOURCE_NOT_VISIBLE`, `SKIPPED_NO_PRODUCT`, `SKIPPED_TABAN_BILDIRILMEMIS`, `DEAD`) ve İKİNCİ BİR YERDE YAZILMADI: yüklem, okuma yüzeyinin `failed_only` süzgecini kuran `_kosul(..., failed_only=True)` dalından geliyor — sabit bir SQL metnine dört kova adı yazmak terminal küme için İKİNCİ bir doğruluk kaynağı olurdu ve C2'nin eklediği kova (`SKIPPED_TABAN_BILDIRILMEMIS`) ekranda görünüp uçta geri alınamaz kalırdı; SATIRDA DEĞİŞEN ÜÇ SÜTUN: `status` -> `PENDING`, `attempts` -> `0`, `updated_at` -> şimdi; SÜTUN SÖZLEŞMESİ ÖLÇÜLDÜ (göç 0044 + 0061): tablo `id, company_id, source_type, source_id, target, idempotency_key, status, attempts, last_error, processed_at, created_at, updated_at` taşıyor — `next_attempt_at` YOK (yazılacak sütun da yok, üstelik seçici `WHERE status='PENDING' ORDER BY id`dir ve böyle bir sütuna BAKMAZ), `requeued_at`/`requeued_by` de YOK; `attempts` SIFIRLANIR ve BU BİR KARARDIR, BEDELİ AÇIKÇA YAZILI: ilk niyet onu KORUMAKTI ("deneme geçmişini silme") ve ÖLÇÜLDÜ, o niyet mekanizmayı ATEŞLENEMEZ kılıyor — `_bir_olayi_isle` denemeyi `attempts + 1` hesaplar ve `deneme > AZAMI_DENEME` (3) ise olayı KAYNAĞA HİÇ BAKMADAN `DEAD` yazar, tavan kolu ise `attempts = :deneme` MUTLAK yazdığı için tavanla ölen satırın değeri **4**'tür; korunsaydı geri alınan satır bir sonraki döngüde 5 > 3 ile YENİDEN ölürdü, yani uç KOŞULUN VAR OLMA SEBEBİ olan sınıfta (deneme hakkı bitmiş olay) yalnız `updated_at`i kımıldatan bir gürültü olurdu — kaybolan bilgi YER DEĞİŞTİRİR: `last_error` KORUNUR ve tüketicinin kendi cümlesini taşır ("deneme tavanı aşıldı (3)"), `activity_logs` satırının `details`i ÖNCEKİ durumu VE ÖNCEKİ `attempts`i saklar (`"previous_attempts": 4` teste çivili); `processed_at` KORUNUR ve bu sıfırlanan `attempts`in İSTENEN karşılığıdır — `attempts=0` yazıldıktan sonra geri alınmış satırı HİÇ denenmemiş satırdan ayıran TEK sütun odur ve belgenin kendi şikâyeti ("hiç denenmemiş bir olayla ... AYIRT EDİLEMEZ") tam buydu, temizlemek onu BÜYÜTÜRDÜ; `SENT` GERİ ALINAMAZ (409 `EVENT_ALREADY_SENT`): gönderilmiş olayın stok hareketi YAZILMIŞTIR ve tüketici `stock_movements`ı hiçbir yolda UPDATE/DELETE etmez, yeniden gönderim "tekrar denemek" değil İKİNCİ BİR HAREKET yazmayı denemek olurdu — veritabanı ikinci hattı zaten tutuyor (göç 0060, `uq_stock_movements_field_event` kısmi benzersiz indeksi: olay + ürün başına EN FAZLA BİR hareket) ama ona GÜVENİLMEDİ çünkü kısıt bir `IntegrityError` üretir, uç ise okunur bir cevap borçludur; `PENDING`/`CLAIMED` satır 409 `EVENT_NOT_TERMINAL` alır ve DOKUNULMAZ; `SENT`in ADI BU MODÜLDE YAZILI DEĞİL, `field_stok_tuketici.DURUM_UYGULANDI`dan geliyor (fonksiyon İÇİNDE import: modül düzeyinde olsaydı okuma yönlendiricisi tüketiciyi ve zamanlayıcı yüzeyini import zincirine sokardı) — iki yerde yazılsaydı tüketici onu yeniden adlandırdığında kapı SESSİZCE açılırdı ve olay "terminal değil" gerekçesiyle reddedilirdi: DOĞRU sonuç, YANLIŞ gerekçe; KARARI VEREN ŞEY `status IN (<terminal>)` yüklemli KOŞULLU UPDATE'in rowcount'udur (tüketicinin `_talep_et`indeki desenin AYNISI) — önündeki SELECT yalnızca hangi 4xx'in döneceğini SEÇER, satırı kımıldatma yetkisi ONDA DEĞİL; ÇAPRAZ KİRACI 404 (403 değil: varlığı sızdırmamak için) ve var olmayan olay da AYNI cevabı alır; DENETİM İZİ GÖÇ İSTEMEDİ: `requeued_by` sütunu olmadığı için kimin hangi olayı hangi durumdan geri aldığı `activity_logs`ta `field_event.requeued` satırıdır (katalog **60 -> 61**, `RESOURCE_TYPES`e `field_integration_event`), satır ucun KENDİ işleminde yazılır ve TEK commit ile biter — denetimsiz bir yeniden kuyruklama OLUŞAMAZ, üstelik defter append-only olduğu için bu sütunun bir İKAMESİ değil ondan DAHA GÜÇLÜ bir izdir; YÜZEY ALANDAN BAĞIMSIZ KALDI: `OlayYuzeyi` betimleyicisine `yeniden_kuyruklanabilir` bayrağı eklendi ve uç YALNIZ o bayrağı taşıyan yüzeye kaydediliyor — `herd_integration_events`in bugün ne yazıcısı ne okuyucusu var, tüketicisi olmayan bir kuyrukta `PENDING`e döndürmek bir olayı DÜZELTMEZ, yalnız okuma yüzeyinde TAZE İŞ gibi göstererek bir başarısızlığı GİZLERDİ; kayıt fonksiyonu `deneme_sutunu is None` olan yüzeyi `ValueError` ile REDDEDİYOR (sıfırlanacak sayaç yoksa yukarıdaki tavan gerekçesi de tutmaz — sürü tablosunda o sütun YOK); TESTLER `backend/tests/test_outbox_requeue.py` (6 test: dört saf birim kapısı + iki alt süreç senaryosu) ve PG ikizine EKLENEN İKİ test (`test_field_stok_tuketici_postgresql.py`, YENİ İKİZ DOSYA AÇILMADI); DÖRT TERMİNAL KOVA ELLE YAZILMADI, GERÇEKTEN ÜRETİLDİ: ürünsüz sezon -> `SKIPPED_NO_PRODUCT`, taban birimi bildirilmemiş ürün -> `SKIPPED_TABAN_BILDIRILMEMIS`, `attempts=3` ile tavan -> `DEAD` (satırda 4 olduğu AYRICA ölçülüyor), kaynağı olmayan olay -> `SKIPPED_SOURCE_NOT_VISIBLE`, taban birimi yazılı ürün -> `SENT` + BİR hareket; `SENT` reddi tek başına durum koduyla değil HAREKET SAYISININ KIMILDAMADIĞIYLA da ölçülüyor (red ETKİSİZ olmalı, yalnız gürültüsüz değil); UÇTAN UCA BACAK BAYRAK AÇIKKEN KOŞUYOR (`FIELD_STOCK_OUTBOX_ENABLED=true`, `FIELD_STOCK_OUTBOX_INTERVAL_SECONDS=1`): olay `SKIPPED_TABAN_BILDIRILMEMIS` düşüyor, taban birim `PUT /api/products/{id}` ile yazılıyor, olayın KENDİLİĞİNDEN geri gelmediği 3 sn beklenerek ölçülüyor (koşulun var olma sebebi), sonra uç çağrılıyor ve olayı ÜRETİM ZAMANLAYICI THREAD'İ `SENT` yapıp hareketi yazıyor — doğrudan `olaylari_isle` çağrısı bu bacakta BİLEREK kullanılmadı, çünkü uç ile onu üretimde tüketecek şey arasındaki bağ ancak bayrak açıkken ölçülebilir; ÖLÇÜLEN VE ADIYLA YAZILAN BULGU — DÖRT MUTASYONUN İKİSİ SQLite DAVRANIŞ SENARYOSUNDA HAYATTA KALDI: `status IN (...)` yüklemini düşürmek ve `company_id`yi UPDATE'ten çıkarmak, ikisi de ÖNÜNDEKİ SELECT KAPISI tarafından zaten kapatıldığı için (terminal olmayan satır SELECT'ten sonraki dalda, çapraz kiracı SELECT'in kendisinde reddediliyor) YEŞİL kaldı; ikisi de savunmanın İKİNCİ hattıdır ve ikisi de kapatıldı — senaryo artık ucun GERÇEKTEN KOŞTURDUĞU SQL'i sürücü düzeyinde yakalıyor (`SqlKaydi`, kaynak dosya OKUNMUYOR: sorgu bir gün başka bir yardımcıdan gelse bile aynı şey ölçülür) ve UPDATE metninde `WHERE company_id=` ile `status IN (...)` yüklemlerini artı yer tutucu SAYISININ izinli kova sayısına eşitliğini çiviliyor; YÜKLEMİN DAVRANIŞ KANITI PG İKİZİNDE: bir süreç olay satırının kilidini AÇIK BİR İŞLEMDE tutuyor, uç isteği o kilidin üstüne gelip UPDATE'inde BLOKE oluyor, kilit sahibi satırı `DEAD -> PENDING -> CLAIMED` yapıp COMMIT edince istek çözülüyor ve yüklemini YENİ hâle karşı yeniden değerlendiriyor (READ COMMITTED) — pencere ZAMANLAMAYLA değil DOSYA İŞARETİYLE açılıyor, yani deterministik; ÖLÇÜLDÜ: yüklem yerindeyken `ISTEK 409 EVENT_NOT_TERMINAL` + satır `CLAIMED` + `DENETIM 0`, yüklem düşürülünce `ISTEK 200` + satır `PENDING` + `DENETIM 1` — yani uç uçuştaki bir işlemin ALTINDAN satır çekiyor; İKİNCİ PG TESTİ iki eşzamanlı isteği bariyerle aynı ana nişanlıyor ve TAM OLARAK bir 200, bir 409 ve TEK denetim satırı ölçüyor ("geri alındı" bir GEÇİŞTİR ve bir kez olur; iki denetim satırı iki kazanan demektir); PG KOŞUMU docker `pglens` PostgreSQL **16** üzerinde (:5433, `postgresql+psycopg` — sürücü **psycopg v3**, `psycopg2` DEĞİL), HER KOŞUMDA YENİ VERİTABANI; PG bacağı admin şifresini SATIRDAN yazıyor ve dosya bitiminde AÇILIŞ DURUMUNU (`admin123` + `must_change_password`) GERİ KOYUYOR — ikizler aynı veritabanını paylaşıyor ve şifre SIRAYA BAĞLI olurdu (`test_d2_avans_tescil_postgresql.py`de gerekçesiyle ölçülmüş tuzak); `change-password` ucu HİÇ kullanılmadı çünkü iki eşzamanlı sürecin aynı şifreyi değiştirmesi kendi başına bir yarış olurdu; MUTASYONLAR (dördü de tek tek uygulandı, adı geçen test KIRMIZI oldu, geri alındı): `SENT` kapısı kaldırıldı -> senaryo `SENT_RED` (ve kaldırılan kapının ürettiği "doğru sonuç, yanlış gerekçe" hâli çıktıda GÖRÜNDÜ: `EVENT_NOT_TERMINAL`), durum yüklemi düşürüldü -> SQLite yapısal kapı + PG `TALEP_EDILMIS` probu, `company_id` UPDATE'ten çıkarıldı -> SQLite yapısal kapı, `attempts` KORUNDU (sıfırlanmadı) -> senaryo "geri alinan olay YENIDEN ISLENMEDI"; İĞNELER (hepsi kapının kendisine ÖLÇTÜRÜLDÜ, aritmetikle türetilmedi): rota sözleşmesi 360/278 -> **361/279** ve parmak izi `8f3eb86b` -> `352cc9f9`, `EXPECTED_AUTHENTICATED` 350 -> **351** ile `EXPECTED_READ` 93 ve `EXPECTED_UNDENIABLE` 100 SABİT, `FARM_HERD_VIEW_OPERATIONS` **33'te SABİT** ve bu SABİTLİK BİR TANIKTIR (uç `farm.manage` istiyor, yani rol ile REDDEDİLEBİLİR — "kuyruğu görmek" ile "kuyruğu oynatmak" ayrı izinlerdir), aktivite kataloğu 60 -> **61**, dinamik SQL envanteri: `routers/entegrasyon_olaylari.py` çağrı 3 -> **5** ve parmak izi `644dd6ad` -> `c0fcbafb`, `app/activity_log.py` parmak izi `567f6d1d` -> `3a0bd7a2` (çağrı 3'te SABİT — kımıldayan şey iki KÜME LİTERALİ), küresel toplam 258 -> **260**, alt süreç SQL sayacı 110/177 -> **111/184** (yeni dosya `tests/test_outbox_requeue.py`, artı PG ikizine giren metinler); ÇEKİRDEK CORE SORGU ENVANTERİ (140) KIMILDAMADI ve bu ÖLÇÜLDÜ — eklenen iki sorgu ham `text()`tir, Core ifadesi DEĞİL; PG POPÜLASYONU KIMILDAMADI çünkü YENİ İKİZ DOSYA AÇILMADI (testler mevcut dosyaya eklendi); GET izin envanteri KIMILDAMADI (uç POST); SÖZLEŞME ZİNCİRİ: `export_openapi.py -o` -> `types:gen` -> `capture_frontend_fixtures.py`; delta YALNIZ `frontend/src/api/types.gen.ts` (+53), `pos-api.json` DEĞİŞMEDİ, `backend/openapi.json` `.gitignore`da; `npx tsc -b` çıkış 0 (`npm ci` gerekti: bu worktree'de `node_modules` YOKTU); BELGE GÜNCELLENDİ (`app/FIELD_STOK_OUTBOX_ACILIS_KOSULLARI.md`): koşul 3 üstü çizildi ve KARŞILANDI olarak yazıldı (yukarıdaki gerekçelerle birlikte), başlığa "1, 2 ve 3 KARŞILANDI; **4 DURUYOR**" satırı kondu ve "AYRI İŞLER OLARAK SIRADADIR" paragrafı koşul 4 için AYNEN geçerli kalacak biçimde daraltıldı — okuma yüzeyinin canlılık boşluğunu KAPATMADIĞI da oraya yazıldı (ölü bir thread ile boş bir kuyruk `summary` ekranında AYNI görünür); KAYIT NUMARASI 0095 -> 0096 -> 0097 ÇÜNKÜ ÖNCE #52 0095'İ, SONRA E1b/#53 0096'YI ALDI: numara hiçbir turda elle seçilmedi, her turda `python scripts/durum.py --sonraki 55` verdi ve satır içeriği BİREBİR taşındı; çakışmayı `--kapi` kapısı YAKALARDI (YİNELENEN SIRA), her rebase sonrası YEŞİL. #52 TURUNDA İĞNELER YENİDEN ÖLÇÜLDÜ ve HİÇBİRİ KIMILDAMADI (o dilim yalnız bir yetki iğnesinin paylaşılan sqlite URL'sini sızdırmasını durdurdu, göç AÇMADI). E1b/#53 TURU FARKLIYDI ve ÜÇ İĞNE DOSYASINDA ÇAKIŞTI — beklenen yerlerde, çünkü E1b `farm.py`ye, `auth` önek listesine ve `tenancy.py` sütununa dokundu (göç `20260907_0072`, `TENANT_TABLES` 110, PG popülasyonu 105): rota sözleşmesi TABANI 361/279 -> **365/281** oldu ve parmak izi `a49f58e6` -> `d11a83eb` YENİDEN TÜRETİLDİ, `EXPECTED_AUTHENTICATED` 351 -> **355** (E1b'nin dört ucu + bu ucun biri), `FARM_HERD_VIEW_OPERATIONS` 33 -> **35**'te SABİT (E1b'nin İKİ OKUMA ucu girdi; BU ucun POST'u yine GİRMEDİ ve sabitlik yine `farm.manage`in reddedilebilir olduğunun TANIĞI), dinamik SQL küresel toplamı 260 -> **262** (E1b `farm.py`ye iki çağrı ekleyip tabanı 258 -> 260 yapmıştı, bu dilimin iki çağrısı onun ÜSTÜNE bindi). ÖNCEKİ TURLARIN SAYILARI TABAN DEĞİŞTİĞİ ANDA GEÇERSİZ OLDU ve hepsi BİRLEŞMİŞ AĞAÇTA (CPython 3.12.10) YENİDEN ÖLÇÜLDÜ, ARİTMETİKLE TÜRETİLMEDİ. KIMILDAMAYANLAR DA ÖLÇÜLDÜ: `EXPECTED_READ` 93, `EXPECTED_UNDENIABLE` 102 (E1b onu 100 -> 102 yapmıştı, bu dilim BÜYÜTMEDİ), alt süreç SQL sayacı 111/184, çekirdek Core sorgu envanteri, GET izin envanteri, PG popülasyonu 105 (yeni ikiz dosya AÇILMADI), `entegrasyon_olaylari.py` çağrı 5 ve parmak izi `c0fcbafb` (E1b o dosyaya DEĞMEDİ). YAPILMADI, ADIYLA: (1) KOŞUL 4 (canlılık/gecikme sinyali) DURUYOR ve tek başına anahtarı kapalı tutmaya yeter; (2) `RECOVERY_FAILED` sınıfı bu uçla da KAPANMADI — o olay veritabanında iz bırakmaz, `PENDING` kalır ve bu ucun seçeceği bir işaret yoktur (belgenin kendi ölçümü, yeniden yazılmadı); (3) FRONTEND YÜZEYİ yazılmadı, yalnız `types.gen.ts` türedi — kuyruğu ekrandan geri alacak bir düğme YOK; (4) TOPLU yeniden kuyruklama YOK, uç olay BAŞINA çalışır (bir kovanın tamamını geri almak N istek demektir ve bu bilinçli: her geri alma AYRI bir denetim satırı ve AYRI bir karardır); (5) `company_id`nin UPDATE'teki varlığı DAVRANIŞLA ölçülemedi (SELECT kapısı önce kapatıyor) ve YALNIZ yapısal olarak çivili — bu bir eksik değil, kapının SINIRIDIR ve testin başlığında yazılı.
