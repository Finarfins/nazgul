#127 — E4b-1 e-İRSALİYE KISMİ SEVK: despatch_lines, GÖÇ 0087, KISMİ UBL (backend; frontend YOK, ReceiptAdvice E4b-2) — [CS2 #129 sonrası `989b735` üzerine yeniden tabanlandı]: göç `20260915_0087` (Şef: 0086 CS2'nin, `down_revision=20260914_0086`) `uq_despatch_notes_company_invoice`ı batch ile düşürür (SQLite yeniden inşası 0083'ün iki CHECK'ini, üç FK'sini ve `ix_despatch_notes_company_issue`i KORUDU, ölçüldü) ve `despatch_lines`ı §1 birebir kurar (CHECK quantity>0, `uq_despatch_lines_company_id`, `uq_despatch_lines_despatch_line`, `ix_despatch_lines_company_despatch`); İKİ FK DE BİLEŞİK: `(company_id,despatch_id)->despatch_notes` ve `(company_id,invoice_item_id)->invoice_items(company_id,id)` — hedef `uq_invoice_items_company_id` YOKTU (ölçüldü: invoice_items'a dokunan tek göç 0012), 0085/0071 kalıbıyla varlık denetimli eklendi, downgrade koşullu düşürür (ilk yazımda çıplak FK'ydi, Şef reddetti); SQLite yeniden inşası `ix_invoice_items_invoice` ve `invoice_id ON DELETE CASCADE`i KORUDU; GERİ DOLDURMA: her eski irsaliye faturasının LABOR olmayan kalemlerinden tam miktarla satır alır — ölçü: 1 irsaliye (LABOR 2 + PART 3.5 + PART 0) -> 1 satır (LABOR süzüldü, 0 miktarlı atlandı, product_id 77 source_snapshot'tan), SQLite ve PG16'da aynı; downgrade birden çok irsaliyeli fatura varken ADIYLA durur, tekleşince 1:1 kısıtı geri kurulur ve PG'de yeniden reddeder; UÇ: `POST /api/despatch-notes` isteğe bağlı `lines`, gövdesiz = LABOR dışı kalanlar; 422 SEVK_MIKTAR_ASIMI / HIZMET_SATIRI_SEVK_EDILMEZ / FATURA_KALEMI_YOK / SEVK_SATIRI_TEKRAR / SEVK_KALEMI_YOK, 409 IRSALIYE_TAMAMLANDI; ölü `IRSALIYE_ZATEN_VAR`/`FATURA_TEKIL_IMZALARI` dalı SİLİNDİ (Şef onayı); kalan okunmadan ÖNCE faturanın satır kilidi (boş UPDATE) alınır — PG ikizi ikinci işlemin lock_timeout ile düştüğünü ölçer; yeni `GET /api/invoices/{id}/despatchable-items` (izin `sales`, SEC-3 kuralından, yeni kural YOK; matris ölçüldü: yönetici/muhasebe/satış 200, depo/rapor 403); UBL: DespatchLine despatch_lines satırı başına (DeliveredQuantity satır miktarı, unitCode unit_code, OrderLineReference/LineID faturadaki sıra); DOCUMENT_ENGINE BULGUSU: E4a IRS+yıl+9 haneyi document_engine'SİZ üretiyordu ama sırayı `invoice_id`den türetiyordu — N irsaliyede ÇAKIŞIR; `despatch_notes` DOCUMENT_TABLES'a girdi ve yeni `next_sequence_value` AYNI `document_sequences` sayacını (anahtar `despatch_notes:IRS<yıl>`) ilerletir, ikinci sayaç kaynağı YOK; tohum o yılın verilmiş en büyük numarası; sayaç FİRMA KAPSAMLI (PK `(company_id,sequence_key)`, ölçüldü — tasarım kusuru YOK, test başka firmanın numarasının tohumu etkilemediğini çiviler); yeni kod Core (`app/despatch_schema.py`, create_all EDİLMEZ); PİNLER (TABAN `989b735`, CS2 sonrası YENİDEN ölçüldü): 419/325 -> 420/326 (parmak izi 35f45088 -> 958a9c0e), GET 199 -> 200, AUTHENTICATED 406 -> 407, READ 87/GUARDED 31/UNDENIABLE 97 SABİT, TENANT_TABLES 122 -> 123, pg_twins 131 -> 132, baş 0086 -> 0087 (9 dosya/11 iddia), Core kiracı kapısı 175 -> 183, Core envanter 223 -> 230 (select 155/update 63/delete 12; 72b2110c -> 1c166663), document_engine dinamik parmak izi 9da37689 -> 626752bf (dinamik text() 9'da sabit), sayısal manifesto SABİT (`despatch_lines.quantity` `sonradan_dogan`da 18,4), DOGRUDAN_HEDEFLER += despatch_lines.product_id, types.gen.ts yeniden üretildi; TESTLER: `tests/test_e4b1_kismi_sevk.py` 21 (ileri/ters/2× karışık yeşil; mutasyon: LABOR süzgeci, miktar tavanı kırmızı), PG ikizi 6 (iki bileşik FK çapraz firmayı reddetti; çıplak FK mutasyonu kırmızı), E4a'nın üç 1:1 iddiası BİLİNÇLİ yeniden yazıldı (eski iddia docstring'de alıntılı) ve E4a PG ikizi 10 tersine çevrildi, 5.1c iki satırlı irsaliye yeni firmaya üç referansla eşlendi (SQLite 19, PG 5; DOGRUDAN girdisi mutasyonu kırmızı), tam izole paket `989b735` üzerinde 469/469 dosya yeşil (3516 sonuç, 1005s, 4 işçi), PG ikizleri dosya başına taze DB'de 7 dosya/57 test yeşil; AÇIK: H39 UBL-TR XSD doğrulaması (depoda XSD yok, yapısal testler korunuyor)