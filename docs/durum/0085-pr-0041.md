#41 — DEVELOP'TA YİNELENEN SIRA 0083: `#37` ile `#38` AYNI SIRAYI İNDİRDİ ve kapı bunu GÖRMEDİ; onarım `0083-pr-0037.md` -> `0084-pr-0037.md` (SONRA İNEN girdi taşınır, `yinelenen_sira_denetle`nin çaresinin aynısı) ve bu kayıt 0085'tir; OLAY ÖLÇÜLEREK KURULDU, akıl yürütülerek değil: iki PR de tabanını AYNI revizyonda bildiriyor — #37'nin kaydı "TABAN `origin/develop` `24fcf52` (PR #31 birleşmesi)", #38'inki "TABAN `24fcf522` yani #31'in birleşmesinden SONRAKİ develop" — yani ikisi de #31 sonrası develop'tan dallanmış KARDEŞTİ ve o tabanda en büyük sıra 0082'ydi, dolayısıyla İKİSİ DE 0083'ü MEŞRU olarak seçti (`bayat_sira_denetle` eşzamanlılığı zaten meşru sayar: girdi base'in en büyüğünde ya da bir fazlasında olabilir); sonra `git log --merges` sırası şunu gösteriyor: `75ac35d` (#38) ÖNCE indi ve `0083-pr-0038.md`yi develop'a koydu, `96d8ce1` (#37) SONRA indi ve `0083-pr-0037.md`yi ONUN ÜSTÜNE koydu — sonuçta develop iki 0083 taşıyor ve `python scripts/durum.py --kapi` develop'un kendi ağacında "YİNELENEN sıra 0083: 0083-pr-0037.md, 0083-pr-0038.md" diyor; KAPI NİYE KAÇIRDI, adıyla: `durum-kaydi` işi BİRLEŞME SONUCUNU açıkça kurar ("Birleşme sonucunu AÇIKÇA kur" adımı) ve `yinelenen_sira_denetle` tam bu durumu reddetmek için vardır — ama #37'nin koşusu `24fcf52` TABANINA karşı koştu ve O TABANDA `0083-pr-0038.md` HENÜZ YOKTU; yani kapı yeşildi ve koştuğu anda DOĞRUYDU, birleşme anında BAYATTI; kusur kapının mantığında DEĞİL, yeşil bir kontrolün ne kadar eski bir tabana ait olabileceğinin SINIRSIZ olmasındadır — kontrol ile birleşme arasında base değişirse hiçbir şey yeniden ölçmüyor; ÇARE ve KAPSAMI: bu PR yalnız KAYDI onarıyor, kapıyı DEĞİŞTİRMİYOR — gerçek çare depo ayarındadır ve kodda değil: `develop` dal korumasında "Require branches to be up to date before merging" açılmalıdır, çünkü o ayar birleşmeden önce dalı güncel olmaya zorlar ve `durum-kaydi` GÜNCEL tabana karşı yeniden koşar; bu ayar açılmadıkça aynı arıza HER eşzamanlı kayıt çiftinde tekrar edebilir ve tekrar ettiğinde yine yeşil görünecektir; `pr` İKİNCİL ANAHTARDIR VE BİR LİSANS DEĞİLDİR (`durum.py` belge dizgisi): aynı sırayı taşıyan iki girdi OKUNABİLİR kalsın diye `pr` vardır, o durumun İNMESİ serbest demek DEĞİLDİR — bu kayıt o cümlenin ölçülmüş karşı örneğidir; HANGİ GİRDİ TAŞINDI ve NİYE O: SONRA inen #37, çünkü sıra İNİŞ SIRASIDIR ve önce inen #38'in 0083'ü kayıttan okunduğu gibi kalmalıdır; taşınan dosyanın GÖVDESİNE DOKUNULMADI ve dokunulmasına gerek de yoktu — ÖLÇÜLDÜ: `0083-pr-0037.md` kendi sırasını hiçbir yerde ANMIYOR (tek `00xx` eşleşmesi bir CI süre listesinin içindeki `0047` alt dizgisiydi), yani ad ile gövde birbirini YALANLAMIYOR; ÖLÇÜLEN KOŞULAR: taban `96d8ce1`, taşımadan sonra ağaçta YİNELENEN SIRA YOK, `tests/test_durum_kaydi.py` yeşil, `--kapi origin/develop --pr 41` yeşil; ÖLÇÜLMEDİ, ADIYLA: dal koruması ayarının KENDİSİ bu PR'da AÇILMADI (depo ayarı, kod değil — sahibin yapmasıgerekir) ve açılmadığı için "aynı arıza artık imkânsız" İDDİA EDİLMİYOR; ayrıca develop'un GEÇMİŞİNDE başka yinelenen sıra olup olmadığı bu turda taranmadı — yalnız tepe durum onarıldı.
