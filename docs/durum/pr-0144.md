#144 — F10-1 ÇİFTÇİ SELF-SERVICE KEŞİF + TASARIM (BELGE-YALNIZ; backend/frontend/alembic DOKUNULMADI, göç YOK, yeni rota YOK): tek dosya `docs/f10-1-ciftci-selfservice-kesif-2026-09-17.md` (833 satır), taban `f0e3918` (her sayı bu tabanda ölçüldü). ÜÇ ÖLÇÜLMÜŞ BULGU — (1) çiftçi TEK cari DEĞİL İKİ ayrı caridir ve ikisi bağlı değildir: ekstre/çiftlik `customers` (`farms.customer_id`, göç 0044), avans + müstahsil makbuzu `suppliers` (`producer_receipts.supplier_id` göç 0070, `supplier_advances.supplier_id`), kantar fişinin (`field_harvest_tickets`, göç 0069) taraf sütunu HİÇ YOK (tek bağ `harvest_id`; `buyer_name` serbest metin ve ALICIYI anlatır) — yani brifingin `whatsapp_customer_links` önerisi dört niyetin ÜÇÜNÜ cevaplayamaz ve DÜŞER; (2) rıza defteri ZATEN VAR: `notification_consents` `CONSENT_REQUIRED_CHANNELS`te `WHATSAPP` ve `PARTY_TYPES={CUSTOMER,SUPPLIER}` taşıyor, fail-closed + append-only + "anlık görüntü karar verici değildir" — KVKK kapısı sıfırdan yazılmamalı; (3) iki telefon normalleştiricisi demo kümesinin %100'ünde ayrışıyor: 25 demo müşterisinin 25'i de telefonlu ve 25'i de 13 haneli, `telefon.e164` KABUL (`+9053155011001`) ama `consents.normalize_msisdn` RED (`None`) — bugünkü demo verisiyle F10-1 uçtan uca DENENEMEZ. YAN BULGULAR: `kimlik.user_id` ONBİR yerde çözülüyor (`baglam` 3, `bekleyen` 8) ama `yurutucu.py`de SIFIR (yedi okuma aracının hiçbiri kullanıcı bilmiyor → okuma yüzeyi zaten kullanıcıdan bağımsız), `kimlik.company_id` 23 yerde; BAĞLI numaranın normal mesaj yolunda numara başına hız sınırı YOK (`rate_limit|hiz_sinir|RateLimit|limiter` `routers/whatsapp.py` + `whatsapp/service.py` içinde hiç geçmiyor), yalnız eşleştirme yolunda var (pencere 15 dk / sınır 5 / cevap sınırı 3); `uq_customers_company_id` göç 0026'da ZATEN VAR (E4b-1'in `invoice_items` için açtığı bileşik-FK ön koşulu BURADA YOK), `uq_suppliers_company_id` de VAR (göç 0070 `:103`, `:133-136`; `supplier_advances` 0071 `:199`da ona bileşik FK ile asılı) — göç 0090 müşteri/tedarikçi kısıtı AÇMAZ; `EntityDetail.tsx:214` rıza panelini yalnız `type==='customer'` için çiziyor. TASARIM: `whatsapp_party_links` + `whatsapp_party_pairing_codes` (`notification_consents`in polimorfik biçimi; personel tablolarına SIFIR dokunuş), dağıtım `service._mesaj_isle`de personel dalı ÖNCE, ayrı `CIFTCI_BEYAZ_LISTESI`, `tahsilat_coz` çiftçi dalında HİÇ çağrılmaz, `build_statement`ın maskeli-varsayılan `rol`ü aynen kullanılır. TABAN PİNLERİ ölçüldü: 421/327 rota, GET 201, AUTH 408 (read 87 / undeniable 97), `TENANT_TABLES` 125 BEŞ dosyada YEDİ çivi (`len(TENANT_TABLES)` dört kez + `test_kiraci_disa_aktarim.py` `:497`/`:515`/`:746` üç kez, adı anmadan) + tanım `test_tenant_scoping_guard.py:41` = ALTI dosya, `pg_twins.txt` 134 (f0e3918'de; develop `bf8e73f`de zaten 135), `alt_surec_sql.txt` 137, `cari_alan_envanteri.txt` 56, alembic başı `20260915_0089` ON ÜÇ dosyada ON DÖRT çivi (dokuzu `backend/` kökündeki PG ikizleri, `test_goc_zinciri.py` `:424`+`:511`; `test_e4b2_irsaliye_yaniti.py:95` çivi DEĞİL, göç dosyasının metnini denetler), F10-1a EN AZ OTUZ pin noktasına dokunur, Core envanteri 233 (select 158 / update 63 / delete 12) parmak izi `494f7b8b`, Core kiracı ifadesi 191, kiracı istisnası 14 girdi. DÖRT DİLİM pin deltaları ve PG ikizi adlarıyla (F10-1a göç 0090 + dört uç → 425/331, GET 202, AUTH 412, TENANT_TABLES 127; F10-1b göç 0091 hız sınırı, rota YOK; F10-1c göç YOK; F10-1d ön yüz) + ALTI açık karar tavsiyeleriyle · DÜZELTME TURU (doc lens NO-GO `f52edbc`): pin siteleri `backend/`in tamamında yeniden grep'lendi, `uq_suppliers_company_id` ölçüldü, K4 alıntısı ASCII hâliyle, satır numaraları (`service.py` 465/467/471, `niyet.py:80`, `core_schema.py` 34/53, envanter `:1255-1256`, parmak izi `:1354`) düzeltildi, tedarikçi tohumunun `212` sabit hat öneki notu eklendi — <merge tarihi>
