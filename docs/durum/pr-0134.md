#134 — 1B-H parti mutabakat raporu EKRANI: `GET /api/products/lots/mutabakat` artık `/raporlar/parti-mutabakati`da (`read`, uçla aynı izin; Stok & Ürünler grubunda, rapor iş grubunda durur kuralı); kova kartları sayfa satırlarından DEĞİL tenant geneli `counts`tan + `bos_ciftler` notu, ürün/depo adları `/products?limit=2000` ve `/warehouses`ten ekran başına BİRER istekle (bulunamayan `#id`), fark işareti `quantityDecimal` ile (negatif error, pozitif warning, `Number()` yok), kova filtresi YOK çünkü uçta yok ve sayfa sunucuda kesiliyor — yerine sayfa içi SAPMA→LOTSUZ_TASARIM→ESIT sırası altyazıda söyleniyor, "Daha fazla" limit 200 offset biriktirir, boş durum `total 0` ("rapor boş") ile "hepsi eşit"i `counts`la ayırır; yalnız frontend, backend/göç/types.gen DOKUNULMADI; pinler ÖLÇÜLDÜ: rota envanteri 79→80 (`kapi`, işaret veriden bağımsız "rapordan düşülen boş çift"), menü maddesi 58→59 hem navigation.consistency hem AppShell.test'te (AppShell pini brief'te yoktu, hedefli koşu kırmızıyla buldu); lint 0 hata, build yeşil, tam vitest `--testTimeout=30000` tek süreç 106 dosya / 786/786 yeşil — <merge tarihi>
