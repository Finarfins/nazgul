#113 — docs: platform yönetim paneli keşfi ve 3 PR'lık uygulama planı (Büyük Sipariş Adım 5); docs/platform-paneli-kesif-2026-09-10.md dokümanı ölçülen gerçeklerle bağlandı: doküman satır sayısı 267 (≤ 400 bütçesi korundu); veritabanında 135 tablo sayımı (120 kiracı tablosu TENANT_TABLES, 15 platform/kiracısız tablo); bulgu 2a: /api/platform/* uçlarında kiracı muafiyeti olmaması nedeniyle aktif firma üyeliği bulunmayan operatör için 403 COMPANY_ACCESS_DENIED durumu ve platform yedekleme olayının kiracının activity_logs kütüğüne yazılması kusuru belgelendi; 0 backend dosyası değiştirildi (git diff origin/develop HEAD -- backend boş, 0 backend dosyası, veritabanı göçü yok); lens düzeltmeleri uygulandı (atıf satırları düzeltildi, olmayan /auth/switch-company kaldırılıp X-Company-Id başlığı açıklandı, izibiz_env ve test/live ortam değerleri bağlandı, PP1 pin-delta listesine GUARDED_READ_OPERATIONS 24 -> 31 eklendi); kayıt adı python scripts/durum.py --sonraki 113
