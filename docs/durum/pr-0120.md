#120 — SEC-9 `payment_idempotency` KULLANICI KAPSAMI (göç `20260914_0084`, YENİ tablo YOK, YENİ uç YOK): 0025'in kurduğu tekillik YALNIZ firmayı taşıyordu (`uq_payment_idempotency_operation` = company_id + operation_type + resource_type + resource_id + idempotency_key) ve `_claim_payment_create` bu defteri SABİT `resource_id='create'` ile yazıyor, yani BİR FİRMADAKİ TÜM KULLANICILAR TEK ANAHTAR UZAYINI paylaşıyordu; TABAN `origin/develop` @ `28c9dd0` ve sha OTURUM AÇILIŞINDAKİ ANLIK GÖRÜNTÜDEN DEĞİL `git fetch` sonrası ÖLÇÜLDÜ (anlık görüntü `42b6a6b` diyordu ve o sha 28c9dd0'ı ATA OLARAK TAŞIMIYOR, yani yalnız bayat değil AYRIŞMIŞTI), `merge-base origin/develop HEAD` == `28c9dd0` (yani `git diff origin/develop HEAD` başkasının birleşmiş PR'ını benim silmem gibi göstermiyor), hash `59e1ff6aa358`; AÇIK ÖNCE ÖLÇÜLDÜ SONRA KAPATILDI ve ÖLÇÜM GÖREVİN HİPOTEZİNİ DÜZELTTİ — görev "iki kullanıcı, aynı anahtar, FARKLI gövde -> ikinci kullanıcı birincinin snapshot'ını alır" diyordu, GERÇEK (SQLite, `PAYMENT_ALLOCATION_ENGINE_ENABLED=true`, iki kullanıcı AYNI firmada) İKİ UÇLUDUR ve hipotez YALNIZ YARISIDIR: (a) FARKLI gövdede parmak izi denetimi ÖNCE devreye giriyor ve B kullanıcısı `409 Idempotency anahtarı farklı bir ödeme için kullanılmış` alıyor — snapshot SIZMIYOR ama B'nin MEŞRU ödemesi HİÇ YAZILMIYOR, yani A'nın anahtar SEÇİMİ B'ye HİZMET REDDİ uyguluyor ve mesajın kendisi bir KÂHİN (B, o anahtarın firmada BAŞKASI tarafından yakıldığını öğreniyor); (b) AYNI gövdede B, A'nın `result_snapshot`ını ALIYOR (A'nın `payments.id`si) ve B'nin isteği HİÇ YÜRÜTÜLMÜYOR — DÖRT ayrı kullanıcı isteğine karşılık `payments` tablosunda İKİ satır kaldı; yama sonrası AYNI ölçüm: dört istek -> DÖRT satır, `b1.entity_id` = B'nin müşterisi, `b2.id != a2.id`; SIZINTININ ASIL YÜZEYİ `create_payment`TİR ve bu SEBEBİYLE AÇIK: `allocate_payment` ve üç mutasyon yolu (`manual_allocation`, `reverse_allocation`, `reallocate_allocation`) `resource_id`yi ÖDEME/TAHSİS KİMLİĞİNDEN alır, yani iki kullanıcı ancak AYNI kaynağa AYNI işlemi yaparken çakışabilir; `create_payment`in `resource_id`si ise SABİT `'create'`tir ve firmadaki HER ödeme oluşturma isteği AYNI satır uzayına düşer — buna rağmen DÖRDÜ DE kapsamlandı çünkü kapsamı yalnız "bugün sömürülebilir olan" yola koymak, yarın `resource_id`si sabitlenen bir beşinci yolu SESSİZCE açık bırakırdı; GÖÇ `20260914_0084`: `user_id` Integer NOT NULL ve NULLABLE OLMAMASI ÖLÇÜLMÜŞ BİR KARARDIR — PG'de `NULL = NULL` YANLIŞ olduğu için nullable bir sütun eski satırlar arasında tekilliği HİÇ ÇALIŞTIRMAZDI ve açık NULL taşıyan her satırda AYNEN kalırdı; GERİ DOLDURMA `0` NÖBETÇİSİYLE yapıldı çünkü var olan satırların YETKİLİ SAHİBİ YOKTUR (defter kimi yazdığını hiç kaydetmedi ve `result_snapshot` ödemenin GÖVDESİDİR yazarı değil), `app_users.id` 1'den başlayan bir dizidir yani `0` hiçbir gerçek kullanıcıyla ÇAKIŞAMAZ, ve sütun FOREIGN KEY DEĞİLDİR çünkü nöbetçi hiçbir `app_users` satırına işaret etmez — FK konulsaydı geri doldurmanın KENDİSİ düşerdi; ESKİ TEKİLLİK DÜŞÜRÜLDÜ (bırakılsaydı açık AYNEN kalırdı), yerine `uq_payment_idempotency_kullanici_islem` (company_id, user_id, operation_type, resource_type, resource_id, idempotency_key); YENİ İNDEKS AÇILMADI ve bu ÖLÇÜLDÜ — 0025'in indeks şekli `ix_payment_idempotency_company_resource` (company_id, resource_type, resource_id) `idempotency_key` TAŞIMIYOR, yani "anahtar araması için AYNALANACAK" bir indeks YOK ve motorun tam-demet okumasını YENİ TEKİLLİĞİN kendi indeksi zaten karşılıyor; var olan indeks OLDUĞU GİBİ bırakıldı çünkü kullanıcıdan BAĞIMSIZ kaynak taraması (rapor/denetim) hâlâ doğru yoldur; TTL/TEMİZLİK YOLU YOK ve bu VARSAYILMADI GREPLENDİ (`grep -rn "DELETE FROM payment_idempotency" backend/app` BOŞ) — süpürme YALNIZ genel `idempotency_keys` defterinde var (`app/idempotency.py:296,324`) ve o tabloya bu göç DOKUNMUYOR, yani yeni sütunun bozacağı bir temizlik yolu yoktur; SQLite YOLU TABLOYU YENİDEN KURAR (NOT NULL'a çekmek de kısıt takası da bunu gerektirir) ve `batch_alter_table` YANSITMAYA BIRAKILMADI, AÇIK `copy_from` ile beslendi çünkü SQLite'ta yabancı anahtar ADSIZ yansıyor (ölçüldü: `get_foreign_keys(...)[0]["name"] is None`); BU KARAR BİR KUSURU DA ORTAYA ÇIKARDI VE KUSUR BENİM GÖÇÜMDEYDİ — `copy_from`a İNDEKS İLAN EDİLMEDİĞİNDE yeniden kurma `ix_payment_idempotency_company_resource`u SESSİZCE DÜŞÜRÜYORDU (ilk ölçümde `get_indexes(...) == []`), yani göç bir indeks kaybettiriyordu ve HİÇBİR ŞEY bağırmıyordu; indeks artık `sa.Index` olarak AÇIKÇA ilan ediliyor ve İKİ KAPI DA (SQLite + PG) up->down->up boyunca varlığını ÇİVİLİYOR; CHECK ve FK yeniden kurmadan SAĞ ÇIKTI ve bu İDDİA EDİLMEDİ ÖLÇÜLDÜ (göç turu `checkler` ve `fkler` kümelerini ÖNCE/SONRA birebir karşılaştırıyor); DOWNGRADE ÇAKIŞAN SATIRLARI DÜŞÜRÜR ve bu ÖRTÜLMEDİ: eski şema aynı (firma, işlem, kaynak, anahtar) demetini İKİ kullanıcı için TUTAMAZ, o satırların eski şemada bir YERİ yoktur, bu yüzden her demette EN KÜÇÜK `id` tutulup ötekiler SİLİNİR — kaybedilen şey DEFTER kaydıdır yani TEKRAR KORUMASIDIR, VERİ DEĞİL (`payments` ve `payment_allocations` AYRI tablolardır ve DOKUNULMAZ), ve zaten eski şemada o koruma YANLIŞ KULLANICIYA karşı çalışıyordu; alternatif "çakışma varsa gürültülü düş" SEÇİLMEDİ çünkü downgrade tam da kötü bir dağıtımı geri sarmak için koşulur ve o anda yürümeyen bir downgrade operatörü ELLE SQL'e iter; KOD: `payment_allocation_engine.py`de ÜÇ INSERT'e `user_id` sütunu + `:uid` yer tutucusu, ÜÇ SELECT'e ve BEŞ UPDATE'e `AND user_id=:uid` yüklemi; kimlik `_kullanici_kapsami(created_by)`den geliyor ve `created_by` uçtan `request.state.user["id"]` ile besleniyor, GÖVDEDEN OKUNMUYOR; aktörü OLMAYAN sistem yolu `SISTEM_KULLANICISI = 0` nöbetçisine düşer (göçün geri doldurmasıyla AYNI değer, aynı gerekçe); `routers/finance.py` ve `whatsapp/bekleyen.py` `created_by`yi ZATEN geçiriyordu (ölçüldü) ve İKİSİNE DE DOKUNULMADI — `routers/finance.py` ayrıca PR #114'ün (SEC-3b) SAHİP OLDUĞU dosyalardandır; KİMLİKSİZ TEK YOL `routers/transactions.py:437` idi (belgeden doğan otomatik ödemenin tahsisi) ve ORAYA THREAD EDİLDİ: `_sync_payment` artık `created_by` anahtar kelime parametresi alıyor, `request` satış yolunda HER ZAMAN geçiliyor (`create_order`/`update_order` ikisi de `request=request` veriyor) ve alış yolunda tahsis motoru `kind == "sale"` koşuluyla ZATEN çağrılmıyor, yani nöbetçiye düşen gerçek bir uç yolu KALMADI; `kiraci_geri_yukleme.py`de `("payment_idempotency", "user_id")` `KULLANICI_SUTUNLARI`na eklendi (9 -> 10) ve girdinin YÜK TAŞIDIĞI İDDİA EDİLMEDİ ÖLÇÜLDÜ — girdi GEÇİCİ olarak çıkarıldığında `test_siniflandirilmamis_yumusak_referans_yok` `[["payment_idempotency", "user_id"]]` ile KIRMIZI oluyor; aynı tablonun `resource_id` girdisi AYRI bir sınıftır (`METIN_KIMLIK_SUTUNLARI`) ve OLDUĞU GİBİ bırakıldı; TESTLER: `backend/test_sec9_payment_idempotency_user.py` (SQLite) ve `backend/test_sec9_payment_idempotency_postgresql.py` (GERÇEK PG 16.4), İKİZ SQLite dosyasının KOŞUCULARINI İTHAL EDER yani iddia gövdesi TEK YERDE durur ve iki lehçe için AYRIŞAMAZ (`test_payment_allocation_foundation_postgresql.py`nin kalıbı; ikisi de bu yüzden `backend/tests/` altında DEĞİL `backend/` kökündedir — görev `backend/tests/` diyordu, konvansiyon ÖLÇÜLDÜ ve konvansiyona uyuldu); kapılar: çapraz kullanıcı (iki ölçümün YEŞİL hâli), AYNI kullanıcı + aynı anahtar + aynı gövde -> TEKRAR OYNATMA (idempotens BOZULMADI, defterde TEK satır), AYNI kullanıcı + aynı anahtar + FARKLI gövde -> `409` ve MESAJIN KENDİSİ çivili (bu davranış SEC-9 ÖNCESİ ölçüldü ve AYNEN korunuyor; metni çivilemek, kapsam eklerken sessizce gevşemesini engelliyor), şema (sütun + NOT NULL + YENİ tekillik + ESKİ tekilliğin YOKLUĞU + 0025 indeksinin VARLIĞI) ve göç up->down->up + `0` nöbetçisi; KIRMIZI OLDUĞU İDDİA EDİLMEDİ KOŞULDU — temiz `28c9dd0` worktree'sine YALNIZ test dosyası kopyalandı ve İKİ TEST DE DÜŞTÜ (`user_id` sütun listesinde YOK / `No such revision 20260914_0084`); İKİNCİ VE DAHA KESKİN TUR: göç de kopyalandı ama motor yaması KOPYALANMADI ve `_claim_payment_create` `user_id` NOT NULL'da SERT DÜŞTÜ, yani göç ile kod BİRBİRİNE BAĞLIDIR ve yarısı tek başına dağıtılamaz; PİNLER ÖNCE/SONRA HEPSİ ÖLÇÜLDÜ: alembic başı `20260913_0083 -> 20260914_0084` ve çivi SEKİZ dosyada ON İDDİADA — SAYILMADI GREPLENDİ ve GÖREV YEDİ diyordu, GREP SEKİZ buldu (`tests/test_e1b_plantback`, `tests/test_e2_tedavi_arinma`, `tests/test_e3_karantina`, `tests/test_goc_zinciri` [ÜÇ satır: `zincir.bas`, `atalar` olumsuzu ve CLI çıktısı], `test_1b_a_alis_lot_postgresql`, `test_e1_efatura_sertlestirme_postgresql` [`BAS` sabiti], `test_e4a_despatch_notes_postgresql` [`BAS` sabiti], `test_wa3_worker_postgresql`); E4a'nın KENDİ göçünü ADLANDIRAN ÜÇ referansa DOKUNULMADI ve bu AYIRIM ÖLÇÜLDÜ (`tests/test_e4a_despatch_notes.py`nin `GOC` yolu ve `revision = "..."` kaynak iddiası, `test_e4a_despatch_notes_postgresql.py`nin `GOC` sabiti) — onlar BAŞ çivisi değil KİMLİK çivisidir ve başla birlikte kaydırılsalardı E4a'nın göçünü ölçen kapılar YANLIŞ DOSYAYI arardı; `pins/pg_twins.txt` 125 -> 126; `pins/alt_surec_sql.txt` 135 -> 136 ve BU PİN GÖREVDE YAZMIYORDU — yeni test dosyası SQL'i bir ALT SÜREÇ metninde taşıdığı için `test_alt_surecte_sql_sayisi_donduruldu` onu YAKALADI ve 2 sayısıyla kaydedildi; `tenant_scoping_guard` parmak izleri İKİ dosyada kımıldadı — `payment_allocation_engine.py` `8bbd3c8c -> f68677b9` ve `routers/transactions.py` `b1547e6c -> 0ec945dd`, İKİSİNİN DE gerekçesi izin listesine YAZILDI; DİNAMİK `text()` SAYILARI DEĞİŞMEDİ ve bu ÖLÇÜLDÜ (motor 16, uç 18, küresel 269 — hepsi SABİT), çünkü değişen şey SABİT METİNLİ SQL'lerdir: eklenen `AND user_id=:uid` yüklemi kiracı kapsamını DARALTIR genişletmez (sekizinin de kök yüklemi ZATEN `company_id=:cid` idi ve AYNEN duruyor) ve `user_id` DEĞERİ hiçbir yerde METNE girmez, BAĞLI PARAMETREDİR; `routers/transactions.py`de HİÇBİR SQL METNİ kımıldamadı, değişen tek şey bir ÇAĞRI ARGÜMANIDIR; `kiraci_geri_yukleme.py` için parmak izi YOK ve bu VARSAYILMADI ÖLÇÜLDÜ — dosyada HİÇ `text()` çağrısı yoktur (`grep -o "text(" ... | wc -l` -> 0), yani dinamik SQL izin listesine HİÇ girmez ve girmeyen bir dosyanın parmak izi de kayamaz (görev orada bir parmak izi bekliyordu); `TENANT_TABLES` 121'de SABİT, rota sözleşmesi 400/308 SABİT, GET izin envanteri 190 SABİT, `EXPECTED_READ` 80 ve `EXPECTED_UNDENIABLE` 97 SABİT ve DÖRDÜ DE ANLAMLIDIR: bu dilim YENİ TABLO ve YENİ UÇ AÇMADI, var olan bir tabloya SÜTUN ekledi; `test_core_query_inventory` DOKUNULMADAN yeşil (Core sorgu eklenmedi, değişen SQL'lerin hepsi `text()` gövdeleridir); KOŞULAR: yeni kapılar SQLite'ta 2/2 YEŞİL; PG ikizi GERÇEK PostgreSQL 16.4'te (`postgresql+psycopg://lens:lens@localhost:5433/sec9lens`, TAZE veritabanı) 2/2 YEŞİL; 5.1c GERİ YÜKLEME TURU GERÇEK PG'de koşuldu (`test_kiraci_geri_yukleme_postgresql` + `tests/test_kiraci_disa_aktarim` + `test_v2_9_change_history_restore_tenant_scope` -> 21 YEŞİL), yani yeni sütun yansımadan geçiyor ve "yeni firma" kopyasını BOZMUYOR (küresel tekil AÇILMADI: yeni tekillik `company_id` ile BAŞLIYOR); pin kapıları (`test_tenant_scoping_guard` 33, `test_ci_gates` 26, `test_goc_zinciri` + `test_alembic_chain_integrity` 32, `test_e1b_plantback`/`test_e2_tedavi_arinma`/`test_e3_karantina`/`test_e4a_despatch_notes` 86, `test_route_get_permission_inventory`/`test_route_security_contracts`/`test_core_query_inventory` 69, `test_kiraci_geri_yukleme` 18, `test_backend_encoding` 2) hepsi YEŞİL; DEĞİŞEN DOSYALAR (17): YENİ -> `backend/alembic/versions/20260914_0084_payment_idempotency_user_id.py`, `backend/test_sec9_payment_idempotency_user.py`, `backend/test_sec9_payment_idempotency_postgresql.py`; DEĞİŞEN -> `backend/app/payment_allocation_engine.py`, `backend/app/routers/transactions.py`, `backend/app/kiraci_geri_yukleme.py`, artı ON BİR pin/çivi dosyası — ve bu kayıt; kayıt adı `python scripts/durum.py --sonraki 120`
