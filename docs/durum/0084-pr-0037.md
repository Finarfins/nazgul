#37 — `npm ci`'NİN ÖRTÜK AUDIT'İ KAYNAĞINDAN KAPATILDI (`frontend` + `e2e` işlerinde `npm ci` → `npm ci --no-audit`) VE FRONTEND BÜTÇESİ SİVRİSİZ ARİTMETİKLE YENİDEN TÜRETİLDİ (`timeout-minutes: 12` → `20` KALIR); TABAN `origin/develop` `24fcf52` (PR #31 birleşmesi; ölçüldü `git rev-parse HEAD` işe başlamadan); `deploy/ci-required-contexts.json` DOKUNULMADI (`git diff origin/develop -- deploy/ci-required-contexts.json` = 0 satır) ve 17 bağlam olduğu gibi (`python deploy/ci-gerekli-baglam-kapisi.py .github/workflows/ci.yml deploy/ci-required-contexts.json` → "YESIL: 17 baglam manifest ile tam kume esitliginde", çıkış 0); `ci.yml` ÜÇ HUNK (`git diff origin/develop -- .github/workflows/ci.yml | grep -c '^@@'` = 3): (1) `@@ -623,7 +623,37 @@` `frontend` işinin `timeout-minutes` yorum bloğu, (2) `@@ -634,7 +664,23 @@` `frontend` işinin `npm ci` satırı, (3) `@@ -869,7 +915,20 @@` `e2e` işinin `npm ci` satırı; `contract-drift` işinin `npm ci`'si BİLEREK DOKUNULMADI (YAML'dan doğrulandı: `frontend` → `npm ci --no-audit` timeout 20, `e2e` → `npm ci --no-audit` timeout 35, `contract-drift` → `npm ci` timeout 10); MEKANİZMA — ÖRTÜK AUDIT vs AÇIK KAPI: `npm ci` kendiliğinden bir audit isteği yapar ve kayıt defteri bozukken bu istek sabit bir zaman aşımı merdiveninde asılır; log'dan AYIRT EDİLİR çünkü npm audit cümlesini yalnız istek HATA ALDIĞINDA düşürür — audit koştuysa "added 608 packages, and audited 609 packages in <süre>", asıldıysa "added 608 packages in 7m"; paket sayısı (608) ÖLÇÜLEN HER KOŞUDA AYNI, yani inen ağırlık değişmiyor, asılan şey audit; `npm ci` audit bulgusunda ASLA non-zero dönmez, dolayısıyla bu çağrı bu depoda hiçbir zaman kapı DEĞİLDİ ve çıktısı hiçbir işte okunmuyordu — KAPSAM KAYBI YOK: gerçek GHSA kapısı `frontend` işindeki AÇIK `npm run test:audit-policy` + `npm run audit:policy -- --degrade-on-unavailable` adımı (#34, ağ sınıfı hatada `::warning::AUDIT_UNAVAILABLE` basıp 0 ile çıkar ama POLİTİKA İHLALİNİ asla yutmaz), `package-lock.json` sha256'sının `audit-allowlist.json` içinde beyan edilmesi (çevrimdışı sinyal, bozulmuş geçiş yolunda DA koşar) ve KATI hâliyle günlük `audit-gunluk.yml` iş akışıdır; TABLO 1 — FRONTEND, SON 30 TAMAMLANMIŞ KOŞU (29'unda `frontend` işi var; `gh run list --workflow=ci.yml --limit 40 --json databaseId,status,conclusion` → her koşu `gh run view <id> --json jobs -q '.jobs[]|select(.name=="frontend")|...steps[]|select(.name=="Run npm ci")'`): frontend min 0m48s, medyan 6m09s, maks 12m19s; `npm ci` min 0m14s, medyan 2m15s, maks 7m04s; tavanda öldürülen 3 (33856366292 12m19s, 33853270963 12m18s, 33854225188 deneme 1 12m15s) ve GitHub bunları `failure` DEĞİL `cancelled` bildiriyor; AUDIT SİVRİLERİ LOG SATIRIYLA TEŞHİS EDİLDİ (`gh run view <id> --job <frontend_job_id> --log | grep -oE "added [0-9]+ packages.*in [0-9a-z]+"`) — audit cümlesi DÜŞMÜŞ ALTI koşu: 33856366292 (7m03s, "in 7m"), 33853270963 (7m03s, "in 7m"), 33846053464 (7m02s, "in 7m"), 33859065506 (7m04s, "in 7m"), 33843671226 (7m03s, "in 7m"), 33815691003 (5m03s, "in 5m"); KARŞIT KONTROL — audit BAŞARIYLA koşan koşular: 33848969644 4m54s ("and audited 609 packages in 5m"), 33819153815 4m15s ("in 4m"), 33821605217 3m46s ("in 4m"), 33820047886 3m16s ("in 3m"), 33857237082 47s ("in 47s"), 33879160486 18s ("in 18s"); TABLO 2 — E2E, 5 TAMAMLANMIŞ KOŞU (adım süreleri `gh run view <id> --json jobs`): 33857237082 toplam 21m26s / `npm ci` 7m03s / build 18s / pw hazırlık 24s / tam süit 8m27s / touch#2 4m54s; 33854225188 22m18s / 5m53s / 24s / 22s / 9m57s / 5m23s; 33853952479 23m28s / 7m03s / 24s / 21s / 9m56s / 5m25s; 33848969644 **17m19s** / **0m53s** / 24s / 22s / 9m58s / 5m21s; 33846053464 21m24s / 7m02s / 19s / 23s / 8m26s / 4m51s — yani e2e toplamı min 17m19s, medyan 21m26s, maks 23m28s ve audit'in başarıyla koştuğu TEK koşu (33848969644) aynı zamanda EN KISA koşu; NOT — ARIZA KOŞU BAŞINA DEĞİL İŞ BAŞINA: 33857237082'nin `e2e` işinde `npm ci` 7m03s (audit cümlesi düşmüş) iken AYNI koşunun `frontend` işinde 47s (audit koşmuş); ARİTMETİK (audit sivrileri ÇIKARILARAK, 29 koşudan 6'sı çıktı, 23 kaldı): kalan maks **11m15s** = 33848969644, `success`, tavanda KIRPILMAMIŞ, `npm ci` 4m54s ve audit BAŞARIYLA koşmuş — yani bu koşu (a) örtük audit sınıfı DEĞİL, (b) YAVAŞ İNDİRME sınıfı ve `--no-audit` onu KAPATMAZ; 11m15s → yukarı yuvarla 12 dk → 12 × 1.5 = 18 → 5'e yukarı yuvarla → **20**; SİVRİLER ÇIKARILSA BİLE 12 DK YETMEZ (11m15s × 1.5 = 16.9 dk > 12), bu yüzden bütçe 20'de KALIR ve düzeltme ile bütçe İKİ AYRI KARARDIR: `--no-audit` (a) sınıfını kaynağından kapatır, `timeout-minutes: 20` (b) sınıfına karşı tavan verir; `backend/tests/test_ci_gates.py` 16 passed (1.74s, ölçüldü) ve `test_ci_gates.py + test_durum_kaydi.py + test_ci_playwright_hazirlik.py + test_ci_pg_istemci_hazirlik.py` birlikte 71 passed (11.65s); hiçbir test `npm ci` metnine bağlı değil (`grep -rn "npm ci" backend/tests/ deploy/*.py deploy/*.sh scripts/` BOŞ), yani bu değişiklik hiçbir iddiayı kırmaz; ÖLÇÜLMEDİ, ADIYLA: **`--no-audit`'in GERÇEK bir kayıt defteri arızası altında CI'da izole doğrulaması ÖLÇÜLMEDİ** — korelasyon frontend'de 12/12, e2e'de 5/5 kusursuz ve mekanizma tutarlı, ama kayıt defteri bozukken `npm ci --no-audit` koşan bir CI koşusu YOK; bu ancak arıza TEKRARLADIĞINDA gözlenebilir, çünkü kayıt defteri sağlıklıyken iki yol da hızlıdır ve ayrım görünmez; AYRICA ÖLÇÜLMEDİ: `contract-drift` işindeki `npm ci`'nin aynı sınıftan kaybı (bilerek kapsam dışı bırakıldı — "başka hiçbir şeye dokunma").
