#50 — E1a TARLAYA GİRİŞ YASAĞI (REI) KATALOGDAN ÇÖZÜLÜYOR: GÖÇ YOK, TABAN `9cc9edb` (`origin/develop`, DOĞRULANDI); ÖLÇÜLEN KUSUR — `create_activity` PHI'yi katalogdan ÇÖZÜYOR (`_katalog_phi`/`_phi_coz`, göç 0063) ama giriş yasağını ÇÖZMÜYORDU, `reentry_interval_days` `payload.model_dump()`tan olduğu gibi geçiyordu ve operatör yazmadıysa BOŞ kalıyordu; 0064 o sayının üstüne GERÇEK bir kilit kurmuştu (`_giris_ihlalleri`, `_GIRIS_SORGU`'nun `reentry_interval_days IS NOT NULL` yüklemi) ve boş süre ihlal sayılmadığı için kilit SESSİZCE devre dışı kalıyordu — firma kataloğa süreyi GİRMİŞ olsa bile tarlaya erken giriliyordu; İKİ YENİ FONKSİYON: `_katalog_giris_yasagi` (bitkiye ÖZEL satır önce, `crop=''` yedek — `_katalog_phi` ile aynı sıra) ve `_giris_yasagi_coz` (birden çok girdide EN UZUN yasak kazanır, operatörün yazdığı değer HER ZAMAN kazanır, `product_id` taşımayan serbest metin girdi çözülmez — `_phi_coz` ile aynı kararlar); TEK FARK katalogda `reentry_interval_days`in NULL KABUL ETMESİDİR (`preharvest_interval_days` etmez, 0063'te PHI kataloğun VAR OLMA sebebidir, REI isteğe bağlı taşınır) ve NULL bir SÜRE DEĞİL SÜRE HAKKINDA SUSMAKTIR: susan satır çözüme HİÇ girmez, bitkiye özel satırın boş REI'si firmanın gerçekten girdiği `crop=''` satırını BASTIRMAZ — bastırsaydı kimsenin yazmadığı bir boşluk gerçek bir süreyi siler ve tarlaya erken girişe izin verirdi; KÖKEN KAYDI KAPSAM DIŞI VE BU BİR EKSİKLİK DEĞİL ÖLÇÜLMÜŞ BİR SINIRDIR — PHI'nin kökeni `field_activities`te İKİ sütunla duruyor (`preharvest_source` VARCHAR(20), `catalogue_preharvest_days` INTEGER; 0063) ve giriş yasağının karşılıkları için `0044`/`0046`/`0053`/`0063`/`0064`ün o tabloya açtığı sütunların TAMAMI tarandı: `reentry_source`/`catalogue_reentry_days` YOK, yani kökeni mevcut şemayla kaydetmek İMKÂNSIZ ve onları açmak bir GÖÇTÜR; görevin kuralı "sütun gerekiyorsa DUR" olduğu için köken BİLEREK yazılmadı ve PR hiçbir yerde köken İDDİA ETMİYOR; mevcut bir sütuna sıkıştırmak da çözüm değildi — `reentry_warning` SİSTEMİN bulduğu uyarıdır (0064) ve kökeni oraya yazmak 0048 ile 0063'ün İKİSİNİN DE açıkça reddettiği şeyi yapardı, kullanıcının söylediğiyle sistemin bulduğunu aynı sütunda toplamak denetimde kimin ne dediğini ayırt edilemez kılar; GÖZLENEBİLİR DAVRANIŞ bundan etkilenmiyor: "operatör kazanır" kuralı köken sütunu olmadan da aynen geçerli ve `backend/tests/test_e1a_rei_katalog.py`nin bütün iddiaları ETKİN SÜRE üzerinden ölçüyor; TESTLER — uçtan uca smoke (katalog boş bırakılanı çözer, bitkiye özel 8 bitkiden bağımsız 3'ü yener, `Patlican` sezonu genele düşer, iki girdide 8 ile 2'den 8 kazanır ve SIRADAN BAĞIMSIZ, susan satır konuşanı bastırmaz, operatör 30 ve 1 yazdığında katalog ÜSTÜNE YAZAMAZ, katalog susuyorsa ve serbest metin girdide süre BOŞ kalır, çözülen süre `_giris_ihlalleri` ile GERÇEK kilit üretir — 2026-06-05 ihlal, 2026-06-09 temiz — ve B firması A'nın kataloğundan ne süre ne kilit alır, aynı `product_id` çözücüye DOĞRUDAN sorularak da çivilendi) artı veritabanına inmeyen karar testi (sahte katalogla `max`, sıra bağımsızlığı, operatörün SIFIRI da bir karardır, NULL suskunluğu, `product_id`siz girdi kataloğa hiç SORULMAZ); MUTASYONLAR (üçü de kırmızı, geri alındığında yeşil): `max`→`min` 2 failed, katalog operatörün üstüne yazıyor 2 failed, `company_id` süzgeci düşürüldü 1 failed; İĞNELER (CPython 3.12.10, taban `9cc9edb`): `farm.py` DİNAMİK `text()` sayısı 15'te SABİT ve küresel toplam 257'de SABİT — eklenen TEK SQL sabit metinlidir (f-string DEĞİL), kök yüklemi `company_id=:cid` taşır ve `product_id` bağlı parametredir, yani zaten gözden geçirilmiş `_katalog_phi` sorgusunun BİREBİR ikizidir; `create_activity`nin INSERT METNİ BİREBİR AYNI — hâlihazırda var olan `:reentry_interval_days` yer tutucusuna ham değer yerine ÇÖZÜLMÜŞ değer bağlanıyor, sütun EKLENMEDİ; `farm.py` parmak izi `46734f13`→`2f0ba1f9` (yalnız AST kımıldadı, iki fonksiyon ve bir çağrı satırı girdi); `test_core_query_inventory` yeşil; contract-drift zinciri (openapi→`types.gen.ts`→fixture) yeniden üretildi ve DRIFT YOK — şema değişmedi; PostgreSQL İKİZİ EKLENMEDİ ve gerekçesi ölçüldü: `non_twin_skip_exceptions` kapısı yalnız SQLite'ta ATLANAN testler için ikiz istiyor, buradaki iki test de atlanmıyor ve bu dilim şema açmıyor; D2 dosyalarına (`mustahsil.py`, `finance*`, `payments`, `movement_references`) DOKUNULMADI.
