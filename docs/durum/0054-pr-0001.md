#1 — Hasadın ürüne giden yolu açıldı: `crop_seasons` NULL KABUL EDEN bir `product_id` alıyor ve kısıt ÇIPLAK değil BİLEŞİK — `(company_id, product_id) -> products(company_id, id)`, hedefi tekil olsun diye `products`a `UNIQUE(company_id, id)` eklendi (0058'in dört ebeveynde kurduğu desenin beşincisi), ürünü SEZON bildirir hasat DEVRALIR; tüketici `_hasat_kalemleri` ile hasat -> sezon -> ürün yolunu iki yandan kiracı kapsamlı okuyor ve dağıtım `_KALEM_OKUYUCU` haritasına taşındı, haritanın anahtar kümesi `_KAYNAK`la EŞİT olacak biçimde donduruldu çünkü eksik bir okuyucu hata vermez, o kaynağın HER olayını sessizce kovaya düşürürdü; `SKIPPED_NO_PRODUCT` KALDIRILMADI, KAÇINILABİLİR yapıldı — ürünü bildirilmemiş sezonun hasadı hâlâ oraya düşer ve gerekçe artık HANGİ kaydın düzeltileceğini söyler; `FIELD_STOCK_OUTBOX_ENABLED` AÇILMADI, varsayılanı false kaldı; yol açılırken GERÇEK bir kusur ölçülüp kapatıldı: SQLite'ta `PRAGMA foreign_keys` AÇIK BİR İŞLEM İÇİNDE NO-OP'tur ve göçün batch yeniden kurulumu işlemi çoktan açtığı için `finally`deki geri açma ETKİSİZDİ — havuzdan gelen bağlantı `foreign_keys=0` ile geliyor, 409 dönmesi gereken kısıt ihlali 200 dönüyordu; `app/db.py` artık pragma'yı HER HAVUZ TESLİMİNDE yeniden dayatıyor, teslim anında açık işlem olmadığı için orada GERÇEKTEN etki eder; mutasyonla kanıtlı: hasat dağıtımı geri alınıp `_KALEM_OKUYUCU` hasat için boş dönünce mutlu yol test_hasat_URUNLU_sezonda_HAREKET_uretiyor kırmızı, `_hasat_kalemleri`nden `product_id IS NOT NULL` yüklemi kaldırılınca ÜÇ test kırmızı — kova testi test_hasat_URUNSUZ_kovasina_dusuyor, test_hasat_URUNLU_sezonda_HAREKET_uretiyor ve korunum denetimi test_korunum_girdi_KOVALARIN_toplamina_esit
