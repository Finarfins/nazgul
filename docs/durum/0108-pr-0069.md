#69 — HİJYEN H5 DURUM NUMARALAMA KARAR NOTU: `sıra`nın dalda seçilmesi bayat-sıra yarışına yol açıyor; iki seçenek yazıldı — (A) PR `pr-NNNN.md` yazar, develop post-merge işi `SSSS-pr-NNNN.md`e yeniden adlandırır; (B) `sıra` first-parent `git log` birleşme sırasından türetilir; kapı sonuçları tabloda; KOD YOK (`scripts/durum.py`/CI dokunulmadı); not `docs/NUMARALAMA.md`de çünkü bugün `docs/durum/*.md` yalnız girdi deseni kabul ediyor (`test_dosya_adlari_desene_uyuyor`) — hedef yol kapı kararı sonrası `docs/durum/NUMARALAMA.md`; KANIT: `PYTHONUTF8=1 python run_isolated_tests.py tests/test_durum_kaydi.py --workers 4` → 33 passed; run id `b4d7249c-0e23-457a-afc5-7caac94efaf5`; 17 bağlam SABİT; dal `test/h5`; `--sonraki 69`.
