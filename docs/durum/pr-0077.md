#77 — WA3-core NİYET ÇÖZÜCÜ + KÖPRÜ İMZACISI, TABLO YOK, ROTA YOK, KABLO YOK: `nazgul_website/backend/app/whatsapp`ten `app/whatsapp/{__init__,telefon,niyet,kopru}.py` taşındı ve ÖLÇÜLDÜ — kaynak `niyet.py` (830 satır) DB'ye DOKUNMUYOR (`sqlalchemy`/`Session` importu YOK), tek dış bağımlılığı `assistant.masking` (`SORU_SOZLUGU`, `_EK_TOLERANSI`, `_YUMUSAMA`, `_kok_eslesir`) ve `assistant.tools.tr_katla` idi, bu depoda `assistant` paketi YOK, o beş sembol `niyet.py`ye BİREBİR gömüldü; BİREBİR: `normalize_phone`, `Niyet`, `coz`, `_ozel_niyet`, `_terim_cikar`, `_donem_acik`, `tahsilat_coz`, `tutar_ayikla`, `cevap_yaz` ve 7 şablon, `sonuc_bos_mu`, `para_tr`/`tutar_yaz`/`tahsilat_ozeti`/`tahsilat_basarili`, `ARAC_BEYAZ_LISTESI` (kaynakta `service.py`deydi); UYARLANAN: `service.py::arac_kos`un deneme döngüsü → `dene(niyet, yurutucu)` ve `NiyetYurutucu` Protocol + `NoOpYurutucu` (bu PR'da HİÇBİR koşturucu bağlı DEĞİL; `BOS_SONUC` açık yazıldı çünkü kaynak `{}`yi DOLU okur — ölçüldü), `danisman.py::sor` → `KopruIstemcisi.sor` (gövde `{numara,soru}` ve hata→`None` AYNI; sır artık telde DEĞİL, istek İMZALI), `danisman.acik_mi` (ikincil sır eklendi); TAŞINMAYAN: `gunluk_sinir_asildi_mi` (`whatsapp_inbound` sayıyordu, tablo YOK), `service/worker/webhook/bekleyen/provider`; EKLENEN: `telefon.e164`+`TelefonGecersiz`; SÖZLEŞME `docs/whatsapp/KOPRU_SOZLESMESI.md` bir FIXTURE — `test_wa3_kopru.py` JSON bloğunu OKUR: `X-Harman-Kopru-Timestamp` (unix s) + `X-Harman-Kopru-Nonce` (≥16 bayt küçük hex) + `X-Harman-Kopru-Imza` = HMAC-SHA256(sır, ts<NL>nonce<NL>sha256(gövde)), ±300 s (300 GEÇER 301 DÜŞER iki yönde), ÇİFT SIR (`HARMAN_KOPRU_SIRRI` imzalar, `_IKINCIL` yalnız doğrulamada), ÜÇ altın vektör `app.whatsapp.kopru` İÇE AKTARILMADAN yalnız `hashlib/hmac` ile üretildi ve testte ikinci kez bağımsız hesaplanıyor, NONCE TEKRARI BURADA DENETLENMEZ (alıcının işi, `test_nonce_tekrari_BURADA_denetlenmez` yokluğu adıyla sabitler); AYAR `app/config.py`: `harman_kopru_url: str|None`, `harman_kopru_sirri`/`harman_kopru_sirri_ikincil: SecretStr|None` — ÜÇÜ DE VARSAYILAN BOŞ = `KopruKapali`, ağ YOK (urllib.urlopen ve httpx yasaklanarak ölçüldü; taşıma katmanı `urllib` çünkü `httpx` bu deponun çalışma zamanı bağımlılığı DEĞİL, yalnız `requirements-dev.txt`), `.env.example`e yorumlu üç satır (compose `environment:` haritasına DOKUNULMADI, `test_env_example_completeness` yeşil); TESTLER `tests/test_wa3_niyet.py` 55 + `tests/test_wa3_kopru.py` 37 = 92 yeşil; kaynağın 64 testinden 10'u TAŞINDI (DB fixture'sız: assistant 5, tahsilat 5, gövdeler AYNI), 54'ü DÜŞÜRÜLDÜ (52'si `ortam`/`istemci` DB fixture'ı istiyor, 2'si taşınmayan modülü ölçüyor: `soru_maskele`, `provider`); Türkçe katlama (ş/ı/İ, `bakiye`/`stok`/`vade`, yumuşama `stoğu`), belirsizlik → ARAÇ YOK (9 çapraz/boş girdi), `Yılmaz`→`YIL` yaslanması KAYNAK DAVRANIŞI olarak sabitlendi (düzeltilmedi); MUTASYON ADIYLA KIRMIZI (5/5, kaynak yamalanıp geri alındı, commit yok): `IMZA GOVDE OZETSIZ` (3 vektör + gövde kapısı), `KAYMA SINIRSIZ` (301 iki yön), `IKINCIL SIR YOK SAYILIR`, `BELIRSIZLIKTE TAHMIN`, `DENEME SIRASI`; ÇİVİLER KIMILDAMADI ve `git diff 0538a4a` ile ÖLÇÜLDÜ: `tests/pins/*` boş fark, rota envanterleri 372/98/113 ve 382/293, GET 182, TENANT_TABLES 116, alembic başı `20260909_0077`, `types.gen.ts`/openapi/`main.py`/`routers/` SIFIR fark — tek izlenen değişiklik `config.py` (+9) ve `.env.example` (+5); PG ikizi YOK çünkü saf kod (SQL yok, tablo yok), `pins/pg_twins.txt` ve `alt_surec_sql.txt` DEĞİŞMEDİ; komşu kapılar izole koşturucuda 9 dosya 196 test yeşil; TABAN `origin/develop` `0538a4a` (#76 birleşmiş) `git rev-parse` ile YENİDEN ölçüldü; dal `feat/wa3-niyet-kopru`, çalışma ağacı `F:/nazgul-wa3`; kayıt adı `python scripts/durum.py --sonraki 77`
