#22 — Uygulama Kayıt Çizelgesi: tarla uygulama ve hasat kayıtlarının denetime gösterilebilir tek çıktısı iki SALT OKUMA ucuyla eklendi (`GET /api/exports/producer-logbook` JSON önizleme ve `GET /api/exports/producer-logbook.xlsx` iki sayfalı çalışma kitabı); YOL ADI GÜVENLİK KARARIDIR — uçlar `/api/field-…` altına KONMADI çünkü `auth.py`de `_FARM_PATH_PREFIXES` listesinde olmayan her `/api/field-` yolu sessizce `field_service` iznine düşüyor ve bu tuzağa iki kez düşüldü, bunun yerine `/api/exports/` altında middleware `read` çözüyor ve ASIL kapı handler'daki `_require_permission(request, "farm.view")` çağrısı oluyor; bu iddia sözel değil YAPISAL olarak çivili — `test_authorization_population_reconciliation.py` handler AST'sini tarayıp `DENYING_GUARDS` çağrısı arıyor ve iki uç da `GUARDED_READ_OPERATIONS` kümesine ÜYELİK olarak (sayı olarak değil) pinlendi; ÖLÇÜLEN PİN HAREKETLERİ (aritmetik değil, birleşme sonrası ağaçta koşturulup okundu): operasyon 344→346, yol 265→267, GET envanteri 164→166, authenticated 334→336, read 91→93, GUARDED_READ 27→29, dinamik SQL allowlist 253→254, PG test popülasyonu 94→96, ve EXPECTED_UNDENIABLE 99→99 DEĞİŞMİYOR — bu kıpırdamama tasarımın asıl ölçülebilir sonucudur çünkü uçlar rol ile REDDEDİLEBİLİR olduğu için "reddedilemez okuma" popülasyonunu büyütmüyorlar; PG popülasyonu 95 değil 96 çünkü iki ayrı dal (BKÜ kataloğu ve bu iş) birer ikiz ekledi, ikisi de "94→95" yazmıştı ve birleşmede TOPLANDI — kapının yakaladığı tam olarak bu; GET envanteri drift raporu ayrıca ölçüldü, `changed` ve `stale` BOŞ, yani artış YALNIZ ekleme; RESMÎ FORM İDDİA EDİLMİYOR — depoda "Üretici Kayıt Defteri"nin bakanlıkça yayımlanmış sütun düzenine dair hiçbir kaynak yok, yalnızca yükümlülüğün VARLIĞINA atıflar var, ve elle yeniden kurulmuş bir bakanlık formu deponun kaynağı olmayan bir DÜZENİ iddia etmesi olurdu (göç 0063'te reddedilen "depoya gömülen rakam" kusurunun bir üst seviyesi), bu yüzden çizelge kendi tasarımımızdır ve bunu SAYFANIN 1. SATIRINDA söyler: arma yok, form numarası yok, "onaylıdır" ifadesi yok; sütunlar göçlerden ÖLÇÜLEREK çıkarıldı (0044/0046/0048/0062) ve deponun TUTMADIĞI hiçbir alan için sütun AÇILMADI — boş bir "Hedef zararlı" sütunu sistemin o veriyi izlediğini sanmaya davet ederdi; PARA SÜTUNLARI BİLEREK DIŞARIDA (`unit_cost`/`total_cost`, `labor_hourly_rate`/`machine_hourly_rate`, `revenue_amount`) çünkü çizelge denetime gösterilmek için üretiliyor ve daha önemlisi `farm.py`deki okuma yolu kapısı donmuş oran sütunlarını ROLE GÖRE maskeliyor — bu çıktı o maskenin ETRAFINDAN DOLAŞAN ikinci bir yol olamazdı, sütunları hiç SEÇMEMEK maskeyi burada yeniden kurmaktan güvenli ve bir test bunu hem başlık hem değer düzeyinde çiviliyor; NUMARALI/DONDURULMUŞ DEFTER GEREKMİYOR (ölçüldü): `field_activities` ve `field_harvests` üzerinde PUT/PATCH/DELETE YOK, kayıtlar değişmez ve PHI satıra EKLEME ANINDA yazılıyor, dolayısıyla aynı süzgeçle iki kez üretilen çizelge aynı satırları verir; kartezyen çarpıma karşı faaliyet/girdi/hasat AYRI sorgularla toplanıp Python'da birleştiriliyor (`parcel_timeline` ile aynı çözüm) ve sınırsız sonuca karşı en az bir KAPSAM süzgeci zorunlu (tarih aralığı TEK BAŞINA yetmez, "2020-2026 arası" bütün tarihçedir) artı 20.000 satır üst sınırı sessizce KESMİYOR, 400 dönüyor; gün İSTANBUL takvimine göre yazılıyor (`farm.py::_yerel_gun` ile aynı kural) yoksa bekleme süresi hesabı ile çizelge hangi gün olduğu konusunda ANLAŞMAZDI, bir test UTC 22:30 kaydının `02.06.2026` olarak yazıldığını çiviliyor; ÇAPRAZ KİRACI 404 DEĞİL 200+BOŞ döner çünkü uç bir KAYNAĞI değil bir SÜZGECİ okuyor ve 404 "o kimlik başka yerde VAR" bilgisini sızdırırdı; ÖLÇÜM: backend CI'nin kendi izole koşucusuyla 366 dosya sıfır hata (exit 0), GERÇEK PostgreSQL 16.14 üzerinde çapraz kiracı ikizi geçti ve İKİZ İŞİNİ YAPTI — testin para sondasındaki gerçek bir kusuru yakaladı, `'100.00'` sondası parselin `100.0000` dekarlık ALANIYLA eşleşiyordu ve SQLite bunu (`100` olarak vererek) gizliyordu, sonda ayırt edici bir değere çevrildi, frontend `tsc -b` temiz ve vitest `Tests 588 passed (588)`, ayrıca `ci.yml:790` sözleşme drift kapısı gereği `types.gen.ts` yeniden üretildi (fark +116/-0, yalnız iki yeni uç) — bu yapılmasaydı CI KIRMIZI olurdu; ÖN YÜZ YOK VE BU BİLİNÇLİ: bir sayfa `frontend/e2e/rota-envanteri.ts`i ve navigasyon tutarlılık pinlerini kaydırırdı, ayrı bir dilim ve ayrı bir inceleme hak ediyor, eksiklik gizlenmiyor yazılıyor; PR AÇILMADAN ÖNCE İKİ KIRMIZI TAHMİN EDİLDİ ve tahmin sonucun ÖNÜNE yazıldı: (1) `durum-kaydi` — kayıt dosyasının adı PR NUMARASINI içerdiği için PR açılmadan dosya VAR OLAMAZ, bu bir sıra sorunu, ve bu girdinin kendisi o kırmızıyı kapatıyor; (2) `test_v3_harman_vadeli` — BU İŞLE İLGİSİZ, temiz `develop` üzerinde `cfe7362`de İZOLE koşumda da kırmızı, ve bir izolasyon oynaklığı DEĞİL teste gömülmüş bir TARİH BOMBASI (test `2026-09-01` gününe çapalanmış, İstanbul o günü geçti, iddia döndü) — ayrım önemli çünkü izolasyon oynaklığı kendi kendini temizler, tarih bombası TEMİZLEMEZ; ayrıca ŞEFİN KAYDA GEÇEN DÜZELTMESİ: "PR açmadan önce CI yeşil olsun" talimatı UYGULANAMAZDI çünkü `ci.yml` yalnız `pull_request` ve `workflow_dispatch` ile tetikleniyor, özellik dalına push hiçbir şey koşturmuyor — istenen sıra var olamaz ve CI'nin ilk koşumu PR'ın kendisidir.
