#138 — H51 İRSALİYE LİSTESİ PG'DE 500 (backend-only, göç YOK, rota envanteri SABİT): taban `0868b0d`de gerçek PG 16'da YENİDEN ÜRETİLDİ — `GET /api/despatch-notes` `invoice_id` verilmeden her seferinde 500 `(psycopg.errors.AmbiguousParameter) could not determine data type of parameter $2` (psycopg3 `None`ı tipsiz gönderiyor, PG `(:invoice_id IS NULL OR invoice_id=:invoice_id)` içindeki tipi çıkaramıyor; SQLite'ta görünmez, frontend hep `invoice_id` gönderdiği için E4a `1143025`ten beri yaşadı) ve `offset=2^63` 500 `(psycopg.errors.NumericValueOutOfRange) bigint out of range`; DÜZELTME seçenek (a): iki `text()`e `.bindparams(bindparam("invoice_id", type_=Integer))` — SQL metinleri tek sabit literal kaldı, dosya `DYNAMIC_SQL_FILE_ALLOWLIST`e girmedi (270 sabit), Core envanteri `bindparam`ı saymadığı için 230/`1c166663` ve kiracı bekçisi 183 SABİT; İKİNCİ TUR (çalışma zamanı merceği, ÖLÇÜLDÜ): tipli `::INTEGER` bağı int4 dışındaki `invoice_id`yi (2147483648, -2147483649, 2^63) `NumericValueOutOfRange: integer out of range` ile 500 yapıyordu (tabanın tipsiz bağı 200 veriyordu) — Şef kararı: `BigInteger`a GEÇİLMEDİ (sınırı yalnız 2^63'e ötelerdi), sınır UÇTA: `invoice_id: int | None = Query(None, ge=1, le=INT4_UST)` (kimlikler 1'den başlar, kardeş uçların 404'üyle aynı ret sınıfı, daha erken) ve `offset` tavanı `2**63-1` yerine `INT4_UST` (H54: tek tavan her yerde; `2^63-1` sürücünün sınırı, ürünün değil), `INT4_UST` `cek_senetler.py`den içe aktarıldı, docstring'e tek satır eklendi ve `types.gen.ts` o satırla yeniden üretildi (1 satır); bu dosyada başka `offset` yok, tavansız kardeşler H54'e kaldı: `activity_logs.py:78`, `products.py:330`, `platform_management.py:436/495/568`; TESTLER: PG ikizi `test_h51_irsaliye_listesi_postgresql.py` 12 test (süzgeçsiz 200 ve yalnız kendi firması, COUNT/sayfa `total` anlaşması, B firmasının irsaliyesi ne listelenir ne sayılır, `offset` 2^31/2^63 422 INT4_UST 200, `invoice_id` 2147483648/-2147483649/2^63/0 için 422 VE ayrıca 500-değil iddiası, INT4_UST 200), mutasyonlar: PR'ın önceki başı `0b5c724` 6 kırmızı, yalnız `invoice_id` sınırını silmek 4 kırmızı, yalnız `offset` tavanını 2^63-1'e geri almak 2 kırmızı, yalnız COUNT'tan `.bindparams` silmek (ilk tur) 3 kırmızı; SQLite `test_e4a_despatch_notes.py` +7 sonuç; `pg_twins.txt` 132 -> 133; rota 420/326, GET 200, AUTHENTICATED 407, TENANT_TABLES 123, alembic başı `20260915_0087` DEĞİŞMEDİ; tam izole süit 470/470 dosya 3535 sonuç yeşil, PG ikizleri e4a 10/10 e4b1 6/6 h51 12/12 skip yok; `577ba82`ye (#134 1B-H sayfası, #135 H48 sonrası) yeniden taban alındı, çakışma yok: app+alembic patch-id `73e70827de78` DEĞİŞMEDİ, `types.gen.ts` uygulamadan iki kez üretildi ikisi de işlenmiş blobla bayt-aynı, `pg_twins.txt` yeni tabanda ölçüldü 132 -> 133, rota 420/326, GET 200; yeni tabanda `test_e4a_despatch_notes.py` 57, rota sözleşmeleri 12, GET envanteri 8, ci_gates 26, taze PG veritabanında h51 12/12 skip yok; `0885616`ya (#139 H17+H27, göç 0088 sonrası) İKİNCİ KEZ yeniden taban alındı: tek çakışma `pg_twins.txt` (`test_h17_…` ve `test_h51_…` satırları, sıralı birleşim, satır düşmedi) yeni tabanda ölçüldü 133 -> 134; alembic başı artık `20260915_0088`, bu PR'da göç ve baş pini yok; app+alembic patch-id `73e70827de78` DEĞİŞMEDİ (PG ikizi bu yüzden yeniden koşulmadı); `types.gen.ts` iki kez üretildi ikisi de işlenmiş blobla bayt-aynı; yeni tabanda `test_e4a_despatch_notes.py` 57, rota sözleşmeleri 12 (420/326), GET envanteri 8 (200), ci_gates 26; `add303c`e (#136 H23, #137 H49, #140 E4b-2 göç 0089 sonrası) ÜÇÜNCÜ KEZ yeniden taban alındı: tek çakışma `despatch_notes.py` içe aktarma satırı (E4b-2 `and_` + `IntegrityError` ekledi, H51 `Integer, bindparam` ekledi — sıralı birleşim, satır düşmedi); `pg_twins.txt`, `test_e4a_despatch_notes.py` ve `types.gen.ts` kendiliğinden birleşti, develop'ın yeniden yazdığı 3 terminal-durum testi KORUNDU (dosyada 46 ekleme 0 silme); app+alembic patch-id `73e70827de78` -> `60ef2ada2b7a` DEĞİŞTİ (listeleme fonksiyonunun bağlamı kaydı, gövde aynı) — `597d1c9`teki çalışma zamanı GO'su bu yüzden TAŞINMIYOR, yeni başta yeniden ölçülecek; `pg_twins.txt` yeni tabanda ölçüldü 134 -> 135 (sıralı); alembic başı `20260915_0089`, bu PR'da göç ve baş pini YOK; `types.gen.ts` uygulamadan yeniden üretildi, işlenmiş blobla bayt-aynı; yeni tabanda rota 421/327, GET 201, AUTHENTICATED 408, TENANT_TABLES 125 SABİT; `test_e4a_despatch_notes.py` 57, `test_e4b2_irsaliye_yaniti.py` 43, rota sözleşmeleri 12, yetki uzlaşımı 7, ci_gates 26, Core envanteri 49, kiracı dışa aktarım 20, geri yükleme 22; taze PG veritabanlarında h51 12/12, e4a 10/10, e4b2 7/7 skip YOK; tam izole süit 473/473 dosya 3616 sonuç yeşil — <merge tarihi>
