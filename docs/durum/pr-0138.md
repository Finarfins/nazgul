#138 — H51 İRSALİYE LİSTESİ PG'DE 500 (backend-only, göç YOK, rota envanteri SABİT): taban `0868b0d`de gerçek PG 16'da YENİDEN ÜRETİLDİ — `GET /api/despatch-notes` `invoice_id` verilmeden her seferinde 500 `(psycopg.errors.AmbiguousParameter) could not determine data type of parameter $2` (psycopg3 `None`ı tipsiz gönderiyor, PG `(:invoice_id IS NULL OR invoice_id=:invoice_id)` içindeki tipi çıkaramıyor; SQLite'ta görünmez, frontend hep `invoice_id` gönderdiği için E4a `1143025`ten beri yaşadı) ve `offset=2^63` 500 `(psycopg.errors.NumericValueOutOfRange) bigint out of range`; DÜZELTME seçenek (a): iki `text()`e `.bindparams(bindparam("invoice_id", type_=Integer))` — SQL metinleri tek sabit literal kaldı, dosya `DYNAMIC_SQL_FILE_ALLOWLIST`e girmedi (270 sabit), Core envanteri `bindparam`ı saymadığı için 230/`1c166663` ve kiracı bekçisi 183 SABİT, (b)'nin iki ayrı sabiti yeni SQL sitesi eklerdi; `offset` `le=OFFSET_TAVANI=2**63-1` ÖLÇÜLDÜ (2^63-1 200 boş sayfa, 2^63 artık 422 `loc=[query,offset]`, sürücü hatası yakalanmıyor); uç docstring'i değişmedi, `types.gen.ts` openapi-typescript 7.13.0 ile yeniden üretildi içerik farkı YOK; bu dosyada başka `offset` yok, tavansız kardeşler RAPORLANDI dokunulmadı: `activity_logs.py:78`, `products.py:330`, `platform_management.py:436/495/568`; TESTLER: yeni PG ikizi `test_h51_irsaliye_listesi_postgresql.py` 5 test (süzgeçsiz 200 ve yalnız kendi firması, COUNT/sayfa `total` anlaşması, B firmasının irsaliyesi ne listelenir ne sayılır, offset 422/200) tabanda 4 kırmızı/1 yeşil, yalnız COUNT'tan `.bindparams` silen mutasyon 3 kırmızı; SQLite `test_e4a_despatch_notes.py` +2; `pg_twins.txt` 132 -> 133; rota 420/326, GET 200, AUTHENTICATED 407, TENANT_TABLES 123, alembic başı `20260915_0087` DEĞİŞMEDİ; tam izole süit 470/470 dosya 3523 sonuç yeşil, PG ikizleri e4a 10/10 e4b1 6/6 h51 5/5 skip yok — <merge tarihi>
