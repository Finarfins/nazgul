#118 — H25 CI KAPISI: autoload kapalı adımlarda pytest-asyncio ZORUNLU (test + ci.yml, GÖÇ YOK, ROTA YOK, `backend/app` ve `frontend` DOKUNULMADI): PR #115'te ölçülen kusur — `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` kuran adım `-p pytest_asyncio.plugin` vermeyince pytest-asyncio yüklenmez, `pytest.ini`deki `asyncio_mode = auto` hiçbir şey yapmaz ve her `async def` test HER sırada "async def functions are not natively supported" ile düşer; kusur ancak async testi olan bir dosya DEĞİŞTİĞİNDE görünüyordu — bu PR onu yapısal olarak geriletilemez kılar; KAPI `backend/tests/test_ci_gates.py` (+6 test): `ci.yml` `yaml.safe_load` ile okunur, workflow→job→step env BİRLEŞTİRİLİR (anahtar hangi katmanda olursa olsun pytest süreç ortamını okur), autoload'ı kapatan her adımın `run` betiğindeki her `python -m pytest`/`pytest` çağrısı KOMUT-KONUMU regex'iyle sayılır (`pip install pytest` ve argüman konumundaki `pytest` SAYILMAZ, tırnaklı `DATABASE_URL="...$(basename "$dosya")..."` öneki geçilir — ilk taslak bu öneki kaçırıp ters-sıra adımının run çağrısını GÖRMÜYORDU, regex birim testi bu kalıbı çiviler), ihlal mesajı ölçülen çağrı sayısını + job + adım + çağrı metnini verir; BOŞ EŞLEŞME YEŞİL YANMAZ: ters-sıra adımının iki çağrısı (collect-only + run) görülmek ZORUNDA; NEGATİF İKİZ (2 parametre): `ci.yml` kopyasında TEK çağrının bayrağı sökülür → kapı `backend-quality-canonical / Değişen SQLite testlerini ters sırada koştur :: python -m pytest ...` biçiminde adımı ve çağrıyı adıyla düşürür (önce/sonra fark tam 1); `run_isolated_tests.py` AST ile (grep değil): `[..., "-m", "pytest", ...]` komut listesi sayısı **2**'ye çivili (satır 337 ve 592), ikisi de `-p pytest_asyncio.plugin` taşımalı, `env["PYTEST_DISABLE_PLUGIN_AUTOLOAD"] = "1"` satırı aranır, bayrağı sökülen kopya düşer; ÖLÇÜLDÜ (c66e232, kapı yazıldıktan sonra): autoload kapalı **5 adım / 6 pytest çağrısı / 4 İHLAL** — `alembic-chain / Tek baş ve çakışmayan revision id` (yalnız `pytest` kuruyordu, pytest-asyncio HİÇ yoktu), `durum-kaydi / Kayıt biçim kapıları` (aynı), `backend-postgresql / Run PostgreSQL integration gates` collect-only ve dosya-başına çağrıları (requirements-dev kurulu, bayrak yok); PG ikizlerinin 122 dosyasında + iki özel dosyada bugün `async def` YOK, yani dördü de BEKLEYEN kusurdu (#115'in şekli: ilk async test eklendiğinde patlar); görev kuralı gereği DURULDU ve raporlandı, Şef onayıyla (muafiyet YOK) ci.yml AYNI PR'da düzeltildi: `alembic-chain` ve `durum-kaydi` `pip install "pytest>=8.2,<9" "pytest-asyncio>=0.23,<1"` (requirements-dev ile BİREBİR aynı pin) + bayrak; `backend-postgresql` iki çağrıya bayrak (`-p pytest_asyncio.plugin -p isolated_test_reporter`); `deploy/ci-postgresql-shard-kapisi.py`deki iki ÇİVİLİ çağrı metni (satır 110 ve 126) yeni haliyle güncellendi — şard kapısı aksi halde "bagimsiz PostgreSQL canonical node manifesti uretilmiyor" ile KIRMIZIYDI ve `test_ci_gates`te 3 test düştü (ölçüldü, sonra düzeltildi); #114/#116 bu dosyaya DOKUNMUYOR, `route contract/authenticated/pg_twins/tenant_scoping_guard` pinlerine DOKUNULMADI; SONUÇ **4 adım / 6 çağrı / 0 ihlal**; KOŞU: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python backend/run_isolated_tests.py --workers 4 tests/test_ci_gates.py` → **26/26**; ters sıra (ci.yml adımıyla birebir collect→tac→run) → **26/26**; `python deploy/ci-postgresql-shard-kapisi.py .github/workflows/ci.yml` → YEŞİL; değişen iki adımın test dosyaları (`test_alembic_chain_integrity` + `test_goc_zinciri` + `test_durum_kaydi`) autoload kapalı + bayrakla → **70/70**; KISIT: kapı `run:` betiğini metin olarak okur — `uses:` ile çağrılan bir composite action içindeki pytest çağrısını GÖREMEZ (bugün ci.yml'de yok); kayıt adı `python scripts/durum.py --sonraki 118`
