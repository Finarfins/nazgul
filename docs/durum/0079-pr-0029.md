#29 — ÜRETİM İMAJI TEST VARLIKLARINDAN ARINDIRILDI: temizlik gelenekti, artık KAPI. TABAN `caf41140` (develop; PR #25 indikten SONRA bu dala merge edildi, ölçüldü: `git rev-parse origin/develop` -> `caf41140` ve `git merge-base origin/develop HEAD` -> `caf41140`, yani ŞİMDİ aynı; BU KAYDIN ÖNCEKİ SÜRÜMÜ İKİSİNİ EŞİT SAYIYORDU VE #25 İNİNCE BU BAYATLADI: merge yapılmadan önce rev-parse `caf41140`, merge-base `fa3d493a` idi — İKİ FARKLI SHA. BU KAYIT 0078'DEN 0079'A ALINDI: tabandaki EN YÜKSEK sıra `docs/durum/0078-pr-0025.md`'dir, yani #25 0078'i aldı ve CI'nın birleşme sonucunda kapı `YİNELENEN sıra 0078` ile KIRMIZI oluyordu (ölçüldü).) TABAN BU PR SÜRERKEN İKİ KEZ OYNADI: `f623190d` -> `23b11ef2` (PR #28) -> `fa3d493a` (PR #30); aşağıdaki sayılar SON tabanda yeniden ölçüldü. BU KAYDIN ÖNCEKİ SÜRÜMÜ `f623190d`'yi "develop" DİYE ADLANDIRIYORDU VE BU YANLIŞTI: `f623190d` PR #27'nin tepesiydi ve develop O GÜNDEN BERİ İKİ KEZ İLERLEDİ; aşağıdaki her sayının yanında hangi sha'da ölçüldüğü YAZILIDIR. SAYIMLAR (dördü de ölçüldü, `git ls-tree -r --name-only <sha> backend/tests | grep -cE '(^|/)test_[^/]*\.py$'`): `backend/tests/` altında `f623190d`'de 110, `23b11ef2`'te 111, `fa3d493a`'te 111, bu dalda 112. 110 -> 111 ARTIŞI PR #28'in `test_donmus_saat_kapisi.py`sinden, 111 -> 112 ARTIŞI BU PR'IN KENDİ KAPI TESTİ `test_uretim_imaji_kapisi.py`den gelir (ikisi de ölçüldü, iki `git ls-tree` listesinin `diff`i tek satır döndürür). PR #30 SAYIYI DEĞİŞTİRMEZ: üç MEVCUT test dosyasını değiştirdi, yeni dosya EKLEMEDİ (ölçüldü) — bu yüzden `23b11ef2` ve `fa3d493a` aynı 111'i verir. Kök `backend/test_*.py` DÖRT SHA'DA DA 290'dır (ölçüldü) — yani test stage imajındaki toplam 402 = 290 + 112 (ölçüldü, imajın içinde `find /app/backend -name 'test_*.py' | wc -l`). BU KAYDIN ÖNCEKİ SÜRÜMÜNDEKİ 401 RAKAMI `f623190d` TABANININ 290 + 111'İDİR VE TABAN OYNAYINCA BAYATLADI; burada yeniden ölçülerek yazıldı, eski değerin üzerine aritmetik yapılarak DEĞİL. SİLİNEN TEST-ONLY VARLIKLAR, ADIYLA (production stage, `USER root` altında, sonra tekrar `USER app`): `backend/tests/`, `backend/test_*.py`, `backend/donmus_saat.py`, `backend/conftest.py`, `backend/pytest.ini`, `backend/run_isolated_tests.py`, `backend/isolated_test_reporter.py`, `backend/aggregate_isolated_test_reports.py`, `backend/merge_postgresql_test_reports.py`, ve BU TURDA EKLENEN BEŞİ: `backend/non_twin_skip_exceptions.json`, `backend/sandbox/` (tek dosyası `izibiz_smoke.py` GERÇEK SOAP çağrısı yapar — üretim imajında durması, kabuğa erişen birinin canlı e-fatura ucuna istek atabilmesi demektir), `backend/requirements-dev.txt`, `backend/LEGACY_TEST_MIGRATION_PLAN.md`, `backend/tools/capture_frontend_fixtures.py`. BEŞİNİN HİÇBİRİ ÇALIŞMA ZAMANINDA REFERANSLI DEĞİL, ÖLÇÜLDÜ: `grep -rn <ad> backend/app backend/run.py backend/alembic scripts` yalnız `sandbox`/`izibiz_smoke` için satır döndürür ve o satırların TAMAMI `app/einvoice/{endpoints,provider,ubl_xml}.py` içindeki DOCSTRING KANIT ATIFLARIDIR; aynı yollarda `(import|open\(|Path\(|read_text|load)` ile birlikte arandığında SIFIR satır döner. `deploy/` ve `docker-compose*.yml` de tarandı: sıfır. `requirements-dev.txt`in silinmesi `test` stage'i ETKİLEMEZ, ölçüldü: `test` stage dosyayı `Dockerfile:62`'de AYRICA `COPY` eder ve `rm` yalnız `production` stage'indedir; `backend/tools/`un DİĞER dosyası `report_receivable_allocation_cutover.py` KORUNUR (dizin değil, tek dosya silinir). ÜRETİM İMAJI ÖLÇÜMLERİ (bu dal, `--target production`): `find /app/backend -name 'test_*.py' | wc -l` -> 0; yedi yasaklı yolun (`tests`, `donmus_saat.py`, `non_twin_skip_exceptions.json`, `sandbox`, `requirements-dev.txt`, `LEGACY_TEST_MIGRATION_PLAN.md`, `tools/capture_frontend_fixtures.py`) her biri için `test -e` -> yok; temizlenmiş ağaçta `import app.main` YEŞİL (ölçüldü, imajın içinde, `SUNGUR_DATA_DIR=/opt/sungur-data`, alembic göçleri koştu). TEST STAGE ÖLÇÜMLERİ: `tests/` VAR, aynı yollar VAR, toplam 402 / `tests/` altında 112 / kökte 290. KAPI TESTİ İKİ YERDEN SERTLEŞTİRİLDİ, HER İKİSİ DE MUTASYONLA KIRMIZI GÖSTERİLDİ. (1) STATİK KAPI ARTIK MESAJ VERİR: `next(...)` VARSAYILANSIZ bırakıldığında `RUN rm` satırı yoksa `StopIteration` ile patlıyordu — kapı kırmızıydı ama NEDENİ okunmuyordu; `next(..., None)` + `assert removal_line is not None, "production stage'de RUN rm satırı yok"` ile değiştirildi. ÖLÇÜLDÜ, İKİ MUTASYON: `RUN rm` satırı `test` stage'e TAŞINDIĞINDA ve satır YORUMA ALINDIĞINDA testin çıktısı `AssertionError: production stage'de RUN rm satırı yok`. (2) `test_stage_still_contains_tests` YALANCI YEŞİL VERİYORDU: komut `test -d /app/backend/tests; find /app/backend -name 'test_*.py' | wc -l` idi; `;` `test -d`nin çıkış kodunu YUTAR ve sayım `/app/backend` GENELİNDE yapıldığı için kökteki 290 dosya toplamı tek başına >0 tutar. ÖLÇÜLDÜ, DOCTORED İMAJLA: `FROM yerel-hesap-test:...` üstüne `RUN rm -rf /app/backend/tests` konarak üretilen, `tests/`i TAMAMEN SİLİNMİŞ imajda ESKİ komut `290` basıp 0 ile çıkıyordu — yani kapı tests/ yokken de YEŞİLDİ. Komut `test -d /app/backend/tests && find /app/backend/tests -name 'test_*.py' | wc -l` yapıldı (`&&`, ve sayım tests/ ALTINDA) ve toplam sayım İKİNCİ bir iddia olarak eklendi; aynı doctored ağaçta test artık `AssertionError: test stage'de /app/backend/tests yok ya da sayım çalışmadı` ile KIRMIZI. Temiz ağaçta `python -m pytest backend/tests/test_uretim_imaji_kapisi.py -q` -> 3 passed (docker'lı ikisi dahil). `ci.yml` KAPSAMI GERİ ALINDI: önceki sürüm PG-shard yorum bloğuna BAŞKA BİR PR'IN metnini ("99 postgresql-adlı + 2 özel = 101" ve sekiz satırlık "100 -> 101: test_parti_skt_postgresql.py..." paragrafı) taşıyordu, üstelik sabit 100 ve glob 98 iken — yani yorum kendi çivisini YALANLIYORDU. Dosya `git checkout origin/develop -- .github/workflows/ci.yml` ile tabana döndürüldü ve YALNIZ bu PR'ın adımı yeniden kondu; ölçüldü: `git diff origin/develop...HEAD -- .github/workflows/ci.yml | grep -c '^@@'` -> 1. CI adımı `Verify production image has no test assets` yedi yasaklı yolu tek tek `test -e` ile ölçer, hangisinin kaldığını ADIYLA basar ve `test_*.py` sayısını ayrıca 0'a karşı denetler. ÖLÇÜLMEDİ, AÇIKÇA: (a) GHCR yayın yolu (`publish-image` job'u, `docker push ghcr.io/...`) — bu PR'da hiç koşmadı, temizlenmiş imajın yayınlanan etiketle aynı olduğu DOĞRULANMADI; (b) `deploy/veri-kaliciligi-kabul-testi.sh` ve `deploy/surum-ve-kurtarma-testi.sh` TEMİZLENMİŞ imaja karşı KOŞULMADI — yalnız iki betikte silinen yollara `grep` referansı bulunmadığı doğrulandı, betiklerin kendileri çalıştırılmadı; (c) `e2e` job'u; (d) silinen dosyaların DEPO DIŞI tüketicileri (başka repo, CI dışı betik, elle çalıştırılan araç) — depo içi referans yokluğu ölçüldü, depo dışı ölçülemez; (e) `production` stage'den SONRA yeni bir stage EKLENİRSE ne olacağı: statik kapının `_production_stage` dilimi `content[start:]` ile DOSYA SONUNA KADAR gider, yani sonraki stage'in satırları da "production stage" sayılır ve `RUN rm` oradan da bulunabilir — bu bir AÇIKTIR, bugün `production` son stage olduğu için ölçülmedi; (f) PostgreSQL 16 ikiz takımı ve CI iş akışının kendisi. `backend/app` altına DOKUNULMADI (ölçüldü, `git diff --name-only origin/develop...HEAD -- backend/app/` sıfır satır); alembic göçü EKLENMEDİ. Değişen dosyalar BEŞ: `.github/workflows/ci.yml`, `Dockerfile`, `backend/tests/test_uretim_imaji_kapisi.py`, `backend/test_production_deployment.py`, `docs/durum/0079-pr-0029.md`. BEŞİNCİ DOSYA PLANLI DEĞİLDİ ve CI kırmızısıyla ORTAYA ÇIKTI (koşu `33804511076`, `backend-quality (0)`): `test_production_deployment.py::test_runtime_image_remains_non_root_and_test_tools_are_separate` `assert "requirements-dev.txt" not in production` ile düşüyordu. İddianın KORUDUĞU şey "üretim imajı dev bağımlılıklarını KURMASIN"dır; ölçüm ise "metinde HİÇ geçmesin" diye yazılmış bir VEKİLDİ ve dosyayı SİLEN satırın kendisini de reddediyordu — yani vekil, koruduğu şeyin TERSİNİ cezalandırıyordu. Bir dosyayı silmek onu ADIYLA anmayı gerektirdiği için bu çarpışma kaçınılmazdı. İddia yöne bakacak şekilde yeniden yazıldı: yorum olmayan hiçbir satır `requirements-dev.txt`i pip ile KURMAMALI, ve `RUN rm -rf` satırı onu SİLMELİ. İKİ YÖNDE DE MUTASYONLA KIRMIZI GÖSTERİLDİ: `rm` listesinden yol çıkarılınca `production stage requirements-dev.txt'i SILMIYOR`, stage'e `pip install -r backend/requirements-dev.txt` eklenince `production stage dev bagimliliklarini kuruyor`. PR #30'un dört dosyasıyla KESİŞİM BOŞ (ölçüldü, `comm -12`). KAYIT 0077'DEN 0078'E ALINDI: PR #30 aynı oturumda 0077'yi aldı ve kapı `YİNELENEN sıra 0077` diye KIRMIZI oldu; sıra bir MÜLK değil, iniş anında boş olan bir yerdir.
