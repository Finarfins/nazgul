#42 — `npm ci`'NİN ÖRTÜK AUDIT'İ `contract-drift` İŞİNDE KAYNAĞINDAN KAPATILDI (`npm ci` → `npm ci --no-audit`, #37'nin `frontend` + `e2e` için uyguladığı mekanizmanın AYNI satırı: `npm ci` kendiliğinden audit isteği yapar ve kayıt defteri bozukken sabit zaman aşımı merdiveninde asılır; log "added 608 packages in 7m" vs "and audited 609 packages in 53s" ile AYIRT EDİLİR; `npm ci` audit bulgusunda non-zero DÖNMEZ yani kapı değildi); `ci.yml` TEK HUNK (`git diff origin/develop -- .github/workflows/ci.yml | grep -c '^@@'` = 1) ve hunk `contract-drift` içinde (`@@ -919,7 +919,8 @@`); yorum `e2e` işindeki tek satırın aynısı (`# `--no-audit`: KAPSAM KAYBI YOK — gerekçe `frontend` işindekiyle AYNI.`); `deploy/ci-required-contexts.json` DOKUNULMADI; TABAN `origin/develop` `77babbc`; ÖLÇÜLMEDİ — `--no-audit` tasarrufu yalnız kayıt defteri arızasında gözlemlenir ("saving observable only during an outage").
