#24 — Üç MEVCUT test, adını taşıdığı mekanizma kaldırıldığında KIRMIZI olacak hâle getirildi; yeni kapı YOK, yeni zorunlu bağlam YOK, `backend/app` altında değişiklik YOK ve HİÇBİR PİN KIPIRDAMADI (`ci.yml`/`test_ci_gates.py` PG dosya sayısı 99'da SABİT — ölçüldü: `test_*postgresql*.py` + iki özel dosya = 99, çünkü yeni ölçümler YENİ DOSYA açmak yerine zaten sayılan iki ikizin İÇİNE eklendi); kural şu ve bu PR onu üç kez ölçüyor: yeşil bir test "kod doğru" DEMEZ, "bu girdi bu kodu kırmadı" der, mekanizmanın iş gördüğünü görmek için o mekanizma olmadan KIRILACAK bir girdi gerekir; (a) `bindparam` KAPISI GREP'TEN DAVRANIŞA TAŞINDI: `tests/test_farm_reentry_enforcement.py::test_reentry_query_is_parcel_scoped_and_tenant_bound` kaynağı `bindparam("pid", type_=Integer)` DİZGESİ için grep'liyordu ve dizgeyi koruyup davranışı bozan bir yeniden düzenleme o kapıdan GEÇERDİ — ölçüldü: `_GIRIS_SORGU`ya `_GIRIS_SORGU = text(str(_GIRIS_SORGU))` satırı eklendiğinde dizge YERİNDE kalır, grep testi YEŞİL geçer ve `field-safety` ucunun `pid=None` çağrısı üretim diyalektinde 500 döndürür; grep iddiası SİLİNDİ, yerine `run_bindparam_typing_probe` kondu ve PostgreSQL ikizinden (`test_giris_sorgusu_pid_tipi_postgresql`) koşuyor: sorgunun METNİ birebir aynı kalıp yalnız tip düşünce PostgreSQL `42P08 AmbiguousParameter` ("could not determine data type of parameter $2") veriyor, GERÇEK sabit ise `pid=None` ve `pid=7` ile hata VERMİYOR — bu ayrım SQLite'ta ölçülemez çünkü orada tipi belirsiz bir parametre hata vermez; (b) BKÜ İÇE AKTARMA İKİZİ ADINI TAŞIDIĞI MEKANİZMAYI AYIRT ETMİYORDU VE BU BİR HİPOTEZ DEĞİL ÖLÇÜMDÜR: `test_farm_bku_ice_aktarma_postgresql.py` yeşildi ama paylaşılan `_SMOKE`un bozuk satırlarının ÜÇÜ DE (ürün bulunamadı, ürün boş, "yirmi" sayı değil) HİÇBİR DEYİM ÇALIŞTIRILMADAN reddediliyor — SQLAlchemy `handle_error` dinleyicisiyle sayıldı: bütün koşu boyunca BAŞARISIZ OLAN DEYİM SAYISI SIFIR ve `db.begin_nested()` SİLİNDİĞİNDE ikiz YEŞİL KALIYOR, yani savepoint o dosyada yük TAŞIMIYORDU; bu yüzden ikizin kendi belgesindeki 2. iddia — `SELECT` ile `INSERT` arasındaki yarış — gerçekten kuruldu (`run_savepoint_smoke`, `test_bku_ice_aktarma_savepoint_postgresql`): bir `after_cursor_execute` dinleyicisi çakışma araması ÇALIŞTIKTAN SONRA aynı (ürün, bitki) satırını BAŞKA bir bağlantıdan yazıp işliyor, uç `uq_ppp_company_product_crop` ihlaliyle GERÇEK bir `IntegrityError` alıyor ve iddia yaristan SONRAKİ satırın YAZILDIĞIDIR; `after_` şart ve bu da ölçüldü — ilk sürüm `before_cursor_execute` kullanıyordu, satır SELECT'ten ÖNCE görünüyordu, uç onu NORMAL çakışma yolundan reddediyordu, `IntegrityError` HİÇ oluşmuyordu ve ölçüm savepoint'i sınadığını SANARAK yeşil kalıyordu, o yüzden koşu artık redin gerekçesinde `(#` BULUNMADIĞINI da iddia ediyor (SELECT yolu çakışan kaydın KİMLİĞİNİ söyler, kısıt yolu söylemez) ve yarışın gerçekten tetiklendiğini AYRICA iddia ediyor; (c) XLSX KESİRLİ GÜN REDDİ BİRİMDEN UÇTAN UCA ÇIKARILDI: `test_kesirli_gun_sessizce_yuvarlanmaz` çevirici FONKSİYONU doğrudan çağırıyordu ve uçtan uca fikstürlerin TAMAMI CSV'ydi, CSV ise her hücreyi METİN olarak verir, dolayısıyla çeviricinin ÜSTÜNE eklenen bir `float` dalı hiçbir uçtan uca koşuyu kırmazdı — mekanizma ölçüldü: openpyxl `<v>` içeriğini nokta YOKSA `int`, VARSA `float` yapar, yani `21.0` normal yazıldığında `21` (int) olarak gelir ve yalnızca TAM OLMAYAN bir hücre gerçekten `float` teslim eder, tehlikeli durum tam olarak budur; paylaşılan `_SMOKE`a (yani HEM SQLite HEM PostgreSQL koşusuna) PHI hücresi gerçek `20.6` sayısı olan bir `.xlsx` yüklemesi eklendi, satır REDDEDİLİYOR, gerekçe hücrenin kendisini (`20.6`) söylüyor ve katalogda `Kesirli Bitki` için HİÇBİR SATIR YOK (aşağı yuvarlanmış bir `20` yazılsaydı kilit süresi dolmadan hasada izin verirdi), aynı dosyanın `21.0` satırı ise `21` olarak YAZILIYOR — yani xlsx yolu gerçekten çalışıyor ve dosya bütünüyle reddedilmiş değil, ayrıca hücrelerin `float`/`int` olarak GELDİĞİ koşunun içinde ayrıca çivileniyor; KENDİ İŞİMİN KABUL TESTİ — HER ÜÇÜ DE MUTASYONLA KIRMIZI GÖSTERİLDİ: (a) `bindparam` tipi tamamen kaldırılınca yeni ölçüm KIRMIZI (`AmbiguousParameter`) ve daha önemlisi DİZGEYİ KORUYAN mutasyonda (`_GIRIS_SORGU = text(str(_GIRIS_SORGU))`) eski grep testi YEŞİL kalırken yeni ölçüm KIRMIZI, (b) `db.begin_nested()` silinince yeni koşu KIRMIZI (`psycopg.errors.InFailedSqlTransaction: current transaction is aborted`) ve AYNI koşuda eski ikiz YEŞİL kalıyor — kapatılan boşluk tam olarak budur, (c) çeviriciye `int(float(...))` geri konunca hem birim testi hem yeni uçtan uca koşu KIRMIZI, ÇEVİRİCİNİN ÜSTÜNE (`import_ppp` içine, `_ice_aktarma_tamsayi` çağrısından önce) bir `float` dalı konunca ise birim testi YEŞİL kalırken yalnız yeni uçtan uca koşu KIRMIZI; her mutasyon geri alındıktan sonra dört dosya da YEŞİL; ÖLÇÜLEN KOŞULAR: gerçek PostgreSQL 16.14 (docker `postgres:16-alpine`, alembic `20260902_0065`) üzerinde `test_farm_bku_ice_aktarma_postgresql.py` 2/2 ve `test_farm_reentry_enforcement_postgresql.py` 2/2 (CI'ın dosya başına yalıtımını taklit ederek HER DOSYA KENDİ TAZE VERİTABANINDA), SQLite tarafında `tests/test_farm_bku_ice_aktarma.py` ve `tests/test_farm_reentry_enforcement.py` tamamı geçti, ayrıca `tests/test_ci_gates.py`, `test_backend_encoding.py`, `test_v2_9_decimal_contract.py` ve `tests/test_durum_kaydi.py` geçti — SAYIM DÜZELTİLDİ, aşağıdaki kayıt düzeltme turuna bak: bu altı dosya TEK komutla (`python -m pytest tests/test_farm_bku_ice_aktarma.py tests/test_farm_reentry_enforcement.py tests/test_ci_gates.py test_backend_encoding.py test_v2_9_decimal_contract.py tests/test_durum_kaydi.py -p no:cacheprovider -q`) `61 passed in 12.82s`, iki PostgreSQL ikizi her biri kendi taze şemasında `2 passed` + `2 passed`, yani 61 + 4 = 65 test, 0 başarısız; ÖLÇÜLEN VE BU PR'A AİT OLMAYAN: iki PostgreSQL ikiz dosyası AYNI veritabanını paylaşarak ARDIŞIK koşturulduğunda `test_reentry_enforcement_postgresql` düşüyor çünkü BKÜ koşusu admin parolasını zaten değiştirmiş oluyor — bu develop `7498ab3` üzerinde de AYNEN görülüyor (değişikliklerim `git stash` ile kenara alınarak ölçüldü), CI her dosyayı kendi sürecinde ve `PG_RESET_URL` ile koşturduğu için orada görünmüyor, dolayısıyla bu dalın açtığı bir kusur DEĞİLDİR ve burada düzeltilmedi; yalnız yeni `run_savepoint_smoke` kendi firmasını (`Yaris Firma`) yaratıyor ve parolanın değişmiş olma ihtimalini kabul ediyor, çünkü kardeş koşuyla AYNI dosyada ve AYNI veritabanındadır ve otekinin katalog SAYIMINI bozmamalıdır; DEĞİŞEN DOSYALAR: `backend/tests/` altında iki dosya (ölçümlerin GÖVDESİ orada) ve zaten PG sayımında olan iki ikiz dosyası (`backend/test_farm_bku_ice_aktarma_postgresql.py`, `backend/test_farm_reentry_enforcement_postgresql.py`) yalnızca yeni ölçümü ÇAĞIRAN birkaç satırla — ikizler `backend/tests/` ALTINDA DEĞİLDİR ve orada olamazlardı, çünkü `ci.yml`in PG seçicisi `test_*postgresql*.py`yi DEPO KÖKÜNDE arar, `backend/tests/` altına konan PG işaretli bir test `backend-postgresql` işinde HİÇ KOŞMAZDI, yani dekoratif olurdu; ÖLÇÜLMEDİ: tam yalıtım koşusu (`run_isolated_tests.py` ile 370 dosya), frontend ve mobil derleme bu dalda hiç koşulmadı — bu dilim `backend/tests` dışına çıkmıyor ve kalan doğrulama CI'a bırakıldı; İKİNCİ BİR PİN CI'DA ORTAYA ÇIKTI VE PİNİ OYNATMAK YERİNE ÖLÇÜM YENİDEN YAZILDI: ilk itişte `backend/tests/test_tenant_scoping_guard.py::test_alt_surecte_sql_sayisi_donduruldu` KIRMIZI oldu (dosya 102 -> 103, gömülü metin 165 -> 166) çünkü yarış kurucusu alt sürece ham SQL metni veriyordu ve o kapı "alt sürece SQL veren yüzey"i donduruyor — bu yüzey AST tabanlı kiracı nöbetçisine GÖRÜNMEZ, o yüzden büyümesi bilinçli bir karar olmalıdır ve BU ÖLÇÜMÜN böyle bir karar istemeye hiç hakkı yoktu: ölçtüğü şey İŞLEM YALITIMIDIR, SQL yazmak değil; bu yüzden sabit çivi DEĞİŞTİRİLMEDİ, yazma YANSITILAN tabloya (`Table(..., autoload_with=...)` + `insert().values(...)`) çevrildi ve koşunun içindeki Türkçe açıklamalardaki SQL anahtar sözcükleri de ayıklandı (kapı 120 karakterden uzun HER dizgede anahtar sözcük arar, yorumda geçse bile); yeniden yazımdan sonra kapı YEŞİL (dosya 102, metin 165 — develop'un değeri) ve (b) mutasyonu TEKRARLANDI: `db.begin_nested()` silindiğinde yeni koşu yine KIRMIZI (`InFailedSqlTransaction`), eski ikiz yine YEŞİL, yani yeniden yazım ayırt ediciliği KAYBETMEDİ; AYRICA ÖLÇÜLEN VE BU PR'A AİT OLMAYAN İKİNCİ ŞEY: `frontend` işi `AUDIT_POLICY_RED: unapproved advisory GHSA-6GMQ-8VP8-GCM6 (@xmldom/xmldom)` ile düşüyor, bu dal `frontend/` altında SIFIR satır değiştiriyor ve AYNI başarısızlık bu dalın TABANI olan develop `7498ab3` üzerinde de görülüyor (koşu 33655135774), yani yeni yayımlanmış bir npm danışmanlığıdır ve bu dilime ait değildir; REBASE TURUNDA KENDİ ÖLÇÜMÜMDE BİR SIRA BAĞIMLILIĞI BULUNDU VE KAPATILDI (`9c8cb87` üzerine birleştirme): (a)'nın `_BINDPARAM_PROBE`u doğrudan `create_engine` ile bağlanıyordu ve ŞEMANIN VAR OLDUĞUNU VARSAYIYORDU — TEK BAŞINA, BOŞ bir veritabanında, MUTASYON OLMADAN `UndefinedTable` ile DÜŞÜYOR, çünkü PostgreSQL tabloyu parametre tipini çözmeden ÖNCE arıyor; ilk turda yeşil görünmesinin sebebi AYNI dosyadaki kardeş koşunun (`test_reentry_enforcement_postgresql`) göçleri ÖNCEDEN çalıştırmış olmasıydı, yani ölçümün yeşilliği TEST SIRASINA bağlıydı ve bu tam olarak bu PR'ın karşı çıktığı türden bir kaza eseri yeşilliktir; bu turda taze bir veritabanı kullanıldığı için görüldü ve ÖLÇÜLDÜ (mutasyonsuz, tek başına, boş veritabanı -> KIRMIZI), sonra probe'a `with TestClient(app): pass` eklendi (şemayı uygulamanın KENDİSİ kuruyor) ve artık TEK BAŞINA ve TAZE veritabanında YEŞİL, ardından beş mutasyonun HEPSİ yeni taban üzerinde YENİDEN koşuldu ve HEPSİ hâlâ ayırt ediyor: (a) `bindparam` tipi kaldırılınca KIRMIZI (`AmbiguousParameter`, `tests/test_farm_reentry_enforcement.py:87`), (a2) dizge KORUNUP tip etkisiz kılınınca eski grep YEŞİL / yeni probe KIRMIZI, (b) `db.begin_nested()` silinince KIRMIZI (`InFailedSqlTransaction`, `tests/test_farm_bku_ice_aktarma.py:84`) ve eski ikiz YEŞİL, (c) çeviriciye `int(float(...))` konunca birim ve uçtan uca KIRMIZI, (c2) `float` dalı çeviricinin ÜSTÜNE konunca birim testi YEŞİL / uçtan uca KIRMIZI; birleşme çakışmasızdı (PR #26 yalnız `frontend/package-lock.json` ve bir durum girdisi değiştirdi) ve tek düzenleme durum kaydının YENİDEN ADLANDIRILMASIYDI (`0072-pr-0024.md` -> `0073-pr-0024.md`, `--kapi` YİNELENEN SIRA hatası verdikten sonra `--sonraki 24`nün söylediği ad); yeni tabanda PİNLERİN HİÇBİRİ KIPIRDAMADI: `EXPECTED_UNDENIABLE` 99, `GET_INVENTORY_COUNT` 166, PG dosya sayısı 99, alembic başı `20260902_0065`, `BEKLENEN_ALT_SUREC_SQL` 102/165 (yansıtılan tablo yazımı bu turda da yüzeyi büyütmedi).; KAYIT DÜZELTME TURU (`783908c` üzerine, yalnız belge ve durum kaydı — `backend/app` altında SIFIR değişiklik): bağımsız sözleşme merceği mekanizmayı kendi mutasyonlarıyla ölçüp GO verdi ama kaydın iki kopyasının birbiriyle çeliştiğini buldu — PR gövdesi "61 test geçti", bu kayıt "toplam 64 test" diyordu ve ikisi de HANGİ kümeyi saydığını söylemiyordu; sayılar EŞİTLENMEDİ, ÖLÇÜLDÜ: altı dosyalık SQLite tarafı kümesi yukarıdaki tek komutla `61 passed in 12.82s` (dosya başına 2+3+16+2+5+33), `test_farm_reentry_enforcement_postgresql.py` taze şemada `2 passed in 8.02s`, `test_farm_bku_ice_aktarma_postgresql.py` taze şemada `2 passed in 8.16s` (PostgreSQL 16.14, alembic `20260902_0065`), toplam 65; `64` HİÇBİR yeniden kurulabilir kümeye karşılık gelmiyor ve YANLIŞTI, `61` doğruydu ama kapsamı belirsizdi, bu yüzden iki kopyaya da ölçülen sayı KAPSAMIYLA birlikte yazıldı; ikinci bulgu: PR gövdesi `783908c`ye göre BAYATTI — sıra bağımlılığı bulgusunu, `with TestClient(app): pass` düzeltmesini ve 0073 yeniden adlandırmasını taşımıyordu, "her dosya kendi taze veritabanında … 2/2" satırı `783908c` ÖNCESİ ağacı anlatıyordu — gövde mevcut baş için yeniden yazıldı; `fac6c9d`nin `0072-pr-0024.md` göndermesi TARİHSELDİR ve dokunulmadı; taban yeniden doğrulandı: develop `9c8cb878ce0745b21260b16851c68d678af57e5c`, kayıt numarası 0073 = 0072 + 1 yerinde.
