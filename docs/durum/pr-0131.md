#131 — H37 TRANSACTIONS TESTİ SIRA BAĞIMLILIĞI, KÖK NEDEN (frontend test altyapısı; bileşen/backend YOK): vitest `globals` kapalı olduğundan RTL `afterEach(cleanup)` kaydedemiyordu, `src/test/setup.ts` de temizlemiyordu; `Transactions.test.tsx`'te `?new=1` alış testinin ağacı bağlı kalıp sonraki testin `dialogProps.mockReset()`'inden sonra `purchase:true` yazıyor, "parametresiz açılışta belge formunu kendiliğinden açmaz" son çağrıyı okuyup `expected true to be false` ile aralıklı düşüyordu (sonda: sızıntıda `roots: 2, purchaseCalls: 1`); DÜZELTME: `setup.ts`'e global `afterEach(()=>cleanup())` (dosya başına 70 cleanup zararsız; test dosyası değişmedi; retry/waitFor/timeout YOK) — cleanup yapmayan SupplierPriceHistory, SupplierPriceImportDialog, EntityLedgerOverview, AppShell.backups da kapsandı; aynı dosyada ikinci sıra bağımlı test (`satış listesinin Müşteri kolonu…`, yalnız shuffle) da kapandı; ÖLÇÜM: önce 1/20 ileri, 2/20 shuffle → sonra 0/50 ileri, 0/50 shuffle; mutasyon: global satır silinince sonda yeniden sızıyor + 3/20 düşüyor, `if(!quickStart)return;` kaldırılınca hedef test düşüyor; lint 0 hata, build yeşil, tam vitest 101/101 dosya 754/754 test (e396094 üstünde)
