#147 — H54 + H69 OFFSET TAVANI `INT4_UST` BEŞ UÇTA + ORTAK SABİT (backend-only, göç YOK, rota/GET sayıları SABİT): H69 — `INT4_UST = 2147483647` `routers/cek_senetler.py`den çıktı, TEK EVİ `app/sinirlar.py` (docstring: tavan veritabanının int4 tavanıdır, sürücünün değil); `cek_senetler.py`, `despatch_notes.py` (önceden KARDEŞ yönlendiriciden içe aktarıyordu) ve iki test (`tests/test_e4a_despatch_notes.py`, `test_h51_irsaliye_listesi_postgresql.py`) oradan alıyor, davranış değişikliği yok, AST kapısı ikinci bir `INT4_UST =` atamasını KIRMIZI yapar; H54 — beş tavansız `offset: int = Query(0, ge=0)` (`activity_logs.py:78`, `products.py:330` `/lots/mutabakat`, `platform_management.py:436/495/568` companies/users/verifications) → `Query(0, ge=0, le=INT4_UST)`, yanlarındaki `limit`lerin HEPSİNDE `le` zaten vardı ve dokunulmadı; ÖLÇÜLDÜ (PG 16.4, taze db, düzeltmeden önce `60f737b`): `offset=2^31` BEŞİNDE DE 200 (brifin beklediği 500 ÖLÇÜLMEDİ — 2^31 geçerli bir bigint OFFSET'tir), `offset=2^63` DÖRDÜNDE 500 `(psycopg.errors.NumericValueOutOfRange) bigint out of range`, `/lots/mutabakat`ta 200 (sayfayı Python'da dilimliyor); sonra 2^31 ve 2^63 422 (`loc == ["query","offset"]`), `INT4_UST` 200 boş sayfa; testler `tests/test_h54_offset_tavani.py` (SQLite, uç başına 422@INT4_UST+1 / 200@INT4_UST + tek-ev AST kapısı, 6 test, ileri ve ters sırada yeşil) ve `test_h54_offset_tavani_postgresql.py` (PG ikizi, 20 test, taze db, 0 atlama), `pg_twins` 135 → 136; mutasyon: her ucun `le=INT4_UST`si tek tek silinince SQLite 1 + PG 2 KIRMIZI; `products.py` dinamik SQL parmak izi 674de294 → c5c71b29 (SQL metni değişmedi, dinamik sayı 6'da, toplam 270 SABİT), Core envanteri 233/494f7b8b SABİT; `types.gen.ts` İÇERİK OLARAK DEĞİŞMEDİ (openapi-typescript `maximum` üretmiyor; openapi.json'da `maximum: 2147483647` var), ikinci koşu byte-özdeş.
