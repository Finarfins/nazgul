#162 — F9-5-fix (K8) servis faturası KDV, GÖÇ YOK / ROTA YOK: (H79) LABOR satırı `invoice_service.SERVICE_LABOR_VAT_RATE`=20 taşır (firma KDV ayarı yok, değer çekirdek `vat_rate` server_default'u; hukuki oran DOĞRULANMADI, firma ayarı F9-5a sorusu); (H80) belge iskontosu satır MATRAHINA dağıtılır ve `tax_amount = round(oran × (matrah − pay))` yeniden türetilir (`line.tax` kalktı), FIXED değer matrah iskontosu, `totals.global_discount` ödenecekteki düşüş, PDF Ara Toplam = grand + global_discount (eskilerde labor+parts ile aynı); absorpsiyon faturalı işçilik gelirini NET okur (tenant guard izi 95f560bc->cfcb945a); kesilmiş faturalar yeniden hesaplanmaz; (H82) seed başlık `vat_total` = Σ satır KDV (satışta 11/60 kayıyordu, alış 0/25) — şimdi 60/60 + 25/25. Önizleme/COMPLETED alacağı işçiliği hâlâ KDV'siz gösteriyor (G, fatura alacağı revize ediyor). pg_twins 142->143, openapi bayt-aynı. Kanıt: izole suite 489/489 dosya, 4279 sonuç (8'i 180 sn zaman aşımı, 900 sn ile 8/8), rebase sonrası kapı+tarayıcı+fatura 11/11; mutasyon `tax_amount=line.tax` test 2'yi kırdı; PG ikizi yerelde DOĞRULANMADI. DÜZELTME 1 (lens: kanonik, PDF iskonto sırası, 422): PG ikizinin `kanonik()` bool'u `str(int())` metnine indiriyor (PG `False` ≠ altın "0"); PDF toplam bloğu Matrah → İskonto → KDV Matrahı → KDV → Genel Toplam (KDVK m.25; FIXED 10 artık 10,00 basılır, 12,00 değil), İskonto yeni EK anahtar `totals.global_discount_base`tan, eski faturada `global_discount`tan — iki özdeşlik her ikisinde tutuyor (anahtar yeniden adlandırılmadı, dağıtım kodu aynı); iskonto matrahı aşarsa 500 yerine 422 `ISKONTO_TOPLAMI_ASIYOR` (FIXED ve PERCENT). İKİNCİ REBASE (develop ad36c64): 0 çakışma, backend/app+alembic patch-id aynı (42c96a2117a8), pg_twins 144, dinamik text() 271 ve absorpsiyon izi cfcb945a aynı, 5 dosya yeşil. — <merge tarihi>
