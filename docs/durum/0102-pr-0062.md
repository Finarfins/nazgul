#62 — VETERİNER İLAÇ KATALOĞU, TEDAVİ DEFTERİ ve ARINMA (BEKLEME) KİLİTLERİ (E2): hayvancılık modülünde ilaç arınma süresi kavramı HİÇ YOKTU ve bu ÖLÇÜLDÜ, varsayılmadı — `treatment`/`withdrawal`/`arinma`/`bekleme` literalleri `app/routers/herd.py` ve `app/herd_*.py` içinde SIFIR isabet veriyordu, `animal_vaccinations` bir AŞI defteridir ve aşının kalıntı süresi yoktur, yani antibiyotik uygulanmış bir hayvanın sütü sistem HİÇBİR ŞEY BİLMEDEN tanka yazılabiliyordu; durum tarla tarafındaki 0063 öncesinden DAHA KÖTÜYDÜ çünkü orada sayı elle GİRİLEBİLİYORDU (boş kalıyordu), burada GİRİLECEK ALAN YOKTU; göç `20260908_0074`, down_revision `20260908_0073`, TABAN `baadecf` (`origin/develop`) ve açılıştan önce `gh pr list --state open` KOŞTURULDU — AÇIK PR YOKTU, yani `20260908_0074` TEK BAŞ ve rebase gerekmedi; GÖÇÜN ALTI PARÇASI: (a) `vet_drugs` — `(company_id, product_id, species)` tekil, `species` BOŞ DİZE = bütün türler (0063'ün `crop=''` deseni; NULL DEĞİL çünkü SQL'de UNIQUE NULL'ları BİRBİRİNDEN FARKLI sayar ve aynı ilaca sınırsız 'bütün türler' satırı girilebilirdi), `product_id` NOT NULL (bağsız satır hiçbir tedaviyi çözemez), `milk_withdrawal_days` ve `meat_withdrawal_days` İKİSİ DE NOT NULL ve aralık CHECK'li (0..3650), artı `route`/`dose_unit`/`registration_no`/`notes`/`status` ve 0065'in `origin`/`origin_reference` çifti; (b) `animal_treatments` — `animal_id` XOR `group_id` (`ck_animal_treatments_hedef`), `treated_on DATE`, ÇÖZÜLMÜŞ etkin süreler (NULL kabul eder), `withdrawal_source` (CATALOGUE | OPERATOR | OPERATOR_OVERRIDE — 0063/0072 ile BİREBİR AYNI sözlük ve AYNI genişlik, hayvancılığa ÖZEL bir küme AÇILMADI), `catalogue_milk_days`/`catalogue_meat_days`; (c) `animal_treatment_items` — `treatment_id` bileşik FK, `product_id` NULL KABUL EDER ve asimetri 0063'ünkiyle aynı gerekçeye dayanır (serbest metin ilaç çözülmez ve çözülmemeli: veteriner kendi getirdiği, depoda stok kartı olmayan ilacı da kaydedebilmeli), `dose NUMERIC(14,4)`; (d) `milk_yields` += `withdrawal_warning` (SİSTEMİN bulduğu) / `withdrawal_override_reason` (KULLANICININ söylediği) — 0048'in ayrımı, aynı sütunda tutmak denetimde kimin ne dediğini ayırt edilemez kılardı; (e) `animal_movements` += aynı çift; (f) `companies.herd_withdrawal_policy` VARCHAR(20) NOT NULL DEFAULT 'require_reason' CHECK IN (block, require_reason, warn) ve "allow" YOK — gerekçe kardeşlerininkinden GÜÇLÜ çünkü kapatılan şey İNSAN GIDASIDIR; İKİ SÜRE NEDEN AYRI SÜTUN: prospektüste ayrıdır (bir ilacın süt arınması 3, et arınması 28 gün olabilir) ve tek sütuna indirmek ikisinden birini YALAN SÖYLETİRDİ; `crop`TAN BİR FARK ŞEMAYA YAZILDI ve bu bir zevk değil katlamayı GEREKSİZ KILAN dayanaktır — `crop_seasons.crop` SERBEST METİNDİR (0044) ve 0063 eşleştirmeyi Python'da Türkçe katlamayla yapmak ZORUNDA kalmıştı, `animals.species` ise KAPALI bir kümedir (`ck_animals_species`, 0049) ve katalog sütunu da aynı kümeye kısıtlı (`ck_vet_drugs_species`), bu yüzden eşleştirme TAM EŞİTLİKTİR ve katlama BİLEREK kullanılmadı; kısıt olmasaydı 'Sığır' gibi yanlış yazılmış bir satır girilir ve HİÇBİR hayvana eşleşmezdi — katalog SESSİZCE işlevsiz kalırdı, ve kısıtın GERÇEKTEN ısırdığı PG ikizinde ölçülüyor; `ck_animal_treatments_hedef` NEDEN ŞEMADA: `milk_yields`te aynı kural 0049'dan beri YALNIZ uygulama katmanındaydı (0049'un kendi notu bunu söylüyor) ve uygulama katmanı bir betiği ya da elle yazılmış bir INSERT'ü DURDURMAZ; DİYALEKT AYRIMI AÇIK YAZILDI ve İKİ DERSİ BİRDEN uyguluyor — `companies` dalında sütun ve CHECK AYRI AYRI SORULUYOR (0072'de ölçülmüş kusur: `app/tenancy.py` `companies`i `Table()` olarak bildiriyor, uygulama AÇILIŞI tabloyu alembic'ten ÖNCE kurabiliyor, sütun bildirime de eklendiği için tek koşullu bir dal CHECK'i HİÇ KURMAZDI ve göç YEŞİL biterdi), SQLite dalında sütun + CHECK TEK batch (0071'in dersi: iki ayrı batch tabloyu iki kez yeniden kurar), öteki dalda AÇIK `op.add_column` / `op.create_check_constraint` (0072'nin dersi: batch'in `create_check_constraint`i yeniden kurulum YAPILMAYAN diyalektte SESSİZCE hiçbir DDL üretmiyor), ve `milk_yields`/`animal_movements` dallarında CHECK olmadığı için tek batch YETİYOR; `up -> down -> up` turu HEM SQLite'ta HEM gerçek PostgreSQL 16'da KOŞTU; `downgrade` 0073'ün 'tablo BOŞ olmalı' NÖBETÇİSİNİ TAŞIMIYOR ve fark göçün başlığında ADIYLA yazılı: 0073 VAR OLAN bir tablonun ŞEKLİNİ değiştiriyordu ve mevcut satırlar için anlamlı bir varsayılan ÜRETEMİYORDU, burada tabloların TAMAMI bu göçün eseridir ve geri alma 'bu göçün getirdiğini götür' demektir; DEPO HİÇBİR ARINMA SÜRESİ İDDİA ETMİYOR — göç TEK SATIR VERİ YAZMIYOR (başlangıç ilaç listesi, tür başına varsayılan, 'antibiyotikler için 7 gün' gibi bir kod sabiti YOK) ve `test_kodda_YASAL_ARINMA_SABITI_YOK` bunu çiviliyor; DAVRANIŞ — KATALOG `/api/vet-drugs` CRUD (okuma `herd.view`, yazma `herd.manage`), liste ürün adını KİRACI İÇİNDE birleştiriyor (`u.company_id=k.company_id` olmadan başka firmanın ürün adı listeye düşerdi), güncelleme iyimser kilitli; TEDAVİ `/api/animal-treatments` yazması `herd.health` ve bu `auth.py`deki AŞI kuralının GENİŞLETİLMESİ DEĞİL AYNEN UYGULANMASIDIR (kural 'veteriner ya da sağlık sorumlusu' diyor ve ilaç tedavisi aşıdan daha da açık biçimde veterinerlik işidir; `herd.manage`a bağlamak aşı girebilen veterinerin tedavi giremediği bir sistem üretirdi), KATALOG ise `herd.manage`da KALIR çünkü o bir OLAY DEĞİL TANIMDIR — firmanın bütün gelecek tedavilerinin süresini belirler; ÇÖZÜM kalem başına türe ÖZEL satır, yoksa `species=''` yedeği, EN UZUN KAZANIR ve İKİ ALAN BAĞIMSIZ hesaplanıyor (bir ilacın süt arınması, başka bir ilacın et arınması uzun olabilir ve 'en uzun süreli ilacın çiftini al' demek öteki ilacın uzun olan alanını SESSİZCE kısaltırdı), operatör kazanır ama üstüne yazma SESSİZ OLAMAZ (`OPERATOR_OVERRIDE` + `catalogue_*`), aynı şeyi söylemek üstüne yazma DEĞİLDİR (`OPERATOR`), katalog susarsa süre BOŞ kalır ve BOŞ İHLAL DEĞİLDİR (0063 kuralı); süreler YAZMA anında çözülüp SATIRA yazılıyor çünkü katalog yarın değişirse DÜN kaydedilmiş bir tedavinin arınması geriye dönük KISALMAMALI; SÜT KİLİDİ `POST /api/milk-yields` HAYVAN ve SÜRÜ yolunun İKİSİNDE DE — üyeliğin nasıl modellendiği ÖLÇÜLDÜ (`animals.group_id`, TEK sütun): hayvan yolu hayvanın KENDİ tedavilerini VE içinde bulunduğu SÜRÜYE yazılmış tedavileri birlikte sayıyor (sürünün tamamı ilaçlandığında o sürüdeki her hayvanın sütü etkilenir ve yalnız bireysel kayda bakmak kilidi küçükbaş işletmesinde TAMAMEN işlevsiz bırakırdı), sürü yolu ise sürüye yazılmış tedavileri VE sürüdeki HERHANGİ BİR hayvanın bireysel tedavisini birlikte sayıyor (tek bir tedavi edilmiş inek tankın tamamını kirletir); `earliest_allowed = treated_on + milk_days` ve SINIR GÜNÜ SERBEST (`<`); tarla tarafındaki `_yerel_gun` çevrimi burada GEREKMİYOR ve gerekçesi ÖLÇÜLDÜ — `performed_at` bir ZAMAN DAMGASIDIR (UTC gününe göre hesaplamak süreyi bir gün kaydırırdı), `treated_on` bir DATE'tir ve saat taşımaz, `_gun` yalnız sürücü farkını (SQLite metin / PostgreSQL `date`) düzeltiyor; ÖLÇÜLMÜŞ SINIR ADIYLA YAZILI — üyelik GÜNCELDİR, GEÇMİŞ DEĞİL: depoda üyelik geçmişi tutan hiçbir tablo YOKTUR, yani tedaviden sonra sürü değiştiren bir hayvan YENİ sürüsünün kurallarıyla değerlendirilir ve bunu düzeltmek bir üyelik defteri açmak demektir, o KENDİ göçünü ister ve burada İDDİA EDİLMİYOR; ET KİLİDİ `POST /api/animal-movements` YALNIZ `SALE` ve `SLAUGHTER`da ve dışarıda kalanların gerekçesi kodda ADIYLA yazılı, unutkanlık DEĞİL: `DEATH` — hayvan ölmüştür ve kilidi buraya koymak ölümü BİLDİRMEYİ zorlaştırırdı, oysa ölüm kaydı denetimin en çok ihtiyaç duyduğu kayıttır ve onu caydırmak arınma süresini KORUMAZ, defteri BOZAR; `TRANSFER_OUT` — hayvan başka işletmeye gidiyor, kesime değil, arınma süresi hayvanla BİRLİKTE taşınır ve kesim kararını KARŞI TARAF alır, buraya kilit koymak aynı süreyi iki kez sorardı ve ikincisinde (gerçek kesimde) hiçbir şey bilmezdik; `PURCHASE`/`TRANSFER_IN` — hayvan GELİYOR, zaten kesilmiyor; POLİTİKA ÜÇ SEVİYE (block ret, require_reason varsayılan, warn kabul+uyarı) ve okunamayan ayarda SIKI tarafa düşülüyor (`_firma_kurallari`nın fail-closed duruşu); HAYVANCILIĞIN KATALOĞA GİREN İLK AKTİVİTE KAYITLARI: `vet_drug.create`, `vet_drug.update`, `herd_withdrawal.overridden` artı kaynak tipleri `vet_drug` ve `herd_withdrawal` — modül 0049'dan beri HİÇ aktivite kaydı yazmıyordu ve o boşluk buraya kadar zararsızdı, arınma FARKLIDIR ve fark İKİ YERDEDİR: (1) katalog satırı BİR SAYIYI TANIMLAR ve değiştirilebilir, 28 günlük et arınmasını 2 güne çeken bir düzenleme o andan sonraki bütün tedavileri sessizce serbest bırakır ve satırın kendi `updated_at`i yalnız 'değişti' der 'neydi' demez (eski ve yeni değer `details`ta), (2) kilidin GEREKÇEYLE geçilmesi bir KARARDIR ve gerekçe metni satırdaki `withdrawal_override_reason` sütununda dursa da O SATIR KİMİN YAZDIĞINI TAŞIMAZ — `milk_yields`te ve `animal_movements`ta KULLANICI SÜTUNU YOKTUR (0049; ölçüldü, varsayılmadı), yani 'bu sütü kim tanka yazdırdı' sorusunun cevabı YALNIZ buradan çıkar; kayıt YALNIZ uyarı VE gerekçe birlikte varken yazılıyor (uyarı yoksa geçilecek bir şey yoktu, gerekçe yoksa `warn` politikasıdır ve karar kullanıcının DEĞİL firmanın ayarınındır, o ayar zaten `company-settings` üzerinden denetlenebilir); KAPSAM DIŞI ADIYLA: STOK HAREKETİ YOK — kalemin `product_id`si YALNIZ katalog çözümü içindir, depodan ilaç DÜŞMEZ ve modülün 0049'dan beri geçerli sınırı ('V1 stok/muhasebeye fiş yazmaz') AYNEN korundu, outbox olayı da YOK; TESTLER: `backend/tests/test_e2_tedavi_arinma.py` (12 test — 11 statik kapı + gerçek şemalı 17 adımlı davranış smoke'u) ve PG ikizi `backend/test_e2_tedavi_arinma_postgresql.py` (16 test) GERÇEK PostgreSQL 16'da TAZE ŞEMADA KOŞULDU, SIFIR ATLAMA; PG koşumunda HEM `DATABASE_URL` HEM `APP_TEST_DATABASE_URL` verildi artı `REQUIRE_PG=1` (yalnız birini vermek uygulamayı SESSİZCE SQLite'a düşürüyor — #47'nin kaydında ölçülmüş tuzak) ve sürücü `postgresql+psycopg`; SQLite dosyasının başlığında MUTASYON TABLOSU var ve her mutasyon HANGİ adımı kırmızı yaptığını ADIYLA söylüyor (alanları birleştirmek -> EN UZUN KAZANIR; tür eşleşmesini atlamak -> TÜRE ÖZEL SATIR; `<` yerine `<=` -> SINIR GÜNÜ; `block`u `warn` gibi davrandırmak -> POLİTİKA block; iç sorgudan `h.company_id=:cid` yüklemini düşürmek -> ÇAPRAZ KİRACI; `catalogue_*` sütunlarını yazmamak -> KÖKEN; sürü yolunu atlamak -> SÜRÜ YOLU); PG İKİZİNİN ÖLÇTÜKLERİ (beşi de yalnız üretim diyalektinde görünür): BEŞ bileşik yabancı anahtarın GERÇEKTEN ısırması (SQLite'ta `PRAGMA foreign_keys` temiz bir şemada 0 döner, yani bu göçün kiracı savunmasının TAMAMI orada YEŞİL kalırdı), `ck_animal_treatments_hedef`in ikisi-birden ve hiçbiri hâllerini REDDETMESİ, `herd_withdrawal_policy` CHECK'inin 'allow'u ŞEMA seviyesinde reddetmesi (0072'de ölçülen kusur tam olarak orada görünür), `ck_vet_drugs_species` KAPALI KÜMESİNİN ısırması (tam-eşitlik çözümünün DAYANAĞI; ısırmasaydı dayanak ÇÜRÜK olurdu) ve `NUMERIC(14,4)` doz ölçeği; ikize `_acilisa_cek` fikstürü de eklendi (D2'nin kalıbı, İKİ UÇTAN — PG ikizleri CI'da aynı veritabanını paylaşıyor ve tek yönlü bir çare dosyayı iyi komşu yapar ama KENDİSİNİ korumaz); İĞNELER (hepsi kapının kendisine ÖLÇTÜRÜLDÜ, aritmetikle türetilmedi): göç başı `20260908_0073` -> `20260908_0074` (4 yer — `test_goc_zinciri`in iki iddiası, CLI çıktısı kapısı ve `test_e1b_plantback`in baş iddiası), `TENANT_TABLES` 110 -> 113, kiracı dışa aktarımı dosya sayısı 110 -> 113 (3 yer) ve katman sayısı 9'da SABİT, `herd.py` dinamik `text()` 19 -> 23 ve parmak izi `f4799533` -> `3bac666f`, `activity_log.py` parmak izi `0a19ef72` -> `27ebdf52` (çağrı sayısı 3'te SABİT), `tenancy.py` parmak izi `45d7b295` -> `54e1e958` (çağrı sayısı 4'te SABİT), küresel dinamik `text()` toplamı 262 -> 266, rota sözleşmesi 367/283 -> 374/287 ve parmak izi `4de38594` -> `411caebd`, GET envanteri 175 -> 179 ve parmak izi `ea3ee703` -> `19771066` (drift raporu ÖLÇÜLDÜ: `missing`/`stale`/`changed` ÜÇÜ DE BOŞ, artış YALNIZ ekleme), `EXPECTED_AUTHENTICATED` 357 -> 364, `EXPECTED_READ` 94'te SABİT, `EXPECTED_UNDENIABLE` 103 -> 107, `FARM_HERD_VIEW_OPERATIONS` 35 -> 39, ÇIPLAK read farkı 68'de SABİT, alt süreçte SQL taşıyan yüzey 114/195 -> 115/196, PG popülasyonu 106 -> 107 (3 yer: test ADI, iddia ve `ci.yml`in `BEKLENEN_PG_DOSYA_SAYISI`'ı), `core_query_inventory` bir satır (`2a91c7d8` -> `6f7b973d`) ve parmak izi (`6031430b` -> `5cb0ad7b`); `EXPECTED_READ`İN 94'TE SABİT KALMASI BİR TANIKTIR — yedi ucun HİÇBİRİ genel `read`e düşmüyor ve düşselerdi OKUMA yetkisi olan herkes arınma sürelerini DEĞİŞTİREBİLİRDİ; `auth.py`ye iki önek satırının (`/api/vet-drugs`, `/api/animal-treatments`) yazılma sebebi tam olarak bu ve ÖLÇÜLDÜ: satırlar silinince `required_permission` ikisini de `read` gösteriyor, ayrıca `/api/animal-treatments` `/api/animals` önekiyle EŞLEŞMİYOR ('animal' sonrası 's' değil '-' geliyor); `UNDENIABLE` +4 (+7 DEĞİL) çünkü DÖRT OKUMA ucu `herd.view`a çözülüyor (bugün sevk edilen altı rolün hepsinde var) ve ÜÇ YAZMA ucu (`herd.manage`/`herd.health`) girmiyor — girmemesi ayrımın TANIĞIDIR; `core_query_inventory` NEDEN KIMILDADI: `get_company_settings`in Core `select`ine `companies.c.herd_withdrawal_policy` sütunu girdi, YENİ bir sorgu değil var olanın PROJEKSİYONU, ve bu PR'ın öteki SQL'lerinin hepsi ham `text()`tir (o kapı yalnız Core `select/update/delete` çağrılarını sayar); KOŞUM: SQLite süiti `1067 passed, 0 failed` (`pytest tests --ignore=tests/fixtures`), PG ikizi taze PG16 şemasında `16 passed`, ön yüz `npm test -- --run` 89 dosya / 614 test GEÇTİ; SÖZLEŞME ZİNCİRİ: `export_openapi.py -o` -> `npm run types:gen` -> `capture_frontend_fixtures.py`, delta YALNIZ `frontend/src/api/types.gen.ts` (+440), `pos-api.json` DEĞİŞMEDİ, `backend/openapi.json` `.gitignore`da; `npx tsc -b` çıkış 0 (`npm ci` gerekti: bu worktree'de `node_modules` YOKTU) ve `tsc -b` KULLANILDI, `tsc --noEmit -p tsconfig.json` DEĞİL — ikincisi bu depoda SIFIR dosya denetler ve boş bir yeşildir; ÖLÇÜLMEDİ, ADIYLA: (1) ÜYELİK GEÇMİŞİ — yukarıda, kendi göçünü ister; (2) STOK HAREKETİ — tedavi depodan ilaç düşmüyor, modülün V1 sınırı korundu; (3) ÖN YÜZ EKRANI — yalnız `types.gen.ts` türedi, gösterim yazılmadı; (4) KATALOG CSV İÇE AKTARMA — 0065'in BKÜ karşılığı var ve `origin`/`origin_reference` sütunları o gün için HAZIR duruyor ama uç YOK; (5) TEDAVİ GÜNCELLEME/SİLME — yalnız oluşturma ve okuma var, bir tedavinin düzeltilmesi arınma sürelerini yeniden çözmeyi VE geçmiş sağım/hareket satırlarının uyarılarını yeniden değerlendirmeyi gerektirir, o KENDİ dilimidir; (6) EŞZAMANLILIK — aynı ürün+tür için iki eşzamanlı katalog yazımı; ayıran şey ŞEMADIR (`uq_vet_drugs_company_product_species`, PG ikizinde ölçüldü) ve uç 409 döner ama yarışın KENDİSİ ölçülmedi; (7) `transactions.py`/`workflow.py`/`products.py`/`warehouse_counts.py` — 1B-B/C uçuşta, bu PR hiçbirine DOKUNMADI.
