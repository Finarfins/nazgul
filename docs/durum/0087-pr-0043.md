#43 — E2E İKİNCİ KOŞUM DARALTILDI: `ci.yml` e2e işindeki `PAYMENT_ALLOCATION_ENGINE_ENABLED=true` çağrısı artık `--grep "@tahsis-motoru"` ile yalnız bayrağı OKUYAN iki teste iniyor; koşum 1 (`npx playwright test`) DOKUNULMADI; TABAN `77babbc` (`origin/develop`, ölçüldü `git rev-parse HEAD` işe başlamadan); ÖLÇÜM 2/47 (yerelde yeniden sayıldı, keşif devralınmadı): `npx playwright test e2e/touch-targets.spec.ts --project=chromium-utc --list` → `Total: 47 tests in 1 file`; aynı komut `--grep "@tahsis-motoru"` → iki satır: `six-screen measured mobile batch gate › mobile-390: seeded data and required actions survive responsive rendering @tahsis-motoru` ve `› desktop-1280: … @tahsis-motoru` (spec:2275, VIEWPORTS döngüsü); KAPALI MOTOR iki testi bayrak açıkken `test.skip` (`ALLOCATION_ENGINE_ENABLED`, satır 2663 ve 2690) ve grep'e GİRMEZ; diğer 43 başlık bayrak durumunda BAYT AYNI (yalnız bu iki test + tohum `createBatchThreeSeedUret` `if (ALLOCATION_ENGINE_ENABLED)` okur, ölçüldü `grep ALLOCATION_ENGINE_ENABLED frontend/e2e/touch-targets.spec.ts`); KAPSAM KAPISI daraltılmış koşumu meşru sayar — `e2e/rota-kapsam-raportoru.ts:276-287` (alıntı, koşuldu): `ROTA KAPSAM SÖZLEŞMESİ KURULMADI — daraltılmış koşum. Koşan spec dosyası: 1, koşmayan: 20. Sözleşme yalnız kapsam kapısını (e2e/rota-render-kapisi.spec.ts) içeren koşumlarda ölçer.`; `ci.yml` TEK HUNK (`git diff origin/develop -- .github/workflows/ci.yml | grep -c '^@@'` = 1): `@@ -1030,7 +1030,7 @@` ikinci çağrıya `--grep "@tahsis-motoru"`; birinci çağrı `npx playwright test` AYNEN; `deploy/ci-required-contexts.json` DOKUNULMADI (`git diff origin/develop -- deploy/ci-required-contexts.json` = 0 satır) ve 17 bağlam (`python deploy/ci-gerekli-baglam-kapisi.py` → `K8 CI gerekli baglam kapisi YESIL: 17 baglam manifest ile tam kume esitliginde`, çıkış 0); `backend/tests/test_ci_gates.py` 16/16 (2.62s); `npx tsc -b --force` çıkış 0; SIFIR-TEST TUZAĞI (etiket her iki başlıktan silindi, GERİ YÜKLENDİ): `npx playwright test e2e/touch-targets.spec.ts --project=chromium-utc --grep "@tahsis-motoru" --list` → çıkış 1, `Error: No tests found.` / `Total: 0 tests in 0 files` (`--pass-with-no-tests` YOK); BAYRAK KAPALI + etiketli koşu ÖLÇÜLDÜ, varsayılmadı: `env -u PAYMENT_ALLOCATION_ENGINE_ENABLED npx playwright test e2e/touch-targets.spec.ts --project=chromium-utc --grep "@tahsis-motoru"` → `2 passed (23.2s)`, `CONFIG_STATE PaymentAllocations history: engine=off -> disabled state, surfaces=0` — if/else kapalı yüzeyi iddia ettiği için YEŞİL KALIR; açık yüzey iddiasını zorlayınca (`if (!ALLOCATION_ENGINE_ENABLED)` → `if (false && !ALLOCATION_ENGINE_ENABLED)`, GERİ YÜKLENDİ) AYNI komut ÇIKIŞ 1: `1 failed` / `1 did not run` (serial describe, birincisi düşünce ikincisi koşmaz), hata `seeded allocation must name the searched order` / `getByTestId('payment-allocations-history-data-surface')` / `Expected substring: "#2"` / `Error: element(s) not found` (15s); CI komutu (`PAYMENT_ALLOCATION_ENGINE_ENABLED=true`) + sunucu zorla kapalı (`serve.py` env=false, GERİ YÜKLENDİ) → tohum `409 {"detail":"Belge tahsisi için allocation motoru etkinleştirilmelidir"}` (`createBatchThreeSeedUret:1384`, çıkış 1); ÖNCE (koşu 33909794524, e2e işi 101143433793, adım 12 `Run npx playwright test e2e/touch-targets.spec.ts --project=chromium-utc`): 19:22:08Z → 19:27:15Z = 5m07s (kullanıcı ~5m15s demişti; logdaki startedAt/completedAt ÖLÇÜLDÜ); SONRA: CI ikinci adım süresi bu kaydın yazıldığı anda henüz yok, bir sonraki girdi/güncelleme koşu kimliğiyle yazacak; ÖLÇÜLMEDİ, ADIYLA: diğer spec dosyalarının bayrak açıkken davranışı (yalnız `touch-targets.spec.ts` `ALLOCATION_ENGINE_ENABLED` okur, `e2e/` altında başka geçiş YOK ama o dosyalar bayrakla KOŞTURULMADI); WebKit; Firefox (`playwright.config.ts` yalnız `chromium-utc` + `saat-dilimi-istanbul`).
