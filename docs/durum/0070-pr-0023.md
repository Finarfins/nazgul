#23 — BKÜ kataloğu artık firmanın KENDİ dosyasından doldurulabiliyor (`POST /api/plant-protection-products/import`, göç `20260902_0065`) ve katalog satırının KÖKENİ ilk kez kayda geçti; ÖLÇÜLEN KUSUR: göç 0063 (PR #18) kataloğu açtı ve PHI gün sayısını faaliyet dışında kalıcı bir kayda bağladı ama doldurma yolu TEK TEK FORMDU, dolayısıyla BKÜ listesi kalabalık olan bir firma için katalog gerçek bir veri girişi yükü ardında kalıyordu ve katalog boş kaldığı sürece PHI kilidi 0063 ÖNCESİNDEKİ gibi susmaya devam ediyordu — bu PR o yükü kaldırıyor, yeni bir PHI rakamı İDDİA ETMEDEN: 0063'ün duruşu aynen sürüyor, başlangıç listesi, paketlenmiş bakanlık verisi ve şablonda ÖRNEK GÜN SAYISI bile YOKTUR çünkü örnek bir gün sayısı kopyalanan bir gün sayısıdır ve depoya giren her değer yine o değerin sahibi firmanın etiketten okuyup girdiği değerdir; BİR BOZUK SATIR YALNIZ KENDİSİNİ REDDEDER, DOSYAYI DEĞİL: 200 satırlık listede bir yazım hatası olan çiftçi diğer 199'u kaybetmemeli, ama SESSİZ KISMİ BAŞARI da yok — yanıt (`{filename, total_rows, imported, rejected}`) reddedilen HER satırı kendi satır numarası ve gerekçesiyle veriyor ve `rejected` bir SAYI DEĞİL LİSTEDİR, çünkü "3 satır atlandı" kullanıcıya hangisini düzelteceğini SÖYLEMEZ ve 200 satırlık bir dosyada o cümle dosyayı baştan sona gözle taramak demektir; ekran da bu yüzden listenin TAMAMINI gösteriyor ve HİÇBİRİNİ kısaltmıyor (uygulamanın diğer içe aktarma diyaloğu 20'de kesip "+N hata daha" der; kısaltılan satır DÜZELTİLMEYEN satırdır) ve liste CSV olarak da indirilebiliyor çünkü otuz hatayı ekrandan not almak gerçekçi değil; yanıt biçimi deponun diğer içe aktarmalarındaki `inserted/updated/errors`'tan BİLEREK farklıdır çünkü bu uç hiçbir şeyi güncellemez ve her zaman sıfır olan bir `updated` alanı okuyanı "acaba ne güncellendi" diye düşündürürdü; ÇAKIŞMA REDDEDİLİR, GÜNCELLENMEZ ve bu deponun diğer içe aktarmalarından (`routers/imports.py`, müşteri/ürün: eşleşen satırı GÜNCELLER) bilinçli bir AYRILIŞTIR — katalogdaki değer bir insanın ETİKETE BAKARAK yazdığı yasal bir süredir ve bir dosya onu sessizce ezerse ezildiği KİMSEYE GÖRÜNMEZ, atlamak da yanlış olurdu çünkü kullanıcı listesinin tamamının yazıldığını sanırdı, dolayısıyla satır REDDEDİLİR ve çakıştığı kaydın KİMLİĞİ söylenir (kullanıcı ekrandan bakıp hangisinin doğru olduğuna kendisi karar verip yeniden yükler) ve kural yükleme BAŞLAMADAN ÖNCE ekranda yazar, sonradan söylenirse kullanıcı listesinin tamamının yazıldığını sanmış olarak devam ederdi; DOSYA İÇİ TEKRAR AYRI BİR GEREKÇE TAŞIR çünkü yalnız veritabanına bakmak yetmez: aynı yüklemede iki kez geçen (ürün, bitki) çifti, ilki yazıldıktan SONRA veritabanında görünür ve gerekçe "katalogda zaten var" olurdu, oysa kullanıcı satırı KENDİSİNİN iki kez yazdığını anlamalı, o yüzden ikinci satır dosyadaki ÖNCEKİ satır numarasıyla reddedilir ve bu karşılaştırma `casefold` ile PYTHON'da yapılır, SQL `LOWER()`ında değil — 0063'ün `_bitki_esit` gerekçesinin aynısı, Türkçe İ/ı eşlemesi diyalekte bağlıdır; KÖKEN KAYIT ALTINDA: 0063 "operatör katalogdaki 21 günü 7 yaptı" olayını denetimde GÖRÜNÜR kılmıştı ama aynı soru bir adım GERİYE de sorulur ve 0063 onu cevaplayamıyordu — "katalogdaki 21 NEREDEN GELDİ" — bu yüzden göç iki sütun açıyor, `origin` (`MANUAL` | `IMPORT`) NASIL geldiğini ve `origin_reference` (`<dosya adı>:<satır no>`) NEREDEN geldiğini söyler, iki ayrı sütundur çünkü tek sütunda birleştirmek BOŞ değeri iki şeyi birden söyler hâle getirirdi ("elle girildi" ve "içe aktarıldı ama dosya adı kaydedilmedi") ve bu 0048 ile 0063'ün kaçındığı hatanın aynısı olurdu, ayrıca depo dosyanın KENDİSİNİ SAKLAMAZ (0063'ün duruşu gereği kaynak firmanın elindedir), sakladığı yalnızca firmanın kendi arşivinde satırı bulmasına yetecek İŞARETTİR; `server_default='MANUAL'` BİR GERİYE DOLDURMA DEĞİLDİR ve 0063'ün geriye doldurmayı reddeden kuralını çiğnemez, çünkü uydurma değil ÖLÇÜLEBİLİR bir olgudur: bu göçten önce katalog satırı yaratmanın TEK yolu `create_ppp` ucuydu, yani mevcut her satır GERÇEKTEN elle yazılmıştır ve varsayılan geçmişe bir iddia eklemez, geçmişte zaten doğru olanı yazıya geçirir; DÜZENLEME KÖKENİ DEĞİŞTİRMEZ (`update_ppp`in `SET` listesinde bu iki sütun YOKTUR ve bu bilinçlidir): köken satırın NEREDEN GELDİĞİDİR, bir insanın sonradan değeri düzeltmesi onu "elle girilmiş" YAPMAZ ve tersi tercih denetçiden bilgi SAKLARDI — satırın bir listeden geldiği gerçeği İLK DÜZELTMEDE silinirdi; UÇ `farm.py`DEDİR, `/api/imports` ALTINDA DEĞİLDİR ve bu bu PR'ın en önemli kararıdır: okuyucu (`_read_tabular_upload`) PAYLAŞILIYOR çünkü 10 MB gövde tavanı, 50.000 satır tavanı, UTF-8/cp1254 çözümü ve zip-bomba kontrolü ikinci kez yazılsaydı o tavanlardan biri unutulurdu ve unutulan bir tavan YALNIZ SALDIRI ANINDA görünür, ama YETKİ PAYLAŞILMIYOR: izin yoldan gelir ve `/api/plant-protection-products` `_FARM_PATH_PREFIXES` içindedir (güvenli olmayan yöntemler `farm.manage`) oysa `/api/imports` o listede DEĞİLDİR, dolayısıyla uç `imports` yönlendiricisine konsaydı yasal bekleme sürelerini `stock` yetkisi olan HERKES — yani depo/stok rolü — yazabilirdi; bu VARSAYILMADI, ÖLÇÜLDÜ: `required_permission("POST", "/api/plant-protection-products/import")` -> `farm.manage` ve karşılaştırma için `required_permission("POST", "/api/imports/products/excel")` -> `stock`; SAVEPOINT ZORUNLU VE FARKI YALNIZ ÜRETİMDE GÖRÜNÜR: PostgreSQL'de başarısız bir deyimden sonra işlem ABORTED duruma düşer ve aynı işlemdeki SONRAKİ HER sorgu kusursuz olsa bile hata verir, SQLite'ta böyle bir hâl YOKTUR, yani `db.begin_nested()` kaldırılsaydı SQLite koşusu YEŞİL KALIR ve üretim diyalektinde tek bozuk satır "dosya düşmez" kuralını SESSİZCE bozup dosyanın kalanını da yutardı — bu yüzden PostgreSQL ikizi (`test_farm_bku_ice_aktarma_postgresql.py`) ZORUNLUDUR ve bu turda CI'ın `backend-postgresql` shard'larında KOŞTU (atlanmadı) ve GEÇTİ, dolayısıyla savepoint yalıtımı, yarış hâlindeki tekillik ihlali, `crop` boş dize yedeği ve `LOWER()` ürün eşlemesi artık AKIL YÜRÜTME değil ÖLÇÜMDÜR; AŞAĞI YUVARLAMA YOK: `int(float(...))` kısa yolu `21.0`'ı 21 yapardı ama AYNI yol `20,6`'yı da sessizce 20 yapardı ve aşağı yuvarlanmış bir gün süresi dolmadan hasada izin verir, sıfır olmayan kesir REDDEDİLİR — ilk sürümde bir `float` dalı vardı ve onu `test_v2_9_decimal_contract` kapısı CI'da yakaladı (`app/routers/farm.py:2360: float`), dal KALDIRILDI ve kaldırırken ölçüldü ki dal zaten GEREKSİZDİ (metin yolu `21.0`'ı zaten doğru çözüyor, `20,6`'yı zaten reddediyordu) ve birim testinin iddiası dalın VARLIĞINA değil DAVRANIŞA bağlı olduğu için test değişmeden geçti; GÖÇ 0064 OLARAK YAZILDI 0065 OLARAK YENİDEN NUMARALANDI ve bu ci.yml'nin başındaki #56 + #60 notunun tarif ettiği olayın AYNISIDIR: bu dal PR #18'in başı üzerinde açılmıştı ve oradaki en büyük sıra 0063 olduğu için 0064 doğru seçimdi, ancak develop'a birleşirken PR #19'un (`tarla_kilitleri`) AYNI numarayı AYNI atadan (`20260901_0063`) türeterek aldığı görüldü ve BUNU GIT YAKALAYAMAZDI çünkü iki dal da `test_goc_zinciri.py`deki baş pinini AYNI METNE ("20260901_0064") çevirmişti, dolayısıyla o dosya ÇAKIŞMASIZ birleşti ve çakışma yalnızca alembic'in "Revision 20260901_0064 is present more than once" uyarısında ve zincir kapısının ÇOKLU BAŞ hatasında göründü; göç `20260902_0065`'e alındı ve develop'un 0064'ünün ARDINA zincirlendi, gövdedeki 0063 atıfları DEĞİŞMEDİ ve doğrudur çünkü katalog tablosunu açan göç hâlâ 0063'tür ve bu göç ona yalnız sütun ekler; TABANI DEĞİŞEN ÖLÇÜM ÖLÇÜM DEĞİLDİR: bu dal önce `claude/tarla-bku-katalogu` üzerinde ölçülmüştü, #18 `cfe7362` ve #19 `d156d13` olarak develop'a indikten sonra donuk sabitlerin HEPSİ develop'un GÜNCEL değerinden başlanarak YENİDEN ÖLÇÜLDÜ, eski sayıların üzerine aritmetik yapılarak DEĞİL (önceki turda ölçülen `pg_population` 95 -> 96 tam olarak bu yüzden ATILDI; develop'un tabanı 97'dir): `route_security` 344 -> 345 operasyon ve 265 -> 266 yol ve parmak izi, `authorization_population` kimliklenmiş 334 -> 335, `tenant_scoping_guard` parmak izi `d543d853` -> `8635fc6d`, `goc_zinciri` baş `20260901_0064` -> `20260902_0065`, `pg_population` 97 -> 98 (`ci.yml` ve `test_ci_gates.py` aynı sayıyı tutar ve birbirini doğrular); DEĞİŞMEYEN ÜÇ SAYI AYRICA VE AÇIKÇA DOĞRULANDI çünkü bir "geçsin diye" düzenlemeyi yakalayacak olan onlardır: `EXPECTED_UNDENIABLE` 99'da SABİT ve `farm_herd` 32'de SABİT (uç yalnızca POST, okuma yüzeyi büyümedi), `GET_INVENTORY_COUNT` 164'te SABİT (yeni GET yok) ve `tenant_scoping_guard`ın `farm.py` dinamik `text()` sayısı 15'te SABİT ile küresel toplam 253'te SABİT (içe aktarma yolunun SQL'lerinin TAMAMI sabit metindir ve dosyadan gelen hiçbir değer SQL metnine girmez, başlıklar SQL'e hiç ulaşmaz çünkü `_map` onları yalnızca SÜTUN İNDEKSİNE çevirir) — bu üçünden biri kımıldasaydı niyet edilmemiş bir yüzey eklenmiş demek olurdu ve yeniden pinlenmeyip DURULUP RAPOR EDİLMESİ gerekirdi; `browserslist` BUMPI DÜŞÜRÜLDÜ: bu dal 4.28.6 -> 4.28.8 bumpını iki YENİ GHSA uyarısı (`GHSA-C83G-RGW3-J3CX`, `GHSA-73WF-GQ98-2V4G`) frontend işini kırdığı için taşıyordu ve tetikleyicinin bu dal olmadığı ölçülmüştü (aynı taban 68 dakika önce yeşildi), ancak develop aynı yamayı kendisi getirdiği için commit'in içeriği birleşmede DÜŞÜRÜLDÜ ve kilitli dosya artık develop ile BİREBİR AYNIDIR (`git diff origin/develop -- frontend/package-lock.json` 0 satır); ÖLÇÜLEN KOŞULAR: CI'ın KENDİ izolasyon koşucusuyla (`run_isolated_tests.py`, her dosya kendi sürecinde) 370 aktif test dosyasının TAMAMI geçti ve 1797 test sonucu kaydedildi, 0 başarısız — bu, önceki turda kendi raporumda "ÖLÇÜLMEDİ" diye adlandırdığım boşluğun KAPATILMASIDIR çünkü o turda yalnız `backend/tests/` koşulmuştu ve hem `harman_vadeli` hem `float` kapısı tam olarak oradan kaçmıştı; frontend 89 dosya / 593 test tamamı geçti, `npx tsc -b` çıkış 0, `npm run lint` 0 hata, `npm run typecheck:e2e` çıkış 0, `npm run build:bridge` çıkış 0, `audit:policy` YEŞİL (tek kabul kaydı `GHSA-W5HQ-G745-H8PQ`), sözleşme yeniden üretimi sonrası `git diff --exit-code` çıkış 0, zincir `TEK BAŞ VAR` / `HEPSİ ERİŞİLEBİLİR` / `SAYIM TUTUYOR` çıkış 0 ve 66/66; TESTLER MUTASYONLA ÖLÇÜLDÜ ve dekoratif DEĞİLDİR: çakışan satırı sessizce ATLAYAN, çakışanı GÜNCELLEYEN ve kökeni KAYDETMEYEN üç ayrı sürümün ÜÇÜ DE backend testini düşürdü, ret listesini 20'de KESEN sürüm frontend testini düşürdü, ayrıca çakışma testi yalnız "reddedildi"yi değil ESKİ DEĞERİN YERİNDE DURDUĞUNU iddia eder çünkü reddin asıl anlamı budur ve yalnız yanıt gövdesine bakan bir test sessizce üstüne yazan bir sürümü yakalayamazdı; `backend/test_v3_harman_vadeli.py` bu dalda DEĞİŞTİRİLMEDİ (develop'a göre 0 satır fark) ve o dosya PR #21'e aittir — tarih sınırına bağlı o kusuru bu dal da bağımsız olarak bulmuştu ve kanıtı (aynı taban 68 dakika önce yeşil, İstanbul gece yarısının diğer tarafında) kayda geçti, ama düzeltmesi başka bir dilime aittir; ÖLÇÜLMEDİ: Android/Capacitor mobil derlemesi bu dalda hiç koşulmadı ve `browserslist` bumpı düşürüldüğü için artık ilgisizdir de.
