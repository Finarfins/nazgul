#141 — H50: çek/senet riskli geçişleri (ciro_edildi/karsiliksiz/iade) ekranda yalnız admin/yonetici/muhasebe rolüne gösterilir — backend H48 `MUHASEBE_HEDEFLERI`/`MUHASEBE_ROLLERI` aynası `cekSenet.ts::rolHedefeGidebilirMi`, satis için GİZLEME (devre dışı değil), `can(...)` kapısı ve 403 `CEK_DURUM_ROL_YETKISIZ` mesaj yolu değişmedi; yalnız frontend, rota envanteri 80 ve navigasyon pinleri blob-özdeş; lint 0 hata, build ok, vitest 106 dosya / 819 test yeşil — <merge tarihi>
