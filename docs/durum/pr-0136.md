#136 — H23 kiracı geri yüklemede TEK KİP: gerçek akışta erişilemeyen ve tek çağıranı `trg_activity_logs_no_delete` tetikleyicisini düşüren bir test simülasyonu olan `yerine` kipi uçtan uca kaldırıldı (`KIPLER` -> `KIP="yeni"`, `kip` parametresi `geri_yukle`/`_firmayi_yaz`/`_tabloyu_yaz`/`_uyelikleri_yaz`dan kalktı, `_kaynak_firma_durumu`/`_artik_satirlar`/`_sirayi_ilerlet` ve üç `RESTORE_SOURCE_*` dalı silindi, göç YOK); `mode` form alanı KALDI ve `yeni` dışı her değer `422 "mode yalnız 'yeni' olabilir"` alır; BİLİNÇLİ çivilenen davranış (Şef kararı): aktif B'nin kimliğini taşıyan zip 409 DEĞİL 200 alır ve YENİ firma açar, B'nin satırları/bayrağı/adı AYNI kalır (zip kimliği kaynak, hedef değil); `app/` altında `RESTORE_SOURCE_` kalmadığı durağan pinle çivilendi; #127 SONRASI `8675dbe` tabanına rebase edildi ve Core envanteri tarayıcıyla YENİDEN ölçüldü: 230 -> 226 (select 155 -> 152, update 63 -> 62, dört SİLME), `UNRESOLVED_ALLOWLIST` 8 -> 6, parmak izi 1c166663 -> c39e5056 (önceki `989b735` ölçümü 223 -> 219 / 67e10ae0 geçersiz); öteki pinler kıpırdamadı (rota 420/326, GET 200, AUTHENTICATED 407 = 87/31/97, TENANT_TABLES 123, pg_twins 132, dinamik-SQL 270, Core tenant guard 183, baş `20260915_0087`); `backend/app`+`backend/alembic` patch-id rebase öncesi ve sonrası aynı `a92c510b`; kanıt: tam izole süit 469/469 dosya 3516 test, PG ikizi taze `lensh23_test` 5/5 ve 0 skip, kapılar 118/118; önceki turda yalnız 4 işçili süpürmede düşen `backend/test_platform_backup_security.py` bu turda düşmedi; ilk turdaki PG kırmızısı tüm firmaları sayan assertion kusuruydu, ayrı commit'te düzeltildi — <merge tarihi>
