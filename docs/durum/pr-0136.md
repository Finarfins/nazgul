#136 — H23 kiracı geri yüklemede TEK KİP: gerçek akışta erişilemeyen ve tek çağıranı `trg_activity_logs_no_delete` tetikleyicisini düşüren bir test simülasyonu olan `yerine` kipi uçtan uca kaldırıldı (`KIPLER` -> `KIP="yeni"`, `kip` parametresi `geri_yukle`/`_firmayi_yaz`/`_tabloyu_yaz`/`_uyelikleri_yaz`dan kalktı, `_kaynak_firma_durumu`/`_artik_satirlar`/`_sirayi_ilerlet` ve üç `RESTORE_SOURCE_*` dalı silindi, göç YOK, baş `20260914_0086`); `mode` form alanı KALDI ve `yeni` dışı her değer `422 "mode yalnız 'yeni' olabilir"` alır, rota 419/325 aynı, `types.gen.ts` yalnız endpoint açıklaması; ölçülen ve BİLİNÇLİ çivilenen davranış: aktif B'nin kimliğini taşıyan zip 409 DEĞİL 200 alır ve YENİ firma açar, B'nin satırları/bayrağı/adı AYNI kalır (zip kimliği kaynak, hedef değil); `app/` altında `RESTORE_SOURCE_` kalmadığı durağan pinle çivilendi; Core envanteri 223 -> 219 (dört SİLME: 3 select + 1 update, `UNRESOLVED_ALLOWLIST` -2, parmak izi 72b2110c -> 67e10ae0), öteki pinler kıpırdamadı; kanıt: izole koşucu 4/4 dosya 69 test, PG ikizi taze `lensh23_test` 4/4 (ilk koşudaki kırmızı tüm firmaları sayan assertion kusuruydu, ayrı commit'te düzeltildi), tam süit 466/467 — tek kırmızı `backend/test_platform_backup_security.py` tek başına dalda ve taban `989b735`te yeşil; birleşme sırası #127 önce, H23 sonra, Core envanteri rebase sonrası YENİDEN ölçülecek — <merge tarihi>
