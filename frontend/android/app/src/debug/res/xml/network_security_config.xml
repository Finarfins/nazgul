<?xml version="1.0" encoding="utf-8"?>
<!--
  YALNIZ DEBUG DERLEMESİ. `src/debug/` altında olduğu için release paketine
  ASLA girmez; yayınlanan uygulamada açık metin trafiği kapalı kalır.

  Neden gerekli: Android 9'dan beri açık metin (http://) trafiği varsayılan
  olarak engelli. Geliştirirken uygulamayı emülatörden ana makinedeki yerel
  backend'e (http://10.0.2.2:5050) bağlamak istiyoruz; onsuz her istek
  "Failed to fetch" ile düşüyor.

  10.0.2.2 emülatörün ana makineyi gördüğü sabit adrestir. Liste bilerek dar
  tutuldu: `cleartextTrafficPermitted="true"` genel olarak açılmadı, yalnız
  bu iki ad için izin verildi.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
