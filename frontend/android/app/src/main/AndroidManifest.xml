<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- KAMERA — @capacitor/camera bunları KENDİ manifestinde bildirmiyor,
         uygulamanın eklemesi gerekiyor. Ölçüldü: eklenmeden önce birleşmiş
         manifeste yalnız INTERNET giriyordu ve derleme yine de başarılı
         oluyordu; hata ancak cihazda, teknisyen fotoğraf çekmeye çalıştığında
         çıkardı. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 13+ galeri okuma izni; imza/fotoğraf seçme yolu için. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- required="false": kamerasız bir tablet uygulamayı yine de KURABİLSİN.
         true olsaydı Play Store o cihazları uyumsuz sayıp gizlerdi; hedef hem
         telefon hem tablet. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
